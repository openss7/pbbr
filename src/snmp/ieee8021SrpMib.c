/*****************************************************************************

 @(#) src/snmp/ieee8021srpmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021srpmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021SrpMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021SrpMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021SrpMib_refresh = 1;
volatile int ieee8021SrpBridgeBaseTable_refresh = 1;
volatile int ieee8021SrpBridgePortTable_refresh = 1;
volatile int ieee8021SrpLatencyTable_refresh = 1;
volatile int ieee8021SrpStreamTable_refresh = 1;
volatile int ieee8021SrpReservationsTable_refresh = 1;

/*
 * ieee8021SrpMib_variables_oid: object identifier for ieee8021SrpMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021SrpMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 19 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021SrpCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 19, 2, 1, 1 };
oid ieee8021SrpConfigurationGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 19, 2, 2, 1 };
oid ieee8021SrpLatencyGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 19, 2, 2, 2 };
oid ieee8021SrpStreamsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 19, 2, 2, 3 };
oid ieee8021SrpReservationsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 19, 2, 2, 4 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021SrpMib_variables: tree for ieee8021SrpMib
 * This variable defines function callbacks and type return information for the ieee8021SrpMib mib section
 */
struct variable7 ieee8021SrpMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021SRPBRIDGEBASEMSRPENABLEDSTATUS  1
	{(u_char) IEEE8021SRPBRIDGEBASEMSRPENABLEDSTATUS, ASN_INTEGER, RWRITE, var_ieee8021SrpBridgeBaseTable, 5, {1, 1, 1, 1, 1}},
#define   IEEE8021SRPBRIDGEBASEMSRPTALKERPRUNING  2
	{(u_char) IEEE8021SRPBRIDGEBASEMSRPTALKERPRUNING, ASN_INTEGER, RWRITE, var_ieee8021SrpBridgeBaseTable, 5, {1, 1, 1, 1, 2}},
#define   IEEE8021SRPBRIDGEBASEMSRPMAXFANINPORTS  3
	{(u_char) IEEE8021SRPBRIDGEBASEMSRPMAXFANINPORTS, ASN_UNSIGNED, RWRITE, var_ieee8021SrpBridgeBaseTable, 5, {1, 1, 1, 1, 3}},
#define   IEEE8021SRPBRIDGEBASEMSRPLATENCYMAXFRAMESIZE  4
	{(u_char) IEEE8021SRPBRIDGEBASEMSRPLATENCYMAXFRAMESIZE, ASN_UNSIGNED, RWRITE, var_ieee8021SrpBridgeBaseTable, 5, {1, 1, 1, 1, 4}},
#define   IEEE8021SRPBRIDGEPORTMSRPENABLEDSTATUS  5
	{(u_char) IEEE8021SRPBRIDGEPORTMSRPENABLEDSTATUS, ASN_INTEGER, RWRITE, var_ieee8021SrpBridgePortTable, 5, {1, 1, 2, 1, 1}},
#define   IEEE8021SRPBRIDGEPORTMSRPFAILEDREGISTRATIONS  6
	{(u_char) IEEE8021SRPBRIDGEPORTMSRPFAILEDREGISTRATIONS, ASN_COUNTER64, RONLY, var_ieee8021SrpBridgePortTable, 5, {1, 1, 2, 1, 2}},
#define   IEEE8021SRPBRIDGEPORTMSRPLASTPDUORIGIN  7
	{(u_char) IEEE8021SRPBRIDGEPORTMSRPLASTPDUORIGIN, ASN_OCTET_STR, RONLY, var_ieee8021SrpBridgePortTable, 5, {1, 1, 2, 1, 3}},
#define   IEEE8021SRPBRIDGEPORTSRPVID  8
	{(u_char) IEEE8021SRPBRIDGEPORTSRPVID, ASN_UNSIGNED, RWRITE, var_ieee8021SrpBridgePortTable, 5, {1, 1, 2, 1, 4}},
#define   IEEE8021SRPPORTTCLATENCY  9
	{(u_char) IEEE8021SRPPORTTCLATENCY, ASN_UNSIGNED, RONLY, var_ieee8021SrpLatencyTable, 5, {1, 2, 1, 1, 2}},
#define   IEEE8021SRPSTREAMDESTINATIONADDRESS  10
	{(u_char) IEEE8021SRPSTREAMDESTINATIONADDRESS, ASN_OCTET_STR, RONLY, var_ieee8021SrpStreamTable, 5, {1, 3, 1, 1, 2}},
#define   IEEE8021SRPSTREAMVLANID  11
	{(u_char) IEEE8021SRPSTREAMVLANID, ASN_UNSIGNED, RONLY, var_ieee8021SrpStreamTable, 5, {1, 3, 1, 1, 3}},
#define   IEEE8021SRPSTREAMTSPECMAXFRAMESIZE  12
	{(u_char) IEEE8021SRPSTREAMTSPECMAXFRAMESIZE, ASN_UNSIGNED, RONLY, var_ieee8021SrpStreamTable, 5, {1, 3, 1, 1, 4}},
#define   IEEE8021SRPSTREAMTSPECMAXINTERVALFRAMES  13
	{(u_char) IEEE8021SRPSTREAMTSPECMAXINTERVALFRAMES, ASN_UNSIGNED, RONLY, var_ieee8021SrpStreamTable, 5, {1, 3, 1, 1, 5}},
#define   IEEE8021SRPSTREAMDATAFRAMEPRIORITY  14
	{(u_char) IEEE8021SRPSTREAMDATAFRAMEPRIORITY, ASN_INTEGER, RONLY, var_ieee8021SrpStreamTable, 5, {1, 3, 1, 1, 6}},
#define   IEEE8021SRPSTREAMRANK  15
	{(u_char) IEEE8021SRPSTREAMRANK, ASN_INTEGER, RONLY, var_ieee8021SrpStreamTable, 5, {1, 3, 1, 1, 7}},
#define   IEEE8021SRPRESERVATIONDECLARATIONTYPE  16
	{(u_char) IEEE8021SRPRESERVATIONDECLARATIONTYPE, ASN_INTEGER, RONLY, var_ieee8021SrpReservationsTable, 5, {1, 4, 1, 1, 3}},
#define   IEEE8021SRPRESERVATIONACCUMULATEDLATENCY  17
	{(u_char) IEEE8021SRPRESERVATIONACCUMULATEDLATENCY, ASN_UNSIGNED, RONLY, var_ieee8021SrpReservationsTable, 5, {1, 4, 1, 1, 4}},
#define   IEEE8021SRPRESERVATIONFAILUREBRIDGEID  18
	{(u_char) IEEE8021SRPRESERVATIONFAILUREBRIDGEID, ASN_OCTET_STR, RONLY, var_ieee8021SrpReservationsTable, 5, {1, 4, 1, 1, 5}},
#define   IEEE8021SRPRESERVATIONFAILURECODE  19
	{(u_char) IEEE8021SRPRESERVATIONFAILURECODE, ASN_INTEGER, RONLY, var_ieee8021SrpReservationsTable, 5, {1, 4, 1, 1, 6}},
#define   IEEE8021SRPRESERVATIONDROPPEDSTREAMFRAMES  20
	{(u_char) IEEE8021SRPRESERVATIONDROPPEDSTREAMFRAMES, ASN_COUNTER64, RONLY, var_ieee8021SrpReservationsTable, 5, {1, 4, 1, 1, 7}},
#define   IEEE8021SRPRESERVATIONSTREAMAGE  21
	{(u_char) IEEE8021SRPRESERVATIONSTREAMAGE, ASN_UNSIGNED, RONLY, var_ieee8021SrpReservationsTable, 5, {1, 4, 1, 1, 8}},
};

/* (L = length of the oidsuffix) */
struct ieee8021SrpMib_data *ieee8021SrpMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021SrpBridgeBaseTableStorage = NULL;
struct header_complex_index *ieee8021SrpBridgePortTableStorage = NULL;
struct header_complex_index *ieee8021SrpLatencyTableStorage = NULL;
struct header_complex_index *ieee8021SrpStreamTableStorage = NULL;
struct header_complex_index *ieee8021SrpReservationsTableStorage = NULL;

void (*ieee8021SrpMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021SrpMib_loop_handler(int);
void ieee8021SrpMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021SrpMib(void)
 * @brief ieee8021SrpMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021SrpMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021SrpMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021SrpMib", "init_ieee8021SrpMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021SrpMib", ieee8021SrpMib_variables, variable7, ieee8021SrpMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021SrpMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021SrpMib", parse_ieee8021SrpMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SrpBridgeBaseTable", parse_ieee8021SrpBridgeBaseTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SrpBridgePortTable", parse_ieee8021SrpBridgePortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SrpLatencyTable", parse_ieee8021SrpLatencyTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SrpStreamTable", parse_ieee8021SrpStreamTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SrpReservationsTable", parse_ieee8021SrpReservationsTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SrpMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SrpBridgeBaseTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SrpBridgePortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SrpLatencyTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SrpStreamTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SrpReservationsTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021SrpMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021SrpMib_fd_handler, (void *) 1);
	}
	ieee8021SrpMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021SrpMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021SrpMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021SrpMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021SrpMib(void)
{
	DEBUGMSGTL(("ieee8021SrpMib", "deinit_ieee8021SrpMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021SrpMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021SrpMib_variables_oid, sizeof(ieee8021SrpMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021SrpMib");
	snmpd_unregister_config_handler("ieee8021SrpBridgeBaseTable");
	snmpd_unregister_config_handler("ieee8021SrpBridgePortTable");
	snmpd_unregister_config_handler("ieee8021SrpLatencyTable");
	snmpd_unregister_config_handler("ieee8021SrpStreamTable");
	snmpd_unregister_config_handler("ieee8021SrpReservationsTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

int
term_ieee8021SrpMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021SrpMib", "term_ieee8021SrpMib: terminating...  "));
	deinit_ieee8021SrpMib();
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021SrpMib_data *ieee8021SrpMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021SrpMib.
 *
 * Creates a new ieee8021SrpMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021SrpMib.
 */
struct ieee8021SrpMib_data *
ieee8021SrpMib_create(void)
{
	struct ieee8021SrpMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpMib_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SrpMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SrpMib_data *ieee8021SrpMib_duplicate(struct ieee8021SrpMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021SrpMib_data *
ieee8021SrpMib_duplicate(struct ieee8021SrpMib_data *thedata)
{
	struct ieee8021SrpMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpMib_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SrpMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SrpMib_destroy(struct ieee8021SrpMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021SrpMib.
 * @brief delete a scalars structure from ieee8021SrpMib.
 *
 * Frees scalars that were previously removed from ieee8021SrpMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SrpMib_destroy(struct ieee8021SrpMib_data **thedata)
{
	struct ieee8021SrpMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpMib_add(struct ieee8021SrpMib_data *thedata)
 * @param thedata the structure representing ieee8021SrpMib scalars.
 * @brief adds node to the ieee8021SrpMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021SrpMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021SrpMib_add(struct ieee8021SrpMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpMib_add: adding data...  "));
	if (thedata)
		ieee8021SrpMibStorage = thedata;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SrpMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SrpMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SrpMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021SrpMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SrpMib_data *StorageTmp = ieee8021SrpMib_create();

	DEBUGMSGTL(("ieee8021SrpMib", "parse_ieee8021SrpMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021SrpMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/*
 * store_ieee8021SrpMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SrpMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SrpMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021SrpMib", "store_ieee8021SrpMib: storing data...  "));
	refresh_ieee8021SrpMib(1);
	if ((StorageTmp = ieee8021SrpMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021SrpMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021SrpMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021SrpMib(struct ieee8021SrpMib_data *StorageTmp, struct ieee8021SrpMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021SrpMib(struct ieee8021SrpMib_data *StorageTmp, struct ieee8021SrpMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SrpMib(struct ieee8021SrpMib_data *StorageTmp, struct ieee8021SrpMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021SrpMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SrpMib(struct ieee8021SrpMib_data *StorageTmp, struct ieee8021SrpMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SrpMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021SrpMib(struct 
 * @fn void revert_ieee8021SrpMib(struct ieee8021SrpMib_data *StorageTmp, struct ieee8021SrpMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021SrpMib(struct ieee8021SrpMib_data *StorageTmp, struct ieee8021SrpMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SrpMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SrpMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021SrpMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021SrpMib(int force)
{
	if (ieee8021SrpMibStorage == NULL) {
		struct ieee8021SrpMib_data *StorageNew;

		if ((StorageNew = ieee8021SrpMib_create()) == NULL)
			return;
		ieee8021SrpMibStorage = StorageNew;
		ieee8021SrpMib_refresh = 1;
	}
	if (!force && ieee8021SrpMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021SrpMib", "refresh_ieee8021SrpMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021SrpMib_refresh = 0;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021SrpMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021SrpMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021SrpMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SrpMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SrpMib", "var_ieee8021SrpMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021SrpMib(0);
	if ((StorageTmp = ieee8021SrpMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021SrpMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021SrpMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021SrpMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021SrpBridgeBaseTable_data *ieee8021SrpBridgeBaseTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SrpBridgeBaseTable table.
 *
 * Creates a new ieee8021SrpBridgeBaseTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SrpBridgeBaseTable_data *
ieee8021SrpBridgeBaseTable_create(void)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpBridgeBaseTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgeBaseTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021SrpBridgeBaseMsrpEnabledStatus = TV_TRUE;
		StorageNew->ieee8021SrpBridgeBaseMsrpTalkerPruning = TV_FALSE;
		StorageNew->ieee8021SrpBridgeBaseMsrpMaxFanInPorts = 0;
		StorageNew->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize = 2000;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SrpBridgeBaseTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SrpBridgeBaseTable_data *ieee8021SrpBridgeBaseTable_duplicate(struct ieee8021SrpBridgeBaseTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SrpBridgeBaseTable_data *
ieee8021SrpBridgeBaseTable_duplicate(struct ieee8021SrpBridgeBaseTable_data *thedata)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpBridgeBaseTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgeBaseTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SrpBridgeBaseTable_id = thedata->ieee8021SrpBridgeBaseTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021SrpBridgeBaseMsrpEnabledStatus = thedata->ieee8021SrpBridgeBaseMsrpEnabledStatus;
		StorageNew->ieee8021SrpBridgeBaseMsrpTalkerPruning = thedata->ieee8021SrpBridgeBaseMsrpTalkerPruning;
		StorageNew->ieee8021SrpBridgeBaseMsrpMaxFanInPorts = thedata->ieee8021SrpBridgeBaseMsrpMaxFanInPorts;
		StorageNew->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize = thedata->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SrpBridgeBaseTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SrpBridgeBaseTable_destroy(struct ieee8021SrpBridgeBaseTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SrpBridgeBaseTable_destroy(struct ieee8021SrpBridgeBaseTable_data **thedata)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgeBaseTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpBridgeBaseTable_add(struct ieee8021SrpBridgeBaseTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SrpBridgeBaseTable table data set.
 *
 * Adds a table row structure to the ieee8021SrpBridgeBaseTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SrpBridgeBaseTable_add(struct ieee8021SrpBridgeBaseTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgeBaseTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		header_complex_add_data(&ieee8021SrpBridgeBaseTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpBridgeBaseTable_del(struct ieee8021SrpBridgeBaseTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SrpBridgeBaseTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SrpBridgeBaseTable_del(struct ieee8021SrpBridgeBaseTable_data *thedata)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgeBaseTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SrpBridgeBaseTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SrpBridgeBaseTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SrpBridgeBaseTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SrpBridgeBaseTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SrpBridgeBaseTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SrpBridgeBaseTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp = ieee8021SrpBridgeBaseTable_create();

	DEBUGMSGTL(("ieee8021SrpMib", "parse_ieee8021SrpBridgeBaseTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize, &tmpsize);
	ieee8021SrpBridgeBaseTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/*
 * store_ieee8021SrpBridgeBaseTable(): store configuraiton file for ieee8021SrpBridgeBaseTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SrpBridgeBaseTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SrpMib", "store_ieee8021SrpBridgeBaseTable: storing data...  "));
	refresh_ieee8021SrpBridgeBaseTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SrpBridgeBaseTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SrpBridgeBaseTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SrpBridgeBaseTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SrpBridgePortTable_data *ieee8021SrpBridgePortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SrpBridgePortTable table.
 *
 * Creates a new ieee8021SrpBridgePortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SrpBridgePortTable_data *
ieee8021SrpBridgePortTable_create(void)
{
	struct ieee8021SrpBridgePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpBridgePortTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgePortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021SrpBridgePortMsrpEnabledStatus = TV_TRUE;
		StorageNew->ieee8021SrpBridgePortMsrpFailedRegistrations = (struct counter64) {
		0, 0};
		if ((StorageNew->ieee8021SrpBridgePortMsrpLastPduOrigin = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SrpBridgePortMsrpLastPduOriginLen = 0;
		StorageNew->ieee8021SrpBridgePortMsrpLastPduOrigin[StorageNew->ieee8021SrpBridgePortMsrpLastPduOriginLen] = 0;
		StorageNew->ieee8021SrpBridgePortSrPvid = 2;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SrpBridgePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SrpBridgePortTable_data *ieee8021SrpBridgePortTable_duplicate(struct ieee8021SrpBridgePortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SrpBridgePortTable_data *
ieee8021SrpBridgePortTable_duplicate(struct ieee8021SrpBridgePortTable_data *thedata)
{
	struct ieee8021SrpBridgePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpBridgePortTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgePortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SrpBridgePortTable_id = thedata->ieee8021SrpBridgePortTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021SrpBridgePortMsrpEnabledStatus = thedata->ieee8021SrpBridgePortMsrpEnabledStatus;
		StorageNew->ieee8021SrpBridgePortMsrpFailedRegistrations = thedata->ieee8021SrpBridgePortMsrpFailedRegistrations;
		if (!(StorageNew->ieee8021SrpBridgePortMsrpLastPduOrigin = malloc(thedata->ieee8021SrpBridgePortMsrpLastPduOriginLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SrpBridgePortMsrpLastPduOrigin, thedata->ieee8021SrpBridgePortMsrpLastPduOrigin, thedata->ieee8021SrpBridgePortMsrpLastPduOriginLen);
		StorageNew->ieee8021SrpBridgePortMsrpLastPduOriginLen = thedata->ieee8021SrpBridgePortMsrpLastPduOriginLen;
		StorageNew->ieee8021SrpBridgePortMsrpLastPduOrigin[StorageNew->ieee8021SrpBridgePortMsrpLastPduOriginLen] = 0;
		StorageNew->ieee8021SrpBridgePortSrPvid = thedata->ieee8021SrpBridgePortSrPvid;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SrpBridgePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SrpBridgePortTable_destroy(struct ieee8021SrpBridgePortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SrpBridgePortTable_destroy(struct ieee8021SrpBridgePortTable_data **thedata)
{
	struct ieee8021SrpBridgePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgePortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SrpBridgePortMsrpLastPduOrigin);
		StorageDel->ieee8021SrpBridgePortMsrpLastPduOriginLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpBridgePortTable_add(struct ieee8021SrpBridgePortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SrpBridgePortTable table data set.
 *
 * Adds a table row structure to the ieee8021SrpBridgePortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SrpBridgePortTable_add(struct ieee8021SrpBridgePortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgePortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021SrpBridgePortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpBridgePortTable_del(struct ieee8021SrpBridgePortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SrpBridgePortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SrpBridgePortTable_del(struct ieee8021SrpBridgePortTable_data *thedata)
{
	struct ieee8021SrpBridgePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpBridgePortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SrpBridgePortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SrpBridgePortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SrpBridgePortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SrpBridgePortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SrpBridgePortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SrpBridgePortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SrpBridgePortTable_data *StorageTmp = ieee8021SrpBridgePortTable_create();

	DEBUGMSGTL(("ieee8021SrpMib", "parse_ieee8021SrpBridgePortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021SrpBridgePortMsrpFailedRegistrations, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SrpBridgePortMsrpLastPduOrigin);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SrpBridgePortMsrpLastPduOrigin, &StorageTmp->ieee8021SrpBridgePortMsrpLastPduOriginLen);
	if (StorageTmp->ieee8021SrpBridgePortMsrpLastPduOrigin == NULL) {
		config_perror("invalid specification for ieee8021SrpBridgePortMsrpLastPduOrigin");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpBridgePortSrPvid, &tmpsize);
	ieee8021SrpBridgePortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/*
 * store_ieee8021SrpBridgePortTable(): store configuraiton file for ieee8021SrpBridgePortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SrpBridgePortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SrpBridgePortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SrpMib", "store_ieee8021SrpBridgePortTable: storing data...  "));
	refresh_ieee8021SrpBridgePortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SrpBridgePortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SrpBridgePortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SrpBridgePortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021SrpBridgePortMsrpFailedRegistrations, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SrpBridgePortMsrpLastPduOrigin, &StorageTmp->ieee8021SrpBridgePortMsrpLastPduOriginLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpBridgePortSrPvid, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SrpLatencyTable_data *ieee8021SrpLatencyTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SrpLatencyTable table.
 *
 * Creates a new ieee8021SrpLatencyTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SrpLatencyTable_data *
ieee8021SrpLatencyTable_create(void)
{
	struct ieee8021SrpLatencyTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpLatencyTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpLatencyTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021SrpPortTcLatency = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SrpLatencyTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SrpLatencyTable_data *ieee8021SrpLatencyTable_duplicate(struct ieee8021SrpLatencyTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SrpLatencyTable_data *
ieee8021SrpLatencyTable_duplicate(struct ieee8021SrpLatencyTable_data *thedata)
{
	struct ieee8021SrpLatencyTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpLatencyTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpLatencyTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SrpLatencyTable_id = thedata->ieee8021SrpLatencyTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021SrpTrafficClass = thedata->ieee8021SrpTrafficClass;
		StorageNew->ieee8021SrpPortTcLatency = thedata->ieee8021SrpPortTcLatency;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SrpLatencyTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SrpLatencyTable_destroy(struct ieee8021SrpLatencyTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SrpLatencyTable_destroy(struct ieee8021SrpLatencyTable_data **thedata)
{
	struct ieee8021SrpLatencyTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpLatencyTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpLatencyTable_add(struct ieee8021SrpLatencyTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SrpLatencyTable table data set.
 *
 * Adds a table row structure to the ieee8021SrpLatencyTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SrpLatencyTable_add(struct ieee8021SrpLatencyTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpLatencyTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021SrpTrafficClass */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SrpTrafficClass, sizeof(thedata->ieee8021SrpTrafficClass));
		header_complex_add_data(&ieee8021SrpLatencyTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpLatencyTable_del(struct ieee8021SrpLatencyTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SrpLatencyTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SrpLatencyTable_del(struct ieee8021SrpLatencyTable_data *thedata)
{
	struct ieee8021SrpLatencyTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpLatencyTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SrpLatencyTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SrpLatencyTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SrpLatencyTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SrpLatencyTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SrpLatencyTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SrpLatencyTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SrpLatencyTable_data *StorageTmp = ieee8021SrpLatencyTable_create();

	DEBUGMSGTL(("ieee8021SrpMib", "parse_ieee8021SrpLatencyTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpTrafficClass, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpPortTcLatency, &tmpsize);
	ieee8021SrpLatencyTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/*
 * store_ieee8021SrpLatencyTable(): store configuraiton file for ieee8021SrpLatencyTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SrpLatencyTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SrpLatencyTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SrpMib", "store_ieee8021SrpLatencyTable: storing data...  "));
	refresh_ieee8021SrpLatencyTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SrpLatencyTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SrpLatencyTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SrpLatencyTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpTrafficClass, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpPortTcLatency, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SrpStreamTable_data *ieee8021SrpStreamTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SrpStreamTable table.
 *
 * Creates a new ieee8021SrpStreamTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SrpStreamTable_data *
ieee8021SrpStreamTable_create(void)
{
	struct ieee8021SrpStreamTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpStreamTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpStreamTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021SrpStreamDestinationAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SrpStreamDestinationAddressLen = 0;
		StorageNew->ieee8021SrpStreamDestinationAddress[StorageNew->ieee8021SrpStreamDestinationAddressLen] = 0;
		StorageNew->ieee8021SrpStreamVlanId = 0;
		StorageNew->ieee8021SrpStreamTspecMaxFrameSize = 0;
		StorageNew->ieee8021SrpStreamTspecMaxIntervalFrames = 0;
		StorageNew->ieee8021SrpStreamDataFramePriority = 0;
		StorageNew->ieee8021SrpStreamRank = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SrpStreamTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SrpStreamTable_data *ieee8021SrpStreamTable_duplicate(struct ieee8021SrpStreamTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SrpStreamTable_data *
ieee8021SrpStreamTable_duplicate(struct ieee8021SrpStreamTable_data *thedata)
{
	struct ieee8021SrpStreamTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpStreamTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpStreamTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SrpStreamTable_id = thedata->ieee8021SrpStreamTable_id;
		if (!(StorageNew->ieee8021SrpStreamId = malloc(thedata->ieee8021SrpStreamIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SrpStreamId, thedata->ieee8021SrpStreamId, thedata->ieee8021SrpStreamIdLen);
		StorageNew->ieee8021SrpStreamIdLen = thedata->ieee8021SrpStreamIdLen;
		StorageNew->ieee8021SrpStreamId[StorageNew->ieee8021SrpStreamIdLen] = 0;
		if (!(StorageNew->ieee8021SrpStreamDestinationAddress = malloc(thedata->ieee8021SrpStreamDestinationAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SrpStreamDestinationAddress, thedata->ieee8021SrpStreamDestinationAddress, thedata->ieee8021SrpStreamDestinationAddressLen);
		StorageNew->ieee8021SrpStreamDestinationAddressLen = thedata->ieee8021SrpStreamDestinationAddressLen;
		StorageNew->ieee8021SrpStreamDestinationAddress[StorageNew->ieee8021SrpStreamDestinationAddressLen] = 0;
		StorageNew->ieee8021SrpStreamVlanId = thedata->ieee8021SrpStreamVlanId;
		StorageNew->ieee8021SrpStreamTspecMaxFrameSize = thedata->ieee8021SrpStreamTspecMaxFrameSize;
		StorageNew->ieee8021SrpStreamTspecMaxIntervalFrames = thedata->ieee8021SrpStreamTspecMaxIntervalFrames;
		StorageNew->ieee8021SrpStreamDataFramePriority = thedata->ieee8021SrpStreamDataFramePriority;
		StorageNew->ieee8021SrpStreamRank = thedata->ieee8021SrpStreamRank;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SrpStreamTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SrpStreamTable_destroy(struct ieee8021SrpStreamTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SrpStreamTable_destroy(struct ieee8021SrpStreamTable_data **thedata)
{
	struct ieee8021SrpStreamTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpStreamTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SrpStreamId);
		StorageDel->ieee8021SrpStreamIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SrpStreamDestinationAddress);
		StorageDel->ieee8021SrpStreamDestinationAddressLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpStreamTable_add(struct ieee8021SrpStreamTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SrpStreamTable table data set.
 *
 * Adds a table row structure to the ieee8021SrpStreamTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SrpStreamTable_add(struct ieee8021SrpStreamTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpStreamTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SrpStreamId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SrpStreamId, thedata->ieee8021SrpStreamIdLen);
		header_complex_add_data(&ieee8021SrpStreamTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpStreamTable_del(struct ieee8021SrpStreamTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SrpStreamTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SrpStreamTable_del(struct ieee8021SrpStreamTable_data *thedata)
{
	struct ieee8021SrpStreamTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpStreamTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SrpStreamTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SrpStreamTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SrpStreamTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SrpStreamTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SrpStreamTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SrpStreamTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SrpStreamTable_data *StorageTmp = ieee8021SrpStreamTable_create();

	DEBUGMSGTL(("ieee8021SrpMib", "parse_ieee8021SrpStreamTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->ieee8021SrpStreamId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SrpStreamId, &StorageTmp->ieee8021SrpStreamIdLen);
	if (StorageTmp->ieee8021SrpStreamId == NULL) {
		config_perror("invalid specification for ieee8021SrpStreamId");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SrpStreamDestinationAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SrpStreamDestinationAddress, &StorageTmp->ieee8021SrpStreamDestinationAddressLen);
	if (StorageTmp->ieee8021SrpStreamDestinationAddress == NULL) {
		config_perror("invalid specification for ieee8021SrpStreamDestinationAddress");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpStreamVlanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpStreamTspecMaxFrameSize, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpStreamTspecMaxIntervalFrames, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpStreamDataFramePriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpStreamRank, &tmpsize);
	ieee8021SrpStreamTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/*
 * store_ieee8021SrpStreamTable(): store configuraiton file for ieee8021SrpStreamTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SrpStreamTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SrpStreamTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SrpMib", "store_ieee8021SrpStreamTable: storing data...  "));
	refresh_ieee8021SrpStreamTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SrpStreamTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SrpStreamTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SrpStreamTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SrpStreamId, &StorageTmp->ieee8021SrpStreamIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SrpStreamDestinationAddress, &StorageTmp->ieee8021SrpStreamDestinationAddressLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpStreamVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpStreamTspecMaxFrameSize, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpStreamTspecMaxIntervalFrames, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpStreamDataFramePriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpStreamRank, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SrpReservationsTable_data *ieee8021SrpReservationsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SrpReservationsTable table.
 *
 * Creates a new ieee8021SrpReservationsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SrpReservationsTable_data *
ieee8021SrpReservationsTable_create(void)
{
	struct ieee8021SrpReservationsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpReservationsTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpReservationsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021SrpReservationDeclarationType = 0;
		StorageNew->ieee8021SrpReservationAccumulatedLatency = 0;
		if ((StorageNew->ieee8021SrpReservationFailureBridgeId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SrpReservationFailureBridgeIdLen = 0;
		StorageNew->ieee8021SrpReservationFailureBridgeId[StorageNew->ieee8021SrpReservationFailureBridgeIdLen] = 0;
		StorageNew->ieee8021SrpReservationFailureCode = 0;
		StorageNew->ieee8021SrpReservationDroppedStreamFrames = (struct counter64) {
		0, 0};
		StorageNew->ieee8021SrpReservationStreamAge = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SrpReservationsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SrpReservationsTable_data *ieee8021SrpReservationsTable_duplicate(struct ieee8021SrpReservationsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SrpReservationsTable_data *
ieee8021SrpReservationsTable_duplicate(struct ieee8021SrpReservationsTable_data *thedata)
{
	struct ieee8021SrpReservationsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SrpReservationsTable_data);

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpReservationsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SrpReservationsTable_id = thedata->ieee8021SrpReservationsTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		if (!(StorageNew->ieee8021SrpReservationStreamId = malloc(thedata->ieee8021SrpReservationStreamIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SrpReservationStreamId, thedata->ieee8021SrpReservationStreamId, thedata->ieee8021SrpReservationStreamIdLen);
		StorageNew->ieee8021SrpReservationStreamIdLen = thedata->ieee8021SrpReservationStreamIdLen;
		StorageNew->ieee8021SrpReservationStreamId[StorageNew->ieee8021SrpReservationStreamIdLen] = 0;
		StorageNew->ieee8021SrpReservationDirection = thedata->ieee8021SrpReservationDirection;
		StorageNew->ieee8021SrpReservationDeclarationType = thedata->ieee8021SrpReservationDeclarationType;
		StorageNew->ieee8021SrpReservationAccumulatedLatency = thedata->ieee8021SrpReservationAccumulatedLatency;
		if (!(StorageNew->ieee8021SrpReservationFailureBridgeId = malloc(thedata->ieee8021SrpReservationFailureBridgeIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SrpReservationFailureBridgeId, thedata->ieee8021SrpReservationFailureBridgeId, thedata->ieee8021SrpReservationFailureBridgeIdLen);
		StorageNew->ieee8021SrpReservationFailureBridgeIdLen = thedata->ieee8021SrpReservationFailureBridgeIdLen;
		StorageNew->ieee8021SrpReservationFailureBridgeId[StorageNew->ieee8021SrpReservationFailureBridgeIdLen] = 0;
		StorageNew->ieee8021SrpReservationFailureCode = thedata->ieee8021SrpReservationFailureCode;
		StorageNew->ieee8021SrpReservationDroppedStreamFrames = thedata->ieee8021SrpReservationDroppedStreamFrames;
		StorageNew->ieee8021SrpReservationStreamAge = thedata->ieee8021SrpReservationStreamAge;
	}
      done:
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SrpReservationsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SrpReservationsTable_destroy(struct ieee8021SrpReservationsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SrpReservationsTable_destroy(struct ieee8021SrpReservationsTable_data **thedata)
{
	struct ieee8021SrpReservationsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpReservationsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SrpReservationStreamId);
		StorageDel->ieee8021SrpReservationStreamIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SrpReservationFailureBridgeId);
		StorageDel->ieee8021SrpReservationFailureBridgeIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpReservationsTable_add(struct ieee8021SrpReservationsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SrpReservationsTable table data set.
 *
 * Adds a table row structure to the ieee8021SrpReservationsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SrpReservationsTable_add(struct ieee8021SrpReservationsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpReservationsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021SrpReservationStreamId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SrpReservationStreamId, thedata->ieee8021SrpReservationStreamIdLen);
		/* ieee8021SrpReservationDirection */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SrpReservationDirection, sizeof(thedata->ieee8021SrpReservationDirection));
		header_complex_add_data(&ieee8021SrpReservationsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SrpReservationsTable_del(struct ieee8021SrpReservationsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SrpReservationsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SrpReservationsTable_del(struct ieee8021SrpReservationsTable_data *thedata)
{
	struct ieee8021SrpReservationsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpReservationsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SrpReservationsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SrpReservationsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SrpReservationsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SrpReservationsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SrpReservationsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SrpReservationsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SrpReservationsTable_data *StorageTmp = ieee8021SrpReservationsTable_create();

	DEBUGMSGTL(("ieee8021SrpMib", "parse_ieee8021SrpReservationsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SrpReservationStreamId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SrpReservationStreamId, &StorageTmp->ieee8021SrpReservationStreamIdLen);
	if (StorageTmp->ieee8021SrpReservationStreamId == NULL) {
		config_perror("invalid specification for ieee8021SrpReservationStreamId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpReservationDirection, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpReservationDeclarationType, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpReservationAccumulatedLatency, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SrpReservationFailureBridgeId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SrpReservationFailureBridgeId, &StorageTmp->ieee8021SrpReservationFailureBridgeIdLen);
	if (StorageTmp->ieee8021SrpReservationFailureBridgeId == NULL) {
		config_perror("invalid specification for ieee8021SrpReservationFailureBridgeId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SrpReservationFailureCode, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021SrpReservationDroppedStreamFrames, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SrpReservationStreamAge, &tmpsize);
	ieee8021SrpReservationsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/*
 * store_ieee8021SrpReservationsTable(): store configuraiton file for ieee8021SrpReservationsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SrpReservationsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SrpReservationsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SrpMib", "store_ieee8021SrpReservationsTable: storing data...  "));
	refresh_ieee8021SrpReservationsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SrpReservationsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SrpReservationsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SrpReservationsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SrpReservationStreamId, &StorageTmp->ieee8021SrpReservationStreamIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpReservationDirection, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpReservationDeclarationType, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpReservationAccumulatedLatency, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SrpReservationFailureBridgeId, &StorageTmp->ieee8021SrpReservationFailureBridgeIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SrpReservationFailureCode, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021SrpReservationDroppedStreamFrames, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SrpReservationStreamAge, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, struct ieee8021SrpBridgeBaseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, struct ieee8021SrpBridgeBaseTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, struct ieee8021SrpBridgeBaseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SrpBridgeBaseTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, struct ieee8021SrpBridgeBaseTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SrpBridgeBaseTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, struct ieee8021SrpBridgeBaseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, struct ieee8021SrpBridgeBaseTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SrpBridgeBaseTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SrpBridgeBaseTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SrpBridgeBaseTable_data *
refresh_ieee8021SrpBridgeBaseTable_row(struct ieee8021SrpBridgeBaseTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SrpBridgeBaseTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SrpBridgeBaseTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SrpBridgeBaseTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SrpBridgeBaseTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SrpBridgeBaseTable(int force)
{
	if (!force && ieee8021SrpBridgeBaseTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SrpBridgeBaseTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SrpBridgeBaseTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SrpBridgeBaseTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SrpMib above.
 */
u_char *
var_ieee8021SrpBridgeBaseTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SrpMib", "var_ieee8021SrpBridgeBaseTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SrpBridgeBaseTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SrpBridgeBaseTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SrpBridgeBaseTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SRPBRIDGEBASEMSRPENABLEDSTATUS:	/* Create */
		*write_method = write_ieee8021SrpBridgeBaseMsrpEnabledStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus;
		break;
	case (u_char) IEEE8021SRPBRIDGEBASEMSRPTALKERPRUNING:	/* Create */
		*write_method = write_ieee8021SrpBridgeBaseMsrpTalkerPruning;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning;
		break;
	case (u_char) IEEE8021SRPBRIDGEBASEMSRPMAXFANINPORTS:	/* Create */
		*write_method = write_ieee8021SrpBridgeBaseMsrpMaxFanInPorts;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts;
		break;
	case (u_char) IEEE8021SRPBRIDGEBASEMSRPLATENCYMAXFRAMESIZE:	/* Create */
		*write_method = write_ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, struct ieee8021SrpBridgePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, struct ieee8021SrpBridgePortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, struct ieee8021SrpBridgePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SrpBridgePortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, struct ieee8021SrpBridgePortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SrpBridgePortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, struct ieee8021SrpBridgePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, struct ieee8021SrpBridgePortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SrpBridgePortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SrpBridgePortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SrpBridgePortTable_data *
refresh_ieee8021SrpBridgePortTable_row(struct ieee8021SrpBridgePortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SrpBridgePortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SrpBridgePortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SrpBridgePortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SrpBridgePortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SrpBridgePortTable(int force)
{
	if (!force && ieee8021SrpBridgePortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SrpBridgePortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SrpBridgePortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SrpBridgePortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SrpMib above.
 */
u_char *
var_ieee8021SrpBridgePortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SrpBridgePortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SrpMib", "var_ieee8021SrpBridgePortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SrpBridgePortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SrpBridgePortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SrpBridgePortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SRPBRIDGEPORTMSRPENABLEDSTATUS:	/* Create */
		*write_method = write_ieee8021SrpBridgePortMsrpEnabledStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus;
		break;
	case (u_char) IEEE8021SRPBRIDGEPORTMSRPFAILEDREGISTRATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgePortMsrpFailedRegistrations);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgePortMsrpFailedRegistrations;
		break;
	case (u_char) IEEE8021SRPBRIDGEPORTMSRPLASTPDUORIGIN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SrpBridgePortMsrpLastPduOriginLen;
		rval = (u_char *) StorageTmp->ieee8021SrpBridgePortMsrpLastPduOrigin;
		break;
	case (u_char) IEEE8021SRPBRIDGEPORTSRPVID:	/* Create */
		*write_method = write_ieee8021SrpBridgePortSrPvid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpBridgePortSrPvid);
		rval = (u_char *) &StorageTmp->ieee8021SrpBridgePortSrPvid;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, struct ieee8021SrpLatencyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, struct ieee8021SrpLatencyTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, struct ieee8021SrpLatencyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SrpLatencyTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, struct ieee8021SrpLatencyTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SrpLatencyTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, struct ieee8021SrpLatencyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, struct ieee8021SrpLatencyTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SrpLatencyTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SrpLatencyTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SrpLatencyTable_data *
refresh_ieee8021SrpLatencyTable_row(struct ieee8021SrpLatencyTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SrpLatencyTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SrpLatencyTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SrpLatencyTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SrpLatencyTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SrpLatencyTable(int force)
{
	if (!force && ieee8021SrpLatencyTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SrpLatencyTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SrpLatencyTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SrpLatencyTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SrpMib above.
 */
u_char *
var_ieee8021SrpLatencyTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SrpLatencyTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SrpMib", "var_ieee8021SrpLatencyTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SrpLatencyTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SrpLatencyTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SrpLatencyTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SRPPORTTCLATENCY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpPortTcLatency);
		rval = (u_char *) &StorageTmp->ieee8021SrpPortTcLatency;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, struct ieee8021SrpStreamTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, struct ieee8021SrpStreamTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, struct ieee8021SrpStreamTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SrpStreamTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, struct ieee8021SrpStreamTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SrpStreamTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, struct ieee8021SrpStreamTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, struct ieee8021SrpStreamTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SrpStreamTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SrpStreamTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SrpStreamTable_data *
refresh_ieee8021SrpStreamTable_row(struct ieee8021SrpStreamTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SrpStreamTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SrpStreamTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SrpStreamTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SrpStreamTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SrpStreamTable(int force)
{
	if (!force && ieee8021SrpStreamTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SrpStreamTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SrpStreamTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SrpStreamTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SrpMib above.
 */
u_char *
var_ieee8021SrpStreamTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SrpStreamTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SrpMib", "var_ieee8021SrpStreamTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SrpStreamTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SrpStreamTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SrpStreamTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SRPSTREAMDESTINATIONADDRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SrpStreamDestinationAddressLen;
		rval = (u_char *) StorageTmp->ieee8021SrpStreamDestinationAddress;
		break;
	case (u_char) IEEE8021SRPSTREAMVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpStreamVlanId);
		rval = (u_char *) &StorageTmp->ieee8021SrpStreamVlanId;
		break;
	case (u_char) IEEE8021SRPSTREAMTSPECMAXFRAMESIZE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpStreamTspecMaxFrameSize);
		rval = (u_char *) &StorageTmp->ieee8021SrpStreamTspecMaxFrameSize;
		break;
	case (u_char) IEEE8021SRPSTREAMTSPECMAXINTERVALFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpStreamTspecMaxIntervalFrames);
		rval = (u_char *) &StorageTmp->ieee8021SrpStreamTspecMaxIntervalFrames;
		break;
	case (u_char) IEEE8021SRPSTREAMDATAFRAMEPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpStreamDataFramePriority);
		rval = (u_char *) &StorageTmp->ieee8021SrpStreamDataFramePriority;
		break;
	case (u_char) IEEE8021SRPSTREAMRANK:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpStreamRank);
		rval = (u_char *) &StorageTmp->ieee8021SrpStreamRank;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, struct ieee8021SrpReservationsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, struct ieee8021SrpReservationsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, struct ieee8021SrpReservationsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SrpReservationsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, struct ieee8021SrpReservationsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SrpReservationsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, struct ieee8021SrpReservationsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, struct ieee8021SrpReservationsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SrpReservationsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SrpReservationsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SrpReservationsTable_data *
refresh_ieee8021SrpReservationsTable_row(struct ieee8021SrpReservationsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SrpReservationsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SrpReservationsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SrpReservationsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SrpReservationsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SrpReservationsTable(int force)
{
	if (!force && ieee8021SrpReservationsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SrpReservationsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SrpReservationsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SrpReservationsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SrpMib above.
 */
u_char *
var_ieee8021SrpReservationsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SrpReservationsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SrpMib", "var_ieee8021SrpReservationsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SrpReservationsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SrpReservationsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SrpReservationsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SRPRESERVATIONDECLARATIONTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpReservationDeclarationType);
		rval = (u_char *) &StorageTmp->ieee8021SrpReservationDeclarationType;
		break;
	case (u_char) IEEE8021SRPRESERVATIONACCUMULATEDLATENCY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpReservationAccumulatedLatency);
		rval = (u_char *) &StorageTmp->ieee8021SrpReservationAccumulatedLatency;
		break;
	case (u_char) IEEE8021SRPRESERVATIONFAILUREBRIDGEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SrpReservationFailureBridgeIdLen;
		rval = (u_char *) StorageTmp->ieee8021SrpReservationFailureBridgeId;
		break;
	case (u_char) IEEE8021SRPRESERVATIONFAILURECODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpReservationFailureCode);
		rval = (u_char *) &StorageTmp->ieee8021SrpReservationFailureCode;
		break;
	case (u_char) IEEE8021SRPRESERVATIONDROPPEDSTREAMFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpReservationDroppedStreamFrames);
		rval = (u_char *) &StorageTmp->ieee8021SrpReservationDroppedStreamFrames;
		break;
	case (u_char) IEEE8021SRPRESERVATIONSTREAMAGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SrpReservationStreamAge);
		rval = (u_char *) &StorageTmp->ieee8021SrpReservationStreamAge;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021SrpBridgeBaseMsrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SrpBridgeBaseMsrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SrpMib", "write_ieee8021SrpBridgeBaseMsrpEnabledStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SrpBridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpEnabledStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpEnabledStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpEnabledStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old = ieee8021SrpBridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs++;
		StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
			revert_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021SrpBridgeBaseMsrpEnabledStatus = StorageOld->ieee8021SrpBridgeBaseMsrpEnabledStatus;
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SrpBridgeBaseMsrpTalkerPruning(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SrpBridgeBaseMsrpTalkerPruning(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SrpMib", "write_ieee8021SrpBridgeBaseMsrpTalkerPruning entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SrpBridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpTalkerPruning not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpTalkerPruning: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpTalkerPruning: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old = ieee8021SrpBridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs++;
		StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
			revert_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021SrpBridgeBaseMsrpTalkerPruning = StorageOld->ieee8021SrpBridgeBaseMsrpTalkerPruning;
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SrpBridgeBaseMsrpMaxFanInPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SrpBridgeBaseMsrpMaxFanInPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SrpMib", "write_ieee8021SrpBridgeBaseMsrpMaxFanInPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SrpBridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpMaxFanInPorts not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpMaxFanInPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old = ieee8021SrpBridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs++;
		StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
			revert_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021SrpBridgeBaseMsrpMaxFanInPorts = StorageOld->ieee8021SrpBridgeBaseMsrpMaxFanInPorts;
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SrpBridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SrpMib", "write_ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SrpBridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2000 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old = ieee8021SrpBridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs++;
		StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize for you to use, and you have just been asked to do something with it.  Note 
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) != NULL) {
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
			StorageTmp->ieee8021SrpBridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021SrpBridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_sets == 0)
			revert_ieee8021SrpBridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize = StorageOld->ieee8021SrpBridgeBaseMsrpLatencyMaxFrameSize;
		if (--StorageTmp->ieee8021SrpBridgeBaseTable_rsvs == 0)
			ieee8021SrpBridgeBaseTable_destroy(&StorageTmp->ieee8021SrpBridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SrpBridgePortMsrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SrpBridgePortMsrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SrpBridgePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SrpMib", "write_ieee8021SrpBridgePortMsrpEnabledStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SrpBridgePortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgePortMsrpEnabledStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgePortMsrpEnabledStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgePortMsrpEnabledStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) == NULL)
			if (StorageTmp->ieee8021SrpBridgePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old = ieee8021SrpBridgePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SrpBridgePortTable_rsvs++;
		StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SrpBridgePortTable_tsts == 0)
				if ((ret = check_ieee8021SrpBridgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SrpBridgePortTable_sets == 0)
				if ((ret = update_ieee8021SrpBridgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) != NULL) {
			ieee8021SrpBridgePortTable_destroy(&StorageTmp->ieee8021SrpBridgePortTable_old);
			StorageTmp->ieee8021SrpBridgePortTable_rsvs = 0;
			StorageTmp->ieee8021SrpBridgePortTable_tsts = 0;
			StorageTmp->ieee8021SrpBridgePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SrpBridgePortTable_sets == 0)
			revert_ieee8021SrpBridgePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SrpBridgePortMsrpEnabledStatus = StorageOld->ieee8021SrpBridgePortMsrpEnabledStatus;
		if (--StorageTmp->ieee8021SrpBridgePortTable_rsvs == 0)
			ieee8021SrpBridgePortTable_destroy(&StorageTmp->ieee8021SrpBridgePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SrpBridgePortSrPvid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SrpBridgePortSrPvid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SrpBridgePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SrpMib", "write_ieee8021SrpBridgePortSrPvid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SrpBridgePortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgePortSrPvid not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgePortSrPvid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2 */
		/* Note: ranges 1..4094 4096..-1 */
		if ((1 > set_value || set_value > 4094) && (4096 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SrpBridgePortSrPvid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) == NULL)
			if (StorageTmp->ieee8021SrpBridgePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old = ieee8021SrpBridgePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SrpBridgePortTable_rsvs++;
		StorageTmp->ieee8021SrpBridgePortSrPvid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SrpBridgePortTable_tsts == 0)
				if ((ret = check_ieee8021SrpBridgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SrpBridgePortSrPvid for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SrpBridgePortTable_sets == 0)
				if ((ret = update_ieee8021SrpBridgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SrpBridgePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) != NULL) {
			ieee8021SrpBridgePortTable_destroy(&StorageTmp->ieee8021SrpBridgePortTable_old);
			StorageTmp->ieee8021SrpBridgePortTable_rsvs = 0;
			StorageTmp->ieee8021SrpBridgePortTable_tsts = 0;
			StorageTmp->ieee8021SrpBridgePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SrpBridgePortTable_sets == 0)
			revert_ieee8021SrpBridgePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SrpBridgePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SrpBridgePortSrPvid = StorageOld->ieee8021SrpBridgePortSrPvid;
		if (--StorageTmp->ieee8021SrpBridgePortTable_rsvs == 0)
			ieee8021SrpBridgePortTable_destroy(&StorageTmp->ieee8021SrpBridgePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021SrpMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021SrpMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021SrpMibold_signal_handler != NULL)
		(*ieee8021SrpMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
}

/**
 * @fn void ieee8021SrpMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021SrpMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021SrpMib", "ieee8021SrpMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021SrpMib", "done.\n"));
	return;
}
