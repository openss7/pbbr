/*****************************************************************************

 @(#) src/snmp/ieee8021bridgeevbmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021bridgeevbmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021BridgeEvbMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021BridgeEvbMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021BridgeEvbMib_refresh = 1;
volatile int ieee8021BridgeEvbSbpTable_refresh = 1;
volatile int ieee8021BridgeEvbVSIDBTable_refresh = 1;
volatile int ieee8021BridgeEvbVSIDBMacTable_refresh = 1;
volatile int ieee8021BridgeEvbUAPConfigTable_refresh = 1;
volatile int ieee8021BridgeEvbCAPConfigTable_refresh = 1;
volatile int ieee8021BridgeEvbURPTable_refresh = 1;
volatile int ieee8021BridgeEvbEcpTable_refresh = 1;

/*
 * ieee8021BridgeEvbMib_variables_oid: object identifier for ieee8021BridgeEvbMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021BridgeEvbMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 24 };
oid ieee8021BridgeEvbUAPConfigTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 24, 1, 3, 1, 1 };
oid ieee8021BridgeEvbCAPConfigTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 24, 1, 3, 2, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021BridgeEvbSysGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 1 };
oid ieee8021BridgeEvbSbpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 3 };
oid ieee8021BridgeEvbVSIDBGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 4 };
oid ieee8021BridgeEvbUAPGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 5 };
oid ieee8021BridgeEvbCAPConfigGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 6 };
oid ieee8021BridgeEvbsURPGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 7 };
oid ieee8021BridgeEvbEcpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 1, 8 };
oid ieee8021BridgeEvbbCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 2, 1 };
oid ieee8021BridgeEvbsCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 24, 2, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021BridgeEvbMib_variables: tree for ieee8021BridgeEvbMib
 * This variable defines function callbacks and type return information for the ieee8021BridgeEvbMib mib section
 */
struct variable7 ieee8021BridgeEvbMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021BRIDGEEVBSYSTYPE  1
	{(u_char) IEEE8021BRIDGEEVBSYSTYPE, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbMib, 3, {1, 1, 1}},
#define   IEEE8021BRIDGEEVBSYSNUMEXTERNALPORTS  2
	{(u_char) IEEE8021BRIDGEEVBSYSNUMEXTERNALPORTS, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbMib, 3, {1, 1, 2}},
#define   IEEE8021BRIDGEEVBSYSEVBLLDPTXENABLE  3
	{(u_char) IEEE8021BRIDGEEVBSYSEVBLLDPTXENABLE, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 3}},
#define   IEEE8021BRIDGEEVBSYSEVBLLDPMANUAL  4
	{(u_char) IEEE8021BRIDGEEVBSYSEVBLLDPMANUAL, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 4}},
#define   IEEE8021BRIDGEEVBSYSEVBLLDPGIDCAPABLE  5
	{(u_char) IEEE8021BRIDGEEVBSYSEVBLLDPGIDCAPABLE, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 5}},
#define   IEEE8021BRIDGEEVBSYSECPACKTIMER  6
	{(u_char) IEEE8021BRIDGEEVBSYSECPACKTIMER, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 6}},
#define   IEEE8021BRIDGEEVBSYSECPMAXRETRIES  7
	{(u_char) IEEE8021BRIDGEEVBSYSECPMAXRETRIES, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 7}},
#define   IEEE8021BRIDGEEVBSYSVDPDFLTRSRCWAITDELAY  8
	{(u_char) IEEE8021BRIDGEEVBSYSVDPDFLTRSRCWAITDELAY, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 8}},
#define   IEEE8021BRIDGEEVBSYSVDPDFLTREINITKEEPALIVE  9
	{(u_char) IEEE8021BRIDGEEVBSYSVDPDFLTREINITKEEPALIVE, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbMib, 3, {1, 1, 9}},
#define   IEEE8021BRIDGEEVBSBPLLDPMANUAL  10
	{(u_char) IEEE8021BRIDGEEVBSBPLLDPMANUAL, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbSbpTable, 5, {1, 1, 10, 1, 3}},
#define   IEEE8021BRIDGEEVBSBPVDPOPERRSRCWAITDELAY  11
	{(u_char) IEEE8021BRIDGEEVBSBPVDPOPERRSRCWAITDELAY, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbSbpTable, 5, {1, 1, 10, 1, 4}},
#define   IEEE8021BRIDGEEVBSBPVDPOPERREINITKEEPALIVE  12
	{(u_char) IEEE8021BRIDGEEVBSBPVDPOPERREINITKEEPALIVE, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbSbpTable, 5, {1, 1, 10, 1, 5}},
#define   IEEE8021BRIDGEEVBSBPVDPOPERTOUTKEEPALIVE  13
	{(u_char) IEEE8021BRIDGEEVBSBPVDPOPERTOUTKEEPALIVE, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbSbpTable, 5, {1, 1, 10, 1, 6}},
#define   IEEE8021BRIDGEEVBVSITIMESINCECREATE  14
	{(u_char) IEEE8021BRIDGEEVBVSITIMESINCECREATE, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 5}},
#define   IEEE8021BRIDGEEVBVSIVDPOPERCMD  15
	{(u_char) IEEE8021BRIDGEEVBVSIVDPOPERCMD, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 6}},
#define   IEEE8021BRIDGEEVBVSIOPERREVERT  16
	{(u_char) IEEE8021BRIDGEEVBVSIOPERREVERT, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 7}},
#define   IEEE8021BRIDGEEVBVSIOPERHARD  17
	{(u_char) IEEE8021BRIDGEEVBVSIOPERHARD, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 8}},
#define   IEEE8021BRIDGEEVBVSIOPERREASON  18
	{(u_char) IEEE8021BRIDGEEVBVSIOPERREASON, ASN_OCTET_STR, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 9}},
#define   IEEE8021BRIDGEEVBVSIMGRID  19
	{(u_char) IEEE8021BRIDGEEVBVSIMGRID, ASN_OCTET_STR, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 10}},
#define   IEEE8021BRIDGEEVBVSITYPE  20
	{(u_char) IEEE8021BRIDGEEVBVSITYPE, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 11}},
#define   IEEE8021BRIDGEEVBVSITYPEVERSION  21
	{(u_char) IEEE8021BRIDGEEVBVSITYPEVERSION, ASN_OCTET_STR, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 12}},
#define   IEEE8021BRIDGEEVBVSIMVFORMAT  22
	{(u_char) IEEE8021BRIDGEEVBVSIMVFORMAT, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 13}},
#define   IEEE8021BRIDGEEVBVSINUMMACS  23
	{(u_char) IEEE8021BRIDGEEVBVSINUMMACS, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 14}},
#define   IEEE8021BRIDGEEVBVDPMACHINESTATE  24
	{(u_char) IEEE8021BRIDGEEVBVDPMACHINESTATE, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 15}},
#define   IEEE8021BRIDGEEVBVDPCOMMANDSSUCCEEDED  25
	{(u_char) IEEE8021BRIDGEEVBVDPCOMMANDSSUCCEEDED, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 16}},
#define   IEEE8021BRIDGEEVBVDPCOMMANDSFAILED  26
	{(u_char) IEEE8021BRIDGEEVBVDPCOMMANDSFAILED, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 17}},
#define   IEEE8021BRIDGEEVBVDPCOMMANDREVERTS  27
	{(u_char) IEEE8021BRIDGEEVBVDPCOMMANDREVERTS, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 18}},
#define   IEEE8021BRIDGEEVBVDPCOUNTERDISCONTINUITY  28
	{(u_char) IEEE8021BRIDGEEVBVDPCOUNTERDISCONTINUITY, ASN_TIMETICKS, RONLY, var_ieee8021BridgeEvbVSIDBTable, 5, {1, 2, 1, 1, 19}},
#define   IEEE8021BRIDGEEVBUAPCOMPONENTID  30
	{(u_char) IEEE8021BRIDGEEVBUAPCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 1}},
#define   IEEE8021BRIDGEEVBUAPPORT  31
	{(u_char) IEEE8021BRIDGEEVBUAPPORT, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 2}},
#define   IEEE8021BRIDGEEVBUAPCONFIGIFINDEX  32
	{(u_char) IEEE8021BRIDGEEVBUAPCONFIGIFINDEX, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 3}},
#define   IEEE8021BRIDGEEVBUAPSCHCDCPADMINENABLE  33
	{(u_char) IEEE8021BRIDGEEVBUAPSCHCDCPADMINENABLE, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 4}},
#define   IEEE8021BRIDGEEVBUAPSCHADMINCDCPROLE  34
	{(u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPROLE, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 5}},
#define   IEEE8021BRIDGEEVBUAPSCHADMINCDCPCHANCAP  35
	{(u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPCHANCAP, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 6}},
#define   IEEE8021BRIDGEEVBUAPSCHOPERCDCPCHANCAP  36
	{(u_char) IEEE8021BRIDGEEVBUAPSCHOPERCDCPCHANCAP, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 7}},
#define   IEEE8021BRIDGEEVBUAPSCHADMINCDCPSVIDPOOLLOW  37
	{(u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPSVIDPOOLLOW, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 8}},
#define   IEEE8021BRIDGEEVBUAPSCHADMINCDCPSVIDPOOLHIGH  38
	{(u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPSVIDPOOLHIGH, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 9}},
#define   IEEE8021BRIDGEEVBUAPSCHOPERSTATE  39
	{(u_char) IEEE8021BRIDGEEVBUAPSCHOPERSTATE, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 10}},
#define   IEEE8021BRIDGEEVBSCHCDCPREMOTEENABLED  40
	{(u_char) IEEE8021BRIDGEEVBSCHCDCPREMOTEENABLED, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 11}},
#define   IEEE8021BRIDGEEVBSCHCDCPREMOTEROLE  41
	{(u_char) IEEE8021BRIDGEEVBSCHCDCPREMOTEROLE, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 12}},
#define   IEEE8021BRIDGEEVBUAPCONFIGSTORAGETYPE  42
	{(u_char) IEEE8021BRIDGEEVBUAPCONFIGSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 13}},
#define   IEEE8021BRIDGEEVBUAPCONFIGROWSTATUS  43
	{(u_char) IEEE8021BRIDGEEVBUAPCONFIGROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbUAPConfigTable, 5, {1, 3, 1, 1, 14}},
#define   IEEE8021BRIDGEEVBCAPCOMPONENTID  44
	{(u_char) IEEE8021BRIDGEEVBCAPCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 2}},
#define   IEEE8021BRIDGEEVBCAPCONFIGIFINDEX  45
	{(u_char) IEEE8021BRIDGEEVBCAPCONFIGIFINDEX, ASN_INTEGER, RONLY, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 3}},
#define   IEEE8021BRIDGEEVBCAPPORT  46
	{(u_char) IEEE8021BRIDGEEVBCAPPORT, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 4}},
#define   IEEE8021BRIDGEEVBCAPSCHANNELID  47
	{(u_char) IEEE8021BRIDGEEVBCAPSCHANNELID, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 5}},
#define   IEEE8021BRIDGEEVBCAPASSOCIATESBPORURPCOMPID  48
	{(u_char) IEEE8021BRIDGEEVBCAPASSOCIATESBPORURPCOMPID, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 6}},
#define   IEEE8021BRIDGEEVBCAPASSOCIATESBPORURPPORT  49
	{(u_char) IEEE8021BRIDGEEVBCAPASSOCIATESBPORURPPORT, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 7}},
#define   IEEE8021BRIDGEEVBCAPROWSTATUS  50
	{(u_char) IEEE8021BRIDGEEVBCAPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbCAPConfigTable, 5, {1, 3, 2, 1, 8}},
#define   IEEE8021BRIDGEEVBURPIFINDEX  51
	{(u_char) IEEE8021BRIDGEEVBURPIFINDEX, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbURPTable, 5, {1, 3, 3, 1, 3}},
#define   IEEE8021BRIDGEEVBURPBINDTOISSPORT  52
	{(u_char) IEEE8021BRIDGEEVBURPBINDTOISSPORT, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbURPTable, 5, {1, 3, 3, 1, 4}},
#define   IEEE8021BRIDGEEVBURPLLDPMANUAL  53
	{(u_char) IEEE8021BRIDGEEVBURPLLDPMANUAL, ASN_INTEGER, RWRITE, var_ieee8021BridgeEvbURPTable, 5, {1, 3, 3, 1, 6}},
#define   IEEE8021BRIDGEEVBURPVDPOPERRSRCWAITDELAY  54
	{(u_char) IEEE8021BRIDGEEVBURPVDPOPERRSRCWAITDELAY, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbURPTable, 5, {1, 3, 3, 1, 9}},
#define   IEEE8021BRIDGEEVBURPVDPOPERRESPWAITDELAY  55
	{(u_char) IEEE8021BRIDGEEVBURPVDPOPERRESPWAITDELAY, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbURPTable, 5, {1, 3, 3, 1, 10}},
#define   IEEE8021BRIDGEEVBURPVDPOPERREINITKEEPALIVE  56
	{(u_char) IEEE8021BRIDGEEVBURPVDPOPERREINITKEEPALIVE, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeEvbURPTable, 5, {1, 3, 3, 1, 11}},
#define   IEEE8021BRIDGEEVBECPOPERACKTIMERINIT  57
	{(u_char) IEEE8021BRIDGEEVBECPOPERACKTIMERINIT, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbEcpTable, 5, {1, 3, 4, 1, 3}},
#define   IEEE8021BRIDGEEVBECPOPERMAXRETRIES  58
	{(u_char) IEEE8021BRIDGEEVBECPOPERMAXRETRIES, ASN_UNSIGNED, RONLY, var_ieee8021BridgeEvbEcpTable, 5, {1, 3, 4, 1, 4}},
#define   IEEE8021BRIDGEEVBECPTXFRAMECOUNT  59
	{(u_char) IEEE8021BRIDGEEVBECPTXFRAMECOUNT, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbEcpTable, 5, {1, 3, 4, 1, 5}},
#define   IEEE8021BRIDGEEVBECPTXRETRYCOUNT  60
	{(u_char) IEEE8021BRIDGEEVBECPTXRETRYCOUNT, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbEcpTable, 5, {1, 3, 4, 1, 6}},
#define   IEEE8021BRIDGEEVBECPTXFAILURES  61
	{(u_char) IEEE8021BRIDGEEVBECPTXFAILURES, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbEcpTable, 5, {1, 3, 4, 1, 7}},
#define   IEEE8021BRIDGEEVBECPRXFRAMECOUNT  62
	{(u_char) IEEE8021BRIDGEEVBECPRXFRAMECOUNT, ASN_COUNTER, RONLY, var_ieee8021BridgeEvbEcpTable, 5, {1, 3, 4, 1, 8}},
};

/* (L = length of the oidsuffix) */
struct ieee8021BridgeEvbMib_data *ieee8021BridgeEvbMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021BridgeEvbSbpTableStorage = NULL;
struct header_complex_index *ieee8021BridgeEvbVSIDBTableStorage = NULL;
struct header_complex_index *ieee8021BridgeEvbVSIDBMacTableStorage = NULL;
struct header_complex_index *ieee8021BridgeEvbUAPConfigTableStorage = NULL;
struct header_complex_index *ieee8021BridgeEvbCAPConfigTableStorage = NULL;
struct header_complex_index *ieee8021BridgeEvbURPTableStorage = NULL;
struct header_complex_index *ieee8021BridgeEvbEcpTableStorage = NULL;

void (*ieee8021BridgeEvbMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021BridgeEvbMib_loop_handler(int);
void ieee8021BridgeEvbMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021BridgeEvbMib(void)
 * @brief ieee8021BridgeEvbMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021BridgeEvbMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021BridgeEvbMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "init_ieee8021BridgeEvbMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021BridgeEvbMib", ieee8021BridgeEvbMib_variables, variable7, ieee8021BridgeEvbMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021BridgeEvbMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021BridgeEvbMib", parse_ieee8021BridgeEvbMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbSbpTable", parse_ieee8021BridgeEvbSbpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbVSIDBTable", parse_ieee8021BridgeEvbVSIDBTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbVSIDBMacTable", parse_ieee8021BridgeEvbVSIDBMacTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbUAPConfigTable", parse_ieee8021BridgeEvbUAPConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbCAPConfigTable", parse_ieee8021BridgeEvbCAPConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbURPTable", parse_ieee8021BridgeEvbURPTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeEvbEcpTable", parse_ieee8021BridgeEvbEcpTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbSbpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbVSIDBTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbVSIDBMacTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbUAPConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbCAPConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbURPTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeEvbEcpTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021BridgeEvbMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021BridgeEvbMib_fd_handler, (void *) 1);
	}
	ieee8021BridgeEvbMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021BridgeEvbMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021BridgeEvbMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021BridgeEvbMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021BridgeEvbMib(void)
{
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "deinit_ieee8021BridgeEvbMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021BridgeEvbMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021BridgeEvbMib_variables_oid, sizeof(ieee8021BridgeEvbMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021BridgeEvbMib");
	snmpd_unregister_config_handler("ieee8021BridgeEvbSbpTable");
	snmpd_unregister_config_handler("ieee8021BridgeEvbVSIDBTable");
	snmpd_unregister_config_handler("ieee8021BridgeEvbVSIDBMacTable");
	snmpd_unregister_config_handler("ieee8021BridgeEvbUAPConfigTable");
	snmpd_unregister_config_handler("ieee8021BridgeEvbCAPConfigTable");
	snmpd_unregister_config_handler("ieee8021BridgeEvbURPTable");
	snmpd_unregister_config_handler("ieee8021BridgeEvbEcpTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

int
term_ieee8021BridgeEvbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "term_ieee8021BridgeEvbMib: terminating...  "));
	deinit_ieee8021BridgeEvbMib();
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021BridgeEvbMib_data *ieee8021BridgeEvbMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021BridgeEvbMib.
 *
 * Creates a new ieee8021BridgeEvbMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021BridgeEvbMib.
 */
struct ieee8021BridgeEvbMib_data *
ieee8021BridgeEvbMib_create(void)
{
	struct ieee8021BridgeEvbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbMib_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->ieee8021BridgeEvbSysType = 0;
		StorageNew->ieee8021BridgeEvbSysNumExternalPorts = 0;
		StorageNew->ieee8021BridgeEvbSysEvbLldpTxEnable = TV_TRUE;
		StorageNew->ieee8021BridgeEvbSysEvbLldpManual = TV_FALSE;
		StorageNew->ieee8021BridgeEvbSysEvbLldpGidCapable = 0;
		StorageNew->ieee8021BridgeEvbSysEcpAckTimer = 0;
		StorageNew->ieee8021BridgeEvbSysEcpMaxRetries = 3;
		StorageNew->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay = 0;
		StorageNew->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive = 0;

	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbMib_data *ieee8021BridgeEvbMib_duplicate(struct ieee8021BridgeEvbMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbMib_data *
ieee8021BridgeEvbMib_duplicate(struct ieee8021BridgeEvbMib_data *thedata)
{
	struct ieee8021BridgeEvbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbMib_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbSysType = thedata->ieee8021BridgeEvbSysType;
		StorageNew->ieee8021BridgeEvbSysNumExternalPorts = thedata->ieee8021BridgeEvbSysNumExternalPorts;
		StorageNew->ieee8021BridgeEvbSysEvbLldpTxEnable = thedata->ieee8021BridgeEvbSysEvbLldpTxEnable;
		StorageNew->ieee8021BridgeEvbSysEvbLldpManual = thedata->ieee8021BridgeEvbSysEvbLldpManual;
		StorageNew->ieee8021BridgeEvbSysEvbLldpGidCapable = thedata->ieee8021BridgeEvbSysEvbLldpGidCapable;
		StorageNew->ieee8021BridgeEvbSysEcpAckTimer = thedata->ieee8021BridgeEvbSysEcpAckTimer;
		StorageNew->ieee8021BridgeEvbSysEcpMaxRetries = thedata->ieee8021BridgeEvbSysEcpMaxRetries;
		StorageNew->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay = thedata->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay;
		StorageNew->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive = thedata->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbMib_destroy(struct ieee8021BridgeEvbMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021BridgeEvbMib.
 * @brief delete a scalars structure from ieee8021BridgeEvbMib.
 *
 * Frees scalars that were previously removed from ieee8021BridgeEvbMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbMib_destroy(struct ieee8021BridgeEvbMib_data **thedata)
{
	struct ieee8021BridgeEvbMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbMib_add(struct ieee8021BridgeEvbMib_data *thedata)
 * @param thedata the structure representing ieee8021BridgeEvbMib scalars.
 * @brief adds node to the ieee8021BridgeEvbMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021BridgeEvbMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021BridgeEvbMib_add(struct ieee8021BridgeEvbMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbMib_add: adding data...  "));
	if (thedata)
		ieee8021BridgeEvbMibStorage = thedata;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbMib_data *StorageTmp = ieee8021BridgeEvbMib_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysType, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSysNumExternalPorts, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysEvbLldpManual, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysEcpAckTimer, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive, &tmpsize);
	ieee8021BridgeEvbMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbMib: storing data...  "));
	refresh_ieee8021BridgeEvbMib(1);
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021BridgeEvbMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021BridgeEvbMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysType, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSysNumExternalPorts, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysEvbLldpManual, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysEcpAckTimer, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021BridgeEvbMib(struct ieee8021BridgeEvbMib_data *StorageTmp, struct ieee8021BridgeEvbMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021BridgeEvbMib(struct ieee8021BridgeEvbMib_data *StorageTmp, struct ieee8021BridgeEvbMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbMib(struct ieee8021BridgeEvbMib_data *StorageTmp, struct ieee8021BridgeEvbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021BridgeEvbMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbMib(struct ieee8021BridgeEvbMib_data *StorageTmp, struct ieee8021BridgeEvbMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021BridgeEvbMib(struct 
 * @fn void revert_ieee8021BridgeEvbMib(struct ieee8021BridgeEvbMib_data *StorageTmp, struct ieee8021BridgeEvbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021BridgeEvbMib(struct ieee8021BridgeEvbMib_data *StorageTmp, struct ieee8021BridgeEvbMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021BridgeEvbMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021BridgeEvbMib(int force)
{
	if (ieee8021BridgeEvbMibStorage == NULL) {
		struct ieee8021BridgeEvbMib_data *StorageNew;

		if ((StorageNew = ieee8021BridgeEvbMib_create()) == NULL)
			return;
		ieee8021BridgeEvbMibStorage = StorageNew;
		ieee8021BridgeEvbMib_refresh = 1;
	}
	if (!force && ieee8021BridgeEvbMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "refresh_ieee8021BridgeEvbMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021BridgeEvbMib_refresh = 0;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021BridgeEvbMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021BridgeEvbMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021BridgeEvbMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021BridgeEvbMib(0);
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021BridgeEvbMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBSYSTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysType);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysType;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSNUMEXTERNALPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysNumExternalPorts);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysNumExternalPorts;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSEVBLLDPTXENABLE:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysEvbLldpTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSEVBLLDPMANUAL:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysEvbLldpManual;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysEvbLldpManual);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysEvbLldpManual;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSEVBLLDPGIDCAPABLE:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysEvbLldpGidCapable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSECPACKTIMER:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysEcpAckTimer;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysEcpAckTimer);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysEcpAckTimer;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSECPMAXRETRIES:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysEcpMaxRetries;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSVDPDFLTRSRCWAITDELAY:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay;
		break;
	case (u_char) IEEE8021BRIDGEEVBSYSVDPDFLTREINITKEEPALIVE:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSysVdpDfltReinitKeepAlive;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021BridgeEvbMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021BridgeEvbMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021BridgeEvbSbpTable_data *ieee8021BridgeEvbSbpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbSbpTable table.
 *
 * Creates a new ieee8021BridgeEvbSbpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbSbpTable_data *
ieee8021BridgeEvbSbpTable_create(void)
{
	struct ieee8021BridgeEvbSbpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbSbpTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbSbpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeEvbSbpLldpManual = 0;
		StorageNew->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay = 0;
		StorageNew->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive = 0;
		StorageNew->ieee8021BridgeEvbSbpVdpOperToutKeepAlive = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbSbpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbSbpTable_data *ieee8021BridgeEvbSbpTable_duplicate(struct ieee8021BridgeEvbSbpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbSbpTable_data *
ieee8021BridgeEvbSbpTable_duplicate(struct ieee8021BridgeEvbSbpTable_data *thedata)
{
	struct ieee8021BridgeEvbSbpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbSbpTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbSbpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbSbpTable_id = thedata->ieee8021BridgeEvbSbpTable_id;
		StorageNew->ieee8021BridgeEvbSbpComponentID = thedata->ieee8021BridgeEvbSbpComponentID;
		StorageNew->ieee8021BridgeEvbSbpPortNumber = thedata->ieee8021BridgeEvbSbpPortNumber;
		StorageNew->ieee8021BridgeEvbSbpLldpManual = thedata->ieee8021BridgeEvbSbpLldpManual;
		StorageNew->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay = thedata->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay;
		StorageNew->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive = thedata->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive;
		StorageNew->ieee8021BridgeEvbSbpVdpOperToutKeepAlive = thedata->ieee8021BridgeEvbSbpVdpOperToutKeepAlive;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbSbpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbSbpTable_destroy(struct ieee8021BridgeEvbSbpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbSbpTable_destroy(struct ieee8021BridgeEvbSbpTable_data **thedata)
{
	struct ieee8021BridgeEvbSbpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbSbpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbSbpTable_add(struct ieee8021BridgeEvbSbpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbSbpTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbSbpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbSbpTable_add(struct ieee8021BridgeEvbSbpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbSbpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeEvbSbpComponentID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbSbpComponentID, sizeof(thedata->ieee8021BridgeEvbSbpComponentID));
		/* ieee8021BridgeEvbSbpPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbSbpPortNumber, sizeof(thedata->ieee8021BridgeEvbSbpPortNumber));
		header_complex_add_data(&ieee8021BridgeEvbSbpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbSbpTable_del(struct ieee8021BridgeEvbSbpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbSbpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbSbpTable_del(struct ieee8021BridgeEvbSbpTable_data *thedata)
{
	struct ieee8021BridgeEvbSbpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbSbpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbSbpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbSbpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbSbpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbSbpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbSbpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbSbpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbSbpTable_data *StorageTmp = ieee8021BridgeEvbSbpTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbSbpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSbpComponentID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSbpPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSbpLldpManual, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSbpVdpOperToutKeepAlive, &tmpsize);
	ieee8021BridgeEvbSbpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbSbpTable(): store configuraiton file for ieee8021BridgeEvbSbpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbSbpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbSbpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbSbpTable: storing data...  "));
	refresh_ieee8021BridgeEvbSbpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbSbpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbSbpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbSbpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSbpComponentID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSbpPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSbpLldpManual, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSbpVdpOperToutKeepAlive, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeEvbVSIDBTable_data *ieee8021BridgeEvbVSIDBTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbVSIDBTable table.
 *
 * Creates a new ieee8021BridgeEvbVSIDBTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbVSIDBTable_data *
ieee8021BridgeEvbVSIDBTable_create(void)
{
	struct ieee8021BridgeEvbVSIDBTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbVSIDBTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeEvbVSITimeSinceCreate = 0;
		StorageNew->ieee8021BridgeEvbVsiVdpOperCmd = 0;
		StorageNew->ieee8021BridgeEvbVsiOperRevert = 0;
		StorageNew->ieee8021BridgeEvbVsiOperHard = 0;
		if (memdup((u_char **) &StorageNew->ieee8021BridgeEvbVsiOperReason, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021BridgeEvbVsiOperReasonLen = 1;
		if ((StorageNew->ieee8021BridgeEvbVSIMgrID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgeEvbVSIMgrIDLen = 0;
		StorageNew->ieee8021BridgeEvbVSIMgrID[StorageNew->ieee8021BridgeEvbVSIMgrIDLen] = 0;
		StorageNew->ieee8021BridgeEvbVSIType = 0;
		if ((StorageNew->ieee8021BridgeEvbVSITypeVersion = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgeEvbVSITypeVersionLen = 0;
		StorageNew->ieee8021BridgeEvbVSITypeVersion[StorageNew->ieee8021BridgeEvbVSITypeVersionLen] = 0;
		StorageNew->ieee8021BridgeEvbVSIMvFormat = 0;
		StorageNew->ieee8021BridgeEvbVSINumMACs = 0;
		StorageNew->ieee8021BridgeEvbVDPMachineState = 0;
		StorageNew->ieee8021BridgeEvbVDPCommandsSucceeded = 0;
		StorageNew->ieee8021BridgeEvbVDPCommandsFailed = 0;
		StorageNew->ieee8021BridgeEvbVDPCommandReverts = 0;
		StorageNew->ieee8021BridgeEvbVDPCounterDiscontinuity = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbVSIDBTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbVSIDBTable_data *ieee8021BridgeEvbVSIDBTable_duplicate(struct ieee8021BridgeEvbVSIDBTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbVSIDBTable_data *
ieee8021BridgeEvbVSIDBTable_duplicate(struct ieee8021BridgeEvbVSIDBTable_data *thedata)
{
	struct ieee8021BridgeEvbVSIDBTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbVSIDBTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbVSIDBTable_id = thedata->ieee8021BridgeEvbVSIDBTable_id;
		StorageNew->ieee8021BridgeEvbVSIComponentID = thedata->ieee8021BridgeEvbVSIComponentID;
		StorageNew->ieee8021BridgeEvbVSIPortNumber = thedata->ieee8021BridgeEvbVSIPortNumber;
		StorageNew->ieee8021BridgeEvbVSIIDType = thedata->ieee8021BridgeEvbVSIIDType;
		if (!(StorageNew->ieee8021BridgeEvbVSIID = malloc(thedata->ieee8021BridgeEvbVSIIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeEvbVSIID, thedata->ieee8021BridgeEvbVSIID, thedata->ieee8021BridgeEvbVSIIDLen);
		StorageNew->ieee8021BridgeEvbVSIIDLen = thedata->ieee8021BridgeEvbVSIIDLen;
		StorageNew->ieee8021BridgeEvbVSIID[StorageNew->ieee8021BridgeEvbVSIIDLen] = 0;
		StorageNew->ieee8021BridgeEvbVSITimeSinceCreate = thedata->ieee8021BridgeEvbVSITimeSinceCreate;
		StorageNew->ieee8021BridgeEvbVsiVdpOperCmd = thedata->ieee8021BridgeEvbVsiVdpOperCmd;
		StorageNew->ieee8021BridgeEvbVsiOperRevert = thedata->ieee8021BridgeEvbVsiOperRevert;
		StorageNew->ieee8021BridgeEvbVsiOperHard = thedata->ieee8021BridgeEvbVsiOperHard;
		if (!(StorageNew->ieee8021BridgeEvbVsiOperReason = malloc(thedata->ieee8021BridgeEvbVsiOperReasonLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeEvbVsiOperReason, thedata->ieee8021BridgeEvbVsiOperReason, thedata->ieee8021BridgeEvbVsiOperReasonLen);
		StorageNew->ieee8021BridgeEvbVsiOperReasonLen = thedata->ieee8021BridgeEvbVsiOperReasonLen;
		StorageNew->ieee8021BridgeEvbVsiOperReason[StorageNew->ieee8021BridgeEvbVsiOperReasonLen] = 0;
		if (!(StorageNew->ieee8021BridgeEvbVSIMgrID = malloc(thedata->ieee8021BridgeEvbVSIMgrIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeEvbVSIMgrID, thedata->ieee8021BridgeEvbVSIMgrID, thedata->ieee8021BridgeEvbVSIMgrIDLen);
		StorageNew->ieee8021BridgeEvbVSIMgrIDLen = thedata->ieee8021BridgeEvbVSIMgrIDLen;
		StorageNew->ieee8021BridgeEvbVSIMgrID[StorageNew->ieee8021BridgeEvbVSIMgrIDLen] = 0;
		StorageNew->ieee8021BridgeEvbVSIType = thedata->ieee8021BridgeEvbVSIType;
		if (!(StorageNew->ieee8021BridgeEvbVSITypeVersion = malloc(thedata->ieee8021BridgeEvbVSITypeVersionLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeEvbVSITypeVersion, thedata->ieee8021BridgeEvbVSITypeVersion, thedata->ieee8021BridgeEvbVSITypeVersionLen);
		StorageNew->ieee8021BridgeEvbVSITypeVersionLen = thedata->ieee8021BridgeEvbVSITypeVersionLen;
		StorageNew->ieee8021BridgeEvbVSITypeVersion[StorageNew->ieee8021BridgeEvbVSITypeVersionLen] = 0;
		StorageNew->ieee8021BridgeEvbVSIMvFormat = thedata->ieee8021BridgeEvbVSIMvFormat;
		StorageNew->ieee8021BridgeEvbVSINumMACs = thedata->ieee8021BridgeEvbVSINumMACs;
		StorageNew->ieee8021BridgeEvbVDPMachineState = thedata->ieee8021BridgeEvbVDPMachineState;
		StorageNew->ieee8021BridgeEvbVDPCommandsSucceeded = thedata->ieee8021BridgeEvbVDPCommandsSucceeded;
		StorageNew->ieee8021BridgeEvbVDPCommandsFailed = thedata->ieee8021BridgeEvbVDPCommandsFailed;
		StorageNew->ieee8021BridgeEvbVDPCommandReverts = thedata->ieee8021BridgeEvbVDPCommandReverts;
		StorageNew->ieee8021BridgeEvbVDPCounterDiscontinuity = thedata->ieee8021BridgeEvbVDPCounterDiscontinuity;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbVSIDBTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbVSIDBTable_destroy(struct ieee8021BridgeEvbVSIDBTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbVSIDBTable_destroy(struct ieee8021BridgeEvbVSIDBTable_data **thedata)
{
	struct ieee8021BridgeEvbVSIDBTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021BridgeEvbVSIID);
		StorageDel->ieee8021BridgeEvbVSIIDLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeEvbVsiOperReason);
		StorageDel->ieee8021BridgeEvbVsiOperReasonLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeEvbVSIMgrID);
		StorageDel->ieee8021BridgeEvbVSIMgrIDLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeEvbVSITypeVersion);
		StorageDel->ieee8021BridgeEvbVSITypeVersionLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbVSIDBTable_add(struct ieee8021BridgeEvbVSIDBTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbVSIDBTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbVSIDBTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbVSIDBTable_add(struct ieee8021BridgeEvbVSIDBTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeEvbVSIComponentID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbVSIComponentID, sizeof(thedata->ieee8021BridgeEvbVSIComponentID));
		/* ieee8021BridgeEvbVSIPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbVSIPortNumber, sizeof(thedata->ieee8021BridgeEvbVSIPortNumber));
		/* ieee8021BridgeEvbVSIIDType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgeEvbVSIIDType, sizeof(thedata->ieee8021BridgeEvbVSIIDType));
		/* ieee8021BridgeEvbVSIID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021BridgeEvbVSIID, thedata->ieee8021BridgeEvbVSIIDLen);
		header_complex_add_data(&ieee8021BridgeEvbVSIDBTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbVSIDBTable_del(struct ieee8021BridgeEvbVSIDBTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbVSIDBTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbVSIDBTable_del(struct ieee8021BridgeEvbVSIDBTable_data *thedata)
{
	struct ieee8021BridgeEvbVSIDBTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbVSIDBTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbVSIDBTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbVSIDBTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbVSIDBTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbVSIDBTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbVSIDBTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp = ieee8021BridgeEvbVSIDBTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbVSIDBTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbVSIComponentID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbVSIPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVSIIDType, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeEvbVSIID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeEvbVSIID, &StorageTmp->ieee8021BridgeEvbVSIIDLen);
	if (StorageTmp->ieee8021BridgeEvbVSIID == NULL) {
		config_perror("invalid specification for ieee8021BridgeEvbVSIID");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbVSITimeSinceCreate, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVsiVdpOperCmd, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVsiOperRevert, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVsiOperHard, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeEvbVsiOperReason);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeEvbVsiOperReason, &StorageTmp->ieee8021BridgeEvbVsiOperReasonLen);
	if (StorageTmp->ieee8021BridgeEvbVsiOperReason == NULL) {
		config_perror("invalid specification for ieee8021BridgeEvbVsiOperReason");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021BridgeEvbVSIMgrID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeEvbVSIMgrID, &StorageTmp->ieee8021BridgeEvbVSIMgrIDLen);
	if (StorageTmp->ieee8021BridgeEvbVSIMgrID == NULL) {
		config_perror("invalid specification for ieee8021BridgeEvbVSIMgrID");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVSIType, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeEvbVSITypeVersion);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeEvbVSITypeVersion, &StorageTmp->ieee8021BridgeEvbVSITypeVersionLen);
	if (StorageTmp->ieee8021BridgeEvbVSITypeVersion == NULL) {
		config_perror("invalid specification for ieee8021BridgeEvbVSITypeVersion");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVSIMvFormat, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVSINumMACs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVDPMachineState, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbVDPCommandsSucceeded, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbVDPCommandsFailed, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbVDPCommandReverts, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021BridgeEvbVDPCounterDiscontinuity, &tmpsize);
	ieee8021BridgeEvbVSIDBTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbVSIDBTable(): store configuraiton file for ieee8021BridgeEvbVSIDBTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbVSIDBTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbVSIDBTable: storing data...  "));
	refresh_ieee8021BridgeEvbVSIDBTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbVSIDBTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbVSIDBTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbVSIDBTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbVSIComponentID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbVSIPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVSIIDType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeEvbVSIID, &StorageTmp->ieee8021BridgeEvbVSIIDLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbVSITimeSinceCreate, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVsiVdpOperCmd, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVsiOperRevert, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVsiOperHard, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeEvbVsiOperReason, &StorageTmp->ieee8021BridgeEvbVsiOperReasonLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeEvbVSIMgrID, &StorageTmp->ieee8021BridgeEvbVSIMgrIDLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVSIType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeEvbVSITypeVersion, &StorageTmp->ieee8021BridgeEvbVSITypeVersionLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVSIMvFormat, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVSINumMACs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVDPMachineState, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbVDPCommandsSucceeded, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbVDPCommandsFailed, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbVDPCommandReverts, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021BridgeEvbVDPCounterDiscontinuity, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeEvbVSIDBMacTable_data *ieee8021BridgeEvbVSIDBMacTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbVSIDBMacTable table.
 *
 * Creates a new ieee8021BridgeEvbVSIDBMacTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbVSIDBMacTable_data *
ieee8021BridgeEvbVSIDBMacTable_create(void)
{
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbVSIDBMacTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBMacTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeEvbVSIComponentID = 0;
		StorageNew->ieee8021BridgeEvbVSIPortNumber = 0;
		StorageNew->ieee8021BridgeEvbVSIIDType = 0;
		if ((StorageNew->ieee8021BridgeEvbVSIID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgeEvbVSIIDLen = 0;
		StorageNew->ieee8021BridgeEvbVSIID[StorageNew->ieee8021BridgeEvbVSIIDLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbVSIDBMacTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbVSIDBMacTable_data *ieee8021BridgeEvbVSIDBMacTable_duplicate(struct ieee8021BridgeEvbVSIDBMacTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbVSIDBMacTable_data *
ieee8021BridgeEvbVSIDBMacTable_duplicate(struct ieee8021BridgeEvbVSIDBMacTable_data *thedata)
{
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbVSIDBMacTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBMacTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbVSIDBMacTable_id = thedata->ieee8021BridgeEvbVSIDBMacTable_id;
		StorageNew->ieee8021BridgeEvbVSIComponentID = thedata->ieee8021BridgeEvbVSIComponentID;
		StorageNew->ieee8021BridgeEvbVSIPortNumber = thedata->ieee8021BridgeEvbVSIPortNumber;
		StorageNew->ieee8021BridgeEvbVSIIDType = thedata->ieee8021BridgeEvbVSIIDType;
		if (!(StorageNew->ieee8021BridgeEvbVSIID = malloc(thedata->ieee8021BridgeEvbVSIIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeEvbVSIID, thedata->ieee8021BridgeEvbVSIID, thedata->ieee8021BridgeEvbVSIIDLen);
		StorageNew->ieee8021BridgeEvbVSIIDLen = thedata->ieee8021BridgeEvbVSIIDLen;
		StorageNew->ieee8021BridgeEvbVSIID[StorageNew->ieee8021BridgeEvbVSIIDLen] = 0;
		StorageNew->ieee8021BridgeEvbGroupID = thedata->ieee8021BridgeEvbGroupID;
		if (!(StorageNew->ieee8021BridgeEvbVSIMac = malloc(thedata->ieee8021BridgeEvbVSIMacLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeEvbVSIMac, thedata->ieee8021BridgeEvbVSIMac, thedata->ieee8021BridgeEvbVSIMacLen);
		StorageNew->ieee8021BridgeEvbVSIMacLen = thedata->ieee8021BridgeEvbVSIMacLen;
		StorageNew->ieee8021BridgeEvbVSIMac[StorageNew->ieee8021BridgeEvbVSIMacLen] = 0;
		StorageNew->ieee8021BridgeEvbVSIVlanId = thedata->ieee8021BridgeEvbVSIVlanId;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbVSIDBMacTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbVSIDBMacTable_destroy(struct ieee8021BridgeEvbVSIDBMacTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbVSIDBMacTable_destroy(struct ieee8021BridgeEvbVSIDBMacTable_data **thedata)
{
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBMacTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021BridgeEvbVSIID);
		StorageDel->ieee8021BridgeEvbVSIIDLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeEvbVSIMac);
		StorageDel->ieee8021BridgeEvbVSIMacLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbVSIDBMacTable_add(struct ieee8021BridgeEvbVSIDBMacTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbVSIDBMacTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbVSIDBMacTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbVSIDBMacTable_add(struct ieee8021BridgeEvbVSIDBMacTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBMacTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeEvbVSIComponentID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbVSIComponentID, sizeof(thedata->ieee8021BridgeEvbVSIComponentID));
		/* ieee8021BridgeEvbVSIPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbVSIPortNumber, sizeof(thedata->ieee8021BridgeEvbVSIPortNumber));
		/* ieee8021BridgeEvbVSIIDType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgeEvbVSIIDType, sizeof(thedata->ieee8021BridgeEvbVSIIDType));
		/* ieee8021BridgeEvbVSIID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021BridgeEvbVSIID, thedata->ieee8021BridgeEvbVSIIDLen);
		/* ieee8021BridgeEvbGroupID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbGroupID, sizeof(thedata->ieee8021BridgeEvbGroupID));
		/* ieee8021BridgeEvbVSIMac */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021BridgeEvbVSIMac, thedata->ieee8021BridgeEvbVSIMacLen);
		/* ieee8021BridgeEvbVSIVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbVSIVlanId, sizeof(thedata->ieee8021BridgeEvbVSIVlanId));
		header_complex_add_data(&ieee8021BridgeEvbVSIDBMacTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbVSIDBMacTable_del(struct ieee8021BridgeEvbVSIDBMacTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbVSIDBMacTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbVSIDBMacTable_del(struct ieee8021BridgeEvbVSIDBMacTable_data *thedata)
{
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbVSIDBMacTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbVSIDBMacTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbVSIDBMacTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbVSIDBMacTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbVSIDBMacTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbVSIDBMacTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbVSIDBMacTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp = ieee8021BridgeEvbVSIDBMacTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbVSIDBMacTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbVSIComponentID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbVSIPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbVSIIDType, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeEvbVSIID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeEvbVSIID, &StorageTmp->ieee8021BridgeEvbVSIIDLen);
	if (StorageTmp->ieee8021BridgeEvbVSIID == NULL) {
		config_perror("invalid specification for ieee8021BridgeEvbVSIID");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbGroupID, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeEvbVSIMac);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeEvbVSIMac, &StorageTmp->ieee8021BridgeEvbVSIMacLen);
	if (StorageTmp->ieee8021BridgeEvbVSIMac == NULL) {
		config_perror("invalid specification for ieee8021BridgeEvbVSIMac");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbVSIVlanId, &tmpsize);
	ieee8021BridgeEvbVSIDBMacTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbVSIDBMacTable(): store configuraiton file for ieee8021BridgeEvbVSIDBMacTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbVSIDBMacTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbVSIDBMacTable: storing data...  "));
	refresh_ieee8021BridgeEvbVSIDBMacTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbVSIDBMacTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbVSIDBMacTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbVSIDBMacTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbVSIComponentID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbVSIPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbVSIIDType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeEvbVSIID, &StorageTmp->ieee8021BridgeEvbVSIIDLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbGroupID, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeEvbVSIMac, &StorageTmp->ieee8021BridgeEvbVSIMacLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbVSIVlanId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeEvbUAPConfigTable_data *ieee8021BridgeEvbUAPConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbUAPConfigTable table.
 *
 * Creates a new ieee8021BridgeEvbUAPConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbUAPConfigTable_data *
ieee8021BridgeEvbUAPConfigTable_create(void)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbUAPConfigTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbUAPConfigTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgePhyPort = 0;
		StorageNew->ieee8021BridgeEvbUAPComponentId = 0;
		StorageNew->ieee8021BridgeEvbUAPPort = 0;
		StorageNew->ieee8021BridgeEvbUapConfigIfIndex = 0;
		StorageNew->ieee8021BridgeEvbUAPSchCdcpAdminEnable = 0;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPRole = IEEE8021BRIDGEEVBUAPSCHADMINCDCPROLE_CDCPROLEB;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPChanCap = 0;
		StorageNew->ieee8021BridgeEvbUAPSchOperCDCPChanCap = 0;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow = 0;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh = 0;
		StorageNew->ieee8021BridgeEvbUAPSchOperState = 0;
		StorageNew->ieee8021BridgeEvbSchCdcpRemoteEnabled = 0;
		StorageNew->ieee8021BridgeEvbSchCdcpRemoteRole = 0;
		StorageNew->ieee8021BridgeEvbUAPConfigStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021BridgeEvbUAPConfigRowStatus = 0;
		StorageNew->ieee8021BridgeEvbUAPConfigRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbUAPConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbUAPConfigTable_data *ieee8021BridgeEvbUAPConfigTable_duplicate(struct ieee8021BridgeEvbUAPConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbUAPConfigTable_data *
ieee8021BridgeEvbUAPConfigTable_duplicate(struct ieee8021BridgeEvbUAPConfigTable_data *thedata)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbUAPConfigTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbUAPConfigTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbUAPConfigTable_id = thedata->ieee8021BridgeEvbUAPConfigTable_id;
		StorageNew->ieee8021BridgePhyPort = thedata->ieee8021BridgePhyPort;
		StorageNew->ieee8021BridgeEvbUAPComponentId = thedata->ieee8021BridgeEvbUAPComponentId;
		StorageNew->ieee8021BridgeEvbUAPPort = thedata->ieee8021BridgeEvbUAPPort;
		StorageNew->ieee8021BridgeEvbUapConfigIfIndex = thedata->ieee8021BridgeEvbUapConfigIfIndex;
		StorageNew->ieee8021BridgeEvbUAPSchCdcpAdminEnable = thedata->ieee8021BridgeEvbUAPSchCdcpAdminEnable;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPRole = thedata->ieee8021BridgeEvbUAPSchAdminCDCPRole;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPChanCap = thedata->ieee8021BridgeEvbUAPSchAdminCDCPChanCap;
		StorageNew->ieee8021BridgeEvbUAPSchOperCDCPChanCap = thedata->ieee8021BridgeEvbUAPSchOperCDCPChanCap;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow = thedata->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow;
		StorageNew->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh = thedata->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh;
		StorageNew->ieee8021BridgeEvbUAPSchOperState = thedata->ieee8021BridgeEvbUAPSchOperState;
		StorageNew->ieee8021BridgeEvbSchCdcpRemoteEnabled = thedata->ieee8021BridgeEvbSchCdcpRemoteEnabled;
		StorageNew->ieee8021BridgeEvbSchCdcpRemoteRole = thedata->ieee8021BridgeEvbSchCdcpRemoteRole;
		StorageNew->ieee8021BridgeEvbUAPConfigStorageType = thedata->ieee8021BridgeEvbUAPConfigStorageType;
		StorageNew->ieee8021BridgeEvbUAPConfigRowStatus = thedata->ieee8021BridgeEvbUAPConfigRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbUAPConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbUAPConfigTable_destroy(struct ieee8021BridgeEvbUAPConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbUAPConfigTable_destroy(struct ieee8021BridgeEvbUAPConfigTable_data **thedata)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbUAPConfigTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbUAPConfigTable_add(struct ieee8021BridgeEvbUAPConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbUAPConfigTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbUAPConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbUAPConfigTable_add(struct ieee8021BridgeEvbUAPConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbUAPConfigTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgePhyPort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePhyPort, sizeof(thedata->ieee8021BridgePhyPort));
		header_complex_add_data(&ieee8021BridgeEvbUAPConfigTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbUAPConfigTable_del(struct ieee8021BridgeEvbUAPConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbUAPConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbUAPConfigTable_del(struct ieee8021BridgeEvbUAPConfigTable_data *thedata)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbUAPConfigTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbUAPConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbUAPConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbUAPConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbUAPConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbUAPConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbUAPConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = ieee8021BridgeEvbUAPConfigTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbUAPConfigTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePhyPort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbUAPComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbUAPPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUapConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPSchOperCDCPChanCap, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPSchOperState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSchCdcpRemoteEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbSchCdcpRemoteRole, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPConfigStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus, &tmpsize);
	ieee8021BridgeEvbUAPConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbUAPConfigTable(): store configuraiton file for ieee8021BridgeEvbUAPConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbUAPConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbUAPConfigTable: storing data...  "));
	refresh_ieee8021BridgeEvbUAPConfigTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbUAPConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbUAPConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021BridgeEvbUAPConfigStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbUAPConfigTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePhyPort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbUAPComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbUAPPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUapConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchOperCDCPChanCap, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPSchOperState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSchCdcpRemoteEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbSchCdcpRemoteRole, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPConfigStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeEvbCAPConfigTable_data *ieee8021BridgeEvbCAPConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbCAPConfigTable table.
 *
 * Creates a new ieee8021BridgeEvbCAPConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbCAPConfigTable_data *
ieee8021BridgeEvbCAPConfigTable_create(void)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbCAPConfigTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbCAPConfigTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgePhyPort = 0;
		StorageNew->ieee8021BridgeEvbCAPComponentId = 0;
		StorageNew->ieee8021BridgeEvbCapConfigIfIndex = 0;
		StorageNew->ieee8021BridgeEvbCAPPort = 0;
		StorageNew->ieee8021BridgeEvbCAPSChannelID = 0;
		StorageNew->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID = 0;
		StorageNew->ieee8021BridgeEvbCAPAssociateSBPOrURPPort = 0;
		StorageNew->ieee8021BridgeEvbCAPRowStatus = 0;
		StorageNew->ieee8021BridgeEvbCAPRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbCAPConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbCAPConfigTable_data *ieee8021BridgeEvbCAPConfigTable_duplicate(struct ieee8021BridgeEvbCAPConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbCAPConfigTable_data *
ieee8021BridgeEvbCAPConfigTable_duplicate(struct ieee8021BridgeEvbCAPConfigTable_data *thedata)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbCAPConfigTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbCAPConfigTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbCAPConfigTable_id = thedata->ieee8021BridgeEvbCAPConfigTable_id;
		StorageNew->ieee8021BridgePhyPort = thedata->ieee8021BridgePhyPort;
		StorageNew->ieee8021BridgeEvbSchID = thedata->ieee8021BridgeEvbSchID;
		StorageNew->ieee8021BridgeEvbCAPComponentId = thedata->ieee8021BridgeEvbCAPComponentId;
		StorageNew->ieee8021BridgeEvbCapConfigIfIndex = thedata->ieee8021BridgeEvbCapConfigIfIndex;
		StorageNew->ieee8021BridgeEvbCAPPort = thedata->ieee8021BridgeEvbCAPPort;
		StorageNew->ieee8021BridgeEvbCAPSChannelID = thedata->ieee8021BridgeEvbCAPSChannelID;
		StorageNew->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID = thedata->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID;
		StorageNew->ieee8021BridgeEvbCAPAssociateSBPOrURPPort = thedata->ieee8021BridgeEvbCAPAssociateSBPOrURPPort;
		StorageNew->ieee8021BridgeEvbCAPRowStatus = thedata->ieee8021BridgeEvbCAPRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbCAPConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbCAPConfigTable_destroy(struct ieee8021BridgeEvbCAPConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbCAPConfigTable_destroy(struct ieee8021BridgeEvbCAPConfigTable_data **thedata)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbCAPConfigTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbCAPConfigTable_add(struct ieee8021BridgeEvbCAPConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbCAPConfigTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbCAPConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbCAPConfigTable_add(struct ieee8021BridgeEvbCAPConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbCAPConfigTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgePhyPort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePhyPort, sizeof(thedata->ieee8021BridgePhyPort));
		/* ieee8021BridgeEvbSchID */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbSchID, sizeof(thedata->ieee8021BridgeEvbSchID));
		header_complex_add_data(&ieee8021BridgeEvbCAPConfigTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbCAPConfigTable_del(struct ieee8021BridgeEvbCAPConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbCAPConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbCAPConfigTable_del(struct ieee8021BridgeEvbCAPConfigTable_data *thedata)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbCAPConfigTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbCAPConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbCAPConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbCAPConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbCAPConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbCAPConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbCAPConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp = ieee8021BridgeEvbCAPConfigTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbCAPConfigTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePhyPort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbSchID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbCAPComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbCapConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbCAPPort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbCAPSChannelID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbCAPRowStatus, &tmpsize);
	ieee8021BridgeEvbCAPConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbCAPConfigTable(): store configuraiton file for ieee8021BridgeEvbCAPConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbCAPConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbCAPConfigTable: storing data...  "));
	refresh_ieee8021BridgeEvbCAPConfigTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbCAPConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbCAPConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbCAPConfigTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePhyPort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbSchID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbCAPComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbCapConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbCAPPort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbCAPSChannelID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbCAPRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeEvbURPTable_data *ieee8021BridgeEvbURPTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbURPTable table.
 *
 * Creates a new ieee8021BridgeEvbURPTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbURPTable_data *
ieee8021BridgeEvbURPTable_create(void)
{
	struct ieee8021BridgeEvbURPTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbURPTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbURPTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeEvbURPIfIndex = 0;
		StorageNew->ieee8021BridgeEvbURPBindToISSPort = 0;
		StorageNew->ieee8021BridgeEvbURPLldpManual = 0;
		StorageNew->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay = 0;
		StorageNew->ieee8021BridgeEvbURPVdpOperRespWaitDelay = 0;
		StorageNew->ieee8021BridgeEvbURPVdpOperReinitKeepAlive = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbURPTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbURPTable_data *ieee8021BridgeEvbURPTable_duplicate(struct ieee8021BridgeEvbURPTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbURPTable_data *
ieee8021BridgeEvbURPTable_duplicate(struct ieee8021BridgeEvbURPTable_data *thedata)
{
	struct ieee8021BridgeEvbURPTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbURPTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbURPTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbURPTable_id = thedata->ieee8021BridgeEvbURPTable_id;
		StorageNew->ieee8021BridgeEvbURPComponentId = thedata->ieee8021BridgeEvbURPComponentId;
		StorageNew->ieee8021BridgeEvbURPPort = thedata->ieee8021BridgeEvbURPPort;
		StorageNew->ieee8021BridgeEvbURPIfIndex = thedata->ieee8021BridgeEvbURPIfIndex;
		StorageNew->ieee8021BridgeEvbURPBindToISSPort = thedata->ieee8021BridgeEvbURPBindToISSPort;
		StorageNew->ieee8021BridgeEvbURPLldpManual = thedata->ieee8021BridgeEvbURPLldpManual;
		StorageNew->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay = thedata->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay;
		StorageNew->ieee8021BridgeEvbURPVdpOperRespWaitDelay = thedata->ieee8021BridgeEvbURPVdpOperRespWaitDelay;
		StorageNew->ieee8021BridgeEvbURPVdpOperReinitKeepAlive = thedata->ieee8021BridgeEvbURPVdpOperReinitKeepAlive;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbURPTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbURPTable_destroy(struct ieee8021BridgeEvbURPTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbURPTable_destroy(struct ieee8021BridgeEvbURPTable_data **thedata)
{
	struct ieee8021BridgeEvbURPTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbURPTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbURPTable_add(struct ieee8021BridgeEvbURPTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbURPTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbURPTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbURPTable_add(struct ieee8021BridgeEvbURPTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbURPTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeEvbURPComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbURPComponentId, sizeof(thedata->ieee8021BridgeEvbURPComponentId));
		/* ieee8021BridgeEvbURPPort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbURPPort, sizeof(thedata->ieee8021BridgeEvbURPPort));
		header_complex_add_data(&ieee8021BridgeEvbURPTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbURPTable_del(struct ieee8021BridgeEvbURPTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbURPTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbURPTable_del(struct ieee8021BridgeEvbURPTable_data *thedata)
{
	struct ieee8021BridgeEvbURPTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbURPTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbURPTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbURPTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbURPTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbURPTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbURPTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbURPTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = ieee8021BridgeEvbURPTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbURPTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbURPComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbURPPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbURPIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbURPBindToISSPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeEvbURPLldpManual, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive, &tmpsize);
	ieee8021BridgeEvbURPTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbURPTable(): store configuraiton file for ieee8021BridgeEvbURPTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbURPTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbURPTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbURPTable: storing data...  "));
	refresh_ieee8021BridgeEvbURPTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbURPTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbURPTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbURPTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbURPComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbURPPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbURPIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbURPBindToISSPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeEvbURPLldpManual, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeEvbEcpTable_data *ieee8021BridgeEvbEcpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeEvbEcpTable table.
 *
 * Creates a new ieee8021BridgeEvbEcpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeEvbEcpTable_data *
ieee8021BridgeEvbEcpTable_create(void)
{
	struct ieee8021BridgeEvbEcpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbEcpTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbEcpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeEvbEcpOperAckTimerInit = 0;
		StorageNew->ieee8021BridgeEvbEcpOperMaxRetries = 0;
		StorageNew->ieee8021BridgeEvbEcpTxFrameCount = 0;
		StorageNew->ieee8021BridgeEvbEcpTxRetryCount = 0;
		StorageNew->ieee8021BridgeEvbEcpTxFailures = 0;
		StorageNew->ieee8021BridgeEvbEcpRxFrameCount = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeEvbEcpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeEvbEcpTable_data *ieee8021BridgeEvbEcpTable_duplicate(struct ieee8021BridgeEvbEcpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeEvbEcpTable_data *
ieee8021BridgeEvbEcpTable_duplicate(struct ieee8021BridgeEvbEcpTable_data *thedata)
{
	struct ieee8021BridgeEvbEcpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeEvbEcpTable_data);

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbEcpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeEvbEcpTable_id = thedata->ieee8021BridgeEvbEcpTable_id;
		StorageNew->ieee8021BridgeEvbEcpComponentId = thedata->ieee8021BridgeEvbEcpComponentId;
		StorageNew->ieee8021BridgeEvbEcpPort = thedata->ieee8021BridgeEvbEcpPort;
		StorageNew->ieee8021BridgeEvbEcpOperAckTimerInit = thedata->ieee8021BridgeEvbEcpOperAckTimerInit;
		StorageNew->ieee8021BridgeEvbEcpOperMaxRetries = thedata->ieee8021BridgeEvbEcpOperMaxRetries;
		StorageNew->ieee8021BridgeEvbEcpTxFrameCount = thedata->ieee8021BridgeEvbEcpTxFrameCount;
		StorageNew->ieee8021BridgeEvbEcpTxRetryCount = thedata->ieee8021BridgeEvbEcpTxRetryCount;
		StorageNew->ieee8021BridgeEvbEcpTxFailures = thedata->ieee8021BridgeEvbEcpTxFailures;
		StorageNew->ieee8021BridgeEvbEcpRxFrameCount = thedata->ieee8021BridgeEvbEcpRxFrameCount;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeEvbEcpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeEvbEcpTable_destroy(struct ieee8021BridgeEvbEcpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeEvbEcpTable_destroy(struct ieee8021BridgeEvbEcpTable_data **thedata)
{
	struct ieee8021BridgeEvbEcpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbEcpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbEcpTable_add(struct ieee8021BridgeEvbEcpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeEvbEcpTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeEvbEcpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeEvbEcpTable_add(struct ieee8021BridgeEvbEcpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbEcpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeEvbEcpComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbEcpComponentId, sizeof(thedata->ieee8021BridgeEvbEcpComponentId));
		/* ieee8021BridgeEvbEcpPort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeEvbEcpPort, sizeof(thedata->ieee8021BridgeEvbEcpPort));
		header_complex_add_data(&ieee8021BridgeEvbEcpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeEvbEcpTable_del(struct ieee8021BridgeEvbEcpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeEvbEcpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeEvbEcpTable_del(struct ieee8021BridgeEvbEcpTable_data *thedata)
{
	struct ieee8021BridgeEvbEcpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbEcpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeEvbEcpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeEvbEcpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeEvbEcpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeEvbEcpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeEvbEcpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeEvbEcpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeEvbEcpTable_data *StorageTmp = ieee8021BridgeEvbEcpTable_create();

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "parse_ieee8021BridgeEvbEcpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbEcpComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbEcpPort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbEcpOperAckTimerInit, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeEvbEcpOperMaxRetries, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbEcpTxFrameCount, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbEcpTxRetryCount, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbEcpTxFailures, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021BridgeEvbEcpRxFrameCount, &tmpsize);
	ieee8021BridgeEvbEcpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/*
 * store_ieee8021BridgeEvbEcpTable(): store configuraiton file for ieee8021BridgeEvbEcpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeEvbEcpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeEvbEcpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "store_ieee8021BridgeEvbEcpTable: storing data...  "));
	refresh_ieee8021BridgeEvbEcpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeEvbEcpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeEvbEcpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeEvbEcpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbEcpComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbEcpPort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbEcpOperAckTimerInit, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeEvbEcpOperMaxRetries, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbEcpTxFrameCount, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbEcpTxRetryCount, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbEcpTxFailures, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021BridgeEvbEcpRxFrameCount, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, struct ieee8021BridgeEvbSbpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, struct ieee8021BridgeEvbSbpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, struct ieee8021BridgeEvbSbpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbSbpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, struct ieee8021BridgeEvbSbpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbSbpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, struct ieee8021BridgeEvbSbpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, struct ieee8021BridgeEvbSbpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbSbpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbSbpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbSbpTable_data *
refresh_ieee8021BridgeEvbSbpTable_row(struct ieee8021BridgeEvbSbpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbSbpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbSbpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbSbpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbSbpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbSbpTable(int force)
{
	if (!force && ieee8021BridgeEvbSbpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbSbpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbSbpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbSbpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbSbpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbSbpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbSbpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbSbpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbSbpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbSbpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBSBPLLDPMANUAL:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbSbpLldpManual;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSbpLldpManual);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSbpLldpManual;
		break;
	case (u_char) IEEE8021BRIDGEEVBSBPVDPOPERRSRCWAITDELAY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSbpVdpOperRsrcWaitDelay;
		break;
	case (u_char) IEEE8021BRIDGEEVBSBPVDPOPERREINITKEEPALIVE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSbpVdpOperReinitKeepAlive;
		break;
	case (u_char) IEEE8021BRIDGEEVBSBPVDPOPERTOUTKEEPALIVE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSbpVdpOperToutKeepAlive);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSbpVdpOperToutKeepAlive;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbVSIDBTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbVSIDBTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbVSIDBTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbVSIDBTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbVSIDBTable_data *
refresh_ieee8021BridgeEvbVSIDBTable_row(struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbVSIDBTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbVSIDBTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbVSIDBTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbVSIDBTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbVSIDBTable(int force)
{
	if (!force && ieee8021BridgeEvbVSIDBTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbVSIDBTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbVSIDBTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbVSIDBTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbVSIDBTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbVSIDBTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbVSIDBTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbVSIDBTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbVSIDBTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbVSIDBTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBVSITIMESINCECREATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVSITimeSinceCreate);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVSITimeSinceCreate;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSIVDPOPERCMD:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVsiVdpOperCmd);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVsiVdpOperCmd;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSIOPERREVERT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVsiOperRevert);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVsiOperRevert;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSIOPERHARD:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVsiOperHard);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVsiOperHard;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSIOPERREASON:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeEvbVsiOperReasonLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeEvbVsiOperReason;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSIMGRID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeEvbVSIMgrIDLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeEvbVSIMgrID;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSITYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVSIType);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVSIType;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSITYPEVERSION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeEvbVSITypeVersionLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeEvbVSITypeVersion;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSIMVFORMAT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVSIMvFormat);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVSIMvFormat;
		break;
	case (u_char) IEEE8021BRIDGEEVBVSINUMMACS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVSINumMACs);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVSINumMACs;
		break;
	case (u_char) IEEE8021BRIDGEEVBVDPMACHINESTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVDPMachineState);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVDPMachineState;
		break;
	case (u_char) IEEE8021BRIDGEEVBVDPCOMMANDSSUCCEEDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVDPCommandsSucceeded);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVDPCommandsSucceeded;
		break;
	case (u_char) IEEE8021BRIDGEEVBVDPCOMMANDSFAILED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVDPCommandsFailed);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVDPCommandsFailed;
		break;
	case (u_char) IEEE8021BRIDGEEVBVDPCOMMANDREVERTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVDPCommandReverts);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVDPCommandReverts;
		break;
	case (u_char) IEEE8021BRIDGEEVBVDPCOUNTERDISCONTINUITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbVDPCounterDiscontinuity);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbVDPCounterDiscontinuity;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBMacTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBMacTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBMacTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbVSIDBMacTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBMacTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbVSIDBMacTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBMacTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, struct ieee8021BridgeEvbVSIDBMacTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbVSIDBMacTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbVSIDBMacTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbVSIDBMacTable_data *
refresh_ieee8021BridgeEvbVSIDBMacTable_row(struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbVSIDBMacTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbVSIDBMacTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbVSIDBMacTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbVSIDBMacTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbVSIDBMacTable(int force)
{
	if (!force && ieee8021BridgeEvbVSIDBMacTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbVSIDBMacTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbVSIDBMacTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbVSIDBMacTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbVSIDBMacTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbVSIDBMacTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbVSIDBMacTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbVSIDBMacTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbVSIDBMacTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbVSIDBMacTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbUAPConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbUAPConfigTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbUAPConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbUAPConfigTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbUAPConfigTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbUAPConfigTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbUAPConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbUAPConfigTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbUAPConfigTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbUAPConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbUAPConfigTable_data *
refresh_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbUAPConfigTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbUAPConfigTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbUAPConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbUAPConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbUAPConfigTable(int force)
{
	if (!force && ieee8021BridgeEvbUAPConfigTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbUAPConfigTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbUAPConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbUAPConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbUAPConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbUAPConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbUAPConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBUAPCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPComponentId);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPComponentId;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPPort);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPPort;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPCONFIGIFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUapConfigIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUapConfigIfIndex;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHCDCPADMINENABLE:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPSchCdcpAdminEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPROLE:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPSchAdminCDCPRole;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPCHANCAP:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPSchAdminCDCPChanCap;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHOPERCDCPCHANCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchOperCDCPChanCap);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchOperCDCPChanCap;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPSVIDPOOLLOW:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHADMINCDCPSVIDPOOLHIGH:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPSCHOPERSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPSchOperState);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPSchOperState;
		break;
	case (u_char) IEEE8021BRIDGEEVBSCHCDCPREMOTEENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSchCdcpRemoteEnabled);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSchCdcpRemoteEnabled;
		break;
	case (u_char) IEEE8021BRIDGEEVBSCHCDCPREMOTEROLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbSchCdcpRemoteRole);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbSchCdcpRemoteRole;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPCONFIGSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPConfigStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPConfigStorageType);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPConfigStorageType;
		break;
	case (u_char) IEEE8021BRIDGEEVBUAPCONFIGROWSTATUS:	/* Create */
		*write_method = write_ieee8021BridgeEvbUAPConfigRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbCAPConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbCAPConfigTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbCAPConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbCAPConfigTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbCAPConfigTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbCAPConfigTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbCAPConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, struct ieee8021BridgeEvbCAPConfigTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbCAPConfigTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbCAPConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbCAPConfigTable_data *
refresh_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbCAPConfigTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbCAPConfigTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbCAPConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbCAPConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbCAPConfigTable(int force)
{
	if (!force && ieee8021BridgeEvbCAPConfigTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbCAPConfigTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbCAPConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbCAPConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbCAPConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbCAPConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbCAPConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbCAPConfigTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBCAPCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCAPComponentId);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCAPComponentId;
		break;
	case (u_char) IEEE8021BRIDGEEVBCAPCONFIGIFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCapConfigIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCapConfigIfIndex;
		break;
	case (u_char) IEEE8021BRIDGEEVBCAPPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCAPPort);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCAPPort;
		break;
	case (u_char) IEEE8021BRIDGEEVBCAPSCHANNELID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCAPSChannelID);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCAPSChannelID;
		break;
	case (u_char) IEEE8021BRIDGEEVBCAPASSOCIATESBPORURPCOMPID:	/* Create */
		*write_method = write_ieee8021BridgeEvbCAPAssociateSBPOrURPCompID;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID;
		break;
	case (u_char) IEEE8021BRIDGEEVBCAPASSOCIATESBPORURPPORT:	/* Create */
		*write_method = write_ieee8021BridgeEvbCAPAssociateSBPOrURPPort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort;
		break;
	case (u_char) IEEE8021BRIDGEEVBCAPROWSTATUS:	/* Create */
		*write_method = write_ieee8021BridgeEvbCAPRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbCAPRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbCAPRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, struct ieee8021BridgeEvbURPTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, struct ieee8021BridgeEvbURPTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, struct ieee8021BridgeEvbURPTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbURPTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, struct ieee8021BridgeEvbURPTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbURPTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, struct ieee8021BridgeEvbURPTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, struct ieee8021BridgeEvbURPTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbURPTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbURPTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbURPTable_data *
refresh_ieee8021BridgeEvbURPTable_row(struct ieee8021BridgeEvbURPTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbURPTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbURPTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbURPTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbURPTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbURPTable(int force)
{
	if (!force && ieee8021BridgeEvbURPTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbURPTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbURPTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbURPTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbURPTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbURPTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbURPTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbURPTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbURPTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBURPIFINDEX:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbURPIfIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbURPIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbURPIfIndex;
		break;
	case (u_char) IEEE8021BRIDGEEVBURPBINDTOISSPORT:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbURPBindToISSPort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbURPBindToISSPort);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbURPBindToISSPort;
		break;
	case (u_char) IEEE8021BRIDGEEVBURPLLDPMANUAL:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbURPLldpManual;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbURPLldpManual);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbURPLldpManual;
		break;
	case (u_char) IEEE8021BRIDGEEVBURPVDPOPERRSRCWAITDELAY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbURPVdpOperRsrcWaitDelay;
		break;
	case (u_char) IEEE8021BRIDGEEVBURPVDPOPERRESPWAITDELAY:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbURPVdpOperRespWaitDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay;
		break;
	case (u_char) IEEE8021BRIDGEEVBURPVDPOPERREINITKEEPALIVE:	/* ReadWrite */
		*write_method = write_ieee8021BridgeEvbURPVdpOperReinitKeepAlive;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, struct ieee8021BridgeEvbEcpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, struct ieee8021BridgeEvbEcpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, struct ieee8021BridgeEvbEcpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeEvbEcpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, struct ieee8021BridgeEvbEcpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeEvbEcpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, struct ieee8021BridgeEvbEcpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, struct ieee8021BridgeEvbEcpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeEvbEcpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeEvbEcpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeEvbEcpTable_data *
refresh_ieee8021BridgeEvbEcpTable_row(struct ieee8021BridgeEvbEcpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeEvbEcpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeEvbEcpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeEvbEcpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeEvbEcpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeEvbEcpTable(int force)
{
	if (!force && ieee8021BridgeEvbEcpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeEvbEcpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeEvbEcpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeEvbEcpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeEvbMib above.
 */
u_char *
var_ieee8021BridgeEvbEcpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeEvbEcpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "var_ieee8021BridgeEvbEcpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeEvbEcpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeEvbEcpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeEvbEcpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEEVBECPOPERACKTIMERINIT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbEcpOperAckTimerInit);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbEcpOperAckTimerInit;
		break;
	case (u_char) IEEE8021BRIDGEEVBECPOPERMAXRETRIES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbEcpOperMaxRetries);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbEcpOperMaxRetries;
		break;
	case (u_char) IEEE8021BRIDGEEVBECPTXFRAMECOUNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbEcpTxFrameCount);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbEcpTxFrameCount;
		break;
	case (u_char) IEEE8021BRIDGEEVBECPTXRETRYCOUNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbEcpTxRetryCount);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbEcpTxRetryCount;
		break;
	case (u_char) IEEE8021BRIDGEEVBECPTXFAILURES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbEcpTxFailures);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbEcpTxFailures;
		break;
	case (u_char) IEEE8021BRIDGEEVBECPRXFRAMECOUNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeEvbEcpRxFrameCount);
		rval = (u_char *) &StorageTmp->ieee8021BridgeEvbEcpRxFrameCount;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021BridgeEvbSbpLldpManual(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbSbpLldpManual(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbSbpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSbpLldpManual entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbSbpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSbpLldpManual not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSbpLldpManual: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSbpLldpManual: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbSbpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old = ieee8021BridgeEvbSbpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbSbpTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbSbpLldpManual = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbSbpTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbSbpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbSbpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSbpLldpManual for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbSbpTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbSbpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbSbpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old) != NULL) {
			ieee8021BridgeEvbSbpTable_destroy(&StorageTmp->ieee8021BridgeEvbSbpTable_old);
			StorageTmp->ieee8021BridgeEvbSbpTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbSbpTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbSbpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbSbpTable_sets == 0)
			revert_ieee8021BridgeEvbSbpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbSbpTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSbpLldpManual = StorageOld->ieee8021BridgeEvbSbpLldpManual;
		if (--StorageTmp->ieee8021BridgeEvbSbpTable_rsvs == 0)
			ieee8021BridgeEvbSbpTable_destroy(&StorageTmp->ieee8021BridgeEvbSbpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPSchCdcpAdminEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbUAPSchCdcpAdminEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPSchCdcpAdminEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchCdcpAdminEnable: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchCdcpAdminEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchCdcpAdminEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021BRIDGEEVBUAPSCHCDCPADMINENABLE_ENABLE:
		case IEEE8021BRIDGEEVBUAPSCHCDCPADMINENABLE_DISABLE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchCdcpAdminEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbUAPSchCdcpAdminEnable = StorageOld->ieee8021BridgeEvbUAPSchCdcpAdminEnable;
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPSchAdminCDCPRole(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbUAPSchAdminCDCPRole(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPSchAdminCDCPRole entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPRole: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPRole not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPRole: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value cdcpRoleB */
		switch (set_value) {
		case IEEE8021BRIDGEEVBUAPSCHADMINCDCPROLE_CDCPROLEB:
		case IEEE8021BRIDGEEVBUAPSCHADMINCDCPROLE_CDCPROLES:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPRole: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPRole = StorageOld->ieee8021BridgeEvbUAPSchAdminCDCPRole;
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPSchAdminCDCPChanCap(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbUAPSchAdminCDCPChanCap(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPSchAdminCDCPChanCap entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPChanCap: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPChanCap not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPChanCap: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..167 */
		if ((1 > set_value || set_value > 167)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPChanCap: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPChanCap = StorageOld->ieee8021BridgeEvbUAPSchAdminCDCPChanCap;
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow = StorageOld->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolLow;
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh for you to use, and you have just been asked to do something with it.  Note 
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh = StorageOld->ieee8021BridgeEvbUAPSchAdminCDCPSVIDPoolHigh;
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPConfigStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbUAPConfigStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPConfigStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPConfigStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPConfigStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPConfigStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPConfigStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbUAPConfigStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPConfigStorageType for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbUAPConfigStorageType = StorageOld->ieee8021BridgeEvbUAPConfigStorageType;
		if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbCAPAssociateSBPOrURPCompID(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbCAPAssociateSBPOrURPCompID(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbCAPAssociateSBPOrURPCompID entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbCAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbCAPRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPCompID: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPCompID not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPCompID: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..-1 */
		if ((1 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPCompID: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old = ieee8021BridgeEvbCAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbCAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbCAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID = StorageOld->ieee8021BridgeEvbCAPAssociateSBPOrURPCompID;
		if (--StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbCAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbCAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbCAPAssociateSBPOrURPPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbCAPAssociateSBPOrURPPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbCAPAssociateSBPOrURPPort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbCAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeEvbCAPRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPPort: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPPort not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPPort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..65535 */
		if ((1 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPAssociateSBPOrURPPort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old = ieee8021BridgeEvbCAPConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
			ieee8021BridgeEvbCAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbCAPConfigTable_old);
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets == 0)
			revert_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbCAPAssociateSBPOrURPPort = StorageOld->ieee8021BridgeEvbCAPAssociateSBPOrURPPort;
		if (--StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs == 0)
			ieee8021BridgeEvbCAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbCAPConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbURPIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbURPIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbURPIfIndex entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbURPTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPIfIndex not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPIfIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPIfIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old = ieee8021BridgeEvbURPTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbURPIfIndex = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbURPTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbURPIfIndex for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
			revert_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbURPIfIndex = StorageOld->ieee8021BridgeEvbURPIfIndex;
		if (--StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbURPBindToISSPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbURPBindToISSPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbURPBindToISSPort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbURPTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPBindToISSPort not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPBindToISSPort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..65535 */
		if ((1 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPBindToISSPort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old = ieee8021BridgeEvbURPTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbURPBindToISSPort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbURPTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbURPBindToISSPort for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
			revert_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbURPBindToISSPort = StorageOld->ieee8021BridgeEvbURPBindToISSPort;
		if (--StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbURPLldpManual(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbURPLldpManual(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbURPLldpManual entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbURPTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPLldpManual not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPLldpManual: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPLldpManual: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old = ieee8021BridgeEvbURPTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbURPLldpManual = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbURPTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbURPLldpManual for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
			revert_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbURPLldpManual = StorageOld->ieee8021BridgeEvbURPLldpManual;
		if (--StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbURPVdpOperRespWaitDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbURPVdpOperRespWaitDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbURPVdpOperRespWaitDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbURPTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPVdpOperRespWaitDelay not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPVdpOperRespWaitDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old = ieee8021BridgeEvbURPTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbURPTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
			revert_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbURPVdpOperRespWaitDelay = StorageOld->ieee8021BridgeEvbURPVdpOperRespWaitDelay;
		if (--StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbURPVdpOperReinitKeepAlive(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeEvbURPVdpOperReinitKeepAlive(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbURPTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbURPVdpOperReinitKeepAlive entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeEvbURPTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPVdpOperReinitKeepAlive not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbURPVdpOperReinitKeepAlive: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old = ieee8021BridgeEvbURPTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs++;
		StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeEvbURPTable_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
				if ((ret = update_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbURPTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) != NULL) {
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
			StorageTmp->ieee8021BridgeEvbURPTable_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_tsts = 0;
			StorageTmp->ieee8021BridgeEvbURPTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbURPTable_sets == 0)
			revert_ieee8021BridgeEvbURPTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbURPTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbURPVdpOperReinitKeepAlive = StorageOld->ieee8021BridgeEvbURPVdpOperReinitKeepAlive;
		if (--StorageTmp->ieee8021BridgeEvbURPTable_rsvs == 0)
			ieee8021BridgeEvbURPTable_destroy(&StorageTmp->ieee8021BridgeEvbURPTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysEvbLldpTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysEvbLldpTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysEvbLldpTxEnable entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysEvbLldpTxEnable = StorageOld->ieee8021BridgeEvbSysEvbLldpTxEnable;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysEvbLldpManual(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysEvbLldpManual(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysEvbLldpManual entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpManual not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpManual: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpManual: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysEvbLldpManual = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysEvbLldpManual for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysEvbLldpManual = StorageOld->ieee8021BridgeEvbSysEvbLldpManual;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysEvbLldpGidCapable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysEvbLldpGidCapable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysEvbLldpGidCapable entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpGidCapable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpGidCapable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEvbLldpGidCapable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysEvbLldpGidCapable = StorageOld->ieee8021BridgeEvbSysEvbLldpGidCapable;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysEcpAckTimer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysEcpAckTimer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysEcpAckTimer entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEcpAckTimer not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEcpAckTimer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysEcpAckTimer = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysEcpAckTimer for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysEcpAckTimer = StorageOld->ieee8021BridgeEvbSysEcpAckTimer;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysEcpMaxRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysEcpMaxRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysEcpMaxRetries entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEcpMaxRetries not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEcpMaxRetries: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 3 */
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysEcpMaxRetries: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysEcpMaxRetries = StorageOld->ieee8021BridgeEvbSysEcpMaxRetries;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay = StorageOld->ieee8021BridgeEvbSysVdpDfltRsrcWaitDelay;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbSysVdpDfltReinitKeepAlive(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021BridgeEvbSysVdpDfltReinitKeepAlive(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbSysVdpDfltReinitKeepAlive entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021BridgeEvbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysVdpDfltReinitKeepAlive not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbSysVdpDfltReinitKeepAlive: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			if (StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old = ieee8021BridgeEvbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeEvbMib_rsvs++;
		StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
				if ((ret = check_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeEvbMib_sets == 0)
				if ((ret = update_ieee8021BridgeEvbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeEvbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) != NULL) {
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
			StorageTmp->ieee8021BridgeEvbMib_rsvs = 0;
			StorageTmp->ieee8021BridgeEvbMib_tsts = 0;
			StorageTmp->ieee8021BridgeEvbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeEvbMib_tsts == 0)
			revert_ieee8021BridgeEvbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeEvbMib_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive = StorageOld->ieee8021BridgeEvbSysVdpDfltReinitKeepAlive;
		if (--StorageTmp->ieee8021BridgeEvbMib_rsvs == 0)
			ieee8021BridgeEvbMib_destroy(&StorageTmp->ieee8021BridgeEvbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021BridgeEvbUAPConfigTable_row(struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021BridgeEvbCAPConfigTable_row(struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbUAPConfigRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021BridgeEvbUAPConfigRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbUAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021BridgeEvbUAPConfigTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbUAPConfigRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021BridgeEvbUAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPConfigRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbUAPConfigRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgePhyPort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgePhyPort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgePhyPort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgePhyPort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021BridgeEvbUAPConfigTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021BridgeEvbUAPConfigTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgePhyPort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021BridgeEvbUAPConfigTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021BridgeEvbUAPConfigTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
				if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old = ieee8021BridgeEvbUAPConfigTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021BridgeEvbUAPConfigTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021BridgeEvbUAPConfigTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021BridgeEvbUAPConfigTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021BridgeEvbUAPConfigTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021BridgeEvbUAPConfigRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021BridgeEvbUAPConfigRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021BridgeEvbUAPConfigRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) != NULL) {
				ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
				StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs = 0;
				StorageTmp->ieee8021BridgeEvbUAPConfigTable_tsts = 0;
				StorageTmp->ieee8021BridgeEvbUAPConfigTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021BridgeEvbUAPConfigTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021BridgeEvbUAPConfigTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021BridgeEvbUAPConfigRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021BridgeEvbUAPConfigTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021BridgeEvbUAPConfigTable_del(StorageNew);
				ieee8021BridgeEvbUAPConfigTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021BridgeEvbUAPConfigTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021BridgeEvbUAPConfigTable_rsvs == 0)
				ieee8021BridgeEvbUAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbUAPConfigTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021BridgeEvbUAPConfigTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeEvbCAPRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021BridgeEvbCAPRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeEvbCAPConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021BridgeEvbCAPConfigTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021BridgeEvbMib", "write_ieee8021BridgeEvbCAPRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021BridgeEvbCAPConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeEvbCAPRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgePhyPort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeEvbSchID */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgePhyPort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgePhyPort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgePhyPort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeEvbSchID */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeEvbSchID: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeEvbSchID: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021BridgeEvbCAPConfigTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021BridgeEvbCAPConfigTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgePhyPort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeEvbSchID = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021BridgeEvbCAPConfigTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021BridgeEvbCAPConfigTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
				if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old = ieee8021BridgeEvbCAPConfigTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021BridgeEvbCAPConfigTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021BridgeEvbCAPConfigTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021BridgeEvbCAPConfigTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021BridgeEvbCAPRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must
		   be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021BridgeEvbCAPConfigTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021BridgeEvbCAPConfigTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021BridgeEvbCAPRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021BridgeEvbCAPRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021BridgeEvbCAPRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) != NULL) {
				ieee8021BridgeEvbCAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbCAPConfigTable_old);
				StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs = 0;
				StorageTmp->ieee8021BridgeEvbCAPConfigTable_tsts = 0;
				StorageTmp->ieee8021BridgeEvbCAPConfigTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021BridgeEvbCAPConfigTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021BridgeEvbCAPConfigTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021BridgeEvbCAPRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021BridgeEvbCAPConfigTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021BridgeEvbCAPConfigTable_del(StorageNew);
				ieee8021BridgeEvbCAPConfigTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021BridgeEvbCAPConfigTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021BridgeEvbCAPConfigTable_rsvs == 0)
				ieee8021BridgeEvbCAPConfigTable_destroy(&StorageTmp->ieee8021BridgeEvbCAPConfigTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021BridgeEvbCAPConfigTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021BridgeEvbMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021BridgeEvbMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021BridgeEvbMibold_signal_handler != NULL)
		(*ieee8021BridgeEvbMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
}

/**
 * @fn void ieee8021BridgeEvbMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021BridgeEvbMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "ieee8021BridgeEvbMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021BridgeEvbMib", "done.\n"));
	return;
}
