/*****************************************************************************

 @(#) src/snmp/ieee8021spanningtreemib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021spanningtreemib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021SpanningTreeMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021SpanningTreeMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021SpanningTreeMib_refresh = 1;
volatile int ieee8021SpanningTreeTable_refresh = 1;
volatile int ieee8021SpanningTreePortTable_refresh = 1;
volatile int ieee8021SpanningTreePortExtensionTable_refresh = 1;

/*
 * ieee8021SpanningTreeMib_variables_oid: object identifier for ieee8021SpanningTreeMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021SpanningTreeMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 3 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid ieee8021SpanningTreeNewRoot_oid[10] = { 1, 3, 111, 2, 802, 1, 1, 3, 0, 1 };
oid ieee8021SpanningTreeTopologyChange_oid[10] = { 1, 3, 111, 2, 802, 1, 1, 3, 0, 2 };

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021SpanningTreeCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 1, 1 };
oid ieee8021SpanningTreeRstpCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 1, 2 };
oid ieee8021SpanningTreeGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 2, 1 };
oid ieee8021SpanningTreeRstpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 2, 2 };
oid ieee8021SpanningTreePortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 2, 3 };
oid ieee8021SpanningTreeRstpPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 2, 4 };
oid ieee8021SpanningTreeNotificationGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 2, 5 };
oid ieee8021SpanningTreeRstpFragileGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 3, 2, 2, 6 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021SpanningTreeMib_variables: tree for ieee8021SpanningTreeMib
 * This variable defines function callbacks and type return information for the ieee8021SpanningTreeMib mib section
 */
struct variable4 ieee8021SpanningTreeMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021SPANNINGTREEPROTOCOLSPECIFICATION  1
	{(u_char) IEEE8021SPANNINGTREEPROTOCOLSPECIFICATION, ASN_INTEGER, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 2}},
#define   IEEE8021SPANNINGTREEPRIORITY  2
	{(u_char) IEEE8021SPANNINGTREEPRIORITY, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 3}},
#define   IEEE8021SPANNINGTREETIMESINCETOPOLOGYCHANGE  3
	{(u_char) IEEE8021SPANNINGTREETIMESINCETOPOLOGYCHANGE, ASN_TIMETICKS, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 4}},
#define   IEEE8021SPANNINGTREETOPCHANGES  4
	{(u_char) IEEE8021SPANNINGTREETOPCHANGES, ASN_COUNTER64, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 5}},
#define   IEEE8021SPANNINGTREEDESIGNATEDROOT  5
	{(u_char) IEEE8021SPANNINGTREEDESIGNATEDROOT, ASN_OCTET_STR, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 6}},
#define   IEEE8021SPANNINGTREEROOTCOST  6
	{(u_char) IEEE8021SPANNINGTREEROOTCOST, ASN_INTEGER, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 7}},
#define   IEEE8021SPANNINGTREEROOTPORT  7
	{(u_char) IEEE8021SPANNINGTREEROOTPORT, ASN_UNSIGNED, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 8}},
#define   IEEE8021SPANNINGTREEMAXAGE  8
	{(u_char) IEEE8021SPANNINGTREEMAXAGE, ASN_INTEGER, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 9}},
#define   IEEE8021SPANNINGTREEHELLOTIME  9
	{(u_char) IEEE8021SPANNINGTREEHELLOTIME, ASN_INTEGER, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 10}},
#define   IEEE8021SPANNINGTREEHOLDTIME  10
	{(u_char) IEEE8021SPANNINGTREEHOLDTIME, ASN_INTEGER, RONLY, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 11}},
#define   IEEE8021SPANNINGTREEFORWARDDELAY  11
	{(u_char) IEEE8021SPANNINGTREEFORWARDDELAY, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 12}},
#define   IEEE8021SPANNINGTREEBRIDGEMAXAGE  12
	{(u_char) IEEE8021SPANNINGTREEBRIDGEMAXAGE, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 13}},
#define   IEEE8021SPANNINGTREEBRIDGEHELLOTIME  13
	{(u_char) IEEE8021SPANNINGTREEBRIDGEHELLOTIME, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 14}},
#define   IEEE8021SPANNINGTREEBRIDGEFORWARDDELAY  14
	{(u_char) IEEE8021SPANNINGTREEBRIDGEFORWARDDELAY, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 15}},
#define   IEEE8021SPANNINGTREEVERSION  15
	{(u_char) IEEE8021SPANNINGTREEVERSION, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 16}},
#define   IEEE8021SPANNINGTREERSTPTXHOLDCOUNT  16
	{(u_char) IEEE8021SPANNINGTREERSTPTXHOLDCOUNT, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreeTable, 4, {1, 1, 1, 17}},
#define   IEEE8021SPANNINGTREEPORTPRIORITY  17
	{(u_char) IEEE8021SPANNINGTREEPORTPRIORITY, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 3}},
#define   IEEE8021SPANNINGTREEPORTSTATE  18
	{(u_char) IEEE8021SPANNINGTREEPORTSTATE, ASN_INTEGER, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 4}},
#define   IEEE8021SPANNINGTREEPORTENABLED  19
	{(u_char) IEEE8021SPANNINGTREEPORTENABLED, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 5}},
#define   IEEE8021SPANNINGTREEPORTPATHCOST  20
	{(u_char) IEEE8021SPANNINGTREEPORTPATHCOST, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 6}},
#define   IEEE8021SPANNINGTREEPORTDESIGNATEDROOT  21
	{(u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDROOT, ASN_OCTET_STR, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 7}},
#define   IEEE8021SPANNINGTREEPORTDESIGNATEDCOST  22
	{(u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDCOST, ASN_INTEGER, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 8}},
#define   IEEE8021SPANNINGTREEPORTDESIGNATEDBRIDGE  23
	{(u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDBRIDGE, ASN_OCTET_STR, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 9}},
#define   IEEE8021SPANNINGTREEPORTDESIGNATEDPORT  24
	{(u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDPORT, ASN_OCTET_STR, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 10}},
#define   IEEE8021SPANNINGTREEPORTFORWARDTRANSITIONS  25
	{(u_char) IEEE8021SPANNINGTREEPORTFORWARDTRANSITIONS, ASN_COUNTER64, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 11}},
#define   IEEE8021SPANNINGTREERSTPPORTPROTOCOLMIGRATION  26
	{(u_char) IEEE8021SPANNINGTREERSTPPORTPROTOCOLMIGRATION, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 12}},
#define   IEEE8021SPANNINGTREERSTPPORTADMINEDGEPORT  27
	{(u_char) IEEE8021SPANNINGTREERSTPPORTADMINEDGEPORT, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 13}},
#define   IEEE8021SPANNINGTREERSTPPORTOPEREDGEPORT  28
	{(u_char) IEEE8021SPANNINGTREERSTPPORTOPEREDGEPORT, ASN_INTEGER, RONLY, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 14}},
#define   IEEE8021SPANNINGTREERSTPPORTADMINPATHCOST  29
	{(u_char) IEEE8021SPANNINGTREERSTPPORTADMINPATHCOST, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortTable, 4, {1, 2, 1, 15}},
#define   IEEE8021SPANNINGTREERSTPPORTAUTOEDGEPORT  30
	{(u_char) IEEE8021SPANNINGTREERSTPPORTAUTOEDGEPORT, ASN_INTEGER, RWRITE, var_ieee8021SpanningTreePortExtensionTable, 4, {1, 3, 1, 1}},
#define   IEEE8021SPANNINGTREERSTPPORTAUTOISOLATEPORT  31
	{(u_char) IEEE8021SPANNINGTREERSTPPORTAUTOISOLATEPORT, ASN_INTEGER, RONLY, var_ieee8021SpanningTreePortExtensionTable, 4, {1, 3, 1, 2}},
#define   IEEE8021SPANNINGTREERSTPPORTISOLATEPORT  32
	{(u_char) IEEE8021SPANNINGTREERSTPPORTISOLATEPORT, ASN_INTEGER, RONLY, var_ieee8021SpanningTreePortExtensionTable, 4, {1, 3, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct ieee8021SpanningTreeMib_data *ieee8021SpanningTreeMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021SpanningTreeTableStorage = NULL;
struct header_complex_index *ieee8021SpanningTreePortTableStorage = NULL;
struct header_complex_index *ieee8021SpanningTreePortExtensionTableStorage = NULL;

void (*ieee8021SpanningTreeMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021SpanningTreeMib_loop_handler(int);
void ieee8021SpanningTreeMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021SpanningTreeMib(void)
 * @brief ieee8021SpanningTreeMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021SpanningTreeMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021SpanningTreeMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "init_ieee8021SpanningTreeMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021SpanningTreeMib", ieee8021SpanningTreeMib_variables, variable4, ieee8021SpanningTreeMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021SpanningTreeMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021SpanningTreeMib", parse_ieee8021SpanningTreeMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpanningTreeTable", parse_ieee8021SpanningTreeTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpanningTreePortTable", parse_ieee8021SpanningTreePortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpanningTreePortExtensionTable", parse_ieee8021SpanningTreePortExtensionTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpanningTreeMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpanningTreeTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpanningTreePortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpanningTreePortExtensionTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021SpanningTreeMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021SpanningTreeMib_fd_handler, (void *) 1);
	}
	ieee8021SpanningTreeMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021SpanningTreeMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021SpanningTreeMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021SpanningTreeMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021SpanningTreeMib(void)
{
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "deinit_ieee8021SpanningTreeMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021SpanningTreeMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021SpanningTreeMib_variables_oid, sizeof(ieee8021SpanningTreeMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021SpanningTreeMib");
	snmpd_unregister_config_handler("ieee8021SpanningTreeTable");
	snmpd_unregister_config_handler("ieee8021SpanningTreePortTable");
	snmpd_unregister_config_handler("ieee8021SpanningTreePortExtensionTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

int
term_ieee8021SpanningTreeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "term_ieee8021SpanningTreeMib: terminating...  "));
	deinit_ieee8021SpanningTreeMib();
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021SpanningTreeMib_data *ieee8021SpanningTreeMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021SpanningTreeMib.
 *
 * Creates a new ieee8021SpanningTreeMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021SpanningTreeMib.
 */
struct ieee8021SpanningTreeMib_data *
ieee8021SpanningTreeMib_create(void)
{
	struct ieee8021SpanningTreeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreeMib_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpanningTreeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpanningTreeMib_data *ieee8021SpanningTreeMib_duplicate(struct ieee8021SpanningTreeMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021SpanningTreeMib_data *
ieee8021SpanningTreeMib_duplicate(struct ieee8021SpanningTreeMib_data *thedata)
{
	struct ieee8021SpanningTreeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreeMib_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpanningTreeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpanningTreeMib_destroy(struct ieee8021SpanningTreeMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021SpanningTreeMib.
 * @brief delete a scalars structure from ieee8021SpanningTreeMib.
 *
 * Frees scalars that were previously removed from ieee8021SpanningTreeMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpanningTreeMib_destroy(struct ieee8021SpanningTreeMib_data **thedata)
{
	struct ieee8021SpanningTreeMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreeMib_add(struct ieee8021SpanningTreeMib_data *thedata)
 * @param thedata the structure representing ieee8021SpanningTreeMib scalars.
 * @brief adds node to the ieee8021SpanningTreeMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021SpanningTreeMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021SpanningTreeMib_add(struct ieee8021SpanningTreeMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeMib_add: adding data...  "));
	if (thedata)
		ieee8021SpanningTreeMibStorage = thedata;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpanningTreeMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpanningTreeMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpanningTreeMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021SpanningTreeMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpanningTreeMib_data *StorageTmp = ieee8021SpanningTreeMib_create();

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "parse_ieee8021SpanningTreeMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021SpanningTreeMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/*
 * store_ieee8021SpanningTreeMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpanningTreeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpanningTreeMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "store_ieee8021SpanningTreeMib: storing data...  "));
	refresh_ieee8021SpanningTreeMib(1);
	if ((StorageTmp = ieee8021SpanningTreeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021SpanningTreeMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021SpanningTreeMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021SpanningTreeMib(struct ieee8021SpanningTreeMib_data *StorageTmp, struct ieee8021SpanningTreeMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021SpanningTreeMib(struct ieee8021SpanningTreeMib_data *StorageTmp, struct ieee8021SpanningTreeMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpanningTreeMib(struct ieee8021SpanningTreeMib_data *StorageTmp, struct ieee8021SpanningTreeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021SpanningTreeMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpanningTreeMib(struct ieee8021SpanningTreeMib_data *StorageTmp, struct ieee8021SpanningTreeMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpanningTreeMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021SpanningTreeMib(struct 
 * @fn void revert_ieee8021SpanningTreeMib(struct ieee8021SpanningTreeMib_data *StorageTmp, struct ieee8021SpanningTreeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021SpanningTreeMib(struct ieee8021SpanningTreeMib_data *StorageTmp, struct ieee8021SpanningTreeMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpanningTreeMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpanningTreeMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021SpanningTreeMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021SpanningTreeMib(int force)
{
	if (ieee8021SpanningTreeMibStorage == NULL) {
		struct ieee8021SpanningTreeMib_data *StorageNew;

		if ((StorageNew = ieee8021SpanningTreeMib_create()) == NULL)
			return;
		ieee8021SpanningTreeMibStorage = StorageNew;
		ieee8021SpanningTreeMib_refresh = 1;
	}
	if (!force && ieee8021SpanningTreeMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "refresh_ieee8021SpanningTreeMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021SpanningTreeMib_refresh = 0;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021SpanningTreeMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021SpanningTreeMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021SpanningTreeMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpanningTreeMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "var_ieee8021SpanningTreeMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021SpanningTreeMib(0);
	if ((StorageTmp = ieee8021SpanningTreeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021SpanningTreeMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021SpanningTreeMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021SpanningTreeMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021SpanningTreeTable_data *ieee8021SpanningTreeTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpanningTreeTable table.
 *
 * Creates a new ieee8021SpanningTreeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpanningTreeTable_data *
ieee8021SpanningTreeTable_create(void)
{
	struct ieee8021SpanningTreeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreeTable_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpanningTreeProtocolSpecification = 0;
		StorageNew->ieee8021SpanningTreePriority = 0;
		StorageNew->ieee8021SpanningTreeTimeSinceTopologyChange = 0;
		StorageNew->ieee8021SpanningTreeTopChanges = (struct counter64) {
		0, 0};
		if ((StorageNew->ieee8021SpanningTreeDesignatedRoot = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpanningTreeDesignatedRootLen = 0;
		StorageNew->ieee8021SpanningTreeDesignatedRoot[StorageNew->ieee8021SpanningTreeDesignatedRootLen] = 0;
		StorageNew->ieee8021SpanningTreeRootCost = 0;
		StorageNew->ieee8021SpanningTreeRootPort = 0;
		StorageNew->ieee8021SpanningTreeMaxAge = 0;
		StorageNew->ieee8021SpanningTreeHelloTime = 0;
		StorageNew->ieee8021SpanningTreeHoldTime = 0;
		StorageNew->ieee8021SpanningTreeForwardDelay = 0;
		StorageNew->ieee8021SpanningTreeBridgeMaxAge = 0;
		StorageNew->ieee8021SpanningTreeBridgeHelloTime = 0;
		StorageNew->ieee8021SpanningTreeBridgeForwardDelay = 0;
		StorageNew->ieee8021SpanningTreeVersion = 0;
		StorageNew->ieee8021SpanningTreeRstpTxHoldCount = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpanningTreeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpanningTreeTable_data *ieee8021SpanningTreeTable_duplicate(struct ieee8021SpanningTreeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpanningTreeTable_data *
ieee8021SpanningTreeTable_duplicate(struct ieee8021SpanningTreeTable_data *thedata)
{
	struct ieee8021SpanningTreeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreeTable_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpanningTreeTable_id = thedata->ieee8021SpanningTreeTable_id;
		StorageNew->ieee8021SpanningTreeComponentId = thedata->ieee8021SpanningTreeComponentId;
		StorageNew->ieee8021SpanningTreeProtocolSpecification = thedata->ieee8021SpanningTreeProtocolSpecification;
		StorageNew->ieee8021SpanningTreePriority = thedata->ieee8021SpanningTreePriority;
		StorageNew->ieee8021SpanningTreeTimeSinceTopologyChange = thedata->ieee8021SpanningTreeTimeSinceTopologyChange;
		StorageNew->ieee8021SpanningTreeTopChanges = thedata->ieee8021SpanningTreeTopChanges;
		if (!(StorageNew->ieee8021SpanningTreeDesignatedRoot = malloc(thedata->ieee8021SpanningTreeDesignatedRootLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpanningTreeDesignatedRoot, thedata->ieee8021SpanningTreeDesignatedRoot, thedata->ieee8021SpanningTreeDesignatedRootLen);
		StorageNew->ieee8021SpanningTreeDesignatedRootLen = thedata->ieee8021SpanningTreeDesignatedRootLen;
		StorageNew->ieee8021SpanningTreeDesignatedRoot[StorageNew->ieee8021SpanningTreeDesignatedRootLen] = 0;
		StorageNew->ieee8021SpanningTreeRootCost = thedata->ieee8021SpanningTreeRootCost;
		StorageNew->ieee8021SpanningTreeRootPort = thedata->ieee8021SpanningTreeRootPort;
		StorageNew->ieee8021SpanningTreeMaxAge = thedata->ieee8021SpanningTreeMaxAge;
		StorageNew->ieee8021SpanningTreeHelloTime = thedata->ieee8021SpanningTreeHelloTime;
		StorageNew->ieee8021SpanningTreeHoldTime = thedata->ieee8021SpanningTreeHoldTime;
		StorageNew->ieee8021SpanningTreeForwardDelay = thedata->ieee8021SpanningTreeForwardDelay;
		StorageNew->ieee8021SpanningTreeBridgeMaxAge = thedata->ieee8021SpanningTreeBridgeMaxAge;
		StorageNew->ieee8021SpanningTreeBridgeHelloTime = thedata->ieee8021SpanningTreeBridgeHelloTime;
		StorageNew->ieee8021SpanningTreeBridgeForwardDelay = thedata->ieee8021SpanningTreeBridgeForwardDelay;
		StorageNew->ieee8021SpanningTreeVersion = thedata->ieee8021SpanningTreeVersion;
		StorageNew->ieee8021SpanningTreeRstpTxHoldCount = thedata->ieee8021SpanningTreeRstpTxHoldCount;
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpanningTreeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpanningTreeTable_destroy(struct ieee8021SpanningTreeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpanningTreeTable_destroy(struct ieee8021SpanningTreeTable_data **thedata)
{
	struct ieee8021SpanningTreeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpanningTreeDesignatedRoot);
		StorageDel->ieee8021SpanningTreeDesignatedRootLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreeTable_add(struct ieee8021SpanningTreeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpanningTreeTable table data set.
 *
 * Adds a table row structure to the ieee8021SpanningTreeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpanningTreeTable_add(struct ieee8021SpanningTreeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpanningTreeComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpanningTreeComponentId, sizeof(thedata->ieee8021SpanningTreeComponentId));
		header_complex_add_data(&ieee8021SpanningTreeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreeTable_del(struct ieee8021SpanningTreeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpanningTreeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpanningTreeTable_del(struct ieee8021SpanningTreeTable_data *thedata)
{
	struct ieee8021SpanningTreeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpanningTreeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpanningTreeTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpanningTreeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpanningTreeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpanningTreeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpanningTreeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpanningTreeTable_data *StorageTmp = ieee8021SpanningTreeTable_create();

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "parse_ieee8021SpanningTreeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpanningTreeComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeProtocolSpecification, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreePriority, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021SpanningTreeTimeSinceTopologyChange, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021SpanningTreeTopChanges, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpanningTreeDesignatedRoot);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpanningTreeDesignatedRoot, &StorageTmp->ieee8021SpanningTreeDesignatedRootLen);
	if (StorageTmp->ieee8021SpanningTreeDesignatedRoot == NULL) {
		config_perror("invalid specification for ieee8021SpanningTreeDesignatedRoot");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRootCost, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpanningTreeRootPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeMaxAge, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeHelloTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeHoldTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeForwardDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeBridgeMaxAge, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeBridgeHelloTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeBridgeForwardDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeVersion, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpTxHoldCount, &tmpsize);
	ieee8021SpanningTreeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/*
 * store_ieee8021SpanningTreeTable(): store configuraiton file for ieee8021SpanningTreeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpanningTreeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpanningTreeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "store_ieee8021SpanningTreeTable: storing data...  "));
	refresh_ieee8021SpanningTreeTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpanningTreeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpanningTreeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpanningTreeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpanningTreeComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeProtocolSpecification, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreePriority, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021SpanningTreeTimeSinceTopologyChange, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021SpanningTreeTopChanges, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpanningTreeDesignatedRoot, &StorageTmp->ieee8021SpanningTreeDesignatedRootLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRootCost, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpanningTreeRootPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeMaxAge, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeHelloTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeHoldTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeForwardDelay, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeBridgeMaxAge, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeBridgeHelloTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeBridgeForwardDelay, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeVersion, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpTxHoldCount, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpanningTreePortTable_data *ieee8021SpanningTreePortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpanningTreePortTable table.
 *
 * Creates a new ieee8021SpanningTreePortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpanningTreePortTable_data *
ieee8021SpanningTreePortTable_create(void)
{
	struct ieee8021SpanningTreePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreePortTable_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpanningTreePortPriority = 0;
		StorageNew->ieee8021SpanningTreePortState = 0;
		StorageNew->ieee8021SpanningTreePortEnabled = 0;
		StorageNew->ieee8021SpanningTreePortPathCost = 0;
		if ((StorageNew->ieee8021SpanningTreePortDesignatedRoot = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpanningTreePortDesignatedRootLen = 0;
		StorageNew->ieee8021SpanningTreePortDesignatedRoot[StorageNew->ieee8021SpanningTreePortDesignatedRootLen] = 0;
		StorageNew->ieee8021SpanningTreePortDesignatedCost = 0;
		if ((StorageNew->ieee8021SpanningTreePortDesignatedBridge = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpanningTreePortDesignatedBridgeLen = 0;
		StorageNew->ieee8021SpanningTreePortDesignatedBridge[StorageNew->ieee8021SpanningTreePortDesignatedBridgeLen] = 0;
		if ((StorageNew->ieee8021SpanningTreePortDesignatedPort = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpanningTreePortDesignatedPortLen = 0;
		StorageNew->ieee8021SpanningTreePortDesignatedPort[StorageNew->ieee8021SpanningTreePortDesignatedPortLen] = 0;
		StorageNew->ieee8021SpanningTreePortForwardTransitions = (struct counter64) {
		0, 0};
		StorageNew->ieee8021SpanningTreeRstpPortProtocolMigration = 0;
		StorageNew->ieee8021SpanningTreeRstpPortAdminEdgePort = 0;
		StorageNew->ieee8021SpanningTreeRstpPortOperEdgePort = 0;
		StorageNew->ieee8021SpanningTreeRstpPortAdminPathCost = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpanningTreePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpanningTreePortTable_data *ieee8021SpanningTreePortTable_duplicate(struct ieee8021SpanningTreePortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpanningTreePortTable_data *
ieee8021SpanningTreePortTable_duplicate(struct ieee8021SpanningTreePortTable_data *thedata)
{
	struct ieee8021SpanningTreePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreePortTable_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpanningTreePortTable_id = thedata->ieee8021SpanningTreePortTable_id;
		StorageNew->ieee8021SpanningTreePortComponentId = thedata->ieee8021SpanningTreePortComponentId;
		StorageNew->ieee8021SpanningTreePort = thedata->ieee8021SpanningTreePort;
		StorageNew->ieee8021SpanningTreePortPriority = thedata->ieee8021SpanningTreePortPriority;
		StorageNew->ieee8021SpanningTreePortState = thedata->ieee8021SpanningTreePortState;
		StorageNew->ieee8021SpanningTreePortEnabled = thedata->ieee8021SpanningTreePortEnabled;
		StorageNew->ieee8021SpanningTreePortPathCost = thedata->ieee8021SpanningTreePortPathCost;
		if (!(StorageNew->ieee8021SpanningTreePortDesignatedRoot = malloc(thedata->ieee8021SpanningTreePortDesignatedRootLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpanningTreePortDesignatedRoot, thedata->ieee8021SpanningTreePortDesignatedRoot, thedata->ieee8021SpanningTreePortDesignatedRootLen);
		StorageNew->ieee8021SpanningTreePortDesignatedRootLen = thedata->ieee8021SpanningTreePortDesignatedRootLen;
		StorageNew->ieee8021SpanningTreePortDesignatedRoot[StorageNew->ieee8021SpanningTreePortDesignatedRootLen] = 0;
		StorageNew->ieee8021SpanningTreePortDesignatedCost = thedata->ieee8021SpanningTreePortDesignatedCost;
		if (!(StorageNew->ieee8021SpanningTreePortDesignatedBridge = malloc(thedata->ieee8021SpanningTreePortDesignatedBridgeLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpanningTreePortDesignatedBridge, thedata->ieee8021SpanningTreePortDesignatedBridge, thedata->ieee8021SpanningTreePortDesignatedBridgeLen);
		StorageNew->ieee8021SpanningTreePortDesignatedBridgeLen = thedata->ieee8021SpanningTreePortDesignatedBridgeLen;
		StorageNew->ieee8021SpanningTreePortDesignatedBridge[StorageNew->ieee8021SpanningTreePortDesignatedBridgeLen] = 0;
		if (!(StorageNew->ieee8021SpanningTreePortDesignatedPort = malloc(thedata->ieee8021SpanningTreePortDesignatedPortLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpanningTreePortDesignatedPort, thedata->ieee8021SpanningTreePortDesignatedPort, thedata->ieee8021SpanningTreePortDesignatedPortLen);
		StorageNew->ieee8021SpanningTreePortDesignatedPortLen = thedata->ieee8021SpanningTreePortDesignatedPortLen;
		StorageNew->ieee8021SpanningTreePortDesignatedPort[StorageNew->ieee8021SpanningTreePortDesignatedPortLen] = 0;
		StorageNew->ieee8021SpanningTreePortForwardTransitions = thedata->ieee8021SpanningTreePortForwardTransitions;
		StorageNew->ieee8021SpanningTreeRstpPortProtocolMigration = thedata->ieee8021SpanningTreeRstpPortProtocolMigration;
		StorageNew->ieee8021SpanningTreeRstpPortAdminEdgePort = thedata->ieee8021SpanningTreeRstpPortAdminEdgePort;
		StorageNew->ieee8021SpanningTreeRstpPortOperEdgePort = thedata->ieee8021SpanningTreeRstpPortOperEdgePort;
		StorageNew->ieee8021SpanningTreeRstpPortAdminPathCost = thedata->ieee8021SpanningTreeRstpPortAdminPathCost;
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpanningTreePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpanningTreePortTable_destroy(struct ieee8021SpanningTreePortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpanningTreePortTable_destroy(struct ieee8021SpanningTreePortTable_data **thedata)
{
	struct ieee8021SpanningTreePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpanningTreePortDesignatedRoot);
		StorageDel->ieee8021SpanningTreePortDesignatedRootLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpanningTreePortDesignatedBridge);
		StorageDel->ieee8021SpanningTreePortDesignatedBridgeLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpanningTreePortDesignatedPort);
		StorageDel->ieee8021SpanningTreePortDesignatedPortLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreePortTable_add(struct ieee8021SpanningTreePortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpanningTreePortTable table data set.
 *
 * Adds a table row structure to the ieee8021SpanningTreePortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpanningTreePortTable_add(struct ieee8021SpanningTreePortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpanningTreePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpanningTreePortComponentId, sizeof(thedata->ieee8021SpanningTreePortComponentId));
		/* ieee8021SpanningTreePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpanningTreePort, sizeof(thedata->ieee8021SpanningTreePort));
		header_complex_add_data(&ieee8021SpanningTreePortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreePortTable_del(struct ieee8021SpanningTreePortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpanningTreePortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpanningTreePortTable_del(struct ieee8021SpanningTreePortTable_data *thedata)
{
	struct ieee8021SpanningTreePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpanningTreePortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpanningTreePortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpanningTreePortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpanningTreePortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpanningTreePortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpanningTreePortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpanningTreePortTable_data *StorageTmp = ieee8021SpanningTreePortTable_create();

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "parse_ieee8021SpanningTreePortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpanningTreePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpanningTreePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreePortPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreePortState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreePortEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreePortPathCost, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpanningTreePortDesignatedRoot);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpanningTreePortDesignatedRoot, &StorageTmp->ieee8021SpanningTreePortDesignatedRootLen);
	if (StorageTmp->ieee8021SpanningTreePortDesignatedRoot == NULL) {
		config_perror("invalid specification for ieee8021SpanningTreePortDesignatedRoot");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreePortDesignatedCost, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpanningTreePortDesignatedBridge);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpanningTreePortDesignatedBridge, &StorageTmp->ieee8021SpanningTreePortDesignatedBridgeLen);
	if (StorageTmp->ieee8021SpanningTreePortDesignatedBridge == NULL) {
		config_perror("invalid specification for ieee8021SpanningTreePortDesignatedBridge");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpanningTreePortDesignatedPort);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpanningTreePortDesignatedPort, &StorageTmp->ieee8021SpanningTreePortDesignatedPortLen);
	if (StorageTmp->ieee8021SpanningTreePortDesignatedPort == NULL) {
		config_perror("invalid specification for ieee8021SpanningTreePortDesignatedPort");
		return;
	}
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021SpanningTreePortForwardTransitions, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortOperEdgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost, &tmpsize);
	ieee8021SpanningTreePortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/*
 * store_ieee8021SpanningTreePortTable(): store configuraiton file for ieee8021SpanningTreePortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpanningTreePortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpanningTreePortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "store_ieee8021SpanningTreePortTable: storing data...  "));
	refresh_ieee8021SpanningTreePortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpanningTreePortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpanningTreePortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpanningTreePortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpanningTreePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpanningTreePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreePortPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreePortState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreePortEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreePortPathCost, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpanningTreePortDesignatedRoot, &StorageTmp->ieee8021SpanningTreePortDesignatedRootLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreePortDesignatedCost, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpanningTreePortDesignatedBridge, &StorageTmp->ieee8021SpanningTreePortDesignatedBridgeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpanningTreePortDesignatedPort, &StorageTmp->ieee8021SpanningTreePortDesignatedPortLen);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021SpanningTreePortForwardTransitions, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortOperEdgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpanningTreePortExtensionTable_data *ieee8021SpanningTreePortExtensionTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpanningTreePortExtensionTable table.
 *
 * Creates a new ieee8021SpanningTreePortExtensionTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpanningTreePortExtensionTable_data *
ieee8021SpanningTreePortExtensionTable_create(void)
{
	struct ieee8021SpanningTreePortExtensionTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreePortExtensionTable_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortExtensionTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpanningTreePortComponentId = 0;
		StorageNew->ieee8021SpanningTreePort = 0;
		StorageNew->ieee8021SpanningTreeRstpPortAutoEdgePort = 0;
		StorageNew->ieee8021SpanningTreeRstpPortAutoIsolatePort = 0;
		StorageNew->ieee8021SpanningTreeRstpPortIsolatePort = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpanningTreePortExtensionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpanningTreePortExtensionTable_data *ieee8021SpanningTreePortExtensionTable_duplicate(struct ieee8021SpanningTreePortExtensionTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpanningTreePortExtensionTable_data *
ieee8021SpanningTreePortExtensionTable_duplicate(struct ieee8021SpanningTreePortExtensionTable_data *thedata)
{
	struct ieee8021SpanningTreePortExtensionTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpanningTreePortExtensionTable_data);

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortExtensionTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpanningTreePortExtensionTable_id = thedata->ieee8021SpanningTreePortExtensionTable_id;
		StorageNew->ieee8021SpanningTreePortComponentId = thedata->ieee8021SpanningTreePortComponentId;
		StorageNew->ieee8021SpanningTreePort = thedata->ieee8021SpanningTreePort;
		StorageNew->ieee8021SpanningTreeRstpPortAutoEdgePort = thedata->ieee8021SpanningTreeRstpPortAutoEdgePort;
		StorageNew->ieee8021SpanningTreeRstpPortAutoIsolatePort = thedata->ieee8021SpanningTreeRstpPortAutoIsolatePort;
		StorageNew->ieee8021SpanningTreeRstpPortIsolatePort = thedata->ieee8021SpanningTreeRstpPortIsolatePort;
	}
      done:
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpanningTreePortExtensionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpanningTreePortExtensionTable_destroy(struct ieee8021SpanningTreePortExtensionTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpanningTreePortExtensionTable_destroy(struct ieee8021SpanningTreePortExtensionTable_data **thedata)
{
	struct ieee8021SpanningTreePortExtensionTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortExtensionTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreePortExtensionTable_add(struct ieee8021SpanningTreePortExtensionTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpanningTreePortExtensionTable table data set.
 *
 * Adds a table row structure to the ieee8021SpanningTreePortExtensionTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpanningTreePortExtensionTable_add(struct ieee8021SpanningTreePortExtensionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortExtensionTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpanningTreePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpanningTreePortComponentId, sizeof(thedata->ieee8021SpanningTreePortComponentId));
		/* ieee8021SpanningTreePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpanningTreePort, sizeof(thedata->ieee8021SpanningTreePort));
		header_complex_add_data(&ieee8021SpanningTreePortExtensionTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpanningTreePortExtensionTable_del(struct ieee8021SpanningTreePortExtensionTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpanningTreePortExtensionTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpanningTreePortExtensionTable_del(struct ieee8021SpanningTreePortExtensionTable_data *thedata)
{
	struct ieee8021SpanningTreePortExtensionTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreePortExtensionTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpanningTreePortExtensionTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpanningTreePortExtensionTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpanningTreePortExtensionTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpanningTreePortExtensionTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpanningTreePortExtensionTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpanningTreePortExtensionTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp = ieee8021SpanningTreePortExtensionTable_create();

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "parse_ieee8021SpanningTreePortExtensionTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpanningTreePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpanningTreePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortAutoIsolatePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpanningTreeRstpPortIsolatePort, &tmpsize);
	ieee8021SpanningTreePortExtensionTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/*
 * store_ieee8021SpanningTreePortExtensionTable(): store configuraiton file for ieee8021SpanningTreePortExtensionTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpanningTreePortExtensionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "store_ieee8021SpanningTreePortExtensionTable: storing data...  "));
	refresh_ieee8021SpanningTreePortExtensionTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpanningTreePortExtensionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpanningTreePortExtensionTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpanningTreePortExtensionTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpanningTreePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpanningTreePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortAutoIsolatePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpanningTreeRstpPortIsolatePort, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, struct ieee8021SpanningTreeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, struct ieee8021SpanningTreeTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, struct ieee8021SpanningTreeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpanningTreeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, struct ieee8021SpanningTreeTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpanningTreeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, struct ieee8021SpanningTreeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, struct ieee8021SpanningTreeTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpanningTreeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpanningTreeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpanningTreeTable_data *
refresh_ieee8021SpanningTreeTable_row(struct ieee8021SpanningTreeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpanningTreeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpanningTreeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpanningTreeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpanningTreeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpanningTreeTable(int force)
{
	if (!force && ieee8021SpanningTreeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpanningTreeTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpanningTreeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpanningTreeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpanningTreeMib above.
 */
u_char *
var_ieee8021SpanningTreeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "var_ieee8021SpanningTreeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpanningTreeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpanningTreeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPANNINGTREEPROTOCOLSPECIFICATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeProtocolSpecification);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeProtocolSpecification;
		break;
	case (u_char) IEEE8021SPANNINGTREEPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreePriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePriority);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePriority;
		break;
	case (u_char) IEEE8021SPANNINGTREETIMESINCETOPOLOGYCHANGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeTimeSinceTopologyChange);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeTimeSinceTopologyChange;
		break;
	case (u_char) IEEE8021SPANNINGTREETOPCHANGES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeTopChanges);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeTopChanges;
		break;
	case (u_char) IEEE8021SPANNINGTREEDESIGNATEDROOT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpanningTreeDesignatedRootLen;
		rval = (u_char *) StorageTmp->ieee8021SpanningTreeDesignatedRoot;
		break;
	case (u_char) IEEE8021SPANNINGTREEROOTCOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRootCost);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRootCost;
		break;
	case (u_char) IEEE8021SPANNINGTREEROOTPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRootPort);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRootPort;
		break;
	case (u_char) IEEE8021SPANNINGTREEMAXAGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeMaxAge);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeMaxAge;
		break;
	case (u_char) IEEE8021SPANNINGTREEHELLOTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeHelloTime);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeHelloTime;
		break;
	case (u_char) IEEE8021SPANNINGTREEHOLDTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeHoldTime);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeHoldTime;
		break;
	case (u_char) IEEE8021SPANNINGTREEFORWARDDELAY:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeForwardDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeForwardDelay);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeForwardDelay;
		break;
	case (u_char) IEEE8021SPANNINGTREEBRIDGEMAXAGE:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeBridgeMaxAge;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeBridgeMaxAge);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeBridgeMaxAge;
		break;
	case (u_char) IEEE8021SPANNINGTREEBRIDGEHELLOTIME:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeBridgeHelloTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeBridgeHelloTime);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeBridgeHelloTime;
		break;
	case (u_char) IEEE8021SPANNINGTREEBRIDGEFORWARDDELAY:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeBridgeForwardDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeBridgeForwardDelay);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeBridgeForwardDelay;
		break;
	case (u_char) IEEE8021SPANNINGTREEVERSION:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeVersion;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeVersion);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeVersion;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPTXHOLDCOUNT:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeRstpTxHoldCount;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpTxHoldCount);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpTxHoldCount;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, struct ieee8021SpanningTreePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, struct ieee8021SpanningTreePortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, struct ieee8021SpanningTreePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpanningTreePortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, struct ieee8021SpanningTreePortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpanningTreePortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, struct ieee8021SpanningTreePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, struct ieee8021SpanningTreePortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpanningTreePortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpanningTreePortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpanningTreePortTable_data *
refresh_ieee8021SpanningTreePortTable_row(struct ieee8021SpanningTreePortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpanningTreePortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpanningTreePortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpanningTreePortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpanningTreePortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpanningTreePortTable(int force)
{
	if (!force && ieee8021SpanningTreePortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpanningTreePortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpanningTreePortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpanningTreePortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpanningTreeMib above.
 */
u_char *
var_ieee8021SpanningTreePortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "var_ieee8021SpanningTreePortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpanningTreePortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpanningTreePortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPANNINGTREEPORTPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreePortPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePortPriority);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePortPriority;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePortState);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePortState;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTENABLED:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreePortEnabled;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePortEnabled);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePortEnabled;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTPATHCOST:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreePortPathCost;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePortPathCost);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePortPathCost;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDROOT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpanningTreePortDesignatedRootLen;
		rval = (u_char *) StorageTmp->ieee8021SpanningTreePortDesignatedRoot;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDCOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePortDesignatedCost);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePortDesignatedCost;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDBRIDGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpanningTreePortDesignatedBridgeLen;
		rval = (u_char *) StorageTmp->ieee8021SpanningTreePortDesignatedBridge;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTDESIGNATEDPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpanningTreePortDesignatedPortLen;
		rval = (u_char *) StorageTmp->ieee8021SpanningTreePortDesignatedPort;
		break;
	case (u_char) IEEE8021SPANNINGTREEPORTFORWARDTRANSITIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreePortForwardTransitions);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreePortForwardTransitions;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPPORTPROTOCOLMIGRATION:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeRstpPortProtocolMigration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPPORTADMINEDGEPORT:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeRstpPortAdminEdgePort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPPORTOPEREDGEPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortOperEdgePort);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortOperEdgePort;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPPORTADMINPATHCOST:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeRstpPortAdminPathCost;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, struct ieee8021SpanningTreePortExtensionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, struct ieee8021SpanningTreePortExtensionTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, struct ieee8021SpanningTreePortExtensionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpanningTreePortExtensionTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, struct ieee8021SpanningTreePortExtensionTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpanningTreePortExtensionTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, struct ieee8021SpanningTreePortExtensionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, struct ieee8021SpanningTreePortExtensionTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpanningTreePortExtensionTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpanningTreePortExtensionTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpanningTreePortExtensionTable_data *
refresh_ieee8021SpanningTreePortExtensionTable_row(struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpanningTreePortExtensionTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpanningTreePortExtensionTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpanningTreePortExtensionTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpanningTreePortExtensionTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpanningTreePortExtensionTable(int force)
{
	if (!force && ieee8021SpanningTreePortExtensionTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpanningTreePortExtensionTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpanningTreePortExtensionTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpanningTreePortExtensionTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpanningTreeMib above.
 */
u_char *
var_ieee8021SpanningTreePortExtensionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "var_ieee8021SpanningTreePortExtensionTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpanningTreePortExtensionTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpanningTreePortExtensionTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpanningTreePortExtensionTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPANNINGTREERSTPPORTAUTOEDGEPORT:	/* ReadWrite */
		*write_method = write_ieee8021SpanningTreeRstpPortAutoEdgePort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPPORTAUTOISOLATEPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortAutoIsolatePort);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortAutoIsolatePort;
		break;
	case (u_char) IEEE8021SPANNINGTREERSTPPORTISOLATEPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpanningTreeRstpPortIsolatePort);
		rval = (u_char *) &StorageTmp->ieee8021SpanningTreeRstpPortIsolatePort;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021SpanningTreePriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreePriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreePriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePriority not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreePriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreePriority for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreePriority = StorageOld->ieee8021SpanningTreePriority;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeForwardDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeForwardDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeForwardDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeForwardDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeForwardDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeForwardDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeForwardDelay for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeForwardDelay = StorageOld->ieee8021SpanningTreeForwardDelay;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeBridgeMaxAge(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeBridgeMaxAge(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeBridgeMaxAge entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeMaxAge not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeMaxAge: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 600..4000 */
		if ((600 > set_value || set_value > 4000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeMaxAge: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeBridgeMaxAge = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeBridgeMaxAge for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeBridgeMaxAge = StorageOld->ieee8021SpanningTreeBridgeMaxAge;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeBridgeHelloTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeBridgeHelloTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeBridgeHelloTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeHelloTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeHelloTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 100..1000 */
		if ((100 > set_value || set_value > 1000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeHelloTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeBridgeHelloTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeBridgeHelloTime for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeBridgeHelloTime = StorageOld->ieee8021SpanningTreeBridgeHelloTime;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeBridgeForwardDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeBridgeForwardDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeBridgeForwardDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeForwardDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeForwardDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 400..3000 */
		if ((400 > set_value || set_value > 3000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeBridgeForwardDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeBridgeForwardDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeBridgeForwardDelay for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeBridgeForwardDelay = StorageOld->ieee8021SpanningTreeBridgeForwardDelay;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeVersion entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeVersion not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeVersion: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021SPANNINGTREEVERSION_STP:
		case IEEE8021SPANNINGTREEVERSION_RSTP:
		case IEEE8021SPANNINGTREEVERSION_MSTP:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeVersion: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeVersion = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeVersion for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeVersion = StorageOld->ieee8021SpanningTreeVersion;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeRstpTxHoldCount(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeRstpTxHoldCount(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeRstpTxHoldCount entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreeTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpTxHoldCount not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpTxHoldCount: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..10 */
		if ((1 > set_value || set_value > 10)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpTxHoldCount: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old = ieee8021SpanningTreeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreeTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeRstpTxHoldCount = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreeTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeRstpTxHoldCount for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreeTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) != NULL) {
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
			StorageTmp->ieee8021SpanningTreeTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreeTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreeTable_sets == 0)
			revert_ieee8021SpanningTreeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreeTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeRstpTxHoldCount = StorageOld->ieee8021SpanningTreeRstpTxHoldCount;
		if (--StorageTmp->ieee8021SpanningTreeTable_rsvs == 0)
			ieee8021SpanningTreeTable_destroy(&StorageTmp->ieee8021SpanningTreeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreePortPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreePortPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreePortPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortPriority not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..255 */
		if ((0 > set_value || set_value > 255)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old = ieee8021SpanningTreePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortTable_rsvs++;
		StorageTmp->ieee8021SpanningTreePortPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreePortPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
			StorageTmp->ieee8021SpanningTreePortTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
			revert_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreePortPriority = StorageOld->ieee8021SpanningTreePortPriority;
		if (--StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreePortEnabled(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreePortEnabled(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreePortEnabled entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortEnabled not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortEnabled: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortEnabled: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old = ieee8021SpanningTreePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortTable_rsvs++;
		StorageTmp->ieee8021SpanningTreePortEnabled = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreePortEnabled for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
			StorageTmp->ieee8021SpanningTreePortTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
			revert_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreePortEnabled = StorageOld->ieee8021SpanningTreePortEnabled;
		if (--StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreePortPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreePortPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreePortPathCost entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortPathCost not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortPathCost: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..200000000 */
		if ((1 > set_value || set_value > 200000000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreePortPathCost: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old = ieee8021SpanningTreePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortTable_rsvs++;
		StorageTmp->ieee8021SpanningTreePortPathCost = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreePortPathCost for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
			StorageTmp->ieee8021SpanningTreePortTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
			revert_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreePortPathCost = StorageOld->ieee8021SpanningTreePortPathCost;
		if (--StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeRstpPortProtocolMigration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeRstpPortProtocolMigration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeRstpPortProtocolMigration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortProtocolMigration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortProtocolMigration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortProtocolMigration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old = ieee8021SpanningTreePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
			StorageTmp->ieee8021SpanningTreePortTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
			revert_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeRstpPortProtocolMigration = StorageOld->ieee8021SpanningTreeRstpPortProtocolMigration;
		if (--StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeRstpPortAdminEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeRstpPortAdminEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeRstpPortAdminEdgePort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAdminEdgePort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAdminEdgePort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAdminEdgePort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old = ieee8021SpanningTreePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
			StorageTmp->ieee8021SpanningTreePortTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
			revert_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeRstpPortAdminEdgePort = StorageOld->ieee8021SpanningTreeRstpPortAdminEdgePort;
		if (--StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeRstpPortAdminPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeRstpPortAdminPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeRstpPortAdminPathCost entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAdminPathCost not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAdminPathCost: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..200000000 */
		if ((0 > set_value || set_value > 200000000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAdminPathCost: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old = ieee8021SpanningTreePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) != NULL) {
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
			StorageTmp->ieee8021SpanningTreePortTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortTable_sets == 0)
			revert_ieee8021SpanningTreePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeRstpPortAdminPathCost = StorageOld->ieee8021SpanningTreeRstpPortAdminPathCost;
		if (--StorageTmp->ieee8021SpanningTreePortTable_rsvs == 0)
			ieee8021SpanningTreePortTable_destroy(&StorageTmp->ieee8021SpanningTreePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpanningTreeRstpPortAutoEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpanningTreeRstpPortAutoEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpanningTreePortExtensionTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "write_ieee8021SpanningTreeRstpPortAutoEdgePort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpanningTreePortExtensionTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAutoEdgePort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAutoEdgePort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpanningTreeRstpPortAutoEdgePort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old) == NULL)
			if (StorageTmp->ieee8021SpanningTreePortExtensionTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old = ieee8021SpanningTreePortExtensionTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpanningTreePortExtensionTable_rsvs++;
		StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpanningTreePortExtensionTable_tsts == 0)
				if ((ret = check_ieee8021SpanningTreePortExtensionTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortExtensionTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpanningTreePortExtensionTable_sets == 0)
				if ((ret = update_ieee8021SpanningTreePortExtensionTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpanningTreePortExtensionTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old) != NULL) {
			ieee8021SpanningTreePortExtensionTable_destroy(&StorageTmp->ieee8021SpanningTreePortExtensionTable_old);
			StorageTmp->ieee8021SpanningTreePortExtensionTable_rsvs = 0;
			StorageTmp->ieee8021SpanningTreePortExtensionTable_tsts = 0;
			StorageTmp->ieee8021SpanningTreePortExtensionTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpanningTreePortExtensionTable_sets == 0)
			revert_ieee8021SpanningTreePortExtensionTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpanningTreePortExtensionTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpanningTreeRstpPortAutoEdgePort = StorageOld->ieee8021SpanningTreeRstpPortAutoEdgePort;
		if (--StorageTmp->ieee8021SpanningTreePortExtensionTable_rsvs == 0)
			ieee8021SpanningTreePortExtensionTable_destroy(&StorageTmp->ieee8021SpanningTreePortExtensionTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_ieee8021SpanningTreeNewRoot_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "send_ieee8021SpanningTreeNewRoot_v2trap: sending trap...  "));
	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = ieee8021SpanningTreeNewRoot_oid;
	trap.val_len = sizeof(ieee8021SpanningTreeNewRoot_oid);
	trap.index = 0;
	send_v2trap(&trap);
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

void
send_ieee8021SpanningTreeTopologyChange_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	DEBUGMSGTL(("ieee8021SpanningTreeMib", "send_ieee8021SpanningTreeTopologyChange_v2trap: sending trap...  "));
	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = ieee8021SpanningTreeTopologyChange_oid;
	trap.val_len = sizeof(ieee8021SpanningTreeTopologyChange_oid);
	trap.index = 0;
	send_v2trap(&trap);
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/**
 * @fn void ieee8021SpanningTreeMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021SpanningTreeMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021SpanningTreeMibold_signal_handler != NULL)
		(*ieee8021SpanningTreeMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
}

/**
 * @fn void ieee8021SpanningTreeMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021SpanningTreeMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "ieee8021SpanningTreeMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021SpanningTreeMib", "done.\n"));
	return;
}
