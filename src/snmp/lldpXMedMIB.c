/*****************************************************************************

 @(#) src/snmp/lldpxmedmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/lldpxmedmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "lldpXMedMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "lldpXMedMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int lldpXMedMIB_refresh = 1;
volatile int lldpXMedPortConfigTable_refresh = 1;
volatile int lldpXMedLocMediaPolicyTable_refresh = 1;
volatile int lldpXMedLocLocationTable_refresh = 1;
volatile int lldpXMedLocXPoEPSEPortTable_refresh = 1;
volatile int lldpXMedRemCapabilitiesTable_refresh = 1;
volatile int lldpXMedRemMediaPolicyTable_refresh = 1;
volatile int lldpXMedRemInventoryTable_refresh = 1;
volatile int lldpXMedRemLocationTable_refresh = 1;
volatile int lldpXMedRemXPoETable_refresh = 1;
volatile int lldpXMedRemXPoEPSETable_refresh = 1;
volatile int lldpXMedRemXPoEPDTable_refresh = 1;

/*
 * lldpXMedMIB_variables_oid: object identifier for lldpXMedMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid lldpXMedMIB_variables_oid[9] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid lldpXMedTopologyChangeDetected_oid[11] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 0, 1 };

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid lldpXMedCompliance_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 1, 1 };
oid lldpXMedConfigGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 1 };
oid lldpXMedOptMediaPolicyGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 2 };
oid lldpXMedOptInventoryGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 3 };
oid lldpXMedOptLocationGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 4 };
oid lldpXMedOptPoEPSEGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 5 };
oid lldpXMedOptPoEPDGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 6 };
oid lldpXMedRemSysGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 7 };
oid lldpXMedNotificationsGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 4795, 2, 2, 8 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 lldpXMedMIB_variables: tree for lldpXMedMIB
 * This variable defines function callbacks and type return information for the lldpXMedMIB mib section
 */
struct variable7 lldpXMedMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   LLDPXMEDLOCDEVICECLASS  1
	{(u_char) LLDPXMEDLOCDEVICECLASS, ASN_INTEGER, RONLY, var_lldpXMedMIB, 3, {1, 1, 1}},
#define   LLDPXMEDPORTCAPSUPPORTED  2
	{(u_char) LLDPXMEDPORTCAPSUPPORTED, ASN_OCTET_STR, RONLY, var_lldpXMedPortConfigTable, 5, {1, 1, 2, 1, 1}},
#define   LLDPXMEDPORTCONFIGTLVSTXENABLE  3
	{(u_char) LLDPXMEDPORTCONFIGTLVSTXENABLE, ASN_OCTET_STR, RWRITE, var_lldpXMedPortConfigTable, 5, {1, 1, 2, 1, 2}},
#define   LLDPXMEDPORTCONFIGNOTIFENABLE  4
	{(u_char) LLDPXMEDPORTCONFIGNOTIFENABLE, ASN_INTEGER, RWRITE, var_lldpXMedPortConfigTable, 5, {1, 1, 2, 1, 3}},
#define   LLDPXMEDFASTSTARTREPEATCOUNT  5
	{(u_char) LLDPXMEDFASTSTARTREPEATCOUNT, ASN_UNSIGNED, RWRITE, var_lldpXMedMIB, 3, {1, 1, 3}},
#define   LLDPXMEDLOCMEDIAPOLICYVLANID  6
	{(u_char) LLDPXMEDLOCMEDIAPOLICYVLANID, ASN_INTEGER, RONLY, var_lldpXMedLocMediaPolicyTable, 5, {1, 2, 1, 1, 2}},
#define   LLDPXMEDLOCMEDIAPOLICYPRIORITY  7
	{(u_char) LLDPXMEDLOCMEDIAPOLICYPRIORITY, ASN_INTEGER, RONLY, var_lldpXMedLocMediaPolicyTable, 5, {1, 2, 1, 1, 3}},
#define   LLDPXMEDLOCMEDIAPOLICYDSCP  8
	{(u_char) LLDPXMEDLOCMEDIAPOLICYDSCP, ASN_INTEGER, RONLY, var_lldpXMedLocMediaPolicyTable, 5, {1, 2, 1, 1, 4}},
#define   LLDPXMEDLOCMEDIAPOLICYUNKNOWN  9
	{(u_char) LLDPXMEDLOCMEDIAPOLICYUNKNOWN, ASN_INTEGER, RONLY, var_lldpXMedLocMediaPolicyTable, 5, {1, 2, 1, 1, 5}},
#define   LLDPXMEDLOCMEDIAPOLICYTAGGED  10
	{(u_char) LLDPXMEDLOCMEDIAPOLICYTAGGED, ASN_INTEGER, RONLY, var_lldpXMedLocMediaPolicyTable, 5, {1, 2, 1, 1, 6}},
#define   LLDPXMEDLOCHARDWAREREV  11
	{(u_char) LLDPXMEDLOCHARDWAREREV, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 2}},
#define   LLDPXMEDLOCFIRMWAREREV  12
	{(u_char) LLDPXMEDLOCFIRMWAREREV, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 3}},
#define   LLDPXMEDLOCSOFTWAREREV  13
	{(u_char) LLDPXMEDLOCSOFTWAREREV, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 4}},
#define   LLDPXMEDLOCSERIALNUM  14
	{(u_char) LLDPXMEDLOCSERIALNUM, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 5}},
#define   LLDPXMEDLOCMFGNAME    15
	{(u_char) LLDPXMEDLOCMFGNAME, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 6}},
#define   LLDPXMEDLOCMODELNAME  16
	{(u_char) LLDPXMEDLOCMODELNAME, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 7}},
#define   LLDPXMEDLOCASSETID    17
	{(u_char) LLDPXMEDLOCASSETID, ASN_OCTET_STR, RONLY, var_lldpXMedMIB, 3, {1, 2, 8}},
#define   LLDPXMEDLOCLOCATIONINFO  18
	{(u_char) LLDPXMEDLOCLOCATIONINFO, ASN_OCTET_STR, RWRITE, var_lldpXMedLocLocationTable, 5, {1, 2, 9, 1, 2}},
#define   LLDPXMEDLOCXPOEDEVICETYPE  19
	{(u_char) LLDPXMEDLOCXPOEDEVICETYPE, ASN_INTEGER, RONLY, var_lldpXMedMIB, 3, {1, 2, 10}},
#define   LLDPXMEDLOCXPOEPSEPORTPOWERAV  20
	{(u_char) LLDPXMEDLOCXPOEPSEPORTPOWERAV, ASN_GAUGE, RONLY, var_lldpXMedLocXPoEPSEPortTable, 5, {1, 2, 11, 1, 1}},
#define   LLDPXMEDLOCXPOEPSEPORTPDPRIORITY  21
	{(u_char) LLDPXMEDLOCXPOEPSEPORTPDPRIORITY, ASN_INTEGER, RONLY, var_lldpXMedLocXPoEPSEPortTable, 5, {1, 2, 11, 1, 2}},
#define   LLDPXMEDLOCXPOEPSEPOWERSOURCE  22
	{(u_char) LLDPXMEDLOCXPOEPSEPOWERSOURCE, ASN_INTEGER, RONLY, var_lldpXMedMIB, 3, {1, 2, 12}},
#define   LLDPXMEDLOCXPOEPDPOWERREQ  23
	{(u_char) LLDPXMEDLOCXPOEPDPOWERREQ, ASN_GAUGE, RONLY, var_lldpXMedMIB, 3, {1, 2, 13}},
#define   LLDPXMEDLOCXPOEPDPOWERSOURCE  24
	{(u_char) LLDPXMEDLOCXPOEPDPOWERSOURCE, ASN_INTEGER, RONLY, var_lldpXMedMIB, 3, {1, 2, 14}},
#define   LLDPXMEDLOCXPOEPDPOWERPRIORITY  25
	{(u_char) LLDPXMEDLOCXPOEPDPOWERPRIORITY, ASN_INTEGER, RONLY, var_lldpXMedMIB, 3, {1, 2, 15}},
#define   LLDPXMEDREMCAPSUPPORTED  26
	{(u_char) LLDPXMEDREMCAPSUPPORTED, ASN_OCTET_STR, RONLY, var_lldpXMedRemCapabilitiesTable, 5, {1, 3, 1, 1, 1}},
#define   LLDPXMEDREMCAPCURRENT  27
	{(u_char) LLDPXMEDREMCAPCURRENT, ASN_OCTET_STR, RONLY, var_lldpXMedRemCapabilitiesTable, 5, {1, 3, 1, 1, 2}},
#define   LLDPXMEDREMDEVICECLASS  28
	{(u_char) LLDPXMEDREMDEVICECLASS, ASN_INTEGER, RONLY, var_lldpXMedRemCapabilitiesTable, 5, {1, 3, 1, 1, 3}},
#define   LLDPXMEDREMMEDIAPOLICYVLANID  29
	{(u_char) LLDPXMEDREMMEDIAPOLICYVLANID, ASN_INTEGER, RONLY, var_lldpXMedRemMediaPolicyTable, 5, {1, 3, 2, 1, 2}},
#define   LLDPXMEDREMMEDIAPOLICYPRIORITY  30
	{(u_char) LLDPXMEDREMMEDIAPOLICYPRIORITY, ASN_INTEGER, RONLY, var_lldpXMedRemMediaPolicyTable, 5, {1, 3, 2, 1, 3}},
#define   LLDPXMEDREMMEDIAPOLICYDSCP  31
	{(u_char) LLDPXMEDREMMEDIAPOLICYDSCP, ASN_INTEGER, RONLY, var_lldpXMedRemMediaPolicyTable, 5, {1, 3, 2, 1, 4}},
#define   LLDPXMEDREMMEDIAPOLICYUNKNOWN  32
	{(u_char) LLDPXMEDREMMEDIAPOLICYUNKNOWN, ASN_INTEGER, RONLY, var_lldpXMedRemMediaPolicyTable, 5, {1, 3, 2, 1, 5}},
#define   LLDPXMEDREMMEDIAPOLICYTAGGED  33
	{(u_char) LLDPXMEDREMMEDIAPOLICYTAGGED, ASN_INTEGER, RONLY, var_lldpXMedRemMediaPolicyTable, 5, {1, 3, 2, 1, 6}},
#define   LLDPXMEDREMHARDWAREREV  34
	{(u_char) LLDPXMEDREMHARDWAREREV, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 1}},
#define   LLDPXMEDREMFIRMWAREREV  35
	{(u_char) LLDPXMEDREMFIRMWAREREV, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 2}},
#define   LLDPXMEDREMSOFTWAREREV  36
	{(u_char) LLDPXMEDREMSOFTWAREREV, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 3}},
#define   LLDPXMEDREMSERIALNUM  37
	{(u_char) LLDPXMEDREMSERIALNUM, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 4}},
#define   LLDPXMEDREMMFGNAME    38
	{(u_char) LLDPXMEDREMMFGNAME, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 5}},
#define   LLDPXMEDREMMODELNAME  39
	{(u_char) LLDPXMEDREMMODELNAME, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 6}},
#define   LLDPXMEDREMASSETID    40
	{(u_char) LLDPXMEDREMASSETID, ASN_OCTET_STR, RONLY, var_lldpXMedRemInventoryTable, 5, {1, 3, 3, 1, 7}},
#define   LLDPXMEDREMLOCATIONINFO  41
	{(u_char) LLDPXMEDREMLOCATIONINFO, ASN_OCTET_STR, RONLY, var_lldpXMedRemLocationTable, 5, {1, 3, 4, 1, 2}},
#define   LLDPXMEDREMXPOEDEVICETYPE  42
	{(u_char) LLDPXMEDREMXPOEDEVICETYPE, ASN_INTEGER, RONLY, var_lldpXMedRemXPoETable, 5, {1, 3, 5, 1, 1}},
#define   LLDPXMEDREMXPOEPSEPOWERAV  43
	{(u_char) LLDPXMEDREMXPOEPSEPOWERAV, ASN_GAUGE, RONLY, var_lldpXMedRemXPoEPSETable, 5, {1, 3, 6, 1, 1}},
#define   LLDPXMEDREMXPOEPSEPOWERSOURCE  44
	{(u_char) LLDPXMEDREMXPOEPSEPOWERSOURCE, ASN_INTEGER, RONLY, var_lldpXMedRemXPoEPSETable, 5, {1, 3, 6, 1, 2}},
#define   LLDPXMEDREMXPOEPSEPOWERPRIORITY  45
	{(u_char) LLDPXMEDREMXPOEPSEPOWERPRIORITY, ASN_INTEGER, RONLY, var_lldpXMedRemXPoEPSETable, 5, {1, 3, 6, 1, 3}},
#define   LLDPXMEDREMXPOEPDPOWERREQ  46
	{(u_char) LLDPXMEDREMXPOEPDPOWERREQ, ASN_GAUGE, RONLY, var_lldpXMedRemXPoEPDTable, 5, {1, 3, 7, 1, 1}},
#define   LLDPXMEDREMXPOEPDPOWERSOURCE  47
	{(u_char) LLDPXMEDREMXPOEPDPOWERSOURCE, ASN_INTEGER, RONLY, var_lldpXMedRemXPoEPDTable, 5, {1, 3, 7, 1, 2}},
#define   LLDPXMEDREMXPOEPDPOWERPRIORITY  48
	{(u_char) LLDPXMEDREMXPOEPDPOWERPRIORITY, ASN_INTEGER, RONLY, var_lldpXMedRemXPoEPDTable, 5, {1, 3, 7, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct lldpXMedMIB_data *lldpXMedMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *lldpXMedPortConfigTableStorage = NULL;
struct header_complex_index *lldpXMedLocMediaPolicyTableStorage = NULL;
struct header_complex_index *lldpXMedLocLocationTableStorage = NULL;
struct header_complex_index *lldpXMedLocXPoEPSEPortTableStorage = NULL;
struct header_complex_index *lldpXMedRemCapabilitiesTableStorage = NULL;
struct header_complex_index *lldpXMedRemMediaPolicyTableStorage = NULL;
struct header_complex_index *lldpXMedRemInventoryTableStorage = NULL;
struct header_complex_index *lldpXMedRemLocationTableStorage = NULL;
struct header_complex_index *lldpXMedRemXPoETableStorage = NULL;
struct header_complex_index *lldpXMedRemXPoEPSETableStorage = NULL;
struct header_complex_index *lldpXMedRemXPoEPDTableStorage = NULL;

void (*lldpXMedMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void lldpXMedMIB_loop_handler(int);
void lldpXMedMIB_fd_handler(int, void *);

/**
 * @fn void init_lldpXMedMIB(void)
 * @brief lldpXMedMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (lldpXMedMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_lldpXMedMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("lldpXMedMIB", "init_lldpXMedMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("lldpXMedMIB", lldpXMedMIB_variables, variable7, lldpXMedMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_lldpXMedMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("lldpXMedMIB", parse_lldpXMedMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedPortConfigTable", parse_lldpXMedPortConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedLocMediaPolicyTable", parse_lldpXMedLocMediaPolicyTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedLocLocationTable", parse_lldpXMedLocLocationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedLocXPoEPSEPortTable", parse_lldpXMedLocXPoEPSEPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemCapabilitiesTable", parse_lldpXMedRemCapabilitiesTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemMediaPolicyTable", parse_lldpXMedRemMediaPolicyTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemInventoryTable", parse_lldpXMedRemInventoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemLocationTable", parse_lldpXMedRemLocationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemXPoETable", parse_lldpXMedRemXPoETable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemXPoEPSETable", parse_lldpXMedRemXPoEPSETable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXMedRemXPoEPDTable", parse_lldpXMedRemXPoEPDTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedPortConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedLocMediaPolicyTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedLocLocationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedLocXPoEPSEPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemCapabilitiesTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemMediaPolicyTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemInventoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemLocationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemXPoETable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemXPoEPSETable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXMedRemXPoEPDTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, lldpXMedMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, lldpXMedMIB_fd_handler, (void *) 1);
	}
	lldpXMedMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &lldpXMedMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/**
 * @fn void deinit_lldpXMedMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (lldpXMedMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_lldpXMedMIB(void)
{
	DEBUGMSGTL(("lldpXMedMIB", "deinit_lldpXMedMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = lldpXMedMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(lldpXMedMIB_variables_oid, sizeof(lldpXMedMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("lldpXMedMIB");
	snmpd_unregister_config_handler("lldpXMedPortConfigTable");
	snmpd_unregister_config_handler("lldpXMedLocMediaPolicyTable");
	snmpd_unregister_config_handler("lldpXMedLocLocationTable");
	snmpd_unregister_config_handler("lldpXMedLocXPoEPSEPortTable");
	snmpd_unregister_config_handler("lldpXMedRemCapabilitiesTable");
	snmpd_unregister_config_handler("lldpXMedRemMediaPolicyTable");
	snmpd_unregister_config_handler("lldpXMedRemInventoryTable");
	snmpd_unregister_config_handler("lldpXMedRemLocationTable");
	snmpd_unregister_config_handler("lldpXMedRemXPoETable");
	snmpd_unregister_config_handler("lldpXMedRemXPoEPSETable");
	snmpd_unregister_config_handler("lldpXMedRemXPoEPDTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

int
term_lldpXMedMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("lldpXMedMIB", "term_lldpXMedMIB: terminating...  "));
	deinit_lldpXMedMIB();
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct lldpXMedMIB_data *lldpXMedMIB_create(void)
 * @brief create a fresh data structure representing scalars in lldpXMedMIB.
 *
 * Creates a new lldpXMedMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in lldpXMedMIB.
 */
struct lldpXMedMIB_data *
lldpXMedMIB_create(void)
{
	struct lldpXMedMIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedMIB_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->lldpXMedLocDeviceClass = 0;
		StorageNew->lldpXMedFastStartRepeatCount = 3;
		if ((StorageNew->lldpXMedLocHardwareRev = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocHardwareRevLen = 0;
		StorageNew->lldpXMedLocHardwareRev[StorageNew->lldpXMedLocHardwareRevLen] = 0;
		if ((StorageNew->lldpXMedLocFirmwareRev = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocFirmwareRevLen = 0;
		StorageNew->lldpXMedLocFirmwareRev[StorageNew->lldpXMedLocFirmwareRevLen] = 0;
		if ((StorageNew->lldpXMedLocSoftwareRev = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocSoftwareRevLen = 0;
		StorageNew->lldpXMedLocSoftwareRev[StorageNew->lldpXMedLocSoftwareRevLen] = 0;
		if ((StorageNew->lldpXMedLocSerialNum = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocSerialNumLen = 0;
		StorageNew->lldpXMedLocSerialNum[StorageNew->lldpXMedLocSerialNumLen] = 0;
		if ((StorageNew->lldpXMedLocMfgName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocMfgNameLen = 0;
		StorageNew->lldpXMedLocMfgName[StorageNew->lldpXMedLocMfgNameLen] = 0;
		if ((StorageNew->lldpXMedLocModelName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocModelNameLen = 0;
		StorageNew->lldpXMedLocModelName[StorageNew->lldpXMedLocModelNameLen] = 0;
		if ((StorageNew->lldpXMedLocAssetID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocAssetIDLen = 0;
		StorageNew->lldpXMedLocAssetID[StorageNew->lldpXMedLocAssetIDLen] = 0;
		StorageNew->lldpXMedLocXPoEDeviceType = 0;
		StorageNew->lldpXMedLocXPoEPSEPowerSource = 0;
		StorageNew->lldpXMedLocXPoEPDPowerReq = 0;
		StorageNew->lldpXMedLocXPoEPDPowerSource = 0;
		StorageNew->lldpXMedLocXPoEPDPowerPriority = 0;

	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedMIB_data *lldpXMedMIB_duplicate(struct lldpXMedMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct lldpXMedMIB_data *
lldpXMedMIB_duplicate(struct lldpXMedMIB_data *thedata)
{
	struct lldpXMedMIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedMIB_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedLocDeviceClass = thedata->lldpXMedLocDeviceClass;
		StorageNew->lldpXMedFastStartRepeatCount = thedata->lldpXMedFastStartRepeatCount;
		if (!(StorageNew->lldpXMedLocHardwareRev = malloc(thedata->lldpXMedLocHardwareRevLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocHardwareRev, thedata->lldpXMedLocHardwareRev, thedata->lldpXMedLocHardwareRevLen);
		StorageNew->lldpXMedLocHardwareRevLen = thedata->lldpXMedLocHardwareRevLen;
		StorageNew->lldpXMedLocHardwareRev[StorageNew->lldpXMedLocHardwareRevLen] = 0;
		if (!(StorageNew->lldpXMedLocFirmwareRev = malloc(thedata->lldpXMedLocFirmwareRevLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocFirmwareRev, thedata->lldpXMedLocFirmwareRev, thedata->lldpXMedLocFirmwareRevLen);
		StorageNew->lldpXMedLocFirmwareRevLen = thedata->lldpXMedLocFirmwareRevLen;
		StorageNew->lldpXMedLocFirmwareRev[StorageNew->lldpXMedLocFirmwareRevLen] = 0;
		if (!(StorageNew->lldpXMedLocSoftwareRev = malloc(thedata->lldpXMedLocSoftwareRevLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocSoftwareRev, thedata->lldpXMedLocSoftwareRev, thedata->lldpXMedLocSoftwareRevLen);
		StorageNew->lldpXMedLocSoftwareRevLen = thedata->lldpXMedLocSoftwareRevLen;
		StorageNew->lldpXMedLocSoftwareRev[StorageNew->lldpXMedLocSoftwareRevLen] = 0;
		if (!(StorageNew->lldpXMedLocSerialNum = malloc(thedata->lldpXMedLocSerialNumLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocSerialNum, thedata->lldpXMedLocSerialNum, thedata->lldpXMedLocSerialNumLen);
		StorageNew->lldpXMedLocSerialNumLen = thedata->lldpXMedLocSerialNumLen;
		StorageNew->lldpXMedLocSerialNum[StorageNew->lldpXMedLocSerialNumLen] = 0;
		if (!(StorageNew->lldpXMedLocMfgName = malloc(thedata->lldpXMedLocMfgNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocMfgName, thedata->lldpXMedLocMfgName, thedata->lldpXMedLocMfgNameLen);
		StorageNew->lldpXMedLocMfgNameLen = thedata->lldpXMedLocMfgNameLen;
		StorageNew->lldpXMedLocMfgName[StorageNew->lldpXMedLocMfgNameLen] = 0;
		if (!(StorageNew->lldpXMedLocModelName = malloc(thedata->lldpXMedLocModelNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocModelName, thedata->lldpXMedLocModelName, thedata->lldpXMedLocModelNameLen);
		StorageNew->lldpXMedLocModelNameLen = thedata->lldpXMedLocModelNameLen;
		StorageNew->lldpXMedLocModelName[StorageNew->lldpXMedLocModelNameLen] = 0;
		if (!(StorageNew->lldpXMedLocAssetID = malloc(thedata->lldpXMedLocAssetIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocAssetID, thedata->lldpXMedLocAssetID, thedata->lldpXMedLocAssetIDLen);
		StorageNew->lldpXMedLocAssetIDLen = thedata->lldpXMedLocAssetIDLen;
		StorageNew->lldpXMedLocAssetID[StorageNew->lldpXMedLocAssetIDLen] = 0;
		StorageNew->lldpXMedLocXPoEDeviceType = thedata->lldpXMedLocXPoEDeviceType;
		StorageNew->lldpXMedLocXPoEPSEPowerSource = thedata->lldpXMedLocXPoEPSEPowerSource;
		StorageNew->lldpXMedLocXPoEPDPowerReq = thedata->lldpXMedLocXPoEPDPowerReq;
		StorageNew->lldpXMedLocXPoEPDPowerSource = thedata->lldpXMedLocXPoEPDPowerSource;
		StorageNew->lldpXMedLocXPoEPDPowerPriority = thedata->lldpXMedLocXPoEPDPowerPriority;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedMIB_destroy(struct lldpXMedMIB_data **thedata)
 * @param thedata pointer to the data structure in lldpXMedMIB.
 * @brief delete a scalars structure from lldpXMedMIB.
 *
 * Frees scalars that were previously removed from lldpXMedMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedMIB_destroy(struct lldpXMedMIB_data **thedata)
{
	struct lldpXMedMIB_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedLocHardwareRev);
		StorageDel->lldpXMedLocHardwareRevLen = 0;
		SNMP_FREE(StorageDel->lldpXMedLocFirmwareRev);
		StorageDel->lldpXMedLocFirmwareRevLen = 0;
		SNMP_FREE(StorageDel->lldpXMedLocSoftwareRev);
		StorageDel->lldpXMedLocSoftwareRevLen = 0;
		SNMP_FREE(StorageDel->lldpXMedLocSerialNum);
		StorageDel->lldpXMedLocSerialNumLen = 0;
		SNMP_FREE(StorageDel->lldpXMedLocMfgName);
		StorageDel->lldpXMedLocMfgNameLen = 0;
		SNMP_FREE(StorageDel->lldpXMedLocModelName);
		StorageDel->lldpXMedLocModelNameLen = 0;
		SNMP_FREE(StorageDel->lldpXMedLocAssetID);
		StorageDel->lldpXMedLocAssetIDLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedMIB_add(struct lldpXMedMIB_data *thedata)
 * @param thedata the structure representing lldpXMedMIB scalars.
 * @brief adds node to the lldpXMedMIB scalar data set.
 *
 * Adds a scalar structure to the lldpXMedMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
lldpXMedMIB_add(struct lldpXMedMIB_data *thedata)
{
	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedMIB_add: adding data...  "));
	if (thedata)
		lldpXMedMIBStorage = thedata;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_lldpXMedMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedMIB_data *StorageTmp = lldpXMedMIB_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocDeviceClass, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpXMedFastStartRepeatCount, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedLocHardwareRev);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocHardwareRev, &StorageTmp->lldpXMedLocHardwareRevLen);
	if (StorageTmp->lldpXMedLocHardwareRev == NULL) {
		config_perror("invalid specification for lldpXMedLocHardwareRev");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedLocFirmwareRev);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocFirmwareRev, &StorageTmp->lldpXMedLocFirmwareRevLen);
	if (StorageTmp->lldpXMedLocFirmwareRev == NULL) {
		config_perror("invalid specification for lldpXMedLocFirmwareRev");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedLocSoftwareRev);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocSoftwareRev, &StorageTmp->lldpXMedLocSoftwareRevLen);
	if (StorageTmp->lldpXMedLocSoftwareRev == NULL) {
		config_perror("invalid specification for lldpXMedLocSoftwareRev");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedLocSerialNum);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocSerialNum, &StorageTmp->lldpXMedLocSerialNumLen);
	if (StorageTmp->lldpXMedLocSerialNum == NULL) {
		config_perror("invalid specification for lldpXMedLocSerialNum");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedLocMfgName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocMfgName, &StorageTmp->lldpXMedLocMfgNameLen);
	if (StorageTmp->lldpXMedLocMfgName == NULL) {
		config_perror("invalid specification for lldpXMedLocMfgName");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedLocModelName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocModelName, &StorageTmp->lldpXMedLocModelNameLen);
	if (StorageTmp->lldpXMedLocModelName == NULL) {
		config_perror("invalid specification for lldpXMedLocModelName");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedLocAssetID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocAssetID, &StorageTmp->lldpXMedLocAssetIDLen);
	if (StorageTmp->lldpXMedLocAssetID == NULL) {
		config_perror("invalid specification for lldpXMedLocAssetID");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocXPoEDeviceType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocXPoEPSEPowerSource, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpXMedLocXPoEPDPowerReq, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocXPoEPDPowerSource, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocXPoEPDPowerPriority, &tmpsize);
	lldpXMedMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedMIB_data *StorageTmp;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedMIB: storing data...  "));
	refresh_lldpXMedMIB(1);
	if ((StorageTmp = lldpXMedMIBStorage) == NULL) {
		DEBUGMSGTL(("lldpXMedMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "lldpXMedMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocDeviceClass, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpXMedFastStartRepeatCount, &tmpsize);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocHardwareRev, &StorageTmp->lldpXMedLocHardwareRevLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocFirmwareRev, &StorageTmp->lldpXMedLocFirmwareRevLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocSoftwareRev, &StorageTmp->lldpXMedLocSoftwareRevLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocSerialNum, &StorageTmp->lldpXMedLocSerialNumLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocMfgName, &StorageTmp->lldpXMedLocMfgNameLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocModelName, &StorageTmp->lldpXMedLocModelNameLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocAssetID, &StorageTmp->lldpXMedLocAssetIDLen);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocXPoEDeviceType, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocXPoEPSEPowerSource, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpXMedLocXPoEPDPowerReq, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocXPoEPDPowerSource, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocXPoEPDPowerPriority, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpXMedMIB(struct lldpXMedMIB_data *StorageTmp, struct lldpXMedMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_lldpXMedMIB(struct lldpXMedMIB_data *StorageTmp, struct lldpXMedMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedMIB(struct lldpXMedMIB_data *StorageTmp, struct lldpXMedMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_lldpXMedMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedMIB(struct lldpXMedMIB_data *StorageTmp, struct lldpXMedMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_lldpXMedMIB(struct 
 * @fn void revert_lldpXMedMIB(struct lldpXMedMIB_data *StorageTmp, struct lldpXMedMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_lldpXMedMIB(struct lldpXMedMIB_data *StorageTmp, struct lldpXMedMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of lldpXMedMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_lldpXMedMIB(int force)
{
	if (lldpXMedMIBStorage == NULL) {
		struct lldpXMedMIB_data *StorageNew;

		if ((StorageNew = lldpXMedMIB_create()) == NULL)
			return;
		lldpXMedMIBStorage = StorageNew;
		lldpXMedMIB_refresh = 1;
	}
	if (!force && lldpXMedMIB_refresh == 0)
		return;
	DEBUGMSGTL(("lldpXMedMIB", "refresh_lldpXMedMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	lldpXMedMIB_refresh = 0;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/**
 * @fn u_char * var_lldpXMedMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in lldpXMedMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_lldpXMedMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_lldpXMedMIB(0);
	if ((StorageTmp = lldpXMedMIBStorage) == NULL) {
		DEBUGMSGTL(("lldpXMedMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDLOCDEVICECLASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocDeviceClass);
		rval = (u_char *) &StorageTmp->lldpXMedLocDeviceClass;
		break;
	case (u_char) LLDPXMEDFASTSTARTREPEATCOUNT:	/* ReadWrite */
		*write_method = write_lldpXMedFastStartRepeatCount;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedFastStartRepeatCount);
		rval = (u_char *) &StorageTmp->lldpXMedFastStartRepeatCount;
		break;
	case (u_char) LLDPXMEDLOCHARDWAREREV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocHardwareRevLen;
		rval = (u_char *) StorageTmp->lldpXMedLocHardwareRev;
		break;
	case (u_char) LLDPXMEDLOCFIRMWAREREV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocFirmwareRevLen;
		rval = (u_char *) StorageTmp->lldpXMedLocFirmwareRev;
		break;
	case (u_char) LLDPXMEDLOCSOFTWAREREV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocSoftwareRevLen;
		rval = (u_char *) StorageTmp->lldpXMedLocSoftwareRev;
		break;
	case (u_char) LLDPXMEDLOCSERIALNUM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocSerialNumLen;
		rval = (u_char *) StorageTmp->lldpXMedLocSerialNum;
		break;
	case (u_char) LLDPXMEDLOCMFGNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocMfgNameLen;
		rval = (u_char *) StorageTmp->lldpXMedLocMfgName;
		break;
	case (u_char) LLDPXMEDLOCMODELNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocModelNameLen;
		rval = (u_char *) StorageTmp->lldpXMedLocModelName;
		break;
	case (u_char) LLDPXMEDLOCASSETID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocAssetIDLen;
		rval = (u_char *) StorageTmp->lldpXMedLocAssetID;
		break;
	case (u_char) LLDPXMEDLOCXPOEDEVICETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEDeviceType);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEDeviceType;
		break;
	case (u_char) LLDPXMEDLOCXPOEPSEPOWERSOURCE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEPSEPowerSource);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEPSEPowerSource;
		break;
	case (u_char) LLDPXMEDLOCXPOEPDPOWERREQ:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEPDPowerReq);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEPDPowerReq;
		break;
	case (u_char) LLDPXMEDLOCXPOEPDPOWERSOURCE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEPDPowerSource);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEPDPowerSource;
		break;
	case (u_char) LLDPXMEDLOCXPOEPDPOWERPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEPDPowerPriority);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEPDPowerPriority;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("lldpXMedMIB", "found.\n"));
	else
		DEBUGMSGTL(("lldpXMedMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct lldpXMedPortConfigTable_data *lldpXMedPortConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedPortConfigTable table.
 *
 * Creates a new lldpXMedPortConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedPortConfigTable_data *
lldpXMedPortConfigTable_create(void)
{
	struct lldpXMedPortConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedPortConfigTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedPortConfigTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpPortConfigPortNum = 0;
		if (memdup((u_char **) &StorageNew->lldpXMedPortCapSupported, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpXMedPortCapSupportedLen = 1;
		if (memdup((u_char **) &StorageNew->lldpXMedPortConfigTLVsTxEnable, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpXMedPortConfigTLVsTxEnableLen = 1;
		StorageNew->lldpXMedPortConfigNotifEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedPortConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedPortConfigTable_data *lldpXMedPortConfigTable_duplicate(struct lldpXMedPortConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedPortConfigTable_data *
lldpXMedPortConfigTable_duplicate(struct lldpXMedPortConfigTable_data *thedata)
{
	struct lldpXMedPortConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedPortConfigTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedPortConfigTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedPortConfigTable_id = thedata->lldpXMedPortConfigTable_id;
		StorageNew->lldpPortConfigPortNum = thedata->lldpPortConfigPortNum;
		if (!(StorageNew->lldpXMedPortCapSupported = malloc(thedata->lldpXMedPortCapSupportedLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedPortCapSupported, thedata->lldpXMedPortCapSupported, thedata->lldpXMedPortCapSupportedLen);
		StorageNew->lldpXMedPortCapSupportedLen = thedata->lldpXMedPortCapSupportedLen;
		StorageNew->lldpXMedPortCapSupported[StorageNew->lldpXMedPortCapSupportedLen] = 0;
		if (!(StorageNew->lldpXMedPortConfigTLVsTxEnable = malloc(thedata->lldpXMedPortConfigTLVsTxEnableLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedPortConfigTLVsTxEnable, thedata->lldpXMedPortConfigTLVsTxEnable, thedata->lldpXMedPortConfigTLVsTxEnableLen);
		StorageNew->lldpXMedPortConfigTLVsTxEnableLen = thedata->lldpXMedPortConfigTLVsTxEnableLen;
		StorageNew->lldpXMedPortConfigTLVsTxEnable[StorageNew->lldpXMedPortConfigTLVsTxEnableLen] = 0;
		StorageNew->lldpXMedPortConfigNotifEnable = thedata->lldpXMedPortConfigNotifEnable;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedPortConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedPortConfigTable_destroy(struct lldpXMedPortConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedPortConfigTable_destroy(struct lldpXMedPortConfigTable_data **thedata)
{
	struct lldpXMedPortConfigTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedPortConfigTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedPortCapSupported);
		StorageDel->lldpXMedPortCapSupportedLen = 0;
		SNMP_FREE(StorageDel->lldpXMedPortConfigTLVsTxEnable);
		StorageDel->lldpXMedPortConfigTLVsTxEnableLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedPortConfigTable_add(struct lldpXMedPortConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedPortConfigTable table data set.
 *
 * Adds a table row structure to the lldpXMedPortConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedPortConfigTable_add(struct lldpXMedPortConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedPortConfigTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpPortConfigPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpPortConfigPortNum, sizeof(thedata->lldpPortConfigPortNum));
		header_complex_add_data(&lldpXMedPortConfigTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedPortConfigTable_del(struct lldpXMedPortConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedPortConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedPortConfigTable_del(struct lldpXMedPortConfigTable_data *thedata)
{
	struct lldpXMedPortConfigTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedPortConfigTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedPortConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedPortConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedPortConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedPortConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedPortConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedPortConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedPortConfigTable_data *StorageTmp = lldpXMedPortConfigTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedPortConfigTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpPortConfigPortNum, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedPortCapSupported);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedPortCapSupported, &StorageTmp->lldpXMedPortCapSupportedLen);
	if (StorageTmp->lldpXMedPortCapSupported == NULL) {
		config_perror("invalid specification for lldpXMedPortCapSupported");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedPortConfigTLVsTxEnable);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedPortConfigTLVsTxEnable, &StorageTmp->lldpXMedPortConfigTLVsTxEnableLen);
	if (StorageTmp->lldpXMedPortConfigTLVsTxEnable == NULL) {
		config_perror("invalid specification for lldpXMedPortConfigTLVsTxEnable");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedPortConfigNotifEnable, &tmpsize);
	lldpXMedPortConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedPortConfigTable(): store configuraiton file for lldpXMedPortConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedPortConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedPortConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedPortConfigTable: storing data...  "));
	refresh_lldpXMedPortConfigTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedPortConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedPortConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedPortConfigTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpPortConfigPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedPortCapSupported, &StorageTmp->lldpXMedPortCapSupportedLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedPortConfigTLVsTxEnable, &StorageTmp->lldpXMedPortConfigTLVsTxEnableLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedPortConfigNotifEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedLocMediaPolicyTable_data *lldpXMedLocMediaPolicyTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedLocMediaPolicyTable table.
 *
 * Creates a new lldpXMedLocMediaPolicyTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedLocMediaPolicyTable_data *
lldpXMedLocMediaPolicyTable_create(void)
{
	struct lldpXMedLocMediaPolicyTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedLocMediaPolicyTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocMediaPolicyTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXMedLocMediaPolicyVlanID = 0;
		StorageNew->lldpXMedLocMediaPolicyPriority = 0;
		StorageNew->lldpXMedLocMediaPolicyDscp = 0;
		StorageNew->lldpXMedLocMediaPolicyUnknown = 0;
		StorageNew->lldpXMedLocMediaPolicyTagged = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedLocMediaPolicyTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedLocMediaPolicyTable_data *lldpXMedLocMediaPolicyTable_duplicate(struct lldpXMedLocMediaPolicyTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedLocMediaPolicyTable_data *
lldpXMedLocMediaPolicyTable_duplicate(struct lldpXMedLocMediaPolicyTable_data *thedata)
{
	struct lldpXMedLocMediaPolicyTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedLocMediaPolicyTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocMediaPolicyTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedLocMediaPolicyTable_id = thedata->lldpXMedLocMediaPolicyTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		if (!(StorageNew->lldpXMedLocMediaPolicyAppType = malloc(thedata->lldpXMedLocMediaPolicyAppTypeLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocMediaPolicyAppType, thedata->lldpXMedLocMediaPolicyAppType, thedata->lldpXMedLocMediaPolicyAppTypeLen);
		StorageNew->lldpXMedLocMediaPolicyAppTypeLen = thedata->lldpXMedLocMediaPolicyAppTypeLen;
		StorageNew->lldpXMedLocMediaPolicyAppType[StorageNew->lldpXMedLocMediaPolicyAppTypeLen] = 0;
		StorageNew->lldpXMedLocMediaPolicyVlanID = thedata->lldpXMedLocMediaPolicyVlanID;
		StorageNew->lldpXMedLocMediaPolicyPriority = thedata->lldpXMedLocMediaPolicyPriority;
		StorageNew->lldpXMedLocMediaPolicyDscp = thedata->lldpXMedLocMediaPolicyDscp;
		StorageNew->lldpXMedLocMediaPolicyUnknown = thedata->lldpXMedLocMediaPolicyUnknown;
		StorageNew->lldpXMedLocMediaPolicyTagged = thedata->lldpXMedLocMediaPolicyTagged;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedLocMediaPolicyTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedLocMediaPolicyTable_destroy(struct lldpXMedLocMediaPolicyTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedLocMediaPolicyTable_destroy(struct lldpXMedLocMediaPolicyTable_data **thedata)
{
	struct lldpXMedLocMediaPolicyTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocMediaPolicyTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedLocMediaPolicyAppType);
		StorageDel->lldpXMedLocMediaPolicyAppTypeLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedLocMediaPolicyTable_add(struct lldpXMedLocMediaPolicyTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedLocMediaPolicyTable table data set.
 *
 * Adds a table row structure to the lldpXMedLocMediaPolicyTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedLocMediaPolicyTable_add(struct lldpXMedLocMediaPolicyTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocMediaPolicyTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXMedLocMediaPolicyAppType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->lldpXMedLocMediaPolicyAppType, thedata->lldpXMedLocMediaPolicyAppTypeLen);
		header_complex_add_data(&lldpXMedLocMediaPolicyTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedLocMediaPolicyTable_del(struct lldpXMedLocMediaPolicyTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedLocMediaPolicyTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedLocMediaPolicyTable_del(struct lldpXMedLocMediaPolicyTable_data *thedata)
{
	struct lldpXMedLocMediaPolicyTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocMediaPolicyTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedLocMediaPolicyTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedLocMediaPolicyTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedLocMediaPolicyTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedLocMediaPolicyTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedLocMediaPolicyTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedLocMediaPolicyTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedLocMediaPolicyTable_data *StorageTmp = lldpXMedLocMediaPolicyTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedLocMediaPolicyTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedLocMediaPolicyAppType);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocMediaPolicyAppType, &StorageTmp->lldpXMedLocMediaPolicyAppTypeLen);
	if (StorageTmp->lldpXMedLocMediaPolicyAppType == NULL) {
		config_perror("invalid specification for lldpXMedLocMediaPolicyAppType");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocMediaPolicyVlanID, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocMediaPolicyPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocMediaPolicyDscp, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocMediaPolicyUnknown, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocMediaPolicyTagged, &tmpsize);
	lldpXMedLocMediaPolicyTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedLocMediaPolicyTable(): store configuraiton file for lldpXMedLocMediaPolicyTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedLocMediaPolicyTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedLocMediaPolicyTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedLocMediaPolicyTable: storing data...  "));
	refresh_lldpXMedLocMediaPolicyTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedLocMediaPolicyTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedLocMediaPolicyTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedLocMediaPolicyTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocMediaPolicyAppType, &StorageTmp->lldpXMedLocMediaPolicyAppTypeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocMediaPolicyVlanID, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocMediaPolicyPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocMediaPolicyDscp, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocMediaPolicyUnknown, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocMediaPolicyTagged, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedLocLocationTable_data *lldpXMedLocLocationTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedLocLocationTable table.
 *
 * Creates a new lldpXMedLocLocationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedLocLocationTable_data *
lldpXMedLocLocationTable_create(void)
{
	struct lldpXMedLocLocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedLocLocationTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocLocationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		if ((StorageNew->lldpXMedLocLocationInfo = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->lldpXMedLocLocationInfoLen = strlen("");
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedLocLocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedLocLocationTable_data *lldpXMedLocLocationTable_duplicate(struct lldpXMedLocLocationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedLocLocationTable_data *
lldpXMedLocLocationTable_duplicate(struct lldpXMedLocLocationTable_data *thedata)
{
	struct lldpXMedLocLocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedLocLocationTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocLocationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedLocLocationTable_id = thedata->lldpXMedLocLocationTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXMedLocLocationSubtype = thedata->lldpXMedLocLocationSubtype;
		if (!(StorageNew->lldpXMedLocLocationInfo = malloc(thedata->lldpXMedLocLocationInfoLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedLocLocationInfo, thedata->lldpXMedLocLocationInfo, thedata->lldpXMedLocLocationInfoLen);
		StorageNew->lldpXMedLocLocationInfoLen = thedata->lldpXMedLocLocationInfoLen;
		StorageNew->lldpXMedLocLocationInfo[StorageNew->lldpXMedLocLocationInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedLocLocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedLocLocationTable_destroy(struct lldpXMedLocLocationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedLocLocationTable_destroy(struct lldpXMedLocLocationTable_data **thedata)
{
	struct lldpXMedLocLocationTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocLocationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedLocLocationInfo);
		StorageDel->lldpXMedLocLocationInfoLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedLocLocationTable_add(struct lldpXMedLocLocationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedLocLocationTable table data set.
 *
 * Adds a table row structure to the lldpXMedLocLocationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedLocLocationTable_add(struct lldpXMedLocLocationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocLocationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXMedLocLocationSubtype */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXMedLocLocationSubtype, sizeof(thedata->lldpXMedLocLocationSubtype));
		header_complex_add_data(&lldpXMedLocLocationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedLocLocationTable_del(struct lldpXMedLocLocationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedLocLocationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedLocLocationTable_del(struct lldpXMedLocLocationTable_data *thedata)
{
	struct lldpXMedLocLocationTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocLocationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedLocLocationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedLocLocationTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedLocLocationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedLocLocationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedLocLocationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedLocLocationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedLocLocationTable_data *StorageTmp = lldpXMedLocLocationTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedLocLocationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocLocationSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedLocLocationInfo);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedLocLocationInfo, &StorageTmp->lldpXMedLocLocationInfoLen);
	if (StorageTmp->lldpXMedLocLocationInfo == NULL) {
		config_perror("invalid specification for lldpXMedLocLocationInfo");
		return;
	}
	lldpXMedLocLocationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedLocLocationTable(): store configuraiton file for lldpXMedLocLocationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedLocLocationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedLocLocationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedLocLocationTable: storing data...  "));
	refresh_lldpXMedLocLocationTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedLocLocationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedLocLocationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedLocLocationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocLocationSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedLocLocationInfo, &StorageTmp->lldpXMedLocLocationInfoLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedLocXPoEPSEPortTable_data *lldpXMedLocXPoEPSEPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedLocXPoEPSEPortTable table.
 *
 * Creates a new lldpXMedLocXPoEPSEPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedLocXPoEPSEPortTable_data *
lldpXMedLocXPoEPSEPortTable_create(void)
{
	struct lldpXMedLocXPoEPSEPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedLocXPoEPSEPortTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocXPoEPSEPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXMedLocXPoEPSEPortPowerAv = 0;
		StorageNew->lldpXMedLocXPoEPSEPortPDPriority = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedLocXPoEPSEPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedLocXPoEPSEPortTable_data *lldpXMedLocXPoEPSEPortTable_duplicate(struct lldpXMedLocXPoEPSEPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedLocXPoEPSEPortTable_data *
lldpXMedLocXPoEPSEPortTable_duplicate(struct lldpXMedLocXPoEPSEPortTable_data *thedata)
{
	struct lldpXMedLocXPoEPSEPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedLocXPoEPSEPortTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocXPoEPSEPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedLocXPoEPSEPortTable_id = thedata->lldpXMedLocXPoEPSEPortTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXMedLocXPoEPSEPortPowerAv = thedata->lldpXMedLocXPoEPSEPortPowerAv;
		StorageNew->lldpXMedLocXPoEPSEPortPDPriority = thedata->lldpXMedLocXPoEPSEPortPDPriority;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedLocXPoEPSEPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedLocXPoEPSEPortTable_destroy(struct lldpXMedLocXPoEPSEPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedLocXPoEPSEPortTable_destroy(struct lldpXMedLocXPoEPSEPortTable_data **thedata)
{
	struct lldpXMedLocXPoEPSEPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocXPoEPSEPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedLocXPoEPSEPortTable_add(struct lldpXMedLocXPoEPSEPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedLocXPoEPSEPortTable table data set.
 *
 * Adds a table row structure to the lldpXMedLocXPoEPSEPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedLocXPoEPSEPortTable_add(struct lldpXMedLocXPoEPSEPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocXPoEPSEPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		header_complex_add_data(&lldpXMedLocXPoEPSEPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedLocXPoEPSEPortTable_del(struct lldpXMedLocXPoEPSEPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedLocXPoEPSEPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedLocXPoEPSEPortTable_del(struct lldpXMedLocXPoEPSEPortTable_data *thedata)
{
	struct lldpXMedLocXPoEPSEPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedLocXPoEPSEPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedLocXPoEPSEPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedLocXPoEPSEPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedLocXPoEPSEPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedLocXPoEPSEPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedLocXPoEPSEPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedLocXPoEPSEPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp = lldpXMedLocXPoEPSEPortTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedLocXPoEPSEPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpXMedLocXPoEPSEPortPowerAv, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedLocXPoEPSEPortPDPriority, &tmpsize);
	lldpXMedLocXPoEPSEPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedLocXPoEPSEPortTable(): store configuraiton file for lldpXMedLocXPoEPSEPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedLocXPoEPSEPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedLocXPoEPSEPortTable: storing data...  "));
	refresh_lldpXMedLocXPoEPSEPortTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedLocXPoEPSEPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedLocXPoEPSEPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedLocXPoEPSEPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpXMedLocXPoEPSEPortPowerAv, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedLocXPoEPSEPortPDPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemCapabilitiesTable_data *lldpXMedRemCapabilitiesTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemCapabilitiesTable table.
 *
 * Creates a new lldpXMedRemCapabilitiesTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemCapabilitiesTable_data *
lldpXMedRemCapabilitiesTable_create(void)
{
	struct lldpXMedRemCapabilitiesTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemCapabilitiesTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemCapabilitiesTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		if (memdup((u_char **) &StorageNew->lldpXMedRemCapSupported, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpXMedRemCapSupportedLen = 1;
		if (memdup((u_char **) &StorageNew->lldpXMedRemCapCurrent, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpXMedRemCapCurrentLen = 1;
		StorageNew->lldpXMedRemDeviceClass = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemCapabilitiesTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemCapabilitiesTable_data *lldpXMedRemCapabilitiesTable_duplicate(struct lldpXMedRemCapabilitiesTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemCapabilitiesTable_data *
lldpXMedRemCapabilitiesTable_duplicate(struct lldpXMedRemCapabilitiesTable_data *thedata)
{
	struct lldpXMedRemCapabilitiesTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemCapabilitiesTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemCapabilitiesTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemCapabilitiesTable_id = thedata->lldpXMedRemCapabilitiesTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		if (!(StorageNew->lldpXMedRemCapSupported = malloc(thedata->lldpXMedRemCapSupportedLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemCapSupported, thedata->lldpXMedRemCapSupported, thedata->lldpXMedRemCapSupportedLen);
		StorageNew->lldpXMedRemCapSupportedLen = thedata->lldpXMedRemCapSupportedLen;
		StorageNew->lldpXMedRemCapSupported[StorageNew->lldpXMedRemCapSupportedLen] = 0;
		if (!(StorageNew->lldpXMedRemCapCurrent = malloc(thedata->lldpXMedRemCapCurrentLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemCapCurrent, thedata->lldpXMedRemCapCurrent, thedata->lldpXMedRemCapCurrentLen);
		StorageNew->lldpXMedRemCapCurrentLen = thedata->lldpXMedRemCapCurrentLen;
		StorageNew->lldpXMedRemCapCurrent[StorageNew->lldpXMedRemCapCurrentLen] = 0;
		StorageNew->lldpXMedRemDeviceClass = thedata->lldpXMedRemDeviceClass;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemCapabilitiesTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemCapabilitiesTable_destroy(struct lldpXMedRemCapabilitiesTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemCapabilitiesTable_destroy(struct lldpXMedRemCapabilitiesTable_data **thedata)
{
	struct lldpXMedRemCapabilitiesTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemCapabilitiesTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedRemCapSupported);
		StorageDel->lldpXMedRemCapSupportedLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemCapCurrent);
		StorageDel->lldpXMedRemCapCurrentLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemCapabilitiesTable_add(struct lldpXMedRemCapabilitiesTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemCapabilitiesTable table data set.
 *
 * Adds a table row structure to the lldpXMedRemCapabilitiesTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemCapabilitiesTable_add(struct lldpXMedRemCapabilitiesTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemCapabilitiesTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		header_complex_add_data(&lldpXMedRemCapabilitiesTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemCapabilitiesTable_del(struct lldpXMedRemCapabilitiesTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemCapabilitiesTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemCapabilitiesTable_del(struct lldpXMedRemCapabilitiesTable_data *thedata)
{
	struct lldpXMedRemCapabilitiesTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemCapabilitiesTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemCapabilitiesTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemCapabilitiesTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemCapabilitiesTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemCapabilitiesTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemCapabilitiesTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemCapabilitiesTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemCapabilitiesTable_data *StorageTmp = lldpXMedRemCapabilitiesTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemCapabilitiesTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedRemCapSupported);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemCapSupported, &StorageTmp->lldpXMedRemCapSupportedLen);
	if (StorageTmp->lldpXMedRemCapSupported == NULL) {
		config_perror("invalid specification for lldpXMedRemCapSupported");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemCapCurrent);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemCapCurrent, &StorageTmp->lldpXMedRemCapCurrentLen);
	if (StorageTmp->lldpXMedRemCapCurrent == NULL) {
		config_perror("invalid specification for lldpXMedRemCapCurrent");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemDeviceClass, &tmpsize);
	lldpXMedRemCapabilitiesTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemCapabilitiesTable(): store configuraiton file for lldpXMedRemCapabilitiesTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemCapabilitiesTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemCapabilitiesTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemCapabilitiesTable: storing data...  "));
	refresh_lldpXMedRemCapabilitiesTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemCapabilitiesTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemCapabilitiesTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemCapabilitiesTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemCapSupported, &StorageTmp->lldpXMedRemCapSupportedLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemCapCurrent, &StorageTmp->lldpXMedRemCapCurrentLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemDeviceClass, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemMediaPolicyTable_data *lldpXMedRemMediaPolicyTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemMediaPolicyTable table.
 *
 * Creates a new lldpXMedRemMediaPolicyTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemMediaPolicyTable_data *
lldpXMedRemMediaPolicyTable_create(void)
{
	struct lldpXMedRemMediaPolicyTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemMediaPolicyTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemMediaPolicyTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		StorageNew->lldpXMedRemMediaPolicyVlanID = 0;
		StorageNew->lldpXMedRemMediaPolicyPriority = 0;
		StorageNew->lldpXMedRemMediaPolicyDscp = 0;
		StorageNew->lldpXMedRemMediaPolicyUnknown = 0;
		StorageNew->lldpXMedRemMediaPolicyTagged = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemMediaPolicyTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemMediaPolicyTable_data *lldpXMedRemMediaPolicyTable_duplicate(struct lldpXMedRemMediaPolicyTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemMediaPolicyTable_data *
lldpXMedRemMediaPolicyTable_duplicate(struct lldpXMedRemMediaPolicyTable_data *thedata)
{
	struct lldpXMedRemMediaPolicyTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemMediaPolicyTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemMediaPolicyTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemMediaPolicyTable_id = thedata->lldpXMedRemMediaPolicyTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		if (!(StorageNew->lldpXMedRemMediaPolicyAppType = malloc(thedata->lldpXMedRemMediaPolicyAppTypeLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemMediaPolicyAppType, thedata->lldpXMedRemMediaPolicyAppType, thedata->lldpXMedRemMediaPolicyAppTypeLen);
		StorageNew->lldpXMedRemMediaPolicyAppTypeLen = thedata->lldpXMedRemMediaPolicyAppTypeLen;
		StorageNew->lldpXMedRemMediaPolicyAppType[StorageNew->lldpXMedRemMediaPolicyAppTypeLen] = 0;
		StorageNew->lldpXMedRemMediaPolicyVlanID = thedata->lldpXMedRemMediaPolicyVlanID;
		StorageNew->lldpXMedRemMediaPolicyPriority = thedata->lldpXMedRemMediaPolicyPriority;
		StorageNew->lldpXMedRemMediaPolicyDscp = thedata->lldpXMedRemMediaPolicyDscp;
		StorageNew->lldpXMedRemMediaPolicyUnknown = thedata->lldpXMedRemMediaPolicyUnknown;
		StorageNew->lldpXMedRemMediaPolicyTagged = thedata->lldpXMedRemMediaPolicyTagged;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemMediaPolicyTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemMediaPolicyTable_destroy(struct lldpXMedRemMediaPolicyTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemMediaPolicyTable_destroy(struct lldpXMedRemMediaPolicyTable_data **thedata)
{
	struct lldpXMedRemMediaPolicyTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemMediaPolicyTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedRemMediaPolicyAppType);
		StorageDel->lldpXMedRemMediaPolicyAppTypeLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemMediaPolicyTable_add(struct lldpXMedRemMediaPolicyTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemMediaPolicyTable table data set.
 *
 * Adds a table row structure to the lldpXMedRemMediaPolicyTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemMediaPolicyTable_add(struct lldpXMedRemMediaPolicyTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemMediaPolicyTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		/* lldpXMedRemMediaPolicyAppType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->lldpXMedRemMediaPolicyAppType, thedata->lldpXMedRemMediaPolicyAppTypeLen);
		header_complex_add_data(&lldpXMedRemMediaPolicyTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemMediaPolicyTable_del(struct lldpXMedRemMediaPolicyTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemMediaPolicyTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemMediaPolicyTable_del(struct lldpXMedRemMediaPolicyTable_data *thedata)
{
	struct lldpXMedRemMediaPolicyTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemMediaPolicyTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemMediaPolicyTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemMediaPolicyTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemMediaPolicyTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemMediaPolicyTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemMediaPolicyTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemMediaPolicyTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemMediaPolicyTable_data *StorageTmp = lldpXMedRemMediaPolicyTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemMediaPolicyTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedRemMediaPolicyAppType);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemMediaPolicyAppType, &StorageTmp->lldpXMedRemMediaPolicyAppTypeLen);
	if (StorageTmp->lldpXMedRemMediaPolicyAppType == NULL) {
		config_perror("invalid specification for lldpXMedRemMediaPolicyAppType");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemMediaPolicyVlanID, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemMediaPolicyPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemMediaPolicyDscp, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemMediaPolicyUnknown, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemMediaPolicyTagged, &tmpsize);
	lldpXMedRemMediaPolicyTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemMediaPolicyTable(): store configuraiton file for lldpXMedRemMediaPolicyTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemMediaPolicyTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemMediaPolicyTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemMediaPolicyTable: storing data...  "));
	refresh_lldpXMedRemMediaPolicyTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemMediaPolicyTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemMediaPolicyTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemMediaPolicyTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemMediaPolicyAppType, &StorageTmp->lldpXMedRemMediaPolicyAppTypeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemMediaPolicyVlanID, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemMediaPolicyPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemMediaPolicyDscp, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemMediaPolicyUnknown, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemMediaPolicyTagged, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemInventoryTable_data *lldpXMedRemInventoryTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemInventoryTable table.
 *
 * Creates a new lldpXMedRemInventoryTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemInventoryTable_data *
lldpXMedRemInventoryTable_create(void)
{
	struct lldpXMedRemInventoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemInventoryTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemInventoryTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		if ((StorageNew->lldpXMedRemHardwareRev = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemHardwareRevLen = 0;
		StorageNew->lldpXMedRemHardwareRev[StorageNew->lldpXMedRemHardwareRevLen] = 0;
		if ((StorageNew->lldpXMedRemFirmwareRev = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemFirmwareRevLen = 0;
		StorageNew->lldpXMedRemFirmwareRev[StorageNew->lldpXMedRemFirmwareRevLen] = 0;
		if ((StorageNew->lldpXMedRemSoftwareRev = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemSoftwareRevLen = 0;
		StorageNew->lldpXMedRemSoftwareRev[StorageNew->lldpXMedRemSoftwareRevLen] = 0;
		if ((StorageNew->lldpXMedRemSerialNum = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemSerialNumLen = 0;
		StorageNew->lldpXMedRemSerialNum[StorageNew->lldpXMedRemSerialNumLen] = 0;
		if ((StorageNew->lldpXMedRemMfgName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemMfgNameLen = 0;
		StorageNew->lldpXMedRemMfgName[StorageNew->lldpXMedRemMfgNameLen] = 0;
		if ((StorageNew->lldpXMedRemModelName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemModelNameLen = 0;
		StorageNew->lldpXMedRemModelName[StorageNew->lldpXMedRemModelNameLen] = 0;
		if ((StorageNew->lldpXMedRemAssetID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemAssetIDLen = 0;
		StorageNew->lldpXMedRemAssetID[StorageNew->lldpXMedRemAssetIDLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemInventoryTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemInventoryTable_data *lldpXMedRemInventoryTable_duplicate(struct lldpXMedRemInventoryTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemInventoryTable_data *
lldpXMedRemInventoryTable_duplicate(struct lldpXMedRemInventoryTable_data *thedata)
{
	struct lldpXMedRemInventoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemInventoryTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemInventoryTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemInventoryTable_id = thedata->lldpXMedRemInventoryTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		if (!(StorageNew->lldpXMedRemHardwareRev = malloc(thedata->lldpXMedRemHardwareRevLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemHardwareRev, thedata->lldpXMedRemHardwareRev, thedata->lldpXMedRemHardwareRevLen);
		StorageNew->lldpXMedRemHardwareRevLen = thedata->lldpXMedRemHardwareRevLen;
		StorageNew->lldpXMedRemHardwareRev[StorageNew->lldpXMedRemHardwareRevLen] = 0;
		if (!(StorageNew->lldpXMedRemFirmwareRev = malloc(thedata->lldpXMedRemFirmwareRevLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemFirmwareRev, thedata->lldpXMedRemFirmwareRev, thedata->lldpXMedRemFirmwareRevLen);
		StorageNew->lldpXMedRemFirmwareRevLen = thedata->lldpXMedRemFirmwareRevLen;
		StorageNew->lldpXMedRemFirmwareRev[StorageNew->lldpXMedRemFirmwareRevLen] = 0;
		if (!(StorageNew->lldpXMedRemSoftwareRev = malloc(thedata->lldpXMedRemSoftwareRevLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemSoftwareRev, thedata->lldpXMedRemSoftwareRev, thedata->lldpXMedRemSoftwareRevLen);
		StorageNew->lldpXMedRemSoftwareRevLen = thedata->lldpXMedRemSoftwareRevLen;
		StorageNew->lldpXMedRemSoftwareRev[StorageNew->lldpXMedRemSoftwareRevLen] = 0;
		if (!(StorageNew->lldpXMedRemSerialNum = malloc(thedata->lldpXMedRemSerialNumLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemSerialNum, thedata->lldpXMedRemSerialNum, thedata->lldpXMedRemSerialNumLen);
		StorageNew->lldpXMedRemSerialNumLen = thedata->lldpXMedRemSerialNumLen;
		StorageNew->lldpXMedRemSerialNum[StorageNew->lldpXMedRemSerialNumLen] = 0;
		if (!(StorageNew->lldpXMedRemMfgName = malloc(thedata->lldpXMedRemMfgNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemMfgName, thedata->lldpXMedRemMfgName, thedata->lldpXMedRemMfgNameLen);
		StorageNew->lldpXMedRemMfgNameLen = thedata->lldpXMedRemMfgNameLen;
		StorageNew->lldpXMedRemMfgName[StorageNew->lldpXMedRemMfgNameLen] = 0;
		if (!(StorageNew->lldpXMedRemModelName = malloc(thedata->lldpXMedRemModelNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemModelName, thedata->lldpXMedRemModelName, thedata->lldpXMedRemModelNameLen);
		StorageNew->lldpXMedRemModelNameLen = thedata->lldpXMedRemModelNameLen;
		StorageNew->lldpXMedRemModelName[StorageNew->lldpXMedRemModelNameLen] = 0;
		if (!(StorageNew->lldpXMedRemAssetID = malloc(thedata->lldpXMedRemAssetIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemAssetID, thedata->lldpXMedRemAssetID, thedata->lldpXMedRemAssetIDLen);
		StorageNew->lldpXMedRemAssetIDLen = thedata->lldpXMedRemAssetIDLen;
		StorageNew->lldpXMedRemAssetID[StorageNew->lldpXMedRemAssetIDLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemInventoryTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemInventoryTable_destroy(struct lldpXMedRemInventoryTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemInventoryTable_destroy(struct lldpXMedRemInventoryTable_data **thedata)
{
	struct lldpXMedRemInventoryTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemInventoryTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedRemHardwareRev);
		StorageDel->lldpXMedRemHardwareRevLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemFirmwareRev);
		StorageDel->lldpXMedRemFirmwareRevLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemSoftwareRev);
		StorageDel->lldpXMedRemSoftwareRevLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemSerialNum);
		StorageDel->lldpXMedRemSerialNumLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemMfgName);
		StorageDel->lldpXMedRemMfgNameLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemModelName);
		StorageDel->lldpXMedRemModelNameLen = 0;
		SNMP_FREE(StorageDel->lldpXMedRemAssetID);
		StorageDel->lldpXMedRemAssetIDLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemInventoryTable_add(struct lldpXMedRemInventoryTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemInventoryTable table data set.
 *
 * Adds a table row structure to the lldpXMedRemInventoryTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemInventoryTable_add(struct lldpXMedRemInventoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemInventoryTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		header_complex_add_data(&lldpXMedRemInventoryTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemInventoryTable_del(struct lldpXMedRemInventoryTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemInventoryTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemInventoryTable_del(struct lldpXMedRemInventoryTable_data *thedata)
{
	struct lldpXMedRemInventoryTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemInventoryTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemInventoryTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemInventoryTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemInventoryTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemInventoryTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemInventoryTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemInventoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemInventoryTable_data *StorageTmp = lldpXMedRemInventoryTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemInventoryTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedRemHardwareRev);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemHardwareRev, &StorageTmp->lldpXMedRemHardwareRevLen);
	if (StorageTmp->lldpXMedRemHardwareRev == NULL) {
		config_perror("invalid specification for lldpXMedRemHardwareRev");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemFirmwareRev);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemFirmwareRev, &StorageTmp->lldpXMedRemFirmwareRevLen);
	if (StorageTmp->lldpXMedRemFirmwareRev == NULL) {
		config_perror("invalid specification for lldpXMedRemFirmwareRev");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemSoftwareRev);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemSoftwareRev, &StorageTmp->lldpXMedRemSoftwareRevLen);
	if (StorageTmp->lldpXMedRemSoftwareRev == NULL) {
		config_perror("invalid specification for lldpXMedRemSoftwareRev");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemSerialNum);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemSerialNum, &StorageTmp->lldpXMedRemSerialNumLen);
	if (StorageTmp->lldpXMedRemSerialNum == NULL) {
		config_perror("invalid specification for lldpXMedRemSerialNum");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemMfgName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemMfgName, &StorageTmp->lldpXMedRemMfgNameLen);
	if (StorageTmp->lldpXMedRemMfgName == NULL) {
		config_perror("invalid specification for lldpXMedRemMfgName");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemModelName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemModelName, &StorageTmp->lldpXMedRemModelNameLen);
	if (StorageTmp->lldpXMedRemModelName == NULL) {
		config_perror("invalid specification for lldpXMedRemModelName");
		return;
	}
	SNMP_FREE(StorageTmp->lldpXMedRemAssetID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemAssetID, &StorageTmp->lldpXMedRemAssetIDLen);
	if (StorageTmp->lldpXMedRemAssetID == NULL) {
		config_perror("invalid specification for lldpXMedRemAssetID");
		return;
	}
	lldpXMedRemInventoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemInventoryTable(): store configuraiton file for lldpXMedRemInventoryTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemInventoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemInventoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemInventoryTable: storing data...  "));
	refresh_lldpXMedRemInventoryTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemInventoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemInventoryTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemInventoryTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemHardwareRev, &StorageTmp->lldpXMedRemHardwareRevLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemFirmwareRev, &StorageTmp->lldpXMedRemFirmwareRevLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemSoftwareRev, &StorageTmp->lldpXMedRemSoftwareRevLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemSerialNum, &StorageTmp->lldpXMedRemSerialNumLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemMfgName, &StorageTmp->lldpXMedRemMfgNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemModelName, &StorageTmp->lldpXMedRemModelNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemAssetID, &StorageTmp->lldpXMedRemAssetIDLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemLocationTable_data *lldpXMedRemLocationTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemLocationTable table.
 *
 * Creates a new lldpXMedRemLocationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemLocationTable_data *
lldpXMedRemLocationTable_create(void)
{
	struct lldpXMedRemLocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemLocationTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemLocationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		if ((StorageNew->lldpXMedRemLocationInfo = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXMedRemLocationInfoLen = 0;
		StorageNew->lldpXMedRemLocationInfo[StorageNew->lldpXMedRemLocationInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemLocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemLocationTable_data *lldpXMedRemLocationTable_duplicate(struct lldpXMedRemLocationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemLocationTable_data *
lldpXMedRemLocationTable_duplicate(struct lldpXMedRemLocationTable_data *thedata)
{
	struct lldpXMedRemLocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemLocationTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemLocationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemLocationTable_id = thedata->lldpXMedRemLocationTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXMedRemLocationSubtype = thedata->lldpXMedRemLocationSubtype;
		if (!(StorageNew->lldpXMedRemLocationInfo = malloc(thedata->lldpXMedRemLocationInfoLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXMedRemLocationInfo, thedata->lldpXMedRemLocationInfo, thedata->lldpXMedRemLocationInfoLen);
		StorageNew->lldpXMedRemLocationInfoLen = thedata->lldpXMedRemLocationInfoLen;
		StorageNew->lldpXMedRemLocationInfo[StorageNew->lldpXMedRemLocationInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemLocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemLocationTable_destroy(struct lldpXMedRemLocationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemLocationTable_destroy(struct lldpXMedRemLocationTable_data **thedata)
{
	struct lldpXMedRemLocationTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemLocationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXMedRemLocationInfo);
		StorageDel->lldpXMedRemLocationInfoLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemLocationTable_add(struct lldpXMedRemLocationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemLocationTable table data set.
 *
 * Adds a table row structure to the lldpXMedRemLocationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemLocationTable_add(struct lldpXMedRemLocationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemLocationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		/* lldpXMedRemLocationSubtype */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXMedRemLocationSubtype, sizeof(thedata->lldpXMedRemLocationSubtype));
		header_complex_add_data(&lldpXMedRemLocationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemLocationTable_del(struct lldpXMedRemLocationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemLocationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemLocationTable_del(struct lldpXMedRemLocationTable_data *thedata)
{
	struct lldpXMedRemLocationTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemLocationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemLocationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemLocationTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemLocationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemLocationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemLocationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemLocationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemLocationTable_data *StorageTmp = lldpXMedRemLocationTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemLocationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemLocationSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXMedRemLocationInfo);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXMedRemLocationInfo, &StorageTmp->lldpXMedRemLocationInfoLen);
	if (StorageTmp->lldpXMedRemLocationInfo == NULL) {
		config_perror("invalid specification for lldpXMedRemLocationInfo");
		return;
	}
	lldpXMedRemLocationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemLocationTable(): store configuraiton file for lldpXMedRemLocationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemLocationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemLocationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemLocationTable: storing data...  "));
	refresh_lldpXMedRemLocationTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemLocationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemLocationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemLocationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemLocationSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXMedRemLocationInfo, &StorageTmp->lldpXMedRemLocationInfoLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemXPoETable_data *lldpXMedRemXPoETable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemXPoETable table.
 *
 * Creates a new lldpXMedRemXPoETable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemXPoETable_data *
lldpXMedRemXPoETable_create(void)
{
	struct lldpXMedRemXPoETable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemXPoETable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoETable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		StorageNew->lldpXMedRemXPoEDeviceType = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemXPoETable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemXPoETable_data *lldpXMedRemXPoETable_duplicate(struct lldpXMedRemXPoETable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemXPoETable_data *
lldpXMedRemXPoETable_duplicate(struct lldpXMedRemXPoETable_data *thedata)
{
	struct lldpXMedRemXPoETable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemXPoETable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoETable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemXPoETable_id = thedata->lldpXMedRemXPoETable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXMedRemXPoEDeviceType = thedata->lldpXMedRemXPoEDeviceType;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemXPoETable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemXPoETable_destroy(struct lldpXMedRemXPoETable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemXPoETable_destroy(struct lldpXMedRemXPoETable_data **thedata)
{
	struct lldpXMedRemXPoETable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoETable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemXPoETable_add(struct lldpXMedRemXPoETable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemXPoETable table data set.
 *
 * Adds a table row structure to the lldpXMedRemXPoETable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemXPoETable_add(struct lldpXMedRemXPoETable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoETable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		header_complex_add_data(&lldpXMedRemXPoETableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemXPoETable_del(struct lldpXMedRemXPoETable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemXPoETable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemXPoETable_del(struct lldpXMedRemXPoETable_data *thedata)
{
	struct lldpXMedRemXPoETable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoETable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemXPoETableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemXPoETableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemXPoETable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemXPoETable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemXPoETable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemXPoETable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemXPoETable_data *StorageTmp = lldpXMedRemXPoETable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemXPoETable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemXPoEDeviceType, &tmpsize);
	lldpXMedRemXPoETable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemXPoETable(): store configuraiton file for lldpXMedRemXPoETable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemXPoETable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemXPoETable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemXPoETable: storing data...  "));
	refresh_lldpXMedRemXPoETable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemXPoETableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemXPoETable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemXPoETable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemXPoEDeviceType, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemXPoEPSETable_data *lldpXMedRemXPoEPSETable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemXPoEPSETable table.
 *
 * Creates a new lldpXMedRemXPoEPSETable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemXPoEPSETable_data *
lldpXMedRemXPoEPSETable_create(void)
{
	struct lldpXMedRemXPoEPSETable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemXPoEPSETable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPSETable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		StorageNew->lldpXMedRemXPoEPSEPowerAv = 0;
		StorageNew->lldpXMedRemXPoEPSEPowerSource = 0;
		StorageNew->lldpXMedRemXPoEPSEPowerPriority = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemXPoEPSETable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemXPoEPSETable_data *lldpXMedRemXPoEPSETable_duplicate(struct lldpXMedRemXPoEPSETable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemXPoEPSETable_data *
lldpXMedRemXPoEPSETable_duplicate(struct lldpXMedRemXPoEPSETable_data *thedata)
{
	struct lldpXMedRemXPoEPSETable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemXPoEPSETable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPSETable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemXPoEPSETable_id = thedata->lldpXMedRemXPoEPSETable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXMedRemXPoEPSEPowerAv = thedata->lldpXMedRemXPoEPSEPowerAv;
		StorageNew->lldpXMedRemXPoEPSEPowerSource = thedata->lldpXMedRemXPoEPSEPowerSource;
		StorageNew->lldpXMedRemXPoEPSEPowerPriority = thedata->lldpXMedRemXPoEPSEPowerPriority;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemXPoEPSETable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemXPoEPSETable_destroy(struct lldpXMedRemXPoEPSETable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemXPoEPSETable_destroy(struct lldpXMedRemXPoEPSETable_data **thedata)
{
	struct lldpXMedRemXPoEPSETable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPSETable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemXPoEPSETable_add(struct lldpXMedRemXPoEPSETable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemXPoEPSETable table data set.
 *
 * Adds a table row structure to the lldpXMedRemXPoEPSETable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemXPoEPSETable_add(struct lldpXMedRemXPoEPSETable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPSETable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		header_complex_add_data(&lldpXMedRemXPoEPSETableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemXPoEPSETable_del(struct lldpXMedRemXPoEPSETable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemXPoEPSETable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemXPoEPSETable_del(struct lldpXMedRemXPoEPSETable_data *thedata)
{
	struct lldpXMedRemXPoEPSETable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPSETable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemXPoEPSETableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemXPoEPSETableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemXPoEPSETable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemXPoEPSETable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemXPoEPSETable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemXPoEPSETable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemXPoEPSETable_data *StorageTmp = lldpXMedRemXPoEPSETable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemXPoEPSETable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpXMedRemXPoEPSEPowerAv, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemXPoEPSEPowerSource, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemXPoEPSEPowerPriority, &tmpsize);
	lldpXMedRemXPoEPSETable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemXPoEPSETable(): store configuraiton file for lldpXMedRemXPoEPSETable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemXPoEPSETable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemXPoEPSETable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemXPoEPSETable: storing data...  "));
	refresh_lldpXMedRemXPoEPSETable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemXPoEPSETableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemXPoEPSETable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemXPoEPSETable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpXMedRemXPoEPSEPowerAv, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemXPoEPSEPowerSource, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemXPoEPSEPowerPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXMedRemXPoEPDTable_data *lldpXMedRemXPoEPDTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXMedRemXPoEPDTable table.
 *
 * Creates a new lldpXMedRemXPoEPDTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXMedRemXPoEPDTable_data *
lldpXMedRemXPoEPDTable_create(void)
{
	struct lldpXMedRemXPoEPDTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemXPoEPDTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPDTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		StorageNew->lldpXMedRemXPoEPDPowerReq = 0;
		StorageNew->lldpXMedRemXPoEPDPowerSource = 0;
		StorageNew->lldpXMedRemXPoEPDPowerPriority = 0;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXMedRemXPoEPDTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXMedRemXPoEPDTable_data *lldpXMedRemXPoEPDTable_duplicate(struct lldpXMedRemXPoEPDTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXMedRemXPoEPDTable_data *
lldpXMedRemXPoEPDTable_duplicate(struct lldpXMedRemXPoEPDTable_data *thedata)
{
	struct lldpXMedRemXPoEPDTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXMedRemXPoEPDTable_data);

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPDTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXMedRemXPoEPDTable_id = thedata->lldpXMedRemXPoEPDTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXMedRemXPoEPDPowerReq = thedata->lldpXMedRemXPoEPDPowerReq;
		StorageNew->lldpXMedRemXPoEPDPowerSource = thedata->lldpXMedRemXPoEPDPowerSource;
		StorageNew->lldpXMedRemXPoEPDPowerPriority = thedata->lldpXMedRemXPoEPDPowerPriority;
	}
      done:
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXMedRemXPoEPDTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXMedRemXPoEPDTable_destroy(struct lldpXMedRemXPoEPDTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXMedRemXPoEPDTable_destroy(struct lldpXMedRemXPoEPDTable_data **thedata)
{
	struct lldpXMedRemXPoEPDTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPDTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemXPoEPDTable_add(struct lldpXMedRemXPoEPDTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXMedRemXPoEPDTable table data set.
 *
 * Adds a table row structure to the lldpXMedRemXPoEPDTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXMedRemXPoEPDTable_add(struct lldpXMedRemXPoEPDTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPDTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		header_complex_add_data(&lldpXMedRemXPoEPDTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXMedMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXMedRemXPoEPDTable_del(struct lldpXMedRemXPoEPDTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXMedRemXPoEPDTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXMedRemXPoEPDTable_del(struct lldpXMedRemXPoEPDTable_data *thedata)
{
	struct lldpXMedRemXPoEPDTable_data *StorageDel;

	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedRemXPoEPDTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXMedRemXPoEPDTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXMedRemXPoEPDTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXMedRemXPoEPDTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXMedRemXPoEPDTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXMedRemXPoEPDTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXMedRemXPoEPDTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXMedRemXPoEPDTable_data *StorageTmp = lldpXMedRemXPoEPDTable_create();

	DEBUGMSGTL(("lldpXMedMIB", "parse_lldpXMedRemXPoEPDTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpXMedRemXPoEPDPowerReq, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemXPoEPDPowerSource, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXMedRemXPoEPDPowerPriority, &tmpsize);
	lldpXMedRemXPoEPDTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/*
 * store_lldpXMedRemXPoEPDTable(): store configuraiton file for lldpXMedRemXPoEPDTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXMedRemXPoEPDTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXMedRemXPoEPDTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXMedMIB", "store_lldpXMedRemXPoEPDTable: storing data...  "));
	refresh_lldpXMedRemXPoEPDTable(1);
	(void) tmpsize;
	for (hcindex = lldpXMedRemXPoEPDTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXMedRemXPoEPDTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXMedRemXPoEPDTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpXMedRemXPoEPDPowerReq, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemXPoEPDPowerSource, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXMedRemXPoEPDPowerPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, struct lldpXMedPortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, struct lldpXMedPortConfigTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, struct lldpXMedPortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedPortConfigTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, struct lldpXMedPortConfigTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedPortConfigTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, struct lldpXMedPortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, struct lldpXMedPortConfigTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedPortConfigTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedPortConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedPortConfigTable_data *
refresh_lldpXMedPortConfigTable_row(struct lldpXMedPortConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedPortConfigTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedPortConfigTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedPortConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedPortConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedPortConfigTable(int force)
{
	if (!force && lldpXMedPortConfigTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedPortConfigTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedPortConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedPortConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedPortConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedPortConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedPortConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedPortConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedPortConfigTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedPortConfigTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDPORTCAPSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedPortCapSupportedLen;
		rval = (u_char *) StorageTmp->lldpXMedPortCapSupported;
		break;
	case (u_char) LLDPXMEDPORTCONFIGTLVSTXENABLE:	/* ReadWrite */
		*write_method = write_lldpXMedPortConfigTLVsTxEnable;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedPortConfigTLVsTxEnableLen;
		rval = (u_char *) StorageTmp->lldpXMedPortConfigTLVsTxEnable;
		break;
	case (u_char) LLDPXMEDPORTCONFIGNOTIFENABLE:	/* ReadWrite */
		*write_method = write_lldpXMedPortConfigNotifEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedPortConfigNotifEnable);
		rval = (u_char *) &StorageTmp->lldpXMedPortConfigNotifEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, struct lldpXMedLocMediaPolicyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, struct lldpXMedLocMediaPolicyTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, struct lldpXMedLocMediaPolicyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedLocMediaPolicyTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, struct lldpXMedLocMediaPolicyTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedLocMediaPolicyTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, struct lldpXMedLocMediaPolicyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, struct lldpXMedLocMediaPolicyTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedLocMediaPolicyTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedLocMediaPolicyTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedLocMediaPolicyTable_data *
refresh_lldpXMedLocMediaPolicyTable_row(struct lldpXMedLocMediaPolicyTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedLocMediaPolicyTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedLocMediaPolicyTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedLocMediaPolicyTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedLocMediaPolicyTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedLocMediaPolicyTable(int force)
{
	if (!force && lldpXMedLocMediaPolicyTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedLocMediaPolicyTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedLocMediaPolicyTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedLocMediaPolicyTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedLocMediaPolicyTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedLocMediaPolicyTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedLocMediaPolicyTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedLocMediaPolicyTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedLocMediaPolicyTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedLocMediaPolicyTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDLOCMEDIAPOLICYVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocMediaPolicyVlanID);
		rval = (u_char *) &StorageTmp->lldpXMedLocMediaPolicyVlanID;
		break;
	case (u_char) LLDPXMEDLOCMEDIAPOLICYPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocMediaPolicyPriority);
		rval = (u_char *) &StorageTmp->lldpXMedLocMediaPolicyPriority;
		break;
	case (u_char) LLDPXMEDLOCMEDIAPOLICYDSCP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocMediaPolicyDscp);
		rval = (u_char *) &StorageTmp->lldpXMedLocMediaPolicyDscp;
		break;
	case (u_char) LLDPXMEDLOCMEDIAPOLICYUNKNOWN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocMediaPolicyUnknown);
		rval = (u_char *) &StorageTmp->lldpXMedLocMediaPolicyUnknown;
		break;
	case (u_char) LLDPXMEDLOCMEDIAPOLICYTAGGED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocMediaPolicyTagged);
		rval = (u_char *) &StorageTmp->lldpXMedLocMediaPolicyTagged;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, struct lldpXMedLocLocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, struct lldpXMedLocLocationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, struct lldpXMedLocLocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedLocLocationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, struct lldpXMedLocLocationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedLocLocationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, struct lldpXMedLocLocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, struct lldpXMedLocLocationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedLocLocationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedLocLocationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedLocLocationTable_data *
refresh_lldpXMedLocLocationTable_row(struct lldpXMedLocLocationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedLocLocationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedLocLocationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedLocLocationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedLocLocationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedLocLocationTable(int force)
{
	if (!force && lldpXMedLocLocationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedLocLocationTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedLocLocationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedLocLocationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedLocLocationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedLocLocationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedLocLocationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedLocLocationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedLocLocationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedLocLocationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDLOCLOCATIONINFO:	/* ReadWrite */
		*write_method = write_lldpXMedLocLocationInfo;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedLocLocationInfoLen;
		rval = (u_char *) StorageTmp->lldpXMedLocLocationInfo;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, struct lldpXMedLocXPoEPSEPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, struct lldpXMedLocXPoEPSEPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, struct lldpXMedLocXPoEPSEPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedLocXPoEPSEPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, struct lldpXMedLocXPoEPSEPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedLocXPoEPSEPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, struct lldpXMedLocXPoEPSEPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, struct lldpXMedLocXPoEPSEPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedLocXPoEPSEPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedLocXPoEPSEPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedLocXPoEPSEPortTable_data *
refresh_lldpXMedLocXPoEPSEPortTable_row(struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedLocXPoEPSEPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedLocXPoEPSEPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedLocXPoEPSEPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedLocXPoEPSEPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedLocXPoEPSEPortTable(int force)
{
	if (!force && lldpXMedLocXPoEPSEPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedLocXPoEPSEPortTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedLocXPoEPSEPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedLocXPoEPSEPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedLocXPoEPSEPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedLocXPoEPSEPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedLocXPoEPSEPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedLocXPoEPSEPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedLocXPoEPSEPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedLocXPoEPSEPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDLOCXPOEPSEPORTPOWERAV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEPSEPortPowerAv);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEPSEPortPowerAv;
		break;
	case (u_char) LLDPXMEDLOCXPOEPSEPORTPDPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedLocXPoEPSEPortPDPriority);
		rval = (u_char *) &StorageTmp->lldpXMedLocXPoEPSEPortPDPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, struct lldpXMedRemCapabilitiesTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, struct lldpXMedRemCapabilitiesTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, struct lldpXMedRemCapabilitiesTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemCapabilitiesTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, struct lldpXMedRemCapabilitiesTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemCapabilitiesTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, struct lldpXMedRemCapabilitiesTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, struct lldpXMedRemCapabilitiesTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemCapabilitiesTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemCapabilitiesTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemCapabilitiesTable_data *
refresh_lldpXMedRemCapabilitiesTable_row(struct lldpXMedRemCapabilitiesTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemCapabilitiesTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemCapabilitiesTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemCapabilitiesTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemCapabilitiesTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemCapabilitiesTable(int force)
{
	if (!force && lldpXMedRemCapabilitiesTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemCapabilitiesTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemCapabilitiesTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemCapabilitiesTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemCapabilitiesTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemCapabilitiesTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemCapabilitiesTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemCapabilitiesTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemCapabilitiesTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemCapabilitiesTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMCAPSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemCapSupportedLen;
		rval = (u_char *) StorageTmp->lldpXMedRemCapSupported;
		break;
	case (u_char) LLDPXMEDREMCAPCURRENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemCapCurrentLen;
		rval = (u_char *) StorageTmp->lldpXMedRemCapCurrent;
		break;
	case (u_char) LLDPXMEDREMDEVICECLASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemDeviceClass);
		rval = (u_char *) &StorageTmp->lldpXMedRemDeviceClass;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, struct lldpXMedRemMediaPolicyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, struct lldpXMedRemMediaPolicyTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, struct lldpXMedRemMediaPolicyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemMediaPolicyTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, struct lldpXMedRemMediaPolicyTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemMediaPolicyTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, struct lldpXMedRemMediaPolicyTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, struct lldpXMedRemMediaPolicyTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemMediaPolicyTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemMediaPolicyTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemMediaPolicyTable_data *
refresh_lldpXMedRemMediaPolicyTable_row(struct lldpXMedRemMediaPolicyTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemMediaPolicyTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemMediaPolicyTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemMediaPolicyTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemMediaPolicyTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemMediaPolicyTable(int force)
{
	if (!force && lldpXMedRemMediaPolicyTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemMediaPolicyTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemMediaPolicyTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemMediaPolicyTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemMediaPolicyTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemMediaPolicyTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemMediaPolicyTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemMediaPolicyTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemMediaPolicyTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemMediaPolicyTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMMEDIAPOLICYVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemMediaPolicyVlanID);
		rval = (u_char *) &StorageTmp->lldpXMedRemMediaPolicyVlanID;
		break;
	case (u_char) LLDPXMEDREMMEDIAPOLICYPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemMediaPolicyPriority);
		rval = (u_char *) &StorageTmp->lldpXMedRemMediaPolicyPriority;
		break;
	case (u_char) LLDPXMEDREMMEDIAPOLICYDSCP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemMediaPolicyDscp);
		rval = (u_char *) &StorageTmp->lldpXMedRemMediaPolicyDscp;
		break;
	case (u_char) LLDPXMEDREMMEDIAPOLICYUNKNOWN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemMediaPolicyUnknown);
		rval = (u_char *) &StorageTmp->lldpXMedRemMediaPolicyUnknown;
		break;
	case (u_char) LLDPXMEDREMMEDIAPOLICYTAGGED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemMediaPolicyTagged);
		rval = (u_char *) &StorageTmp->lldpXMedRemMediaPolicyTagged;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, struct lldpXMedRemInventoryTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, struct lldpXMedRemInventoryTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, struct lldpXMedRemInventoryTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemInventoryTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, struct lldpXMedRemInventoryTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemInventoryTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, struct lldpXMedRemInventoryTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, struct lldpXMedRemInventoryTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemInventoryTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemInventoryTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemInventoryTable_data *
refresh_lldpXMedRemInventoryTable_row(struct lldpXMedRemInventoryTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemInventoryTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemInventoryTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemInventoryTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemInventoryTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemInventoryTable(int force)
{
	if (!force && lldpXMedRemInventoryTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemInventoryTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemInventoryTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemInventoryTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemInventoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemInventoryTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemInventoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemInventoryTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemInventoryTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemInventoryTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMHARDWAREREV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemHardwareRevLen;
		rval = (u_char *) StorageTmp->lldpXMedRemHardwareRev;
		break;
	case (u_char) LLDPXMEDREMFIRMWAREREV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemFirmwareRevLen;
		rval = (u_char *) StorageTmp->lldpXMedRemFirmwareRev;
		break;
	case (u_char) LLDPXMEDREMSOFTWAREREV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemSoftwareRevLen;
		rval = (u_char *) StorageTmp->lldpXMedRemSoftwareRev;
		break;
	case (u_char) LLDPXMEDREMSERIALNUM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemSerialNumLen;
		rval = (u_char *) StorageTmp->lldpXMedRemSerialNum;
		break;
	case (u_char) LLDPXMEDREMMFGNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemMfgNameLen;
		rval = (u_char *) StorageTmp->lldpXMedRemMfgName;
		break;
	case (u_char) LLDPXMEDREMMODELNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemModelNameLen;
		rval = (u_char *) StorageTmp->lldpXMedRemModelName;
		break;
	case (u_char) LLDPXMEDREMASSETID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemAssetIDLen;
		rval = (u_char *) StorageTmp->lldpXMedRemAssetID;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, struct lldpXMedRemLocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, struct lldpXMedRemLocationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, struct lldpXMedRemLocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemLocationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, struct lldpXMedRemLocationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemLocationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, struct lldpXMedRemLocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, struct lldpXMedRemLocationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemLocationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemLocationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemLocationTable_data *
refresh_lldpXMedRemLocationTable_row(struct lldpXMedRemLocationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemLocationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemLocationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemLocationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemLocationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemLocationTable(int force)
{
	if (!force && lldpXMedRemLocationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemLocationTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemLocationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemLocationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemLocationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemLocationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemLocationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemLocationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemLocationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemLocationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMLOCATIONINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXMedRemLocationInfoLen;
		rval = (u_char *) StorageTmp->lldpXMedRemLocationInfo;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, struct lldpXMedRemXPoETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, struct lldpXMedRemXPoETable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, struct lldpXMedRemXPoETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemXPoETable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, struct lldpXMedRemXPoETable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemXPoETable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, struct lldpXMedRemXPoETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, struct lldpXMedRemXPoETable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemXPoETable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemXPoETable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemXPoETable_data *
refresh_lldpXMedRemXPoETable_row(struct lldpXMedRemXPoETable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemXPoETable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemXPoETable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemXPoETable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemXPoETable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemXPoETable(int force)
{
	if (!force && lldpXMedRemXPoETable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemXPoETable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemXPoETable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemXPoETable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemXPoETable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemXPoETable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemXPoETable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemXPoETable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemXPoETableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemXPoETable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMXPOEDEVICETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEDeviceType);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEDeviceType;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, struct lldpXMedRemXPoEPSETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, struct lldpXMedRemXPoEPSETable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, struct lldpXMedRemXPoEPSETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemXPoEPSETable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, struct lldpXMedRemXPoEPSETable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemXPoEPSETable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, struct lldpXMedRemXPoEPSETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, struct lldpXMedRemXPoEPSETable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemXPoEPSETable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemXPoEPSETable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemXPoEPSETable_data *
refresh_lldpXMedRemXPoEPSETable_row(struct lldpXMedRemXPoEPSETable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemXPoEPSETable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemXPoEPSETable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemXPoEPSETable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemXPoEPSETable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemXPoEPSETable(int force)
{
	if (!force && lldpXMedRemXPoEPSETable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemXPoEPSETable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemXPoEPSETable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemXPoEPSETable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemXPoEPSETable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemXPoEPSETable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemXPoEPSETable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemXPoEPSETable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemXPoEPSETableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemXPoEPSETable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMXPOEPSEPOWERAV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEPSEPowerAv);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEPSEPowerAv;
		break;
	case (u_char) LLDPXMEDREMXPOEPSEPOWERSOURCE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEPSEPowerSource);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEPSEPowerSource;
		break;
	case (u_char) LLDPXMEDREMXPOEPSEPOWERPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEPSEPowerPriority);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEPSEPowerPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, struct lldpXMedRemXPoEPDTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, struct lldpXMedRemXPoEPDTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, struct lldpXMedRemXPoEPDTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXMedRemXPoEPDTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, struct lldpXMedRemXPoEPDTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXMedRemXPoEPDTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, struct lldpXMedRemXPoEPDTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, struct lldpXMedRemXPoEPDTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXMedRemXPoEPDTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXMedRemXPoEPDTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXMedRemXPoEPDTable_data *
refresh_lldpXMedRemXPoEPDTable_row(struct lldpXMedRemXPoEPDTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXMedRemXPoEPDTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXMedRemXPoEPDTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXMedRemXPoEPDTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXMedRemXPoEPDTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXMedRemXPoEPDTable(int force)
{
	if (!force && lldpXMedRemXPoEPDTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXMedRemXPoEPDTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXMedRemXPoEPDTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXMedRemXPoEPDTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXMedMIB above.
 */
u_char *
var_lldpXMedRemXPoEPDTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXMedRemXPoEPDTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXMedMIB", "var_lldpXMedRemXPoEPDTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXMedRemXPoEPDTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXMedRemXPoEPDTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXMedRemXPoEPDTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXMEDREMXPOEPDPOWERREQ:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEPDPowerReq);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEPDPowerReq;
		break;
	case (u_char) LLDPXMEDREMXPOEPDPOWERSOURCE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEPDPowerSource);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEPDPowerSource;
		break;
	case (u_char) LLDPXMEDREMXPOEPDPOWERPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXMedRemXPoEPDPowerPriority);
		rval = (u_char *) &StorageTmp->lldpXMedRemXPoEPDPowerPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_lldpXMedPortConfigTLVsTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXMedPortConfigTLVsTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXMedPortConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXMedMIB", "write_lldpXMedPortConfigTLVsTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXMedPortConfigTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedPortConfigTLVsTxEnable not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedPortConfigTLVsTxEnable: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedPortConfigTLVsTxEnable: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) == NULL)
			if (StorageTmp->lldpXMedPortConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old = lldpXMedPortConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXMedPortConfigTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->lldpXMedPortConfigTLVsTxEnable);
		StorageTmp->lldpXMedPortConfigTLVsTxEnable = string;
		StorageTmp->lldpXMedPortConfigTLVsTxEnableLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXMedPortConfigTable_tsts == 0)
				if ((ret = check_lldpXMedPortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedPortConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXMedPortConfigTLVsTxEnable for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXMedPortConfigTable_sets == 0)
				if ((ret = update_lldpXMedPortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedPortConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) != NULL) {
			lldpXMedPortConfigTable_destroy(&StorageTmp->lldpXMedPortConfigTable_old);
			StorageTmp->lldpXMedPortConfigTable_rsvs = 0;
			StorageTmp->lldpXMedPortConfigTable_tsts = 0;
			StorageTmp->lldpXMedPortConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXMedPortConfigTable_sets == 0)
			revert_lldpXMedPortConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) == NULL)
			break;
		if (StorageOld->lldpXMedPortConfigTLVsTxEnable != NULL) {
			SNMP_FREE(StorageTmp->lldpXMedPortConfigTLVsTxEnable);
			StorageTmp->lldpXMedPortConfigTLVsTxEnable = StorageOld->lldpXMedPortConfigTLVsTxEnable;
			StorageTmp->lldpXMedPortConfigTLVsTxEnableLen = StorageOld->lldpXMedPortConfigTLVsTxEnableLen;
			StorageOld->lldpXMedPortConfigTLVsTxEnable = NULL;
			StorageOld->lldpXMedPortConfigTLVsTxEnableLen = 0;
		}
		if (--StorageTmp->lldpXMedPortConfigTable_rsvs == 0)
			lldpXMedPortConfigTable_destroy(&StorageTmp->lldpXMedPortConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXMedPortConfigNotifEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXMedPortConfigNotifEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXMedPortConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXMedMIB", "write_lldpXMedPortConfigNotifEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXMedPortConfigTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedPortConfigNotifEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedPortConfigNotifEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedPortConfigNotifEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) == NULL)
			if (StorageTmp->lldpXMedPortConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old = lldpXMedPortConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXMedPortConfigTable_rsvs++;
		StorageTmp->lldpXMedPortConfigNotifEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXMedPortConfigTable_tsts == 0)
				if ((ret = check_lldpXMedPortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedPortConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXMedPortConfigNotifEnable for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXMedPortConfigTable_sets == 0)
				if ((ret = update_lldpXMedPortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedPortConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) != NULL) {
			lldpXMedPortConfigTable_destroy(&StorageTmp->lldpXMedPortConfigTable_old);
			StorageTmp->lldpXMedPortConfigTable_rsvs = 0;
			StorageTmp->lldpXMedPortConfigTable_tsts = 0;
			StorageTmp->lldpXMedPortConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXMedPortConfigTable_sets == 0)
			revert_lldpXMedPortConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXMedPortConfigTable_old) == NULL)
			break;
		StorageTmp->lldpXMedPortConfigNotifEnable = StorageOld->lldpXMedPortConfigNotifEnable;
		if (--StorageTmp->lldpXMedPortConfigTable_rsvs == 0)
			lldpXMedPortConfigTable_destroy(&StorageTmp->lldpXMedPortConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXMedLocLocationInfo(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXMedLocLocationInfo(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXMedLocLocationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXMedMIB", "write_lldpXMedLocLocationInfo entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXMedLocLocationTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedLocLocationInfo not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..256 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 256))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedLocLocationInfo: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old) == NULL)
			if (StorageTmp->lldpXMedLocLocationTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old = lldpXMedLocLocationTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXMedLocLocationTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->lldpXMedLocLocationInfo);
		StorageTmp->lldpXMedLocLocationInfo = string;
		StorageTmp->lldpXMedLocLocationInfoLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXMedLocLocationTable_tsts == 0)
				if ((ret = check_lldpXMedLocLocationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedLocLocationTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXMedLocLocationInfo for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXMedLocLocationTable_sets == 0)
				if ((ret = update_lldpXMedLocLocationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedLocLocationTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old) != NULL) {
			lldpXMedLocLocationTable_destroy(&StorageTmp->lldpXMedLocLocationTable_old);
			StorageTmp->lldpXMedLocLocationTable_rsvs = 0;
			StorageTmp->lldpXMedLocLocationTable_tsts = 0;
			StorageTmp->lldpXMedLocLocationTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXMedLocLocationTable_sets == 0)
			revert_lldpXMedLocLocationTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXMedLocLocationTable_old) == NULL)
			break;
		if (StorageOld->lldpXMedLocLocationInfo != NULL) {
			SNMP_FREE(StorageTmp->lldpXMedLocLocationInfo);
			StorageTmp->lldpXMedLocLocationInfo = StorageOld->lldpXMedLocLocationInfo;
			StorageTmp->lldpXMedLocLocationInfoLen = StorageOld->lldpXMedLocLocationInfoLen;
			StorageOld->lldpXMedLocLocationInfo = NULL;
			StorageOld->lldpXMedLocLocationInfoLen = 0;
		}
		if (--StorageTmp->lldpXMedLocLocationTable_rsvs == 0)
			lldpXMedLocLocationTable_destroy(&StorageTmp->lldpXMedLocLocationTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXMedFastStartRepeatCount(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpXMedFastStartRepeatCount(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXMedMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXMedMIB", "write_lldpXMedFastStartRepeatCount entering action=%d...  \n", action));
	if ((StorageTmp = lldpXMedMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedFastStartRepeatCount not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedFastStartRepeatCount: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 3 */
		/* Note: ranges 1..10 */
		if ((1 > set_value || set_value > 10)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXMedFastStartRepeatCount: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpXMedMIB_old) == NULL)
			if (StorageTmp->lldpXMedMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXMedMIB_old = lldpXMedMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXMedMIB_rsvs++;
		StorageTmp->lldpXMedFastStartRepeatCount = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXMedMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpXMedMIB_tsts == 0)
				if ((ret = check_lldpXMedMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXMedFastStartRepeatCount for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpXMedMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXMedMIB_sets == 0)
				if ((ret = update_lldpXMedMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXMedMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXMedMIB_old) != NULL) {
			lldpXMedMIB_destroy(&StorageTmp->lldpXMedMIB_old);
			StorageTmp->lldpXMedMIB_rsvs = 0;
			StorageTmp->lldpXMedMIB_tsts = 0;
			StorageTmp->lldpXMedMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXMedMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXMedMIB_tsts == 0)
			revert_lldpXMedMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXMedMIB_old) == NULL)
			break;
		StorageTmp->lldpXMedFastStartRepeatCount = StorageOld->lldpXMedFastStartRepeatCount;
		if (--StorageTmp->lldpXMedMIB_rsvs == 0)
			lldpXMedMIB_destroy(&StorageTmp->lldpXMedMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_lldpXMedTopologyChangeDetected_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	DEBUGMSGTL(("lldpXMedMIB", "send_lldpXMedTopologyChangeDetected_v2trap: sending trap...  "));
	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = lldpXMedTopologyChangeDetected_oid;
	trap.val_len = sizeof(lldpXMedTopologyChangeDetected_oid);
	trap.index = 0;
	send_v2trap(&trap);
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/**
 * @fn void lldpXMedMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
lldpXMedMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (lldpXMedMIBold_signal_handler != NULL)
		(*lldpXMedMIBold_signal_handler) (sig);
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
}

/**
 * @fn void lldpXMedMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
lldpXMedMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("lldpXMedMIB", "lldpXMedMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("lldpXMedMIB", "done.\n"));
	return;
}
