/*****************************************************************************

 @(#) src/snmp/ieee8021tpmrmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021tpmrmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021TpmrMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021TpmrMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021TpmrMib_refresh = 1;
volatile int ieee8021TpmrPortTable_refresh = 1;
volatile int ieee8021TpmrPortStatsTable_refresh = 1;
volatile int ieee8021TpmrPortDiscardDetailsTable_refresh = 1;
volatile int ieee8021TpmrMspTable_refresh = 1;
volatile int ieee8021TpmrMspStatsTable_refresh = 1;

/*
 * ieee8021TpmrMib_variables_oid: object identifier for ieee8021TpmrMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021TpmrMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 14 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021TpmrCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 14, 2, 1, 1 };
oid ieee8021TpmrPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 14, 2, 2, 1 };
oid ieee8021TpmrPortStatsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 14, 2, 2, 2 };
oid ieee8021TpmrPortDiscardDetailsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 14, 2, 2, 3 };
oid ieee8021TpmrMspGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 14, 2, 2, 4 };
oid ieee8021TpmrMspStatsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 14, 2, 2, 5 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021TpmrMib_variables: tree for ieee8021TpmrMib
 * This variable defines function callbacks and type return information for the ieee8021TpmrMib mib section
 */
struct variable4 ieee8021TpmrMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021TPMRPORTMGMTADDR  1
	{(u_char) IEEE8021TPMRPORTMGMTADDR, ASN_INTEGER, RONLY, var_ieee8021TpmrPortTable, 4, {1, 1, 1, 2}},
#define   IEEE8021TPMRPORTMGMTADDRFORWARDING  2
	{(u_char) IEEE8021TPMRPORTMGMTADDRFORWARDING, ASN_INTEGER, RONLY, var_ieee8021TpmrPortTable, 4, {1, 1, 1, 3}},
#define   IEEE8021TPMRPORTSTATSRXFRAMES  3
	{(u_char) IEEE8021TPMRPORTSTATSRXFRAMES, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 1}},
#define   IEEE8021TPMRPORTSTATSRXOCTETS  4
	{(u_char) IEEE8021TPMRPORTSTATSRXOCTETS, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 2}},
#define   IEEE8021TPMRPORTSTATSFRAMESFORWARDED  5
	{(u_char) IEEE8021TPMRPORTSTATSFRAMESFORWARDED, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 3}},
#define   IEEE8021TPMRPORTSTATSFRAMESDISCARDED  6
	{(u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDED, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 4}},
#define   IEEE8021TPMRPORTSTATSFRAMESDISCARDEDQUEUEFULL  7
	{(u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDEDQUEUEFULL, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 5}},
#define   IEEE8021TPMRPORTSTATSFRAMESDISCARDEDLIFETIME  8
	{(u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDEDLIFETIME, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 6}},
#define   IEEE8021TPMRPORTSTATSFRAMESDISCARDEDERROR  9
	{(u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDEDERROR, ASN_COUNTER64, RONLY, var_ieee8021TpmrPortStatsTable, 4, {1, 2, 1, 7}},
#define   IEEE8021TPMRPORTDISCARDDETAILSSOURCE  10
	{(u_char) IEEE8021TPMRPORTDISCARDDETAILSSOURCE, ASN_OCTET_STR, RONLY, var_ieee8021TpmrPortDiscardDetailsTable, 4, {1, 3, 1, 2}},
#define   IEEE8021TPMRPORTDISCARDDETAILSREASON  11
	{(u_char) IEEE8021TPMRPORTDISCARDDETAILSREASON, ASN_INTEGER, RONLY, var_ieee8021TpmrPortDiscardDetailsTable, 4, {1, 3, 1, 3}},
#define   IEEE8021TPMRMSPLINKNOTIFY  12
	{(u_char) IEEE8021TPMRMSPLINKNOTIFY, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 1}},
#define   IEEE8021TPMRMSPLINKNOTIFYWAIT  13
	{(u_char) IEEE8021TPMRMSPLINKNOTIFYWAIT, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 2}},
#define   IEEE8021TPMRMSPLINKNOTIFYRETRY  14
	{(u_char) IEEE8021TPMRMSPLINKNOTIFYRETRY, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 3}},
#define   IEEE8021TPMRMSPMACNOTIFY  15
	{(u_char) IEEE8021TPMRMSPMACNOTIFY, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 4}},
#define   IEEE8021TPMRMSPMACNOTIFYTIME  16
	{(u_char) IEEE8021TPMRMSPMACNOTIFYTIME, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 5}},
#define   IEEE8021TPMRMSPMACRECOVERTIME  17
	{(u_char) IEEE8021TPMRMSPMACRECOVERTIME, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 6}},
#define   IEEE8021TPMRMSPSTORAGETYPE  18
	{(u_char) IEEE8021TPMRMSPSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021TpmrMspTable, 4, {1, 4, 1, 7}},
#define   IEEE8021TPMRMSPSTATSTXACKS  19
	{(u_char) IEEE8021TPMRMSPSTATSTXACKS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 1}},
#define   IEEE8021TPMRMSPSTATSTXADDNOTIFICATIONS  20
	{(u_char) IEEE8021TPMRMSPSTATSTXADDNOTIFICATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 2}},
#define   IEEE8021TPMRMSPSTATSTXADDCONFIRMATIONS  21
	{(u_char) IEEE8021TPMRMSPSTATSTXADDCONFIRMATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 3}},
#define   IEEE8021TPMRMSPSTATSTXLOSSNOTIFICATIONS  22
	{(u_char) IEEE8021TPMRMSPSTATSTXLOSSNOTIFICATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 4}},
#define   IEEE8021TPMRMSPSTATSTXLOSSCONFIRMATIONS  23
	{(u_char) IEEE8021TPMRMSPSTATSTXLOSSCONFIRMATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 5}},
#define   IEEE8021TPMRMSPSTATSRXACKS  24
	{(u_char) IEEE8021TPMRMSPSTATSRXACKS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 6}},
#define   IEEE8021TPMRMSPSTATSRXADDNOTIFICATIONS  25
	{(u_char) IEEE8021TPMRMSPSTATSRXADDNOTIFICATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 7}},
#define   IEEE8021TPMRMSPSTATSRXADDCONFIRMATIONS  26
	{(u_char) IEEE8021TPMRMSPSTATSRXADDCONFIRMATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 8}},
#define   IEEE8021TPMRMSPSTATSRXLOSSNOTIFICATIONS  27
	{(u_char) IEEE8021TPMRMSPSTATSRXLOSSNOTIFICATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 9}},
#define   IEEE8021TPMRMSPSTATSRXLOSSCONFIRMATIONS  28
	{(u_char) IEEE8021TPMRMSPSTATSRXLOSSCONFIRMATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 10}},
#define   IEEE8021TPMRMSPSTATSADDEVENTS  29
	{(u_char) IEEE8021TPMRMSPSTATSADDEVENTS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 11}},
#define   IEEE8021TPMRMSPSTATSLOSSEVENTS  30
	{(u_char) IEEE8021TPMRMSPSTATSLOSSEVENTS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 12}},
#define   IEEE8021TPMRMSPSTATSMACSTATUSNOTIFICATIONS  31
	{(u_char) IEEE8021TPMRMSPSTATSMACSTATUSNOTIFICATIONS, ASN_COUNTER, RONLY, var_ieee8021TpmrMspStatsTable, 4, {1, 5, 1, 13}},
};

/* (L = length of the oidsuffix) */
struct ieee8021TpmrMib_data *ieee8021TpmrMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021TpmrPortTableStorage = NULL;
struct header_complex_index *ieee8021TpmrPortStatsTableStorage = NULL;
struct header_complex_index *ieee8021TpmrPortDiscardDetailsTableStorage = NULL;
struct header_complex_index *ieee8021TpmrMspTableStorage = NULL;
struct header_complex_index *ieee8021TpmrMspStatsTableStorage = NULL;

void (*ieee8021TpmrMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021TpmrMib_loop_handler(int);
void ieee8021TpmrMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021TpmrMib(void)
 * @brief ieee8021TpmrMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021TpmrMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021TpmrMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021TpmrMib", "init_ieee8021TpmrMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021TpmrMib", ieee8021TpmrMib_variables, variable4, ieee8021TpmrMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021TpmrMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021TpmrMib", parse_ieee8021TpmrMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021TpmrPortTable", parse_ieee8021TpmrPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021TpmrPortStatsTable", parse_ieee8021TpmrPortStatsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021TpmrPortDiscardDetailsTable", parse_ieee8021TpmrPortDiscardDetailsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021TpmrMspTable", parse_ieee8021TpmrMspTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021TpmrMspStatsTable", parse_ieee8021TpmrMspStatsTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021TpmrMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021TpmrPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021TpmrPortStatsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021TpmrPortDiscardDetailsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021TpmrMspTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021TpmrMspStatsTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021TpmrMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021TpmrMib_fd_handler, (void *) 1);
	}
	ieee8021TpmrMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021TpmrMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021TpmrMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021TpmrMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021TpmrMib(void)
{
	DEBUGMSGTL(("ieee8021TpmrMib", "deinit_ieee8021TpmrMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021TpmrMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021TpmrMib_variables_oid, sizeof(ieee8021TpmrMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021TpmrMib");
	snmpd_unregister_config_handler("ieee8021TpmrPortTable");
	snmpd_unregister_config_handler("ieee8021TpmrPortStatsTable");
	snmpd_unregister_config_handler("ieee8021TpmrPortDiscardDetailsTable");
	snmpd_unregister_config_handler("ieee8021TpmrMspTable");
	snmpd_unregister_config_handler("ieee8021TpmrMspStatsTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

int
term_ieee8021TpmrMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021TpmrMib", "term_ieee8021TpmrMib: terminating...  "));
	deinit_ieee8021TpmrMib();
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021TpmrMib_data *ieee8021TpmrMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021TpmrMib.
 *
 * Creates a new ieee8021TpmrMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021TpmrMib.
 */
struct ieee8021TpmrMib_data *
ieee8021TpmrMib_create(void)
{
	struct ieee8021TpmrMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrMib_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021TpmrMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021TpmrMib_data *ieee8021TpmrMib_duplicate(struct ieee8021TpmrMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021TpmrMib_data *
ieee8021TpmrMib_duplicate(struct ieee8021TpmrMib_data *thedata)
{
	struct ieee8021TpmrMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrMib_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021TpmrMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021TpmrMib_destroy(struct ieee8021TpmrMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021TpmrMib.
 * @brief delete a scalars structure from ieee8021TpmrMib.
 *
 * Frees scalars that were previously removed from ieee8021TpmrMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021TpmrMib_destroy(struct ieee8021TpmrMib_data **thedata)
{
	struct ieee8021TpmrMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrMib_add(struct ieee8021TpmrMib_data *thedata)
 * @param thedata the structure representing ieee8021TpmrMib scalars.
 * @brief adds node to the ieee8021TpmrMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021TpmrMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021TpmrMib_add(struct ieee8021TpmrMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMib_add: adding data...  "));
	if (thedata)
		ieee8021TpmrMibStorage = thedata;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021TpmrMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021TpmrMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021TpmrMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021TpmrMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021TpmrMib_data *StorageTmp = ieee8021TpmrMib_create();

	DEBUGMSGTL(("ieee8021TpmrMib", "parse_ieee8021TpmrMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021TpmrMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/*
 * store_ieee8021TpmrMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021TpmrMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021TpmrMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021TpmrMib", "store_ieee8021TpmrMib: storing data...  "));
	refresh_ieee8021TpmrMib(1);
	if ((StorageTmp = ieee8021TpmrMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021TpmrMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021TpmrMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021TpmrMib(struct ieee8021TpmrMib_data *StorageTmp, struct ieee8021TpmrMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021TpmrMib(struct ieee8021TpmrMib_data *StorageTmp, struct ieee8021TpmrMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021TpmrMib(struct ieee8021TpmrMib_data *StorageTmp, struct ieee8021TpmrMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021TpmrMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021TpmrMib(struct ieee8021TpmrMib_data *StorageTmp, struct ieee8021TpmrMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021TpmrMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021TpmrMib(struct 
 * @fn void revert_ieee8021TpmrMib(struct ieee8021TpmrMib_data *StorageTmp, struct ieee8021TpmrMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021TpmrMib(struct ieee8021TpmrMib_data *StorageTmp, struct ieee8021TpmrMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021TpmrMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021TpmrMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021TpmrMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021TpmrMib(int force)
{
	if (ieee8021TpmrMibStorage == NULL) {
		struct ieee8021TpmrMib_data *StorageNew;

		if ((StorageNew = ieee8021TpmrMib_create()) == NULL)
			return;
		ieee8021TpmrMibStorage = StorageNew;
		ieee8021TpmrMib_refresh = 1;
	}
	if (!force && ieee8021TpmrMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021TpmrMib", "refresh_ieee8021TpmrMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021TpmrMib_refresh = 0;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021TpmrMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021TpmrMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021TpmrMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021TpmrMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021TpmrMib", "var_ieee8021TpmrMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021TpmrMib(0);
	if ((StorageTmp = ieee8021TpmrMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021TpmrMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021TpmrMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021TpmrMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021TpmrPortTable_data *ieee8021TpmrPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021TpmrPortTable table.
 *
 * Creates a new ieee8021TpmrPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021TpmrPortTable_data *
ieee8021TpmrPortTable_create(void)
{
	struct ieee8021TpmrPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrPortTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021TpmrPortMgmtAddr = 0;
		StorageNew->ieee8021TpmrPortMgmtAddrForwarding = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021TpmrPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021TpmrPortTable_data *ieee8021TpmrPortTable_duplicate(struct ieee8021TpmrPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021TpmrPortTable_data *
ieee8021TpmrPortTable_duplicate(struct ieee8021TpmrPortTable_data *thedata)
{
	struct ieee8021TpmrPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrPortTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021TpmrPortTable_id = thedata->ieee8021TpmrPortTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021TpmrPortNumber = thedata->ieee8021TpmrPortNumber;
		StorageNew->ieee8021TpmrPortMgmtAddr = thedata->ieee8021TpmrPortMgmtAddr;
		StorageNew->ieee8021TpmrPortMgmtAddrForwarding = thedata->ieee8021TpmrPortMgmtAddrForwarding;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021TpmrPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021TpmrPortTable_destroy(struct ieee8021TpmrPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021TpmrPortTable_destroy(struct ieee8021TpmrPortTable_data **thedata)
{
	struct ieee8021TpmrPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrPortTable_add(struct ieee8021TpmrPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021TpmrPortTable table data set.
 *
 * Adds a table row structure to the ieee8021TpmrPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021TpmrPortTable_add(struct ieee8021TpmrPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021TpmrPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021TpmrPortNumber, sizeof(thedata->ieee8021TpmrPortNumber));
		header_complex_add_data(&ieee8021TpmrPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrPortTable_del(struct ieee8021TpmrPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021TpmrPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021TpmrPortTable_del(struct ieee8021TpmrPortTable_data *thedata)
{
	struct ieee8021TpmrPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021TpmrPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021TpmrPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021TpmrPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021TpmrPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021TpmrPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021TpmrPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021TpmrPortTable_data *StorageTmp = ieee8021TpmrPortTable_create();

	DEBUGMSGTL(("ieee8021TpmrMib", "parse_ieee8021TpmrPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrPortMgmtAddr, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrPortMgmtAddrForwarding, &tmpsize);
	ieee8021TpmrPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/*
 * store_ieee8021TpmrPortTable(): store configuraiton file for ieee8021TpmrPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021TpmrPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021TpmrPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021TpmrMib", "store_ieee8021TpmrPortTable: storing data...  "));
	refresh_ieee8021TpmrPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021TpmrPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021TpmrPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021TpmrPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrPortMgmtAddr, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrPortMgmtAddrForwarding, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021TpmrPortStatsTable_data *ieee8021TpmrPortStatsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021TpmrPortStatsTable table.
 *
 * Creates a new ieee8021TpmrPortStatsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021TpmrPortStatsTable_data *
ieee8021TpmrPortStatsTable_create(void)
{
	struct ieee8021TpmrPortStatsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrPortStatsTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortStatsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021TpmrPortNumber = 0;
		StorageNew->ieee8021TpmrPortStatsRxFrames = (struct counter64) {
		0, 0};
		StorageNew->ieee8021TpmrPortStatsRxOctets = (struct counter64) {
		0, 0};
		StorageNew->ieee8021TpmrPortStatsFramesForwarded = (struct counter64) {
		0, 0};
		StorageNew->ieee8021TpmrPortStatsFramesDiscarded = (struct counter64) {
		0, 0};
		StorageNew->ieee8021TpmrPortStatsFramesDiscardedQueueFull = (struct counter64) {
		0, 0};
		StorageNew->ieee8021TpmrPortStatsFramesDiscardedLifetime = (struct counter64) {
		0, 0};
		StorageNew->ieee8021TpmrPortStatsFramesDiscardedError = (struct counter64) {
		0, 0};
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021TpmrPortStatsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021TpmrPortStatsTable_data *ieee8021TpmrPortStatsTable_duplicate(struct ieee8021TpmrPortStatsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021TpmrPortStatsTable_data *
ieee8021TpmrPortStatsTable_duplicate(struct ieee8021TpmrPortStatsTable_data *thedata)
{
	struct ieee8021TpmrPortStatsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrPortStatsTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortStatsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021TpmrPortStatsTable_id = thedata->ieee8021TpmrPortStatsTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021TpmrPortNumber = thedata->ieee8021TpmrPortNumber;
		StorageNew->ieee8021TpmrPortStatsRxFrames = thedata->ieee8021TpmrPortStatsRxFrames;
		StorageNew->ieee8021TpmrPortStatsRxOctets = thedata->ieee8021TpmrPortStatsRxOctets;
		StorageNew->ieee8021TpmrPortStatsFramesForwarded = thedata->ieee8021TpmrPortStatsFramesForwarded;
		StorageNew->ieee8021TpmrPortStatsFramesDiscarded = thedata->ieee8021TpmrPortStatsFramesDiscarded;
		StorageNew->ieee8021TpmrPortStatsFramesDiscardedQueueFull = thedata->ieee8021TpmrPortStatsFramesDiscardedQueueFull;
		StorageNew->ieee8021TpmrPortStatsFramesDiscardedLifetime = thedata->ieee8021TpmrPortStatsFramesDiscardedLifetime;
		StorageNew->ieee8021TpmrPortStatsFramesDiscardedError = thedata->ieee8021TpmrPortStatsFramesDiscardedError;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021TpmrPortStatsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021TpmrPortStatsTable_destroy(struct ieee8021TpmrPortStatsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021TpmrPortStatsTable_destroy(struct ieee8021TpmrPortStatsTable_data **thedata)
{
	struct ieee8021TpmrPortStatsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortStatsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrPortStatsTable_add(struct ieee8021TpmrPortStatsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021TpmrPortStatsTable table data set.
 *
 * Adds a table row structure to the ieee8021TpmrPortStatsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021TpmrPortStatsTable_add(struct ieee8021TpmrPortStatsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortStatsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021TpmrPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021TpmrPortNumber, sizeof(thedata->ieee8021TpmrPortNumber));
		header_complex_add_data(&ieee8021TpmrPortStatsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrPortStatsTable_del(struct ieee8021TpmrPortStatsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021TpmrPortStatsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021TpmrPortStatsTable_del(struct ieee8021TpmrPortStatsTable_data *thedata)
{
	struct ieee8021TpmrPortStatsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortStatsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021TpmrPortStatsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021TpmrPortStatsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021TpmrPortStatsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021TpmrPortStatsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021TpmrPortStatsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021TpmrPortStatsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021TpmrPortStatsTable_data *StorageTmp = ieee8021TpmrPortStatsTable_create();

	DEBUGMSGTL(("ieee8021TpmrMib", "parse_ieee8021TpmrPortStatsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsRxFrames, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsRxOctets, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsFramesForwarded, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsFramesDiscarded, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedQueueFull, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedLifetime, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedError, &tmpsize);
	ieee8021TpmrPortStatsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/*
 * store_ieee8021TpmrPortStatsTable(): store configuraiton file for ieee8021TpmrPortStatsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021TpmrPortStatsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021TpmrPortStatsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021TpmrMib", "store_ieee8021TpmrPortStatsTable: storing data...  "));
	refresh_ieee8021TpmrPortStatsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021TpmrPortStatsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021TpmrPortStatsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021TpmrPortStatsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsRxFrames, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsRxOctets, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsFramesForwarded, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsFramesDiscarded, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedQueueFull, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedLifetime, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021TpmrPortDiscardDetailsTable_data *ieee8021TpmrPortDiscardDetailsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021TpmrPortDiscardDetailsTable table.
 *
 * Creates a new ieee8021TpmrPortDiscardDetailsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021TpmrPortDiscardDetailsTable_data *
ieee8021TpmrPortDiscardDetailsTable_create(void)
{
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrPortDiscardDetailsTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortDiscardDetailsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021TpmrPortNumber = 0;
		if ((StorageNew->ieee8021TpmrPortDiscardDetailsSource = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021TpmrPortDiscardDetailsSourceLen = 0;
		StorageNew->ieee8021TpmrPortDiscardDetailsSource[StorageNew->ieee8021TpmrPortDiscardDetailsSourceLen] = 0;
		StorageNew->ieee8021TpmrPortDiscardDetailsReason = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021TpmrPortDiscardDetailsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021TpmrPortDiscardDetailsTable_data *ieee8021TpmrPortDiscardDetailsTable_duplicate(struct ieee8021TpmrPortDiscardDetailsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021TpmrPortDiscardDetailsTable_data *
ieee8021TpmrPortDiscardDetailsTable_duplicate(struct ieee8021TpmrPortDiscardDetailsTable_data *thedata)
{
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrPortDiscardDetailsTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortDiscardDetailsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021TpmrPortDiscardDetailsTable_id = thedata->ieee8021TpmrPortDiscardDetailsTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021TpmrPortNumber = thedata->ieee8021TpmrPortNumber;
		StorageNew->ieee8021TpmrPortDiscardDetailsIndex = thedata->ieee8021TpmrPortDiscardDetailsIndex;
		if (!(StorageNew->ieee8021TpmrPortDiscardDetailsSource = malloc(thedata->ieee8021TpmrPortDiscardDetailsSourceLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021TpmrPortDiscardDetailsSource, thedata->ieee8021TpmrPortDiscardDetailsSource, thedata->ieee8021TpmrPortDiscardDetailsSourceLen);
		StorageNew->ieee8021TpmrPortDiscardDetailsSourceLen = thedata->ieee8021TpmrPortDiscardDetailsSourceLen;
		StorageNew->ieee8021TpmrPortDiscardDetailsSource[StorageNew->ieee8021TpmrPortDiscardDetailsSourceLen] = 0;
		StorageNew->ieee8021TpmrPortDiscardDetailsReason = thedata->ieee8021TpmrPortDiscardDetailsReason;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021TpmrPortDiscardDetailsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021TpmrPortDiscardDetailsTable_destroy(struct ieee8021TpmrPortDiscardDetailsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021TpmrPortDiscardDetailsTable_destroy(struct ieee8021TpmrPortDiscardDetailsTable_data **thedata)
{
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortDiscardDetailsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021TpmrPortDiscardDetailsSource);
		StorageDel->ieee8021TpmrPortDiscardDetailsSourceLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrPortDiscardDetailsTable_add(struct ieee8021TpmrPortDiscardDetailsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021TpmrPortDiscardDetailsTable table data set.
 *
 * Adds a table row structure to the ieee8021TpmrPortDiscardDetailsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021TpmrPortDiscardDetailsTable_add(struct ieee8021TpmrPortDiscardDetailsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortDiscardDetailsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021TpmrPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021TpmrPortNumber, sizeof(thedata->ieee8021TpmrPortNumber));
		/* ieee8021TpmrPortDiscardDetailsIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021TpmrPortDiscardDetailsIndex, sizeof(thedata->ieee8021TpmrPortDiscardDetailsIndex));
		header_complex_add_data(&ieee8021TpmrPortDiscardDetailsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrPortDiscardDetailsTable_del(struct ieee8021TpmrPortDiscardDetailsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021TpmrPortDiscardDetailsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021TpmrPortDiscardDetailsTable_del(struct ieee8021TpmrPortDiscardDetailsTable_data *thedata)
{
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrPortDiscardDetailsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021TpmrPortDiscardDetailsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021TpmrPortDiscardDetailsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021TpmrPortDiscardDetailsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021TpmrPortDiscardDetailsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021TpmrPortDiscardDetailsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021TpmrPortDiscardDetailsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp = ieee8021TpmrPortDiscardDetailsTable_create();

	DEBUGMSGTL(("ieee8021TpmrMib", "parse_ieee8021TpmrPortDiscardDetailsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021TpmrPortDiscardDetailsIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021TpmrPortDiscardDetailsSource);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021TpmrPortDiscardDetailsSource, &StorageTmp->ieee8021TpmrPortDiscardDetailsSourceLen);
	if (StorageTmp->ieee8021TpmrPortDiscardDetailsSource == NULL) {
		config_perror("invalid specification for ieee8021TpmrPortDiscardDetailsSource");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrPortDiscardDetailsReason, &tmpsize);
	ieee8021TpmrPortDiscardDetailsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/*
 * store_ieee8021TpmrPortDiscardDetailsTable(): store configuraiton file for ieee8021TpmrPortDiscardDetailsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021TpmrPortDiscardDetailsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021TpmrMib", "store_ieee8021TpmrPortDiscardDetailsTable: storing data...  "));
	refresh_ieee8021TpmrPortDiscardDetailsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021TpmrPortDiscardDetailsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021TpmrPortDiscardDetailsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021TpmrPortDiscardDetailsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021TpmrPortDiscardDetailsIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021TpmrPortDiscardDetailsSource, &StorageTmp->ieee8021TpmrPortDiscardDetailsSourceLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrPortDiscardDetailsReason, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021TpmrMspTable_data *ieee8021TpmrMspTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021TpmrMspTable table.
 *
 * Creates a new ieee8021TpmrMspTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021TpmrMspTable_data *
ieee8021TpmrMspTable_create(void)
{
	struct ieee8021TpmrMspTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrMspTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021TpmrPortNumber = 0;
		StorageNew->ieee8021TpmrMspLinkNotify = TV_TRUE;
		StorageNew->ieee8021TpmrMspLinkNotifyWait = 40;
		StorageNew->ieee8021TpmrMspLinkNotifyRetry = 100;
		StorageNew->ieee8021TpmrMspMacNotify = TV_TRUE;
		StorageNew->ieee8021TpmrMspMacNotifyTime = 20;
		StorageNew->ieee8021TpmrMspMacRecoverTime = 10;
		StorageNew->ieee8021TpmrMspStorageType = ST_NONVOLATILE;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021TpmrMspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021TpmrMspTable_data *ieee8021TpmrMspTable_duplicate(struct ieee8021TpmrMspTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021TpmrMspTable_data *
ieee8021TpmrMspTable_duplicate(struct ieee8021TpmrMspTable_data *thedata)
{
	struct ieee8021TpmrMspTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrMspTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021TpmrMspTable_id = thedata->ieee8021TpmrMspTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021TpmrPortNumber = thedata->ieee8021TpmrPortNumber;
		StorageNew->ieee8021TpmrMspLinkNotify = thedata->ieee8021TpmrMspLinkNotify;
		StorageNew->ieee8021TpmrMspLinkNotifyWait = thedata->ieee8021TpmrMspLinkNotifyWait;
		StorageNew->ieee8021TpmrMspLinkNotifyRetry = thedata->ieee8021TpmrMspLinkNotifyRetry;
		StorageNew->ieee8021TpmrMspMacNotify = thedata->ieee8021TpmrMspMacNotify;
		StorageNew->ieee8021TpmrMspMacNotifyTime = thedata->ieee8021TpmrMspMacNotifyTime;
		StorageNew->ieee8021TpmrMspMacRecoverTime = thedata->ieee8021TpmrMspMacRecoverTime;
		StorageNew->ieee8021TpmrMspStorageType = thedata->ieee8021TpmrMspStorageType;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021TpmrMspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021TpmrMspTable_destroy(struct ieee8021TpmrMspTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021TpmrMspTable_destroy(struct ieee8021TpmrMspTable_data **thedata)
{
	struct ieee8021TpmrMspTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrMspTable_add(struct ieee8021TpmrMspTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021TpmrMspTable table data set.
 *
 * Adds a table row structure to the ieee8021TpmrMspTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021TpmrMspTable_add(struct ieee8021TpmrMspTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021TpmrPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021TpmrPortNumber, sizeof(thedata->ieee8021TpmrPortNumber));
		header_complex_add_data(&ieee8021TpmrMspTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrMspTable_del(struct ieee8021TpmrMspTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021TpmrMspTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021TpmrMspTable_del(struct ieee8021TpmrMspTable_data *thedata)
{
	struct ieee8021TpmrMspTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021TpmrMspTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021TpmrMspTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021TpmrMspTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021TpmrMspTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021TpmrMspTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021TpmrMspTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021TpmrMspTable_data *StorageTmp = ieee8021TpmrMspTable_create();

	DEBUGMSGTL(("ieee8021TpmrMib", "parse_ieee8021TpmrMspTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspLinkNotify, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspLinkNotifyWait, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspLinkNotifyRetry, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspMacNotify, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspMacNotifyTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspMacRecoverTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021TpmrMspStorageType, &tmpsize);
	ieee8021TpmrMspTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/*
 * store_ieee8021TpmrMspTable(): store configuraiton file for ieee8021TpmrMspTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021TpmrMspTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021TpmrMspTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021TpmrMib", "store_ieee8021TpmrMspTable: storing data...  "));
	refresh_ieee8021TpmrMspTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021TpmrMspTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021TpmrMspTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021TpmrMspStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021TpmrMspTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspLinkNotify, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspLinkNotifyWait, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspLinkNotifyRetry, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspMacNotify, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspMacNotifyTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspMacRecoverTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021TpmrMspStorageType, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021TpmrMspStatsTable_data *ieee8021TpmrMspStatsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021TpmrMspStatsTable table.
 *
 * Creates a new ieee8021TpmrMspStatsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021TpmrMspStatsTable_data *
ieee8021TpmrMspStatsTable_create(void)
{
	struct ieee8021TpmrMspStatsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrMspStatsTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspStatsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021TpmrPortNumber = 0;
		StorageNew->ieee8021TpmrMspStatsTxAcks = 0;
		StorageNew->ieee8021TpmrMspStatsTxAddNotifications = 0;
		StorageNew->ieee8021TpmrMspStatsTxAddConfirmations = 0;
		StorageNew->ieee8021TpmrMspStatsTxLossNotifications = 0;
		StorageNew->ieee8021TpmrMspStatsTxLossConfirmations = 0;
		StorageNew->ieee8021TpmrMspStatsRxAcks = 0;
		StorageNew->ieee8021TpmrMspStatsRxAddNotifications = 0;
		StorageNew->ieee8021TpmrMspStatsRxAddConfirmations = 0;
		StorageNew->ieee8021TpmrMspStatsRxLossNotifications = 0;
		StorageNew->ieee8021TpmrMspStatsRxLossConfirmations = 0;
		StorageNew->ieee8021TpmrMspStatsAddEvents = 0;
		StorageNew->ieee8021TpmrMspStatsLossEvents = 0;
		StorageNew->ieee8021TpmrMspStatsMacStatusNotifications = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021TpmrMspStatsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021TpmrMspStatsTable_data *ieee8021TpmrMspStatsTable_duplicate(struct ieee8021TpmrMspStatsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021TpmrMspStatsTable_data *
ieee8021TpmrMspStatsTable_duplicate(struct ieee8021TpmrMspStatsTable_data *thedata)
{
	struct ieee8021TpmrMspStatsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021TpmrMspStatsTable_data);

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspStatsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021TpmrMspStatsTable_id = thedata->ieee8021TpmrMspStatsTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021TpmrPortNumber = thedata->ieee8021TpmrPortNumber;
		StorageNew->ieee8021TpmrMspStatsTxAcks = thedata->ieee8021TpmrMspStatsTxAcks;
		StorageNew->ieee8021TpmrMspStatsTxAddNotifications = thedata->ieee8021TpmrMspStatsTxAddNotifications;
		StorageNew->ieee8021TpmrMspStatsTxAddConfirmations = thedata->ieee8021TpmrMspStatsTxAddConfirmations;
		StorageNew->ieee8021TpmrMspStatsTxLossNotifications = thedata->ieee8021TpmrMspStatsTxLossNotifications;
		StorageNew->ieee8021TpmrMspStatsTxLossConfirmations = thedata->ieee8021TpmrMspStatsTxLossConfirmations;
		StorageNew->ieee8021TpmrMspStatsRxAcks = thedata->ieee8021TpmrMspStatsRxAcks;
		StorageNew->ieee8021TpmrMspStatsRxAddNotifications = thedata->ieee8021TpmrMspStatsRxAddNotifications;
		StorageNew->ieee8021TpmrMspStatsRxAddConfirmations = thedata->ieee8021TpmrMspStatsRxAddConfirmations;
		StorageNew->ieee8021TpmrMspStatsRxLossNotifications = thedata->ieee8021TpmrMspStatsRxLossNotifications;
		StorageNew->ieee8021TpmrMspStatsRxLossConfirmations = thedata->ieee8021TpmrMspStatsRxLossConfirmations;
		StorageNew->ieee8021TpmrMspStatsAddEvents = thedata->ieee8021TpmrMspStatsAddEvents;
		StorageNew->ieee8021TpmrMspStatsLossEvents = thedata->ieee8021TpmrMspStatsLossEvents;
		StorageNew->ieee8021TpmrMspStatsMacStatusNotifications = thedata->ieee8021TpmrMspStatsMacStatusNotifications;
	}
      done:
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021TpmrMspStatsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021TpmrMspStatsTable_destroy(struct ieee8021TpmrMspStatsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021TpmrMspStatsTable_destroy(struct ieee8021TpmrMspStatsTable_data **thedata)
{
	struct ieee8021TpmrMspStatsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspStatsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrMspStatsTable_add(struct ieee8021TpmrMspStatsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021TpmrMspStatsTable table data set.
 *
 * Adds a table row structure to the ieee8021TpmrMspStatsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021TpmrMspStatsTable_add(struct ieee8021TpmrMspStatsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspStatsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021TpmrPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021TpmrPortNumber, sizeof(thedata->ieee8021TpmrPortNumber));
		header_complex_add_data(&ieee8021TpmrMspStatsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021TpmrMspStatsTable_del(struct ieee8021TpmrMspStatsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021TpmrMspStatsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021TpmrMspStatsTable_del(struct ieee8021TpmrMspStatsTable_data *thedata)
{
	struct ieee8021TpmrMspStatsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMspStatsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021TpmrMspStatsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021TpmrMspStatsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021TpmrMspStatsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021TpmrMspStatsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021TpmrMspStatsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021TpmrMspStatsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021TpmrMspStatsTable_data *StorageTmp = ieee8021TpmrMspStatsTable_create();

	DEBUGMSGTL(("ieee8021TpmrMib", "parse_ieee8021TpmrMspStatsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsTxAcks, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsTxAddNotifications, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsTxAddConfirmations, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsTxLossNotifications, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsTxLossConfirmations, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsRxAcks, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsRxAddNotifications, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsRxAddConfirmations, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsRxLossNotifications, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsRxLossConfirmations, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsAddEvents, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsLossEvents, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->ieee8021TpmrMspStatsMacStatusNotifications, &tmpsize);
	ieee8021TpmrMspStatsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/*
 * store_ieee8021TpmrMspStatsTable(): store configuraiton file for ieee8021TpmrMspStatsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021TpmrMspStatsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021TpmrMspStatsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021TpmrMib", "store_ieee8021TpmrMspStatsTable: storing data...  "));
	refresh_ieee8021TpmrMspStatsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021TpmrMspStatsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021TpmrMspStatsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021TpmrMspStatsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021TpmrPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsTxAcks, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsTxAddNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsTxAddConfirmations, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsTxLossNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsTxLossConfirmations, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsRxAcks, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsRxAddNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsRxAddConfirmations, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsRxLossNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsRxLossConfirmations, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsAddEvents, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsLossEvents, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->ieee8021TpmrMspStatsMacStatusNotifications, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, struct ieee8021TpmrPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, struct ieee8021TpmrPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, struct ieee8021TpmrPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021TpmrPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, struct ieee8021TpmrPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021TpmrPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, struct ieee8021TpmrPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, struct ieee8021TpmrPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021TpmrPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021TpmrPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021TpmrPortTable_data *
refresh_ieee8021TpmrPortTable_row(struct ieee8021TpmrPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021TpmrPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021TpmrPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021TpmrPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021TpmrPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021TpmrPortTable(int force)
{
	if (!force && ieee8021TpmrPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021TpmrPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021TpmrPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021TpmrPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021TpmrMib above.
 */
u_char *
var_ieee8021TpmrPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021TpmrPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021TpmrMib", "var_ieee8021TpmrPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021TpmrPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021TpmrPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021TpmrPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021TPMRPORTMGMTADDR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortMgmtAddr);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortMgmtAddr;
		break;
	case (u_char) IEEE8021TPMRPORTMGMTADDRFORWARDING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortMgmtAddrForwarding);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortMgmtAddrForwarding;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, struct ieee8021TpmrPortStatsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, struct ieee8021TpmrPortStatsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, struct ieee8021TpmrPortStatsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021TpmrPortStatsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, struct ieee8021TpmrPortStatsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021TpmrPortStatsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, struct ieee8021TpmrPortStatsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, struct ieee8021TpmrPortStatsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021TpmrPortStatsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021TpmrPortStatsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021TpmrPortStatsTable_data *
refresh_ieee8021TpmrPortStatsTable_row(struct ieee8021TpmrPortStatsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021TpmrPortStatsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021TpmrPortStatsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021TpmrPortStatsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021TpmrPortStatsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021TpmrPortStatsTable(int force)
{
	if (!force && ieee8021TpmrPortStatsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021TpmrPortStatsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021TpmrPortStatsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021TpmrPortStatsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021TpmrMib above.
 */
u_char *
var_ieee8021TpmrPortStatsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021TpmrPortStatsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021TpmrMib", "var_ieee8021TpmrPortStatsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021TpmrPortStatsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021TpmrPortStatsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021TpmrPortStatsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021TPMRPORTSTATSRXFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsRxFrames);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsRxFrames;
		break;
	case (u_char) IEEE8021TPMRPORTSTATSRXOCTETS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsRxOctets);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsRxOctets;
		break;
	case (u_char) IEEE8021TPMRPORTSTATSFRAMESFORWARDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsFramesForwarded);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsFramesForwarded;
		break;
	case (u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsFramesDiscarded);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsFramesDiscarded;
		break;
	case (u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDEDQUEUEFULL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsFramesDiscardedQueueFull);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedQueueFull;
		break;
	case (u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDEDLIFETIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsFramesDiscardedLifetime);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedLifetime;
		break;
	case (u_char) IEEE8021TPMRPORTSTATSFRAMESDISCARDEDERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortStatsFramesDiscardedError);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortStatsFramesDiscardedError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, struct ieee8021TpmrPortDiscardDetailsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, struct ieee8021TpmrPortDiscardDetailsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, struct ieee8021TpmrPortDiscardDetailsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021TpmrPortDiscardDetailsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, struct ieee8021TpmrPortDiscardDetailsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021TpmrPortDiscardDetailsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, struct ieee8021TpmrPortDiscardDetailsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, struct ieee8021TpmrPortDiscardDetailsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021TpmrPortDiscardDetailsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021TpmrPortDiscardDetailsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021TpmrPortDiscardDetailsTable_data *
refresh_ieee8021TpmrPortDiscardDetailsTable_row(struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021TpmrPortDiscardDetailsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021TpmrPortDiscardDetailsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021TpmrPortDiscardDetailsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021TpmrPortDiscardDetailsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021TpmrPortDiscardDetailsTable(int force)
{
	if (!force && ieee8021TpmrPortDiscardDetailsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021TpmrPortDiscardDetailsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021TpmrPortDiscardDetailsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021TpmrPortDiscardDetailsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021TpmrMib above.
 */
u_char *
var_ieee8021TpmrPortDiscardDetailsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021TpmrPortDiscardDetailsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021TpmrMib", "var_ieee8021TpmrPortDiscardDetailsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021TpmrPortDiscardDetailsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021TpmrPortDiscardDetailsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021TpmrPortDiscardDetailsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021TPMRPORTDISCARDDETAILSSOURCE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021TpmrPortDiscardDetailsSourceLen;
		rval = (u_char *) StorageTmp->ieee8021TpmrPortDiscardDetailsSource;
		break;
	case (u_char) IEEE8021TPMRPORTDISCARDDETAILSREASON:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrPortDiscardDetailsReason);
		rval = (u_char *) &StorageTmp->ieee8021TpmrPortDiscardDetailsReason;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, struct ieee8021TpmrMspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, struct ieee8021TpmrMspTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, struct ieee8021TpmrMspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021TpmrMspTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, struct ieee8021TpmrMspTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021TpmrMspTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, struct ieee8021TpmrMspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, struct ieee8021TpmrMspTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021TpmrMspTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021TpmrMspTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021TpmrMspTable_data *
refresh_ieee8021TpmrMspTable_row(struct ieee8021TpmrMspTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021TpmrMspTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021TpmrMspTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021TpmrMspTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021TpmrMspTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021TpmrMspTable(int force)
{
	if (!force && ieee8021TpmrMspTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021TpmrMspTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021TpmrMspTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021TpmrMspTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021TpmrMib above.
 */
u_char *
var_ieee8021TpmrMspTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021TpmrMib", "var_ieee8021TpmrMspTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021TpmrMspTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021TpmrMspTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021TpmrMspTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021TPMRMSPLINKNOTIFY:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspLinkNotify;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspLinkNotify);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspLinkNotify;
		break;
	case (u_char) IEEE8021TPMRMSPLINKNOTIFYWAIT:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspLinkNotifyWait;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspLinkNotifyWait);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspLinkNotifyWait;
		break;
	case (u_char) IEEE8021TPMRMSPLINKNOTIFYRETRY:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspLinkNotifyRetry;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspLinkNotifyRetry);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspLinkNotifyRetry;
		break;
	case (u_char) IEEE8021TPMRMSPMACNOTIFY:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspMacNotify;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspMacNotify);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspMacNotify;
		break;
	case (u_char) IEEE8021TPMRMSPMACNOTIFYTIME:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspMacNotifyTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspMacNotifyTime);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspMacNotifyTime;
		break;
	case (u_char) IEEE8021TPMRMSPMACRECOVERTIME:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspMacRecoverTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspMacRecoverTime);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspMacRecoverTime;
		break;
	case (u_char) IEEE8021TPMRMSPSTORAGETYPE:	/* ReadWrite */
		*write_method = write_ieee8021TpmrMspStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStorageType);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStorageType;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, struct ieee8021TpmrMspStatsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, struct ieee8021TpmrMspStatsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, struct ieee8021TpmrMspStatsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021TpmrMspStatsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, struct ieee8021TpmrMspStatsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021TpmrMspStatsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, struct ieee8021TpmrMspStatsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, struct ieee8021TpmrMspStatsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021TpmrMspStatsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021TpmrMspStatsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021TpmrMspStatsTable_data *
refresh_ieee8021TpmrMspStatsTable_row(struct ieee8021TpmrMspStatsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021TpmrMspStatsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021TpmrMspStatsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021TpmrMspStatsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021TpmrMspStatsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021TpmrMspStatsTable(int force)
{
	if (!force && ieee8021TpmrMspStatsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021TpmrMspStatsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021TpmrMspStatsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021TpmrMspStatsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021TpmrMib above.
 */
u_char *
var_ieee8021TpmrMspStatsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021TpmrMspStatsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021TpmrMib", "var_ieee8021TpmrMspStatsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021TpmrMspStatsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021TpmrMspStatsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021TpmrMspStatsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021TPMRMSPSTATSTXACKS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsTxAcks);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsTxAcks;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSTXADDNOTIFICATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsTxAddNotifications);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsTxAddNotifications;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSTXADDCONFIRMATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsTxAddConfirmations);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsTxAddConfirmations;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSTXLOSSNOTIFICATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsTxLossNotifications);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsTxLossNotifications;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSTXLOSSCONFIRMATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsTxLossConfirmations);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsTxLossConfirmations;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSRXACKS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsRxAcks);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsRxAcks;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSRXADDNOTIFICATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsRxAddNotifications);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsRxAddNotifications;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSRXADDCONFIRMATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsRxAddConfirmations);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsRxAddConfirmations;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSRXLOSSNOTIFICATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsRxLossNotifications);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsRxLossNotifications;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSRXLOSSCONFIRMATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsRxLossConfirmations);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsRxLossConfirmations;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSADDEVENTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsAddEvents);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsAddEvents;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSLOSSEVENTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsLossEvents);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsLossEvents;
		break;
	case (u_char) IEEE8021TPMRMSPSTATSMACSTATUSNOTIFICATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021TpmrMspStatsMacStatusNotifications);
		rval = (u_char *) &StorageTmp->ieee8021TpmrMspStatsMacStatusNotifications;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021TpmrMspLinkNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspLinkNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspLinkNotify entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotify not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotify: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotify: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspLinkNotify = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspLinkNotify for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspLinkNotify = StorageOld->ieee8021TpmrMspLinkNotify;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021TpmrMspLinkNotifyWait(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspLinkNotifyWait(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspLinkNotifyWait entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotifyWait not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotifyWait: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 40 */
		/* Note: ranges 20..100 */
		if ((20 > set_value || set_value > 100)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotifyWait: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspLinkNotifyWait = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspLinkNotifyWait for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspLinkNotifyWait = StorageOld->ieee8021TpmrMspLinkNotifyWait;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021TpmrMspLinkNotifyRetry(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspLinkNotifyRetry(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspLinkNotifyRetry entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotifyRetry not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotifyRetry: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 10..100 */
		if ((10 > set_value || set_value > 100)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspLinkNotifyRetry: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspLinkNotifyRetry = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspLinkNotifyRetry for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspLinkNotifyRetry = StorageOld->ieee8021TpmrMspLinkNotifyRetry;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021TpmrMspMacNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspMacNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspMacNotify entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacNotify not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacNotify: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacNotify: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspMacNotify = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspMacNotify for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspMacNotify = StorageOld->ieee8021TpmrMspMacNotify;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021TpmrMspMacNotifyTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspMacNotifyTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspMacNotifyTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacNotifyTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacNotifyTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 1..50 */
		if ((1 > set_value || set_value > 50)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacNotifyTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspMacNotifyTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspMacNotifyTime for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspMacNotifyTime = StorageOld->ieee8021TpmrMspMacNotifyTime;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021TpmrMspMacRecoverTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspMacRecoverTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspMacRecoverTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacRecoverTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacRecoverTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		/* Note: ranges 2..50 */
		if ((2 > set_value || set_value > 50)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspMacRecoverTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspMacRecoverTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspMacRecoverTime for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspMacRecoverTime = StorageOld->ieee8021TpmrMspMacRecoverTime;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021TpmrMspStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021TpmrMspStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021TpmrMspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021TpmrMib", "write_ieee8021TpmrMspStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021TpmrMspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021TpmrMspStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			if (StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old = ieee8021TpmrMspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021TpmrMspTable_rsvs++;
		StorageTmp->ieee8021TpmrMspStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021TpmrMspTable_tsts == 0)
				if ((ret = check_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021TpmrMspStorageType for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021TpmrMspTable_sets == 0)
				if ((ret = update_ieee8021TpmrMspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021TpmrMspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) != NULL) {
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
			StorageTmp->ieee8021TpmrMspTable_rsvs = 0;
			StorageTmp->ieee8021TpmrMspTable_tsts = 0;
			StorageTmp->ieee8021TpmrMspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021TpmrMspTable_sets == 0)
			revert_ieee8021TpmrMspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021TpmrMspTable_old) == NULL)
			break;
		StorageTmp->ieee8021TpmrMspStorageType = StorageOld->ieee8021TpmrMspStorageType;
		if (--StorageTmp->ieee8021TpmrMspTable_rsvs == 0)
			ieee8021TpmrMspTable_destroy(&StorageTmp->ieee8021TpmrMspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021TpmrMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021TpmrMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021TpmrMibold_signal_handler != NULL)
		(*ieee8021TpmrMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
}

/**
 * @fn void ieee8021TpmrMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021TpmrMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021TpmrMib", "ieee8021TpmrMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021TpmrMib", "done.\n"));
	return;
}
