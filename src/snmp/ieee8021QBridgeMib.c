/*****************************************************************************

 @(#) src/snmp/ieee8021qbridgemib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021qbridgemib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021QBridgeMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021QBridgeMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021QBridgeMib_refresh = 1;
volatile int ieee8021QBridgeTable_refresh = 1;
volatile int ieee8021QBridgeCVlanPortTable_refresh = 1;
volatile int ieee8021QBridgeFdbTable_refresh = 1;
volatile int ieee8021QBridgeTpFdbTable_refresh = 1;
volatile int ieee8021QBridgeTpGroupTable_refresh = 1;
volatile int ieee8021QBridgeForwardAllTable_refresh = 1;
volatile int ieee8021QBridgeForwardUnregisteredTable_refresh = 1;
volatile int ieee8021QBridgeStaticUnicastTable_refresh = 1;
volatile int ieee8021QBridgeStaticMulticastTable_refresh = 1;
volatile int ieee8021QBridgeVlanCurrentTable_refresh = 1;
volatile int ieee8021QBridgeVlanStaticTable_refresh = 1;
volatile int ieee8021QBridgeNextFreeLocalVlanTable_refresh = 1;
volatile int ieee8021QBridgePortVlanTable_refresh = 1;
volatile int ieee8021QBridgePortVlanStatisticsTable_refresh = 1;
volatile int ieee8021QBridgeLearningConstraintsTable_refresh = 1;
volatile int ieee8021QBridgeLearningConstraintsDefaultsTable_refresh = 1;
volatile int ieee8021QBridgeProtocolGroupTable_refresh = 1;
volatile int ieee8021QBridgeProtocolPortTable_refresh = 1;
volatile int ieee8021QBridgeVIDXTable_refresh = 1;
volatile int ieee8021QBridgeEgressVidXTable_refresh = 1;

/*
 * ieee8021QBridgeMib_variables_oid: object identifier for ieee8021QBridgeMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021QBridgeMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 4 };
oid ieee8021QBridgeCVlanPortTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 1, 2, 1 };
oid ieee8021QBridgeStaticUnicastTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 3, 1, 1 };
oid ieee8021QBridgeStaticMulticastTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 3, 2, 1 };
oid ieee8021QBridgeVlanStaticTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 4, 3, 1 };
oid ieee8021QBridgeLearningConstraintsTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 4, 8, 1 };
oid ieee8021QBridgeProtocolGroupTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 5, 1, 1 };
oid ieee8021QBridgeProtocolPortTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 5, 2, 1 };
oid ieee8021QBridgeVIDXTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 6, 1, 1 };
oid ieee8021QBridgeEgressVidXTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 4, 1, 6, 2, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021QBridgeBaseGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 1 };
oid ieee8021QBridgeFdbUnicastGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 2 };
oid ieee8021QBridgeFdbMulticastGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 3 };
oid ieee8021QBridgeServiceRequirementGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 4 };
oid ieee8021QBridgeFdbStaticGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 5 };
oid ieee8021QBridgeVlanGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 6 };
oid ieee8021QBridgeVlanStaticGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 7 };
oid ieee8021QBridgeVlanStatisticsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 8 };
oid ieee8021QBridgeLearningConstraintsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 9 };
oid ieee8021QBridgeLearningConstraintDefaultGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 10 };
oid ieee8021QBridgeClassificationDeviceGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 11 };
oid ieee8021QBridgeClassificationPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 12 };
oid ieee8021QBridgePortGroup2_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 13 };
oid ieee8021QBridgeCVlanPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 14 };
oid ieee8021QBridgeVIDXGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 15 };
oid ieee8021QBridgeEgressVIDXGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 1, 16 };
oid ieee8021QBridgeCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 4, 2, 2, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021QBridgeMib_variables: tree for ieee8021QBridgeMib
 * This variable defines function callbacks and type return information for the ieee8021QBridgeMib mib section
 */
struct variable7 ieee8021QBridgeMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021QBRIDGEVLANVERSIONNUMBER  1
	{(u_char) IEEE8021QBRIDGEVLANVERSIONNUMBER, ASN_INTEGER, RONLY, var_ieee8021QBridgeTable, 5, {1, 1, 1, 1, 2}},
#define   IEEE8021QBRIDGEMAXVLANID  2
	{(u_char) IEEE8021QBRIDGEMAXVLANID, ASN_INTEGER, RONLY, var_ieee8021QBridgeTable, 5, {1, 1, 1, 1, 3}},
#define   IEEE8021QBRIDGEMAXSUPPORTEDVLANS  3
	{(u_char) IEEE8021QBRIDGEMAXSUPPORTEDVLANS, ASN_UNSIGNED, RONLY, var_ieee8021QBridgeTable, 5, {1, 1, 1, 1, 4}},
#define   IEEE8021QBRIDGENUMVLANS  4
	{(u_char) IEEE8021QBRIDGENUMVLANS, ASN_GAUGE, RONLY, var_ieee8021QBridgeTable, 5, {1, 1, 1, 1, 5}},
#define   IEEE8021QBRIDGEMVRPENABLEDSTATUS  5
	{(u_char) IEEE8021QBRIDGEMVRPENABLEDSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeTable, 5, {1, 1, 1, 1, 6}},
#define   IEEE8021QBRIDGECVLANPORTROWSTATUS  6
	{(u_char) IEEE8021QBRIDGECVLANPORTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeCVlanPortTable, 5, {1, 1, 2, 1, 3}},
#define   IEEE8021QBRIDGEFDBDYNAMICCOUNT  7
	{(u_char) IEEE8021QBRIDGEFDBDYNAMICCOUNT, ASN_GAUGE, RONLY, var_ieee8021QBridgeFdbTable, 5, {1, 2, 1, 1, 3}},
#define   IEEE8021QBRIDGEFDBLEARNEDENTRYDISCARDS  8
	{(u_char) IEEE8021QBRIDGEFDBLEARNEDENTRYDISCARDS, ASN_COUNTER64, RONLY, var_ieee8021QBridgeFdbTable, 5, {1, 2, 1, 1, 4}},
#define   IEEE8021QBRIDGEFDBAGINGTIME  9
	{(u_char) IEEE8021QBRIDGEFDBAGINGTIME, ASN_INTEGER, RWRITE, var_ieee8021QBridgeFdbTable, 5, {1, 2, 1, 1, 5}},
#define   IEEE8021QBRIDGETPFDBPORT  10
	{(u_char) IEEE8021QBRIDGETPFDBPORT, ASN_UNSIGNED, RONLY, var_ieee8021QBridgeTpFdbTable, 5, {1, 2, 2, 1, 2}},
#define   IEEE8021QBRIDGETPFDBSTATUS  11
	{(u_char) IEEE8021QBRIDGETPFDBSTATUS, ASN_INTEGER, RONLY, var_ieee8021QBridgeTpFdbTable, 5, {1, 2, 2, 1, 3}},
#define   IEEE8021QBRIDGETPGROUPEGRESSPORTS  12
	{(u_char) IEEE8021QBRIDGETPGROUPEGRESSPORTS, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeTpGroupTable, 5, {1, 2, 3, 1, 2}},
#define   IEEE8021QBRIDGETPGROUPLEARNT  13
	{(u_char) IEEE8021QBRIDGETPGROUPLEARNT, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeTpGroupTable, 5, {1, 2, 3, 1, 3}},
#define   IEEE8021QBRIDGEFORWARDALLPORTS  14
	{(u_char) IEEE8021QBRIDGEFORWARDALLPORTS, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeForwardAllTable, 5, {1, 2, 4, 1, 2}},
#define   IEEE8021QBRIDGEFORWARDALLSTATICPORTS  15
	{(u_char) IEEE8021QBRIDGEFORWARDALLSTATICPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeForwardAllTable, 5, {1, 2, 4, 1, 3}},
#define   IEEE8021QBRIDGEFORWARDALLFORBIDDENPORTS  16
	{(u_char) IEEE8021QBRIDGEFORWARDALLFORBIDDENPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeForwardAllTable, 5, {1, 2, 4, 1, 4}},
#define   IEEE8021QBRIDGEFORWARDUNREGISTEREDPORTS  17
	{(u_char) IEEE8021QBRIDGEFORWARDUNREGISTEREDPORTS, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeForwardUnregisteredTable, 5, {1, 2, 5, 1, 2}},
#define   IEEE8021QBRIDGEFORWARDUNREGISTEREDSTATICPORTS  18
	{(u_char) IEEE8021QBRIDGEFORWARDUNREGISTEREDSTATICPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeForwardUnregisteredTable, 5, {1, 2, 5, 1, 3}},
#define   IEEE8021QBRIDGEFORWARDUNREGISTEREDFORBIDDENPORTS  19
	{(u_char) IEEE8021QBRIDGEFORWARDUNREGISTEREDFORBIDDENPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeForwardUnregisteredTable, 5, {1, 2, 5, 1, 4}},
#define   IEEE8021QBRIDGESTATICUNICASTSTATICEGRESSPORTS  20
	{(u_char) IEEE8021QBRIDGESTATICUNICASTSTATICEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeStaticUnicastTable, 5, {1, 3, 1, 1, 5}},
#define   IEEE8021QBRIDGESTATICUNICASTFORBIDDENEGRESSPORTS  21
	{(u_char) IEEE8021QBRIDGESTATICUNICASTFORBIDDENEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeStaticUnicastTable, 5, {1, 3, 1, 1, 6}},
#define   IEEE8021QBRIDGESTATICUNICASTSTORAGETYPE  22
	{(u_char) IEEE8021QBRIDGESTATICUNICASTSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021QBridgeStaticUnicastTable, 5, {1, 3, 1, 1, 7}},
#define   IEEE8021QBRIDGESTATICUNICASTROWSTATUS  23
	{(u_char) IEEE8021QBRIDGESTATICUNICASTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeStaticUnicastTable, 5, {1, 3, 1, 1, 8}},
#define   IEEE8021QBRIDGESTATICMULTICASTSTATICEGRESSPORTS  24
	{(u_char) IEEE8021QBRIDGESTATICMULTICASTSTATICEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeStaticMulticastTable, 5, {1, 3, 2, 1, 3}},
#define   IEEE8021QBRIDGESTATICMULTICASTFORBIDDENEGRESSPORTS  25
	{(u_char) IEEE8021QBRIDGESTATICMULTICASTFORBIDDENEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeStaticMulticastTable, 5, {1, 3, 2, 1, 4}},
#define   IEEE8021QBRIDGESTATICMULTICASTSTORAGETYPE  26
	{(u_char) IEEE8021QBRIDGESTATICMULTICASTSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021QBridgeStaticMulticastTable, 5, {1, 3, 2, 1, 5}},
#define   IEEE8021QBRIDGESTATICMULTICASTROWSTATUS  27
	{(u_char) IEEE8021QBRIDGESTATICMULTICASTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeStaticMulticastTable, 5, {1, 3, 2, 1, 6}},
#define   IEEE8021QBRIDGEVLANNUMDELETES  28
	{(u_char) IEEE8021QBRIDGEVLANNUMDELETES, ASN_COUNTER64, RONLY, var_ieee8021QBridgeMib, 3, {1, 4, 1}},
#define   IEEE8021QBRIDGEVLANFDBID  29
	{(u_char) IEEE8021QBRIDGEVLANFDBID, ASN_UNSIGNED, RONLY, var_ieee8021QBridgeVlanCurrentTable, 5, {1, 4, 2, 1, 4}},
#define   IEEE8021QBRIDGEVLANCURRENTEGRESSPORTS  30
	{(u_char) IEEE8021QBRIDGEVLANCURRENTEGRESSPORTS, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeVlanCurrentTable, 5, {1, 4, 2, 1, 5}},
#define   IEEE8021QBRIDGEVLANUNTAGGEDPORTS  31
	{(u_char) IEEE8021QBRIDGEVLANUNTAGGEDPORTS, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeVlanCurrentTable, 5, {1, 4, 2, 1, 6}},
#define   IEEE8021QBRIDGEVLANSTATUS  32
	{(u_char) IEEE8021QBRIDGEVLANSTATUS, ASN_INTEGER, RONLY, var_ieee8021QBridgeVlanCurrentTable, 5, {1, 4, 2, 1, 7}},
#define   IEEE8021QBRIDGEVLANCREATIONTIME  33
	{(u_char) IEEE8021QBRIDGEVLANCREATIONTIME, ASN_TIMETICKS, RONLY, var_ieee8021QBridgeVlanCurrentTable, 5, {1, 4, 2, 1, 8}},
#define   IEEE8021QBRIDGEVLANSTATICNAME  34
	{(u_char) IEEE8021QBRIDGEVLANSTATICNAME, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeVlanStaticTable, 5, {1, 4, 3, 1, 3}},
#define   IEEE8021QBRIDGEVLANSTATICEGRESSPORTS  35
	{(u_char) IEEE8021QBRIDGEVLANSTATICEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeVlanStaticTable, 5, {1, 4, 3, 1, 4}},
#define   IEEE8021QBRIDGEVLANFORBIDDENEGRESSPORTS  36
	{(u_char) IEEE8021QBRIDGEVLANFORBIDDENEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeVlanStaticTable, 5, {1, 4, 3, 1, 5}},
#define   IEEE8021QBRIDGEVLANSTATICUNTAGGEDPORTS  37
	{(u_char) IEEE8021QBRIDGEVLANSTATICUNTAGGEDPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021QBridgeVlanStaticTable, 5, {1, 4, 3, 1, 6}},
#define   IEEE8021QBRIDGEVLANSTATICROWSTATUS  38
	{(u_char) IEEE8021QBRIDGEVLANSTATICROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeVlanStaticTable, 5, {1, 4, 3, 1, 7}},
#define   IEEE8021QBRIDGENEXTFREELOCALVLANINDEX  39
	{(u_char) IEEE8021QBRIDGENEXTFREELOCALVLANINDEX, ASN_UNSIGNED, RONLY, var_ieee8021QBridgeNextFreeLocalVlanTable, 5, {1, 4, 4, 1, 2}},
#define   IEEE8021QBRIDGEPVID   40
	{(u_char) IEEE8021QBRIDGEPVID, ASN_UNSIGNED, RWRITE, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 1}},
#define   IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES  41
	{(u_char) IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES, ASN_INTEGER, RWRITE, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 2}},
#define   IEEE8021QBRIDGEPORTINGRESSFILTERING  42
	{(u_char) IEEE8021QBRIDGEPORTINGRESSFILTERING, ASN_INTEGER, RWRITE, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 3}},
#define   IEEE8021QBRIDGEPORTMVRPENABLEDSTATUS  43
	{(u_char) IEEE8021QBRIDGEPORTMVRPENABLEDSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 4}},
#define   IEEE8021QBRIDGEPORTMVRPFAILEDREGISTRATIONS  44
	{(u_char) IEEE8021QBRIDGEPORTMVRPFAILEDREGISTRATIONS, ASN_COUNTER64, RONLY, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 5}},
#define   IEEE8021QBRIDGEPORTMVRPLASTPDUORIGIN  45
	{(u_char) IEEE8021QBRIDGEPORTMVRPLASTPDUORIGIN, ASN_OCTET_STR, RONLY, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 6}},
#define   IEEE8021QBRIDGEPORTRESTRICTEDVLANREGISTRATION  46
	{(u_char) IEEE8021QBRIDGEPORTRESTRICTEDVLANREGISTRATION, ASN_INTEGER, RWRITE, var_ieee8021QBridgePortVlanTable, 5, {1, 4, 5, 1, 7}},
#define   IEEE8021QBRIDGETPVLANPORTINFRAMES  47
	{(u_char) IEEE8021QBRIDGETPVLANPORTINFRAMES, ASN_COUNTER64, RONLY, var_ieee8021QBridgePortVlanStatisticsTable, 5, {1, 4, 6, 1, 1}},
#define   IEEE8021QBRIDGETPVLANPORTOUTFRAMES  48
	{(u_char) IEEE8021QBRIDGETPVLANPORTOUTFRAMES, ASN_COUNTER64, RONLY, var_ieee8021QBridgePortVlanStatisticsTable, 5, {1, 4, 6, 1, 2}},
#define   IEEE8021QBRIDGETPVLANPORTINDISCARDS  49
	{(u_char) IEEE8021QBRIDGETPVLANPORTINDISCARDS, ASN_COUNTER64, RONLY, var_ieee8021QBridgePortVlanStatisticsTable, 5, {1, 4, 6, 1, 3}},
#define   IEEE8021QBRIDGELEARNINGCONSTRAINTSTYPE  50
	{(u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTSTYPE, ASN_INTEGER, RWRITE, var_ieee8021QBridgeLearningConstraintsTable, 5, {1, 4, 8, 1, 4}},
#define   IEEE8021QBRIDGELEARNINGCONSTRAINTSSTATUS  51
	{(u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTSSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeLearningConstraintsTable, 5, {1, 4, 8, 1, 5}},
#define   IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTSET  52
	{(u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTSET, ASN_INTEGER, RWRITE, var_ieee8021QBridgeLearningConstraintsDefaultsTable, 5, {1, 4, 9, 1, 2}},
#define   IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTTYPE  53
	{(u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTTYPE, ASN_INTEGER, RWRITE, var_ieee8021QBridgeLearningConstraintsDefaultsTable, 5, {1, 4, 9, 1, 3}},
#define   IEEE8021QBRIDGEPROTOCOLGROUPID  54
	{(u_char) IEEE8021QBRIDGEPROTOCOLGROUPID, ASN_INTEGER, RWRITE, var_ieee8021QBridgeProtocolGroupTable, 5, {1, 5, 1, 1, 4}},
#define   IEEE8021QBRIDGEPROTOCOLGROUPROWSTATUS  55
	{(u_char) IEEE8021QBRIDGEPROTOCOLGROUPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeProtocolGroupTable, 5, {1, 5, 1, 1, 5}},
#define   IEEE8021QBRIDGEPROTOCOLPORTGROUPVID  56
	{(u_char) IEEE8021QBRIDGEPROTOCOLPORTGROUPVID, ASN_INTEGER, RWRITE, var_ieee8021QBridgeProtocolPortTable, 5, {1, 5, 2, 1, 2}},
#define   IEEE8021QBRIDGEPROTOCOLPORTROWSTATUS  57
	{(u_char) IEEE8021QBRIDGEPROTOCOLPORTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeProtocolPortTable, 5, {1, 5, 2, 1, 3}},
#define   IEEE8021QBRIDGEVIDXRELAYVID  58
	{(u_char) IEEE8021QBRIDGEVIDXRELAYVID, ASN_INTEGER, RWRITE, var_ieee8021QBridgeVIDXTable, 5, {1, 6, 1, 1, 2}},
#define   IEEE8021QBRIDGEVIDXROWSTATUS  59
	{(u_char) IEEE8021QBRIDGEVIDXROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeVIDXTable, 5, {1, 6, 1, 1, 3}},
#define   IEEE8021QBRIDGEEGRESSVIDXLOCALVID  60
	{(u_char) IEEE8021QBRIDGEEGRESSVIDXLOCALVID, ASN_INTEGER, RWRITE, var_ieee8021QBridgeEgressVidXTable, 5, {1, 6, 2, 1, 2}},
#define   IEEE8021QBRIDGEEGRESSVIDXROWSTATUS  61
	{(u_char) IEEE8021QBRIDGEEGRESSVIDXROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021QBridgeEgressVidXTable, 5, {1, 6, 2, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct ieee8021QBridgeMib_data *ieee8021QBridgeMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021QBridgeTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeCVlanPortTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeFdbTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeTpFdbTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeTpGroupTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeForwardAllTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeForwardUnregisteredTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeStaticUnicastTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeStaticMulticastTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeVlanCurrentTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeVlanStaticTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeNextFreeLocalVlanTableStorage = NULL;
struct header_complex_index *ieee8021QBridgePortVlanTableStorage = NULL;
struct header_complex_index *ieee8021QBridgePortVlanStatisticsTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeLearningConstraintsTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeLearningConstraintsDefaultsTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeProtocolGroupTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeProtocolPortTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeVIDXTableStorage = NULL;
struct header_complex_index *ieee8021QBridgeEgressVidXTableStorage = NULL;

void (*ieee8021QBridgeMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021QBridgeMib_loop_handler(int);
void ieee8021QBridgeMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021QBridgeMib(void)
 * @brief ieee8021QBridgeMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021QBridgeMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021QBridgeMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021QBridgeMib", "init_ieee8021QBridgeMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021QBridgeMib", ieee8021QBridgeMib_variables, variable7, ieee8021QBridgeMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021QBridgeMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021QBridgeMib", parse_ieee8021QBridgeMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeTable", parse_ieee8021QBridgeTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeCVlanPortTable", parse_ieee8021QBridgeCVlanPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeFdbTable", parse_ieee8021QBridgeFdbTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeTpFdbTable", parse_ieee8021QBridgeTpFdbTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeTpGroupTable", parse_ieee8021QBridgeTpGroupTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeForwardAllTable", parse_ieee8021QBridgeForwardAllTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeForwardUnregisteredTable", parse_ieee8021QBridgeForwardUnregisteredTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeStaticUnicastTable", parse_ieee8021QBridgeStaticUnicastTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeStaticMulticastTable", parse_ieee8021QBridgeStaticMulticastTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeVlanCurrentTable", parse_ieee8021QBridgeVlanCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeVlanStaticTable", parse_ieee8021QBridgeVlanStaticTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeNextFreeLocalVlanTable", parse_ieee8021QBridgeNextFreeLocalVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgePortVlanTable", parse_ieee8021QBridgePortVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgePortVlanStatisticsTable", parse_ieee8021QBridgePortVlanStatisticsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeLearningConstraintsTable", parse_ieee8021QBridgeLearningConstraintsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeLearningConstraintsDefaultsTable", parse_ieee8021QBridgeLearningConstraintsDefaultsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeProtocolGroupTable", parse_ieee8021QBridgeProtocolGroupTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeProtocolPortTable", parse_ieee8021QBridgeProtocolPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeVIDXTable", parse_ieee8021QBridgeVIDXTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeEgressVidXTable", parse_ieee8021QBridgeEgressVidXTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeCVlanPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeFdbTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeTpFdbTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeTpGroupTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeForwardAllTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeForwardUnregisteredTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeStaticUnicastTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeStaticMulticastTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeVlanCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeVlanStaticTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeNextFreeLocalVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgePortVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgePortVlanStatisticsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeLearningConstraintsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeLearningConstraintsDefaultsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeProtocolGroupTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeProtocolPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeVIDXTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeEgressVidXTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021QBridgeMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021QBridgeMib_fd_handler, (void *) 1);
	}
	ieee8021QBridgeMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021QBridgeMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021QBridgeMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021QBridgeMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021QBridgeMib(void)
{
	DEBUGMSGTL(("ieee8021QBridgeMib", "deinit_ieee8021QBridgeMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021QBridgeMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021QBridgeMib_variables_oid, sizeof(ieee8021QBridgeMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021QBridgeMib");
	snmpd_unregister_config_handler("ieee8021QBridgeTable");
	snmpd_unregister_config_handler("ieee8021QBridgeCVlanPortTable");
	snmpd_unregister_config_handler("ieee8021QBridgeFdbTable");
	snmpd_unregister_config_handler("ieee8021QBridgeTpFdbTable");
	snmpd_unregister_config_handler("ieee8021QBridgeTpGroupTable");
	snmpd_unregister_config_handler("ieee8021QBridgeForwardAllTable");
	snmpd_unregister_config_handler("ieee8021QBridgeForwardUnregisteredTable");
	snmpd_unregister_config_handler("ieee8021QBridgeStaticUnicastTable");
	snmpd_unregister_config_handler("ieee8021QBridgeStaticMulticastTable");
	snmpd_unregister_config_handler("ieee8021QBridgeVlanCurrentTable");
	snmpd_unregister_config_handler("ieee8021QBridgeVlanStaticTable");
	snmpd_unregister_config_handler("ieee8021QBridgeNextFreeLocalVlanTable");
	snmpd_unregister_config_handler("ieee8021QBridgePortVlanTable");
	snmpd_unregister_config_handler("ieee8021QBridgePortVlanStatisticsTable");
	snmpd_unregister_config_handler("ieee8021QBridgeLearningConstraintsTable");
	snmpd_unregister_config_handler("ieee8021QBridgeLearningConstraintsDefaultsTable");
	snmpd_unregister_config_handler("ieee8021QBridgeProtocolGroupTable");
	snmpd_unregister_config_handler("ieee8021QBridgeProtocolPortTable");
	snmpd_unregister_config_handler("ieee8021QBridgeVIDXTable");
	snmpd_unregister_config_handler("ieee8021QBridgeEgressVidXTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

int
term_ieee8021QBridgeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021QBridgeMib", "term_ieee8021QBridgeMib: terminating...  "));
	deinit_ieee8021QBridgeMib();
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021QBridgeMib_data *ieee8021QBridgeMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021QBridgeMib.
 *
 * Creates a new ieee8021QBridgeMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021QBridgeMib.
 */
struct ieee8021QBridgeMib_data *
ieee8021QBridgeMib_create(void)
{
	struct ieee8021QBridgeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeMib_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanNumDeletes = (struct counter64) {
		0, 0};

	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeMib_data *ieee8021QBridgeMib_duplicate(struct ieee8021QBridgeMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021QBridgeMib_data *
ieee8021QBridgeMib_duplicate(struct ieee8021QBridgeMib_data *thedata)
{
	struct ieee8021QBridgeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeMib_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeVlanNumDeletes = thedata->ieee8021QBridgeVlanNumDeletes;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeMib_destroy(struct ieee8021QBridgeMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021QBridgeMib.
 * @brief delete a scalars structure from ieee8021QBridgeMib.
 *
 * Frees scalars that were previously removed from ieee8021QBridgeMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeMib_destroy(struct ieee8021QBridgeMib_data **thedata)
{
	struct ieee8021QBridgeMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeMib_add(struct ieee8021QBridgeMib_data *thedata)
 * @param thedata the structure representing ieee8021QBridgeMib scalars.
 * @brief adds node to the ieee8021QBridgeMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021QBridgeMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021QBridgeMib_add(struct ieee8021QBridgeMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeMib_add: adding data...  "));
	if (thedata)
		ieee8021QBridgeMibStorage = thedata;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021QBridgeMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeMib_data *StorageTmp = ieee8021QBridgeMib_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgeVlanNumDeletes, &tmpsize);
	ieee8021QBridgeMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeMib: storing data...  "));
	refresh_ieee8021QBridgeMib(1);
	if ((StorageTmp = ieee8021QBridgeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021QBridgeMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021QBridgeMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgeVlanNumDeletes, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021QBridgeMib(struct ieee8021QBridgeMib_data *StorageTmp, struct ieee8021QBridgeMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021QBridgeMib(struct ieee8021QBridgeMib_data *StorageTmp, struct ieee8021QBridgeMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeMib(struct ieee8021QBridgeMib_data *StorageTmp, struct ieee8021QBridgeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021QBridgeMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeMib(struct ieee8021QBridgeMib_data *StorageTmp, struct ieee8021QBridgeMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021QBridgeMib(struct 
 * @fn void revert_ieee8021QBridgeMib(struct ieee8021QBridgeMib_data *StorageTmp, struct ieee8021QBridgeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021QBridgeMib(struct ieee8021QBridgeMib_data *StorageTmp, struct ieee8021QBridgeMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021QBridgeMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021QBridgeMib(int force)
{
	if (ieee8021QBridgeMibStorage == NULL) {
		struct ieee8021QBridgeMib_data *StorageNew;

		if ((StorageNew = ieee8021QBridgeMib_create()) == NULL)
			return;
		ieee8021QBridgeMibStorage = StorageNew;
		ieee8021QBridgeMib_refresh = 1;
	}
	if (!force && ieee8021QBridgeMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021QBridgeMib", "refresh_ieee8021QBridgeMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021QBridgeMib_refresh = 0;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021QBridgeMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021QBridgeMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021QBridgeMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021QBridgeMib(0);
	if ((StorageTmp = ieee8021QBridgeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021QBridgeMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEVLANNUMDELETES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVlanNumDeletes);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVlanNumDeletes;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021QBridgeMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021QBridgeMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021QBridgeTable_data *ieee8021QBridgeTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeTable table.
 *
 * Creates a new ieee8021QBridgeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeTable_data *
ieee8021QBridgeTable_create(void)
{
	struct ieee8021QBridgeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanVersionNumber = 0;
		StorageNew->ieee8021QBridgeMaxVlanId = 0;
		StorageNew->ieee8021QBridgeMaxSupportedVlans = 0;
		StorageNew->ieee8021QBridgeNumVlans = 0;
		StorageNew->ieee8021QBridgeMvrpEnabledStatus = TV_TRUE;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeTable_data *ieee8021QBridgeTable_duplicate(struct ieee8021QBridgeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeTable_data *
ieee8021QBridgeTable_duplicate(struct ieee8021QBridgeTable_data *thedata)
{
	struct ieee8021QBridgeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeTable_id = thedata->ieee8021QBridgeTable_id;
		StorageNew->ieee8021QBridgeComponentId = thedata->ieee8021QBridgeComponentId;
		StorageNew->ieee8021QBridgeVlanVersionNumber = thedata->ieee8021QBridgeVlanVersionNumber;
		StorageNew->ieee8021QBridgeMaxVlanId = thedata->ieee8021QBridgeMaxVlanId;
		StorageNew->ieee8021QBridgeMaxSupportedVlans = thedata->ieee8021QBridgeMaxSupportedVlans;
		StorageNew->ieee8021QBridgeNumVlans = thedata->ieee8021QBridgeNumVlans;
		StorageNew->ieee8021QBridgeMvrpEnabledStatus = thedata->ieee8021QBridgeMvrpEnabledStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeTable_destroy(struct ieee8021QBridgeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeTable_destroy(struct ieee8021QBridgeTable_data **thedata)
{
	struct ieee8021QBridgeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeTable_add(struct ieee8021QBridgeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeTable_add(struct ieee8021QBridgeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeComponentId, sizeof(thedata->ieee8021QBridgeComponentId));
		header_complex_add_data(&ieee8021QBridgeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeTable_del(struct ieee8021QBridgeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeTable_del(struct ieee8021QBridgeTable_data *thedata)
{
	struct ieee8021QBridgeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeTable_data *StorageTmp = ieee8021QBridgeTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeVlanVersionNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeMaxVlanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeMaxSupportedVlans, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->ieee8021QBridgeNumVlans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeMvrpEnabledStatus, &tmpsize);
	ieee8021QBridgeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeTable(): store configuraiton file for ieee8021QBridgeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeTable: storing data...  "));
	refresh_ieee8021QBridgeTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeVlanVersionNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeMaxVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeMaxSupportedVlans, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->ieee8021QBridgeNumVlans, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeMvrpEnabledStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeCVlanPortTable_data *ieee8021QBridgeCVlanPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeCVlanPortTable table.
 *
 * Creates a new ieee8021QBridgeCVlanPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeCVlanPortTable_data *
ieee8021QBridgeCVlanPortTable_create(void)
{
	struct ieee8021QBridgeCVlanPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeCVlanPortTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeCVlanPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeCVlanPortRowStatus = 0;
		StorageNew->ieee8021QBridgeCVlanPortRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeCVlanPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeCVlanPortTable_data *ieee8021QBridgeCVlanPortTable_duplicate(struct ieee8021QBridgeCVlanPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeCVlanPortTable_data *
ieee8021QBridgeCVlanPortTable_duplicate(struct ieee8021QBridgeCVlanPortTable_data *thedata)
{
	struct ieee8021QBridgeCVlanPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeCVlanPortTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeCVlanPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeCVlanPortTable_id = thedata->ieee8021QBridgeCVlanPortTable_id;
		StorageNew->ieee8021QBridgeCVlanPortComponentId = thedata->ieee8021QBridgeCVlanPortComponentId;
		StorageNew->ieee8021QBridgeCVlanPortNumber = thedata->ieee8021QBridgeCVlanPortNumber;
		StorageNew->ieee8021QBridgeCVlanPortRowStatus = thedata->ieee8021QBridgeCVlanPortRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeCVlanPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeCVlanPortTable_destroy(struct ieee8021QBridgeCVlanPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeCVlanPortTable_destroy(struct ieee8021QBridgeCVlanPortTable_data **thedata)
{
	struct ieee8021QBridgeCVlanPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeCVlanPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeCVlanPortTable_add(struct ieee8021QBridgeCVlanPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeCVlanPortTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeCVlanPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeCVlanPortTable_add(struct ieee8021QBridgeCVlanPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeCVlanPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeCVlanPortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeCVlanPortComponentId, sizeof(thedata->ieee8021QBridgeCVlanPortComponentId));
		/* ieee8021QBridgeCVlanPortNumber */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeCVlanPortNumber, sizeof(thedata->ieee8021QBridgeCVlanPortNumber));
		header_complex_add_data(&ieee8021QBridgeCVlanPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeCVlanPortTable_del(struct ieee8021QBridgeCVlanPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeCVlanPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeCVlanPortTable_del(struct ieee8021QBridgeCVlanPortTable_data *thedata)
{
	struct ieee8021QBridgeCVlanPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeCVlanPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeCVlanPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeCVlanPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeCVlanPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeCVlanPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeCVlanPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeCVlanPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeCVlanPortTable_data *StorageTmp = ieee8021QBridgeCVlanPortTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeCVlanPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeCVlanPortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeCVlanPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeCVlanPortRowStatus, &tmpsize);
	ieee8021QBridgeCVlanPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeCVlanPortTable(): store configuraiton file for ieee8021QBridgeCVlanPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeCVlanPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeCVlanPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeCVlanPortTable: storing data...  "));
	refresh_ieee8021QBridgeCVlanPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeCVlanPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeCVlanPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeCVlanPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeCVlanPortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeCVlanPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeCVlanPortRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeFdbTable_data *ieee8021QBridgeFdbTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeFdbTable table.
 *
 * Creates a new ieee8021QBridgeFdbTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeFdbTable_data *
ieee8021QBridgeFdbTable_create(void)
{
	struct ieee8021QBridgeFdbTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeFdbTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeFdbTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeFdbDynamicCount = 0;
		StorageNew->ieee8021QBridgeFdbLearnedEntryDiscards = (struct counter64) {
		0, 0};
		StorageNew->ieee8021QBridgeFdbAgingTime = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeFdbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeFdbTable_data *ieee8021QBridgeFdbTable_duplicate(struct ieee8021QBridgeFdbTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeFdbTable_data *
ieee8021QBridgeFdbTable_duplicate(struct ieee8021QBridgeFdbTable_data *thedata)
{
	struct ieee8021QBridgeFdbTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeFdbTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeFdbTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeFdbTable_id = thedata->ieee8021QBridgeFdbTable_id;
		StorageNew->ieee8021QBridgeFdbComponentId = thedata->ieee8021QBridgeFdbComponentId;
		StorageNew->ieee8021QBridgeFdbId = thedata->ieee8021QBridgeFdbId;
		StorageNew->ieee8021QBridgeFdbDynamicCount = thedata->ieee8021QBridgeFdbDynamicCount;
		StorageNew->ieee8021QBridgeFdbLearnedEntryDiscards = thedata->ieee8021QBridgeFdbLearnedEntryDiscards;
		StorageNew->ieee8021QBridgeFdbAgingTime = thedata->ieee8021QBridgeFdbAgingTime;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeFdbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeFdbTable_destroy(struct ieee8021QBridgeFdbTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeFdbTable_destroy(struct ieee8021QBridgeFdbTable_data **thedata)
{
	struct ieee8021QBridgeFdbTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeFdbTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeFdbTable_add(struct ieee8021QBridgeFdbTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeFdbTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeFdbTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeFdbTable_add(struct ieee8021QBridgeFdbTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeFdbTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeFdbComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeFdbComponentId, sizeof(thedata->ieee8021QBridgeFdbComponentId));
		/* ieee8021QBridgeFdbId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeFdbId, sizeof(thedata->ieee8021QBridgeFdbId));
		header_complex_add_data(&ieee8021QBridgeFdbTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeFdbTable_del(struct ieee8021QBridgeFdbTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeFdbTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeFdbTable_del(struct ieee8021QBridgeFdbTable_data *thedata)
{
	struct ieee8021QBridgeFdbTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeFdbTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeFdbTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeFdbTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeFdbTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeFdbTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeFdbTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeFdbTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeFdbTable_data *StorageTmp = ieee8021QBridgeFdbTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeFdbTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeFdbComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeFdbId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->ieee8021QBridgeFdbDynamicCount, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgeFdbLearnedEntryDiscards, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeFdbAgingTime, &tmpsize);
	ieee8021QBridgeFdbTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeFdbTable(): store configuraiton file for ieee8021QBridgeFdbTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeFdbTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeFdbTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeFdbTable: storing data...  "));
	refresh_ieee8021QBridgeFdbTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeFdbTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeFdbTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeFdbTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeFdbComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeFdbId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->ieee8021QBridgeFdbDynamicCount, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgeFdbLearnedEntryDiscards, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeFdbAgingTime, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeTpFdbTable_data *ieee8021QBridgeTpFdbTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeTpFdbTable table.
 *
 * Creates a new ieee8021QBridgeTpFdbTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeTpFdbTable_data *
ieee8021QBridgeTpFdbTable_create(void)
{
	struct ieee8021QBridgeTpFdbTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeTpFdbTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpFdbTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeFdbComponentId = 0;
		StorageNew->ieee8021QBridgeFdbId = 0;
		StorageNew->ieee8021QBridgeTpFdbPort = 0;
		StorageNew->ieee8021QBridgeTpFdbStatus = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeTpFdbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeTpFdbTable_data *ieee8021QBridgeTpFdbTable_duplicate(struct ieee8021QBridgeTpFdbTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeTpFdbTable_data *
ieee8021QBridgeTpFdbTable_duplicate(struct ieee8021QBridgeTpFdbTable_data *thedata)
{
	struct ieee8021QBridgeTpFdbTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeTpFdbTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpFdbTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeTpFdbTable_id = thedata->ieee8021QBridgeTpFdbTable_id;
		StorageNew->ieee8021QBridgeFdbComponentId = thedata->ieee8021QBridgeFdbComponentId;
		StorageNew->ieee8021QBridgeFdbId = thedata->ieee8021QBridgeFdbId;
		if (!(StorageNew->ieee8021QBridgeTpFdbAddress = malloc(thedata->ieee8021QBridgeTpFdbAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeTpFdbAddress, thedata->ieee8021QBridgeTpFdbAddress, thedata->ieee8021QBridgeTpFdbAddressLen);
		StorageNew->ieee8021QBridgeTpFdbAddressLen = thedata->ieee8021QBridgeTpFdbAddressLen;
		StorageNew->ieee8021QBridgeTpFdbAddress[StorageNew->ieee8021QBridgeTpFdbAddressLen] = 0;
		StorageNew->ieee8021QBridgeTpFdbPort = thedata->ieee8021QBridgeTpFdbPort;
		StorageNew->ieee8021QBridgeTpFdbStatus = thedata->ieee8021QBridgeTpFdbStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeTpFdbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeTpFdbTable_destroy(struct ieee8021QBridgeTpFdbTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeTpFdbTable_destroy(struct ieee8021QBridgeTpFdbTable_data **thedata)
{
	struct ieee8021QBridgeTpFdbTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpFdbTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeTpFdbAddress);
		StorageDel->ieee8021QBridgeTpFdbAddressLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeTpFdbTable_add(struct ieee8021QBridgeTpFdbTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeTpFdbTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeTpFdbTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeTpFdbTable_add(struct ieee8021QBridgeTpFdbTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpFdbTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeFdbComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeFdbComponentId, sizeof(thedata->ieee8021QBridgeFdbComponentId));
		/* ieee8021QBridgeFdbId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeFdbId, sizeof(thedata->ieee8021QBridgeFdbId));
		/* ieee8021QBridgeTpFdbAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021QBridgeTpFdbAddress, thedata->ieee8021QBridgeTpFdbAddressLen);
		header_complex_add_data(&ieee8021QBridgeTpFdbTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeTpFdbTable_del(struct ieee8021QBridgeTpFdbTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeTpFdbTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeTpFdbTable_del(struct ieee8021QBridgeTpFdbTable_data *thedata)
{
	struct ieee8021QBridgeTpFdbTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpFdbTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeTpFdbTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeTpFdbTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeTpFdbTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeTpFdbTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeTpFdbTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeTpFdbTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeTpFdbTable_data *StorageTmp = ieee8021QBridgeTpFdbTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeTpFdbTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeFdbComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeFdbId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeTpFdbAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeTpFdbAddress, &StorageTmp->ieee8021QBridgeTpFdbAddressLen);
	if (StorageTmp->ieee8021QBridgeTpFdbAddress == NULL) {
		config_perror("invalid specification for ieee8021QBridgeTpFdbAddress");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeTpFdbPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeTpFdbStatus, &tmpsize);
	ieee8021QBridgeTpFdbTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeTpFdbTable(): store configuraiton file for ieee8021QBridgeTpFdbTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeTpFdbTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeTpFdbTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeTpFdbTable: storing data...  "));
	refresh_ieee8021QBridgeTpFdbTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeTpFdbTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeTpFdbTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeTpFdbTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeFdbComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeFdbId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeTpFdbAddress, &StorageTmp->ieee8021QBridgeTpFdbAddressLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeTpFdbPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeTpFdbStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeTpGroupTable_data *ieee8021QBridgeTpGroupTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeTpGroupTable table.
 *
 * Creates a new ieee8021QBridgeTpGroupTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeTpGroupTable_data *
ieee8021QBridgeTpGroupTable_create(void)
{
	struct ieee8021QBridgeTpGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeTpGroupTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpGroupTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = 0;
		StorageNew->ieee8021QBridgeVlanIndex = 0;
		if ((StorageNew->ieee8021QBridgeTpGroupEgressPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeTpGroupEgressPortsLen = 0;
		StorageNew->ieee8021QBridgeTpGroupEgressPorts[StorageNew->ieee8021QBridgeTpGroupEgressPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeTpGroupLearnt = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeTpGroupLearntLen = 0;
		StorageNew->ieee8021QBridgeTpGroupLearnt[StorageNew->ieee8021QBridgeTpGroupLearntLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeTpGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeTpGroupTable_data *ieee8021QBridgeTpGroupTable_duplicate(struct ieee8021QBridgeTpGroupTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeTpGroupTable_data *
ieee8021QBridgeTpGroupTable_duplicate(struct ieee8021QBridgeTpGroupTable_data *thedata)
{
	struct ieee8021QBridgeTpGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeTpGroupTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpGroupTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeTpGroupTable_id = thedata->ieee8021QBridgeTpGroupTable_id;
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = thedata->ieee8021QBridgeVlanCurrentComponentId;
		StorageNew->ieee8021QBridgeVlanIndex = thedata->ieee8021QBridgeVlanIndex;
		if (!(StorageNew->ieee8021QBridgeTpGroupAddress = malloc(thedata->ieee8021QBridgeTpGroupAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeTpGroupAddress, thedata->ieee8021QBridgeTpGroupAddress, thedata->ieee8021QBridgeTpGroupAddressLen);
		StorageNew->ieee8021QBridgeTpGroupAddressLen = thedata->ieee8021QBridgeTpGroupAddressLen;
		StorageNew->ieee8021QBridgeTpGroupAddress[StorageNew->ieee8021QBridgeTpGroupAddressLen] = 0;
		if (!(StorageNew->ieee8021QBridgeTpGroupEgressPorts = malloc(thedata->ieee8021QBridgeTpGroupEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeTpGroupEgressPorts, thedata->ieee8021QBridgeTpGroupEgressPorts, thedata->ieee8021QBridgeTpGroupEgressPortsLen);
		StorageNew->ieee8021QBridgeTpGroupEgressPortsLen = thedata->ieee8021QBridgeTpGroupEgressPortsLen;
		StorageNew->ieee8021QBridgeTpGroupEgressPorts[StorageNew->ieee8021QBridgeTpGroupEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeTpGroupLearnt = malloc(thedata->ieee8021QBridgeTpGroupLearntLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeTpGroupLearnt, thedata->ieee8021QBridgeTpGroupLearnt, thedata->ieee8021QBridgeTpGroupLearntLen);
		StorageNew->ieee8021QBridgeTpGroupLearntLen = thedata->ieee8021QBridgeTpGroupLearntLen;
		StorageNew->ieee8021QBridgeTpGroupLearnt[StorageNew->ieee8021QBridgeTpGroupLearntLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeTpGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeTpGroupTable_destroy(struct ieee8021QBridgeTpGroupTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeTpGroupTable_destroy(struct ieee8021QBridgeTpGroupTable_data **thedata)
{
	struct ieee8021QBridgeTpGroupTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpGroupTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeTpGroupAddress);
		StorageDel->ieee8021QBridgeTpGroupAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeTpGroupEgressPorts);
		StorageDel->ieee8021QBridgeTpGroupEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeTpGroupLearnt);
		StorageDel->ieee8021QBridgeTpGroupLearntLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeTpGroupTable_add(struct ieee8021QBridgeTpGroupTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeTpGroupTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeTpGroupTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeTpGroupTable_add(struct ieee8021QBridgeTpGroupTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpGroupTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanCurrentComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanCurrentComponentId, sizeof(thedata->ieee8021QBridgeVlanCurrentComponentId));
		/* ieee8021QBridgeVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanIndex, sizeof(thedata->ieee8021QBridgeVlanIndex));
		/* ieee8021QBridgeTpGroupAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021QBridgeTpGroupAddress, thedata->ieee8021QBridgeTpGroupAddressLen);
		header_complex_add_data(&ieee8021QBridgeTpGroupTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeTpGroupTable_del(struct ieee8021QBridgeTpGroupTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeTpGroupTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeTpGroupTable_del(struct ieee8021QBridgeTpGroupTable_data *thedata)
{
	struct ieee8021QBridgeTpGroupTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeTpGroupTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeTpGroupTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeTpGroupTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeTpGroupTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeTpGroupTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeTpGroupTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeTpGroupTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeTpGroupTable_data *StorageTmp = ieee8021QBridgeTpGroupTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeTpGroupTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeTpGroupAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeTpGroupAddress, &StorageTmp->ieee8021QBridgeTpGroupAddressLen);
	if (StorageTmp->ieee8021QBridgeTpGroupAddress == NULL) {
		config_perror("invalid specification for ieee8021QBridgeTpGroupAddress");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeTpGroupEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeTpGroupEgressPorts, &StorageTmp->ieee8021QBridgeTpGroupEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeTpGroupEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeTpGroupEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeTpGroupLearnt);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeTpGroupLearnt, &StorageTmp->ieee8021QBridgeTpGroupLearntLen);
	if (StorageTmp->ieee8021QBridgeTpGroupLearnt == NULL) {
		config_perror("invalid specification for ieee8021QBridgeTpGroupLearnt");
		return;
	}
	ieee8021QBridgeTpGroupTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeTpGroupTable(): store configuraiton file for ieee8021QBridgeTpGroupTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeTpGroupTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeTpGroupTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeTpGroupTable: storing data...  "));
	refresh_ieee8021QBridgeTpGroupTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeTpGroupTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeTpGroupTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeTpGroupTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeTpGroupAddress, &StorageTmp->ieee8021QBridgeTpGroupAddressLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeTpGroupEgressPorts, &StorageTmp->ieee8021QBridgeTpGroupEgressPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeTpGroupLearnt, &StorageTmp->ieee8021QBridgeTpGroupLearntLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeForwardAllTable_data *ieee8021QBridgeForwardAllTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeForwardAllTable table.
 *
 * Creates a new ieee8021QBridgeForwardAllTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeForwardAllTable_data *
ieee8021QBridgeForwardAllTable_create(void)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeForwardAllTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardAllTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = 0;
		if ((StorageNew->ieee8021QBridgeForwardAllPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeForwardAllPortsLen = 0;
		StorageNew->ieee8021QBridgeForwardAllPorts[StorageNew->ieee8021QBridgeForwardAllPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeForwardAllStaticPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeForwardAllStaticPortsLen = 0;
		StorageNew->ieee8021QBridgeForwardAllStaticPorts[StorageNew->ieee8021QBridgeForwardAllStaticPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeForwardAllForbiddenPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeForwardAllForbiddenPortsLen = 0;
		StorageNew->ieee8021QBridgeForwardAllForbiddenPorts[StorageNew->ieee8021QBridgeForwardAllForbiddenPortsLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeForwardAllTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeForwardAllTable_data *ieee8021QBridgeForwardAllTable_duplicate(struct ieee8021QBridgeForwardAllTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeForwardAllTable_data *
ieee8021QBridgeForwardAllTable_duplicate(struct ieee8021QBridgeForwardAllTable_data *thedata)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeForwardAllTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardAllTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeForwardAllTable_id = thedata->ieee8021QBridgeForwardAllTable_id;
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = thedata->ieee8021QBridgeVlanCurrentComponentId;
		StorageNew->ieee8021QBridgeForwardAllVlanIndex = thedata->ieee8021QBridgeForwardAllVlanIndex;
		if (!(StorageNew->ieee8021QBridgeForwardAllPorts = malloc(thedata->ieee8021QBridgeForwardAllPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeForwardAllPorts, thedata->ieee8021QBridgeForwardAllPorts, thedata->ieee8021QBridgeForwardAllPortsLen);
		StorageNew->ieee8021QBridgeForwardAllPortsLen = thedata->ieee8021QBridgeForwardAllPortsLen;
		StorageNew->ieee8021QBridgeForwardAllPorts[StorageNew->ieee8021QBridgeForwardAllPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeForwardAllStaticPorts = malloc(thedata->ieee8021QBridgeForwardAllStaticPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeForwardAllStaticPorts, thedata->ieee8021QBridgeForwardAllStaticPorts, thedata->ieee8021QBridgeForwardAllStaticPortsLen);
		StorageNew->ieee8021QBridgeForwardAllStaticPortsLen = thedata->ieee8021QBridgeForwardAllStaticPortsLen;
		StorageNew->ieee8021QBridgeForwardAllStaticPorts[StorageNew->ieee8021QBridgeForwardAllStaticPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeForwardAllForbiddenPorts = malloc(thedata->ieee8021QBridgeForwardAllForbiddenPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeForwardAllForbiddenPorts, thedata->ieee8021QBridgeForwardAllForbiddenPorts, thedata->ieee8021QBridgeForwardAllForbiddenPortsLen);
		StorageNew->ieee8021QBridgeForwardAllForbiddenPortsLen = thedata->ieee8021QBridgeForwardAllForbiddenPortsLen;
		StorageNew->ieee8021QBridgeForwardAllForbiddenPorts[StorageNew->ieee8021QBridgeForwardAllForbiddenPortsLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeForwardAllTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeForwardAllTable_destroy(struct ieee8021QBridgeForwardAllTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeForwardAllTable_destroy(struct ieee8021QBridgeForwardAllTable_data **thedata)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardAllTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeForwardAllPorts);
		StorageDel->ieee8021QBridgeForwardAllPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeForwardAllStaticPorts);
		StorageDel->ieee8021QBridgeForwardAllStaticPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeForwardAllForbiddenPorts);
		StorageDel->ieee8021QBridgeForwardAllForbiddenPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeForwardAllTable_add(struct ieee8021QBridgeForwardAllTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeForwardAllTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeForwardAllTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeForwardAllTable_add(struct ieee8021QBridgeForwardAllTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardAllTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanCurrentComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanCurrentComponentId, sizeof(thedata->ieee8021QBridgeVlanCurrentComponentId));
		/* ieee8021QBridgeForwardAllVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeForwardAllVlanIndex, sizeof(thedata->ieee8021QBridgeForwardAllVlanIndex));
		header_complex_add_data(&ieee8021QBridgeForwardAllTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeForwardAllTable_del(struct ieee8021QBridgeForwardAllTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeForwardAllTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeForwardAllTable_del(struct ieee8021QBridgeForwardAllTable_data *thedata)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardAllTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeForwardAllTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeForwardAllTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeForwardAllTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeForwardAllTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeForwardAllTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeForwardAllTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeForwardAllTable_data *StorageTmp = ieee8021QBridgeForwardAllTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeForwardAllTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeForwardAllVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeForwardAllPorts, &StorageTmp->ieee8021QBridgeForwardAllPortsLen);
	if (StorageTmp->ieee8021QBridgeForwardAllPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeForwardAllPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllStaticPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeForwardAllStaticPorts, &StorageTmp->ieee8021QBridgeForwardAllStaticPortsLen);
	if (StorageTmp->ieee8021QBridgeForwardAllStaticPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeForwardAllStaticPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts, &StorageTmp->ieee8021QBridgeForwardAllForbiddenPortsLen);
	if (StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeForwardAllForbiddenPorts");
		return;
	}
	ieee8021QBridgeForwardAllTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeForwardAllTable(): store configuraiton file for ieee8021QBridgeForwardAllTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeForwardAllTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeForwardAllTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeForwardAllTable: storing data...  "));
	refresh_ieee8021QBridgeForwardAllTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeForwardAllTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeForwardAllTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeForwardAllTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeForwardAllVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeForwardAllPorts, &StorageTmp->ieee8021QBridgeForwardAllPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeForwardAllStaticPorts, &StorageTmp->ieee8021QBridgeForwardAllStaticPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts, &StorageTmp->ieee8021QBridgeForwardAllForbiddenPortsLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeForwardUnregisteredTable_data *ieee8021QBridgeForwardUnregisteredTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeForwardUnregisteredTable table.
 *
 * Creates a new ieee8021QBridgeForwardUnregisteredTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeForwardUnregisteredTable_data *
ieee8021QBridgeForwardUnregisteredTable_create(void)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeForwardUnregisteredTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardUnregisteredTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = 0;
		if ((StorageNew->ieee8021QBridgeForwardUnregisteredPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeForwardUnregisteredPortsLen = 0;
		StorageNew->ieee8021QBridgeForwardUnregisteredPorts[StorageNew->ieee8021QBridgeForwardUnregisteredPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeForwardUnregisteredStaticPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeForwardUnregisteredStaticPortsLen = 0;
		StorageNew->ieee8021QBridgeForwardUnregisteredStaticPorts[StorageNew->ieee8021QBridgeForwardUnregisteredStaticPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen = 0;
		StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPorts[StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeForwardUnregisteredTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeForwardUnregisteredTable_data *ieee8021QBridgeForwardUnregisteredTable_duplicate(struct ieee8021QBridgeForwardUnregisteredTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeForwardUnregisteredTable_data *
ieee8021QBridgeForwardUnregisteredTable_duplicate(struct ieee8021QBridgeForwardUnregisteredTable_data *thedata)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeForwardUnregisteredTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardUnregisteredTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeForwardUnregisteredTable_id = thedata->ieee8021QBridgeForwardUnregisteredTable_id;
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = thedata->ieee8021QBridgeVlanCurrentComponentId;
		StorageNew->ieee8021QBridgeForwardUnregisteredVlanIndex = thedata->ieee8021QBridgeForwardUnregisteredVlanIndex;
		if (!(StorageNew->ieee8021QBridgeForwardUnregisteredPorts = malloc(thedata->ieee8021QBridgeForwardUnregisteredPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeForwardUnregisteredPorts, thedata->ieee8021QBridgeForwardUnregisteredPorts, thedata->ieee8021QBridgeForwardUnregisteredPortsLen);
		StorageNew->ieee8021QBridgeForwardUnregisteredPortsLen = thedata->ieee8021QBridgeForwardUnregisteredPortsLen;
		StorageNew->ieee8021QBridgeForwardUnregisteredPorts[StorageNew->ieee8021QBridgeForwardUnregisteredPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeForwardUnregisteredStaticPorts = malloc(thedata->ieee8021QBridgeForwardUnregisteredStaticPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeForwardUnregisteredStaticPorts, thedata->ieee8021QBridgeForwardUnregisteredStaticPorts, thedata->ieee8021QBridgeForwardUnregisteredStaticPortsLen);
		StorageNew->ieee8021QBridgeForwardUnregisteredStaticPortsLen = thedata->ieee8021QBridgeForwardUnregisteredStaticPortsLen;
		StorageNew->ieee8021QBridgeForwardUnregisteredStaticPorts[StorageNew->ieee8021QBridgeForwardUnregisteredStaticPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPorts = malloc(thedata->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPorts, thedata->ieee8021QBridgeForwardUnregisteredForbiddenPorts,
		       thedata->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen);
		StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen = thedata->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen;
		StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPorts[StorageNew->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeForwardUnregisteredTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeForwardUnregisteredTable_destroy(struct ieee8021QBridgeForwardUnregisteredTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeForwardUnregisteredTable_destroy(struct ieee8021QBridgeForwardUnregisteredTable_data **thedata)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardUnregisteredTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeForwardUnregisteredPorts);
		StorageDel->ieee8021QBridgeForwardUnregisteredPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeForwardUnregisteredStaticPorts);
		StorageDel->ieee8021QBridgeForwardUnregisteredStaticPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeForwardUnregisteredForbiddenPorts);
		StorageDel->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeForwardUnregisteredTable_add(struct ieee8021QBridgeForwardUnregisteredTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeForwardUnregisteredTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeForwardUnregisteredTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeForwardUnregisteredTable_add(struct ieee8021QBridgeForwardUnregisteredTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardUnregisteredTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanCurrentComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanCurrentComponentId, sizeof(thedata->ieee8021QBridgeVlanCurrentComponentId));
		/* ieee8021QBridgeForwardUnregisteredVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeForwardUnregisteredVlanIndex, sizeof(thedata->ieee8021QBridgeForwardUnregisteredVlanIndex));
		header_complex_add_data(&ieee8021QBridgeForwardUnregisteredTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeForwardUnregisteredTable_del(struct ieee8021QBridgeForwardUnregisteredTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeForwardUnregisteredTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeForwardUnregisteredTable_del(struct ieee8021QBridgeForwardUnregisteredTable_data *thedata)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeForwardUnregisteredTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeForwardUnregisteredTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeForwardUnregisteredTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeForwardUnregisteredTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeForwardUnregisteredTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeForwardUnregisteredTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeForwardUnregisteredTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp = ieee8021QBridgeForwardUnregisteredTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeForwardUnregisteredTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeForwardUnregisteredVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeForwardUnregisteredPorts, &StorageTmp->ieee8021QBridgeForwardUnregisteredPortsLen);
	if (StorageTmp->ieee8021QBridgeForwardUnregisteredPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeForwardUnregisteredPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts, &StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPortsLen);
	if (StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeForwardUnregisteredStaticPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts, &StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen);
	if (StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeForwardUnregisteredForbiddenPorts");
		return;
	}
	ieee8021QBridgeForwardUnregisteredTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeForwardUnregisteredTable(): store configuraiton file for ieee8021QBridgeForwardUnregisteredTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeForwardUnregisteredTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeForwardUnregisteredTable: storing data...  "));
	refresh_ieee8021QBridgeForwardUnregisteredTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeForwardUnregisteredTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeForwardUnregisteredTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeForwardUnregisteredTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeForwardUnregisteredVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeForwardUnregisteredPorts, &StorageTmp->ieee8021QBridgeForwardUnregisteredPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts, &StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPortsLen);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts,
						   &StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeStaticUnicastTable_data *ieee8021QBridgeStaticUnicastTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeStaticUnicastTable table.
 *
 * Creates a new ieee8021QBridgeStaticUnicastTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeStaticUnicastTable_data *
ieee8021QBridgeStaticUnicastTable_create(void)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeStaticUnicastTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticUnicastTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPorts = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPortsLen = strlen("");
		if ((StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPorts = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen = strlen("");
		StorageNew->ieee8021QBridgeStaticUnicastStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021QBridgeStaticUnicastRowStatus = 0;
		StorageNew->ieee8021QBridgeStaticUnicastRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeStaticUnicastTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeStaticUnicastTable_data *ieee8021QBridgeStaticUnicastTable_duplicate(struct ieee8021QBridgeStaticUnicastTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeStaticUnicastTable_data *
ieee8021QBridgeStaticUnicastTable_duplicate(struct ieee8021QBridgeStaticUnicastTable_data *thedata)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeStaticUnicastTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticUnicastTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeStaticUnicastTable_id = thedata->ieee8021QBridgeStaticUnicastTable_id;
		StorageNew->ieee8021QBridgeStaticUnicastComponentId = thedata->ieee8021QBridgeStaticUnicastComponentId;
		StorageNew->ieee8021QBridgeStaticUnicastVlanIndex = thedata->ieee8021QBridgeStaticUnicastVlanIndex;
		if (!(StorageNew->ieee8021QBridgeStaticUnicastAddress = malloc(thedata->ieee8021QBridgeStaticUnicastAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeStaticUnicastAddress, thedata->ieee8021QBridgeStaticUnicastAddress, thedata->ieee8021QBridgeStaticUnicastAddressLen);
		StorageNew->ieee8021QBridgeStaticUnicastAddressLen = thedata->ieee8021QBridgeStaticUnicastAddressLen;
		StorageNew->ieee8021QBridgeStaticUnicastAddress[StorageNew->ieee8021QBridgeStaticUnicastAddressLen] = 0;
		StorageNew->ieee8021QBridgeStaticUnicastReceivePort = thedata->ieee8021QBridgeStaticUnicastReceivePort;
		if (!(StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPorts = malloc(thedata->ieee8021QBridgeStaticUnicastStaticEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPorts, thedata->ieee8021QBridgeStaticUnicastStaticEgressPorts, thedata->ieee8021QBridgeStaticUnicastStaticEgressPortsLen);
		StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPortsLen = thedata->ieee8021QBridgeStaticUnicastStaticEgressPortsLen;
		StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPorts[StorageNew->ieee8021QBridgeStaticUnicastStaticEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPorts = malloc(thedata->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPorts, thedata->ieee8021QBridgeStaticUnicastForbiddenEgressPorts,
		       thedata->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen);
		StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen = thedata->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen;
		StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPorts[StorageNew->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen] = 0;
		StorageNew->ieee8021QBridgeStaticUnicastStorageType = thedata->ieee8021QBridgeStaticUnicastStorageType;
		StorageNew->ieee8021QBridgeStaticUnicastRowStatus = thedata->ieee8021QBridgeStaticUnicastRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeStaticUnicastTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeStaticUnicastTable_destroy(struct ieee8021QBridgeStaticUnicastTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeStaticUnicastTable_destroy(struct ieee8021QBridgeStaticUnicastTable_data **thedata)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticUnicastTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeStaticUnicastAddress);
		StorageDel->ieee8021QBridgeStaticUnicastAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeStaticUnicastStaticEgressPorts);
		StorageDel->ieee8021QBridgeStaticUnicastStaticEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeStaticUnicastForbiddenEgressPorts);
		StorageDel->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeStaticUnicastTable_add(struct ieee8021QBridgeStaticUnicastTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeStaticUnicastTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeStaticUnicastTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeStaticUnicastTable_add(struct ieee8021QBridgeStaticUnicastTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticUnicastTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeStaticUnicastComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeStaticUnicastComponentId, sizeof(thedata->ieee8021QBridgeStaticUnicastComponentId));
		/* ieee8021QBridgeStaticUnicastVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeStaticUnicastVlanIndex, sizeof(thedata->ieee8021QBridgeStaticUnicastVlanIndex));
		/* ieee8021QBridgeStaticUnicastAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021QBridgeStaticUnicastAddress, thedata->ieee8021QBridgeStaticUnicastAddressLen);
		/* ieee8021QBridgeStaticUnicastReceivePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeStaticUnicastReceivePort, sizeof(thedata->ieee8021QBridgeStaticUnicastReceivePort));
		header_complex_add_data(&ieee8021QBridgeStaticUnicastTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeStaticUnicastTable_del(struct ieee8021QBridgeStaticUnicastTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeStaticUnicastTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeStaticUnicastTable_del(struct ieee8021QBridgeStaticUnicastTable_data *thedata)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticUnicastTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeStaticUnicastTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeStaticUnicastTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeStaticUnicastTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeStaticUnicastTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeStaticUnicastTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeStaticUnicastTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp = ieee8021QBridgeStaticUnicastTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeStaticUnicastTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeStaticUnicastComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeStaticUnicastVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeStaticUnicastAddress, &StorageTmp->ieee8021QBridgeStaticUnicastAddressLen);
	if (StorageTmp->ieee8021QBridgeStaticUnicastAddress == NULL) {
		config_perror("invalid specification for ieee8021QBridgeStaticUnicastAddress");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeStaticUnicastReceivePort, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts, &StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeStaticUnicastStaticEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts, &StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeStaticUnicastForbiddenEgressPorts");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeStaticUnicastStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeStaticUnicastRowStatus, &tmpsize);
	ieee8021QBridgeStaticUnicastTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeStaticUnicastTable(): store configuraiton file for ieee8021QBridgeStaticUnicastTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeStaticUnicastTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeStaticUnicastTable: storing data...  "));
	refresh_ieee8021QBridgeStaticUnicastTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeStaticUnicastTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeStaticUnicastTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021QBridgeStaticUnicastStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeStaticUnicastTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastAddress, &StorageTmp->ieee8021QBridgeStaticUnicastAddressLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastReceivePort, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts, &StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPortsLen);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts,
						   &StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeStaticUnicastRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeStaticMulticastTable_data *ieee8021QBridgeStaticMulticastTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeStaticMulticastTable table.
 *
 * Creates a new ieee8021QBridgeStaticMulticastTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeStaticMulticastTable_data *
ieee8021QBridgeStaticMulticastTable_create(void)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeStaticMulticastTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticMulticastTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = 0;
		StorageNew->ieee8021QBridgeVlanIndex = 0;
		if ((StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPorts = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPortsLen = strlen("");
		if ((StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPorts = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen = strlen("");
		StorageNew->ieee8021QBridgeStaticMulticastStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021QBridgeStaticMulticastRowStatus = 0;
		StorageNew->ieee8021QBridgeStaticMulticastRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeStaticMulticastTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeStaticMulticastTable_data *ieee8021QBridgeStaticMulticastTable_duplicate(struct ieee8021QBridgeStaticMulticastTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeStaticMulticastTable_data *
ieee8021QBridgeStaticMulticastTable_duplicate(struct ieee8021QBridgeStaticMulticastTable_data *thedata)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeStaticMulticastTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticMulticastTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeStaticMulticastTable_id = thedata->ieee8021QBridgeStaticMulticastTable_id;
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = thedata->ieee8021QBridgeVlanCurrentComponentId;
		StorageNew->ieee8021QBridgeVlanIndex = thedata->ieee8021QBridgeVlanIndex;
		if (!(StorageNew->ieee8021QBridgeStaticMulticastAddress = malloc(thedata->ieee8021QBridgeStaticMulticastAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeStaticMulticastAddress, thedata->ieee8021QBridgeStaticMulticastAddress, thedata->ieee8021QBridgeStaticMulticastAddressLen);
		StorageNew->ieee8021QBridgeStaticMulticastAddressLen = thedata->ieee8021QBridgeStaticMulticastAddressLen;
		StorageNew->ieee8021QBridgeStaticMulticastAddress[StorageNew->ieee8021QBridgeStaticMulticastAddressLen] = 0;
		StorageNew->ieee8021QBridgeStaticMulticastReceivePort = thedata->ieee8021QBridgeStaticMulticastReceivePort;
		if (!(StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPorts = malloc(thedata->ieee8021QBridgeStaticMulticastStaticEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPorts, thedata->ieee8021QBridgeStaticMulticastStaticEgressPorts,
		       thedata->ieee8021QBridgeStaticMulticastStaticEgressPortsLen);
		StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPortsLen = thedata->ieee8021QBridgeStaticMulticastStaticEgressPortsLen;
		StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPorts[StorageNew->ieee8021QBridgeStaticMulticastStaticEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPorts = malloc(thedata->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPorts, thedata->ieee8021QBridgeStaticMulticastForbiddenEgressPorts,
		       thedata->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen);
		StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen = thedata->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen;
		StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPorts[StorageNew->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen] = 0;
		StorageNew->ieee8021QBridgeStaticMulticastStorageType = thedata->ieee8021QBridgeStaticMulticastStorageType;
		StorageNew->ieee8021QBridgeStaticMulticastRowStatus = thedata->ieee8021QBridgeStaticMulticastRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeStaticMulticastTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeStaticMulticastTable_destroy(struct ieee8021QBridgeStaticMulticastTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeStaticMulticastTable_destroy(struct ieee8021QBridgeStaticMulticastTable_data **thedata)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticMulticastTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeStaticMulticastAddress);
		StorageDel->ieee8021QBridgeStaticMulticastAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeStaticMulticastStaticEgressPorts);
		StorageDel->ieee8021QBridgeStaticMulticastStaticEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeStaticMulticastForbiddenEgressPorts);
		StorageDel->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeStaticMulticastTable_add(struct ieee8021QBridgeStaticMulticastTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeStaticMulticastTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeStaticMulticastTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeStaticMulticastTable_add(struct ieee8021QBridgeStaticMulticastTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticMulticastTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanCurrentComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanCurrentComponentId, sizeof(thedata->ieee8021QBridgeVlanCurrentComponentId));
		/* ieee8021QBridgeVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanIndex, sizeof(thedata->ieee8021QBridgeVlanIndex));
		/* ieee8021QBridgeStaticMulticastAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021QBridgeStaticMulticastAddress, thedata->ieee8021QBridgeStaticMulticastAddressLen);
		/* ieee8021QBridgeStaticMulticastReceivePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeStaticMulticastReceivePort, sizeof(thedata->ieee8021QBridgeStaticMulticastReceivePort));
		header_complex_add_data(&ieee8021QBridgeStaticMulticastTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeStaticMulticastTable_del(struct ieee8021QBridgeStaticMulticastTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeStaticMulticastTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeStaticMulticastTable_del(struct ieee8021QBridgeStaticMulticastTable_data *thedata)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeStaticMulticastTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeStaticMulticastTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeStaticMulticastTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeStaticMulticastTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeStaticMulticastTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeStaticMulticastTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeStaticMulticastTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp = ieee8021QBridgeStaticMulticastTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeStaticMulticastTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeStaticMulticastAddress, &StorageTmp->ieee8021QBridgeStaticMulticastAddressLen);
	if (StorageTmp->ieee8021QBridgeStaticMulticastAddress == NULL) {
		config_perror("invalid specification for ieee8021QBridgeStaticMulticastAddress");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeStaticMulticastReceivePort, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts, &StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeStaticMulticastStaticEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts, &StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeStaticMulticastForbiddenEgressPorts");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeStaticMulticastStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeStaticMulticastRowStatus, &tmpsize);
	ieee8021QBridgeStaticMulticastTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeStaticMulticastTable(): store configuraiton file for ieee8021QBridgeStaticMulticastTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeStaticMulticastTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeStaticMulticastTable: storing data...  "));
	refresh_ieee8021QBridgeStaticMulticastTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeStaticMulticastTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeStaticMulticastTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021QBridgeStaticMulticastStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeStaticMulticastTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeStaticMulticastAddress, &StorageTmp->ieee8021QBridgeStaticMulticastAddressLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeStaticMulticastReceivePort, &tmpsize);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts, &StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPortsLen);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts,
						   &StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeStaticMulticastStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeStaticMulticastRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeVlanCurrentTable_data *ieee8021QBridgeVlanCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeVlanCurrentTable table.
 *
 * Creates a new ieee8021QBridgeVlanCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeVlanCurrentTable_data *
ieee8021QBridgeVlanCurrentTable_create(void)
{
	struct ieee8021QBridgeVlanCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeVlanCurrentTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanFdbId = 0;
		if ((StorageNew->ieee8021QBridgeVlanCurrentEgressPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeVlanCurrentEgressPortsLen = 0;
		StorageNew->ieee8021QBridgeVlanCurrentEgressPorts[StorageNew->ieee8021QBridgeVlanCurrentEgressPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeVlanUntaggedPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeVlanUntaggedPortsLen = 0;
		StorageNew->ieee8021QBridgeVlanUntaggedPorts[StorageNew->ieee8021QBridgeVlanUntaggedPortsLen] = 0;
		StorageNew->ieee8021QBridgeVlanStatus = 0;
		StorageNew->ieee8021QBridgeVlanCreationTime = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeVlanCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeVlanCurrentTable_data *ieee8021QBridgeVlanCurrentTable_duplicate(struct ieee8021QBridgeVlanCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeVlanCurrentTable_data *
ieee8021QBridgeVlanCurrentTable_duplicate(struct ieee8021QBridgeVlanCurrentTable_data *thedata)
{
	struct ieee8021QBridgeVlanCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeVlanCurrentTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeVlanCurrentTable_id = thedata->ieee8021QBridgeVlanCurrentTable_id;
		StorageNew->ieee8021QBridgeVlanTimeMark = thedata->ieee8021QBridgeVlanTimeMark;
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = thedata->ieee8021QBridgeVlanCurrentComponentId;
		StorageNew->ieee8021QBridgeVlanIndex = thedata->ieee8021QBridgeVlanIndex;
		StorageNew->ieee8021QBridgeVlanFdbId = thedata->ieee8021QBridgeVlanFdbId;
		if (!(StorageNew->ieee8021QBridgeVlanCurrentEgressPorts = malloc(thedata->ieee8021QBridgeVlanCurrentEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeVlanCurrentEgressPorts, thedata->ieee8021QBridgeVlanCurrentEgressPorts, thedata->ieee8021QBridgeVlanCurrentEgressPortsLen);
		StorageNew->ieee8021QBridgeVlanCurrentEgressPortsLen = thedata->ieee8021QBridgeVlanCurrentEgressPortsLen;
		StorageNew->ieee8021QBridgeVlanCurrentEgressPorts[StorageNew->ieee8021QBridgeVlanCurrentEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeVlanUntaggedPorts = malloc(thedata->ieee8021QBridgeVlanUntaggedPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeVlanUntaggedPorts, thedata->ieee8021QBridgeVlanUntaggedPorts, thedata->ieee8021QBridgeVlanUntaggedPortsLen);
		StorageNew->ieee8021QBridgeVlanUntaggedPortsLen = thedata->ieee8021QBridgeVlanUntaggedPortsLen;
		StorageNew->ieee8021QBridgeVlanUntaggedPorts[StorageNew->ieee8021QBridgeVlanUntaggedPortsLen] = 0;
		StorageNew->ieee8021QBridgeVlanStatus = thedata->ieee8021QBridgeVlanStatus;
		StorageNew->ieee8021QBridgeVlanCreationTime = thedata->ieee8021QBridgeVlanCreationTime;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeVlanCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeVlanCurrentTable_destroy(struct ieee8021QBridgeVlanCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeVlanCurrentTable_destroy(struct ieee8021QBridgeVlanCurrentTable_data **thedata)
{
	struct ieee8021QBridgeVlanCurrentTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeVlanCurrentEgressPorts);
		StorageDel->ieee8021QBridgeVlanCurrentEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeVlanUntaggedPorts);
		StorageDel->ieee8021QBridgeVlanUntaggedPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeVlanCurrentTable_add(struct ieee8021QBridgeVlanCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeVlanCurrentTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeVlanCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeVlanCurrentTable_add(struct ieee8021QBridgeVlanCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->ieee8021QBridgeVlanTimeMark, sizeof(thedata->ieee8021QBridgeVlanTimeMark));
		/* ieee8021QBridgeVlanCurrentComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanCurrentComponentId, sizeof(thedata->ieee8021QBridgeVlanCurrentComponentId));
		/* ieee8021QBridgeVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanIndex, sizeof(thedata->ieee8021QBridgeVlanIndex));
		header_complex_add_data(&ieee8021QBridgeVlanCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeVlanCurrentTable_del(struct ieee8021QBridgeVlanCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeVlanCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeVlanCurrentTable_del(struct ieee8021QBridgeVlanCurrentTable_data *thedata)
{
	struct ieee8021QBridgeVlanCurrentTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeVlanCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeVlanCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeVlanCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeVlanCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeVlanCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeVlanCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp = ieee8021QBridgeVlanCurrentTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeVlanCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021QBridgeVlanTimeMark, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanFdbId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeVlanCurrentEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeVlanCurrentEgressPorts, &StorageTmp->ieee8021QBridgeVlanCurrentEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeVlanCurrentEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeVlanCurrentEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeVlanUntaggedPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeVlanUntaggedPorts, &StorageTmp->ieee8021QBridgeVlanUntaggedPortsLen);
	if (StorageTmp->ieee8021QBridgeVlanUntaggedPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeVlanUntaggedPorts");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeVlanStatus, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021QBridgeVlanCreationTime, &tmpsize);
	ieee8021QBridgeVlanCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeVlanCurrentTable(): store configuraiton file for ieee8021QBridgeVlanCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeVlanCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeVlanCurrentTable: storing data...  "));
	refresh_ieee8021QBridgeVlanCurrentTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeVlanCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeVlanCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeVlanCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021QBridgeVlanTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanFdbId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentEgressPorts, &StorageTmp->ieee8021QBridgeVlanCurrentEgressPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeVlanUntaggedPorts, &StorageTmp->ieee8021QBridgeVlanUntaggedPortsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeVlanStatus, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021QBridgeVlanCreationTime, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeVlanStaticTable_data *ieee8021QBridgeVlanStaticTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeVlanStaticTable table.
 *
 * Creates a new ieee8021QBridgeVlanStaticTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeVlanStaticTable_data *
ieee8021QBridgeVlanStaticTable_create(void)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeVlanStaticTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanStaticTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021QBridgeVlanStaticName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeVlanStaticNameLen = 0;
		StorageNew->ieee8021QBridgeVlanStaticName[StorageNew->ieee8021QBridgeVlanStaticNameLen] = 0;
		if ((StorageNew->ieee8021QBridgeVlanStaticEgressPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeVlanStaticEgressPortsLen = 0;
		StorageNew->ieee8021QBridgeVlanStaticEgressPorts[StorageNew->ieee8021QBridgeVlanStaticEgressPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeVlanForbiddenEgressPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeVlanForbiddenEgressPortsLen = 0;
		StorageNew->ieee8021QBridgeVlanForbiddenEgressPorts[StorageNew->ieee8021QBridgeVlanForbiddenEgressPortsLen] = 0;
		if ((StorageNew->ieee8021QBridgeVlanStaticUntaggedPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeVlanStaticUntaggedPortsLen = 0;
		StorageNew->ieee8021QBridgeVlanStaticUntaggedPorts[StorageNew->ieee8021QBridgeVlanStaticUntaggedPortsLen] = 0;
		StorageNew->ieee8021QBridgeVlanStaticRowStatus = 0;
		StorageNew->ieee8021QBridgeVlanStaticRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeVlanStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeVlanStaticTable_data *ieee8021QBridgeVlanStaticTable_duplicate(struct ieee8021QBridgeVlanStaticTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeVlanStaticTable_data *
ieee8021QBridgeVlanStaticTable_duplicate(struct ieee8021QBridgeVlanStaticTable_data *thedata)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeVlanStaticTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanStaticTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeVlanStaticTable_id = thedata->ieee8021QBridgeVlanStaticTable_id;
		StorageNew->ieee8021QBridgeVlanStaticComponentId = thedata->ieee8021QBridgeVlanStaticComponentId;
		StorageNew->ieee8021QBridgeVlanStaticVlanIndex = thedata->ieee8021QBridgeVlanStaticVlanIndex;
		if (!(StorageNew->ieee8021QBridgeVlanStaticName = malloc(thedata->ieee8021QBridgeVlanStaticNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeVlanStaticName, thedata->ieee8021QBridgeVlanStaticName, thedata->ieee8021QBridgeVlanStaticNameLen);
		StorageNew->ieee8021QBridgeVlanStaticNameLen = thedata->ieee8021QBridgeVlanStaticNameLen;
		StorageNew->ieee8021QBridgeVlanStaticName[StorageNew->ieee8021QBridgeVlanStaticNameLen] = 0;
		if (!(StorageNew->ieee8021QBridgeVlanStaticEgressPorts = malloc(thedata->ieee8021QBridgeVlanStaticEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeVlanStaticEgressPorts, thedata->ieee8021QBridgeVlanStaticEgressPorts, thedata->ieee8021QBridgeVlanStaticEgressPortsLen);
		StorageNew->ieee8021QBridgeVlanStaticEgressPortsLen = thedata->ieee8021QBridgeVlanStaticEgressPortsLen;
		StorageNew->ieee8021QBridgeVlanStaticEgressPorts[StorageNew->ieee8021QBridgeVlanStaticEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeVlanForbiddenEgressPorts = malloc(thedata->ieee8021QBridgeVlanForbiddenEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeVlanForbiddenEgressPorts, thedata->ieee8021QBridgeVlanForbiddenEgressPorts, thedata->ieee8021QBridgeVlanForbiddenEgressPortsLen);
		StorageNew->ieee8021QBridgeVlanForbiddenEgressPortsLen = thedata->ieee8021QBridgeVlanForbiddenEgressPortsLen;
		StorageNew->ieee8021QBridgeVlanForbiddenEgressPorts[StorageNew->ieee8021QBridgeVlanForbiddenEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021QBridgeVlanStaticUntaggedPorts = malloc(thedata->ieee8021QBridgeVlanStaticUntaggedPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeVlanStaticUntaggedPorts, thedata->ieee8021QBridgeVlanStaticUntaggedPorts, thedata->ieee8021QBridgeVlanStaticUntaggedPortsLen);
		StorageNew->ieee8021QBridgeVlanStaticUntaggedPortsLen = thedata->ieee8021QBridgeVlanStaticUntaggedPortsLen;
		StorageNew->ieee8021QBridgeVlanStaticUntaggedPorts[StorageNew->ieee8021QBridgeVlanStaticUntaggedPortsLen] = 0;
		StorageNew->ieee8021QBridgeVlanStaticRowStatus = thedata->ieee8021QBridgeVlanStaticRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeVlanStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeVlanStaticTable_destroy(struct ieee8021QBridgeVlanStaticTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeVlanStaticTable_destroy(struct ieee8021QBridgeVlanStaticTable_data **thedata)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanStaticTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeVlanStaticName);
		StorageDel->ieee8021QBridgeVlanStaticNameLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeVlanStaticEgressPorts);
		StorageDel->ieee8021QBridgeVlanStaticEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeVlanForbiddenEgressPorts);
		StorageDel->ieee8021QBridgeVlanForbiddenEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeVlanStaticUntaggedPorts);
		StorageDel->ieee8021QBridgeVlanStaticUntaggedPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeVlanStaticTable_add(struct ieee8021QBridgeVlanStaticTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeVlanStaticTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeVlanStaticTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeVlanStaticTable_add(struct ieee8021QBridgeVlanStaticTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanStaticTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanStaticComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanStaticComponentId, sizeof(thedata->ieee8021QBridgeVlanStaticComponentId));
		/* ieee8021QBridgeVlanStaticVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanStaticVlanIndex, sizeof(thedata->ieee8021QBridgeVlanStaticVlanIndex));
		header_complex_add_data(&ieee8021QBridgeVlanStaticTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeVlanStaticTable_del(struct ieee8021QBridgeVlanStaticTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeVlanStaticTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeVlanStaticTable_del(struct ieee8021QBridgeVlanStaticTable_data *thedata)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVlanStaticTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeVlanStaticTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeVlanStaticTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeVlanStaticTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeVlanStaticTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeVlanStaticTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeVlanStaticTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = ieee8021QBridgeVlanStaticTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeVlanStaticTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanStaticComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanStaticVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeVlanStaticName, &StorageTmp->ieee8021QBridgeVlanStaticNameLen);
	if (StorageTmp->ieee8021QBridgeVlanStaticName == NULL) {
		config_perror("invalid specification for ieee8021QBridgeVlanStaticName");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeVlanStaticEgressPorts, &StorageTmp->ieee8021QBridgeVlanStaticEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeVlanStaticEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeVlanStaticEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts, &StorageTmp->ieee8021QBridgeVlanForbiddenEgressPortsLen);
	if (StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeVlanForbiddenEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts, &StorageTmp->ieee8021QBridgeVlanStaticUntaggedPortsLen);
	if (StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts == NULL) {
		config_perror("invalid specification for ieee8021QBridgeVlanStaticUntaggedPorts");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeVlanStaticRowStatus, &tmpsize);
	ieee8021QBridgeVlanStaticTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeVlanStaticTable(): store configuraiton file for ieee8021QBridgeVlanStaticTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeVlanStaticTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeVlanStaticTable: storing data...  "));
	refresh_ieee8021QBridgeVlanStaticTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeVlanStaticTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeVlanStaticTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeVlanStaticTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanStaticComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanStaticVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeVlanStaticName, &StorageTmp->ieee8021QBridgeVlanStaticNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeVlanStaticEgressPorts, &StorageTmp->ieee8021QBridgeVlanStaticEgressPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts, &StorageTmp->ieee8021QBridgeVlanForbiddenEgressPortsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts, &StorageTmp->ieee8021QBridgeVlanStaticUntaggedPortsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeVlanStaticRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeNextFreeLocalVlanTable_data *ieee8021QBridgeNextFreeLocalVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeNextFreeLocalVlanTable table.
 *
 * Creates a new ieee8021QBridgeNextFreeLocalVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeNextFreeLocalVlanTable_data *
ieee8021QBridgeNextFreeLocalVlanTable_create(void)
{
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeNextFreeLocalVlanTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeNextFreeLocalVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeNextFreeLocalVlanIndex = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeNextFreeLocalVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeNextFreeLocalVlanTable_data *ieee8021QBridgeNextFreeLocalVlanTable_duplicate(struct ieee8021QBridgeNextFreeLocalVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeNextFreeLocalVlanTable_data *
ieee8021QBridgeNextFreeLocalVlanTable_duplicate(struct ieee8021QBridgeNextFreeLocalVlanTable_data *thedata)
{
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeNextFreeLocalVlanTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeNextFreeLocalVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeNextFreeLocalVlanTable_id = thedata->ieee8021QBridgeNextFreeLocalVlanTable_id;
		StorageNew->ieee8021QBridgeNextFreeLocalVlanComponentId = thedata->ieee8021QBridgeNextFreeLocalVlanComponentId;
		StorageNew->ieee8021QBridgeNextFreeLocalVlanIndex = thedata->ieee8021QBridgeNextFreeLocalVlanIndex;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeNextFreeLocalVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeNextFreeLocalVlanTable_destroy(struct ieee8021QBridgeNextFreeLocalVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeNextFreeLocalVlanTable_destroy(struct ieee8021QBridgeNextFreeLocalVlanTable_data **thedata)
{
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeNextFreeLocalVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeNextFreeLocalVlanTable_add(struct ieee8021QBridgeNextFreeLocalVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeNextFreeLocalVlanTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeNextFreeLocalVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeNextFreeLocalVlanTable_add(struct ieee8021QBridgeNextFreeLocalVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeNextFreeLocalVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeNextFreeLocalVlanComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeNextFreeLocalVlanComponentId, sizeof(thedata->ieee8021QBridgeNextFreeLocalVlanComponentId));
		header_complex_add_data(&ieee8021QBridgeNextFreeLocalVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeNextFreeLocalVlanTable_del(struct ieee8021QBridgeNextFreeLocalVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeNextFreeLocalVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeNextFreeLocalVlanTable_del(struct ieee8021QBridgeNextFreeLocalVlanTable_data *thedata)
{
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeNextFreeLocalVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeNextFreeLocalVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeNextFreeLocalVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeNextFreeLocalVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeNextFreeLocalVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeNextFreeLocalVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeNextFreeLocalVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp = ieee8021QBridgeNextFreeLocalVlanTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeNextFreeLocalVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeNextFreeLocalVlanComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeNextFreeLocalVlanIndex, &tmpsize);
	ieee8021QBridgeNextFreeLocalVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeNextFreeLocalVlanTable(): store configuraiton file for ieee8021QBridgeNextFreeLocalVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeNextFreeLocalVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeNextFreeLocalVlanTable: storing data...  "));
	refresh_ieee8021QBridgeNextFreeLocalVlanTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeNextFreeLocalVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeNextFreeLocalVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeNextFreeLocalVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeNextFreeLocalVlanComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeNextFreeLocalVlanIndex, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgePortVlanTable_data *ieee8021QBridgePortVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgePortVlanTable table.
 *
 * Creates a new ieee8021QBridgePortVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgePortVlanTable_data *
ieee8021QBridgePortVlanTable_create(void)
{
	struct ieee8021QBridgePortVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgePortVlanTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021QBridgePvid = 1;
		StorageNew->ieee8021QBridgePortAcceptableFrameTypes = IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES_ADMITALL;
		StorageNew->ieee8021QBridgePortIngressFiltering = TV_FALSE;
		StorageNew->ieee8021QBridgePortMvrpEnabledStatus = TV_TRUE;
		StorageNew->ieee8021QBridgePortMvrpFailedRegistrations = (struct counter64) {
		0, 0};
		if ((StorageNew->ieee8021QBridgePortMvrpLastPduOrigin = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgePortMvrpLastPduOriginLen = 0;
		StorageNew->ieee8021QBridgePortMvrpLastPduOrigin[StorageNew->ieee8021QBridgePortMvrpLastPduOriginLen] = 0;
		StorageNew->ieee8021QBridgePortRestrictedVlanRegistration = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgePortVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgePortVlanTable_data *ieee8021QBridgePortVlanTable_duplicate(struct ieee8021QBridgePortVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgePortVlanTable_data *
ieee8021QBridgePortVlanTable_duplicate(struct ieee8021QBridgePortVlanTable_data *thedata)
{
	struct ieee8021QBridgePortVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgePortVlanTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgePortVlanTable_id = thedata->ieee8021QBridgePortVlanTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021QBridgePvid = thedata->ieee8021QBridgePvid;
		StorageNew->ieee8021QBridgePortAcceptableFrameTypes = thedata->ieee8021QBridgePortAcceptableFrameTypes;
		StorageNew->ieee8021QBridgePortIngressFiltering = thedata->ieee8021QBridgePortIngressFiltering;
		StorageNew->ieee8021QBridgePortMvrpEnabledStatus = thedata->ieee8021QBridgePortMvrpEnabledStatus;
		StorageNew->ieee8021QBridgePortMvrpFailedRegistrations = thedata->ieee8021QBridgePortMvrpFailedRegistrations;
		if (!(StorageNew->ieee8021QBridgePortMvrpLastPduOrigin = malloc(thedata->ieee8021QBridgePortMvrpLastPduOriginLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgePortMvrpLastPduOrigin, thedata->ieee8021QBridgePortMvrpLastPduOrigin, thedata->ieee8021QBridgePortMvrpLastPduOriginLen);
		StorageNew->ieee8021QBridgePortMvrpLastPduOriginLen = thedata->ieee8021QBridgePortMvrpLastPduOriginLen;
		StorageNew->ieee8021QBridgePortMvrpLastPduOrigin[StorageNew->ieee8021QBridgePortMvrpLastPduOriginLen] = 0;
		StorageNew->ieee8021QBridgePortRestrictedVlanRegistration = thedata->ieee8021QBridgePortRestrictedVlanRegistration;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgePortVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgePortVlanTable_destroy(struct ieee8021QBridgePortVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgePortVlanTable_destroy(struct ieee8021QBridgePortVlanTable_data **thedata)
{
	struct ieee8021QBridgePortVlanTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgePortMvrpLastPduOrigin);
		StorageDel->ieee8021QBridgePortMvrpLastPduOriginLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgePortVlanTable_add(struct ieee8021QBridgePortVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgePortVlanTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgePortVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgePortVlanTable_add(struct ieee8021QBridgePortVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021QBridgePortVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgePortVlanTable_del(struct ieee8021QBridgePortVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgePortVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgePortVlanTable_del(struct ieee8021QBridgePortVlanTable_data *thedata)
{
	struct ieee8021QBridgePortVlanTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgePortVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgePortVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgePortVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgePortVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgePortVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgePortVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = ieee8021QBridgePortVlanTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgePortVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgePvid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgePortAcceptableFrameTypes, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgePortIngressFiltering, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgePortMvrpEnabledStatus, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgePortMvrpFailedRegistrations, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgePortMvrpLastPduOrigin);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgePortMvrpLastPduOrigin, &StorageTmp->ieee8021QBridgePortMvrpLastPduOriginLen);
	if (StorageTmp->ieee8021QBridgePortMvrpLastPduOrigin == NULL) {
		config_perror("invalid specification for ieee8021QBridgePortMvrpLastPduOrigin");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration, &tmpsize);
	ieee8021QBridgePortVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgePortVlanTable(): store configuraiton file for ieee8021QBridgePortVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgePortVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgePortVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgePortVlanTable: storing data...  "));
	refresh_ieee8021QBridgePortVlanTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgePortVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgePortVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgePortVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgePvid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgePortAcceptableFrameTypes, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgePortIngressFiltering, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgePortMvrpEnabledStatus, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgePortMvrpFailedRegistrations, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgePortMvrpLastPduOrigin, &StorageTmp->ieee8021QBridgePortMvrpLastPduOriginLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgePortVlanStatisticsTable_data *ieee8021QBridgePortVlanStatisticsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgePortVlanStatisticsTable table.
 *
 * Creates a new ieee8021QBridgePortVlanStatisticsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgePortVlanStatisticsTable_data *
ieee8021QBridgePortVlanStatisticsTable_create(void)
{
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgePortVlanStatisticsTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanStatisticsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021QBridgeVlanIndex = 0;
		StorageNew->ieee8021QBridgeTpVlanPortInFrames = (struct counter64) {
		0, 0};
		StorageNew->ieee8021QBridgeTpVlanPortOutFrames = (struct counter64) {
		0, 0};
		StorageNew->ieee8021QBridgeTpVlanPortInDiscards = (struct counter64) {
		0, 0};
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgePortVlanStatisticsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgePortVlanStatisticsTable_data *ieee8021QBridgePortVlanStatisticsTable_duplicate(struct ieee8021QBridgePortVlanStatisticsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgePortVlanStatisticsTable_data *
ieee8021QBridgePortVlanStatisticsTable_duplicate(struct ieee8021QBridgePortVlanStatisticsTable_data *thedata)
{
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgePortVlanStatisticsTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanStatisticsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgePortVlanStatisticsTable_id = thedata->ieee8021QBridgePortVlanStatisticsTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021QBridgeVlanIndex = thedata->ieee8021QBridgeVlanIndex;
		StorageNew->ieee8021QBridgeTpVlanPortInFrames = thedata->ieee8021QBridgeTpVlanPortInFrames;
		StorageNew->ieee8021QBridgeTpVlanPortOutFrames = thedata->ieee8021QBridgeTpVlanPortOutFrames;
		StorageNew->ieee8021QBridgeTpVlanPortInDiscards = thedata->ieee8021QBridgeTpVlanPortInDiscards;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgePortVlanStatisticsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgePortVlanStatisticsTable_destroy(struct ieee8021QBridgePortVlanStatisticsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgePortVlanStatisticsTable_destroy(struct ieee8021QBridgePortVlanStatisticsTable_data **thedata)
{
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanStatisticsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgePortVlanStatisticsTable_add(struct ieee8021QBridgePortVlanStatisticsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgePortVlanStatisticsTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgePortVlanStatisticsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgePortVlanStatisticsTable_add(struct ieee8021QBridgePortVlanStatisticsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanStatisticsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021QBridgeVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanIndex, sizeof(thedata->ieee8021QBridgeVlanIndex));
		header_complex_add_data(&ieee8021QBridgePortVlanStatisticsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgePortVlanStatisticsTable_del(struct ieee8021QBridgePortVlanStatisticsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgePortVlanStatisticsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgePortVlanStatisticsTable_del(struct ieee8021QBridgePortVlanStatisticsTable_data *thedata)
{
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgePortVlanStatisticsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgePortVlanStatisticsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgePortVlanStatisticsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgePortVlanStatisticsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgePortVlanStatisticsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgePortVlanStatisticsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgePortVlanStatisticsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp = ieee8021QBridgePortVlanStatisticsTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgePortVlanStatisticsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgeTpVlanPortInFrames, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgeTpVlanPortOutFrames, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgeTpVlanPortInDiscards, &tmpsize);
	ieee8021QBridgePortVlanStatisticsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgePortVlanStatisticsTable(): store configuraiton file for ieee8021QBridgePortVlanStatisticsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgePortVlanStatisticsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgePortVlanStatisticsTable: storing data...  "));
	refresh_ieee8021QBridgePortVlanStatisticsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgePortVlanStatisticsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgePortVlanStatisticsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgePortVlanStatisticsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgeTpVlanPortInFrames, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgeTpVlanPortOutFrames, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgeTpVlanPortInDiscards, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeLearningConstraintsTable_data *ieee8021QBridgeLearningConstraintsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeLearningConstraintsTable table.
 *
 * Creates a new ieee8021QBridgeLearningConstraintsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeLearningConstraintsTable_data *
ieee8021QBridgeLearningConstraintsTable_create(void)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeLearningConstraintsTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeLearningConstraintsType = 0;
		StorageNew->ieee8021QBridgeLearningConstraintsStatus = 0;
		StorageNew->ieee8021QBridgeLearningConstraintsStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeLearningConstraintsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeLearningConstraintsTable_data *ieee8021QBridgeLearningConstraintsTable_duplicate(struct ieee8021QBridgeLearningConstraintsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeLearningConstraintsTable_data *
ieee8021QBridgeLearningConstraintsTable_duplicate(struct ieee8021QBridgeLearningConstraintsTable_data *thedata)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeLearningConstraintsTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeLearningConstraintsTable_id = thedata->ieee8021QBridgeLearningConstraintsTable_id;
		StorageNew->ieee8021QBridgeLearningConstraintsComponentId = thedata->ieee8021QBridgeLearningConstraintsComponentId;
		StorageNew->ieee8021QBridgeLearningConstraintsVlan = thedata->ieee8021QBridgeLearningConstraintsVlan;
		StorageNew->ieee8021QBridgeLearningConstraintsSet = thedata->ieee8021QBridgeLearningConstraintsSet;
		StorageNew->ieee8021QBridgeLearningConstraintsType = thedata->ieee8021QBridgeLearningConstraintsType;
		StorageNew->ieee8021QBridgeLearningConstraintsStatus = thedata->ieee8021QBridgeLearningConstraintsStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeLearningConstraintsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeLearningConstraintsTable_destroy(struct ieee8021QBridgeLearningConstraintsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeLearningConstraintsTable_destroy(struct ieee8021QBridgeLearningConstraintsTable_data **thedata)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeLearningConstraintsTable_add(struct ieee8021QBridgeLearningConstraintsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeLearningConstraintsTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeLearningConstraintsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeLearningConstraintsTable_add(struct ieee8021QBridgeLearningConstraintsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeLearningConstraintsComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeLearningConstraintsComponentId,
					  sizeof(thedata->ieee8021QBridgeLearningConstraintsComponentId));
		/* ieee8021QBridgeLearningConstraintsVlan */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeLearningConstraintsVlan, sizeof(thedata->ieee8021QBridgeLearningConstraintsVlan));
		/* ieee8021QBridgeLearningConstraintsSet */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021QBridgeLearningConstraintsSet, sizeof(thedata->ieee8021QBridgeLearningConstraintsSet));
		header_complex_add_data(&ieee8021QBridgeLearningConstraintsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeLearningConstraintsTable_del(struct ieee8021QBridgeLearningConstraintsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeLearningConstraintsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeLearningConstraintsTable_del(struct ieee8021QBridgeLearningConstraintsTable_data *thedata)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeLearningConstraintsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeLearningConstraintsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeLearningConstraintsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeLearningConstraintsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeLearningConstraintsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeLearningConstraintsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp = ieee8021QBridgeLearningConstraintsTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeLearningConstraintsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeLearningConstraintsComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeLearningConstraintsVlan, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeLearningConstraintsSet, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeLearningConstraintsType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeLearningConstraintsStatus, &tmpsize);
	ieee8021QBridgeLearningConstraintsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeLearningConstraintsTable(): store configuraiton file for ieee8021QBridgeLearningConstraintsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeLearningConstraintsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeLearningConstraintsTable: storing data...  "));
	refresh_ieee8021QBridgeLearningConstraintsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeLearningConstraintsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeLearningConstraintsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeLearningConstraintsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintsComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintsVlan, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintsSet, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintsType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintsStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *ieee8021QBridgeLearningConstraintsDefaultsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeLearningConstraintsDefaultsTable table.
 *
 * Creates a new ieee8021QBridgeLearningConstraintsDefaultsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *
ieee8021QBridgeLearningConstraintsDefaultsTable_create(void)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeLearningConstraintsDefaultsTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsDefaultsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeLearningConstraintDefaultSet = 0;
		StorageNew->ieee8021QBridgeLearningConstraintDefaultType = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *ieee8021QBridgeLearningConstraintsDefaultsTable_duplicate(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *
ieee8021QBridgeLearningConstraintsDefaultsTable_duplicate(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *thedata)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeLearningConstraintsDefaultsTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsDefaultsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeLearningConstraintsDefaultsTable_id = thedata->ieee8021QBridgeLearningConstraintsDefaultsTable_id;
		StorageNew->ieee8021QBridgeLearningConstraintDefaultComponentId = thedata->ieee8021QBridgeLearningConstraintDefaultComponentId;
		StorageNew->ieee8021QBridgeLearningConstraintDefaultSet = thedata->ieee8021QBridgeLearningConstraintDefaultSet;
		StorageNew->ieee8021QBridgeLearningConstraintDefaultType = thedata->ieee8021QBridgeLearningConstraintDefaultType;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data **thedata)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsDefaultsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeLearningConstraintsDefaultsTable_add(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeLearningConstraintsDefaultsTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeLearningConstraintsDefaultsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeLearningConstraintsDefaultsTable_add(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsDefaultsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeLearningConstraintDefaultComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeLearningConstraintDefaultComponentId,
					  sizeof(thedata->ieee8021QBridgeLearningConstraintDefaultComponentId));
		header_complex_add_data(&ieee8021QBridgeLearningConstraintsDefaultsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeLearningConstraintsDefaultsTable_del(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeLearningConstraintsDefaultsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeLearningConstraintsDefaultsTable_del(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *thedata)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeLearningConstraintsDefaultsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeLearningConstraintsDefaultsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeLearningConstraintsDefaultsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeLearningConstraintsDefaultsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeLearningConstraintsDefaultsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeLearningConstraintsDefaultsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeLearningConstraintsDefaultsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp = ieee8021QBridgeLearningConstraintsDefaultsTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeLearningConstraintsDefaultsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeLearningConstraintDefaultComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeLearningConstraintDefaultType, &tmpsize);
	ieee8021QBridgeLearningConstraintsDefaultsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeLearningConstraintsDefaultsTable(): store configuraiton file for ieee8021QBridgeLearningConstraintsDefaultsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeLearningConstraintsDefaultsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeLearningConstraintsDefaultsTable: storing data...  "));
	refresh_ieee8021QBridgeLearningConstraintsDefaultsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeLearningConstraintsDefaultsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeLearningConstraintsDefaultsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintDefaultComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeLearningConstraintDefaultType, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeProtocolGroupTable_data *ieee8021QBridgeProtocolGroupTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeProtocolGroupTable table.
 *
 * Creates a new ieee8021QBridgeProtocolGroupTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeProtocolGroupTable_data *
ieee8021QBridgeProtocolGroupTable_create(void)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeProtocolGroupTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolGroupTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeProtocolGroupId = 0;
		StorageNew->ieee8021QBridgeProtocolGroupRowStatus = 0;
		StorageNew->ieee8021QBridgeProtocolGroupRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeProtocolGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeProtocolGroupTable_data *ieee8021QBridgeProtocolGroupTable_duplicate(struct ieee8021QBridgeProtocolGroupTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeProtocolGroupTable_data *
ieee8021QBridgeProtocolGroupTable_duplicate(struct ieee8021QBridgeProtocolGroupTable_data *thedata)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeProtocolGroupTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolGroupTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeProtocolGroupTable_id = thedata->ieee8021QBridgeProtocolGroupTable_id;
		StorageNew->ieee8021QBridgeProtocolGroupComponentId = thedata->ieee8021QBridgeProtocolGroupComponentId;
		StorageNew->ieee8021QBridgeProtocolTemplateFrameType = thedata->ieee8021QBridgeProtocolTemplateFrameType;
		if (!(StorageNew->ieee8021QBridgeProtocolTemplateProtocolValue = malloc(thedata->ieee8021QBridgeProtocolTemplateProtocolValueLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeProtocolTemplateProtocolValue, thedata->ieee8021QBridgeProtocolTemplateProtocolValue, thedata->ieee8021QBridgeProtocolTemplateProtocolValueLen);
		StorageNew->ieee8021QBridgeProtocolTemplateProtocolValueLen = thedata->ieee8021QBridgeProtocolTemplateProtocolValueLen;
		StorageNew->ieee8021QBridgeProtocolTemplateProtocolValue[StorageNew->ieee8021QBridgeProtocolTemplateProtocolValueLen] = 0;
		StorageNew->ieee8021QBridgeProtocolGroupId = thedata->ieee8021QBridgeProtocolGroupId;
		StorageNew->ieee8021QBridgeProtocolGroupRowStatus = thedata->ieee8021QBridgeProtocolGroupRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeProtocolGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeProtocolGroupTable_destroy(struct ieee8021QBridgeProtocolGroupTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeProtocolGroupTable_destroy(struct ieee8021QBridgeProtocolGroupTable_data **thedata)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolGroupTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeProtocolTemplateProtocolValue);
		StorageDel->ieee8021QBridgeProtocolTemplateProtocolValueLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeProtocolGroupTable_add(struct ieee8021QBridgeProtocolGroupTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeProtocolGroupTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeProtocolGroupTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeProtocolGroupTable_add(struct ieee8021QBridgeProtocolGroupTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolGroupTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeProtocolGroupComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeProtocolGroupComponentId, sizeof(thedata->ieee8021QBridgeProtocolGroupComponentId));
		/* ieee8021QBridgeProtocolTemplateFrameType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021QBridgeProtocolTemplateFrameType, sizeof(thedata->ieee8021QBridgeProtocolTemplateFrameType));
		/* ieee8021QBridgeProtocolTemplateProtocolValue */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021QBridgeProtocolTemplateProtocolValue, thedata->ieee8021QBridgeProtocolTemplateProtocolValueLen);
		header_complex_add_data(&ieee8021QBridgeProtocolGroupTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeProtocolGroupTable_del(struct ieee8021QBridgeProtocolGroupTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeProtocolGroupTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeProtocolGroupTable_del(struct ieee8021QBridgeProtocolGroupTable_data *thedata)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolGroupTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeProtocolGroupTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeProtocolGroupTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeProtocolGroupTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeProtocolGroupTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeProtocolGroupTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeProtocolGroupTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp = ieee8021QBridgeProtocolGroupTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeProtocolGroupTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeProtocolGroupComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeProtocolTemplateFrameType, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeProtocolTemplateProtocolValue);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeProtocolTemplateProtocolValue, &StorageTmp->ieee8021QBridgeProtocolTemplateProtocolValueLen);
	if (StorageTmp->ieee8021QBridgeProtocolTemplateProtocolValue == NULL) {
		config_perror("invalid specification for ieee8021QBridgeProtocolTemplateProtocolValue");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeProtocolGroupId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeProtocolGroupRowStatus, &tmpsize);
	ieee8021QBridgeProtocolGroupTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeProtocolGroupTable(): store configuraiton file for ieee8021QBridgeProtocolGroupTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeProtocolGroupTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeProtocolGroupTable: storing data...  "));
	refresh_ieee8021QBridgeProtocolGroupTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeProtocolGroupTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeProtocolGroupTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeProtocolGroupTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeProtocolGroupComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeProtocolTemplateFrameType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeProtocolTemplateProtocolValue, &StorageTmp->ieee8021QBridgeProtocolTemplateProtocolValueLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeProtocolGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeProtocolGroupRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeProtocolPortTable_data *ieee8021QBridgeProtocolPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeProtocolPortTable table.
 *
 * Creates a new ieee8021QBridgeProtocolPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeProtocolPortTable_data *
ieee8021QBridgeProtocolPortTable_create(void)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeProtocolPortTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021QBridgeProtocolPortGroupVid = 0;
		StorageNew->ieee8021QBridgeProtocolPortRowStatus = 0;
		StorageNew->ieee8021QBridgeProtocolPortRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeProtocolPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeProtocolPortTable_data *ieee8021QBridgeProtocolPortTable_duplicate(struct ieee8021QBridgeProtocolPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeProtocolPortTable_data *
ieee8021QBridgeProtocolPortTable_duplicate(struct ieee8021QBridgeProtocolPortTable_data *thedata)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeProtocolPortTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeProtocolPortTable_id = thedata->ieee8021QBridgeProtocolPortTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021QBridgeProtocolPortGroupId = thedata->ieee8021QBridgeProtocolPortGroupId;
		StorageNew->ieee8021QBridgeProtocolPortGroupVid = thedata->ieee8021QBridgeProtocolPortGroupVid;
		StorageNew->ieee8021QBridgeProtocolPortRowStatus = thedata->ieee8021QBridgeProtocolPortRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeProtocolPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeProtocolPortTable_destroy(struct ieee8021QBridgeProtocolPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeProtocolPortTable_destroy(struct ieee8021QBridgeProtocolPortTable_data **thedata)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeProtocolPortTable_add(struct ieee8021QBridgeProtocolPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeProtocolPortTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeProtocolPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeProtocolPortTable_add(struct ieee8021QBridgeProtocolPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021QBridgeProtocolPortGroupId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021QBridgeProtocolPortGroupId, sizeof(thedata->ieee8021QBridgeProtocolPortGroupId));
		header_complex_add_data(&ieee8021QBridgeProtocolPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeProtocolPortTable_del(struct ieee8021QBridgeProtocolPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeProtocolPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeProtocolPortTable_del(struct ieee8021QBridgeProtocolPortTable_data *thedata)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeProtocolPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeProtocolPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeProtocolPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeProtocolPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeProtocolPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeProtocolPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeProtocolPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeProtocolPortTable_data *StorageTmp = ieee8021QBridgeProtocolPortTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeProtocolPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeProtocolPortGroupId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeProtocolPortGroupVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeProtocolPortRowStatus, &tmpsize);
	ieee8021QBridgeProtocolPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeProtocolPortTable(): store configuraiton file for ieee8021QBridgeProtocolPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeProtocolPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeProtocolPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeProtocolPortTable: storing data...  "));
	refresh_ieee8021QBridgeProtocolPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeProtocolPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeProtocolPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeProtocolPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeProtocolPortGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeProtocolPortGroupVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeProtocolPortRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeVIDXTable_data *ieee8021QBridgeVIDXTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeVIDXTable table.
 *
 * Creates a new ieee8021QBridgeVIDXTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeVIDXTable_data *
ieee8021QBridgeVIDXTable_create(void)
{
	struct ieee8021QBridgeVIDXTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeVIDXTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVIDXTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021QBridgeVIDXRelayVid = 0;
		StorageNew->ieee8021QBridgeVIDXRowStatus = 0;
		StorageNew->ieee8021QBridgeVIDXRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeVIDXTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeVIDXTable_data *ieee8021QBridgeVIDXTable_duplicate(struct ieee8021QBridgeVIDXTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeVIDXTable_data *
ieee8021QBridgeVIDXTable_duplicate(struct ieee8021QBridgeVIDXTable_data *thedata)
{
	struct ieee8021QBridgeVIDXTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeVIDXTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVIDXTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeVIDXTable_id = thedata->ieee8021QBridgeVIDXTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021QBridgeVIDXLocalVid = thedata->ieee8021QBridgeVIDXLocalVid;
		StorageNew->ieee8021QBridgeVIDXRelayVid = thedata->ieee8021QBridgeVIDXRelayVid;
		StorageNew->ieee8021QBridgeVIDXRowStatus = thedata->ieee8021QBridgeVIDXRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeVIDXTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeVIDXTable_destroy(struct ieee8021QBridgeVIDXTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeVIDXTable_destroy(struct ieee8021QBridgeVIDXTable_data **thedata)
{
	struct ieee8021QBridgeVIDXTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVIDXTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeVIDXTable_add(struct ieee8021QBridgeVIDXTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeVIDXTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeVIDXTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeVIDXTable_add(struct ieee8021QBridgeVIDXTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVIDXTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021QBridgeVIDXLocalVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021QBridgeVIDXLocalVid, sizeof(thedata->ieee8021QBridgeVIDXLocalVid));
		header_complex_add_data(&ieee8021QBridgeVIDXTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeVIDXTable_del(struct ieee8021QBridgeVIDXTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeVIDXTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeVIDXTable_del(struct ieee8021QBridgeVIDXTable_data *thedata)
{
	struct ieee8021QBridgeVIDXTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeVIDXTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeVIDXTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeVIDXTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeVIDXTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeVIDXTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeVIDXTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeVIDXTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeVIDXTable_data *StorageTmp = ieee8021QBridgeVIDXTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeVIDXTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeVIDXLocalVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeVIDXRelayVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeVIDXRowStatus, &tmpsize);
	ieee8021QBridgeVIDXTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeVIDXTable(): store configuraiton file for ieee8021QBridgeVIDXTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeVIDXTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeVIDXTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeVIDXTable: storing data...  "));
	refresh_ieee8021QBridgeVIDXTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeVIDXTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeVIDXTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeVIDXTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeVIDXLocalVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeVIDXRelayVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeVIDXRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgeEgressVidXTable_data *ieee8021QBridgeEgressVidXTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeEgressVidXTable table.
 *
 * Creates a new ieee8021QBridgeEgressVidXTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeEgressVidXTable_data *
ieee8021QBridgeEgressVidXTable_create(void)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeEgressVidXTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeEgressVidXTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021QBridgeEgressVidXLocalVid = 0;
		StorageNew->ieee8021QBridgeEgressVidXRowStatus = 0;
		StorageNew->ieee8021QBridgeEgressVidXRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeEgressVidXTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeEgressVidXTable_data *ieee8021QBridgeEgressVidXTable_duplicate(struct ieee8021QBridgeEgressVidXTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeEgressVidXTable_data *
ieee8021QBridgeEgressVidXTable_duplicate(struct ieee8021QBridgeEgressVidXTable_data *thedata)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeEgressVidXTable_data);

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeEgressVidXTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeEgressVidXTable_id = thedata->ieee8021QBridgeEgressVidXTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021QBridgeEgressVidXRelayVid = thedata->ieee8021QBridgeEgressVidXRelayVid;
		StorageNew->ieee8021QBridgeEgressVidXLocalVid = thedata->ieee8021QBridgeEgressVidXLocalVid;
		StorageNew->ieee8021QBridgeEgressVidXRowStatus = thedata->ieee8021QBridgeEgressVidXRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeEgressVidXTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeEgressVidXTable_destroy(struct ieee8021QBridgeEgressVidXTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeEgressVidXTable_destroy(struct ieee8021QBridgeEgressVidXTable_data **thedata)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeEgressVidXTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeEgressVidXTable_add(struct ieee8021QBridgeEgressVidXTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeEgressVidXTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeEgressVidXTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeEgressVidXTable_add(struct ieee8021QBridgeEgressVidXTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeEgressVidXTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021QBridgeEgressVidXRelayVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021QBridgeEgressVidXRelayVid, sizeof(thedata->ieee8021QBridgeEgressVidXRelayVid));
		header_complex_add_data(&ieee8021QBridgeEgressVidXTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeEgressVidXTable_del(struct ieee8021QBridgeEgressVidXTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeEgressVidXTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeEgressVidXTable_del(struct ieee8021QBridgeEgressVidXTable_data *thedata)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeEgressVidXTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeEgressVidXTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeEgressVidXTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeEgressVidXTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeEgressVidXTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeEgressVidXTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeEgressVidXTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeEgressVidXTable_data *StorageTmp = ieee8021QBridgeEgressVidXTable_create();

	DEBUGMSGTL(("ieee8021QBridgeMib", "parse_ieee8021QBridgeEgressVidXTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeEgressVidXRelayVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeEgressVidXLocalVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021QBridgeEgressVidXRowStatus, &tmpsize);
	ieee8021QBridgeEgressVidXTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeEgressVidXTable(): store configuraiton file for ieee8021QBridgeEgressVidXTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeEgressVidXTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeEgressVidXTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021QBridgeMib", "store_ieee8021QBridgeEgressVidXTable: storing data...  "));
	refresh_ieee8021QBridgeEgressVidXTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeEgressVidXTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeEgressVidXTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeEgressVidXTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeEgressVidXRelayVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeEgressVidXLocalVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021QBridgeEgressVidXRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, struct ieee8021QBridgeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, struct ieee8021QBridgeTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, struct ieee8021QBridgeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, struct ieee8021QBridgeTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, struct ieee8021QBridgeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, struct ieee8021QBridgeTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeTable_data *
refresh_ieee8021QBridgeTable_row(struct ieee8021QBridgeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeTable(int force)
{
	if (!force && ieee8021QBridgeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEVLANVERSIONNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVlanVersionNumber);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVlanVersionNumber;
		break;
	case (u_char) IEEE8021QBRIDGEMAXVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeMaxVlanId);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeMaxVlanId;
		break;
	case (u_char) IEEE8021QBRIDGEMAXSUPPORTEDVLANS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeMaxSupportedVlans);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeMaxSupportedVlans;
		break;
	case (u_char) IEEE8021QBRIDGENUMVLANS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeNumVlans);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeNumVlans;
		break;
	case (u_char) IEEE8021QBRIDGEMVRPENABLEDSTATUS:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeMvrpEnabledStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeMvrpEnabledStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeMvrpEnabledStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, struct ieee8021QBridgeCVlanPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, struct ieee8021QBridgeCVlanPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, struct ieee8021QBridgeCVlanPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeCVlanPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, struct ieee8021QBridgeCVlanPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeCVlanPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, struct ieee8021QBridgeCVlanPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, struct ieee8021QBridgeCVlanPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeCVlanPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeCVlanPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeCVlanPortTable_data *
refresh_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeCVlanPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeCVlanPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeCVlanPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeCVlanPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeCVlanPortTable(int force)
{
	if (!force && ieee8021QBridgeCVlanPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeCVlanPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeCVlanPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeCVlanPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeCVlanPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeCVlanPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeCVlanPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeCVlanPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeCVlanPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeCVlanPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGECVLANPORTROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeCVlanPortRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeCVlanPortRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeCVlanPortRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, struct ieee8021QBridgeFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, struct ieee8021QBridgeFdbTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, struct ieee8021QBridgeFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeFdbTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, struct ieee8021QBridgeFdbTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeFdbTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, struct ieee8021QBridgeFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, struct ieee8021QBridgeFdbTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeFdbTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeFdbTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeFdbTable_data *
refresh_ieee8021QBridgeFdbTable_row(struct ieee8021QBridgeFdbTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeFdbTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeFdbTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeFdbTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeFdbTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeFdbTable(int force)
{
	if (!force && ieee8021QBridgeFdbTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeFdbTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeFdbTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeFdbTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeFdbTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeFdbTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeFdbTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeFdbTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeFdbTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeFdbTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEFDBDYNAMICCOUNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeFdbDynamicCount);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeFdbDynamicCount;
		break;
	case (u_char) IEEE8021QBRIDGEFDBLEARNEDENTRYDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeFdbLearnedEntryDiscards);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeFdbLearnedEntryDiscards;
		break;
	case (u_char) IEEE8021QBRIDGEFDBAGINGTIME:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeFdbAgingTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeFdbAgingTime);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeFdbAgingTime;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, struct ieee8021QBridgeTpFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, struct ieee8021QBridgeTpFdbTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, struct ieee8021QBridgeTpFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeTpFdbTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, struct ieee8021QBridgeTpFdbTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeTpFdbTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, struct ieee8021QBridgeTpFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, struct ieee8021QBridgeTpFdbTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeTpFdbTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeTpFdbTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeTpFdbTable_data *
refresh_ieee8021QBridgeTpFdbTable_row(struct ieee8021QBridgeTpFdbTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeTpFdbTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeTpFdbTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeTpFdbTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeTpFdbTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeTpFdbTable(int force)
{
	if (!force && ieee8021QBridgeTpFdbTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeTpFdbTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeTpFdbTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeTpFdbTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeTpFdbTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeTpFdbTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeTpFdbTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeTpFdbTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeTpFdbTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeTpFdbTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGETPFDBPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeTpFdbPort);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeTpFdbPort;
		break;
	case (u_char) IEEE8021QBRIDGETPFDBSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeTpFdbStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeTpFdbStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, struct ieee8021QBridgeTpGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, struct ieee8021QBridgeTpGroupTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, struct ieee8021QBridgeTpGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeTpGroupTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, struct ieee8021QBridgeTpGroupTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeTpGroupTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, struct ieee8021QBridgeTpGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, struct ieee8021QBridgeTpGroupTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeTpGroupTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeTpGroupTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeTpGroupTable_data *
refresh_ieee8021QBridgeTpGroupTable_row(struct ieee8021QBridgeTpGroupTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeTpGroupTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeTpGroupTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeTpGroupTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeTpGroupTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeTpGroupTable(int force)
{
	if (!force && ieee8021QBridgeTpGroupTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeTpGroupTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeTpGroupTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeTpGroupTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeTpGroupTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeTpGroupTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeTpGroupTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeTpGroupTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeTpGroupTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeTpGroupTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGETPGROUPEGRESSPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeTpGroupEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeTpGroupEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGETPGROUPLEARNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeTpGroupLearntLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeTpGroupLearnt;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, struct ieee8021QBridgeForwardAllTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, struct ieee8021QBridgeForwardAllTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, struct ieee8021QBridgeForwardAllTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeForwardAllTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, struct ieee8021QBridgeForwardAllTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeForwardAllTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, struct ieee8021QBridgeForwardAllTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, struct ieee8021QBridgeForwardAllTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeForwardAllTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeForwardAllTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeForwardAllTable_data *
refresh_ieee8021QBridgeForwardAllTable_row(struct ieee8021QBridgeForwardAllTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeForwardAllTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeForwardAllTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeForwardAllTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeForwardAllTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeForwardAllTable(int force)
{
	if (!force && ieee8021QBridgeForwardAllTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeForwardAllTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeForwardAllTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeForwardAllTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeForwardAllTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeForwardAllTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeForwardAllTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeForwardAllTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeForwardAllTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEFORWARDALLPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeForwardAllPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeForwardAllPorts;
		break;
	case (u_char) IEEE8021QBRIDGEFORWARDALLSTATICPORTS:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeForwardAllStaticPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeForwardAllStaticPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeForwardAllStaticPorts;
		break;
	case (u_char) IEEE8021QBRIDGEFORWARDALLFORBIDDENPORTS:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeForwardAllForbiddenPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeForwardAllForbiddenPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, struct ieee8021QBridgeForwardUnregisteredTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, struct ieee8021QBridgeForwardUnregisteredTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, struct ieee8021QBridgeForwardUnregisteredTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeForwardUnregisteredTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, struct ieee8021QBridgeForwardUnregisteredTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeForwardUnregisteredTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, struct ieee8021QBridgeForwardUnregisteredTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, struct ieee8021QBridgeForwardUnregisteredTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeForwardUnregisteredTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeForwardUnregisteredTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeForwardUnregisteredTable_data *
refresh_ieee8021QBridgeForwardUnregisteredTable_row(struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeForwardUnregisteredTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeForwardUnregisteredTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeForwardUnregisteredTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeForwardUnregisteredTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeForwardUnregisteredTable(int force)
{
	if (!force && ieee8021QBridgeForwardUnregisteredTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeForwardUnregisteredTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeForwardUnregisteredTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeForwardUnregisteredTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeForwardUnregisteredTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeForwardUnregisteredTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeForwardUnregisteredTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeForwardUnregisteredTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEFORWARDUNREGISTEREDPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeForwardUnregisteredPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeForwardUnregisteredPorts;
		break;
	case (u_char) IEEE8021QBRIDGEFORWARDUNREGISTEREDSTATICPORTS:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeForwardUnregisteredStaticPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts;
		break;
	case (u_char) IEEE8021QBRIDGEFORWARDUNREGISTEREDFORBIDDENPORTS:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeForwardUnregisteredForbiddenPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, struct ieee8021QBridgeStaticUnicastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, struct ieee8021QBridgeStaticUnicastTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, struct ieee8021QBridgeStaticUnicastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeStaticUnicastTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, struct ieee8021QBridgeStaticUnicastTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeStaticUnicastTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, struct ieee8021QBridgeStaticUnicastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, struct ieee8021QBridgeStaticUnicastTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeStaticUnicastTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeStaticUnicastTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeStaticUnicastTable_data *
refresh_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeStaticUnicastTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeStaticUnicastTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeStaticUnicastTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeStaticUnicastTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeStaticUnicastTable(int force)
{
	if (!force && ieee8021QBridgeStaticUnicastTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeStaticUnicastTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeStaticUnicastTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeStaticUnicastTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeStaticUnicastTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeStaticUnicastTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeStaticUnicastTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeStaticUnicastTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGESTATICUNICASTSTATICEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeStaticUnicastStaticEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGESTATICUNICASTFORBIDDENEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeStaticUnicastForbiddenEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGESTATICUNICASTSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021QBridgeStaticUnicastStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeStaticUnicastStorageType);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeStaticUnicastStorageType;
		break;
	case (u_char) IEEE8021QBRIDGESTATICUNICASTROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeStaticUnicastRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeStaticUnicastRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeStaticUnicastRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, struct ieee8021QBridgeStaticMulticastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, struct ieee8021QBridgeStaticMulticastTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, struct ieee8021QBridgeStaticMulticastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeStaticMulticastTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, struct ieee8021QBridgeStaticMulticastTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeStaticMulticastTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, struct ieee8021QBridgeStaticMulticastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, struct ieee8021QBridgeStaticMulticastTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeStaticMulticastTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeStaticMulticastTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeStaticMulticastTable_data *
refresh_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeStaticMulticastTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeStaticMulticastTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeStaticMulticastTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeStaticMulticastTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeStaticMulticastTable(int force)
{
	if (!force && ieee8021QBridgeStaticMulticastTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeStaticMulticastTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeStaticMulticastTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeStaticMulticastTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeStaticMulticastTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeStaticMulticastTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeStaticMulticastTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeStaticMulticastTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGESTATICMULTICASTSTATICEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeStaticMulticastStaticEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGESTATICMULTICASTFORBIDDENEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeStaticMulticastForbiddenEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGESTATICMULTICASTSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021QBridgeStaticMulticastStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeStaticMulticastStorageType);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeStaticMulticastStorageType;
		break;
	case (u_char) IEEE8021QBRIDGESTATICMULTICASTROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeStaticMulticastRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeStaticMulticastRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeStaticMulticastRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, struct ieee8021QBridgeVlanCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, struct ieee8021QBridgeVlanCurrentTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, struct ieee8021QBridgeVlanCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeVlanCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, struct ieee8021QBridgeVlanCurrentTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeVlanCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, struct ieee8021QBridgeVlanCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, struct ieee8021QBridgeVlanCurrentTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeVlanCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeVlanCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeVlanCurrentTable_data *
refresh_ieee8021QBridgeVlanCurrentTable_row(struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeVlanCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeVlanCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeVlanCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeVlanCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeVlanCurrentTable(int force)
{
	if (!force && ieee8021QBridgeVlanCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeVlanCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeVlanCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeVlanCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeVlanCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeVlanCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeVlanCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeVlanCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeVlanCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeVlanCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEVLANFDBID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVlanFdbId);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVlanFdbId;
		break;
	case (u_char) IEEE8021QBRIDGEVLANCURRENTEGRESSPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeVlanCurrentEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeVlanCurrentEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGEVLANUNTAGGEDPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeVlanUntaggedPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeVlanUntaggedPorts;
		break;
	case (u_char) IEEE8021QBRIDGEVLANSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVlanStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVlanStatus;
		break;
	case (u_char) IEEE8021QBRIDGEVLANCREATIONTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVlanCreationTime);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVlanCreationTime;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, struct ieee8021QBridgeVlanStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, struct ieee8021QBridgeVlanStaticTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, struct ieee8021QBridgeVlanStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeVlanStaticTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, struct ieee8021QBridgeVlanStaticTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeVlanStaticTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, struct ieee8021QBridgeVlanStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, struct ieee8021QBridgeVlanStaticTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeVlanStaticTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeVlanStaticTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeVlanStaticTable_data *
refresh_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeVlanStaticTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeVlanStaticTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeVlanStaticTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeVlanStaticTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeVlanStaticTable(int force)
{
	if (!force && ieee8021QBridgeVlanStaticTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeVlanStaticTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeVlanStaticTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeVlanStaticTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeVlanStaticTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeVlanStaticTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeVlanStaticTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeVlanStaticTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeVlanStaticTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEVLANSTATICNAME:	/* Create */
		*write_method = write_ieee8021QBridgeVlanStaticName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeVlanStaticNameLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeVlanStaticName;
		break;
	case (u_char) IEEE8021QBRIDGEVLANSTATICEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeVlanStaticEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeVlanStaticEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeVlanStaticEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGEVLANFORBIDDENEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeVlanForbiddenEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeVlanForbiddenEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts;
		break;
	case (u_char) IEEE8021QBRIDGEVLANSTATICUNTAGGEDPORTS:	/* Create */
		*write_method = write_ieee8021QBridgeVlanStaticUntaggedPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeVlanStaticUntaggedPortsLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts;
		break;
	case (u_char) IEEE8021QBRIDGEVLANSTATICROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeVlanStaticRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVlanStaticRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVlanStaticRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeNextFreeLocalVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeNextFreeLocalVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeNextFreeLocalVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeNextFreeLocalVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeNextFreeLocalVlanTable_data *
refresh_ieee8021QBridgeNextFreeLocalVlanTable_row(struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeNextFreeLocalVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeNextFreeLocalVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeNextFreeLocalVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeNextFreeLocalVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeNextFreeLocalVlanTable(int force)
{
	if (!force && ieee8021QBridgeNextFreeLocalVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeNextFreeLocalVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeNextFreeLocalVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeNextFreeLocalVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeNextFreeLocalVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeNextFreeLocalVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeNextFreeLocalVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeNextFreeLocalVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeNextFreeLocalVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeNextFreeLocalVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGENEXTFREELOCALVLANINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeNextFreeLocalVlanIndex);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeNextFreeLocalVlanIndex;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, struct ieee8021QBridgePortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, struct ieee8021QBridgePortVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, struct ieee8021QBridgePortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgePortVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, struct ieee8021QBridgePortVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgePortVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, struct ieee8021QBridgePortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, struct ieee8021QBridgePortVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgePortVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgePortVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgePortVlanTable_data *
refresh_ieee8021QBridgePortVlanTable_row(struct ieee8021QBridgePortVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgePortVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgePortVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgePortVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgePortVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgePortVlanTable(int force)
{
	if (!force && ieee8021QBridgePortVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgePortVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgePortVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgePortVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgePortVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgePortVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgePortVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgePortVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgePortVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEPVID:	/* ReadWrite */
		*write_method = write_ieee8021QBridgePvid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgePvid);
		rval = (u_char *) &StorageTmp->ieee8021QBridgePvid;
		break;
	case (u_char) IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES:	/* ReadWrite */
		*write_method = write_ieee8021QBridgePortAcceptableFrameTypes;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgePortAcceptableFrameTypes);
		rval = (u_char *) &StorageTmp->ieee8021QBridgePortAcceptableFrameTypes;
		break;
	case (u_char) IEEE8021QBRIDGEPORTINGRESSFILTERING:	/* ReadWrite */
		*write_method = write_ieee8021QBridgePortIngressFiltering;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgePortIngressFiltering);
		rval = (u_char *) &StorageTmp->ieee8021QBridgePortIngressFiltering;
		break;
	case (u_char) IEEE8021QBRIDGEPORTMVRPENABLEDSTATUS:	/* ReadWrite */
		*write_method = write_ieee8021QBridgePortMvrpEnabledStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgePortMvrpEnabledStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgePortMvrpEnabledStatus;
		break;
	case (u_char) IEEE8021QBRIDGEPORTMVRPFAILEDREGISTRATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgePortMvrpFailedRegistrations);
		rval = (u_char *) &StorageTmp->ieee8021QBridgePortMvrpFailedRegistrations;
		break;
	case (u_char) IEEE8021QBRIDGEPORTMVRPLASTPDUORIGIN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgePortMvrpLastPduOriginLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgePortMvrpLastPduOrigin;
		break;
	case (u_char) IEEE8021QBRIDGEPORTRESTRICTEDVLANREGISTRATION:	/* ReadWrite */
		*write_method = write_ieee8021QBridgePortRestrictedVlanRegistration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration);
		rval = (u_char *) &StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanStatisticsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanStatisticsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanStatisticsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgePortVlanStatisticsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanStatisticsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgePortVlanStatisticsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanStatisticsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanStatisticsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgePortVlanStatisticsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgePortVlanStatisticsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgePortVlanStatisticsTable_data *
refresh_ieee8021QBridgePortVlanStatisticsTable_row(struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgePortVlanStatisticsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgePortVlanStatisticsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgePortVlanStatisticsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgePortVlanStatisticsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgePortVlanStatisticsTable(int force)
{
	if (!force && ieee8021QBridgePortVlanStatisticsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgePortVlanStatisticsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgePortVlanStatisticsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgePortVlanStatisticsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgePortVlanStatisticsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgePortVlanStatisticsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgePortVlanStatisticsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgePortVlanStatisticsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgePortVlanStatisticsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgePortVlanStatisticsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGETPVLANPORTINFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeTpVlanPortInFrames);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeTpVlanPortInFrames;
		break;
	case (u_char) IEEE8021QBRIDGETPVLANPORTOUTFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeTpVlanPortOutFrames);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeTpVlanPortOutFrames;
		break;
	case (u_char) IEEE8021QBRIDGETPVLANPORTINDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeTpVlanPortInDiscards);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeTpVlanPortInDiscards;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeLearningConstraintsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeLearningConstraintsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeLearningConstraintsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeLearningConstraintsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeLearningConstraintsTable_data *
refresh_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeLearningConstraintsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeLearningConstraintsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeLearningConstraintsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeLearningConstraintsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeLearningConstraintsTable(int force)
{
	if (!force && ieee8021QBridgeLearningConstraintsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeLearningConstraintsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeLearningConstraintsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeLearningConstraintsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeLearningConstraintsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeLearningConstraintsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeLearningConstraintsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeLearningConstraintsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTSTYPE:	/* Create */
		*write_method = write_ieee8021QBridgeLearningConstraintsType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeLearningConstraintsType);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeLearningConstraintsType;
		break;
	case (u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTSSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeLearningConstraintsStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeLearningConstraintsStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeLearningConstraintsStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp,
							  struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeLearningConstraintsDefaultsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp,
							   struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeLearningConstraintsDefaultsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp, struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp,
							   struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeLearningConstraintsDefaultsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *
refresh_ieee8021QBridgeLearningConstraintsDefaultsTable_row(struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeLearningConstraintsDefaultsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeLearningConstraintsDefaultsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeLearningConstraintsDefaultsTable(int force)
{
	if (!force && ieee8021QBridgeLearningConstraintsDefaultsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeLearningConstraintsDefaultsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeLearningConstraintsDefaultsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeLearningConstraintsDefaultsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeLearningConstraintsDefaultsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeLearningConstraintsDefaultsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeLearningConstraintsDefaultsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeLearningConstraintsDefaultsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTSET:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeLearningConstraintDefaultSet;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet;
		break;
	case (u_char) IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTTYPE:	/* ReadWrite */
		*write_method = write_ieee8021QBridgeLearningConstraintDefaultType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeLearningConstraintDefaultType);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeLearningConstraintDefaultType;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, struct ieee8021QBridgeProtocolGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, struct ieee8021QBridgeProtocolGroupTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, struct ieee8021QBridgeProtocolGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeProtocolGroupTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, struct ieee8021QBridgeProtocolGroupTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeProtocolGroupTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, struct ieee8021QBridgeProtocolGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, struct ieee8021QBridgeProtocolGroupTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeProtocolGroupTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeProtocolGroupTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeProtocolGroupTable_data *
refresh_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeProtocolGroupTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeProtocolGroupTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeProtocolGroupTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeProtocolGroupTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeProtocolGroupTable(int force)
{
	if (!force && ieee8021QBridgeProtocolGroupTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeProtocolGroupTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeProtocolGroupTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeProtocolGroupTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeProtocolGroupTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeProtocolGroupTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeProtocolGroupTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeProtocolGroupTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeProtocolGroupTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEPROTOCOLGROUPID:	/* Create */
		*write_method = write_ieee8021QBridgeProtocolGroupId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeProtocolGroupId);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeProtocolGroupId;
		break;
	case (u_char) IEEE8021QBRIDGEPROTOCOLGROUPROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeProtocolGroupRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeProtocolGroupRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeProtocolGroupRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, struct ieee8021QBridgeProtocolPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, struct ieee8021QBridgeProtocolPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, struct ieee8021QBridgeProtocolPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeProtocolPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, struct ieee8021QBridgeProtocolPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeProtocolPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, struct ieee8021QBridgeProtocolPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, struct ieee8021QBridgeProtocolPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeProtocolPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeProtocolPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeProtocolPortTable_data *
refresh_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeProtocolPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeProtocolPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeProtocolPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeProtocolPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeProtocolPortTable(int force)
{
	if (!force && ieee8021QBridgeProtocolPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeProtocolPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeProtocolPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeProtocolPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeProtocolPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeProtocolPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeProtocolPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeProtocolPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeProtocolPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEPROTOCOLPORTGROUPVID:	/* Create */
		*write_method = write_ieee8021QBridgeProtocolPortGroupVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeProtocolPortGroupVid);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeProtocolPortGroupVid;
		break;
	case (u_char) IEEE8021QBRIDGEPROTOCOLPORTROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeProtocolPortRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeProtocolPortRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeProtocolPortRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, struct ieee8021QBridgeVIDXTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, struct ieee8021QBridgeVIDXTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, struct ieee8021QBridgeVIDXTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeVIDXTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, struct ieee8021QBridgeVIDXTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeVIDXTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, struct ieee8021QBridgeVIDXTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, struct ieee8021QBridgeVIDXTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeVIDXTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeVIDXTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeVIDXTable_data *
refresh_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeVIDXTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeVIDXTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeVIDXTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeVIDXTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeVIDXTable(int force)
{
	if (!force && ieee8021QBridgeVIDXTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeVIDXTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeVIDXTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeVIDXTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeVIDXTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeVIDXTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeVIDXTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeVIDXTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeVIDXTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeVIDXTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEVIDXRELAYVID:	/* Create */
		*write_method = write_ieee8021QBridgeVIDXRelayVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVIDXRelayVid);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVIDXRelayVid;
		break;
	case (u_char) IEEE8021QBRIDGEVIDXROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeVIDXRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeVIDXRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeVIDXRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, struct ieee8021QBridgeEgressVidXTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, struct ieee8021QBridgeEgressVidXTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, struct ieee8021QBridgeEgressVidXTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeEgressVidXTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, struct ieee8021QBridgeEgressVidXTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeEgressVidXTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, struct ieee8021QBridgeEgressVidXTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, struct ieee8021QBridgeEgressVidXTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeEgressVidXTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeEgressVidXTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeEgressVidXTable_data *
refresh_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeEgressVidXTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeEgressVidXTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeEgressVidXTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeEgressVidXTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeEgressVidXTable(int force)
{
	if (!force && ieee8021QBridgeEgressVidXTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeEgressVidXTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeEgressVidXTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeEgressVidXTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021QBridgeMib above.
 */
u_char *
var_ieee8021QBridgeEgressVidXTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021QBridgeMib", "var_ieee8021QBridgeEgressVidXTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeEgressVidXTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeEgressVidXTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeEgressVidXTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEEGRESSVIDXLOCALVID:	/* Create */
		*write_method = write_ieee8021QBridgeEgressVidXLocalVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeEgressVidXLocalVid);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeEgressVidXLocalVid;
		break;
	case (u_char) IEEE8021QBRIDGEEGRESSVIDXROWSTATUS:	/* Create */
		*write_method = write_ieee8021QBridgeEgressVidXRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeEgressVidXRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeEgressVidXRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021QBridgeMvrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeMvrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeMvrpEnabledStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeMvrpEnabledStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeMvrpEnabledStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeMvrpEnabledStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old = ieee8021QBridgeTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeTable_rsvs++;
		StorageTmp->ieee8021QBridgeMvrpEnabledStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeMvrpEnabledStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeTable_sets == 0)
				if ((ret = update_ieee8021QBridgeTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old) != NULL) {
			ieee8021QBridgeTable_destroy(&StorageTmp->ieee8021QBridgeTable_old);
			StorageTmp->ieee8021QBridgeTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeTable_tsts = 0;
			StorageTmp->ieee8021QBridgeTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeTable_sets == 0)
			revert_ieee8021QBridgeTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeMvrpEnabledStatus = StorageOld->ieee8021QBridgeMvrpEnabledStatus;
		if (--StorageTmp->ieee8021QBridgeTable_rsvs == 0)
			ieee8021QBridgeTable_destroy(&StorageTmp->ieee8021QBridgeTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeFdbAgingTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeFdbAgingTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeFdbTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeFdbAgingTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeFdbTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeFdbAgingTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeFdbAgingTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 10..1000000 */
		if ((10 > set_value || set_value > 1000000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeFdbAgingTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeFdbTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old = ieee8021QBridgeFdbTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeFdbTable_rsvs++;
		StorageTmp->ieee8021QBridgeFdbAgingTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeFdbTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeFdbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeFdbTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeFdbAgingTime for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeFdbTable_sets == 0)
				if ((ret = update_ieee8021QBridgeFdbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeFdbTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old) != NULL) {
			ieee8021QBridgeFdbTable_destroy(&StorageTmp->ieee8021QBridgeFdbTable_old);
			StorageTmp->ieee8021QBridgeFdbTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeFdbTable_tsts = 0;
			StorageTmp->ieee8021QBridgeFdbTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeFdbTable_sets == 0)
			revert_ieee8021QBridgeFdbTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeFdbTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeFdbAgingTime = StorageOld->ieee8021QBridgeFdbAgingTime;
		if (--StorageTmp->ieee8021QBridgeFdbTable_rsvs == 0)
			ieee8021QBridgeFdbTable_destroy(&StorageTmp->ieee8021QBridgeFdbTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeForwardAllStaticPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeForwardAllStaticPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeForwardAllStaticPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeForwardAllTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardAllStaticPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardAllStaticPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeForwardAllTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old = ieee8021QBridgeForwardAllTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeForwardAllTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllStaticPorts);
		StorageTmp->ieee8021QBridgeForwardAllStaticPorts = string;
		StorageTmp->ieee8021QBridgeForwardAllStaticPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeForwardAllTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeForwardAllTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardAllTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeForwardAllStaticPorts for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeForwardAllTable_sets == 0)
				if ((ret = update_ieee8021QBridgeForwardAllTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardAllTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) != NULL) {
			ieee8021QBridgeForwardAllTable_destroy(&StorageTmp->ieee8021QBridgeForwardAllTable_old);
			StorageTmp->ieee8021QBridgeForwardAllTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeForwardAllTable_tsts = 0;
			StorageTmp->ieee8021QBridgeForwardAllTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeForwardAllTable_sets == 0)
			revert_ieee8021QBridgeForwardAllTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeForwardAllStaticPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllStaticPorts);
			StorageTmp->ieee8021QBridgeForwardAllStaticPorts = StorageOld->ieee8021QBridgeForwardAllStaticPorts;
			StorageTmp->ieee8021QBridgeForwardAllStaticPortsLen = StorageOld->ieee8021QBridgeForwardAllStaticPortsLen;
			StorageOld->ieee8021QBridgeForwardAllStaticPorts = NULL;
			StorageOld->ieee8021QBridgeForwardAllStaticPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeForwardAllTable_rsvs == 0)
			ieee8021QBridgeForwardAllTable_destroy(&StorageTmp->ieee8021QBridgeForwardAllTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeForwardAllForbiddenPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeForwardAllForbiddenPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeForwardAllTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeForwardAllForbiddenPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeForwardAllTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardAllForbiddenPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardAllForbiddenPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeForwardAllTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old = ieee8021QBridgeForwardAllTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeForwardAllTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts);
		StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts = string;
		StorageTmp->ieee8021QBridgeForwardAllForbiddenPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeForwardAllTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeForwardAllTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardAllTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeForwardAllTable_sets == 0)
				if ((ret = update_ieee8021QBridgeForwardAllTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardAllTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) != NULL) {
			ieee8021QBridgeForwardAllTable_destroy(&StorageTmp->ieee8021QBridgeForwardAllTable_old);
			StorageTmp->ieee8021QBridgeForwardAllTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeForwardAllTable_tsts = 0;
			StorageTmp->ieee8021QBridgeForwardAllTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeForwardAllTable_sets == 0)
			revert_ieee8021QBridgeForwardAllTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardAllTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeForwardAllForbiddenPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts);
			StorageTmp->ieee8021QBridgeForwardAllForbiddenPorts = StorageOld->ieee8021QBridgeForwardAllForbiddenPorts;
			StorageTmp->ieee8021QBridgeForwardAllForbiddenPortsLen = StorageOld->ieee8021QBridgeForwardAllForbiddenPortsLen;
			StorageOld->ieee8021QBridgeForwardAllForbiddenPorts = NULL;
			StorageOld->ieee8021QBridgeForwardAllForbiddenPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeForwardAllTable_rsvs == 0)
			ieee8021QBridgeForwardAllTable_destroy(&StorageTmp->ieee8021QBridgeForwardAllTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeForwardUnregisteredStaticPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeForwardUnregisteredStaticPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeForwardUnregisteredStaticPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeForwardUnregisteredTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardUnregisteredStaticPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardUnregisteredStaticPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old = ieee8021QBridgeForwardUnregisteredTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts);
		StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts = string;
		StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeForwardUnregisteredTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets == 0)
				if ((ret = update_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) != NULL) {
			ieee8021QBridgeForwardUnregisteredTable_destroy(&StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old);
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_tsts = 0;
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets == 0)
			revert_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeForwardUnregisteredStaticPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts);
			StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPorts = StorageOld->ieee8021QBridgeForwardUnregisteredStaticPorts;
			StorageTmp->ieee8021QBridgeForwardUnregisteredStaticPortsLen = StorageOld->ieee8021QBridgeForwardUnregisteredStaticPortsLen;
			StorageOld->ieee8021QBridgeForwardUnregisteredStaticPorts = NULL;
			StorageOld->ieee8021QBridgeForwardUnregisteredStaticPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs == 0)
			ieee8021QBridgeForwardUnregisteredTable_destroy(&StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeForwardUnregisteredForbiddenPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeForwardUnregisteredForbiddenPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeForwardUnregisteredTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeForwardUnregisteredForbiddenPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeForwardUnregisteredTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardUnregisteredForbiddenPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeForwardUnregisteredForbiddenPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old = ieee8021QBridgeForwardUnregisteredTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts);
		StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts = string;
		StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeForwardUnregisteredTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets == 0)
				if ((ret = update_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) != NULL) {
			ieee8021QBridgeForwardUnregisteredTable_destroy(&StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old);
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_tsts = 0;
			StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeForwardUnregisteredTable_sets == 0)
			revert_ieee8021QBridgeForwardUnregisteredTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeForwardUnregisteredForbiddenPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts);
			StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPorts = StorageOld->ieee8021QBridgeForwardUnregisteredForbiddenPorts;
			StorageTmp->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen = StorageOld->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen;
			StorageOld->ieee8021QBridgeForwardUnregisteredForbiddenPorts = NULL;
			StorageOld->ieee8021QBridgeForwardUnregisteredForbiddenPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeForwardUnregisteredTable_rsvs == 0)
			ieee8021QBridgeForwardUnregisteredTable_destroy(&StorageTmp->ieee8021QBridgeForwardUnregisteredTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticUnicastStaticEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeStaticUnicastStaticEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticUnicastStaticEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeStaticUnicastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStaticEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStaticEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStaticEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old = ieee8021QBridgeStaticUnicastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts);
		StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts = string;
		StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_sets == 0)
				if ((ret = update_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts = 0;
			StorageTmp->ieee8021QBridgeStaticUnicastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_sets == 0)
			revert_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeStaticUnicastStaticEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts);
			StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPorts = StorageOld->ieee8021QBridgeStaticUnicastStaticEgressPorts;
			StorageTmp->ieee8021QBridgeStaticUnicastStaticEgressPortsLen = StorageOld->ieee8021QBridgeStaticUnicastStaticEgressPortsLen;
			StorageOld->ieee8021QBridgeStaticUnicastStaticEgressPorts = NULL;
			StorageOld->ieee8021QBridgeStaticUnicastStaticEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticUnicastForbiddenEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeStaticUnicastForbiddenEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticUnicastForbiddenEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeStaticUnicastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastForbiddenEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastForbiddenEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastForbiddenEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old = ieee8021QBridgeStaticUnicastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts);
		StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts = string;
		StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_sets == 0)
				if ((ret = update_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts = 0;
			StorageTmp->ieee8021QBridgeStaticUnicastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_sets == 0)
			revert_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeStaticUnicastForbiddenEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts);
			StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPorts = StorageOld->ieee8021QBridgeStaticUnicastForbiddenEgressPorts;
			StorageTmp->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen = StorageOld->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen;
			StorageOld->ieee8021QBridgeStaticUnicastForbiddenEgressPorts = NULL;
			StorageOld->ieee8021QBridgeStaticUnicastForbiddenEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticUnicastStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeStaticUnicastStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticUnicastStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeStaticUnicastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old = ieee8021QBridgeStaticUnicastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs++;
		StorageTmp->ieee8021QBridgeStaticUnicastStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticUnicastStorageType for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_sets == 0)
				if ((ret = update_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
			StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts = 0;
			StorageTmp->ieee8021QBridgeStaticUnicastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_sets == 0)
			revert_ieee8021QBridgeStaticUnicastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeStaticUnicastStorageType = StorageOld->ieee8021QBridgeStaticUnicastStorageType;
		if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticMulticastStaticEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeStaticMulticastStaticEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticMulticastStaticEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeStaticMulticastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStaticEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStaticEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStaticEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old = ieee8021QBridgeStaticMulticastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts);
		StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts = string;
		StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_sets == 0)
				if ((ret = update_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts = 0;
			StorageTmp->ieee8021QBridgeStaticMulticastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_sets == 0)
			revert_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeStaticMulticastStaticEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts);
			StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPorts = StorageOld->ieee8021QBridgeStaticMulticastStaticEgressPorts;
			StorageTmp->ieee8021QBridgeStaticMulticastStaticEgressPortsLen = StorageOld->ieee8021QBridgeStaticMulticastStaticEgressPortsLen;
			StorageOld->ieee8021QBridgeStaticMulticastStaticEgressPorts = NULL;
			StorageOld->ieee8021QBridgeStaticMulticastStaticEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticMulticastForbiddenEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeStaticMulticastForbiddenEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticMulticastForbiddenEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeStaticMulticastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastForbiddenEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastForbiddenEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastForbiddenEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old = ieee8021QBridgeStaticMulticastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts);
		StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts = string;
		StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts for you to use, and you have just been asked to do something with it. 
				   Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_sets == 0)
				if ((ret = update_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts = 0;
			StorageTmp->ieee8021QBridgeStaticMulticastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_sets == 0)
			revert_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeStaticMulticastForbiddenEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts);
			StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPorts = StorageOld->ieee8021QBridgeStaticMulticastForbiddenEgressPorts;
			StorageTmp->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen = StorageOld->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen;
			StorageOld->ieee8021QBridgeStaticMulticastForbiddenEgressPorts = NULL;
			StorageOld->ieee8021QBridgeStaticMulticastForbiddenEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticMulticastStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeStaticMulticastStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticMulticastStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeStaticMulticastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old = ieee8021QBridgeStaticMulticastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs++;
		StorageTmp->ieee8021QBridgeStaticMulticastStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticMulticastStorageType for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_sets == 0)
				if ((ret = update_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
			StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts = 0;
			StorageTmp->ieee8021QBridgeStaticMulticastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_sets == 0)
			revert_ieee8021QBridgeStaticMulticastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeStaticMulticastStorageType = StorageOld->ieee8021QBridgeStaticMulticastStorageType;
		if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVlanStaticName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeVlanStaticName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVlanStaticName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeVlanStaticTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeVlanStaticRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old = ieee8021QBridgeVlanStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticName);
		StorageTmp->ieee8021QBridgeVlanStaticName = string;
		StorageTmp->ieee8021QBridgeVlanStaticNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeVlanStaticName for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
				if ((ret = update_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
			revert_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeVlanStaticName != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticName);
			StorageTmp->ieee8021QBridgeVlanStaticName = StorageOld->ieee8021QBridgeVlanStaticName;
			StorageTmp->ieee8021QBridgeVlanStaticNameLen = StorageOld->ieee8021QBridgeVlanStaticNameLen;
			StorageOld->ieee8021QBridgeVlanStaticName = NULL;
			StorageOld->ieee8021QBridgeVlanStaticNameLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVlanStaticEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeVlanStaticEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVlanStaticEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeVlanStaticTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeVlanStaticRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old = ieee8021QBridgeVlanStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticEgressPorts);
		StorageTmp->ieee8021QBridgeVlanStaticEgressPorts = string;
		StorageTmp->ieee8021QBridgeVlanStaticEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeVlanStaticEgressPorts for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
				if ((ret = update_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
			revert_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeVlanStaticEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticEgressPorts);
			StorageTmp->ieee8021QBridgeVlanStaticEgressPorts = StorageOld->ieee8021QBridgeVlanStaticEgressPorts;
			StorageTmp->ieee8021QBridgeVlanStaticEgressPortsLen = StorageOld->ieee8021QBridgeVlanStaticEgressPortsLen;
			StorageOld->ieee8021QBridgeVlanStaticEgressPorts = NULL;
			StorageOld->ieee8021QBridgeVlanStaticEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVlanForbiddenEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeVlanForbiddenEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVlanForbiddenEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeVlanStaticTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeVlanStaticRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanForbiddenEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanForbiddenEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanForbiddenEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old = ieee8021QBridgeVlanStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts);
		StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts = string;
		StorageTmp->ieee8021QBridgeVlanForbiddenEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
				if ((ret = update_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
			revert_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeVlanForbiddenEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts);
			StorageTmp->ieee8021QBridgeVlanForbiddenEgressPorts = StorageOld->ieee8021QBridgeVlanForbiddenEgressPorts;
			StorageTmp->ieee8021QBridgeVlanForbiddenEgressPortsLen = StorageOld->ieee8021QBridgeVlanForbiddenEgressPortsLen;
			StorageOld->ieee8021QBridgeVlanForbiddenEgressPorts = NULL;
			StorageOld->ieee8021QBridgeVlanForbiddenEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVlanStaticUntaggedPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeVlanStaticUntaggedPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVlanStaticUntaggedPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeVlanStaticTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeVlanStaticRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticUntaggedPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticUntaggedPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticUntaggedPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old = ieee8021QBridgeVlanStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts);
		StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts = string;
		StorageTmp->ieee8021QBridgeVlanStaticUntaggedPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
				if ((ret = update_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
			StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_tsts = 0;
			StorageTmp->ieee8021QBridgeVlanStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_sets == 0)
			revert_ieee8021QBridgeVlanStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
			break;
		if (StorageOld->ieee8021QBridgeVlanStaticUntaggedPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts);
			StorageTmp->ieee8021QBridgeVlanStaticUntaggedPorts = StorageOld->ieee8021QBridgeVlanStaticUntaggedPorts;
			StorageTmp->ieee8021QBridgeVlanStaticUntaggedPortsLen = StorageOld->ieee8021QBridgeVlanStaticUntaggedPortsLen;
			StorageOld->ieee8021QBridgeVlanStaticUntaggedPorts = NULL;
			StorageOld->ieee8021QBridgeVlanStaticUntaggedPortsLen = 0;
		}
		if (--StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
			ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgePvid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgePvid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgePvid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgePortVlanTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePvid not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePvid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		/* Note: ranges 1..4094 4096..-1 */
		if ((1 > set_value || set_value > 4094) && (4096 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePvid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old = ieee8021QBridgePortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs++;
		StorageTmp->ieee8021QBridgePvid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgePortVlanTable_tsts == 0)
				if ((ret = check_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgePvid for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
				if ((ret = update_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_tsts = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
			revert_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgePvid = StorageOld->ieee8021QBridgePvid;
		if (--StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgePortAcceptableFrameTypes(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgePortAcceptableFrameTypes(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgePortAcceptableFrameTypes entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgePortVlanTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortAcceptableFrameTypes not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortAcceptableFrameTypes: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value admitAll */
		switch (set_value) {
		case IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES_ADMITALL:
		case IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES_ADMITUNTAGGEDANDPRIORITY:
		case IEEE8021QBRIDGEPORTACCEPTABLEFRAMETYPES_ADMITTAGGED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortAcceptableFrameTypes: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old = ieee8021QBridgePortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs++;
		StorageTmp->ieee8021QBridgePortAcceptableFrameTypes = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgePortVlanTable_tsts == 0)
				if ((ret = check_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgePortAcceptableFrameTypes for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
				if ((ret = update_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_tsts = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
			revert_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgePortAcceptableFrameTypes = StorageOld->ieee8021QBridgePortAcceptableFrameTypes;
		if (--StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgePortIngressFiltering(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgePortIngressFiltering(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgePortIngressFiltering entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgePortVlanTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortIngressFiltering not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortIngressFiltering: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortIngressFiltering: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old = ieee8021QBridgePortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs++;
		StorageTmp->ieee8021QBridgePortIngressFiltering = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgePortVlanTable_tsts == 0)
				if ((ret = check_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgePortIngressFiltering for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
				if ((ret = update_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_tsts = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
			revert_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgePortIngressFiltering = StorageOld->ieee8021QBridgePortIngressFiltering;
		if (--StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgePortMvrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgePortMvrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgePortMvrpEnabledStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgePortVlanTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortMvrpEnabledStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortMvrpEnabledStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortMvrpEnabledStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old = ieee8021QBridgePortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs++;
		StorageTmp->ieee8021QBridgePortMvrpEnabledStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgePortVlanTable_tsts == 0)
				if ((ret = check_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgePortMvrpEnabledStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
				if ((ret = update_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_tsts = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
			revert_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgePortMvrpEnabledStatus = StorageOld->ieee8021QBridgePortMvrpEnabledStatus;
		if (--StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgePortRestrictedVlanRegistration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgePortRestrictedVlanRegistration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgePortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgePortRestrictedVlanRegistration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgePortVlanTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortRestrictedVlanRegistration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortRestrictedVlanRegistration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgePortRestrictedVlanRegistration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old = ieee8021QBridgePortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs++;
		StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgePortVlanTable_tsts == 0)
				if ((ret = check_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
				if ((ret = update_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgePortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) != NULL) {
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
			StorageTmp->ieee8021QBridgePortVlanTable_rsvs = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_tsts = 0;
			StorageTmp->ieee8021QBridgePortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgePortVlanTable_sets == 0)
			revert_ieee8021QBridgePortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgePortVlanTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgePortRestrictedVlanRegistration = StorageOld->ieee8021QBridgePortRestrictedVlanRegistration;
		if (--StorageTmp->ieee8021QBridgePortVlanTable_rsvs == 0)
			ieee8021QBridgePortVlanTable_destroy(&StorageTmp->ieee8021QBridgePortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeLearningConstraintsType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeLearningConstraintsType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeLearningConstraintsType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeLearningConstraintsTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeLearningConstraintsStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintsType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintsType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintsType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021QBRIDGELEARNINGCONSTRAINTSTYPE_INDEPENDENT:
		case IEEE8021QBRIDGELEARNINGCONSTRAINTSTYPE_SHARED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintsType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old = ieee8021QBridgeLearningConstraintsTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs++;
		StorageTmp->ieee8021QBridgeLearningConstraintsType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeLearningConstraintsTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeLearningConstraintsType for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsTable_sets == 0)
				if ((ret = update_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeLearningConstraintsTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) != NULL) {
			ieee8021QBridgeLearningConstraintsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsTable_old);
			StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeLearningConstraintsTable_tsts = 0;
			StorageTmp->ieee8021QBridgeLearningConstraintsTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeLearningConstraintsTable_sets == 0)
			revert_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeLearningConstraintsType = StorageOld->ieee8021QBridgeLearningConstraintsType;
		if (--StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs == 0)
			ieee8021QBridgeLearningConstraintsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeLearningConstraintDefaultSet(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeLearningConstraintDefaultSet(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeLearningConstraintDefaultSet entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeLearningConstraintsDefaultsTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintDefaultSet not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintDefaultSet: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..65535 */
		if ((0 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintDefaultSet: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old = ieee8021QBridgeLearningConstraintsDefaultsTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs++;
		StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets == 0)
				if ((ret = update_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) != NULL) {
			ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old);
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_tsts = 0;
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets == 0)
			revert_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeLearningConstraintDefaultSet = StorageOld->ieee8021QBridgeLearningConstraintDefaultSet;
		if (--StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs == 0)
			ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeLearningConstraintDefaultType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeLearningConstraintDefaultType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeLearningConstraintsDefaultsTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeLearningConstraintDefaultType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeLearningConstraintsDefaultsTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintDefaultType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintDefaultType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTTYPE_INDEPENDENT:
		case IEEE8021QBRIDGELEARNINGCONSTRAINTDEFAULTTYPE_SHARED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintDefaultType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old = ieee8021QBridgeLearningConstraintsDefaultsTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs++;
		StorageTmp->ieee8021QBridgeLearningConstraintDefaultType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeLearningConstraintDefaultType for you to use, and you have just been asked to do something with it.  Note 
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets == 0)
				if ((ret = update_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) != NULL) {
			ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old);
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_tsts = 0;
			StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_sets == 0)
			revert_ieee8021QBridgeLearningConstraintsDefaultsTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeLearningConstraintDefaultType = StorageOld->ieee8021QBridgeLearningConstraintDefaultType;
		if (--StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_rsvs == 0)
			ieee8021QBridgeLearningConstraintsDefaultsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsDefaultsTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeProtocolGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeProtocolGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeProtocolGroupId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeProtocolGroupTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolGroupId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolGroupId not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old = ieee8021QBridgeProtocolGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs++;
		StorageTmp->ieee8021QBridgeProtocolGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeProtocolGroupTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeProtocolGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeProtocolGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeProtocolGroupId for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeProtocolGroupTable_sets == 0)
				if ((ret = update_ieee8021QBridgeProtocolGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeProtocolGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) != NULL) {
			ieee8021QBridgeProtocolGroupTable_destroy(&StorageTmp->ieee8021QBridgeProtocolGroupTable_old);
			StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeProtocolGroupTable_tsts = 0;
			StorageTmp->ieee8021QBridgeProtocolGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeProtocolGroupTable_sets == 0)
			revert_ieee8021QBridgeProtocolGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeProtocolGroupId = StorageOld->ieee8021QBridgeProtocolGroupId;
		if (--StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs == 0)
			ieee8021QBridgeProtocolGroupTable_destroy(&StorageTmp->ieee8021QBridgeProtocolGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeProtocolPortGroupVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeProtocolPortGroupVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeProtocolPortGroupVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeProtocolPortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeProtocolPortRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolPortGroupVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolPortGroupVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolPortGroupVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolPortGroupVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old = ieee8021QBridgeProtocolPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs++;
		StorageTmp->ieee8021QBridgeProtocolPortGroupVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeProtocolPortTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeProtocolPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeProtocolPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeProtocolPortGroupVid for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeProtocolPortTable_sets == 0)
				if ((ret = update_ieee8021QBridgeProtocolPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeProtocolPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) != NULL) {
			ieee8021QBridgeProtocolPortTable_destroy(&StorageTmp->ieee8021QBridgeProtocolPortTable_old);
			StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeProtocolPortTable_tsts = 0;
			StorageTmp->ieee8021QBridgeProtocolPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeProtocolPortTable_sets == 0)
			revert_ieee8021QBridgeProtocolPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeProtocolPortGroupVid = StorageOld->ieee8021QBridgeProtocolPortGroupVid;
		if (--StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs == 0)
			ieee8021QBridgeProtocolPortTable_destroy(&StorageTmp->ieee8021QBridgeProtocolPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVIDXRelayVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeVIDXRelayVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVIDXTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVIDXRelayVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeVIDXTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeVIDXRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVIDXRelayVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVIDXRelayVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVIDXRelayVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVIDXRelayVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeVIDXTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old = ieee8021QBridgeVIDXTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeVIDXTable_rsvs++;
		StorageTmp->ieee8021QBridgeVIDXRelayVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeVIDXTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeVIDXTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVIDXTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeVIDXRelayVid for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeVIDXTable_sets == 0)
				if ((ret = update_ieee8021QBridgeVIDXTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeVIDXTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) != NULL) {
			ieee8021QBridgeVIDXTable_destroy(&StorageTmp->ieee8021QBridgeVIDXTable_old);
			StorageTmp->ieee8021QBridgeVIDXTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeVIDXTable_tsts = 0;
			StorageTmp->ieee8021QBridgeVIDXTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeVIDXTable_sets == 0)
			revert_ieee8021QBridgeVIDXTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeVIDXRelayVid = StorageOld->ieee8021QBridgeVIDXRelayVid;
		if (--StorageTmp->ieee8021QBridgeVIDXTable_rsvs == 0)
			ieee8021QBridgeVIDXTable_destroy(&StorageTmp->ieee8021QBridgeVIDXTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeEgressVidXLocalVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021QBridgeEgressVidXLocalVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeEgressVidXLocalVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021QBridgeEgressVidXTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021QBridgeEgressVidXRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeEgressVidXLocalVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeEgressVidXLocalVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeEgressVidXLocalVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeEgressVidXLocalVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) == NULL)
			if (StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old = ieee8021QBridgeEgressVidXTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs++;
		StorageTmp->ieee8021QBridgeEgressVidXLocalVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021QBridgeEgressVidXTable_tsts == 0)
				if ((ret = check_ieee8021QBridgeEgressVidXTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeEgressVidXTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021QBridgeEgressVidXLocalVid for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021QBridgeEgressVidXTable_sets == 0)
				if ((ret = update_ieee8021QBridgeEgressVidXTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021QBridgeEgressVidXTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) != NULL) {
			ieee8021QBridgeEgressVidXTable_destroy(&StorageTmp->ieee8021QBridgeEgressVidXTable_old);
			StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs = 0;
			StorageTmp->ieee8021QBridgeEgressVidXTable_tsts = 0;
			StorageTmp->ieee8021QBridgeEgressVidXTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021QBridgeEgressVidXTable_sets == 0)
			revert_ieee8021QBridgeEgressVidXTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) == NULL)
			break;
		StorageTmp->ieee8021QBridgeEgressVidXLocalVid = StorageOld->ieee8021QBridgeEgressVidXLocalVid;
		if (--StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs == 0)
			ieee8021QBridgeEgressVidXTable_destroy(&StorageTmp->ieee8021QBridgeEgressVidXTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeCVlanPortTable_row(struct ieee8021QBridgeCVlanPortTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeStaticUnicastTable_row(struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeStaticMulticastTable_row(struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeVlanStaticTable_row(struct ieee8021QBridgeVlanStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeLearningConstraintsTable_row(struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeProtocolGroupTable_row(struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeProtocolPortTable_row(struct ieee8021QBridgeProtocolPortTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeVIDXTable_row(struct ieee8021QBridgeVIDXTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021QBridgeEgressVidXTable_row(struct ieee8021QBridgeEgressVidXTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeCVlanPortRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeCVlanPortRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeCVlanPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeCVlanPortTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeCVlanPortRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeCVlanPortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeCVlanPortRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeCVlanPortRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeCVlanPortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeCVlanPortNumber */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeCVlanPortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeCVlanPortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeCVlanPortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeCVlanPortNumber */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeCVlanPortNumber: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeCVlanPortNumber: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeCVlanPortTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeCVlanPortTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeCVlanPortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeCVlanPortNumber = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeCVlanPortTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeCVlanPortTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeCVlanPortTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeCVlanPortTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeCVlanPortTable_old = ieee8021QBridgeCVlanPortTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeCVlanPortTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeCVlanPortTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeCVlanPortTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeCVlanPortTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeCVlanPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeCVlanPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeCVlanPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeCVlanPortRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeCVlanPortTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeCVlanPortTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeCVlanPortTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeCVlanPortTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeCVlanPortRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeCVlanPortRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeCVlanPortRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeCVlanPortTable_old) != NULL) {
				ieee8021QBridgeCVlanPortTable_destroy(&StorageTmp->ieee8021QBridgeCVlanPortTable_old);
				StorageTmp->ieee8021QBridgeCVlanPortTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeCVlanPortTable_tsts = 0;
				StorageTmp->ieee8021QBridgeCVlanPortTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeCVlanPortTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeCVlanPortTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeCVlanPortTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeCVlanPortRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeCVlanPortTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeCVlanPortTable_del(StorageNew);
				ieee8021QBridgeCVlanPortTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeCVlanPortTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeCVlanPortTable_rsvs == 0)
				ieee8021QBridgeCVlanPortTable_destroy(&StorageTmp->ieee8021QBridgeCVlanPortTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeCVlanPortTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticUnicastRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeStaticUnicastRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeStaticUnicastTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticUnicastRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeStaticUnicastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticUnicastRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeStaticUnicastComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeStaticUnicastVlanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeStaticUnicastAddress */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeStaticUnicastReceivePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeStaticUnicastComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeStaticUnicastVlanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastVlanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastVlanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeStaticUnicastAddress */
			/* Note: ranges 6..6 */
			if (vp->val_len > SPRINT_MAX_LEN || (vp->val_len != 6)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastAddress: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeStaticUnicastReceivePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastReceivePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..65535 */
			if ((0 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticUnicastReceivePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeStaticUnicastTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeStaticUnicastTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeStaticUnicastComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeStaticUnicastVlanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->ieee8021QBridgeStaticUnicastAddress, vp->val.string, vp->val_len);
			StorageNew->ieee8021QBridgeStaticUnicastAddressLen = vp->val_len;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeStaticUnicastReceivePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeStaticUnicastTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeStaticUnicastTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old = ieee8021QBridgeStaticUnicastTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeStaticUnicastTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeStaticUnicastTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeStaticUnicastTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeStaticUnicastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeStaticUnicastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeStaticUnicastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticUnicastRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeStaticUnicastTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeStaticUnicastTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeStaticUnicastTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeStaticUnicastTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeStaticUnicastRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeStaticUnicastRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeStaticUnicastRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) != NULL) {
				ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
				StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeStaticUnicastTable_tsts = 0;
				StorageTmp->ieee8021QBridgeStaticUnicastTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeStaticUnicastTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeStaticUnicastTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeStaticUnicastTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeStaticUnicastRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeStaticUnicastTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeStaticUnicastTable_del(StorageNew);
				ieee8021QBridgeStaticUnicastTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeStaticUnicastTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeStaticUnicastTable_rsvs == 0)
				ieee8021QBridgeStaticUnicastTable_destroy(&StorageTmp->ieee8021QBridgeStaticUnicastTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeStaticUnicastTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeStaticMulticastRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeStaticMulticastRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeStaticMulticastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeStaticMulticastTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeStaticMulticastRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeStaticMulticastTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeStaticMulticastRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeVlanCurrentComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeVlanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeStaticMulticastAddress */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeStaticMulticastReceivePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeVlanCurrentComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanCurrentComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanCurrentComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeVlanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 4096..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4094) && (4096 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeStaticMulticastAddress */
			/* Note: ranges 6..6 */
			if (vp->val_len > SPRINT_MAX_LEN || (vp->val_len != 6)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticMulticastAddress: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeStaticMulticastReceivePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticMulticastReceivePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..65535 */
			if ((0 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeStaticMulticastReceivePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeStaticMulticastTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeStaticMulticastTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeVlanCurrentComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeVlanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->ieee8021QBridgeStaticMulticastAddress, vp->val.string, vp->val_len);
			StorageNew->ieee8021QBridgeStaticMulticastAddressLen = vp->val_len;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeStaticMulticastReceivePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeStaticMulticastTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeStaticMulticastTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old = ieee8021QBridgeStaticMulticastTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeStaticMulticastTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeStaticMulticastTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeStaticMulticastTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeStaticMulticastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeStaticMulticastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeStaticMulticastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeStaticMulticastRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeStaticMulticastTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeStaticMulticastTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeStaticMulticastTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeStaticMulticastTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeStaticMulticastRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeStaticMulticastRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeStaticMulticastRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) != NULL) {
				ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
				StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeStaticMulticastTable_tsts = 0;
				StorageTmp->ieee8021QBridgeStaticMulticastTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeStaticMulticastTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeStaticMulticastTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeStaticMulticastTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeStaticMulticastRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeStaticMulticastTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeStaticMulticastTable_del(StorageNew);
				ieee8021QBridgeStaticMulticastTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeStaticMulticastTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeStaticMulticastTable_rsvs == 0)
				ieee8021QBridgeStaticMulticastTable_destroy(&StorageTmp->ieee8021QBridgeStaticMulticastTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeStaticMulticastTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVlanStaticRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeVlanStaticRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVlanStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeVlanStaticTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVlanStaticRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeVlanStaticTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVlanStaticRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeVlanStaticComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeVlanStaticVlanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeVlanStaticComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanStaticComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanStaticComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeVlanStaticVlanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanStaticVlanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 4096..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4094) && (4096 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanStaticVlanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeVlanStaticTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeVlanStaticTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeVlanStaticComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeVlanStaticVlanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeVlanStaticTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeVlanStaticTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old = ieee8021QBridgeVlanStaticTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeVlanStaticTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeVlanStaticTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeVlanStaticTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeVlanStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeVlanStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeVlanStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeVlanStaticRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeVlanStaticTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeVlanStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeVlanStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeVlanStaticTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeVlanStaticRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeVlanStaticRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeVlanStaticRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) != NULL) {
				ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
				StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeVlanStaticTable_tsts = 0;
				StorageTmp->ieee8021QBridgeVlanStaticTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeVlanStaticTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeVlanStaticTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeVlanStaticTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeVlanStaticRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeVlanStaticTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeVlanStaticTable_del(StorageNew);
				ieee8021QBridgeVlanStaticTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeVlanStaticTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeVlanStaticTable_rsvs == 0)
				ieee8021QBridgeVlanStaticTable_destroy(&StorageTmp->ieee8021QBridgeVlanStaticTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeVlanStaticTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeLearningConstraintsStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeLearningConstraintsStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeLearningConstraintsTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeLearningConstraintsTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeLearningConstraintsStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeLearningConstraintsTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintsStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeLearningConstraintsStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeLearningConstraintsComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeLearningConstraintsVlan */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeLearningConstraintsSet */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeLearningConstraintsComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeLearningConstraintsComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeLearningConstraintsComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeLearningConstraintsVlan */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeLearningConstraintsVlan: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 4096..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4094) && (4096 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeLearningConstraintsVlan: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeLearningConstraintsSet */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeLearningConstraintsSet: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..65535 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeLearningConstraintsSet: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeLearningConstraintsTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeLearningConstraintsTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeLearningConstraintsComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeLearningConstraintsVlan = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeLearningConstraintsSet = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeLearningConstraintsTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeLearningConstraintsTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old = ieee8021QBridgeLearningConstraintsTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeLearningConstraintsTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeLearningConstraintsTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeLearningConstraintsStatus for you to use, and you have just been asked to do something with it.  Note that anything done
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeLearningConstraintsTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeLearningConstraintsTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeLearningConstraintsStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeLearningConstraintsStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeLearningConstraintsStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) != NULL) {
				ieee8021QBridgeLearningConstraintsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsTable_old);
				StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeLearningConstraintsTable_tsts = 0;
				StorageTmp->ieee8021QBridgeLearningConstraintsTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeLearningConstraintsTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeLearningConstraintsTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeLearningConstraintsStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeLearningConstraintsTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeLearningConstraintsTable_del(StorageNew);
				ieee8021QBridgeLearningConstraintsTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeLearningConstraintsTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeLearningConstraintsTable_rsvs == 0)
				ieee8021QBridgeLearningConstraintsTable_destroy(&StorageTmp->ieee8021QBridgeLearningConstraintsTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeLearningConstraintsTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeProtocolGroupRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeProtocolGroupRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeProtocolGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeProtocolGroupTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeProtocolGroupRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeProtocolGroupTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolGroupRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolGroupRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeProtocolGroupComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeProtocolTemplateFrameType */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeProtocolTemplateProtocolValue */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeProtocolGroupComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolGroupComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolGroupComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeProtocolTemplateFrameType */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolTemplateFrameType: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			switch ((long) *vp->val.integer) {
			case IEEE8021QBRIDGEPROTOCOLTEMPLATEFRAMETYPE_ETHERNET:
			case IEEE8021QBRIDGEPROTOCOLTEMPLATEFRAMETYPE_RFC1042:
			case IEEE8021QBRIDGEPROTOCOLTEMPLATEFRAMETYPE_SNAP8021H:
			case IEEE8021QBRIDGEPROTOCOLTEMPLATEFRAMETYPE_SNAPOTHER:
			case IEEE8021QBRIDGEPROTOCOLTEMPLATEFRAMETYPE_LLCOTHER:
				break;
			default:
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolTemplateFrameType: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeProtocolTemplateProtocolValue */
			/* Note: ranges 2..2 5..5 */
			if (vp->val_len > SPRINT_MAX_LEN || (vp->val_len != 2 && vp->val_len != 5)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolTemplateProtocolValue: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeProtocolGroupTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeProtocolGroupTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeProtocolGroupComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeProtocolTemplateFrameType = (long) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->ieee8021QBridgeProtocolTemplateProtocolValue, vp->val.string, vp->val_len);
			StorageNew->ieee8021QBridgeProtocolTemplateProtocolValueLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeProtocolGroupTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeProtocolGroupTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old = ieee8021QBridgeProtocolGroupTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeProtocolGroupTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeProtocolGroupTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeProtocolGroupTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeProtocolGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeProtocolGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeProtocolGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeProtocolGroupRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeProtocolGroupTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeProtocolGroupTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeProtocolGroupTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeProtocolGroupTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeProtocolGroupRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeProtocolGroupRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeProtocolGroupRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) != NULL) {
				ieee8021QBridgeProtocolGroupTable_destroy(&StorageTmp->ieee8021QBridgeProtocolGroupTable_old);
				StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeProtocolGroupTable_tsts = 0;
				StorageTmp->ieee8021QBridgeProtocolGroupTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeProtocolGroupTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeProtocolGroupTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeProtocolGroupTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeProtocolGroupRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeProtocolGroupTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeProtocolGroupTable_del(StorageNew);
				ieee8021QBridgeProtocolGroupTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolGroupTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeProtocolGroupTable_rsvs == 0)
				ieee8021QBridgeProtocolGroupTable_destroy(&StorageTmp->ieee8021QBridgeProtocolGroupTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeProtocolGroupTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeProtocolPortRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeProtocolPortRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeProtocolPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeProtocolPortTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeProtocolPortRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeProtocolPortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolPortRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeProtocolPortRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeProtocolPortGroupId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeProtocolPortGroupId */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolPortGroupId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeProtocolPortGroupId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeProtocolPortTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeProtocolPortTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeProtocolPortGroupId = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeProtocolPortTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeProtocolPortTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old = ieee8021QBridgeProtocolPortTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeProtocolPortTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeProtocolPortTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeProtocolPortTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeProtocolPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeProtocolPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeProtocolPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeProtocolPortRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here 
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeProtocolPortTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeProtocolPortTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeProtocolPortTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeProtocolPortTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeProtocolPortRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeProtocolPortRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeProtocolPortRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) != NULL) {
				ieee8021QBridgeProtocolPortTable_destroy(&StorageTmp->ieee8021QBridgeProtocolPortTable_old);
				StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeProtocolPortTable_tsts = 0;
				StorageTmp->ieee8021QBridgeProtocolPortTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeProtocolPortTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeProtocolPortTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeProtocolPortTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeProtocolPortRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeProtocolPortTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeProtocolPortTable_del(StorageNew);
				ieee8021QBridgeProtocolPortTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeProtocolPortTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeProtocolPortTable_rsvs == 0)
				ieee8021QBridgeProtocolPortTable_destroy(&StorageTmp->ieee8021QBridgeProtocolPortTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeProtocolPortTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeVIDXRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeVIDXRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeVIDXTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeVIDXTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeVIDXRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeVIDXTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVIDXRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeVIDXRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeVIDXLocalVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeVIDXLocalVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVIDXLocalVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVIDXLocalVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeVIDXTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeVIDXTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeVIDXLocalVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeVIDXTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeVIDXTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeVIDXTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old = ieee8021QBridgeVIDXTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeVIDXTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeVIDXTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeVIDXTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeVIDXTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeVIDXTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeVIDXTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeVIDXTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeVIDXRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeVIDXTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeVIDXTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeVIDXTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeVIDXTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeVIDXRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeVIDXRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeVIDXRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) != NULL) {
				ieee8021QBridgeVIDXTable_destroy(&StorageTmp->ieee8021QBridgeVIDXTable_old);
				StorageTmp->ieee8021QBridgeVIDXTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeVIDXTable_tsts = 0;
				StorageTmp->ieee8021QBridgeVIDXTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeVIDXTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeVIDXTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeVIDXTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeVIDXRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeVIDXTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeVIDXTable_del(StorageNew);
				ieee8021QBridgeVIDXTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeVIDXTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeVIDXTable_rsvs == 0)
				ieee8021QBridgeVIDXTable_destroy(&StorageTmp->ieee8021QBridgeVIDXTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeVIDXTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021QBridgeEgressVidXRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021QBridgeEgressVidXRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021QBridgeEgressVidXTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021QBridgeEgressVidXTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021QBridgeMib", "write_ieee8021QBridgeEgressVidXRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021QBridgeEgressVidXTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeEgressVidXRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021QBridgeEgressVidXRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021QBridgeEgressVidXRelayVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021QBridgeEgressVidXRelayVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeEgressVidXRelayVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeEgressVidXRelayVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021QBridgeEgressVidXTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021QBridgeEgressVidXTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021QBridgeEgressVidXRelayVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021QBridgeEgressVidXTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021QBridgeEgressVidXTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) == NULL)
				if (StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old = ieee8021QBridgeEgressVidXTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021QBridgeEgressVidXTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021QBridgeEgressVidXTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021QBridgeEgressVidXTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021QBridgeEgressVidXTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeEgressVidXTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021QBridgeEgressVidXTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021QBridgeEgressVidXRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021QBridgeEgressVidXTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021QBridgeEgressVidXTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021QBridgeEgressVidXTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021QBridgeEgressVidXTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021QBridgeEgressVidXRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021QBridgeEgressVidXRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021QBridgeEgressVidXRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) != NULL) {
				ieee8021QBridgeEgressVidXTable_destroy(&StorageTmp->ieee8021QBridgeEgressVidXTable_old);
				StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs = 0;
				StorageTmp->ieee8021QBridgeEgressVidXTable_tsts = 0;
				StorageTmp->ieee8021QBridgeEgressVidXTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021QBridgeEgressVidXTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021QBridgeEgressVidXTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021QBridgeEgressVidXTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021QBridgeEgressVidXRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021QBridgeEgressVidXTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021QBridgeEgressVidXTable_del(StorageNew);
				ieee8021QBridgeEgressVidXTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021QBridgeEgressVidXTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021QBridgeEgressVidXTable_rsvs == 0)
				ieee8021QBridgeEgressVidXTable_destroy(&StorageTmp->ieee8021QBridgeEgressVidXTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021QBridgeEgressVidXTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021QBridgeMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021QBridgeMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021QBridgeMibold_signal_handler != NULL)
		(*ieee8021QBridgeMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
}

/**
 * @fn void ieee8021QBridgeMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021QBridgeMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021QBridgeMib", "ieee8021QBridgeMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021QBridgeMib", "done.\n"));
	return;
}
