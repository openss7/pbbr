/*****************************************************************************

 @(#) src/snmp/ieee8021spbmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021spbmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021SpbMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021SpbMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021SpbMib_refresh = 1;
volatile int ieee8021SpbMtidStaticTable_refresh = 1;
volatile int ieee8021SpbTopIxDynamicTable_refresh = 1;
volatile int ieee8021SpbEctStaticTable_refresh = 1;
volatile int ieee8021SpbEctDynamicTable_refresh = 1;
volatile int ieee8021SpbAdjStaticTable_refresh = 1;
volatile int ieee8021SpbAdjDynamicTable_refresh = 1;
volatile int ieee8021SpbTopNodeTable_refresh = 1;
volatile int ieee8021SpbTopEctTable_refresh = 1;
volatile int ieee8021SpbTopEdgeTable_refresh = 1;
volatile int ieee8021SpbmTopSrvTable_refresh = 1;
volatile int ieee8021SpbvTopSrvTable_refresh = 1;
volatile int ieee8021SpbmBsiStaticTable_refresh = 1;
volatile int dot1agCfmMepSpbmTable_refresh = 1;
volatile int dot1agCfmMepSpbmEspTable_refresh = 1;

/*
 * ieee8021SpbMib_variables_oid: object identifier for ieee8021SpbMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021SpbMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 26 };
oid ieee8021SpbMtidStaticTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 1, 2, 1 };
oid ieee8021SpbEctStaticTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 1, 4, 1 };
oid ieee8021SpbAdjStaticTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 1, 6, 1 };
oid ieee8021SpbmBsiStaticTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 1, 13, 1 };
oid dot1agCfmMepSpbmEspTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 1, 15, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021SpbSysGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 1 };
oid ieee8021SpbMtidStaticTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 2 };
oid ieee8021SpbTopIxDynamicTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 3 };
oid ieee8021SpbEctStaticTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 4 };
oid ieee8021SpbEctDynamicTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 5 };
oid ieee8021SpbAdjStaticTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 6 };
oid ieee8021SpbAdjDynamicTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 7 };
oid ieee8021SpbTopNodeTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 8 };
oid ieee8021SpbTopEctTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 9 };
oid ieee8021SpbTopEdgeTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 10 };
oid ieee8021SpbmTopSrvTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 11 };
oid ieee8021SpbSysGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 12 };
oid ieee8021SpbMtidStaticTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 13 };
oid ieee8021SpbTopIxDynamicTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 14 };
oid ieee8021SpbEctStaticTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 15 };
oid ieee8021SpbEctDynamicTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 16 };
oid ieee8021SpbAdjStaticTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 17 };
oid ieee8021SpbAdjDynamicTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 18 };
oid ieee8021SpbTopNodeTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 19 };
oid ieee8021SpbTopEctTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 20 };
oid ieee8021SpbTopEdgeTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 21 };
oid ieee8021SpbvTopSrvTableGroupSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 22 };
oid ieee8021SpbmBsiStaticTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 23 };
oid dot1agCfmMepSpbmTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 24 };
oid dot1agCfmMepSpbmEspTableGroupSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 1, 25 };
oid ieee8021SpbComplianceSPBM_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 2, 1 };
oid ieee8021SpbComplianceSPBV_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 26, 2, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021SpbMib_variables: tree for ieee8021SpbMib
 * This variable defines function callbacks and type return information for the ieee8021SpbMib mib section
 */
struct variable4 ieee8021SpbMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021SPBSYSAREAADDRESS  1
	{(u_char) IEEE8021SPBSYSAREAADDRESS, ASN_OCTET_STR, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 1}},
#define   IEEE8021SPBSYSID      2
	{(u_char) IEEE8021SPBSYSID, ASN_OCTET_STR, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 2}},
#define   IEEE8021SPBSYSCONTROLADDR  3
	{(u_char) IEEE8021SPBSYSCONTROLADDR, ASN_OCTET_STR, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 3}},
#define   IEEE8021SPBSYSNAME    4
	{(u_char) IEEE8021SPBSYSNAME, ASN_OCTET_STR, RONLY, var_ieee8021SpbMib, 3, {1, 1, 4}},
#define   IEEE8021SPBSYSBRIDGEPRIORITY  5
	{(u_char) IEEE8021SPBSYSBRIDGEPRIORITY, ASN_OCTET_STR, RONLY, var_ieee8021SpbMib, 3, {1, 1, 5}},
#define   IEEE8021SPBMSYSSPSOURCEID  6
	{(u_char) IEEE8021SPBMSYSSPSOURCEID, ASN_OCTET_STR, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 6}},
#define   IEEE8021SPBVSYSMODE   7
	{(u_char) IEEE8021SPBVSYSMODE, ASN_INTEGER, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 7}},
#define   IEEE8021SPBMSYSMODE   8
	{(u_char) IEEE8021SPBMSYSMODE, ASN_INTEGER, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 8}},
#define   IEEE8021SPBSYSDIGESTCONVENTION  9
	{(u_char) IEEE8021SPBSYSDIGESTCONVENTION, ASN_INTEGER, RWRITE, var_ieee8021SpbMib, 3, {1, 1, 9}},
#define   IEEE8021SPBMTIDSTATICENTRYMTIDOVERLOAD  10
	{(u_char) IEEE8021SPBMTIDSTATICENTRYMTIDOVERLOAD, ASN_INTEGER, RWRITE, var_ieee8021SpbMtidStaticTable, 4, {1, 2, 1, 2}},
#define   IEEE8021SPBMTIDSTATICENTRYROWSTATUS  11
	{(u_char) IEEE8021SPBMTIDSTATICENTRYROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021SpbMtidStaticTable, 4, {1, 2, 1, 3}},
#define   IEEE8021SPBTOPIXDYNAMICENTRYAGREEDIGEST  12
	{(u_char) IEEE8021SPBTOPIXDYNAMICENTRYAGREEDIGEST, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopIxDynamicTable, 4, {1, 3, 1, 2}},
#define   IEEE8021SPBTOPIXDYNAMICENTRYMCID  13
	{(u_char) IEEE8021SPBTOPIXDYNAMICENTRYMCID, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopIxDynamicTable, 4, {1, 3, 1, 3}},
#define   IEEE8021SPBTOPIXDYNAMICENTRYAUXMCID  14
	{(u_char) IEEE8021SPBTOPIXDYNAMICENTRYAUXMCID, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopIxDynamicTable, 4, {1, 3, 1, 4}},
#define   IEEE8021SPBECTSTATICENTRYECTALGORITHM  15
	{(u_char) IEEE8021SPBECTSTATICENTRYECTALGORITHM, ASN_OCTET_STR, RWRITE, var_ieee8021SpbEctStaticTable, 4, {1, 4, 1, 3}},
#define   IEEE8021SPBVECTSTATICENTRYSPVID  16
	{(u_char) IEEE8021SPBVECTSTATICENTRYSPVID, ASN_INTEGER, RWRITE, var_ieee8021SpbEctStaticTable, 4, {1, 4, 1, 4}},
#define   IEEE8021SPBECTSTATICENTRYROWSTATUS  17
	{(u_char) IEEE8021SPBECTSTATICENTRYROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021SpbEctStaticTable, 4, {1, 4, 1, 5}},
#define   IEEE8021SPBECTDYNAMICENTRYMODE  18
	{(u_char) IEEE8021SPBECTDYNAMICENTRYMODE, ASN_INTEGER, RONLY, var_ieee8021SpbEctDynamicTable, 4, {1, 5, 1, 3}},
#define   IEEE8021SPBECTDYNAMICENTRYLOCALUSE  19
	{(u_char) IEEE8021SPBECTDYNAMICENTRYLOCALUSE, ASN_INTEGER, RONLY, var_ieee8021SpbEctDynamicTable, 4, {1, 5, 1, 4}},
#define   IEEE8021SPBECTDYNAMICENTRYREMOTEUSE  20
	{(u_char) IEEE8021SPBECTDYNAMICENTRYREMOTEUSE, ASN_INTEGER, RONLY, var_ieee8021SpbEctDynamicTable, 4, {1, 5, 1, 5}},
#define   IEEE8021SPBECTDYNAMICENTRYINGRESSCHECKDISCARDS  21
	{(u_char) IEEE8021SPBECTDYNAMICENTRYINGRESSCHECKDISCARDS, ASN_UNSIGNED, RONLY, var_ieee8021SpbEctDynamicTable, 4, {1, 5, 1, 6}},
#define   IEEE8021SPBADJSTATICENTRYMETRIC  22
	{(u_char) IEEE8021SPBADJSTATICENTRYMETRIC, ASN_INTEGER, RWRITE, var_ieee8021SpbAdjStaticTable, 4, {1, 6, 1, 3}},
#define   IEEE8021SPBADJSTATICENTRYIFADMINSTATE  23
	{(u_char) IEEE8021SPBADJSTATICENTRYIFADMINSTATE, ASN_INTEGER, RWRITE, var_ieee8021SpbAdjStaticTable, 4, {1, 6, 1, 4}},
#define   IEEE8021SPBADJSTATICENTRYROWSTATUS  24
	{(u_char) IEEE8021SPBADJSTATICENTRYROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021SpbAdjStaticTable, 4, {1, 6, 1, 5}},
#define   IEEE8021SPBADJDYNAMICENTRYPORT  25
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPORT, ASN_UNSIGNED, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 4}},
#define   IEEE8021SPBADJDYNAMICENTRYIFOPERSTATE  26
	{(u_char) IEEE8021SPBADJDYNAMICENTRYIFOPERSTATE, ASN_INTEGER, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 5}},
#define   IEEE8021SPBADJDYNAMICENTRYPEERSYSNAME  27
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPEERSYSNAME, ASN_OCTET_STR, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 6}},
#define   IEEE8021SPBADJDYNAMICENTRYPEERAGREEDIGEST  28
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPEERAGREEDIGEST, ASN_OCTET_STR, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 7}},
#define   IEEE8021SPBADJDYNAMICENTRYPEERMCID  29
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPEERMCID, ASN_OCTET_STR, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 8}},
#define   IEEE8021SPBADJDYNAMICENTRYPEERAUXMCID  30
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPEERAUXMCID, ASN_OCTET_STR, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 9}},
#define   IEEE8021SPBADJDYNAMICENTRYLOCALCIRCUITID  31
	{(u_char) IEEE8021SPBADJDYNAMICENTRYLOCALCIRCUITID, ASN_UNSIGNED, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 10}},
#define   IEEE8021SPBADJDYNAMICENTRYPEERLOCALCIRCUITID  32
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPEERLOCALCIRCUITID, ASN_UNSIGNED, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 11}},
#define   IEEE8021SPBADJDYNAMICENTRYPORTIDENTIFIER  33
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPORTIDENTIFIER, ASN_UNSIGNED, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 12}},
#define   IEEE8021SPBADJDYNAMICENTRYPEERPORTIDENTIFIER  34
	{(u_char) IEEE8021SPBADJDYNAMICENTRYPEERPORTIDENTIFIER, ASN_UNSIGNED, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 13}},
#define   IEEE8021SPBADJDYNAMICENTRYISISCIRCINDEX  35
	{(u_char) IEEE8021SPBADJDYNAMICENTRYISISCIRCINDEX, ASN_UNSIGNED, RONLY, var_ieee8021SpbAdjDynamicTable, 4, {1, 7, 1, 14}},
#define   IEEE8021SPBTOPNODEENTRYBRIDGEPRIORITY  36
	{(u_char) IEEE8021SPBTOPNODEENTRYBRIDGEPRIORITY, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopNodeTable, 4, {1, 8, 1, 3}},
#define   IEEE8021SPBMTOPNODEENTRYSPSOURCEID  37
	{(u_char) IEEE8021SPBMTOPNODEENTRYSPSOURCEID, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopNodeTable, 4, {1, 8, 1, 4}},
#define   IEEE8021SPBTOPNODEENTRYSYSNAME  38
	{(u_char) IEEE8021SPBTOPNODEENTRYSYSNAME, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopNodeTable, 4, {1, 8, 1, 5}},
#define   IEEE8021SPBTOPECTENTRYECTALGORITHM  39
	{(u_char) IEEE8021SPBTOPECTENTRYECTALGORITHM, ASN_OCTET_STR, RONLY, var_ieee8021SpbTopEctTable, 4, {1, 9, 1, 4}},
#define   IEEE8021SPBTOPECTENTRYMODE  40
	{(u_char) IEEE8021SPBTOPECTENTRYMODE, ASN_INTEGER, RONLY, var_ieee8021SpbTopEctTable, 4, {1, 9, 1, 5}},
#define   IEEE8021SPBVTOPECTSYSMODE  41
	{(u_char) IEEE8021SPBVTOPECTSYSMODE, ASN_INTEGER, RONLY, var_ieee8021SpbTopEctTable, 4, {1, 9, 1, 6}},
#define   IEEE8021SPBVTOPECTENTRYSPVID  42
	{(u_char) IEEE8021SPBVTOPECTENTRYSPVID, ASN_INTEGER, RONLY, var_ieee8021SpbTopEctTable, 4, {1, 9, 1, 7}},
#define   IEEE8021SPBTOPECTENTRYLOCALUSE  43
	{(u_char) IEEE8021SPBTOPECTENTRYLOCALUSE, ASN_INTEGER, RONLY, var_ieee8021SpbTopEctTable, 4, {1, 9, 1, 8}},
#define   IEEE8021SPBTOPEDGEENTRYMETRICNEAR2FAR  44
	{(u_char) IEEE8021SPBTOPEDGEENTRYMETRICNEAR2FAR, ASN_INTEGER, RONLY, var_ieee8021SpbTopEdgeTable, 4, {1, 10, 1, 4}},
#define   IEEE8021SPBTOPEDGEENTRYMETRICFAR2NEAR  45
	{(u_char) IEEE8021SPBTOPEDGEENTRYMETRICFAR2NEAR, ASN_INTEGER, RONLY, var_ieee8021SpbTopEdgeTable, 4, {1, 10, 1, 5}},
#define   IEEE8021SPBMTOPSRVENTRYISIDFLAGS  46
	{(u_char) IEEE8021SPBMTOPSRVENTRYISIDFLAGS, ASN_OCTET_STR, RONLY, var_ieee8021SpbmTopSrvTable, 4, {1, 11, 1, 6}},
#define   IEEE8021SPBVTOPSRVENTRYBASEVID  47
	{(u_char) IEEE8021SPBVTOPSRVENTRYBASEVID, ASN_INTEGER, RONLY, var_ieee8021SpbvTopSrvTable, 4, {1, 12, 1, 4}},
#define   IEEE8021SPBVTOPSRVENTRYMMACFLAGS  48
	{(u_char) IEEE8021SPBVTOPSRVENTRYMMACFLAGS, ASN_OCTET_STR, RONLY, var_ieee8021SpbvTopSrvTable, 4, {1, 12, 1, 5}},
#define   IEEE8021SPBMBSISTATICENTRYTBIT  49
	{(u_char) IEEE8021SPBMBSISTATICENTRYTBIT, ASN_INTEGER, RWRITE, var_ieee8021SpbmBsiStaticTable, 4, {1, 13, 1, 3}},
#define   IEEE8021SPBMBSISTATICENTRYRBIT  50
	{(u_char) IEEE8021SPBMBSISTATICENTRYRBIT, ASN_INTEGER, RWRITE, var_ieee8021SpbmBsiStaticTable, 4, {1, 13, 1, 4}},
#define   IEEE8021SPBMBSISTATICENTRYTSBIT  51
	{(u_char) IEEE8021SPBMBSISTATICENTRYTSBIT, ASN_INTEGER, RWRITE, var_ieee8021SpbmBsiStaticTable, 4, {1, 13, 1, 5}},
#define   IEEE8021SPBMBSISTATICENTRYTIEBREAKMASK  52
	{(u_char) IEEE8021SPBMBSISTATICENTRYTIEBREAKMASK, ASN_INTEGER, RWRITE, var_ieee8021SpbmBsiStaticTable, 4, {1, 13, 1, 6}},
#define   IEEE8021SPBMBSISTATICENTRYROWSTATUS  53
	{(u_char) IEEE8021SPBMBSISTATICENTRYROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021SpbmBsiStaticTable, 4, {1, 13, 1, 7}},
#define   DOT1AGCFMMEPTRANSMITLBMSPBMDA  54
	{(u_char) DOT1AGCFMMEPTRANSMITLBMSPBMDA, ASN_OCTET_STR, RWRITE, var_dot1agCfmMepSpbmTable, 4, {1, 14, 1, 1}},
#define   DOT1AGCFMMEPTRANSMITLTMSPBMDA  55
	{(u_char) DOT1AGCFMMEPTRANSMITLTMSPBMDA, ASN_OCTET_STR, RWRITE, var_dot1agCfmMepSpbmTable, 4, {1, 14, 1, 2}},
#define   DOT1AGCFMMEPSPBMESPESP  56
	{(u_char) DOT1AGCFMMEPSPBMESPESP, ASN_OCTET_STR, RWRITE, var_dot1agCfmMepSpbmEspTable, 4, {1, 15, 1, 2}},
#define   DOT1AGCFMMEPSPBMESPROWSTATUS  57
	{(u_char) DOT1AGCFMMEPSPBMESPROWSTATUS, ASN_INTEGER, RWRITE, var_dot1agCfmMepSpbmEspTable, 4, {1, 15, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct ieee8021SpbMib_data *ieee8021SpbMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021SpbMtidStaticTableStorage = NULL;
struct header_complex_index *ieee8021SpbTopIxDynamicTableStorage = NULL;
struct header_complex_index *ieee8021SpbEctStaticTableStorage = NULL;
struct header_complex_index *ieee8021SpbEctDynamicTableStorage = NULL;
struct header_complex_index *ieee8021SpbAdjStaticTableStorage = NULL;
struct header_complex_index *ieee8021SpbAdjDynamicTableStorage = NULL;
struct header_complex_index *ieee8021SpbTopNodeTableStorage = NULL;
struct header_complex_index *ieee8021SpbTopEctTableStorage = NULL;
struct header_complex_index *ieee8021SpbTopEdgeTableStorage = NULL;
struct header_complex_index *ieee8021SpbmTopSrvTableStorage = NULL;
struct header_complex_index *ieee8021SpbvTopSrvTableStorage = NULL;
struct header_complex_index *ieee8021SpbmBsiStaticTableStorage = NULL;
struct header_complex_index *dot1agCfmMepSpbmTableStorage = NULL;
struct header_complex_index *dot1agCfmMepSpbmEspTableStorage = NULL;

void (*ieee8021SpbMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021SpbMib_loop_handler(int);
void ieee8021SpbMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021SpbMib(void)
 * @brief ieee8021SpbMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021SpbMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021SpbMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021SpbMib", "init_ieee8021SpbMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021SpbMib", ieee8021SpbMib_variables, variable4, ieee8021SpbMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021SpbMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021SpbMib", parse_ieee8021SpbMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbMtidStaticTable", parse_ieee8021SpbMtidStaticTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbTopIxDynamicTable", parse_ieee8021SpbTopIxDynamicTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbEctStaticTable", parse_ieee8021SpbEctStaticTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbEctDynamicTable", parse_ieee8021SpbEctDynamicTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbAdjStaticTable", parse_ieee8021SpbAdjStaticTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbAdjDynamicTable", parse_ieee8021SpbAdjDynamicTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbTopNodeTable", parse_ieee8021SpbTopNodeTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbTopEctTable", parse_ieee8021SpbTopEctTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbTopEdgeTable", parse_ieee8021SpbTopEdgeTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbmTopSrvTable", parse_ieee8021SpbmTopSrvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbvTopSrvTable", parse_ieee8021SpbvTopSrvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021SpbmBsiStaticTable", parse_ieee8021SpbmBsiStaticTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dot1agCfmMepSpbmTable", parse_dot1agCfmMepSpbmTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dot1agCfmMepSpbmEspTable", parse_dot1agCfmMepSpbmEspTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbMtidStaticTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbTopIxDynamicTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbEctStaticTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbEctDynamicTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbAdjStaticTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbAdjDynamicTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbTopNodeTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbTopEctTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbTopEdgeTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbmTopSrvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbvTopSrvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021SpbmBsiStaticTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dot1agCfmMepSpbmTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dot1agCfmMepSpbmEspTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021SpbMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021SpbMib_fd_handler, (void *) 1);
	}
	ieee8021SpbMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021SpbMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021SpbMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021SpbMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021SpbMib(void)
{
	DEBUGMSGTL(("ieee8021SpbMib", "deinit_ieee8021SpbMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021SpbMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021SpbMib_variables_oid, sizeof(ieee8021SpbMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021SpbMib");
	snmpd_unregister_config_handler("ieee8021SpbMtidStaticTable");
	snmpd_unregister_config_handler("ieee8021SpbTopIxDynamicTable");
	snmpd_unregister_config_handler("ieee8021SpbEctStaticTable");
	snmpd_unregister_config_handler("ieee8021SpbEctDynamicTable");
	snmpd_unregister_config_handler("ieee8021SpbAdjStaticTable");
	snmpd_unregister_config_handler("ieee8021SpbAdjDynamicTable");
	snmpd_unregister_config_handler("ieee8021SpbTopNodeTable");
	snmpd_unregister_config_handler("ieee8021SpbTopEctTable");
	snmpd_unregister_config_handler("ieee8021SpbTopEdgeTable");
	snmpd_unregister_config_handler("ieee8021SpbmTopSrvTable");
	snmpd_unregister_config_handler("ieee8021SpbvTopSrvTable");
	snmpd_unregister_config_handler("ieee8021SpbmBsiStaticTable");
	snmpd_unregister_config_handler("dot1agCfmMepSpbmTable");
	snmpd_unregister_config_handler("dot1agCfmMepSpbmEspTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

int
term_ieee8021SpbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021SpbMib", "term_ieee8021SpbMib: terminating...  "));
	deinit_ieee8021SpbMib();
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021SpbMib_data *ieee8021SpbMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021SpbMib.
 *
 * Creates a new ieee8021SpbMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021SpbMib.
 */
struct ieee8021SpbMib_data *
ieee8021SpbMib_create(void)
{
	struct ieee8021SpbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbMib_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		if ((StorageNew->ieee8021SpbSysAreaAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbSysAreaAddressLen = 0;
		StorageNew->ieee8021SpbSysAreaAddress[StorageNew->ieee8021SpbSysAreaAddressLen] = 0;
		if ((StorageNew->ieee8021SpbSysId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbSysIdLen = 0;
		StorageNew->ieee8021SpbSysId[StorageNew->ieee8021SpbSysIdLen] = 0;
		if ((StorageNew->ieee8021SpbSysControlAddr = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbSysControlAddrLen = 0;
		StorageNew->ieee8021SpbSysControlAddr[StorageNew->ieee8021SpbSysControlAddrLen] = 0;
		if ((StorageNew->ieee8021SpbSysName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbSysNameLen = 0;
		StorageNew->ieee8021SpbSysName[StorageNew->ieee8021SpbSysNameLen] = 0;
		if ((StorageNew->ieee8021SpbSysBridgePriority = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbSysBridgePriorityLen = 0;
		StorageNew->ieee8021SpbSysBridgePriority[StorageNew->ieee8021SpbSysBridgePriorityLen] = 0;
		if ((StorageNew->ieee8021SpbmSysSPSourceId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbmSysSPSourceIdLen = 0;
		StorageNew->ieee8021SpbmSysSPSourceId[StorageNew->ieee8021SpbmSysSPSourceIdLen] = 0;
		StorageNew->ieee8021SpbvSysMode = IEEE8021SPBVSYSMODE_AUTO;
		StorageNew->ieee8021SpbmSysMode = IEEE8021SPBMSYSMODE_AUTO;
		StorageNew->ieee8021SpbSysDigestConvention = IEEE8021SPBSYSDIGESTCONVENTION_LOOPFREEBOTH;

	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbMib_data *ieee8021SpbMib_duplicate(struct ieee8021SpbMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021SpbMib_data *
ieee8021SpbMib_duplicate(struct ieee8021SpbMib_data *thedata)
{
	struct ieee8021SpbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbMib_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		if (!(StorageNew->ieee8021SpbSysAreaAddress = malloc(thedata->ieee8021SpbSysAreaAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbSysAreaAddress, thedata->ieee8021SpbSysAreaAddress, thedata->ieee8021SpbSysAreaAddressLen);
		StorageNew->ieee8021SpbSysAreaAddressLen = thedata->ieee8021SpbSysAreaAddressLen;
		StorageNew->ieee8021SpbSysAreaAddress[StorageNew->ieee8021SpbSysAreaAddressLen] = 0;
		if (!(StorageNew->ieee8021SpbSysId = malloc(thedata->ieee8021SpbSysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbSysId, thedata->ieee8021SpbSysId, thedata->ieee8021SpbSysIdLen);
		StorageNew->ieee8021SpbSysIdLen = thedata->ieee8021SpbSysIdLen;
		StorageNew->ieee8021SpbSysId[StorageNew->ieee8021SpbSysIdLen] = 0;
		if (!(StorageNew->ieee8021SpbSysControlAddr = malloc(thedata->ieee8021SpbSysControlAddrLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbSysControlAddr, thedata->ieee8021SpbSysControlAddr, thedata->ieee8021SpbSysControlAddrLen);
		StorageNew->ieee8021SpbSysControlAddrLen = thedata->ieee8021SpbSysControlAddrLen;
		StorageNew->ieee8021SpbSysControlAddr[StorageNew->ieee8021SpbSysControlAddrLen] = 0;
		if (!(StorageNew->ieee8021SpbSysName = malloc(thedata->ieee8021SpbSysNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbSysName, thedata->ieee8021SpbSysName, thedata->ieee8021SpbSysNameLen);
		StorageNew->ieee8021SpbSysNameLen = thedata->ieee8021SpbSysNameLen;
		StorageNew->ieee8021SpbSysName[StorageNew->ieee8021SpbSysNameLen] = 0;
		if (!(StorageNew->ieee8021SpbSysBridgePriority = malloc(thedata->ieee8021SpbSysBridgePriorityLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbSysBridgePriority, thedata->ieee8021SpbSysBridgePriority, thedata->ieee8021SpbSysBridgePriorityLen);
		StorageNew->ieee8021SpbSysBridgePriorityLen = thedata->ieee8021SpbSysBridgePriorityLen;
		StorageNew->ieee8021SpbSysBridgePriority[StorageNew->ieee8021SpbSysBridgePriorityLen] = 0;
		if (!(StorageNew->ieee8021SpbmSysSPSourceId = malloc(thedata->ieee8021SpbmSysSPSourceIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmSysSPSourceId, thedata->ieee8021SpbmSysSPSourceId, thedata->ieee8021SpbmSysSPSourceIdLen);
		StorageNew->ieee8021SpbmSysSPSourceIdLen = thedata->ieee8021SpbmSysSPSourceIdLen;
		StorageNew->ieee8021SpbmSysSPSourceId[StorageNew->ieee8021SpbmSysSPSourceIdLen] = 0;
		StorageNew->ieee8021SpbvSysMode = thedata->ieee8021SpbvSysMode;
		StorageNew->ieee8021SpbmSysMode = thedata->ieee8021SpbmSysMode;
		StorageNew->ieee8021SpbSysDigestConvention = thedata->ieee8021SpbSysDigestConvention;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbMib_destroy(struct ieee8021SpbMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021SpbMib.
 * @brief delete a scalars structure from ieee8021SpbMib.
 *
 * Frees scalars that were previously removed from ieee8021SpbMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbMib_destroy(struct ieee8021SpbMib_data **thedata)
{
	struct ieee8021SpbMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbSysAreaAddress);
		StorageDel->ieee8021SpbSysAreaAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbSysId);
		StorageDel->ieee8021SpbSysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbSysControlAddr);
		StorageDel->ieee8021SpbSysControlAddrLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbSysName);
		StorageDel->ieee8021SpbSysNameLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbSysBridgePriority);
		StorageDel->ieee8021SpbSysBridgePriorityLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbmSysSPSourceId);
		StorageDel->ieee8021SpbmSysSPSourceIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbMib_add(struct ieee8021SpbMib_data *thedata)
 * @param thedata the structure representing ieee8021SpbMib scalars.
 * @brief adds node to the ieee8021SpbMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021SpbMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021SpbMib_add(struct ieee8021SpbMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMib_add: adding data...  "));
	if (thedata)
		ieee8021SpbMibStorage = thedata;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021SpbMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbMib_data *StorageTmp = ieee8021SpbMib_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	SNMP_FREE(StorageTmp->ieee8021SpbSysAreaAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbSysAreaAddress, &StorageTmp->ieee8021SpbSysAreaAddressLen);
	if (StorageTmp->ieee8021SpbSysAreaAddress == NULL) {
		config_perror("invalid specification for ieee8021SpbSysAreaAddress");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbSysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbSysId, &StorageTmp->ieee8021SpbSysIdLen);
	if (StorageTmp->ieee8021SpbSysId == NULL) {
		config_perror("invalid specification for ieee8021SpbSysId");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbSysControlAddr);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbSysControlAddr, &StorageTmp->ieee8021SpbSysControlAddrLen);
	if (StorageTmp->ieee8021SpbSysControlAddr == NULL) {
		config_perror("invalid specification for ieee8021SpbSysControlAddr");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbSysName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbSysName, &StorageTmp->ieee8021SpbSysNameLen);
	if (StorageTmp->ieee8021SpbSysName == NULL) {
		config_perror("invalid specification for ieee8021SpbSysName");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbSysBridgePriority);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbSysBridgePriority, &StorageTmp->ieee8021SpbSysBridgePriorityLen);
	if (StorageTmp->ieee8021SpbSysBridgePriority == NULL) {
		config_perror("invalid specification for ieee8021SpbSysBridgePriority");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbmSysSPSourceId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmSysSPSourceId, &StorageTmp->ieee8021SpbmSysSPSourceIdLen);
	if (StorageTmp->ieee8021SpbmSysSPSourceId == NULL) {
		config_perror("invalid specification for ieee8021SpbmSysSPSourceId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbvSysMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmSysMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbSysDigestConvention, &tmpsize);
	ieee8021SpbMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbMib: storing data...  "));
	refresh_ieee8021SpbMib(1);
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021SpbMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021SpbMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbSysAreaAddress, &StorageTmp->ieee8021SpbSysAreaAddressLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbSysId, &StorageTmp->ieee8021SpbSysIdLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbSysControlAddr, &StorageTmp->ieee8021SpbSysControlAddrLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbSysName, &StorageTmp->ieee8021SpbSysNameLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbSysBridgePriority, &StorageTmp->ieee8021SpbSysBridgePriorityLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmSysSPSourceId, &StorageTmp->ieee8021SpbmSysSPSourceIdLen);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbvSysMode, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmSysMode, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbSysDigestConvention, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021SpbMib(struct ieee8021SpbMib_data *StorageTmp, struct ieee8021SpbMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021SpbMib(struct ieee8021SpbMib_data *StorageTmp, struct ieee8021SpbMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbMib(struct ieee8021SpbMib_data *StorageTmp, struct ieee8021SpbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021SpbMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbMib(struct ieee8021SpbMib_data *StorageTmp, struct ieee8021SpbMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021SpbMib(struct 
 * @fn void revert_ieee8021SpbMib(struct ieee8021SpbMib_data *StorageTmp, struct ieee8021SpbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021SpbMib(struct ieee8021SpbMib_data *StorageTmp, struct ieee8021SpbMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021SpbMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021SpbMib(int force)
{
	if (ieee8021SpbMibStorage == NULL) {
		struct ieee8021SpbMib_data *StorageNew;

		if ((StorageNew = ieee8021SpbMib_create()) == NULL)
			return;
		ieee8021SpbMibStorage = StorageNew;
		ieee8021SpbMib_refresh = 1;
	}
	if (!force && ieee8021SpbMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021SpbMib", "refresh_ieee8021SpbMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021SpbMib_refresh = 0;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021SpbMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021SpbMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021SpbMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021SpbMib(0);
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021SpbMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBSYSAREAADDRESS:	/* ReadWrite */
		*write_method = write_ieee8021SpbSysAreaAddress;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbSysAreaAddressLen;
		rval = (u_char *) StorageTmp->ieee8021SpbSysAreaAddress;
		break;
	case (u_char) IEEE8021SPBSYSID:	/* ReadWrite */
		*write_method = write_ieee8021SpbSysId;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbSysIdLen;
		rval = (u_char *) StorageTmp->ieee8021SpbSysId;
		break;
	case (u_char) IEEE8021SPBSYSCONTROLADDR:	/* ReadWrite */
		*write_method = write_ieee8021SpbSysControlAddr;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbSysControlAddrLen;
		rval = (u_char *) StorageTmp->ieee8021SpbSysControlAddr;
		break;
	case (u_char) IEEE8021SPBSYSNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbSysNameLen;
		rval = (u_char *) StorageTmp->ieee8021SpbSysName;
		break;
	case (u_char) IEEE8021SPBSYSBRIDGEPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbSysBridgePriorityLen;
		rval = (u_char *) StorageTmp->ieee8021SpbSysBridgePriority;
		break;
	case (u_char) IEEE8021SPBMSYSSPSOURCEID:	/* ReadWrite */
		*write_method = write_ieee8021SpbmSysSPSourceId;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbmSysSPSourceIdLen;
		rval = (u_char *) StorageTmp->ieee8021SpbmSysSPSourceId;
		break;
	case (u_char) IEEE8021SPBVSYSMODE:	/* ReadWrite */
		*write_method = write_ieee8021SpbvSysMode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbvSysMode);
		rval = (u_char *) &StorageTmp->ieee8021SpbvSysMode;
		break;
	case (u_char) IEEE8021SPBMSYSMODE:	/* ReadWrite */
		*write_method = write_ieee8021SpbmSysMode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbmSysMode);
		rval = (u_char *) &StorageTmp->ieee8021SpbmSysMode;
		break;
	case (u_char) IEEE8021SPBSYSDIGESTCONVENTION:	/* ReadWrite */
		*write_method = write_ieee8021SpbSysDigestConvention;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbSysDigestConvention);
		rval = (u_char *) &StorageTmp->ieee8021SpbSysDigestConvention;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021SpbMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021SpbMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021SpbMtidStaticTable_data *ieee8021SpbMtidStaticTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbMtidStaticTable table.
 *
 * Creates a new ieee8021SpbMtidStaticTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbMtidStaticTable_data *
ieee8021SpbMtidStaticTable_create(void)
{
	struct ieee8021SpbMtidStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbMtidStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMtidStaticTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbMtidStaticEntryMtidOverload = TV_FALSE;
		StorageNew->ieee8021SpbMtidStaticEntryRowStatus = 0;
		StorageNew->ieee8021SpbMtidStaticEntryRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbMtidStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbMtidStaticTable_data *ieee8021SpbMtidStaticTable_duplicate(struct ieee8021SpbMtidStaticTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbMtidStaticTable_data *
ieee8021SpbMtidStaticTable_duplicate(struct ieee8021SpbMtidStaticTable_data *thedata)
{
	struct ieee8021SpbMtidStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbMtidStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMtidStaticTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbMtidStaticTable_id = thedata->ieee8021SpbMtidStaticTable_id;
		StorageNew->ieee8021SpbMtidStaticEntryMtid = thedata->ieee8021SpbMtidStaticEntryMtid;
		StorageNew->ieee8021SpbMtidStaticEntryMtidOverload = thedata->ieee8021SpbMtidStaticEntryMtidOverload;
		StorageNew->ieee8021SpbMtidStaticEntryRowStatus = thedata->ieee8021SpbMtidStaticEntryRowStatus;
		StorageNew->ieee8021SpbTopIx = thedata->ieee8021SpbTopIx;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbMtidStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbMtidStaticTable_destroy(struct ieee8021SpbMtidStaticTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbMtidStaticTable_destroy(struct ieee8021SpbMtidStaticTable_data **thedata)
{
	struct ieee8021SpbMtidStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMtidStaticTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbMtidStaticTable_add(struct ieee8021SpbMtidStaticTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbMtidStaticTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbMtidStaticTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbMtidStaticTable_add(struct ieee8021SpbMtidStaticTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMtidStaticTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbMtidStaticEntryMtid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbMtidStaticEntryMtid, sizeof(thedata->ieee8021SpbMtidStaticEntryMtid));
		/* ieee8021SpbTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopIx, sizeof(thedata->ieee8021SpbTopIx));
		header_complex_add_data(&ieee8021SpbMtidStaticTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbMtidStaticTable_del(struct ieee8021SpbMtidStaticTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbMtidStaticTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbMtidStaticTable_del(struct ieee8021SpbMtidStaticTable_data *thedata)
{
	struct ieee8021SpbMtidStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMtidStaticTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbMtidStaticTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbMtidStaticTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbMtidStaticTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbMtidStaticTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbMtidStaticTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbMtidStaticTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbMtidStaticTable_data *StorageTmp = ieee8021SpbMtidStaticTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbMtidStaticTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbMtidStaticEntryMtid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbMtidStaticEntryRowStatus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopIx, &tmpsize);
	ieee8021SpbMtidStaticTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbMtidStaticTable(): store configuraiton file for ieee8021SpbMtidStaticTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbMtidStaticTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbMtidStaticTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbMtidStaticTable: storing data...  "));
	refresh_ieee8021SpbMtidStaticTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbMtidStaticTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbMtidStaticTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbMtidStaticTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbMtidStaticEntryMtid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbMtidStaticEntryRowStatus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopIx, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbTopIxDynamicTable_data *ieee8021SpbTopIxDynamicTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbTopIxDynamicTable table.
 *
 * Creates a new ieee8021SpbTopIxDynamicTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbTopIxDynamicTable_data *
ieee8021SpbTopIxDynamicTable_create(void)
{
	struct ieee8021SpbTopIxDynamicTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopIxDynamicTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopIxDynamicTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigest = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigestLen = 0;
		StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigest[StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigestLen] = 0;
		if ((StorageNew->ieee8021SpbTopIxDynamicEntryMCID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbTopIxDynamicEntryMCIDLen = 0;
		StorageNew->ieee8021SpbTopIxDynamicEntryMCID[StorageNew->ieee8021SpbTopIxDynamicEntryMCIDLen] = 0;
		if ((StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCIDLen = 0;
		StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCID[StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCIDLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbTopIxDynamicTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbTopIxDynamicTable_data *ieee8021SpbTopIxDynamicTable_duplicate(struct ieee8021SpbTopIxDynamicTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbTopIxDynamicTable_data *
ieee8021SpbTopIxDynamicTable_duplicate(struct ieee8021SpbTopIxDynamicTable_data *thedata)
{
	struct ieee8021SpbTopIxDynamicTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopIxDynamicTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopIxDynamicTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbTopIxDynamicTable_id = thedata->ieee8021SpbTopIxDynamicTable_id;
		StorageNew->ieee8021SpbTopIxDynamicEntryTopIx = thedata->ieee8021SpbTopIxDynamicEntryTopIx;
		if (!(StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigest = malloc(thedata->ieee8021SpbTopIxDynamicEntryAgreeDigestLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigest, thedata->ieee8021SpbTopIxDynamicEntryAgreeDigest, thedata->ieee8021SpbTopIxDynamicEntryAgreeDigestLen);
		StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigestLen = thedata->ieee8021SpbTopIxDynamicEntryAgreeDigestLen;
		StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigest[StorageNew->ieee8021SpbTopIxDynamicEntryAgreeDigestLen] = 0;
		if (!(StorageNew->ieee8021SpbTopIxDynamicEntryMCID = malloc(thedata->ieee8021SpbTopIxDynamicEntryMCIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopIxDynamicEntryMCID, thedata->ieee8021SpbTopIxDynamicEntryMCID, thedata->ieee8021SpbTopIxDynamicEntryMCIDLen);
		StorageNew->ieee8021SpbTopIxDynamicEntryMCIDLen = thedata->ieee8021SpbTopIxDynamicEntryMCIDLen;
		StorageNew->ieee8021SpbTopIxDynamicEntryMCID[StorageNew->ieee8021SpbTopIxDynamicEntryMCIDLen] = 0;
		if (!(StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCID = malloc(thedata->ieee8021SpbTopIxDynamicEntryAuxMCIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCID, thedata->ieee8021SpbTopIxDynamicEntryAuxMCID, thedata->ieee8021SpbTopIxDynamicEntryAuxMCIDLen);
		StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCIDLen = thedata->ieee8021SpbTopIxDynamicEntryAuxMCIDLen;
		StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCID[StorageNew->ieee8021SpbTopIxDynamicEntryAuxMCIDLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbTopIxDynamicTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbTopIxDynamicTable_destroy(struct ieee8021SpbTopIxDynamicTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbTopIxDynamicTable_destroy(struct ieee8021SpbTopIxDynamicTable_data **thedata)
{
	struct ieee8021SpbTopIxDynamicTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopIxDynamicTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbTopIxDynamicEntryAgreeDigest);
		StorageDel->ieee8021SpbTopIxDynamicEntryAgreeDigestLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbTopIxDynamicEntryMCID);
		StorageDel->ieee8021SpbTopIxDynamicEntryMCIDLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbTopIxDynamicEntryAuxMCID);
		StorageDel->ieee8021SpbTopIxDynamicEntryAuxMCIDLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopIxDynamicTable_add(struct ieee8021SpbTopIxDynamicTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbTopIxDynamicTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbTopIxDynamicTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbTopIxDynamicTable_add(struct ieee8021SpbTopIxDynamicTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopIxDynamicTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbTopIxDynamicEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopIxDynamicEntryTopIx, sizeof(thedata->ieee8021SpbTopIxDynamicEntryTopIx));
		header_complex_add_data(&ieee8021SpbTopIxDynamicTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopIxDynamicTable_del(struct ieee8021SpbTopIxDynamicTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbTopIxDynamicTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbTopIxDynamicTable_del(struct ieee8021SpbTopIxDynamicTable_data *thedata)
{
	struct ieee8021SpbTopIxDynamicTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopIxDynamicTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbTopIxDynamicTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbTopIxDynamicTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbTopIxDynamicTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbTopIxDynamicTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbTopIxDynamicTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbTopIxDynamicTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbTopIxDynamicTable_data *StorageTmp = ieee8021SpbTopIxDynamicTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbTopIxDynamicTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopIxDynamicEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigest);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigest, &StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigestLen);
	if (StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigest == NULL) {
		config_perror("invalid specification for ieee8021SpbTopIxDynamicEntryAgreeDigest");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbTopIxDynamicEntryMCID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopIxDynamicEntryMCID, &StorageTmp->ieee8021SpbTopIxDynamicEntryMCIDLen);
	if (StorageTmp->ieee8021SpbTopIxDynamicEntryMCID == NULL) {
		config_perror("invalid specification for ieee8021SpbTopIxDynamicEntryMCID");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCID, &StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCIDLen);
	if (StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCID == NULL) {
		config_perror("invalid specification for ieee8021SpbTopIxDynamicEntryAuxMCID");
		return;
	}
	ieee8021SpbTopIxDynamicTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbTopIxDynamicTable(): store configuraiton file for ieee8021SpbTopIxDynamicTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbTopIxDynamicTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbTopIxDynamicTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbTopIxDynamicTable: storing data...  "));
	refresh_ieee8021SpbTopIxDynamicTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbTopIxDynamicTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbTopIxDynamicTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbTopIxDynamicTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopIxDynamicEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigest, &StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigestLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopIxDynamicEntryMCID, &StorageTmp->ieee8021SpbTopIxDynamicEntryMCIDLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCID, &StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCIDLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbEctStaticTable_data *ieee8021SpbEctStaticTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbEctStaticTable table.
 *
 * Creates a new ieee8021SpbEctStaticTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbEctStaticTable_data *
ieee8021SpbEctStaticTable_create(void)
{
	struct ieee8021SpbEctStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbEctStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctStaticTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021SpbEctStaticEntryEctAlgorithm = (uint8_t *) strdup("00-80-c2-01")) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbEctStaticEntryEctAlgorithmLen = strlen("00-80-c2-01");
		StorageNew->ieee8021SpbvEctStaticEntrySpvid = 0;
		StorageNew->ieee8021SpbEctStaticEntryRowStatus = 0;
		StorageNew->ieee8021SpbEctStaticEntryRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbEctStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbEctStaticTable_data *ieee8021SpbEctStaticTable_duplicate(struct ieee8021SpbEctStaticTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbEctStaticTable_data *
ieee8021SpbEctStaticTable_duplicate(struct ieee8021SpbEctStaticTable_data *thedata)
{
	struct ieee8021SpbEctStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbEctStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctStaticTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbEctStaticTable_id = thedata->ieee8021SpbEctStaticTable_id;
		StorageNew->ieee8021SpbEctStaticEntryTopIx = thedata->ieee8021SpbEctStaticEntryTopIx;
		StorageNew->ieee8021SpbEctStaticEntryBaseVid = thedata->ieee8021SpbEctStaticEntryBaseVid;
		if (!(StorageNew->ieee8021SpbEctStaticEntryEctAlgorithm = malloc(thedata->ieee8021SpbEctStaticEntryEctAlgorithmLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbEctStaticEntryEctAlgorithm, thedata->ieee8021SpbEctStaticEntryEctAlgorithm, thedata->ieee8021SpbEctStaticEntryEctAlgorithmLen);
		StorageNew->ieee8021SpbEctStaticEntryEctAlgorithmLen = thedata->ieee8021SpbEctStaticEntryEctAlgorithmLen;
		StorageNew->ieee8021SpbEctStaticEntryEctAlgorithm[StorageNew->ieee8021SpbEctStaticEntryEctAlgorithmLen] = 0;
		StorageNew->ieee8021SpbvEctStaticEntrySpvid = thedata->ieee8021SpbvEctStaticEntrySpvid;
		StorageNew->ieee8021SpbEctStaticEntryRowStatus = thedata->ieee8021SpbEctStaticEntryRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbEctStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbEctStaticTable_destroy(struct ieee8021SpbEctStaticTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbEctStaticTable_destroy(struct ieee8021SpbEctStaticTable_data **thedata)
{
	struct ieee8021SpbEctStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctStaticTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbEctStaticEntryEctAlgorithm);
		StorageDel->ieee8021SpbEctStaticEntryEctAlgorithmLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbEctStaticTable_add(struct ieee8021SpbEctStaticTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbEctStaticTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbEctStaticTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbEctStaticTable_add(struct ieee8021SpbEctStaticTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctStaticTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbEctStaticEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbEctStaticEntryTopIx, sizeof(thedata->ieee8021SpbEctStaticEntryTopIx));
		/* ieee8021SpbEctStaticEntryBaseVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbEctStaticEntryBaseVid, sizeof(thedata->ieee8021SpbEctStaticEntryBaseVid));
		header_complex_add_data(&ieee8021SpbEctStaticTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbEctStaticTable_del(struct ieee8021SpbEctStaticTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbEctStaticTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbEctStaticTable_del(struct ieee8021SpbEctStaticTable_data *thedata)
{
	struct ieee8021SpbEctStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctStaticTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbEctStaticTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbEctStaticTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbEctStaticTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbEctStaticTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbEctStaticTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbEctStaticTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbEctStaticTable_data *StorageTmp = ieee8021SpbEctStaticTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbEctStaticTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbEctStaticEntryTopIx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbEctStaticEntryBaseVid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm, &StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithmLen);
	if (StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm == NULL) {
		config_perror("invalid specification for ieee8021SpbEctStaticEntryEctAlgorithm");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbvEctStaticEntrySpvid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbEctStaticEntryRowStatus, &tmpsize);
	ieee8021SpbEctStaticTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbEctStaticTable(): store configuraiton file for ieee8021SpbEctStaticTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbEctStaticTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbEctStaticTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbEctStaticTable: storing data...  "));
	refresh_ieee8021SpbEctStaticTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbEctStaticTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbEctStaticTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbEctStaticTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbEctStaticEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbEctStaticEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm, &StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithmLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbvEctStaticEntrySpvid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbEctStaticEntryRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbEctDynamicTable_data *ieee8021SpbEctDynamicTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbEctDynamicTable table.
 *
 * Creates a new ieee8021SpbEctDynamicTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbEctDynamicTable_data *
ieee8021SpbEctDynamicTable_create(void)
{
	struct ieee8021SpbEctDynamicTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbEctDynamicTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctDynamicTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbEctDynamicEntryMode = 0;
		StorageNew->ieee8021SpbEctDynamicEntryLocalUse = 0;
		StorageNew->ieee8021SpbEctDynamicEntryRemoteUse = 0;
		StorageNew->ieee8021SpbEctDynamicEntryIngressCheckDiscards = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbEctDynamicTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbEctDynamicTable_data *ieee8021SpbEctDynamicTable_duplicate(struct ieee8021SpbEctDynamicTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbEctDynamicTable_data *
ieee8021SpbEctDynamicTable_duplicate(struct ieee8021SpbEctDynamicTable_data *thedata)
{
	struct ieee8021SpbEctDynamicTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbEctDynamicTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctDynamicTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbEctDynamicTable_id = thedata->ieee8021SpbEctDynamicTable_id;
		StorageNew->ieee8021SpbEctDynamicEntryTopIx = thedata->ieee8021SpbEctDynamicEntryTopIx;
		StorageNew->ieee8021SpbEctDynamicEntryBaseVid = thedata->ieee8021SpbEctDynamicEntryBaseVid;
		StorageNew->ieee8021SpbEctDynamicEntryMode = thedata->ieee8021SpbEctDynamicEntryMode;
		StorageNew->ieee8021SpbEctDynamicEntryLocalUse = thedata->ieee8021SpbEctDynamicEntryLocalUse;
		StorageNew->ieee8021SpbEctDynamicEntryRemoteUse = thedata->ieee8021SpbEctDynamicEntryRemoteUse;
		StorageNew->ieee8021SpbEctDynamicEntryIngressCheckDiscards = thedata->ieee8021SpbEctDynamicEntryIngressCheckDiscards;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbEctDynamicTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbEctDynamicTable_destroy(struct ieee8021SpbEctDynamicTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbEctDynamicTable_destroy(struct ieee8021SpbEctDynamicTable_data **thedata)
{
	struct ieee8021SpbEctDynamicTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctDynamicTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbEctDynamicTable_add(struct ieee8021SpbEctDynamicTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbEctDynamicTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbEctDynamicTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbEctDynamicTable_add(struct ieee8021SpbEctDynamicTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctDynamicTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbEctDynamicEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbEctDynamicEntryTopIx, sizeof(thedata->ieee8021SpbEctDynamicEntryTopIx));
		/* ieee8021SpbEctDynamicEntryBaseVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbEctDynamicEntryBaseVid, sizeof(thedata->ieee8021SpbEctDynamicEntryBaseVid));
		header_complex_add_data(&ieee8021SpbEctDynamicTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbEctDynamicTable_del(struct ieee8021SpbEctDynamicTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbEctDynamicTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbEctDynamicTable_del(struct ieee8021SpbEctDynamicTable_data *thedata)
{
	struct ieee8021SpbEctDynamicTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbEctDynamicTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbEctDynamicTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbEctDynamicTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbEctDynamicTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbEctDynamicTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbEctDynamicTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbEctDynamicTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbEctDynamicTable_data *StorageTmp = ieee8021SpbEctDynamicTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbEctDynamicTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbEctDynamicEntryTopIx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbEctDynamicEntryBaseVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbEctDynamicEntryMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbEctDynamicEntryLocalUse, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbEctDynamicEntryRemoteUse, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbEctDynamicEntryIngressCheckDiscards, &tmpsize);
	ieee8021SpbEctDynamicTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbEctDynamicTable(): store configuraiton file for ieee8021SpbEctDynamicTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbEctDynamicTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbEctDynamicTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbEctDynamicTable: storing data...  "));
	refresh_ieee8021SpbEctDynamicTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbEctDynamicTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbEctDynamicTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbEctDynamicTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbEctDynamicEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbEctDynamicEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbEctDynamicEntryMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbEctDynamicEntryLocalUse, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbEctDynamicEntryRemoteUse, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbEctDynamicEntryIngressCheckDiscards, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbAdjStaticTable_data *ieee8021SpbAdjStaticTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbAdjStaticTable table.
 *
 * Creates a new ieee8021SpbAdjStaticTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbAdjStaticTable_data *
ieee8021SpbAdjStaticTable_create(void)
{
	struct ieee8021SpbAdjStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbAdjStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjStaticTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbAdjStaticEntryMetric = 0;
		StorageNew->ieee8021SpbAdjStaticEntryIfAdminState = IEEE8021SPBADJSTATICENTRYIFADMINSTATE_UP;
		StorageNew->ieee8021SpbAdjStaticEntryRowStatus = 0;
		StorageNew->ieee8021SpbAdjStaticEntryRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbAdjStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbAdjStaticTable_data *ieee8021SpbAdjStaticTable_duplicate(struct ieee8021SpbAdjStaticTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbAdjStaticTable_data *
ieee8021SpbAdjStaticTable_duplicate(struct ieee8021SpbAdjStaticTable_data *thedata)
{
	struct ieee8021SpbAdjStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbAdjStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjStaticTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbAdjStaticTable_id = thedata->ieee8021SpbAdjStaticTable_id;
		StorageNew->ieee8021SpbAdjStaticEntryTopIx = thedata->ieee8021SpbAdjStaticEntryTopIx;
		StorageNew->ieee8021SpbAdjStaticEntryIfIndex = thedata->ieee8021SpbAdjStaticEntryIfIndex;
		StorageNew->ieee8021SpbAdjStaticEntryMetric = thedata->ieee8021SpbAdjStaticEntryMetric;
		StorageNew->ieee8021SpbAdjStaticEntryIfAdminState = thedata->ieee8021SpbAdjStaticEntryIfAdminState;
		StorageNew->ieee8021SpbAdjStaticEntryRowStatus = thedata->ieee8021SpbAdjStaticEntryRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbAdjStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbAdjStaticTable_destroy(struct ieee8021SpbAdjStaticTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbAdjStaticTable_destroy(struct ieee8021SpbAdjStaticTable_data **thedata)
{
	struct ieee8021SpbAdjStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjStaticTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbAdjStaticTable_add(struct ieee8021SpbAdjStaticTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbAdjStaticTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbAdjStaticTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbAdjStaticTable_add(struct ieee8021SpbAdjStaticTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjStaticTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbAdjStaticEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbAdjStaticEntryTopIx, sizeof(thedata->ieee8021SpbAdjStaticEntryTopIx));
		/* ieee8021SpbAdjStaticEntryIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbAdjStaticEntryIfIndex, sizeof(thedata->ieee8021SpbAdjStaticEntryIfIndex));
		header_complex_add_data(&ieee8021SpbAdjStaticTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbAdjStaticTable_del(struct ieee8021SpbAdjStaticTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbAdjStaticTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbAdjStaticTable_del(struct ieee8021SpbAdjStaticTable_data *thedata)
{
	struct ieee8021SpbAdjStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjStaticTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbAdjStaticTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbAdjStaticTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbAdjStaticTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbAdjStaticTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbAdjStaticTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbAdjStaticTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbAdjStaticTable_data *StorageTmp = ieee8021SpbAdjStaticTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbAdjStaticTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjStaticEntryTopIx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbAdjStaticEntryIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbAdjStaticEntryMetric, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbAdjStaticEntryRowStatus, &tmpsize);
	ieee8021SpbAdjStaticTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbAdjStaticTable(): store configuraiton file for ieee8021SpbAdjStaticTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbAdjStaticTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbAdjStaticTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbAdjStaticTable: storing data...  "));
	refresh_ieee8021SpbAdjStaticTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbAdjStaticTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbAdjStaticTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbAdjStaticTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjStaticEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbAdjStaticEntryIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbAdjStaticEntryMetric, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbAdjStaticEntryRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbAdjDynamicTable_data *ieee8021SpbAdjDynamicTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbAdjDynamicTable table.
 *
 * Creates a new ieee8021SpbAdjDynamicTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbAdjDynamicTable_data *
ieee8021SpbAdjDynamicTable_create(void)
{
	struct ieee8021SpbAdjDynamicTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbAdjDynamicTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjDynamicTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbAdjDynamicEntryPort = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryIfOperState = 0;
		if ((StorageNew->ieee8021SpbAdjDynamicEntryPeerSysName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerSysNameLen = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerSysName[StorageNew->ieee8021SpbAdjDynamicEntryPeerSysNameLen] = 0;
		if ((StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigest = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigest[StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen] = 0;
		if ((StorageNew->ieee8021SpbAdjDynamicEntryPeerMCID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerMCIDLen = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerMCID[StorageNew->ieee8021SpbAdjDynamicEntryPeerMCIDLen] = 0;
		if ((StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCID[StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen] = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryLocalCircuitID = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPortIdentifier = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerPortIdentifier = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryIsisCircIndex = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbAdjDynamicTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbAdjDynamicTable_data *ieee8021SpbAdjDynamicTable_duplicate(struct ieee8021SpbAdjDynamicTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbAdjDynamicTable_data *
ieee8021SpbAdjDynamicTable_duplicate(struct ieee8021SpbAdjDynamicTable_data *thedata)
{
	struct ieee8021SpbAdjDynamicTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbAdjDynamicTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjDynamicTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbAdjDynamicTable_id = thedata->ieee8021SpbAdjDynamicTable_id;
		StorageNew->ieee8021SpbAdjDynamicEntryTopIx = thedata->ieee8021SpbAdjDynamicEntryTopIx;
		StorageNew->ieee8021SpbAdjDynamicEntryIfIndex = thedata->ieee8021SpbAdjDynamicEntryIfIndex;
		if (!(StorageNew->ieee8021SpbAdjDynamicEntryPeerSysId = malloc(thedata->ieee8021SpbAdjDynamicEntryPeerSysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbAdjDynamicEntryPeerSysId, thedata->ieee8021SpbAdjDynamicEntryPeerSysId, thedata->ieee8021SpbAdjDynamicEntryPeerSysIdLen);
		StorageNew->ieee8021SpbAdjDynamicEntryPeerSysIdLen = thedata->ieee8021SpbAdjDynamicEntryPeerSysIdLen;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerSysId[StorageNew->ieee8021SpbAdjDynamicEntryPeerSysIdLen] = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryPort = thedata->ieee8021SpbAdjDynamicEntryPort;
		StorageNew->ieee8021SpbAdjDynamicEntryIfOperState = thedata->ieee8021SpbAdjDynamicEntryIfOperState;
		if (!(StorageNew->ieee8021SpbAdjDynamicEntryPeerSysName = malloc(thedata->ieee8021SpbAdjDynamicEntryPeerSysNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbAdjDynamicEntryPeerSysName, thedata->ieee8021SpbAdjDynamicEntryPeerSysName, thedata->ieee8021SpbAdjDynamicEntryPeerSysNameLen);
		StorageNew->ieee8021SpbAdjDynamicEntryPeerSysNameLen = thedata->ieee8021SpbAdjDynamicEntryPeerSysNameLen;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerSysName[StorageNew->ieee8021SpbAdjDynamicEntryPeerSysNameLen] = 0;
		if (!(StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigest = malloc(thedata->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigest, thedata->ieee8021SpbAdjDynamicEntryPeerAgreeDigest, thedata->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen);
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen = thedata->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigest[StorageNew->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen] = 0;
		if (!(StorageNew->ieee8021SpbAdjDynamicEntryPeerMCID = malloc(thedata->ieee8021SpbAdjDynamicEntryPeerMCIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbAdjDynamicEntryPeerMCID, thedata->ieee8021SpbAdjDynamicEntryPeerMCID, thedata->ieee8021SpbAdjDynamicEntryPeerMCIDLen);
		StorageNew->ieee8021SpbAdjDynamicEntryPeerMCIDLen = thedata->ieee8021SpbAdjDynamicEntryPeerMCIDLen;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerMCID[StorageNew->ieee8021SpbAdjDynamicEntryPeerMCIDLen] = 0;
		if (!(StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCID = malloc(thedata->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCID, thedata->ieee8021SpbAdjDynamicEntryPeerAuxMCID, thedata->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen);
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen = thedata->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCID[StorageNew->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen] = 0;
		StorageNew->ieee8021SpbAdjDynamicEntryLocalCircuitID = thedata->ieee8021SpbAdjDynamicEntryLocalCircuitID;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID = thedata->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID;
		StorageNew->ieee8021SpbAdjDynamicEntryPortIdentifier = thedata->ieee8021SpbAdjDynamicEntryPortIdentifier;
		StorageNew->ieee8021SpbAdjDynamicEntryPeerPortIdentifier = thedata->ieee8021SpbAdjDynamicEntryPeerPortIdentifier;
		StorageNew->ieee8021SpbAdjDynamicEntryIsisCircIndex = thedata->ieee8021SpbAdjDynamicEntryIsisCircIndex;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbAdjDynamicTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbAdjDynamicTable_destroy(struct ieee8021SpbAdjDynamicTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbAdjDynamicTable_destroy(struct ieee8021SpbAdjDynamicTable_data **thedata)
{
	struct ieee8021SpbAdjDynamicTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjDynamicTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbAdjDynamicEntryPeerSysId);
		StorageDel->ieee8021SpbAdjDynamicEntryPeerSysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbAdjDynamicEntryPeerSysName);
		StorageDel->ieee8021SpbAdjDynamicEntryPeerSysNameLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbAdjDynamicEntryPeerAgreeDigest);
		StorageDel->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbAdjDynamicEntryPeerMCID);
		StorageDel->ieee8021SpbAdjDynamicEntryPeerMCIDLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbAdjDynamicEntryPeerAuxMCID);
		StorageDel->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbAdjDynamicTable_add(struct ieee8021SpbAdjDynamicTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbAdjDynamicTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbAdjDynamicTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbAdjDynamicTable_add(struct ieee8021SpbAdjDynamicTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjDynamicTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbAdjDynamicEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbAdjDynamicEntryTopIx, sizeof(thedata->ieee8021SpbAdjDynamicEntryTopIx));
		/* ieee8021SpbAdjDynamicEntryIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbAdjDynamicEntryIfIndex, sizeof(thedata->ieee8021SpbAdjDynamicEntryIfIndex));
		/* ieee8021SpbAdjDynamicEntryPeerSysId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbAdjDynamicEntryPeerSysId, thedata->ieee8021SpbAdjDynamicEntryPeerSysIdLen);
		header_complex_add_data(&ieee8021SpbAdjDynamicTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbAdjDynamicTable_del(struct ieee8021SpbAdjDynamicTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbAdjDynamicTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbAdjDynamicTable_del(struct ieee8021SpbAdjDynamicTable_data *thedata)
{
	struct ieee8021SpbAdjDynamicTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbAdjDynamicTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbAdjDynamicTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbAdjDynamicTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbAdjDynamicTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbAdjDynamicTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbAdjDynamicTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbAdjDynamicTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbAdjDynamicTable_data *StorageTmp = ieee8021SpbAdjDynamicTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbAdjDynamicTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryTopIx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbAdjDynamicEntryIfIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysId, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysIdLen);
	if (StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysId == NULL) {
		config_perror("invalid specification for ieee8021SpbAdjDynamicEntryPeerSysId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbAdjDynamicEntryIfOperState, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysName, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysNameLen);
	if (StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysName == NULL) {
		config_perror("invalid specification for ieee8021SpbAdjDynamicEntryPeerSysName");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigest);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigest, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen);
	if (StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigest == NULL) {
		config_perror("invalid specification for ieee8021SpbAdjDynamicEntryPeerAgreeDigest");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCID, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCIDLen);
	if (StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCID == NULL) {
		config_perror("invalid specification for ieee8021SpbAdjDynamicEntryPeerMCID");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCID, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen);
	if (StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCID == NULL) {
		config_perror("invalid specification for ieee8021SpbAdjDynamicEntryPeerAuxMCID");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryLocalCircuitID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPortIdentifier, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerPortIdentifier, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbAdjDynamicEntryIsisCircIndex, &tmpsize);
	ieee8021SpbAdjDynamicTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbAdjDynamicTable(): store configuraiton file for ieee8021SpbAdjDynamicTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbAdjDynamicTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbAdjDynamicTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbAdjDynamicTable: storing data...  "));
	refresh_ieee8021SpbAdjDynamicTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbAdjDynamicTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbAdjDynamicTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbAdjDynamicTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysId, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryIfOperState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysName, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigest, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCID, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCIDLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCID, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryLocalCircuitID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPortIdentifier, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryPeerPortIdentifier, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbAdjDynamicEntryIsisCircIndex, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbTopNodeTable_data *ieee8021SpbTopNodeTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbTopNodeTable table.
 *
 * Creates a new ieee8021SpbTopNodeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbTopNodeTable_data *
ieee8021SpbTopNodeTable_create(void)
{
	struct ieee8021SpbTopNodeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopNodeTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopNodeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021SpbTopNodeEntryBridgePriority = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbTopNodeEntryBridgePriorityLen = 0;
		StorageNew->ieee8021SpbTopNodeEntryBridgePriority[StorageNew->ieee8021SpbTopNodeEntryBridgePriorityLen] = 0;
		if ((StorageNew->ieee8021SpbmTopNodeEntrySPsourceID = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbmTopNodeEntrySPsourceIDLen = 0;
		StorageNew->ieee8021SpbmTopNodeEntrySPsourceID[StorageNew->ieee8021SpbmTopNodeEntrySPsourceIDLen] = 0;
		if ((StorageNew->ieee8021SpbTopNodeEntrySysName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbTopNodeEntrySysNameLen = 0;
		StorageNew->ieee8021SpbTopNodeEntrySysName[StorageNew->ieee8021SpbTopNodeEntrySysNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbTopNodeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbTopNodeTable_data *ieee8021SpbTopNodeTable_duplicate(struct ieee8021SpbTopNodeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbTopNodeTable_data *
ieee8021SpbTopNodeTable_duplicate(struct ieee8021SpbTopNodeTable_data *thedata)
{
	struct ieee8021SpbTopNodeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopNodeTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopNodeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbTopNodeTable_id = thedata->ieee8021SpbTopNodeTable_id;
		StorageNew->ieee8021SpbTopNodeEntryTopIx = thedata->ieee8021SpbTopNodeEntryTopIx;
		if (!(StorageNew->ieee8021SpbTopNodeEntrySysId = malloc(thedata->ieee8021SpbTopNodeEntrySysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopNodeEntrySysId, thedata->ieee8021SpbTopNodeEntrySysId, thedata->ieee8021SpbTopNodeEntrySysIdLen);
		StorageNew->ieee8021SpbTopNodeEntrySysIdLen = thedata->ieee8021SpbTopNodeEntrySysIdLen;
		StorageNew->ieee8021SpbTopNodeEntrySysId[StorageNew->ieee8021SpbTopNodeEntrySysIdLen] = 0;
		if (!(StorageNew->ieee8021SpbTopNodeEntryBridgePriority = malloc(thedata->ieee8021SpbTopNodeEntryBridgePriorityLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopNodeEntryBridgePriority, thedata->ieee8021SpbTopNodeEntryBridgePriority, thedata->ieee8021SpbTopNodeEntryBridgePriorityLen);
		StorageNew->ieee8021SpbTopNodeEntryBridgePriorityLen = thedata->ieee8021SpbTopNodeEntryBridgePriorityLen;
		StorageNew->ieee8021SpbTopNodeEntryBridgePriority[StorageNew->ieee8021SpbTopNodeEntryBridgePriorityLen] = 0;
		if (!(StorageNew->ieee8021SpbmTopNodeEntrySPsourceID = malloc(thedata->ieee8021SpbmTopNodeEntrySPsourceIDLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmTopNodeEntrySPsourceID, thedata->ieee8021SpbmTopNodeEntrySPsourceID, thedata->ieee8021SpbmTopNodeEntrySPsourceIDLen);
		StorageNew->ieee8021SpbmTopNodeEntrySPsourceIDLen = thedata->ieee8021SpbmTopNodeEntrySPsourceIDLen;
		StorageNew->ieee8021SpbmTopNodeEntrySPsourceID[StorageNew->ieee8021SpbmTopNodeEntrySPsourceIDLen] = 0;
		if (!(StorageNew->ieee8021SpbTopNodeEntrySysName = malloc(thedata->ieee8021SpbTopNodeEntrySysNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopNodeEntrySysName, thedata->ieee8021SpbTopNodeEntrySysName, thedata->ieee8021SpbTopNodeEntrySysNameLen);
		StorageNew->ieee8021SpbTopNodeEntrySysNameLen = thedata->ieee8021SpbTopNodeEntrySysNameLen;
		StorageNew->ieee8021SpbTopNodeEntrySysName[StorageNew->ieee8021SpbTopNodeEntrySysNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbTopNodeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbTopNodeTable_destroy(struct ieee8021SpbTopNodeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbTopNodeTable_destroy(struct ieee8021SpbTopNodeTable_data **thedata)
{
	struct ieee8021SpbTopNodeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopNodeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbTopNodeEntrySysId);
		StorageDel->ieee8021SpbTopNodeEntrySysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbTopNodeEntryBridgePriority);
		StorageDel->ieee8021SpbTopNodeEntryBridgePriorityLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbmTopNodeEntrySPsourceID);
		StorageDel->ieee8021SpbmTopNodeEntrySPsourceIDLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbTopNodeEntrySysName);
		StorageDel->ieee8021SpbTopNodeEntrySysNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopNodeTable_add(struct ieee8021SpbTopNodeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbTopNodeTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbTopNodeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbTopNodeTable_add(struct ieee8021SpbTopNodeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopNodeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbTopNodeEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopNodeEntryTopIx, sizeof(thedata->ieee8021SpbTopNodeEntryTopIx));
		/* ieee8021SpbTopNodeEntrySysId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbTopNodeEntrySysId, thedata->ieee8021SpbTopNodeEntrySysIdLen);
		header_complex_add_data(&ieee8021SpbTopNodeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopNodeTable_del(struct ieee8021SpbTopNodeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbTopNodeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbTopNodeTable_del(struct ieee8021SpbTopNodeTable_data *thedata)
{
	struct ieee8021SpbTopNodeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopNodeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbTopNodeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbTopNodeTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbTopNodeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbTopNodeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbTopNodeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbTopNodeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbTopNodeTable_data *StorageTmp = ieee8021SpbTopNodeTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbTopNodeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopNodeEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbTopNodeEntrySysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopNodeEntrySysId, &StorageTmp->ieee8021SpbTopNodeEntrySysIdLen);
	if (StorageTmp->ieee8021SpbTopNodeEntrySysId == NULL) {
		config_perror("invalid specification for ieee8021SpbTopNodeEntrySysId");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbTopNodeEntryBridgePriority);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopNodeEntryBridgePriority, &StorageTmp->ieee8021SpbTopNodeEntryBridgePriorityLen);
	if (StorageTmp->ieee8021SpbTopNodeEntryBridgePriority == NULL) {
		config_perror("invalid specification for ieee8021SpbTopNodeEntryBridgePriority");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbmTopNodeEntrySPsourceID);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmTopNodeEntrySPsourceID, &StorageTmp->ieee8021SpbmTopNodeEntrySPsourceIDLen);
	if (StorageTmp->ieee8021SpbmTopNodeEntrySPsourceID == NULL) {
		config_perror("invalid specification for ieee8021SpbmTopNodeEntrySPsourceID");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbTopNodeEntrySysName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopNodeEntrySysName, &StorageTmp->ieee8021SpbTopNodeEntrySysNameLen);
	if (StorageTmp->ieee8021SpbTopNodeEntrySysName == NULL) {
		config_perror("invalid specification for ieee8021SpbTopNodeEntrySysName");
		return;
	}
	ieee8021SpbTopNodeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbTopNodeTable(): store configuraiton file for ieee8021SpbTopNodeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbTopNodeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbTopNodeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbTopNodeTable: storing data...  "));
	refresh_ieee8021SpbTopNodeTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbTopNodeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbTopNodeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbTopNodeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopNodeEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopNodeEntrySysId, &StorageTmp->ieee8021SpbTopNodeEntrySysIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopNodeEntryBridgePriority, &StorageTmp->ieee8021SpbTopNodeEntryBridgePriorityLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmTopNodeEntrySPsourceID, &StorageTmp->ieee8021SpbmTopNodeEntrySPsourceIDLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopNodeEntrySysName, &StorageTmp->ieee8021SpbTopNodeEntrySysNameLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbTopEctTable_data *ieee8021SpbTopEctTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbTopEctTable table.
 *
 * Creates a new ieee8021SpbTopEctTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbTopEctTable_data *
ieee8021SpbTopEctTable_create(void)
{
	struct ieee8021SpbTopEctTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopEctTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEctTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021SpbTopEctEntryEctAlgorithm = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbTopEctEntryEctAlgorithmLen = 0;
		StorageNew->ieee8021SpbTopEctEntryEctAlgorithm[StorageNew->ieee8021SpbTopEctEntryEctAlgorithmLen] = 0;
		StorageNew->ieee8021SpbTopEctEntryMode = 0;
		StorageNew->ieee8021SpbvTopEctSysMode = 0;
		StorageNew->ieee8021SpbvTopEctEntrySpvid = 0;
		StorageNew->ieee8021SpbTopEctEntryLocalUse = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbTopEctTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbTopEctTable_data *ieee8021SpbTopEctTable_duplicate(struct ieee8021SpbTopEctTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbTopEctTable_data *
ieee8021SpbTopEctTable_duplicate(struct ieee8021SpbTopEctTable_data *thedata)
{
	struct ieee8021SpbTopEctTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopEctTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEctTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbTopEctTable_id = thedata->ieee8021SpbTopEctTable_id;
		StorageNew->ieee8021SpbTopEctEntryTopIx = thedata->ieee8021SpbTopEctEntryTopIx;
		if (!(StorageNew->ieee8021SpbTopEctEntrySysId = malloc(thedata->ieee8021SpbTopEctEntrySysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopEctEntrySysId, thedata->ieee8021SpbTopEctEntrySysId, thedata->ieee8021SpbTopEctEntrySysIdLen);
		StorageNew->ieee8021SpbTopEctEntrySysIdLen = thedata->ieee8021SpbTopEctEntrySysIdLen;
		StorageNew->ieee8021SpbTopEctEntrySysId[StorageNew->ieee8021SpbTopEctEntrySysIdLen] = 0;
		StorageNew->ieee8021SpbTopEctEntryBaseVid = thedata->ieee8021SpbTopEctEntryBaseVid;
		if (!(StorageNew->ieee8021SpbTopEctEntryEctAlgorithm = malloc(thedata->ieee8021SpbTopEctEntryEctAlgorithmLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopEctEntryEctAlgorithm, thedata->ieee8021SpbTopEctEntryEctAlgorithm, thedata->ieee8021SpbTopEctEntryEctAlgorithmLen);
		StorageNew->ieee8021SpbTopEctEntryEctAlgorithmLen = thedata->ieee8021SpbTopEctEntryEctAlgorithmLen;
		StorageNew->ieee8021SpbTopEctEntryEctAlgorithm[StorageNew->ieee8021SpbTopEctEntryEctAlgorithmLen] = 0;
		StorageNew->ieee8021SpbTopEctEntryMode = thedata->ieee8021SpbTopEctEntryMode;
		StorageNew->ieee8021SpbvTopEctSysMode = thedata->ieee8021SpbvTopEctSysMode;
		StorageNew->ieee8021SpbvTopEctEntrySpvid = thedata->ieee8021SpbvTopEctEntrySpvid;
		StorageNew->ieee8021SpbTopEctEntryLocalUse = thedata->ieee8021SpbTopEctEntryLocalUse;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbTopEctTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbTopEctTable_destroy(struct ieee8021SpbTopEctTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbTopEctTable_destroy(struct ieee8021SpbTopEctTable_data **thedata)
{
	struct ieee8021SpbTopEctTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEctTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbTopEctEntrySysId);
		StorageDel->ieee8021SpbTopEctEntrySysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbTopEctEntryEctAlgorithm);
		StorageDel->ieee8021SpbTopEctEntryEctAlgorithmLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopEctTable_add(struct ieee8021SpbTopEctTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbTopEctTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbTopEctTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbTopEctTable_add(struct ieee8021SpbTopEctTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEctTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbTopEctEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopEctEntryTopIx, sizeof(thedata->ieee8021SpbTopEctEntryTopIx));
		/* ieee8021SpbTopEctEntrySysId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbTopEctEntrySysId, thedata->ieee8021SpbTopEctEntrySysIdLen);
		/* ieee8021SpbTopEctEntryBaseVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbTopEctEntryBaseVid, sizeof(thedata->ieee8021SpbTopEctEntryBaseVid));
		header_complex_add_data(&ieee8021SpbTopEctTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopEctTable_del(struct ieee8021SpbTopEctTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbTopEctTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbTopEctTable_del(struct ieee8021SpbTopEctTable_data *thedata)
{
	struct ieee8021SpbTopEctTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEctTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbTopEctTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbTopEctTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbTopEctTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbTopEctTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbTopEctTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbTopEctTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbTopEctTable_data *StorageTmp = ieee8021SpbTopEctTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbTopEctTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopEctEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbTopEctEntrySysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopEctEntrySysId, &StorageTmp->ieee8021SpbTopEctEntrySysIdLen);
	if (StorageTmp->ieee8021SpbTopEctEntrySysId == NULL) {
		config_perror("invalid specification for ieee8021SpbTopEctEntrySysId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbTopEctEntryBaseVid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbTopEctEntryEctAlgorithm);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopEctEntryEctAlgorithm, &StorageTmp->ieee8021SpbTopEctEntryEctAlgorithmLen);
	if (StorageTmp->ieee8021SpbTopEctEntryEctAlgorithm == NULL) {
		config_perror("invalid specification for ieee8021SpbTopEctEntryEctAlgorithm");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbTopEctEntryMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbvTopEctSysMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbvTopEctEntrySpvid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbTopEctEntryLocalUse, &tmpsize);
	ieee8021SpbTopEctTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbTopEctTable(): store configuraiton file for ieee8021SpbTopEctTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbTopEctTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbTopEctTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbTopEctTable: storing data...  "));
	refresh_ieee8021SpbTopEctTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbTopEctTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbTopEctTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbTopEctTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopEctEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopEctEntrySysId, &StorageTmp->ieee8021SpbTopEctEntrySysIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbTopEctEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopEctEntryEctAlgorithm, &StorageTmp->ieee8021SpbTopEctEntryEctAlgorithmLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbTopEctEntryMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbvTopEctSysMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbvTopEctEntrySpvid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbTopEctEntryLocalUse, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbTopEdgeTable_data *ieee8021SpbTopEdgeTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbTopEdgeTable table.
 *
 * Creates a new ieee8021SpbTopEdgeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbTopEdgeTable_data *
ieee8021SpbTopEdgeTable_create(void)
{
	struct ieee8021SpbTopEdgeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopEdgeTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEdgeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbTopEdgeEntryMetricNear2Far = 0;
		StorageNew->ieee8021SpbTopEdgeEntryMetricFar2Near = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbTopEdgeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbTopEdgeTable_data *ieee8021SpbTopEdgeTable_duplicate(struct ieee8021SpbTopEdgeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbTopEdgeTable_data *
ieee8021SpbTopEdgeTable_duplicate(struct ieee8021SpbTopEdgeTable_data *thedata)
{
	struct ieee8021SpbTopEdgeTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbTopEdgeTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEdgeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbTopEdgeTable_id = thedata->ieee8021SpbTopEdgeTable_id;
		StorageNew->ieee8021SpbTopEdgeEntryTopIx = thedata->ieee8021SpbTopEdgeEntryTopIx;
		if (!(StorageNew->ieee8021SpbTopEdgeEntrySysIdNear = malloc(thedata->ieee8021SpbTopEdgeEntrySysIdNearLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopEdgeEntrySysIdNear, thedata->ieee8021SpbTopEdgeEntrySysIdNear, thedata->ieee8021SpbTopEdgeEntrySysIdNearLen);
		StorageNew->ieee8021SpbTopEdgeEntrySysIdNearLen = thedata->ieee8021SpbTopEdgeEntrySysIdNearLen;
		StorageNew->ieee8021SpbTopEdgeEntrySysIdNear[StorageNew->ieee8021SpbTopEdgeEntrySysIdNearLen] = 0;
		if (!(StorageNew->ieee8021SpbTopEdgeEntrySysIdFar = malloc(thedata->ieee8021SpbTopEdgeEntrySysIdFarLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbTopEdgeEntrySysIdFar, thedata->ieee8021SpbTopEdgeEntrySysIdFar, thedata->ieee8021SpbTopEdgeEntrySysIdFarLen);
		StorageNew->ieee8021SpbTopEdgeEntrySysIdFarLen = thedata->ieee8021SpbTopEdgeEntrySysIdFarLen;
		StorageNew->ieee8021SpbTopEdgeEntrySysIdFar[StorageNew->ieee8021SpbTopEdgeEntrySysIdFarLen] = 0;
		StorageNew->ieee8021SpbTopEdgeEntryMetricNear2Far = thedata->ieee8021SpbTopEdgeEntryMetricNear2Far;
		StorageNew->ieee8021SpbTopEdgeEntryMetricFar2Near = thedata->ieee8021SpbTopEdgeEntryMetricFar2Near;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbTopEdgeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbTopEdgeTable_destroy(struct ieee8021SpbTopEdgeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbTopEdgeTable_destroy(struct ieee8021SpbTopEdgeTable_data **thedata)
{
	struct ieee8021SpbTopEdgeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEdgeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbTopEdgeEntrySysIdNear);
		StorageDel->ieee8021SpbTopEdgeEntrySysIdNearLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbTopEdgeEntrySysIdFar);
		StorageDel->ieee8021SpbTopEdgeEntrySysIdFarLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopEdgeTable_add(struct ieee8021SpbTopEdgeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbTopEdgeTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbTopEdgeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbTopEdgeTable_add(struct ieee8021SpbTopEdgeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEdgeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbTopEdgeEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopEdgeEntryTopIx, sizeof(thedata->ieee8021SpbTopEdgeEntryTopIx));
		/* ieee8021SpbTopEdgeEntrySysIdNear */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbTopEdgeEntrySysIdNear, thedata->ieee8021SpbTopEdgeEntrySysIdNearLen);
		/* ieee8021SpbTopEdgeEntrySysIdFar */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbTopEdgeEntrySysIdFar, thedata->ieee8021SpbTopEdgeEntrySysIdFarLen);
		header_complex_add_data(&ieee8021SpbTopEdgeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbTopEdgeTable_del(struct ieee8021SpbTopEdgeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbTopEdgeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbTopEdgeTable_del(struct ieee8021SpbTopEdgeTable_data *thedata)
{
	struct ieee8021SpbTopEdgeTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbTopEdgeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbTopEdgeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbTopEdgeTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbTopEdgeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbTopEdgeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbTopEdgeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbTopEdgeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbTopEdgeTable_data *StorageTmp = ieee8021SpbTopEdgeTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbTopEdgeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopEdgeEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbTopEdgeEntrySysIdNear);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdNear, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdNearLen);
	if (StorageTmp->ieee8021SpbTopEdgeEntrySysIdNear == NULL) {
		config_perror("invalid specification for ieee8021SpbTopEdgeEntrySysIdNear");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbTopEdgeEntrySysIdFar);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdFar, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdFarLen);
	if (StorageTmp->ieee8021SpbTopEdgeEntrySysIdFar == NULL) {
		config_perror("invalid specification for ieee8021SpbTopEdgeEntrySysIdFar");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbTopEdgeEntryMetricNear2Far, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbTopEdgeEntryMetricFar2Near, &tmpsize);
	ieee8021SpbTopEdgeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbTopEdgeTable(): store configuraiton file for ieee8021SpbTopEdgeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbTopEdgeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbTopEdgeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbTopEdgeTable: storing data...  "));
	refresh_ieee8021SpbTopEdgeTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbTopEdgeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbTopEdgeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbTopEdgeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopEdgeEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdNear, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdNearLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdFar, &StorageTmp->ieee8021SpbTopEdgeEntrySysIdFarLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbTopEdgeEntryMetricNear2Far, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbTopEdgeEntryMetricFar2Near, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbmTopSrvTable_data *ieee8021SpbmTopSrvTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbmTopSrvTable table.
 *
 * Creates a new ieee8021SpbmTopSrvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbmTopSrvTable_data *
ieee8021SpbmTopSrvTable_create(void)
{
	struct ieee8021SpbmTopSrvTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbmTopSrvTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmTopSrvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if (memdup((u_char **) &StorageNew->ieee8021SpbmTopSrvEntryIsidFlags, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021SpbmTopSrvEntryIsidFlagsLen = 1;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbmTopSrvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbmTopSrvTable_data *ieee8021SpbmTopSrvTable_duplicate(struct ieee8021SpbmTopSrvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbmTopSrvTable_data *
ieee8021SpbmTopSrvTable_duplicate(struct ieee8021SpbmTopSrvTable_data *thedata)
{
	struct ieee8021SpbmTopSrvTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbmTopSrvTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmTopSrvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbmTopSrvTable_id = thedata->ieee8021SpbmTopSrvTable_id;
		StorageNew->ieee8021SpbmTopSrvEntryTopIx = thedata->ieee8021SpbmTopSrvEntryTopIx;
		if (!(StorageNew->ieee8021SpbmTopSrvEntrySysId = malloc(thedata->ieee8021SpbmTopSrvEntrySysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmTopSrvEntrySysId, thedata->ieee8021SpbmTopSrvEntrySysId, thedata->ieee8021SpbmTopSrvEntrySysIdLen);
		StorageNew->ieee8021SpbmTopSrvEntrySysIdLen = thedata->ieee8021SpbmTopSrvEntrySysIdLen;
		StorageNew->ieee8021SpbmTopSrvEntrySysId[StorageNew->ieee8021SpbmTopSrvEntrySysIdLen] = 0;
		StorageNew->ieee8021SpbmTopSrvEntryIsid = thedata->ieee8021SpbmTopSrvEntryIsid;
		StorageNew->ieee8021SpbmTopSrvEntryBaseVid = thedata->ieee8021SpbmTopSrvEntryBaseVid;
		if (!(StorageNew->ieee8021SpbmTopSrvEntryMac = malloc(thedata->ieee8021SpbmTopSrvEntryMacLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmTopSrvEntryMac, thedata->ieee8021SpbmTopSrvEntryMac, thedata->ieee8021SpbmTopSrvEntryMacLen);
		StorageNew->ieee8021SpbmTopSrvEntryMacLen = thedata->ieee8021SpbmTopSrvEntryMacLen;
		StorageNew->ieee8021SpbmTopSrvEntryMac[StorageNew->ieee8021SpbmTopSrvEntryMacLen] = 0;
		if (!(StorageNew->ieee8021SpbmTopSrvEntryIsidFlags = malloc(thedata->ieee8021SpbmTopSrvEntryIsidFlagsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmTopSrvEntryIsidFlags, thedata->ieee8021SpbmTopSrvEntryIsidFlags, thedata->ieee8021SpbmTopSrvEntryIsidFlagsLen);
		StorageNew->ieee8021SpbmTopSrvEntryIsidFlagsLen = thedata->ieee8021SpbmTopSrvEntryIsidFlagsLen;
		StorageNew->ieee8021SpbmTopSrvEntryIsidFlags[StorageNew->ieee8021SpbmTopSrvEntryIsidFlagsLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbmTopSrvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbmTopSrvTable_destroy(struct ieee8021SpbmTopSrvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbmTopSrvTable_destroy(struct ieee8021SpbmTopSrvTable_data **thedata)
{
	struct ieee8021SpbmTopSrvTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmTopSrvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbmTopSrvEntrySysId);
		StorageDel->ieee8021SpbmTopSrvEntrySysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbmTopSrvEntryMac);
		StorageDel->ieee8021SpbmTopSrvEntryMacLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbmTopSrvEntryIsidFlags);
		StorageDel->ieee8021SpbmTopSrvEntryIsidFlagsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbmTopSrvTable_add(struct ieee8021SpbmTopSrvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbmTopSrvTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbmTopSrvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbmTopSrvTable_add(struct ieee8021SpbmTopSrvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmTopSrvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbmTopSrvEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbmTopSrvEntryTopIx, sizeof(thedata->ieee8021SpbmTopSrvEntryTopIx));
		/* ieee8021SpbmTopSrvEntrySysId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbmTopSrvEntrySysId, thedata->ieee8021SpbmTopSrvEntrySysIdLen);
		/* ieee8021SpbmTopSrvEntryIsid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbmTopSrvEntryIsid, sizeof(thedata->ieee8021SpbmTopSrvEntryIsid));
		/* ieee8021SpbmTopSrvEntryBaseVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbmTopSrvEntryBaseVid, sizeof(thedata->ieee8021SpbmTopSrvEntryBaseVid));
		/* ieee8021SpbmTopSrvEntryMac */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbmTopSrvEntryMac, thedata->ieee8021SpbmTopSrvEntryMacLen);
		header_complex_add_data(&ieee8021SpbmTopSrvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbmTopSrvTable_del(struct ieee8021SpbmTopSrvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbmTopSrvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbmTopSrvTable_del(struct ieee8021SpbmTopSrvTable_data *thedata)
{
	struct ieee8021SpbmTopSrvTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmTopSrvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbmTopSrvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbmTopSrvTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbmTopSrvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbmTopSrvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbmTopSrvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbmTopSrvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbmTopSrvTable_data *StorageTmp = ieee8021SpbmTopSrvTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbmTopSrvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbmTopSrvEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbmTopSrvEntrySysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmTopSrvEntrySysId, &StorageTmp->ieee8021SpbmTopSrvEntrySysIdLen);
	if (StorageTmp->ieee8021SpbmTopSrvEntrySysId == NULL) {
		config_perror("invalid specification for ieee8021SpbmTopSrvEntrySysId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbmTopSrvEntryIsid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmTopSrvEntryBaseVid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbmTopSrvEntryMac);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmTopSrvEntryMac, &StorageTmp->ieee8021SpbmTopSrvEntryMacLen);
	if (StorageTmp->ieee8021SpbmTopSrvEntryMac == NULL) {
		config_perror("invalid specification for ieee8021SpbmTopSrvEntryMac");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbmTopSrvEntryIsidFlags);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmTopSrvEntryIsidFlags, &StorageTmp->ieee8021SpbmTopSrvEntryIsidFlagsLen);
	if (StorageTmp->ieee8021SpbmTopSrvEntryIsidFlags == NULL) {
		config_perror("invalid specification for ieee8021SpbmTopSrvEntryIsidFlags");
		return;
	}
	ieee8021SpbmTopSrvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbmTopSrvTable(): store configuraiton file for ieee8021SpbmTopSrvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbmTopSrvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbmTopSrvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbmTopSrvTable: storing data...  "));
	refresh_ieee8021SpbmTopSrvTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbmTopSrvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbmTopSrvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbmTopSrvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmTopSrvEntrySysId, &StorageTmp->ieee8021SpbmTopSrvEntrySysIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryIsid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryMac, &StorageTmp->ieee8021SpbmTopSrvEntryMacLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryIsidFlags, &StorageTmp->ieee8021SpbmTopSrvEntryIsidFlagsLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbvTopSrvTable_data *ieee8021SpbvTopSrvTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbvTopSrvTable table.
 *
 * Creates a new ieee8021SpbvTopSrvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbvTopSrvTable_data *
ieee8021SpbvTopSrvTable_create(void)
{
	struct ieee8021SpbvTopSrvTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbvTopSrvTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbvTopSrvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbvTopSrvEntryBaseVid = 0;
		if (memdup((u_char **) &StorageNew->ieee8021SpbvTopSrvEntryMMacFlags, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021SpbvTopSrvEntryMMacFlagsLen = 1;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbvTopSrvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbvTopSrvTable_data *ieee8021SpbvTopSrvTable_duplicate(struct ieee8021SpbvTopSrvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbvTopSrvTable_data *
ieee8021SpbvTopSrvTable_duplicate(struct ieee8021SpbvTopSrvTable_data *thedata)
{
	struct ieee8021SpbvTopSrvTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbvTopSrvTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbvTopSrvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbvTopSrvTable_id = thedata->ieee8021SpbvTopSrvTable_id;
		StorageNew->ieee8021SpbvTopSrvEntryTopIx = thedata->ieee8021SpbvTopSrvEntryTopIx;
		if (!(StorageNew->ieee8021SpbvTopSrvEntrySysId = malloc(thedata->ieee8021SpbvTopSrvEntrySysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbvTopSrvEntrySysId, thedata->ieee8021SpbvTopSrvEntrySysId, thedata->ieee8021SpbvTopSrvEntrySysIdLen);
		StorageNew->ieee8021SpbvTopSrvEntrySysIdLen = thedata->ieee8021SpbvTopSrvEntrySysIdLen;
		StorageNew->ieee8021SpbvTopSrvEntrySysId[StorageNew->ieee8021SpbvTopSrvEntrySysIdLen] = 0;
		if (!(StorageNew->ieee8021SpbvTopSrvEntryMMac = malloc(thedata->ieee8021SpbvTopSrvEntryMMacLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbvTopSrvEntryMMac, thedata->ieee8021SpbvTopSrvEntryMMac, thedata->ieee8021SpbvTopSrvEntryMMacLen);
		StorageNew->ieee8021SpbvTopSrvEntryMMacLen = thedata->ieee8021SpbvTopSrvEntryMMacLen;
		StorageNew->ieee8021SpbvTopSrvEntryMMac[StorageNew->ieee8021SpbvTopSrvEntryMMacLen] = 0;
		StorageNew->ieee8021SpbvTopSrvEntryBaseVid = thedata->ieee8021SpbvTopSrvEntryBaseVid;
		if (!(StorageNew->ieee8021SpbvTopSrvEntryMMacFlags = malloc(thedata->ieee8021SpbvTopSrvEntryMMacFlagsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbvTopSrvEntryMMacFlags, thedata->ieee8021SpbvTopSrvEntryMMacFlags, thedata->ieee8021SpbvTopSrvEntryMMacFlagsLen);
		StorageNew->ieee8021SpbvTopSrvEntryMMacFlagsLen = thedata->ieee8021SpbvTopSrvEntryMMacFlagsLen;
		StorageNew->ieee8021SpbvTopSrvEntryMMacFlags[StorageNew->ieee8021SpbvTopSrvEntryMMacFlagsLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbvTopSrvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbvTopSrvTable_destroy(struct ieee8021SpbvTopSrvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbvTopSrvTable_destroy(struct ieee8021SpbvTopSrvTable_data **thedata)
{
	struct ieee8021SpbvTopSrvTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbvTopSrvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbvTopSrvEntrySysId);
		StorageDel->ieee8021SpbvTopSrvEntrySysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbvTopSrvEntryMMac);
		StorageDel->ieee8021SpbvTopSrvEntryMMacLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbvTopSrvEntryMMacFlags);
		StorageDel->ieee8021SpbvTopSrvEntryMMacFlagsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbvTopSrvTable_add(struct ieee8021SpbvTopSrvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbvTopSrvTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbvTopSrvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbvTopSrvTable_add(struct ieee8021SpbvTopSrvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbvTopSrvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbvTopSrvEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbvTopSrvEntryTopIx, sizeof(thedata->ieee8021SpbvTopSrvEntryTopIx));
		/* ieee8021SpbvTopSrvEntrySysId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbvTopSrvEntrySysId, thedata->ieee8021SpbvTopSrvEntrySysIdLen);
		/* ieee8021SpbvTopSrvEntryMMac */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbvTopSrvEntryMMac, thedata->ieee8021SpbvTopSrvEntryMMacLen);
		header_complex_add_data(&ieee8021SpbvTopSrvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbvTopSrvTable_del(struct ieee8021SpbvTopSrvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbvTopSrvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbvTopSrvTable_del(struct ieee8021SpbvTopSrvTable_data *thedata)
{
	struct ieee8021SpbvTopSrvTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbvTopSrvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbvTopSrvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbvTopSrvTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbvTopSrvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbvTopSrvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbvTopSrvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbvTopSrvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbvTopSrvTable_data *StorageTmp = ieee8021SpbvTopSrvTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbvTopSrvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbvTopSrvEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbvTopSrvEntrySysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbvTopSrvEntrySysId, &StorageTmp->ieee8021SpbvTopSrvEntrySysIdLen);
	if (StorageTmp->ieee8021SpbvTopSrvEntrySysId == NULL) {
		config_perror("invalid specification for ieee8021SpbvTopSrvEntrySysId");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021SpbvTopSrvEntryMMac);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbvTopSrvEntryMMac, &StorageTmp->ieee8021SpbvTopSrvEntryMMacLen);
	if (StorageTmp->ieee8021SpbvTopSrvEntryMMac == NULL) {
		config_perror("invalid specification for ieee8021SpbvTopSrvEntryMMac");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbvTopSrvEntryBaseVid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbvTopSrvEntryMMacFlags);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbvTopSrvEntryMMacFlags, &StorageTmp->ieee8021SpbvTopSrvEntryMMacFlagsLen);
	if (StorageTmp->ieee8021SpbvTopSrvEntryMMacFlags == NULL) {
		config_perror("invalid specification for ieee8021SpbvTopSrvEntryMMacFlags");
		return;
	}
	ieee8021SpbvTopSrvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbvTopSrvTable(): store configuraiton file for ieee8021SpbvTopSrvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbvTopSrvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbvTopSrvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbvTopSrvTable: storing data...  "));
	refresh_ieee8021SpbvTopSrvTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbvTopSrvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbvTopSrvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbvTopSrvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbvTopSrvEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbvTopSrvEntrySysId, &StorageTmp->ieee8021SpbvTopSrvEntrySysIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbvTopSrvEntryMMac, &StorageTmp->ieee8021SpbvTopSrvEntryMMacLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbvTopSrvEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbvTopSrvEntryMMacFlags, &StorageTmp->ieee8021SpbvTopSrvEntryMMacFlagsLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021SpbmBsiStaticTable_data *ieee8021SpbmBsiStaticTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021SpbmBsiStaticTable table.
 *
 * Creates a new ieee8021SpbmBsiStaticTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021SpbmBsiStaticTable_data *
ieee8021SpbmBsiStaticTable_create(void)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbmBsiStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmBsiStaticTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbTopIx = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021SpbmBsiStaticEntryTBit = 0;
		StorageNew->ieee8021SpbmBsiStaticEntryRBit = 0;
		StorageNew->ieee8021SpbmBsiStaticEntryTsBit = 0;
		StorageNew->ieee8021SpbmBsiStaticEntryTieBreakMask = 0;
		StorageNew->ieee8021SpbmBsiStaticEntryRowStatus = 0;
		StorageNew->ieee8021SpbmBsiStaticEntryRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021SpbmBsiStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021SpbmBsiStaticTable_data *ieee8021SpbmBsiStaticTable_duplicate(struct ieee8021SpbmBsiStaticTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021SpbmBsiStaticTable_data *
ieee8021SpbmBsiStaticTable_duplicate(struct ieee8021SpbmBsiStaticTable_data *thedata)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021SpbmBsiStaticTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmBsiStaticTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021SpbmBsiStaticTable_id = thedata->ieee8021SpbmBsiStaticTable_id;
		StorageNew->ieee8021SpbTopIx = thedata->ieee8021SpbTopIx;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021SpbmBsiStaticEntryIsid = thedata->ieee8021SpbmBsiStaticEntryIsid;
		StorageNew->ieee8021SpbmBsiStaticEntryBaseVid = thedata->ieee8021SpbmBsiStaticEntryBaseVid;
		StorageNew->ieee8021SpbmBsiStaticEntryTBit = thedata->ieee8021SpbmBsiStaticEntryTBit;
		StorageNew->ieee8021SpbmBsiStaticEntryRBit = thedata->ieee8021SpbmBsiStaticEntryRBit;
		StorageNew->ieee8021SpbmBsiStaticEntryTsBit = thedata->ieee8021SpbmBsiStaticEntryTsBit;
		StorageNew->ieee8021SpbmBsiStaticEntryTieBreakMask = thedata->ieee8021SpbmBsiStaticEntryTieBreakMask;
		StorageNew->ieee8021SpbmBsiStaticEntryRowStatus = thedata->ieee8021SpbmBsiStaticEntryRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021SpbmBsiStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021SpbmBsiStaticTable_destroy(struct ieee8021SpbmBsiStaticTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021SpbmBsiStaticTable_destroy(struct ieee8021SpbmBsiStaticTable_data **thedata)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmBsiStaticTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbmBsiStaticTable_add(struct ieee8021SpbmBsiStaticTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021SpbmBsiStaticTable table data set.
 *
 * Adds a table row structure to the ieee8021SpbmBsiStaticTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021SpbmBsiStaticTable_add(struct ieee8021SpbmBsiStaticTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmBsiStaticTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopIx, sizeof(thedata->ieee8021SpbTopIx));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021SpbmBsiStaticEntryIsid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbmBsiStaticEntryIsid, sizeof(thedata->ieee8021SpbmBsiStaticEntryIsid));
		/* ieee8021SpbmBsiStaticEntryBaseVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbmBsiStaticEntryBaseVid, sizeof(thedata->ieee8021SpbmBsiStaticEntryBaseVid));
		header_complex_add_data(&ieee8021SpbmBsiStaticTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021SpbmBsiStaticTable_del(struct ieee8021SpbmBsiStaticTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021SpbmBsiStaticTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021SpbmBsiStaticTable_del(struct ieee8021SpbmBsiStaticTable_data *thedata)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbmBsiStaticTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021SpbmBsiStaticTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021SpbmBsiStaticTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021SpbmBsiStaticTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021SpbmBsiStaticTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021SpbmBsiStaticTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021SpbmBsiStaticTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = ieee8021SpbmBsiStaticTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_ieee8021SpbmBsiStaticTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopIx, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbmBsiStaticEntryIsid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmBsiStaticEntryBaseVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmBsiStaticEntryTBit, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmBsiStaticEntryRBit, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmBsiStaticEntryTsBit, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus, &tmpsize);
	ieee8021SpbmBsiStaticTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_ieee8021SpbmBsiStaticTable(): store configuraiton file for ieee8021SpbmBsiStaticTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021SpbmBsiStaticTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_ieee8021SpbmBsiStaticTable: storing data...  "));
	refresh_ieee8021SpbmBsiStaticTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021SpbmBsiStaticTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021SpbmBsiStaticTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021SpbmBsiStaticTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryIsid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryTBit, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryRBit, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryTsBit, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dot1agCfmMepSpbmTable_data *dot1agCfmMepSpbmTable_create(void)
 * @brief create a fresh data structure representing a new row in the dot1agCfmMepSpbmTable table.
 *
 * Creates a new dot1agCfmMepSpbmTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dot1agCfmMepSpbmTable_data *
dot1agCfmMepSpbmTable_create(void)
{
	struct dot1agCfmMepSpbmTable_data *StorageNew = SNMP_MALLOC_STRUCT(dot1agCfmMepSpbmTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dot1agCfmMdIndex = 0;
		StorageNew->dot1agCfmMaIndex = 0;
		StorageNew->dot1agCfmMepIdentifier = 0;
		if ((StorageNew->dot1agCfmMepTransmitLbmSpbmDA = malloc(1)) == NULL)
			goto nomem;
		StorageNew->dot1agCfmMepTransmitLbmSpbmDALen = 0;
		StorageNew->dot1agCfmMepTransmitLbmSpbmDA[StorageNew->dot1agCfmMepTransmitLbmSpbmDALen] = 0;
		if ((StorageNew->dot1agCfmMepTransmitLtmSpbmDA = malloc(1)) == NULL)
			goto nomem;
		StorageNew->dot1agCfmMepTransmitLtmSpbmDALen = 0;
		StorageNew->dot1agCfmMepTransmitLtmSpbmDA[StorageNew->dot1agCfmMepTransmitLtmSpbmDALen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	dot1agCfmMepSpbmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct dot1agCfmMepSpbmTable_data *dot1agCfmMepSpbmTable_duplicate(struct dot1agCfmMepSpbmTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dot1agCfmMepSpbmTable_data *
dot1agCfmMepSpbmTable_duplicate(struct dot1agCfmMepSpbmTable_data *thedata)
{
	struct dot1agCfmMepSpbmTable_data *StorageNew = SNMP_MALLOC_STRUCT(dot1agCfmMepSpbmTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->dot1agCfmMepSpbmTable_id = thedata->dot1agCfmMepSpbmTable_id;
		StorageNew->dot1agCfmMdIndex = thedata->dot1agCfmMdIndex;
		StorageNew->dot1agCfmMaIndex = thedata->dot1agCfmMaIndex;
		StorageNew->dot1agCfmMepIdentifier = thedata->dot1agCfmMepIdentifier;
		if (!(StorageNew->dot1agCfmMepTransmitLbmSpbmDA = malloc(thedata->dot1agCfmMepTransmitLbmSpbmDALen + 1)))
			goto destroy;
		memcpy(StorageNew->dot1agCfmMepTransmitLbmSpbmDA, thedata->dot1agCfmMepTransmitLbmSpbmDA, thedata->dot1agCfmMepTransmitLbmSpbmDALen);
		StorageNew->dot1agCfmMepTransmitLbmSpbmDALen = thedata->dot1agCfmMepTransmitLbmSpbmDALen;
		StorageNew->dot1agCfmMepTransmitLbmSpbmDA[StorageNew->dot1agCfmMepTransmitLbmSpbmDALen] = 0;
		if (!(StorageNew->dot1agCfmMepTransmitLtmSpbmDA = malloc(thedata->dot1agCfmMepTransmitLtmSpbmDALen + 1)))
			goto destroy;
		memcpy(StorageNew->dot1agCfmMepTransmitLtmSpbmDA, thedata->dot1agCfmMepTransmitLtmSpbmDA, thedata->dot1agCfmMepTransmitLtmSpbmDALen);
		StorageNew->dot1agCfmMepTransmitLtmSpbmDALen = thedata->dot1agCfmMepTransmitLtmSpbmDALen;
		StorageNew->dot1agCfmMepTransmitLtmSpbmDA[StorageNew->dot1agCfmMepTransmitLtmSpbmDALen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dot1agCfmMepSpbmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dot1agCfmMepSpbmTable_destroy(struct dot1agCfmMepSpbmTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dot1agCfmMepSpbmTable_destroy(struct dot1agCfmMepSpbmTable_data **thedata)
{
	struct dot1agCfmMepSpbmTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->dot1agCfmMepTransmitLbmSpbmDA);
		StorageDel->dot1agCfmMepTransmitLbmSpbmDALen = 0;
		SNMP_FREE(StorageDel->dot1agCfmMepTransmitLtmSpbmDA);
		StorageDel->dot1agCfmMepTransmitLtmSpbmDALen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dot1agCfmMepSpbmTable_add(struct dot1agCfmMepSpbmTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dot1agCfmMepSpbmTable table data set.
 *
 * Adds a table row structure to the dot1agCfmMepSpbmTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dot1agCfmMepSpbmTable_add(struct dot1agCfmMepSpbmTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* dot1agCfmMdIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->dot1agCfmMdIndex, sizeof(thedata->dot1agCfmMdIndex));
		/* dot1agCfmMaIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->dot1agCfmMaIndex, sizeof(thedata->dot1agCfmMaIndex));
		/* dot1agCfmMepIdentifier */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->dot1agCfmMepIdentifier, sizeof(thedata->dot1agCfmMepIdentifier));
		header_complex_add_data(&dot1agCfmMepSpbmTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dot1agCfmMepSpbmTable_del(struct dot1agCfmMepSpbmTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dot1agCfmMepSpbmTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dot1agCfmMepSpbmTable_del(struct dot1agCfmMepSpbmTable_data *thedata)
{
	struct dot1agCfmMepSpbmTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dot1agCfmMepSpbmTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dot1agCfmMepSpbmTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dot1agCfmMepSpbmTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dot1agCfmMepSpbmTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case dot1agCfmMepSpbmTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dot1agCfmMepSpbmTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dot1agCfmMepSpbmTable_data *StorageTmp = dot1agCfmMepSpbmTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_dot1agCfmMepSpbmTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->dot1agCfmMdIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->dot1agCfmMaIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->dot1agCfmMepIdentifier, &tmpsize);
	SNMP_FREE(StorageTmp->dot1agCfmMepTransmitLbmSpbmDA);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->dot1agCfmMepTransmitLbmSpbmDA, &StorageTmp->dot1agCfmMepTransmitLbmSpbmDALen);
	if (StorageTmp->dot1agCfmMepTransmitLbmSpbmDA == NULL) {
		config_perror("invalid specification for dot1agCfmMepTransmitLbmSpbmDA");
		return;
	}
	SNMP_FREE(StorageTmp->dot1agCfmMepTransmitLtmSpbmDA);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->dot1agCfmMepTransmitLtmSpbmDA, &StorageTmp->dot1agCfmMepTransmitLtmSpbmDALen);
	if (StorageTmp->dot1agCfmMepTransmitLtmSpbmDA == NULL) {
		config_perror("invalid specification for dot1agCfmMepTransmitLtmSpbmDA");
		return;
	}
	dot1agCfmMepSpbmTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_dot1agCfmMepSpbmTable(): store configuraiton file for dot1agCfmMepSpbmTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dot1agCfmMepSpbmTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dot1agCfmMepSpbmTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_dot1agCfmMepSpbmTable: storing data...  "));
	refresh_dot1agCfmMepSpbmTable(1);
	(void) tmpsize;
	for (hcindex = dot1agCfmMepSpbmTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dot1agCfmMepSpbmTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dot1agCfmMepSpbmTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->dot1agCfmMdIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->dot1agCfmMaIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->dot1agCfmMepIdentifier, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->dot1agCfmMepTransmitLbmSpbmDA, &StorageTmp->dot1agCfmMepTransmitLbmSpbmDALen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->dot1agCfmMepTransmitLtmSpbmDA, &StorageTmp->dot1agCfmMepTransmitLtmSpbmDALen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dot1agCfmMepSpbmEspTable_data *dot1agCfmMepSpbmEspTable_create(void)
 * @brief create a fresh data structure representing a new row in the dot1agCfmMepSpbmEspTable table.
 *
 * Creates a new dot1agCfmMepSpbmEspTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dot1agCfmMepSpbmEspTable_data *
dot1agCfmMepSpbmEspTable_create(void)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageNew = SNMP_MALLOC_STRUCT(dot1agCfmMepSpbmEspTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmEspTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dot1agCfmMdIndex = 0;
		StorageNew->dot1agCfmMaIndex = 0;
		if ((StorageNew->dot1agCfmMepSpbmEspEsp = malloc(1)) == NULL)
			goto nomem;
		StorageNew->dot1agCfmMepSpbmEspEspLen = 0;
		StorageNew->dot1agCfmMepSpbmEspEsp[StorageNew->dot1agCfmMepSpbmEspEspLen] = 0;
		StorageNew->dot1agCfmMepSpbmEspRowStatus = 0;
		StorageNew->dot1agCfmMepSpbmEspRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	dot1agCfmMepSpbmEspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct dot1agCfmMepSpbmEspTable_data *dot1agCfmMepSpbmEspTable_duplicate(struct dot1agCfmMepSpbmEspTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dot1agCfmMepSpbmEspTable_data *
dot1agCfmMepSpbmEspTable_duplicate(struct dot1agCfmMepSpbmEspTable_data *thedata)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageNew = SNMP_MALLOC_STRUCT(dot1agCfmMepSpbmEspTable_data);

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmEspTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->dot1agCfmMepSpbmEspTable_id = thedata->dot1agCfmMepSpbmEspTable_id;
		StorageNew->dot1agCfmMdIndex = thedata->dot1agCfmMdIndex;
		StorageNew->dot1agCfmMaIndex = thedata->dot1agCfmMaIndex;
		StorageNew->dot1agCfmMepSpbmEspIndex = thedata->dot1agCfmMepSpbmEspIndex;
		if (!(StorageNew->dot1agCfmMepSpbmEspEsp = malloc(thedata->dot1agCfmMepSpbmEspEspLen + 1)))
			goto destroy;
		memcpy(StorageNew->dot1agCfmMepSpbmEspEsp, thedata->dot1agCfmMepSpbmEspEsp, thedata->dot1agCfmMepSpbmEspEspLen);
		StorageNew->dot1agCfmMepSpbmEspEspLen = thedata->dot1agCfmMepSpbmEspEspLen;
		StorageNew->dot1agCfmMepSpbmEspEsp[StorageNew->dot1agCfmMepSpbmEspEspLen] = 0;
		StorageNew->dot1agCfmMepSpbmEspRowStatus = thedata->dot1agCfmMepSpbmEspRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dot1agCfmMepSpbmEspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dot1agCfmMepSpbmEspTable_destroy(struct dot1agCfmMepSpbmEspTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dot1agCfmMepSpbmEspTable_destroy(struct dot1agCfmMepSpbmEspTable_data **thedata)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmEspTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->dot1agCfmMepSpbmEspEsp);
		StorageDel->dot1agCfmMepSpbmEspEspLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dot1agCfmMepSpbmEspTable_add(struct dot1agCfmMepSpbmEspTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dot1agCfmMepSpbmEspTable table data set.
 *
 * Adds a table row structure to the dot1agCfmMepSpbmEspTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dot1agCfmMepSpbmEspTable_add(struct dot1agCfmMepSpbmEspTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmEspTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* dot1agCfmMdIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->dot1agCfmMdIndex, sizeof(thedata->dot1agCfmMdIndex));
		/* dot1agCfmMaIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->dot1agCfmMaIndex, sizeof(thedata->dot1agCfmMaIndex));
		/* dot1agCfmMepSpbmEspIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->dot1agCfmMepSpbmEspIndex, sizeof(thedata->dot1agCfmMepSpbmEspIndex));
		header_complex_add_data(&dot1agCfmMepSpbmEspTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dot1agCfmMepSpbmEspTable_del(struct dot1agCfmMepSpbmEspTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dot1agCfmMepSpbmEspTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dot1agCfmMepSpbmEspTable_del(struct dot1agCfmMepSpbmEspTable_data *thedata)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021SpbMib", "dot1agCfmMepSpbmEspTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dot1agCfmMepSpbmEspTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dot1agCfmMepSpbmEspTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dot1agCfmMepSpbmEspTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dot1agCfmMepSpbmEspTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case dot1agCfmMepSpbmEspTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dot1agCfmMepSpbmEspTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dot1agCfmMepSpbmEspTable_data *StorageTmp = dot1agCfmMepSpbmEspTable_create();

	DEBUGMSGTL(("ieee8021SpbMib", "parse_dot1agCfmMepSpbmEspTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->dot1agCfmMdIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->dot1agCfmMaIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->dot1agCfmMepSpbmEspIndex, &tmpsize);
	SNMP_FREE(StorageTmp->dot1agCfmMepSpbmEspEsp);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->dot1agCfmMepSpbmEspEsp, &StorageTmp->dot1agCfmMepSpbmEspEspLen);
	if (StorageTmp->dot1agCfmMepSpbmEspEsp == NULL) {
		config_perror("invalid specification for dot1agCfmMepSpbmEspEsp");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dot1agCfmMepSpbmEspRowStatus, &tmpsize);
	dot1agCfmMepSpbmEspTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/*
 * store_dot1agCfmMepSpbmEspTable(): store configuraiton file for dot1agCfmMepSpbmEspTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dot1agCfmMepSpbmEspTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dot1agCfmMepSpbmEspTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021SpbMib", "store_dot1agCfmMepSpbmEspTable: storing data...  "));
	refresh_dot1agCfmMepSpbmEspTable(1);
	(void) tmpsize;
	for (hcindex = dot1agCfmMepSpbmEspTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dot1agCfmMepSpbmEspTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dot1agCfmMepSpbmEspTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->dot1agCfmMdIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->dot1agCfmMaIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->dot1agCfmMepSpbmEspIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->dot1agCfmMepSpbmEspEsp, &StorageTmp->dot1agCfmMepSpbmEspEspLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dot1agCfmMepSpbmEspRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, struct ieee8021SpbMtidStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, struct ieee8021SpbMtidStaticTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, struct ieee8021SpbMtidStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbMtidStaticTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, struct ieee8021SpbMtidStaticTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbMtidStaticTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, struct ieee8021SpbMtidStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, struct ieee8021SpbMtidStaticTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbMtidStaticTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbMtidStaticTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbMtidStaticTable_data *
refresh_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbMtidStaticTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbMtidStaticTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbMtidStaticTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbMtidStaticTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbMtidStaticTable(int force)
{
	if (!force && ieee8021SpbMtidStaticTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbMtidStaticTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbMtidStaticTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbMtidStaticTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbMtidStaticTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbMtidStaticTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbMtidStaticTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbMtidStaticTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbMtidStaticTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbMtidStaticTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBMTIDSTATICENTRYMTIDOVERLOAD:	/* Create */
		*write_method = write_ieee8021SpbMtidStaticEntryMtidOverload;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload);
		rval = (u_char *) &StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload;
		break;
	case (u_char) IEEE8021SPBMTIDSTATICENTRYROWSTATUS:	/* Create */
		*write_method = write_ieee8021SpbMtidStaticEntryRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbMtidStaticEntryRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021SpbMtidStaticEntryRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, struct ieee8021SpbTopIxDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, struct ieee8021SpbTopIxDynamicTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, struct ieee8021SpbTopIxDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbTopIxDynamicTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, struct ieee8021SpbTopIxDynamicTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbTopIxDynamicTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, struct ieee8021SpbTopIxDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, struct ieee8021SpbTopIxDynamicTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbTopIxDynamicTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbTopIxDynamicTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbTopIxDynamicTable_data *
refresh_ieee8021SpbTopIxDynamicTable_row(struct ieee8021SpbTopIxDynamicTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbTopIxDynamicTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbTopIxDynamicTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbTopIxDynamicTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbTopIxDynamicTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbTopIxDynamicTable(int force)
{
	if (!force && ieee8021SpbTopIxDynamicTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbTopIxDynamicTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbTopIxDynamicTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbTopIxDynamicTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbTopIxDynamicTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbTopIxDynamicTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbTopIxDynamicTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbTopIxDynamicTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbTopIxDynamicTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbTopIxDynamicTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBTOPIXDYNAMICENTRYAGREEDIGEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigestLen;
		rval = (u_char *) StorageTmp->ieee8021SpbTopIxDynamicEntryAgreeDigest;
		break;
	case (u_char) IEEE8021SPBTOPIXDYNAMICENTRYMCID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbTopIxDynamicEntryMCIDLen;
		rval = (u_char *) StorageTmp->ieee8021SpbTopIxDynamicEntryMCID;
		break;
	case (u_char) IEEE8021SPBTOPIXDYNAMICENTRYAUXMCID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCIDLen;
		rval = (u_char *) StorageTmp->ieee8021SpbTopIxDynamicEntryAuxMCID;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, struct ieee8021SpbEctStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, struct ieee8021SpbEctStaticTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, struct ieee8021SpbEctStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbEctStaticTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, struct ieee8021SpbEctStaticTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbEctStaticTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, struct ieee8021SpbEctStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, struct ieee8021SpbEctStaticTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbEctStaticTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbEctStaticTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbEctStaticTable_data *
refresh_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbEctStaticTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbEctStaticTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbEctStaticTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbEctStaticTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbEctStaticTable(int force)
{
	if (!force && ieee8021SpbEctStaticTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbEctStaticTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbEctStaticTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbEctStaticTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbEctStaticTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbEctStaticTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbEctStaticTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbEctStaticTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbEctStaticTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbEctStaticTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBECTSTATICENTRYECTALGORITHM:	/* Create */
		*write_method = write_ieee8021SpbEctStaticEntryEctAlgorithm;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithmLen;
		rval = (u_char *) StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm;
		break;
	case (u_char) IEEE8021SPBVECTSTATICENTRYSPVID:	/* Create */
		*write_method = write_ieee8021SpbvEctStaticEntrySpvid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbvEctStaticEntrySpvid);
		rval = (u_char *) &StorageTmp->ieee8021SpbvEctStaticEntrySpvid;
		break;
	case (u_char) IEEE8021SPBECTSTATICENTRYROWSTATUS:	/* Create */
		*write_method = write_ieee8021SpbEctStaticEntryRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbEctStaticEntryRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021SpbEctStaticEntryRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, struct ieee8021SpbEctDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, struct ieee8021SpbEctDynamicTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, struct ieee8021SpbEctDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbEctDynamicTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, struct ieee8021SpbEctDynamicTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbEctDynamicTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, struct ieee8021SpbEctDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, struct ieee8021SpbEctDynamicTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbEctDynamicTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbEctDynamicTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbEctDynamicTable_data *
refresh_ieee8021SpbEctDynamicTable_row(struct ieee8021SpbEctDynamicTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbEctDynamicTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbEctDynamicTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbEctDynamicTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbEctDynamicTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbEctDynamicTable(int force)
{
	if (!force && ieee8021SpbEctDynamicTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbEctDynamicTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbEctDynamicTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbEctDynamicTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbEctDynamicTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbEctDynamicTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbEctDynamicTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbEctDynamicTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbEctDynamicTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbEctDynamicTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBECTDYNAMICENTRYMODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbEctDynamicEntryMode);
		rval = (u_char *) &StorageTmp->ieee8021SpbEctDynamicEntryMode;
		break;
	case (u_char) IEEE8021SPBECTDYNAMICENTRYLOCALUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbEctDynamicEntryLocalUse);
		rval = (u_char *) &StorageTmp->ieee8021SpbEctDynamicEntryLocalUse;
		break;
	case (u_char) IEEE8021SPBECTDYNAMICENTRYREMOTEUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbEctDynamicEntryRemoteUse);
		rval = (u_char *) &StorageTmp->ieee8021SpbEctDynamicEntryRemoteUse;
		break;
	case (u_char) IEEE8021SPBECTDYNAMICENTRYINGRESSCHECKDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbEctDynamicEntryIngressCheckDiscards);
		rval = (u_char *) &StorageTmp->ieee8021SpbEctDynamicEntryIngressCheckDiscards;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, struct ieee8021SpbAdjStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, struct ieee8021SpbAdjStaticTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, struct ieee8021SpbAdjStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbAdjStaticTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, struct ieee8021SpbAdjStaticTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbAdjStaticTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, struct ieee8021SpbAdjStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, struct ieee8021SpbAdjStaticTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbAdjStaticTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbAdjStaticTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbAdjStaticTable_data *
refresh_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbAdjStaticTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbAdjStaticTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbAdjStaticTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbAdjStaticTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbAdjStaticTable(int force)
{
	if (!force && ieee8021SpbAdjStaticTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbAdjStaticTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbAdjStaticTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbAdjStaticTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbAdjStaticTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbAdjStaticTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbAdjStaticTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbAdjStaticTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbAdjStaticTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbAdjStaticTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBADJSTATICENTRYMETRIC:	/* Create */
		*write_method = write_ieee8021SpbAdjStaticEntryMetric;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjStaticEntryMetric);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjStaticEntryMetric;
		break;
	case (u_char) IEEE8021SPBADJSTATICENTRYIFADMINSTATE:	/* Create */
		*write_method = write_ieee8021SpbAdjStaticEntryIfAdminState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState;
		break;
	case (u_char) IEEE8021SPBADJSTATICENTRYROWSTATUS:	/* Create */
		*write_method = write_ieee8021SpbAdjStaticEntryRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjStaticEntryRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjStaticEntryRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, struct ieee8021SpbAdjDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, struct ieee8021SpbAdjDynamicTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, struct ieee8021SpbAdjDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbAdjDynamicTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, struct ieee8021SpbAdjDynamicTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbAdjDynamicTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, struct ieee8021SpbAdjDynamicTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, struct ieee8021SpbAdjDynamicTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbAdjDynamicTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbAdjDynamicTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbAdjDynamicTable_data *
refresh_ieee8021SpbAdjDynamicTable_row(struct ieee8021SpbAdjDynamicTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbAdjDynamicTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbAdjDynamicTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbAdjDynamicTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbAdjDynamicTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbAdjDynamicTable(int force)
{
	if (!force && ieee8021SpbAdjDynamicTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbAdjDynamicTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbAdjDynamicTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbAdjDynamicTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbAdjDynamicTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbAdjDynamicTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbAdjDynamicTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbAdjDynamicTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbAdjDynamicTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbAdjDynamicTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryPort);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryPort;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYIFOPERSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryIfOperState);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryIfOperState;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPEERSYSNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysNameLen;
		rval = (u_char *) StorageTmp->ieee8021SpbAdjDynamicEntryPeerSysName;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPEERAGREEDIGEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigestLen;
		rval = (u_char *) StorageTmp->ieee8021SpbAdjDynamicEntryPeerAgreeDigest;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPEERMCID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCIDLen;
		rval = (u_char *) StorageTmp->ieee8021SpbAdjDynamicEntryPeerMCID;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPEERAUXMCID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCIDLen;
		rval = (u_char *) StorageTmp->ieee8021SpbAdjDynamicEntryPeerAuxMCID;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYLOCALCIRCUITID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryLocalCircuitID);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryLocalCircuitID;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPEERLOCALCIRCUITID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryPeerLocalCircuitID;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPORTIDENTIFIER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryPortIdentifier);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryPortIdentifier;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYPEERPORTIDENTIFIER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryPeerPortIdentifier);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryPeerPortIdentifier;
		break;
	case (u_char) IEEE8021SPBADJDYNAMICENTRYISISCIRCINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbAdjDynamicEntryIsisCircIndex);
		rval = (u_char *) &StorageTmp->ieee8021SpbAdjDynamicEntryIsisCircIndex;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, struct ieee8021SpbTopNodeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, struct ieee8021SpbTopNodeTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, struct ieee8021SpbTopNodeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbTopNodeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, struct ieee8021SpbTopNodeTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbTopNodeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, struct ieee8021SpbTopNodeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, struct ieee8021SpbTopNodeTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbTopNodeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbTopNodeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbTopNodeTable_data *
refresh_ieee8021SpbTopNodeTable_row(struct ieee8021SpbTopNodeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbTopNodeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbTopNodeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbTopNodeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbTopNodeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbTopNodeTable(int force)
{
	if (!force && ieee8021SpbTopNodeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbTopNodeTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbTopNodeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbTopNodeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbTopNodeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbTopNodeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbTopNodeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbTopNodeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbTopNodeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbTopNodeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBTOPNODEENTRYBRIDGEPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbTopNodeEntryBridgePriorityLen;
		rval = (u_char *) StorageTmp->ieee8021SpbTopNodeEntryBridgePriority;
		break;
	case (u_char) IEEE8021SPBMTOPNODEENTRYSPSOURCEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbmTopNodeEntrySPsourceIDLen;
		rval = (u_char *) StorageTmp->ieee8021SpbmTopNodeEntrySPsourceID;
		break;
	case (u_char) IEEE8021SPBTOPNODEENTRYSYSNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbTopNodeEntrySysNameLen;
		rval = (u_char *) StorageTmp->ieee8021SpbTopNodeEntrySysName;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, struct ieee8021SpbTopEctTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, struct ieee8021SpbTopEctTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, struct ieee8021SpbTopEctTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbTopEctTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, struct ieee8021SpbTopEctTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbTopEctTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, struct ieee8021SpbTopEctTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, struct ieee8021SpbTopEctTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbTopEctTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbTopEctTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbTopEctTable_data *
refresh_ieee8021SpbTopEctTable_row(struct ieee8021SpbTopEctTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbTopEctTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbTopEctTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbTopEctTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbTopEctTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbTopEctTable(int force)
{
	if (!force && ieee8021SpbTopEctTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbTopEctTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbTopEctTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbTopEctTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbTopEctTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbTopEctTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbTopEctTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbTopEctTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbTopEctTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbTopEctTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBTOPECTENTRYECTALGORITHM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbTopEctEntryEctAlgorithmLen;
		rval = (u_char *) StorageTmp->ieee8021SpbTopEctEntryEctAlgorithm;
		break;
	case (u_char) IEEE8021SPBTOPECTENTRYMODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbTopEctEntryMode);
		rval = (u_char *) &StorageTmp->ieee8021SpbTopEctEntryMode;
		break;
	case (u_char) IEEE8021SPBVTOPECTSYSMODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbvTopEctSysMode);
		rval = (u_char *) &StorageTmp->ieee8021SpbvTopEctSysMode;
		break;
	case (u_char) IEEE8021SPBVTOPECTENTRYSPVID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbvTopEctEntrySpvid);
		rval = (u_char *) &StorageTmp->ieee8021SpbvTopEctEntrySpvid;
		break;
	case (u_char) IEEE8021SPBTOPECTENTRYLOCALUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbTopEctEntryLocalUse);
		rval = (u_char *) &StorageTmp->ieee8021SpbTopEctEntryLocalUse;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, struct ieee8021SpbTopEdgeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, struct ieee8021SpbTopEdgeTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, struct ieee8021SpbTopEdgeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbTopEdgeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, struct ieee8021SpbTopEdgeTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbTopEdgeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, struct ieee8021SpbTopEdgeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, struct ieee8021SpbTopEdgeTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbTopEdgeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbTopEdgeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbTopEdgeTable_data *
refresh_ieee8021SpbTopEdgeTable_row(struct ieee8021SpbTopEdgeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbTopEdgeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbTopEdgeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbTopEdgeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbTopEdgeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbTopEdgeTable(int force)
{
	if (!force && ieee8021SpbTopEdgeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbTopEdgeTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbTopEdgeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbTopEdgeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbTopEdgeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbTopEdgeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbTopEdgeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbTopEdgeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbTopEdgeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbTopEdgeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBTOPEDGEENTRYMETRICNEAR2FAR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbTopEdgeEntryMetricNear2Far);
		rval = (u_char *) &StorageTmp->ieee8021SpbTopEdgeEntryMetricNear2Far;
		break;
	case (u_char) IEEE8021SPBTOPEDGEENTRYMETRICFAR2NEAR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbTopEdgeEntryMetricFar2Near);
		rval = (u_char *) &StorageTmp->ieee8021SpbTopEdgeEntryMetricFar2Near;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, struct ieee8021SpbmTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, struct ieee8021SpbmTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, struct ieee8021SpbmTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbmTopSrvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, struct ieee8021SpbmTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbmTopSrvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, struct ieee8021SpbmTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, struct ieee8021SpbmTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbmTopSrvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbmTopSrvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbmTopSrvTable_data *
refresh_ieee8021SpbmTopSrvTable_row(struct ieee8021SpbmTopSrvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbmTopSrvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbmTopSrvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbmTopSrvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbmTopSrvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbmTopSrvTable(int force)
{
	if (!force && ieee8021SpbmTopSrvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbmTopSrvTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbmTopSrvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbmTopSrvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbmTopSrvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbmTopSrvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbmTopSrvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbmTopSrvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbmTopSrvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbmTopSrvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBMTOPSRVENTRYISIDFLAGS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbmTopSrvEntryIsidFlagsLen;
		rval = (u_char *) StorageTmp->ieee8021SpbmTopSrvEntryIsidFlags;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, struct ieee8021SpbvTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, struct ieee8021SpbvTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, struct ieee8021SpbvTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbvTopSrvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, struct ieee8021SpbvTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbvTopSrvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, struct ieee8021SpbvTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, struct ieee8021SpbvTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbvTopSrvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbvTopSrvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbvTopSrvTable_data *
refresh_ieee8021SpbvTopSrvTable_row(struct ieee8021SpbvTopSrvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbvTopSrvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbvTopSrvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbvTopSrvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbvTopSrvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbvTopSrvTable(int force)
{
	if (!force && ieee8021SpbvTopSrvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbvTopSrvTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbvTopSrvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbvTopSrvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbvTopSrvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbvTopSrvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbvTopSrvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbvTopSrvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbvTopSrvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbvTopSrvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBVTOPSRVENTRYBASEVID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbvTopSrvEntryBaseVid);
		rval = (u_char *) &StorageTmp->ieee8021SpbvTopSrvEntryBaseVid;
		break;
	case (u_char) IEEE8021SPBVTOPSRVENTRYMMACFLAGS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021SpbvTopSrvEntryMMacFlagsLen;
		rval = (u_char *) StorageTmp->ieee8021SpbvTopSrvEntryMMacFlags;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, struct ieee8021SpbmBsiStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, struct ieee8021SpbmBsiStaticTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, struct ieee8021SpbmBsiStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021SpbmBsiStaticTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, struct ieee8021SpbmBsiStaticTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021SpbmBsiStaticTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, struct ieee8021SpbmBsiStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, struct ieee8021SpbmBsiStaticTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021SpbmBsiStaticTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021SpbmBsiStaticTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021SpbmBsiStaticTable_data *
refresh_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021SpbmBsiStaticTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021SpbmBsiStaticTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021SpbmBsiStaticTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021SpbmBsiStaticTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021SpbmBsiStaticTable(int force)
{
	if (!force && ieee8021SpbmBsiStaticTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021SpbmBsiStaticTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021SpbmBsiStaticTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021SpbmBsiStaticTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_ieee8021SpbmBsiStaticTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_ieee8021SpbmBsiStaticTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021SpbmBsiStaticTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021SpbmBsiStaticTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021SpbmBsiStaticTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021SPBMBSISTATICENTRYTBIT:	/* Create */
		*write_method = write_ieee8021SpbmBsiStaticEntryTBit;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbmBsiStaticEntryTBit);
		rval = (u_char *) &StorageTmp->ieee8021SpbmBsiStaticEntryTBit;
		break;
	case (u_char) IEEE8021SPBMBSISTATICENTRYRBIT:	/* Create */
		*write_method = write_ieee8021SpbmBsiStaticEntryRBit;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbmBsiStaticEntryRBit);
		rval = (u_char *) &StorageTmp->ieee8021SpbmBsiStaticEntryRBit;
		break;
	case (u_char) IEEE8021SPBMBSISTATICENTRYTSBIT:	/* Create */
		*write_method = write_ieee8021SpbmBsiStaticEntryTsBit;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbmBsiStaticEntryTsBit);
		rval = (u_char *) &StorageTmp->ieee8021SpbmBsiStaticEntryTsBit;
		break;
	case (u_char) IEEE8021SPBMBSISTATICENTRYTIEBREAKMASK:	/* Create */
		*write_method = write_ieee8021SpbmBsiStaticEntryTieBreakMask;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask);
		rval = (u_char *) &StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask;
		break;
	case (u_char) IEEE8021SPBMBSISTATICENTRYROWSTATUS:	/* Create */
		*write_method = write_ieee8021SpbmBsiStaticEntryRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, struct dot1agCfmMepSpbmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, struct dot1agCfmMepSpbmTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, struct dot1agCfmMepSpbmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_dot1agCfmMepSpbmTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, struct dot1agCfmMepSpbmTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	dot1agCfmMepSpbmTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, struct dot1agCfmMepSpbmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, struct dot1agCfmMepSpbmTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_dot1agCfmMepSpbmTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dot1agCfmMepSpbmTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct dot1agCfmMepSpbmTable_data *
refresh_dot1agCfmMepSpbmTable_row(struct dot1agCfmMepSpbmTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dot1agCfmMepSpbmTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->dot1agCfmMepSpbmTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_dot1agCfmMepSpbmTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dot1agCfmMepSpbmTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dot1agCfmMepSpbmTable(int force)
{
	if (!force && dot1agCfmMepSpbmTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	dot1agCfmMepSpbmTable_refresh = 0;
}

/**
 * @fn u_char *var_dot1agCfmMepSpbmTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dot1agCfmMepSpbmTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_dot1agCfmMepSpbmTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dot1agCfmMepSpbmTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_dot1agCfmMepSpbmTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dot1agCfmMepSpbmTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(dot1agCfmMepSpbmTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_dot1agCfmMepSpbmTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DOT1AGCFMMEPTRANSMITLBMSPBMDA:	/* Create */
		*write_method = write_dot1agCfmMepTransmitLbmSpbmDA;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dot1agCfmMepTransmitLbmSpbmDALen;
		rval = (u_char *) StorageTmp->dot1agCfmMepTransmitLbmSpbmDA;
		break;
	case (u_char) DOT1AGCFMMEPTRANSMITLTMSPBMDA:	/* Create */
		*write_method = write_dot1agCfmMepTransmitLtmSpbmDA;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dot1agCfmMepTransmitLtmSpbmDALen;
		rval = (u_char *) StorageTmp->dot1agCfmMepTransmitLtmSpbmDA;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, struct dot1agCfmMepSpbmEspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, struct dot1agCfmMepSpbmEspTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, struct dot1agCfmMepSpbmEspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_dot1agCfmMepSpbmEspTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, struct dot1agCfmMepSpbmEspTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	dot1agCfmMepSpbmEspTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, struct dot1agCfmMepSpbmEspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, struct dot1agCfmMepSpbmEspTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_dot1agCfmMepSpbmEspTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dot1agCfmMepSpbmEspTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct dot1agCfmMepSpbmEspTable_data *
refresh_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dot1agCfmMepSpbmEspTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->dot1agCfmMepSpbmEspTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_dot1agCfmMepSpbmEspTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dot1agCfmMepSpbmEspTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dot1agCfmMepSpbmEspTable(int force)
{
	if (!force && dot1agCfmMepSpbmEspTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	dot1agCfmMepSpbmEspTable_refresh = 0;
}

/**
 * @fn u_char *var_dot1agCfmMepSpbmEspTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dot1agCfmMepSpbmEspTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021SpbMib above.
 */
u_char *
var_dot1agCfmMepSpbmEspTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021SpbMib", "var_dot1agCfmMepSpbmEspTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dot1agCfmMepSpbmEspTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(dot1agCfmMepSpbmEspTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_dot1agCfmMepSpbmEspTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DOT1AGCFMMEPSPBMESPESP:	/* Create */
		*write_method = write_dot1agCfmMepSpbmEspEsp;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dot1agCfmMepSpbmEspEspLen;
		rval = (u_char *) StorageTmp->dot1agCfmMepSpbmEspEsp;
		break;
	case (u_char) DOT1AGCFMMEPSPBMESPROWSTATUS:	/* Create */
		*write_method = write_dot1agCfmMepSpbmEspRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dot1agCfmMepSpbmEspRowStatus);
		rval = (u_char *) &StorageTmp->dot1agCfmMepSpbmEspRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021SpbMtidStaticEntryMtidOverload(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbMtidStaticEntryMtidOverload(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMtidStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbMtidStaticEntryMtidOverload entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbMtidStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbMtidStaticEntryMtidOverload: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbMtidStaticEntryMtidOverload not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbMtidStaticEntryMtidOverload: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbMtidStaticEntryMtidOverload: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbMtidStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old = ieee8021SpbMtidStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMtidStaticTable_rsvs++;
		StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbMtidStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbMtidStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMtidStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMtidStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbMtidStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMtidStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) != NULL) {
			ieee8021SpbMtidStaticTable_destroy(&StorageTmp->ieee8021SpbMtidStaticTable_old);
			StorageTmp->ieee8021SpbMtidStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbMtidStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbMtidStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMtidStaticTable_sets == 0)
			revert_ieee8021SpbMtidStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbMtidStaticEntryMtidOverload = StorageOld->ieee8021SpbMtidStaticEntryMtidOverload;
		if (--StorageTmp->ieee8021SpbMtidStaticTable_rsvs == 0)
			ieee8021SpbMtidStaticTable_destroy(&StorageTmp->ieee8021SpbMtidStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbEctStaticEntryEctAlgorithm(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbEctStaticEntryEctAlgorithm(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbEctStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbEctStaticEntryEctAlgorithm entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbEctStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbEctStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbEctStaticEntryEctAlgorithm: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbEctStaticEntryEctAlgorithm not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 4..4 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 4)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbEctStaticEntryEctAlgorithm: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"00-80-c2-01\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbEctStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old = ieee8021SpbEctStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbEctStaticTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm);
		StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm = string;
		StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithmLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbEctStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbEctStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbEctStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbEctStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbEctStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbEctStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
			ieee8021SpbEctStaticTable_destroy(&StorageTmp->ieee8021SpbEctStaticTable_old);
			StorageTmp->ieee8021SpbEctStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbEctStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbEctStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbEctStaticTable_sets == 0)
			revert_ieee8021SpbEctStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
			break;
		if (StorageOld->ieee8021SpbEctStaticEntryEctAlgorithm != NULL) {
			SNMP_FREE(StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm);
			StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithm = StorageOld->ieee8021SpbEctStaticEntryEctAlgorithm;
			StorageTmp->ieee8021SpbEctStaticEntryEctAlgorithmLen = StorageOld->ieee8021SpbEctStaticEntryEctAlgorithmLen;
			StorageOld->ieee8021SpbEctStaticEntryEctAlgorithm = NULL;
			StorageOld->ieee8021SpbEctStaticEntryEctAlgorithmLen = 0;
		}
		if (--StorageTmp->ieee8021SpbEctStaticTable_rsvs == 0)
			ieee8021SpbEctStaticTable_destroy(&StorageTmp->ieee8021SpbEctStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbvEctStaticEntrySpvid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbvEctStaticEntrySpvid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbEctStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbvEctStaticEntrySpvid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbEctStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbEctStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvEctStaticEntrySpvid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvEctStaticEntrySpvid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvEctStaticEntrySpvid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..0 1..4094 */
		if (set_value != 0 && (1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvEctStaticEntrySpvid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbEctStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old = ieee8021SpbEctStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbEctStaticTable_rsvs++;
		StorageTmp->ieee8021SpbvEctStaticEntrySpvid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbEctStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbEctStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbEctStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbvEctStaticEntrySpvid for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbEctStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbEctStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbEctStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
			ieee8021SpbEctStaticTable_destroy(&StorageTmp->ieee8021SpbEctStaticTable_old);
			StorageTmp->ieee8021SpbEctStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbEctStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbEctStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbEctStaticTable_sets == 0)
			revert_ieee8021SpbEctStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbvEctStaticEntrySpvid = StorageOld->ieee8021SpbvEctStaticEntrySpvid;
		if (--StorageTmp->ieee8021SpbEctStaticTable_rsvs == 0)
			ieee8021SpbEctStaticTable_destroy(&StorageTmp->ieee8021SpbEctStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbAdjStaticEntryMetric(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbAdjStaticEntryMetric(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbAdjStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbAdjStaticEntryMetric entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbAdjStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryMetric: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryMetric not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryMetric: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..16777215 */
		if ((1 > set_value || set_value > 16777215)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryMetric: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbAdjStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old = ieee8021SpbAdjStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbAdjStaticTable_rsvs++;
		StorageTmp->ieee8021SpbAdjStaticEntryMetric = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbAdjStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbAdjStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbAdjStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbAdjStaticEntryMetric for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbAdjStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbAdjStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbAdjStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
			ieee8021SpbAdjStaticTable_destroy(&StorageTmp->ieee8021SpbAdjStaticTable_old);
			StorageTmp->ieee8021SpbAdjStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbAdjStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbAdjStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbAdjStaticTable_sets == 0)
			revert_ieee8021SpbAdjStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbAdjStaticEntryMetric = StorageOld->ieee8021SpbAdjStaticEntryMetric;
		if (--StorageTmp->ieee8021SpbAdjStaticTable_rsvs == 0)
			ieee8021SpbAdjStaticTable_destroy(&StorageTmp->ieee8021SpbAdjStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbAdjStaticEntryIfAdminState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbAdjStaticEntryIfAdminState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbAdjStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbAdjStaticEntryIfAdminState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbAdjStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryIfAdminState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryIfAdminState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryIfAdminState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value up */
		switch (set_value) {
		case IEEE8021SPBADJSTATICENTRYIFADMINSTATE_UP:
		case IEEE8021SPBADJSTATICENTRYIFADMINSTATE_DOWN:
		case IEEE8021SPBADJSTATICENTRYIFADMINSTATE_TESTING:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryIfAdminState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbAdjStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old = ieee8021SpbAdjStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbAdjStaticTable_rsvs++;
		StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbAdjStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbAdjStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbAdjStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbAdjStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbAdjStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbAdjStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
			ieee8021SpbAdjStaticTable_destroy(&StorageTmp->ieee8021SpbAdjStaticTable_old);
			StorageTmp->ieee8021SpbAdjStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbAdjStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbAdjStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbAdjStaticTable_sets == 0)
			revert_ieee8021SpbAdjStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbAdjStaticEntryIfAdminState = StorageOld->ieee8021SpbAdjStaticEntryIfAdminState;
		if (--StorageTmp->ieee8021SpbAdjStaticTable_rsvs == 0)
			ieee8021SpbAdjStaticTable_destroy(&StorageTmp->ieee8021SpbAdjStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmBsiStaticEntryTBit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbmBsiStaticEntryTBit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmBsiStaticEntryTBit entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbmBsiStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTBit: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTBit not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTBit: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTBit: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old = ieee8021SpbmBsiStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs++;
		StorageTmp->ieee8021SpbmBsiStaticEntryTBit = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbmBsiStaticEntryTBit for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
			revert_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbmBsiStaticEntryTBit = StorageOld->ieee8021SpbmBsiStaticEntryTBit;
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmBsiStaticEntryRBit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbmBsiStaticEntryRBit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmBsiStaticEntryRBit entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbmBsiStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryRBit: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryRBit not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryRBit: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryRBit: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old = ieee8021SpbmBsiStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs++;
		StorageTmp->ieee8021SpbmBsiStaticEntryRBit = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbmBsiStaticEntryRBit for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
			revert_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbmBsiStaticEntryRBit = StorageOld->ieee8021SpbmBsiStaticEntryRBit;
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmBsiStaticEntryTsBit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbmBsiStaticEntryTsBit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmBsiStaticEntryTsBit entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbmBsiStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTsBit: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTsBit not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTsBit: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTsBit: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old = ieee8021SpbmBsiStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs++;
		StorageTmp->ieee8021SpbmBsiStaticEntryTsBit = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbmBsiStaticEntryTsBit for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
			revert_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbmBsiStaticEntryTsBit = StorageOld->ieee8021SpbmBsiStaticEntryTsBit;
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmBsiStaticEntryTieBreakMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021SpbmBsiStaticEntryTieBreakMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmBsiStaticEntryTieBreakMask entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021SpbmBsiStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTieBreakMask: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTieBreakMask not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTieBreakMask: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..15 */
		if ((0 > set_value || set_value > 15)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryTieBreakMask: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			if (StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old = ieee8021SpbmBsiStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs++;
		StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_tsts == 0)
				if ((ret = check_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
				if ((ret = update_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbmBsiStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
			StorageTmp->ieee8021SpbmBsiStaticTable_rsvs = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_tsts = 0;
			StorageTmp->ieee8021SpbmBsiStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_sets == 0)
			revert_ieee8021SpbmBsiStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
			break;
		StorageTmp->ieee8021SpbmBsiStaticEntryTieBreakMask = StorageOld->ieee8021SpbmBsiStaticEntryTieBreakMask;
		if (--StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
			ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dot1agCfmMepTransmitLbmSpbmDA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dot1agCfmMepTransmitLbmSpbmDA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct dot1agCfmMepSpbmTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_dot1agCfmMepTransmitLbmSpbmDA entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(dot1agCfmMepSpbmTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepTransmitLbmSpbmDA not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepTransmitLbmSpbmDA: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) == NULL)
			if (StorageTmp->dot1agCfmMepSpbmTable_rsvs == 0)
				if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old = dot1agCfmMepSpbmTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->dot1agCfmMepSpbmTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->dot1agCfmMepTransmitLbmSpbmDA);
		StorageTmp->dot1agCfmMepTransmitLbmSpbmDA = string;
		StorageTmp->dot1agCfmMepTransmitLbmSpbmDALen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->dot1agCfmMepSpbmTable_tsts == 0)
				if ((ret = check_dot1agCfmMepSpbmTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->dot1agCfmMepSpbmTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dot1agCfmMepTransmitLbmSpbmDA for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->dot1agCfmMepSpbmTable_sets == 0)
				if ((ret = update_dot1agCfmMepSpbmTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->dot1agCfmMepSpbmTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) != NULL) {
			dot1agCfmMepSpbmTable_destroy(&StorageTmp->dot1agCfmMepSpbmTable_old);
			StorageTmp->dot1agCfmMepSpbmTable_rsvs = 0;
			StorageTmp->dot1agCfmMepSpbmTable_tsts = 0;
			StorageTmp->dot1agCfmMepSpbmTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->dot1agCfmMepSpbmTable_sets == 0)
			revert_dot1agCfmMepSpbmTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) == NULL)
			break;
		if (StorageOld->dot1agCfmMepTransmitLbmSpbmDA != NULL) {
			SNMP_FREE(StorageTmp->dot1agCfmMepTransmitLbmSpbmDA);
			StorageTmp->dot1agCfmMepTransmitLbmSpbmDA = StorageOld->dot1agCfmMepTransmitLbmSpbmDA;
			StorageTmp->dot1agCfmMepTransmitLbmSpbmDALen = StorageOld->dot1agCfmMepTransmitLbmSpbmDALen;
			StorageOld->dot1agCfmMepTransmitLbmSpbmDA = NULL;
			StorageOld->dot1agCfmMepTransmitLbmSpbmDALen = 0;
		}
		if (--StorageTmp->dot1agCfmMepSpbmTable_rsvs == 0)
			dot1agCfmMepSpbmTable_destroy(&StorageTmp->dot1agCfmMepSpbmTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dot1agCfmMepTransmitLtmSpbmDA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dot1agCfmMepTransmitLtmSpbmDA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct dot1agCfmMepSpbmTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_dot1agCfmMepTransmitLtmSpbmDA entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(dot1agCfmMepSpbmTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepTransmitLtmSpbmDA not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepTransmitLtmSpbmDA: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) == NULL)
			if (StorageTmp->dot1agCfmMepSpbmTable_rsvs == 0)
				if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old = dot1agCfmMepSpbmTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->dot1agCfmMepSpbmTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->dot1agCfmMepTransmitLtmSpbmDA);
		StorageTmp->dot1agCfmMepTransmitLtmSpbmDA = string;
		StorageTmp->dot1agCfmMepTransmitLtmSpbmDALen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->dot1agCfmMepSpbmTable_tsts == 0)
				if ((ret = check_dot1agCfmMepSpbmTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->dot1agCfmMepSpbmTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dot1agCfmMepTransmitLtmSpbmDA for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->dot1agCfmMepSpbmTable_sets == 0)
				if ((ret = update_dot1agCfmMepSpbmTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->dot1agCfmMepSpbmTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) != NULL) {
			dot1agCfmMepSpbmTable_destroy(&StorageTmp->dot1agCfmMepSpbmTable_old);
			StorageTmp->dot1agCfmMepSpbmTable_rsvs = 0;
			StorageTmp->dot1agCfmMepSpbmTable_tsts = 0;
			StorageTmp->dot1agCfmMepSpbmTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->dot1agCfmMepSpbmTable_sets == 0)
			revert_dot1agCfmMepSpbmTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmTable_old) == NULL)
			break;
		if (StorageOld->dot1agCfmMepTransmitLtmSpbmDA != NULL) {
			SNMP_FREE(StorageTmp->dot1agCfmMepTransmitLtmSpbmDA);
			StorageTmp->dot1agCfmMepTransmitLtmSpbmDA = StorageOld->dot1agCfmMepTransmitLtmSpbmDA;
			StorageTmp->dot1agCfmMepTransmitLtmSpbmDALen = StorageOld->dot1agCfmMepTransmitLtmSpbmDALen;
			StorageOld->dot1agCfmMepTransmitLtmSpbmDA = NULL;
			StorageOld->dot1agCfmMepTransmitLtmSpbmDALen = 0;
		}
		if (--StorageTmp->dot1agCfmMepSpbmTable_rsvs == 0)
			dot1agCfmMepSpbmTable_destroy(&StorageTmp->dot1agCfmMepSpbmTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dot1agCfmMepSpbmEspEsp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dot1agCfmMepSpbmEspEsp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_dot1agCfmMepSpbmEspEsp entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(dot1agCfmMepSpbmEspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->dot1agCfmMepSpbmEspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepSpbmEspEsp: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepSpbmEspEsp not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 14..14 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 14)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepSpbmEspEsp: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) == NULL)
			if (StorageTmp->dot1agCfmMepSpbmEspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old = dot1agCfmMepSpbmEspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->dot1agCfmMepSpbmEspTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->dot1agCfmMepSpbmEspEsp);
		StorageTmp->dot1agCfmMepSpbmEspEsp = string;
		StorageTmp->dot1agCfmMepSpbmEspEspLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->dot1agCfmMepSpbmEspTable_tsts == 0)
				if ((ret = check_dot1agCfmMepSpbmEspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->dot1agCfmMepSpbmEspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dot1agCfmMepSpbmEspEsp for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->dot1agCfmMepSpbmEspTable_sets == 0)
				if ((ret = update_dot1agCfmMepSpbmEspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->dot1agCfmMepSpbmEspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) != NULL) {
			dot1agCfmMepSpbmEspTable_destroy(&StorageTmp->dot1agCfmMepSpbmEspTable_old);
			StorageTmp->dot1agCfmMepSpbmEspTable_rsvs = 0;
			StorageTmp->dot1agCfmMepSpbmEspTable_tsts = 0;
			StorageTmp->dot1agCfmMepSpbmEspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->dot1agCfmMepSpbmEspTable_sets == 0)
			revert_dot1agCfmMepSpbmEspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) == NULL)
			break;
		if (StorageOld->dot1agCfmMepSpbmEspEsp != NULL) {
			SNMP_FREE(StorageTmp->dot1agCfmMepSpbmEspEsp);
			StorageTmp->dot1agCfmMepSpbmEspEsp = StorageOld->dot1agCfmMepSpbmEspEsp;
			StorageTmp->dot1agCfmMepSpbmEspEspLen = StorageOld->dot1agCfmMepSpbmEspEspLen;
			StorageOld->dot1agCfmMepSpbmEspEsp = NULL;
			StorageOld->dot1agCfmMepSpbmEspEspLen = 0;
		}
		if (--StorageTmp->dot1agCfmMepSpbmEspTable_rsvs == 0)
			dot1agCfmMepSpbmEspTable_destroy(&StorageTmp->dot1agCfmMepSpbmEspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbSysAreaAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbSysAreaAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbSysAreaAddress entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysAreaAddress not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 3..3 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 3)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysAreaAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021SpbSysAreaAddress);
		StorageTmp->ieee8021SpbSysAreaAddress = string;
		StorageTmp->ieee8021SpbSysAreaAddressLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbSysAreaAddress for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		if (StorageOld->ieee8021SpbSysAreaAddress != NULL) {
			SNMP_FREE(StorageTmp->ieee8021SpbSysAreaAddress);
			StorageTmp->ieee8021SpbSysAreaAddress = StorageOld->ieee8021SpbSysAreaAddress;
			StorageTmp->ieee8021SpbSysAreaAddressLen = StorageOld->ieee8021SpbSysAreaAddressLen;
			StorageOld->ieee8021SpbSysAreaAddress = NULL;
			StorageOld->ieee8021SpbSysAreaAddressLen = 0;
		}
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbSysId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbSysId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbSysId entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysId not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021SpbSysId);
		StorageTmp->ieee8021SpbSysId = string;
		StorageTmp->ieee8021SpbSysIdLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbSysId for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		if (StorageOld->ieee8021SpbSysId != NULL) {
			SNMP_FREE(StorageTmp->ieee8021SpbSysId);
			StorageTmp->ieee8021SpbSysId = StorageOld->ieee8021SpbSysId;
			StorageTmp->ieee8021SpbSysIdLen = StorageOld->ieee8021SpbSysIdLen;
			StorageOld->ieee8021SpbSysId = NULL;
			StorageOld->ieee8021SpbSysIdLen = 0;
		}
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbSysControlAddr(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbSysControlAddr(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbSysControlAddr entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysControlAddr not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysControlAddr: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021SpbSysControlAddr);
		StorageTmp->ieee8021SpbSysControlAddr = string;
		StorageTmp->ieee8021SpbSysControlAddrLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbSysControlAddr for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		if (StorageOld->ieee8021SpbSysControlAddr != NULL) {
			SNMP_FREE(StorageTmp->ieee8021SpbSysControlAddr);
			StorageTmp->ieee8021SpbSysControlAddr = StorageOld->ieee8021SpbSysControlAddr;
			StorageTmp->ieee8021SpbSysControlAddrLen = StorageOld->ieee8021SpbSysControlAddrLen;
			StorageOld->ieee8021SpbSysControlAddr = NULL;
			StorageOld->ieee8021SpbSysControlAddrLen = 0;
		}
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmSysSPSourceId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbmSysSPSourceId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmSysSPSourceId entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmSysSPSourceId not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 3..3 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 3)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmSysSPSourceId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021SpbmSysSPSourceId);
		StorageTmp->ieee8021SpbmSysSPSourceId = string;
		StorageTmp->ieee8021SpbmSysSPSourceIdLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbmSysSPSourceId for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		if (StorageOld->ieee8021SpbmSysSPSourceId != NULL) {
			SNMP_FREE(StorageTmp->ieee8021SpbmSysSPSourceId);
			StorageTmp->ieee8021SpbmSysSPSourceId = StorageOld->ieee8021SpbmSysSPSourceId;
			StorageTmp->ieee8021SpbmSysSPSourceIdLen = StorageOld->ieee8021SpbmSysSPSourceIdLen;
			StorageOld->ieee8021SpbmSysSPSourceId = NULL;
			StorageOld->ieee8021SpbmSysSPSourceIdLen = 0;
		}
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbvSysMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbvSysMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbvSysMode entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvSysMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvSysMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value auto */
		switch (set_value) {
		case IEEE8021SPBVSYSMODE_AUTO:
		case IEEE8021SPBVSYSMODE_MANUAL:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbvSysMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		StorageTmp->ieee8021SpbvSysMode = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbvSysMode for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		StorageTmp->ieee8021SpbvSysMode = StorageOld->ieee8021SpbvSysMode;
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmSysMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbmSysMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmSysMode entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmSysMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmSysMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value auto */
		switch (set_value) {
		case IEEE8021SPBMSYSMODE_AUTO:
		case IEEE8021SPBMSYSMODE_MANUAL:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmSysMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		StorageTmp->ieee8021SpbmSysMode = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbmSysMode for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		StorageTmp->ieee8021SpbmSysMode = StorageOld->ieee8021SpbmSysMode;
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbSysDigestConvention(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021SpbSysDigestConvention(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbSysDigestConvention entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021SpbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysDigestConvention not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysDigestConvention: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value loopFreeBoth */
		switch (set_value) {
		case IEEE8021SPBSYSDIGESTCONVENTION_OFF:
		case IEEE8021SPBSYSDIGESTCONVENTION_LOOPFREEBOTH:
		case IEEE8021SPBSYSDIGESTCONVENTION_LOOPFREEMCASTONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbSysDigestConvention: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			if (StorageTmp->ieee8021SpbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021SpbMib_old = ieee8021SpbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021SpbMib_rsvs++;
		StorageTmp->ieee8021SpbSysDigestConvention = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021SpbMib_tsts == 0)
				if ((ret = check_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021SpbSysDigestConvention for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021SpbMib_sets == 0)
				if ((ret = update_ieee8021SpbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021SpbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) != NULL) {
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
			StorageTmp->ieee8021SpbMib_rsvs = 0;
			StorageTmp->ieee8021SpbMib_tsts = 0;
			StorageTmp->ieee8021SpbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021SpbMib_tsts == 0)
			revert_ieee8021SpbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021SpbMib_old) == NULL)
			break;
		StorageTmp->ieee8021SpbSysDigestConvention = StorageOld->ieee8021SpbSysDigestConvention;
		if (--StorageTmp->ieee8021SpbMib_rsvs == 0)
			ieee8021SpbMib_destroy(&StorageTmp->ieee8021SpbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021SpbMtidStaticTable_row(struct ieee8021SpbMtidStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021SpbEctStaticTable_row(struct ieee8021SpbEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021SpbAdjStaticTable_row(struct ieee8021SpbAdjStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021SpbmBsiStaticTable_row(struct ieee8021SpbmBsiStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_dot1agCfmMepSpbmEspTable_row(struct dot1agCfmMepSpbmEspTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbMtidStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021SpbMtidStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbMtidStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021SpbMtidStaticTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbMtidStaticEntryRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021SpbMtidStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbMtidStaticEntryRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbMtidStaticEntryRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021SpbMtidStaticEntryMtid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021SpbMtidStaticEntryMtid */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbMtidStaticEntryMtid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021SpbMtidStaticTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021SpbMtidStaticTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021SpbMtidStaticEntryMtid = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021SpbMtidStaticTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021SpbMtidStaticTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) == NULL)
				if (StorageTmp->ieee8021SpbMtidStaticTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old = ieee8021SpbMtidStaticTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021SpbMtidStaticTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021SpbMtidStaticTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021SpbMtidStaticTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021SpbMtidStaticTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021SpbMtidStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbMtidStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbMtidStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021SpbMtidStaticEntryRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021SpbMtidStaticTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021SpbMtidStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021SpbMtidStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021SpbMtidStaticTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021SpbMtidStaticEntryRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021SpbMtidStaticEntryRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021SpbMtidStaticEntryRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) != NULL) {
				ieee8021SpbMtidStaticTable_destroy(&StorageTmp->ieee8021SpbMtidStaticTable_old);
				StorageTmp->ieee8021SpbMtidStaticTable_rsvs = 0;
				StorageTmp->ieee8021SpbMtidStaticTable_tsts = 0;
				StorageTmp->ieee8021SpbMtidStaticTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021SpbMtidStaticTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021SpbMtidStaticTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021SpbMtidStaticTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021SpbMtidStaticEntryRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021SpbMtidStaticTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021SpbMtidStaticTable_del(StorageNew);
				ieee8021SpbMtidStaticTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021SpbMtidStaticTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021SpbMtidStaticTable_rsvs == 0)
				ieee8021SpbMtidStaticTable_destroy(&StorageTmp->ieee8021SpbMtidStaticTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021SpbMtidStaticTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbEctStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021SpbEctStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbEctStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021SpbEctStaticTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbEctStaticEntryRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021SpbEctStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbEctStaticEntryRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbEctStaticEntryRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021SpbEctStaticEntryTopIx */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021SpbEctStaticEntryBaseVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021SpbEctStaticEntryTopIx */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbEctStaticEntryTopIx: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021SpbEctStaticEntryBaseVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbEctStaticEntryBaseVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 4095..4095 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094) && (long) *vp->val.integer != 4095) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbEctStaticEntryBaseVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021SpbEctStaticTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021SpbEctStaticTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021SpbEctStaticEntryTopIx = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021SpbEctStaticEntryBaseVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021SpbEctStaticTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021SpbEctStaticTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
				if (StorageTmp->ieee8021SpbEctStaticTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old = ieee8021SpbEctStaticTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021SpbEctStaticTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021SpbEctStaticTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021SpbEctStaticTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021SpbEctStaticTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021SpbEctStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbEctStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbEctStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021SpbEctStaticEntryRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021SpbEctStaticTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021SpbEctStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021SpbEctStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021SpbEctStaticTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021SpbEctStaticEntryRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021SpbEctStaticEntryRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021SpbEctStaticEntryRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) != NULL) {
				ieee8021SpbEctStaticTable_destroy(&StorageTmp->ieee8021SpbEctStaticTable_old);
				StorageTmp->ieee8021SpbEctStaticTable_rsvs = 0;
				StorageTmp->ieee8021SpbEctStaticTable_tsts = 0;
				StorageTmp->ieee8021SpbEctStaticTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021SpbEctStaticTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021SpbEctStaticTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021SpbEctStaticTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021SpbEctStaticEntryRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021SpbEctStaticTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021SpbEctStaticTable_del(StorageNew);
				ieee8021SpbEctStaticTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021SpbEctStaticTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021SpbEctStaticTable_rsvs == 0)
				ieee8021SpbEctStaticTable_destroy(&StorageTmp->ieee8021SpbEctStaticTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021SpbEctStaticTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbAdjStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021SpbAdjStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbAdjStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021SpbAdjStaticTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbAdjStaticEntryRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021SpbAdjStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbAdjStaticEntryRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021SpbAdjStaticEntryTopIx */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021SpbAdjStaticEntryIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021SpbAdjStaticEntryTopIx */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbAdjStaticEntryTopIx: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021SpbAdjStaticEntryIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbAdjStaticEntryIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..2147483647 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbAdjStaticEntryIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021SpbAdjStaticTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021SpbAdjStaticTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021SpbAdjStaticEntryTopIx = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021SpbAdjStaticEntryIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021SpbAdjStaticTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021SpbAdjStaticTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
				if (StorageTmp->ieee8021SpbAdjStaticTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old = ieee8021SpbAdjStaticTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021SpbAdjStaticTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021SpbAdjStaticTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021SpbAdjStaticTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021SpbAdjStaticTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021SpbAdjStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbAdjStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbAdjStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021SpbAdjStaticEntryRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021SpbAdjStaticTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021SpbAdjStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021SpbAdjStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021SpbAdjStaticTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021SpbAdjStaticEntryRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021SpbAdjStaticEntryRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021SpbAdjStaticEntryRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) != NULL) {
				ieee8021SpbAdjStaticTable_destroy(&StorageTmp->ieee8021SpbAdjStaticTable_old);
				StorageTmp->ieee8021SpbAdjStaticTable_rsvs = 0;
				StorageTmp->ieee8021SpbAdjStaticTable_tsts = 0;
				StorageTmp->ieee8021SpbAdjStaticTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021SpbAdjStaticTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021SpbAdjStaticTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021SpbAdjStaticTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021SpbAdjStaticEntryRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021SpbAdjStaticTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021SpbAdjStaticTable_del(StorageNew);
				ieee8021SpbAdjStaticTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021SpbAdjStaticTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021SpbAdjStaticTable_rsvs == 0)
				ieee8021SpbAdjStaticTable_destroy(&StorageTmp->ieee8021SpbAdjStaticTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021SpbAdjStaticTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021SpbmBsiStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021SpbmBsiStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021SpbmBsiStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021SpbmBsiStaticTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021SpbMib", "write_ieee8021SpbmBsiStaticEntryRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021SpbmBsiStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021SpbmBsiStaticEntryRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021SpbTopIx */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021SpbmBsiStaticEntryIsid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021SpbmBsiStaticEntryBaseVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021SpbTopIx */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbTopIx: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021SpbmBsiStaticEntryIsid */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbmBsiStaticEntryIsid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 256..16777214 */
			if ((256 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16777214)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbmBsiStaticEntryIsid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021SpbmBsiStaticEntryBaseVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbmBsiStaticEntryBaseVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbmBsiStaticEntryBaseVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021SpbmBsiStaticTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021SpbmBsiStaticTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021SpbTopIx = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021SpbmBsiStaticEntryIsid = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021SpbmBsiStaticEntryBaseVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021SpbmBsiStaticTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021SpbmBsiStaticTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
				if (StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old = ieee8021SpbmBsiStaticTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021SpbmBsiStaticTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021SpbmBsiStaticTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021SpbmBsiStaticTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021SpbmBsiStaticTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021SpbmBsiStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbmBsiStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021SpbmBsiStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021SpbmBsiStaticTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021SpbmBsiStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021SpbmBsiStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021SpbmBsiStaticTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021SpbmBsiStaticEntryRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021SpbmBsiStaticEntryRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021SpbmBsiStaticEntryRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) != NULL) {
				ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
				StorageTmp->ieee8021SpbmBsiStaticTable_rsvs = 0;
				StorageTmp->ieee8021SpbmBsiStaticTable_tsts = 0;
				StorageTmp->ieee8021SpbmBsiStaticTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021SpbmBsiStaticTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021SpbmBsiStaticTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021SpbmBsiStaticTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021SpbmBsiStaticEntryRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021SpbmBsiStaticTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021SpbmBsiStaticTable_del(StorageNew);
				ieee8021SpbmBsiStaticTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021SpbmBsiStaticTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021SpbmBsiStaticTable_rsvs == 0)
				ieee8021SpbmBsiStaticTable_destroy(&StorageTmp->ieee8021SpbmBsiStaticTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021SpbmBsiStaticTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dot1agCfmMepSpbmEspRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_dot1agCfmMepSpbmEspRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct dot1agCfmMepSpbmEspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct dot1agCfmMepSpbmEspTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021SpbMib", "write_dot1agCfmMepSpbmEspRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(dot1agCfmMepSpbmEspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepSpbmEspRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to dot1agCfmMepSpbmEspRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* dot1agCfmMdIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* dot1agCfmMaIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* dot1agCfmMepSpbmEspIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* dot1agCfmMdIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index dot1agCfmMdIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index dot1agCfmMdIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* dot1agCfmMaIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index dot1agCfmMaIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index dot1agCfmMaIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* dot1agCfmMepSpbmEspIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index dot1agCfmMepSpbmEspIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index dot1agCfmMepSpbmEspIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = dot1agCfmMepSpbmEspTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->dot1agCfmMepSpbmEspTable_rsvs = 1;
			vp = vars;
			StorageNew->dot1agCfmMdIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->dot1agCfmMaIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->dot1agCfmMepSpbmEspIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&dot1agCfmMepSpbmEspTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->dot1agCfmMepSpbmEspTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) == NULL)
				if (StorageTmp->dot1agCfmMepSpbmEspTable_rsvs == 0)
					if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old = dot1agCfmMepSpbmEspTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->dot1agCfmMepSpbmEspTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->dot1agCfmMepSpbmEspTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				dot1agCfmMepSpbmEspTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_dot1agCfmMepSpbmEspTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus != RS_ACTIVE)
				if ((ret = can_act_dot1agCfmMepSpbmEspTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_dot1agCfmMepSpbmEspTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_dot1agCfmMepSpbmEspTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->dot1agCfmMepSpbmEspRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_dot1agCfmMepSpbmEspTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_dot1agCfmMepSpbmEspTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_dot1agCfmMepSpbmEspTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_dot1agCfmMepSpbmEspTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->dot1agCfmMepSpbmEspRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->dot1agCfmMepSpbmEspRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->dot1agCfmMepSpbmEspRowStatus = set_value;
			if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) != NULL) {
				dot1agCfmMepSpbmEspTable_destroy(&StorageTmp->dot1agCfmMepSpbmEspTable_old);
				StorageTmp->dot1agCfmMepSpbmEspTable_rsvs = 0;
				StorageTmp->dot1agCfmMepSpbmEspTable_tsts = 0;
				StorageTmp->dot1agCfmMepSpbmEspTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			dot1agCfmMepSpbmEspTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_dot1agCfmMepSpbmEspTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_dot1agCfmMepSpbmEspTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->dot1agCfmMepSpbmEspRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_dot1agCfmMepSpbmEspTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				dot1agCfmMepSpbmEspTable_del(StorageNew);
				dot1agCfmMepSpbmEspTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->dot1agCfmMepSpbmEspTable_old) == NULL)
				break;
			if (--StorageTmp->dot1agCfmMepSpbmEspTable_rsvs == 0)
				dot1agCfmMepSpbmEspTable_destroy(&StorageTmp->dot1agCfmMepSpbmEspTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				dot1agCfmMepSpbmEspTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021SpbMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021SpbMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021SpbMibold_signal_handler != NULL)
		(*ieee8021SpbMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
}

/**
 * @fn void ieee8021SpbMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021SpbMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021SpbMib", "ieee8021SpbMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021SpbMib", "done.\n"));
	return;
}
