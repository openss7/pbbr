/*****************************************************************************

 @(#) src/snmp/lldpxdot1mib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/lldpxdot1mib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "lldpXdot1MIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "lldpXdot1MIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int lldpXdot1MIB_refresh = 1;
volatile int lldpXdot1ConfigPortVlanTable_refresh = 1;
volatile int lldpXdot1ConfigVlanNameTable_refresh = 1;
volatile int lldpXdot1ConfigProtoVlanTable_refresh = 1;
volatile int lldpXdot1ConfigProtocolTable_refresh = 1;
volatile int lldpXdot1LocTable_refresh = 1;
volatile int lldpXdot1LocProtoVlanTable_refresh = 1;
volatile int lldpXdot1LocVlanNameTable_refresh = 1;
volatile int lldpXdot1LocProtocolTable_refresh = 1;
volatile int lldpXdot1RemTable_refresh = 1;
volatile int lldpXdot1RemProtoVlanTable_refresh = 1;
volatile int lldpXdot1RemVlanNameTable_refresh = 1;
volatile int lldpXdot1RemProtocolTable_refresh = 1;

/*
 * lldpXdot1MIB_variables_oid: object identifier for lldpXdot1MIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid lldpXdot1MIB_variables_oid[9] = { 1, 0, 8802, 1, 1, 2, 1, 5, 32962 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid lldpXdot1Compliance_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 32962, 2, 1, 1 };
oid lldpXdot1ConfigGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 32962, 2, 2, 1 };
oid lldpXdot1LocSysGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 32962, 2, 2, 2 };
oid lldpXdot1RemSysGroup_oid[12] = { 1, 0, 8802, 1, 1, 2, 1, 5, 32962, 2, 2, 3 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 lldpXdot1MIB_variables: tree for lldpXdot1MIB
 * This variable defines function callbacks and type return information for the lldpXdot1MIB mib section
 */
struct variable7 lldpXdot1MIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   LLDPXDOT1CONFIGPORTVLANTXENABLE  1
	{(u_char) LLDPXDOT1CONFIGPORTVLANTXENABLE, ASN_INTEGER, RWRITE, var_lldpXdot1ConfigPortVlanTable, 5, {1, 1, 1, 1, 1}},
#define   LLDPXDOT1CONFIGVLANNAMETXENABLE  2
	{(u_char) LLDPXDOT1CONFIGVLANNAMETXENABLE, ASN_INTEGER, RWRITE, var_lldpXdot1ConfigVlanNameTable, 5, {1, 1, 2, 1, 1}},
#define   LLDPXDOT1CONFIGPROTOVLANTXENABLE  3
	{(u_char) LLDPXDOT1CONFIGPROTOVLANTXENABLE, ASN_INTEGER, RWRITE, var_lldpXdot1ConfigProtoVlanTable, 5, {1, 1, 3, 1, 1}},
#define   LLDPXDOT1CONFIGPROTOCOLTXENABLE  4
	{(u_char) LLDPXDOT1CONFIGPROTOCOLTXENABLE, ASN_INTEGER, RWRITE, var_lldpXdot1ConfigProtocolTable, 5, {1, 1, 4, 1, 1}},
#define   LLDPXDOT1LOCPORTVLANID  5
	{(u_char) LLDPXDOT1LOCPORTVLANID, ASN_INTEGER, RONLY, var_lldpXdot1LocTable, 5, {1, 2, 1, 1, 1}},
#define   LLDPXDOT1LOCPROTOVLANSUPPORTED  6
	{(u_char) LLDPXDOT1LOCPROTOVLANSUPPORTED, ASN_INTEGER, RONLY, var_lldpXdot1LocProtoVlanTable, 5, {1, 2, 2, 1, 2}},
#define   LLDPXDOT1LOCPROTOVLANENABLED  7
	{(u_char) LLDPXDOT1LOCPROTOVLANENABLED, ASN_INTEGER, RONLY, var_lldpXdot1LocProtoVlanTable, 5, {1, 2, 2, 1, 3}},
#define   LLDPXDOT1LOCVLANNAME  8
	{(u_char) LLDPXDOT1LOCVLANNAME, ASN_OCTET_STR, RONLY, var_lldpXdot1LocVlanNameTable, 5, {1, 2, 3, 1, 2}},
#define   LLDPXDOT1LOCPROTOCOLID  9
	{(u_char) LLDPXDOT1LOCPROTOCOLID, ASN_OCTET_STR, RONLY, var_lldpXdot1LocProtocolTable, 5, {1, 2, 4, 1, 2}},
#define   LLDPXDOT1REMPORTVLANID  10
	{(u_char) LLDPXDOT1REMPORTVLANID, ASN_INTEGER, RONLY, var_lldpXdot1RemTable, 5, {1, 3, 1, 1, 1}},
#define   LLDPXDOT1REMPROTOVLANSUPPORTED  11
	{(u_char) LLDPXDOT1REMPROTOVLANSUPPORTED, ASN_INTEGER, RONLY, var_lldpXdot1RemProtoVlanTable, 5, {1, 3, 2, 1, 2}},
#define   LLDPXDOT1REMPROTOVLANENABLED  12
	{(u_char) LLDPXDOT1REMPROTOVLANENABLED, ASN_INTEGER, RONLY, var_lldpXdot1RemProtoVlanTable, 5, {1, 3, 2, 1, 3}},
#define   LLDPXDOT1REMVLANNAME  13
	{(u_char) LLDPXDOT1REMVLANNAME, ASN_OCTET_STR, RONLY, var_lldpXdot1RemVlanNameTable, 5, {1, 3, 3, 1, 2}},
#define   LLDPXDOT1REMPROTOCOLID  14
	{(u_char) LLDPXDOT1REMPROTOCOLID, ASN_OCTET_STR, RONLY, var_lldpXdot1RemProtocolTable, 5, {1, 3, 4, 1, 2}},
};

/* (L = length of the oidsuffix) */
struct lldpXdot1MIB_data *lldpXdot1MIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *lldpXdot1ConfigPortVlanTableStorage = NULL;
struct header_complex_index *lldpXdot1ConfigVlanNameTableStorage = NULL;
struct header_complex_index *lldpXdot1ConfigProtoVlanTableStorage = NULL;
struct header_complex_index *lldpXdot1ConfigProtocolTableStorage = NULL;
struct header_complex_index *lldpXdot1LocTableStorage = NULL;
struct header_complex_index *lldpXdot1LocProtoVlanTableStorage = NULL;
struct header_complex_index *lldpXdot1LocVlanNameTableStorage = NULL;
struct header_complex_index *lldpXdot1LocProtocolTableStorage = NULL;
struct header_complex_index *lldpXdot1RemTableStorage = NULL;
struct header_complex_index *lldpXdot1RemProtoVlanTableStorage = NULL;
struct header_complex_index *lldpXdot1RemVlanNameTableStorage = NULL;
struct header_complex_index *lldpXdot1RemProtocolTableStorage = NULL;

void (*lldpXdot1MIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void lldpXdot1MIB_loop_handler(int);
void lldpXdot1MIB_fd_handler(int, void *);

/**
 * @fn void init_lldpXdot1MIB(void)
 * @brief lldpXdot1MIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (lldpXdot1MIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_lldpXdot1MIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("lldpXdot1MIB", "init_lldpXdot1MIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("lldpXdot1MIB", lldpXdot1MIB_variables, variable7, lldpXdot1MIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_lldpXdot1MIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("lldpXdot1MIB", parse_lldpXdot1MIB, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1ConfigPortVlanTable", parse_lldpXdot1ConfigPortVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1ConfigVlanNameTable", parse_lldpXdot1ConfigVlanNameTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1ConfigProtoVlanTable", parse_lldpXdot1ConfigProtoVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1ConfigProtocolTable", parse_lldpXdot1ConfigProtocolTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1LocTable", parse_lldpXdot1LocTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1LocProtoVlanTable", parse_lldpXdot1LocProtoVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1LocVlanNameTable", parse_lldpXdot1LocVlanNameTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1LocProtocolTable", parse_lldpXdot1LocProtocolTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1RemTable", parse_lldpXdot1RemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1RemProtoVlanTable", parse_lldpXdot1RemProtoVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1RemVlanNameTable", parse_lldpXdot1RemVlanNameTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1RemProtocolTable", parse_lldpXdot1RemProtocolTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1MIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1ConfigPortVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1ConfigVlanNameTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1ConfigProtoVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1ConfigProtocolTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1LocTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1LocProtoVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1LocVlanNameTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1LocProtocolTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1RemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1RemProtoVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1RemVlanNameTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1RemProtocolTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, lldpXdot1MIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, lldpXdot1MIB_fd_handler, (void *) 1);
	}
	lldpXdot1MIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &lldpXdot1MIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/**
 * @fn void deinit_lldpXdot1MIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (lldpXdot1MIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_lldpXdot1MIB(void)
{
	DEBUGMSGTL(("lldpXdot1MIB", "deinit_lldpXdot1MIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = lldpXdot1MIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(lldpXdot1MIB_variables_oid, sizeof(lldpXdot1MIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("lldpXdot1MIB");
	snmpd_unregister_config_handler("lldpXdot1ConfigPortVlanTable");
	snmpd_unregister_config_handler("lldpXdot1ConfigVlanNameTable");
	snmpd_unregister_config_handler("lldpXdot1ConfigProtoVlanTable");
	snmpd_unregister_config_handler("lldpXdot1ConfigProtocolTable");
	snmpd_unregister_config_handler("lldpXdot1LocTable");
	snmpd_unregister_config_handler("lldpXdot1LocProtoVlanTable");
	snmpd_unregister_config_handler("lldpXdot1LocVlanNameTable");
	snmpd_unregister_config_handler("lldpXdot1LocProtocolTable");
	snmpd_unregister_config_handler("lldpXdot1RemTable");
	snmpd_unregister_config_handler("lldpXdot1RemProtoVlanTable");
	snmpd_unregister_config_handler("lldpXdot1RemVlanNameTable");
	snmpd_unregister_config_handler("lldpXdot1RemProtocolTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

int
term_lldpXdot1MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("lldpXdot1MIB", "term_lldpXdot1MIB: terminating...  "));
	deinit_lldpXdot1MIB();
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return 0;
}

/**
 * @fn struct lldpXdot1MIB_data *lldpXdot1MIB_create(void)
 * @brief create a fresh data structure representing scalars in lldpXdot1MIB.
 *
 * Creates a new lldpXdot1MIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in lldpXdot1MIB.
 */
struct lldpXdot1MIB_data *
lldpXdot1MIB_create(void)
{
	struct lldpXdot1MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1MIB_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1MIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1MIB_data *lldpXdot1MIB_duplicate(struct lldpXdot1MIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct lldpXdot1MIB_data *
lldpXdot1MIB_duplicate(struct lldpXdot1MIB_data *thedata)
{
	struct lldpXdot1MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1MIB_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1MIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1MIB_destroy(struct lldpXdot1MIB_data **thedata)
 * @param thedata pointer to the data structure in lldpXdot1MIB.
 * @brief delete a scalars structure from lldpXdot1MIB.
 *
 * Frees scalars that were previously removed from lldpXdot1MIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1MIB_destroy(struct lldpXdot1MIB_data **thedata)
{
	struct lldpXdot1MIB_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1MIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1MIB_add(struct lldpXdot1MIB_data *thedata)
 * @param thedata the structure representing lldpXdot1MIB scalars.
 * @brief adds node to the lldpXdot1MIB scalar data set.
 *
 * Adds a scalar structure to the lldpXdot1MIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
lldpXdot1MIB_add(struct lldpXdot1MIB_data *thedata)
{
	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1MIB_add: adding data...  "));
	if (thedata)
		lldpXdot1MIBStorage = thedata;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1MIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1MIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1MIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_lldpXdot1MIB(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1MIB_data *StorageTmp = lldpXdot1MIB_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1MIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	lldpXdot1MIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1MIB(): stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1MIB_data *StorageTmp;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1MIB: storing data...  "));
	refresh_lldpXdot1MIB(1);
	if ((StorageTmp = lldpXdot1MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpXdot1MIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "lldpXdot1MIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpXdot1MIB(struct lldpXdot1MIB_data *StorageTmp, struct lldpXdot1MIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_lldpXdot1MIB(struct lldpXdot1MIB_data *StorageTmp, struct lldpXdot1MIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1MIB(struct lldpXdot1MIB_data *StorageTmp, struct lldpXdot1MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_lldpXdot1MIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1MIB(struct lldpXdot1MIB_data *StorageTmp, struct lldpXdot1MIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1MIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_lldpXdot1MIB(struct 
 * @fn void revert_lldpXdot1MIB(struct lldpXdot1MIB_data *StorageTmp, struct lldpXdot1MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_lldpXdot1MIB(struct lldpXdot1MIB_data *StorageTmp, struct lldpXdot1MIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1MIB(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1MIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of lldpXdot1MIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_lldpXdot1MIB(int force)
{
	if (lldpXdot1MIBStorage == NULL) {
		struct lldpXdot1MIB_data *StorageNew;

		if ((StorageNew = lldpXdot1MIB_create()) == NULL)
			return;
		lldpXdot1MIBStorage = StorageNew;
		lldpXdot1MIB_refresh = 1;
	}
	if (!force && lldpXdot1MIB_refresh == 0)
		return;
	DEBUGMSGTL(("lldpXdot1MIB", "refresh_lldpXdot1MIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	lldpXdot1MIB_refresh = 0;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/**
 * @fn u_char * var_lldpXdot1MIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in lldpXdot1MIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_lldpXdot1MIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1MIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1MIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_lldpXdot1MIB(0);
	if ((StorageTmp = lldpXdot1MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpXdot1MIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("lldpXdot1MIB", "found.\n"));
	else
		DEBUGMSGTL(("lldpXdot1MIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct lldpXdot1ConfigPortVlanTable_data *lldpXdot1ConfigPortVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1ConfigPortVlanTable table.
 *
 * Creates a new lldpXdot1ConfigPortVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1ConfigPortVlanTable_data *
lldpXdot1ConfigPortVlanTable_create(void)
{
	struct lldpXdot1ConfigPortVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigPortVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigPortVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpPortConfigPortNum = 0;
		StorageNew->lldpXdot1ConfigPortVlanTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1ConfigPortVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1ConfigPortVlanTable_data *lldpXdot1ConfigPortVlanTable_duplicate(struct lldpXdot1ConfigPortVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1ConfigPortVlanTable_data *
lldpXdot1ConfigPortVlanTable_duplicate(struct lldpXdot1ConfigPortVlanTable_data *thedata)
{
	struct lldpXdot1ConfigPortVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigPortVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigPortVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1ConfigPortVlanTable_id = thedata->lldpXdot1ConfigPortVlanTable_id;
		StorageNew->lldpPortConfigPortNum = thedata->lldpPortConfigPortNum;
		StorageNew->lldpXdot1ConfigPortVlanTxEnable = thedata->lldpXdot1ConfigPortVlanTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1ConfigPortVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1ConfigPortVlanTable_destroy(struct lldpXdot1ConfigPortVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1ConfigPortVlanTable_destroy(struct lldpXdot1ConfigPortVlanTable_data **thedata)
{
	struct lldpXdot1ConfigPortVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigPortVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigPortVlanTable_add(struct lldpXdot1ConfigPortVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1ConfigPortVlanTable table data set.
 *
 * Adds a table row structure to the lldpXdot1ConfigPortVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1ConfigPortVlanTable_add(struct lldpXdot1ConfigPortVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigPortVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpPortConfigPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpPortConfigPortNum, sizeof(thedata->lldpPortConfigPortNum));
		header_complex_add_data(&lldpXdot1ConfigPortVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigPortVlanTable_del(struct lldpXdot1ConfigPortVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1ConfigPortVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1ConfigPortVlanTable_del(struct lldpXdot1ConfigPortVlanTable_data *thedata)
{
	struct lldpXdot1ConfigPortVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigPortVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1ConfigPortVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1ConfigPortVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1ConfigPortVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1ConfigPortVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1ConfigPortVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1ConfigPortVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1ConfigPortVlanTable_data *StorageTmp = lldpXdot1ConfigPortVlanTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1ConfigPortVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpPortConfigPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1ConfigPortVlanTxEnable, &tmpsize);
	lldpXdot1ConfigPortVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1ConfigPortVlanTable(): store configuraiton file for lldpXdot1ConfigPortVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1ConfigPortVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1ConfigPortVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1ConfigPortVlanTable: storing data...  "));
	refresh_lldpXdot1ConfigPortVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1ConfigPortVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1ConfigPortVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1ConfigPortVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpPortConfigPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1ConfigPortVlanTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1ConfigVlanNameTable_data *lldpXdot1ConfigVlanNameTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1ConfigVlanNameTable table.
 *
 * Creates a new lldpXdot1ConfigVlanNameTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1ConfigVlanNameTable_data *
lldpXdot1ConfigVlanNameTable_create(void)
{
	struct lldpXdot1ConfigVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigVlanNameTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigVlanNameTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXdot1LocVlanId = 0;
		StorageNew->lldpXdot1ConfigVlanNameTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1ConfigVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1ConfigVlanNameTable_data *lldpXdot1ConfigVlanNameTable_duplicate(struct lldpXdot1ConfigVlanNameTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1ConfigVlanNameTable_data *
lldpXdot1ConfigVlanNameTable_duplicate(struct lldpXdot1ConfigVlanNameTable_data *thedata)
{
	struct lldpXdot1ConfigVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigVlanNameTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigVlanNameTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1ConfigVlanNameTable_id = thedata->lldpXdot1ConfigVlanNameTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocVlanId = thedata->lldpXdot1LocVlanId;
		StorageNew->lldpXdot1ConfigVlanNameTxEnable = thedata->lldpXdot1ConfigVlanNameTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1ConfigVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1ConfigVlanNameTable_destroy(struct lldpXdot1ConfigVlanNameTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1ConfigVlanNameTable_destroy(struct lldpXdot1ConfigVlanNameTable_data **thedata)
{
	struct lldpXdot1ConfigVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigVlanNameTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigVlanNameTable_add(struct lldpXdot1ConfigVlanNameTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1ConfigVlanNameTable table data set.
 *
 * Adds a table row structure to the lldpXdot1ConfigVlanNameTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1ConfigVlanNameTable_add(struct lldpXdot1ConfigVlanNameTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigVlanNameTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXdot1LocVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1LocVlanId, sizeof(thedata->lldpXdot1LocVlanId));
		header_complex_add_data(&lldpXdot1ConfigVlanNameTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigVlanNameTable_del(struct lldpXdot1ConfigVlanNameTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1ConfigVlanNameTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1ConfigVlanNameTable_del(struct lldpXdot1ConfigVlanNameTable_data *thedata)
{
	struct lldpXdot1ConfigVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigVlanNameTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1ConfigVlanNameTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1ConfigVlanNameTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1ConfigVlanNameTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1ConfigVlanNameTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1ConfigVlanNameTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1ConfigVlanNameTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1ConfigVlanNameTable_data *StorageTmp = lldpXdot1ConfigVlanNameTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1ConfigVlanNameTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1ConfigVlanNameTxEnable, &tmpsize);
	lldpXdot1ConfigVlanNameTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1ConfigVlanNameTable(): store configuraiton file for lldpXdot1ConfigVlanNameTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1ConfigVlanNameTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1ConfigVlanNameTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1ConfigVlanNameTable: storing data...  "));
	refresh_lldpXdot1ConfigVlanNameTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1ConfigVlanNameTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1ConfigVlanNameTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1ConfigVlanNameTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1ConfigVlanNameTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1ConfigProtoVlanTable_data *lldpXdot1ConfigProtoVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1ConfigProtoVlanTable table.
 *
 * Creates a new lldpXdot1ConfigProtoVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1ConfigProtoVlanTable_data *
lldpXdot1ConfigProtoVlanTable_create(void)
{
	struct lldpXdot1ConfigProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigProtoVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtoVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXdot1LocProtoVlanId = 0;
		StorageNew->lldpXdot1ConfigProtoVlanTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1ConfigProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1ConfigProtoVlanTable_data *lldpXdot1ConfigProtoVlanTable_duplicate(struct lldpXdot1ConfigProtoVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1ConfigProtoVlanTable_data *
lldpXdot1ConfigProtoVlanTable_duplicate(struct lldpXdot1ConfigProtoVlanTable_data *thedata)
{
	struct lldpXdot1ConfigProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigProtoVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtoVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1ConfigProtoVlanTable_id = thedata->lldpXdot1ConfigProtoVlanTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocProtoVlanId = thedata->lldpXdot1LocProtoVlanId;
		StorageNew->lldpXdot1ConfigProtoVlanTxEnable = thedata->lldpXdot1ConfigProtoVlanTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1ConfigProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1ConfigProtoVlanTable_destroy(struct lldpXdot1ConfigProtoVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1ConfigProtoVlanTable_destroy(struct lldpXdot1ConfigProtoVlanTable_data **thedata)
{
	struct lldpXdot1ConfigProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtoVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigProtoVlanTable_add(struct lldpXdot1ConfigProtoVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1ConfigProtoVlanTable table data set.
 *
 * Adds a table row structure to the lldpXdot1ConfigProtoVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1ConfigProtoVlanTable_add(struct lldpXdot1ConfigProtoVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtoVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXdot1LocProtoVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1LocProtoVlanId, sizeof(thedata->lldpXdot1LocProtoVlanId));
		header_complex_add_data(&lldpXdot1ConfigProtoVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigProtoVlanTable_del(struct lldpXdot1ConfigProtoVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1ConfigProtoVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1ConfigProtoVlanTable_del(struct lldpXdot1ConfigProtoVlanTable_data *thedata)
{
	struct lldpXdot1ConfigProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtoVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1ConfigProtoVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1ConfigProtoVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1ConfigProtoVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1ConfigProtoVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1ConfigProtoVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1ConfigProtoVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp = lldpXdot1ConfigProtoVlanTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1ConfigProtoVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocProtoVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1ConfigProtoVlanTxEnable, &tmpsize);
	lldpXdot1ConfigProtoVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1ConfigProtoVlanTable(): store configuraiton file for lldpXdot1ConfigProtoVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1ConfigProtoVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1ConfigProtoVlanTable: storing data...  "));
	refresh_lldpXdot1ConfigProtoVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1ConfigProtoVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1ConfigProtoVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1ConfigProtoVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocProtoVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1ConfigProtoVlanTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1ConfigProtocolTable_data *lldpXdot1ConfigProtocolTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1ConfigProtocolTable table.
 *
 * Creates a new lldpXdot1ConfigProtocolTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1ConfigProtocolTable_data *
lldpXdot1ConfigProtocolTable_create(void)
{
	struct lldpXdot1ConfigProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigProtocolTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtocolTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXdot1LocProtocolIndex = 0;
		StorageNew->lldpXdot1ConfigProtocolTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1ConfigProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1ConfigProtocolTable_data *lldpXdot1ConfigProtocolTable_duplicate(struct lldpXdot1ConfigProtocolTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1ConfigProtocolTable_data *
lldpXdot1ConfigProtocolTable_duplicate(struct lldpXdot1ConfigProtocolTable_data *thedata)
{
	struct lldpXdot1ConfigProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1ConfigProtocolTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtocolTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1ConfigProtocolTable_id = thedata->lldpXdot1ConfigProtocolTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocProtocolIndex = thedata->lldpXdot1LocProtocolIndex;
		StorageNew->lldpXdot1ConfigProtocolTxEnable = thedata->lldpXdot1ConfigProtocolTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1ConfigProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1ConfigProtocolTable_destroy(struct lldpXdot1ConfigProtocolTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1ConfigProtocolTable_destroy(struct lldpXdot1ConfigProtocolTable_data **thedata)
{
	struct lldpXdot1ConfigProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtocolTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigProtocolTable_add(struct lldpXdot1ConfigProtocolTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1ConfigProtocolTable table data set.
 *
 * Adds a table row structure to the lldpXdot1ConfigProtocolTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1ConfigProtocolTable_add(struct lldpXdot1ConfigProtocolTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtocolTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXdot1LocProtocolIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1LocProtocolIndex, sizeof(thedata->lldpXdot1LocProtocolIndex));
		header_complex_add_data(&lldpXdot1ConfigProtocolTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1ConfigProtocolTable_del(struct lldpXdot1ConfigProtocolTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1ConfigProtocolTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1ConfigProtocolTable_del(struct lldpXdot1ConfigProtocolTable_data *thedata)
{
	struct lldpXdot1ConfigProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1ConfigProtocolTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1ConfigProtocolTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1ConfigProtocolTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1ConfigProtocolTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1ConfigProtocolTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1ConfigProtocolTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1ConfigProtocolTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1ConfigProtocolTable_data *StorageTmp = lldpXdot1ConfigProtocolTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1ConfigProtocolTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocProtocolIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1ConfigProtocolTxEnable, &tmpsize);
	lldpXdot1ConfigProtocolTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1ConfigProtocolTable(): store configuraiton file for lldpXdot1ConfigProtocolTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1ConfigProtocolTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1ConfigProtocolTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1ConfigProtocolTable: storing data...  "));
	refresh_lldpXdot1ConfigProtocolTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1ConfigProtocolTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1ConfigProtocolTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1ConfigProtocolTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocProtocolIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1ConfigProtocolTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1LocTable_data *lldpXdot1LocTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1LocTable table.
 *
 * Creates a new lldpXdot1LocTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1LocTable_data *
lldpXdot1LocTable_create(void)
{
	struct lldpXdot1LocTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXdot1LocPortVlanId = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1LocTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1LocTable_data *lldpXdot1LocTable_duplicate(struct lldpXdot1LocTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1LocTable_data *
lldpXdot1LocTable_duplicate(struct lldpXdot1LocTable_data *thedata)
{
	struct lldpXdot1LocTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1LocTable_id = thedata->lldpXdot1LocTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocPortVlanId = thedata->lldpXdot1LocPortVlanId;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1LocTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1LocTable_destroy(struct lldpXdot1LocTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1LocTable_destroy(struct lldpXdot1LocTable_data **thedata)
{
	struct lldpXdot1LocTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocTable_add(struct lldpXdot1LocTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1LocTable table data set.
 *
 * Adds a table row structure to the lldpXdot1LocTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1LocTable_add(struct lldpXdot1LocTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		header_complex_add_data(&lldpXdot1LocTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocTable_del(struct lldpXdot1LocTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1LocTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1LocTable_del(struct lldpXdot1LocTable_data *thedata)
{
	struct lldpXdot1LocTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1LocTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1LocTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1LocTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1LocTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1LocTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1LocTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1LocTable_data *StorageTmp = lldpXdot1LocTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1LocTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocPortVlanId, &tmpsize);
	lldpXdot1LocTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1LocTable(): store configuraiton file for lldpXdot1LocTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1LocTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1LocTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1LocTable: storing data...  "));
	refresh_lldpXdot1LocTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1LocTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1LocTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1LocTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocPortVlanId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1LocProtoVlanTable_data *lldpXdot1LocProtoVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1LocProtoVlanTable table.
 *
 * Creates a new lldpXdot1LocProtoVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1LocProtoVlanTable_data *
lldpXdot1LocProtoVlanTable_create(void)
{
	struct lldpXdot1LocProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocProtoVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtoVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		StorageNew->lldpXdot1LocProtoVlanSupported = 0;
		StorageNew->lldpXdot1LocProtoVlanEnabled = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1LocProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1LocProtoVlanTable_data *lldpXdot1LocProtoVlanTable_duplicate(struct lldpXdot1LocProtoVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1LocProtoVlanTable_data *
lldpXdot1LocProtoVlanTable_duplicate(struct lldpXdot1LocProtoVlanTable_data *thedata)
{
	struct lldpXdot1LocProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocProtoVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtoVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1LocProtoVlanTable_id = thedata->lldpXdot1LocProtoVlanTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocProtoVlanId = thedata->lldpXdot1LocProtoVlanId;
		StorageNew->lldpXdot1LocProtoVlanSupported = thedata->lldpXdot1LocProtoVlanSupported;
		StorageNew->lldpXdot1LocProtoVlanEnabled = thedata->lldpXdot1LocProtoVlanEnabled;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1LocProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1LocProtoVlanTable_destroy(struct lldpXdot1LocProtoVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1LocProtoVlanTable_destroy(struct lldpXdot1LocProtoVlanTable_data **thedata)
{
	struct lldpXdot1LocProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtoVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocProtoVlanTable_add(struct lldpXdot1LocProtoVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1LocProtoVlanTable table data set.
 *
 * Adds a table row structure to the lldpXdot1LocProtoVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1LocProtoVlanTable_add(struct lldpXdot1LocProtoVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtoVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXdot1LocProtoVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1LocProtoVlanId, sizeof(thedata->lldpXdot1LocProtoVlanId));
		header_complex_add_data(&lldpXdot1LocProtoVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocProtoVlanTable_del(struct lldpXdot1LocProtoVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1LocProtoVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1LocProtoVlanTable_del(struct lldpXdot1LocProtoVlanTable_data *thedata)
{
	struct lldpXdot1LocProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtoVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1LocProtoVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1LocProtoVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1LocProtoVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1LocProtoVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1LocProtoVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1LocProtoVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1LocProtoVlanTable_data *StorageTmp = lldpXdot1LocProtoVlanTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1LocProtoVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocProtoVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocProtoVlanSupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocProtoVlanEnabled, &tmpsize);
	lldpXdot1LocProtoVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1LocProtoVlanTable(): store configuraiton file for lldpXdot1LocProtoVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1LocProtoVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1LocProtoVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1LocProtoVlanTable: storing data...  "));
	refresh_lldpXdot1LocProtoVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1LocProtoVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1LocProtoVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1LocProtoVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocProtoVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocProtoVlanSupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocProtoVlanEnabled, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1LocVlanNameTable_data *lldpXdot1LocVlanNameTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1LocVlanNameTable table.
 *
 * Creates a new lldpXdot1LocVlanNameTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1LocVlanNameTable_data *
lldpXdot1LocVlanNameTable_create(void)
{
	struct lldpXdot1LocVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocVlanNameTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocVlanNameTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		if ((StorageNew->lldpXdot1LocVlanName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXdot1LocVlanNameLen = 0;
		StorageNew->lldpXdot1LocVlanName[StorageNew->lldpXdot1LocVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1LocVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1LocVlanNameTable_data *lldpXdot1LocVlanNameTable_duplicate(struct lldpXdot1LocVlanNameTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1LocVlanNameTable_data *
lldpXdot1LocVlanNameTable_duplicate(struct lldpXdot1LocVlanNameTable_data *thedata)
{
	struct lldpXdot1LocVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocVlanNameTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocVlanNameTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1LocVlanNameTable_id = thedata->lldpXdot1LocVlanNameTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocVlanId = thedata->lldpXdot1LocVlanId;
		if (!(StorageNew->lldpXdot1LocVlanName = malloc(thedata->lldpXdot1LocVlanNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXdot1LocVlanName, thedata->lldpXdot1LocVlanName, thedata->lldpXdot1LocVlanNameLen);
		StorageNew->lldpXdot1LocVlanNameLen = thedata->lldpXdot1LocVlanNameLen;
		StorageNew->lldpXdot1LocVlanName[StorageNew->lldpXdot1LocVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1LocVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1LocVlanNameTable_destroy(struct lldpXdot1LocVlanNameTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1LocVlanNameTable_destroy(struct lldpXdot1LocVlanNameTable_data **thedata)
{
	struct lldpXdot1LocVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocVlanNameTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXdot1LocVlanName);
		StorageDel->lldpXdot1LocVlanNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocVlanNameTable_add(struct lldpXdot1LocVlanNameTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1LocVlanNameTable table data set.
 *
 * Adds a table row structure to the lldpXdot1LocVlanNameTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1LocVlanNameTable_add(struct lldpXdot1LocVlanNameTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocVlanNameTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXdot1LocVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1LocVlanId, sizeof(thedata->lldpXdot1LocVlanId));
		header_complex_add_data(&lldpXdot1LocVlanNameTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocVlanNameTable_del(struct lldpXdot1LocVlanNameTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1LocVlanNameTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1LocVlanNameTable_del(struct lldpXdot1LocVlanNameTable_data *thedata)
{
	struct lldpXdot1LocVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocVlanNameTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1LocVlanNameTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1LocVlanNameTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1LocVlanNameTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1LocVlanNameTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1LocVlanNameTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1LocVlanNameTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1LocVlanNameTable_data *StorageTmp = lldpXdot1LocVlanNameTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1LocVlanNameTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocVlanId, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXdot1LocVlanName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXdot1LocVlanName, &StorageTmp->lldpXdot1LocVlanNameLen);
	if (StorageTmp->lldpXdot1LocVlanName == NULL) {
		config_perror("invalid specification for lldpXdot1LocVlanName");
		return;
	}
	lldpXdot1LocVlanNameTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1LocVlanNameTable(): store configuraiton file for lldpXdot1LocVlanNameTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1LocVlanNameTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1LocVlanNameTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1LocVlanNameTable: storing data...  "));
	refresh_lldpXdot1LocVlanNameTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1LocVlanNameTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1LocVlanNameTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1LocVlanNameTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXdot1LocVlanName, &StorageTmp->lldpXdot1LocVlanNameLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1LocProtocolTable_data *lldpXdot1LocProtocolTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1LocProtocolTable table.
 *
 * Creates a new lldpXdot1LocProtocolTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1LocProtocolTable_data *
lldpXdot1LocProtocolTable_create(void)
{
	struct lldpXdot1LocProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocProtocolTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtocolTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpLocPortNum = 0;
		if ((StorageNew->lldpXdot1LocProtocolId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXdot1LocProtocolIdLen = 0;
		StorageNew->lldpXdot1LocProtocolId[StorageNew->lldpXdot1LocProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1LocProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1LocProtocolTable_data *lldpXdot1LocProtocolTable_duplicate(struct lldpXdot1LocProtocolTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1LocProtocolTable_data *
lldpXdot1LocProtocolTable_duplicate(struct lldpXdot1LocProtocolTable_data *thedata)
{
	struct lldpXdot1LocProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1LocProtocolTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtocolTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1LocProtocolTable_id = thedata->lldpXdot1LocProtocolTable_id;
		StorageNew->lldpLocPortNum = thedata->lldpLocPortNum;
		StorageNew->lldpXdot1LocProtocolIndex = thedata->lldpXdot1LocProtocolIndex;
		if (!(StorageNew->lldpXdot1LocProtocolId = malloc(thedata->lldpXdot1LocProtocolIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXdot1LocProtocolId, thedata->lldpXdot1LocProtocolId, thedata->lldpXdot1LocProtocolIdLen);
		StorageNew->lldpXdot1LocProtocolIdLen = thedata->lldpXdot1LocProtocolIdLen;
		StorageNew->lldpXdot1LocProtocolId[StorageNew->lldpXdot1LocProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1LocProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1LocProtocolTable_destroy(struct lldpXdot1LocProtocolTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1LocProtocolTable_destroy(struct lldpXdot1LocProtocolTable_data **thedata)
{
	struct lldpXdot1LocProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtocolTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXdot1LocProtocolId);
		StorageDel->lldpXdot1LocProtocolIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocProtocolTable_add(struct lldpXdot1LocProtocolTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1LocProtocolTable table data set.
 *
 * Adds a table row structure to the lldpXdot1LocProtocolTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1LocProtocolTable_add(struct lldpXdot1LocProtocolTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtocolTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpLocPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpLocPortNum, sizeof(thedata->lldpLocPortNum));
		/* lldpXdot1LocProtocolIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1LocProtocolIndex, sizeof(thedata->lldpXdot1LocProtocolIndex));
		header_complex_add_data(&lldpXdot1LocProtocolTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1LocProtocolTable_del(struct lldpXdot1LocProtocolTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1LocProtocolTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1LocProtocolTable_del(struct lldpXdot1LocProtocolTable_data *thedata)
{
	struct lldpXdot1LocProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1LocProtocolTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1LocProtocolTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1LocProtocolTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1LocProtocolTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1LocProtocolTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1LocProtocolTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1LocProtocolTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1LocProtocolTable_data *StorageTmp = lldpXdot1LocProtocolTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1LocProtocolTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpLocPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1LocProtocolIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXdot1LocProtocolId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXdot1LocProtocolId, &StorageTmp->lldpXdot1LocProtocolIdLen);
	if (StorageTmp->lldpXdot1LocProtocolId == NULL) {
		config_perror("invalid specification for lldpXdot1LocProtocolId");
		return;
	}
	lldpXdot1LocProtocolTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1LocProtocolTable(): store configuraiton file for lldpXdot1LocProtocolTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1LocProtocolTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1LocProtocolTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1LocProtocolTable: storing data...  "));
	refresh_lldpXdot1LocProtocolTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1LocProtocolTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1LocProtocolTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1LocProtocolTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpLocPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1LocProtocolIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXdot1LocProtocolId, &StorageTmp->lldpXdot1LocProtocolIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1RemTable_data *lldpXdot1RemTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1RemTable table.
 *
 * Creates a new lldpXdot1RemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1RemTable_data *
lldpXdot1RemTable_create(void)
{
	struct lldpXdot1RemTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		StorageNew->lldpXdot1RemPortVlanId = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1RemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1RemTable_data *lldpXdot1RemTable_duplicate(struct lldpXdot1RemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1RemTable_data *
lldpXdot1RemTable_duplicate(struct lldpXdot1RemTable_data *thedata)
{
	struct lldpXdot1RemTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1RemTable_id = thedata->lldpXdot1RemTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXdot1RemPortVlanId = thedata->lldpXdot1RemPortVlanId;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1RemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1RemTable_destroy(struct lldpXdot1RemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1RemTable_destroy(struct lldpXdot1RemTable_data **thedata)
{
	struct lldpXdot1RemTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemTable_add(struct lldpXdot1RemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1RemTable table data set.
 *
 * Adds a table row structure to the lldpXdot1RemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1RemTable_add(struct lldpXdot1RemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		header_complex_add_data(&lldpXdot1RemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemTable_del(struct lldpXdot1RemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1RemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1RemTable_del(struct lldpXdot1RemTable_data *thedata)
{
	struct lldpXdot1RemTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1RemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1RemTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1RemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1RemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1RemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1RemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1RemTable_data *StorageTmp = lldpXdot1RemTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1RemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1RemPortVlanId, &tmpsize);
	lldpXdot1RemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1RemTable(): store configuraiton file for lldpXdot1RemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1RemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1RemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1RemTable: storing data...  "));
	refresh_lldpXdot1RemTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1RemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1RemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1RemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1RemPortVlanId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1RemProtoVlanTable_data *lldpXdot1RemProtoVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1RemProtoVlanTable table.
 *
 * Creates a new lldpXdot1RemProtoVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1RemProtoVlanTable_data *
lldpXdot1RemProtoVlanTable_create(void)
{
	struct lldpXdot1RemProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemProtoVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtoVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		StorageNew->lldpXdot1RemProtoVlanSupported = 0;
		StorageNew->lldpXdot1RemProtoVlanEnabled = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1RemProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1RemProtoVlanTable_data *lldpXdot1RemProtoVlanTable_duplicate(struct lldpXdot1RemProtoVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1RemProtoVlanTable_data *
lldpXdot1RemProtoVlanTable_duplicate(struct lldpXdot1RemProtoVlanTable_data *thedata)
{
	struct lldpXdot1RemProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemProtoVlanTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtoVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1RemProtoVlanTable_id = thedata->lldpXdot1RemProtoVlanTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXdot1RemProtoVlanId = thedata->lldpXdot1RemProtoVlanId;
		StorageNew->lldpXdot1RemProtoVlanSupported = thedata->lldpXdot1RemProtoVlanSupported;
		StorageNew->lldpXdot1RemProtoVlanEnabled = thedata->lldpXdot1RemProtoVlanEnabled;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1RemProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1RemProtoVlanTable_destroy(struct lldpXdot1RemProtoVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1RemProtoVlanTable_destroy(struct lldpXdot1RemProtoVlanTable_data **thedata)
{
	struct lldpXdot1RemProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtoVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemProtoVlanTable_add(struct lldpXdot1RemProtoVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1RemProtoVlanTable table data set.
 *
 * Adds a table row structure to the lldpXdot1RemProtoVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1RemProtoVlanTable_add(struct lldpXdot1RemProtoVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtoVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		/* lldpXdot1RemProtoVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1RemProtoVlanId, sizeof(thedata->lldpXdot1RemProtoVlanId));
		header_complex_add_data(&lldpXdot1RemProtoVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemProtoVlanTable_del(struct lldpXdot1RemProtoVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1RemProtoVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1RemProtoVlanTable_del(struct lldpXdot1RemProtoVlanTable_data *thedata)
{
	struct lldpXdot1RemProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtoVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1RemProtoVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1RemProtoVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1RemProtoVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1RemProtoVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1RemProtoVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1RemProtoVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1RemProtoVlanTable_data *StorageTmp = lldpXdot1RemProtoVlanTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1RemProtoVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1RemProtoVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1RemProtoVlanSupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1RemProtoVlanEnabled, &tmpsize);
	lldpXdot1RemProtoVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1RemProtoVlanTable(): store configuraiton file for lldpXdot1RemProtoVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1RemProtoVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1RemProtoVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1RemProtoVlanTable: storing data...  "));
	refresh_lldpXdot1RemProtoVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1RemProtoVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1RemProtoVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1RemProtoVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1RemProtoVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1RemProtoVlanSupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1RemProtoVlanEnabled, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1RemVlanNameTable_data *lldpXdot1RemVlanNameTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1RemVlanNameTable table.
 *
 * Creates a new lldpXdot1RemVlanNameTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1RemVlanNameTable_data *
lldpXdot1RemVlanNameTable_create(void)
{
	struct lldpXdot1RemVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemVlanNameTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemVlanNameTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		if ((StorageNew->lldpXdot1RemVlanName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXdot1RemVlanNameLen = 0;
		StorageNew->lldpXdot1RemVlanName[StorageNew->lldpXdot1RemVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1RemVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1RemVlanNameTable_data *lldpXdot1RemVlanNameTable_duplicate(struct lldpXdot1RemVlanNameTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1RemVlanNameTable_data *
lldpXdot1RemVlanNameTable_duplicate(struct lldpXdot1RemVlanNameTable_data *thedata)
{
	struct lldpXdot1RemVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemVlanNameTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemVlanNameTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1RemVlanNameTable_id = thedata->lldpXdot1RemVlanNameTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXdot1RemVlanId = thedata->lldpXdot1RemVlanId;
		if (!(StorageNew->lldpXdot1RemVlanName = malloc(thedata->lldpXdot1RemVlanNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXdot1RemVlanName, thedata->lldpXdot1RemVlanName, thedata->lldpXdot1RemVlanNameLen);
		StorageNew->lldpXdot1RemVlanNameLen = thedata->lldpXdot1RemVlanNameLen;
		StorageNew->lldpXdot1RemVlanName[StorageNew->lldpXdot1RemVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1RemVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1RemVlanNameTable_destroy(struct lldpXdot1RemVlanNameTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1RemVlanNameTable_destroy(struct lldpXdot1RemVlanNameTable_data **thedata)
{
	struct lldpXdot1RemVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemVlanNameTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXdot1RemVlanName);
		StorageDel->lldpXdot1RemVlanNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemVlanNameTable_add(struct lldpXdot1RemVlanNameTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1RemVlanNameTable table data set.
 *
 * Adds a table row structure to the lldpXdot1RemVlanNameTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1RemVlanNameTable_add(struct lldpXdot1RemVlanNameTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemVlanNameTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		/* lldpXdot1RemVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1RemVlanId, sizeof(thedata->lldpXdot1RemVlanId));
		header_complex_add_data(&lldpXdot1RemVlanNameTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemVlanNameTable_del(struct lldpXdot1RemVlanNameTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1RemVlanNameTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1RemVlanNameTable_del(struct lldpXdot1RemVlanNameTable_data *thedata)
{
	struct lldpXdot1RemVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemVlanNameTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1RemVlanNameTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1RemVlanNameTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1RemVlanNameTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1RemVlanNameTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1RemVlanNameTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1RemVlanNameTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1RemVlanNameTable_data *StorageTmp = lldpXdot1RemVlanNameTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1RemVlanNameTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1RemVlanId, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXdot1RemVlanName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXdot1RemVlanName, &StorageTmp->lldpXdot1RemVlanNameLen);
	if (StorageTmp->lldpXdot1RemVlanName == NULL) {
		config_perror("invalid specification for lldpXdot1RemVlanName");
		return;
	}
	lldpXdot1RemVlanNameTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1RemVlanNameTable(): store configuraiton file for lldpXdot1RemVlanNameTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1RemVlanNameTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1RemVlanNameTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1RemVlanNameTable: storing data...  "));
	refresh_lldpXdot1RemVlanNameTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1RemVlanNameTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1RemVlanNameTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1RemVlanNameTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1RemVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXdot1RemVlanName, &StorageTmp->lldpXdot1RemVlanNameLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1RemProtocolTable_data *lldpXdot1RemProtocolTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1RemProtocolTable table.
 *
 * Creates a new lldpXdot1RemProtocolTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1RemProtocolTable_data *
lldpXdot1RemProtocolTable_create(void)
{
	struct lldpXdot1RemProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemProtocolTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtocolTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpRemTimeMark = 0;
		StorageNew->lldpRemLocalPortNum = 0;
		StorageNew->lldpRemIndex = 0;
		if ((StorageNew->lldpXdot1RemProtocolId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpXdot1RemProtocolIdLen = 0;
		StorageNew->lldpXdot1RemProtocolId[StorageNew->lldpXdot1RemProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1RemProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1RemProtocolTable_data *lldpXdot1RemProtocolTable_duplicate(struct lldpXdot1RemProtocolTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1RemProtocolTable_data *
lldpXdot1RemProtocolTable_duplicate(struct lldpXdot1RemProtocolTable_data *thedata)
{
	struct lldpXdot1RemProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1RemProtocolTable_data);

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtocolTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1RemProtocolTable_id = thedata->lldpXdot1RemProtocolTable_id;
		StorageNew->lldpRemTimeMark = thedata->lldpRemTimeMark;
		StorageNew->lldpRemLocalPortNum = thedata->lldpRemLocalPortNum;
		StorageNew->lldpRemIndex = thedata->lldpRemIndex;
		StorageNew->lldpXdot1RemProtocolIndex = thedata->lldpXdot1RemProtocolIndex;
		if (!(StorageNew->lldpXdot1RemProtocolId = malloc(thedata->lldpXdot1RemProtocolIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpXdot1RemProtocolId, thedata->lldpXdot1RemProtocolId, thedata->lldpXdot1RemProtocolIdLen);
		StorageNew->lldpXdot1RemProtocolIdLen = thedata->lldpXdot1RemProtocolIdLen;
		StorageNew->lldpXdot1RemProtocolId[StorageNew->lldpXdot1RemProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1RemProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1RemProtocolTable_destroy(struct lldpXdot1RemProtocolTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1RemProtocolTable_destroy(struct lldpXdot1RemProtocolTable_data **thedata)
{
	struct lldpXdot1RemProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtocolTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpXdot1RemProtocolId);
		StorageDel->lldpXdot1RemProtocolIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemProtocolTable_add(struct lldpXdot1RemProtocolTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1RemProtocolTable table data set.
 *
 * Adds a table row structure to the lldpXdot1RemProtocolTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1RemProtocolTable_add(struct lldpXdot1RemProtocolTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtocolTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpRemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpRemTimeMark, sizeof(thedata->lldpRemTimeMark));
		/* lldpRemLocalPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemLocalPortNum, sizeof(thedata->lldpRemLocalPortNum));
		/* lldpRemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpRemIndex, sizeof(thedata->lldpRemIndex));
		/* lldpXdot1RemProtocolIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpXdot1RemProtocolIndex, sizeof(thedata->lldpXdot1RemProtocolIndex));
		header_complex_add_data(&lldpXdot1RemProtocolTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1RemProtocolTable_del(struct lldpXdot1RemProtocolTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1RemProtocolTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1RemProtocolTable_del(struct lldpXdot1RemProtocolTable_data *thedata)
{
	struct lldpXdot1RemProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1RemProtocolTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1RemProtocolTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1RemProtocolTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1RemProtocolTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1RemProtocolTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1RemProtocolTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1RemProtocolTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1RemProtocolTable_data *StorageTmp = lldpXdot1RemProtocolTable_create();

	DEBUGMSGTL(("lldpXdot1MIB", "parse_lldpXdot1RemProtocolTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpRemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpRemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1RemProtocolIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpXdot1RemProtocolId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpXdot1RemProtocolId, &StorageTmp->lldpXdot1RemProtocolIdLen);
	if (StorageTmp->lldpXdot1RemProtocolId == NULL) {
		config_perror("invalid specification for lldpXdot1RemProtocolId");
		return;
	}
	lldpXdot1RemProtocolTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/*
 * store_lldpXdot1RemProtocolTable(): store configuraiton file for lldpXdot1RemProtocolTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1RemProtocolTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1RemProtocolTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXdot1MIB", "store_lldpXdot1RemProtocolTable: storing data...  "));
	refresh_lldpXdot1RemProtocolTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1RemProtocolTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1RemProtocolTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1RemProtocolTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpRemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemLocalPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpRemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1RemProtocolIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpXdot1RemProtocolId, &StorageTmp->lldpXdot1RemProtocolIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, struct lldpXdot1ConfigPortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, struct lldpXdot1ConfigPortVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, struct lldpXdot1ConfigPortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1ConfigPortVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, struct lldpXdot1ConfigPortVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1ConfigPortVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, struct lldpXdot1ConfigPortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, struct lldpXdot1ConfigPortVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1ConfigPortVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1ConfigPortVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1ConfigPortVlanTable_data *
refresh_lldpXdot1ConfigPortVlanTable_row(struct lldpXdot1ConfigPortVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1ConfigPortVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1ConfigPortVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1ConfigPortVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1ConfigPortVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1ConfigPortVlanTable(int force)
{
	if (!force && lldpXdot1ConfigPortVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1ConfigPortVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1ConfigPortVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1ConfigPortVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1ConfigPortVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1ConfigPortVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1ConfigPortVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1ConfigPortVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1ConfigPortVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1ConfigPortVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1CONFIGPORTVLANTXENABLE:	/* ReadWrite */
		*write_method = write_lldpXdot1ConfigPortVlanTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1ConfigPortVlanTxEnable);
		rval = (u_char *) &StorageTmp->lldpXdot1ConfigPortVlanTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, struct lldpXdot1ConfigVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, struct lldpXdot1ConfigVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, struct lldpXdot1ConfigVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1ConfigVlanNameTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, struct lldpXdot1ConfigVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1ConfigVlanNameTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, struct lldpXdot1ConfigVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, struct lldpXdot1ConfigVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1ConfigVlanNameTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1ConfigVlanNameTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1ConfigVlanNameTable_data *
refresh_lldpXdot1ConfigVlanNameTable_row(struct lldpXdot1ConfigVlanNameTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1ConfigVlanNameTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1ConfigVlanNameTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1ConfigVlanNameTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1ConfigVlanNameTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1ConfigVlanNameTable(int force)
{
	if (!force && lldpXdot1ConfigVlanNameTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1ConfigVlanNameTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1ConfigVlanNameTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1ConfigVlanNameTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1ConfigVlanNameTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1ConfigVlanNameTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1ConfigVlanNameTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1ConfigVlanNameTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1ConfigVlanNameTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1ConfigVlanNameTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1CONFIGVLANNAMETXENABLE:	/* ReadWrite */
		*write_method = write_lldpXdot1ConfigVlanNameTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1ConfigVlanNameTxEnable);
		rval = (u_char *) &StorageTmp->lldpXdot1ConfigVlanNameTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpXdot1ConfigProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpXdot1ConfigProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpXdot1ConfigProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1ConfigProtoVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpXdot1ConfigProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1ConfigProtoVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpXdot1ConfigProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpXdot1ConfigProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1ConfigProtoVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1ConfigProtoVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1ConfigProtoVlanTable_data *
refresh_lldpXdot1ConfigProtoVlanTable_row(struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1ConfigProtoVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1ConfigProtoVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1ConfigProtoVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1ConfigProtoVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1ConfigProtoVlanTable(int force)
{
	if (!force && lldpXdot1ConfigProtoVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1ConfigProtoVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1ConfigProtoVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1ConfigProtoVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1ConfigProtoVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1ConfigProtoVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1ConfigProtoVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1ConfigProtoVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1ConfigProtoVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1CONFIGPROTOVLANTXENABLE:	/* ReadWrite */
		*write_method = write_lldpXdot1ConfigProtoVlanTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1ConfigProtoVlanTxEnable);
		rval = (u_char *) &StorageTmp->lldpXdot1ConfigProtoVlanTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, struct lldpXdot1ConfigProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, struct lldpXdot1ConfigProtocolTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, struct lldpXdot1ConfigProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1ConfigProtocolTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, struct lldpXdot1ConfigProtocolTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1ConfigProtocolTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, struct lldpXdot1ConfigProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, struct lldpXdot1ConfigProtocolTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1ConfigProtocolTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1ConfigProtocolTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1ConfigProtocolTable_data *
refresh_lldpXdot1ConfigProtocolTable_row(struct lldpXdot1ConfigProtocolTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1ConfigProtocolTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1ConfigProtocolTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1ConfigProtocolTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1ConfigProtocolTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1ConfigProtocolTable(int force)
{
	if (!force && lldpXdot1ConfigProtocolTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1ConfigProtocolTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1ConfigProtocolTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1ConfigProtocolTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1ConfigProtocolTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1ConfigProtocolTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1ConfigProtocolTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1ConfigProtocolTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1ConfigProtocolTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1ConfigProtocolTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1CONFIGPROTOCOLTXENABLE:	/* ReadWrite */
		*write_method = write_lldpXdot1ConfigProtocolTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1ConfigProtocolTxEnable);
		rval = (u_char *) &StorageTmp->lldpXdot1ConfigProtocolTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, struct lldpXdot1LocTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, struct lldpXdot1LocTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, struct lldpXdot1LocTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1LocTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, struct lldpXdot1LocTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1LocTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, struct lldpXdot1LocTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, struct lldpXdot1LocTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1LocTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1LocTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1LocTable_data *
refresh_lldpXdot1LocTable_row(struct lldpXdot1LocTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1LocTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1LocTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1LocTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1LocTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1LocTable(int force)
{
	if (!force && lldpXdot1LocTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1LocTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1LocTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1LocTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1LocTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1LocTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1LocTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1LocTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1LocTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1LocTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1LOCPORTVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1LocPortVlanId);
		rval = (u_char *) &StorageTmp->lldpXdot1LocPortVlanId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, struct lldpXdot1LocProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, struct lldpXdot1LocProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, struct lldpXdot1LocProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1LocProtoVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, struct lldpXdot1LocProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1LocProtoVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, struct lldpXdot1LocProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, struct lldpXdot1LocProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1LocProtoVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1LocProtoVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1LocProtoVlanTable_data *
refresh_lldpXdot1LocProtoVlanTable_row(struct lldpXdot1LocProtoVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1LocProtoVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1LocProtoVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1LocProtoVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1LocProtoVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1LocProtoVlanTable(int force)
{
	if (!force && lldpXdot1LocProtoVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1LocProtoVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1LocProtoVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1LocProtoVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1LocProtoVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1LocProtoVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1LocProtoVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1LocProtoVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1LocProtoVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1LocProtoVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1LOCPROTOVLANSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1LocProtoVlanSupported);
		rval = (u_char *) &StorageTmp->lldpXdot1LocProtoVlanSupported;
		break;
	case (u_char) LLDPXDOT1LOCPROTOVLANENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1LocProtoVlanEnabled);
		rval = (u_char *) &StorageTmp->lldpXdot1LocProtoVlanEnabled;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, struct lldpXdot1LocVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, struct lldpXdot1LocVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, struct lldpXdot1LocVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1LocVlanNameTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, struct lldpXdot1LocVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1LocVlanNameTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, struct lldpXdot1LocVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, struct lldpXdot1LocVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1LocVlanNameTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1LocVlanNameTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1LocVlanNameTable_data *
refresh_lldpXdot1LocVlanNameTable_row(struct lldpXdot1LocVlanNameTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1LocVlanNameTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1LocVlanNameTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1LocVlanNameTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1LocVlanNameTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1LocVlanNameTable(int force)
{
	if (!force && lldpXdot1LocVlanNameTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1LocVlanNameTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1LocVlanNameTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1LocVlanNameTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1LocVlanNameTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1LocVlanNameTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1LocVlanNameTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1LocVlanNameTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1LocVlanNameTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1LocVlanNameTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1LOCVLANNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXdot1LocVlanNameLen;
		rval = (u_char *) StorageTmp->lldpXdot1LocVlanName;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, struct lldpXdot1LocProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, struct lldpXdot1LocProtocolTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, struct lldpXdot1LocProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1LocProtocolTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, struct lldpXdot1LocProtocolTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1LocProtocolTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, struct lldpXdot1LocProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, struct lldpXdot1LocProtocolTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1LocProtocolTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1LocProtocolTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1LocProtocolTable_data *
refresh_lldpXdot1LocProtocolTable_row(struct lldpXdot1LocProtocolTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1LocProtocolTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1LocProtocolTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1LocProtocolTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1LocProtocolTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1LocProtocolTable(int force)
{
	if (!force && lldpXdot1LocProtocolTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1LocProtocolTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1LocProtocolTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1LocProtocolTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1LocProtocolTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1LocProtocolTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1LocProtocolTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1LocProtocolTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1LocProtocolTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1LocProtocolTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1LOCPROTOCOLID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXdot1LocProtocolIdLen;
		rval = (u_char *) StorageTmp->lldpXdot1LocProtocolId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, struct lldpXdot1RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, struct lldpXdot1RemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, struct lldpXdot1RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1RemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, struct lldpXdot1RemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1RemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, struct lldpXdot1RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, struct lldpXdot1RemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1RemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1RemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1RemTable_data *
refresh_lldpXdot1RemTable_row(struct lldpXdot1RemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1RemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1RemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1RemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1RemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1RemTable(int force)
{
	if (!force && lldpXdot1RemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1RemTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1RemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1RemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1RemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1RemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1RemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1RemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1RemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1RemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1REMPORTVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1RemPortVlanId);
		rval = (u_char *) &StorageTmp->lldpXdot1RemPortVlanId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, struct lldpXdot1RemProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, struct lldpXdot1RemProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, struct lldpXdot1RemProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1RemProtoVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, struct lldpXdot1RemProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1RemProtoVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, struct lldpXdot1RemProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, struct lldpXdot1RemProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1RemProtoVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1RemProtoVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1RemProtoVlanTable_data *
refresh_lldpXdot1RemProtoVlanTable_row(struct lldpXdot1RemProtoVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1RemProtoVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1RemProtoVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1RemProtoVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1RemProtoVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1RemProtoVlanTable(int force)
{
	if (!force && lldpXdot1RemProtoVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1RemProtoVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1RemProtoVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1RemProtoVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1RemProtoVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1RemProtoVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1RemProtoVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1RemProtoVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1RemProtoVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1RemProtoVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1REMPROTOVLANSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1RemProtoVlanSupported);
		rval = (u_char *) &StorageTmp->lldpXdot1RemProtoVlanSupported;
		break;
	case (u_char) LLDPXDOT1REMPROTOVLANENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1RemProtoVlanEnabled);
		rval = (u_char *) &StorageTmp->lldpXdot1RemProtoVlanEnabled;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, struct lldpXdot1RemVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, struct lldpXdot1RemVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, struct lldpXdot1RemVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1RemVlanNameTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, struct lldpXdot1RemVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1RemVlanNameTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, struct lldpXdot1RemVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, struct lldpXdot1RemVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1RemVlanNameTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1RemVlanNameTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1RemVlanNameTable_data *
refresh_lldpXdot1RemVlanNameTable_row(struct lldpXdot1RemVlanNameTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1RemVlanNameTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1RemVlanNameTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1RemVlanNameTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1RemVlanNameTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1RemVlanNameTable(int force)
{
	if (!force && lldpXdot1RemVlanNameTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1RemVlanNameTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1RemVlanNameTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1RemVlanNameTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1RemVlanNameTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1RemVlanNameTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1RemVlanNameTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1RemVlanNameTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1RemVlanNameTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1RemVlanNameTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1REMVLANNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXdot1RemVlanNameLen;
		rval = (u_char *) StorageTmp->lldpXdot1RemVlanName;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, struct lldpXdot1RemProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, struct lldpXdot1RemProtocolTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, struct lldpXdot1RemProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1RemProtocolTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, struct lldpXdot1RemProtocolTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1RemProtocolTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, struct lldpXdot1RemProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, struct lldpXdot1RemProtocolTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1RemProtocolTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1RemProtocolTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1RemProtocolTable_data *
refresh_lldpXdot1RemProtocolTable_row(struct lldpXdot1RemProtocolTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1RemProtocolTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1RemProtocolTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1RemProtocolTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1RemProtocolTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1RemProtocolTable(int force)
{
	if (!force && lldpXdot1RemProtocolTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1RemProtocolTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1RemProtocolTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1RemProtocolTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXdot1MIB above.
 */
u_char *
var_lldpXdot1RemProtocolTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1RemProtocolTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXdot1MIB", "var_lldpXdot1RemProtocolTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1RemProtocolTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1RemProtocolTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1RemProtocolTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1REMPROTOCOLID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpXdot1RemProtocolIdLen;
		rval = (u_char *) StorageTmp->lldpXdot1RemProtocolId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_lldpXdot1ConfigPortVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXdot1ConfigPortVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXdot1ConfigPortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXdot1MIB", "write_lldpXdot1ConfigPortVlanTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXdot1ConfigPortVlanTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigPortVlanTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigPortVlanTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigPortVlanTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old) == NULL)
			if (StorageTmp->lldpXdot1ConfigPortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old = lldpXdot1ConfigPortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXdot1ConfigPortVlanTable_rsvs++;
		StorageTmp->lldpXdot1ConfigPortVlanTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXdot1ConfigPortVlanTable_tsts == 0)
				if ((ret = check_lldpXdot1ConfigPortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigPortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXdot1ConfigPortVlanTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXdot1ConfigPortVlanTable_sets == 0)
				if ((ret = update_lldpXdot1ConfigPortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigPortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old) != NULL) {
			lldpXdot1ConfigPortVlanTable_destroy(&StorageTmp->lldpXdot1ConfigPortVlanTable_old);
			StorageTmp->lldpXdot1ConfigPortVlanTable_rsvs = 0;
			StorageTmp->lldpXdot1ConfigPortVlanTable_tsts = 0;
			StorageTmp->lldpXdot1ConfigPortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXdot1ConfigPortVlanTable_sets == 0)
			revert_lldpXdot1ConfigPortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigPortVlanTable_old) == NULL)
			break;
		StorageTmp->lldpXdot1ConfigPortVlanTxEnable = StorageOld->lldpXdot1ConfigPortVlanTxEnable;
		if (--StorageTmp->lldpXdot1ConfigPortVlanTable_rsvs == 0)
			lldpXdot1ConfigPortVlanTable_destroy(&StorageTmp->lldpXdot1ConfigPortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXdot1ConfigVlanNameTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXdot1ConfigVlanNameTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXdot1ConfigVlanNameTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXdot1MIB", "write_lldpXdot1ConfigVlanNameTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXdot1ConfigVlanNameTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigVlanNameTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigVlanNameTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigVlanNameTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old) == NULL)
			if (StorageTmp->lldpXdot1ConfigVlanNameTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old = lldpXdot1ConfigVlanNameTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXdot1ConfigVlanNameTable_rsvs++;
		StorageTmp->lldpXdot1ConfigVlanNameTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXdot1ConfigVlanNameTable_tsts == 0)
				if ((ret = check_lldpXdot1ConfigVlanNameTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigVlanNameTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXdot1ConfigVlanNameTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXdot1ConfigVlanNameTable_sets == 0)
				if ((ret = update_lldpXdot1ConfigVlanNameTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigVlanNameTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old) != NULL) {
			lldpXdot1ConfigVlanNameTable_destroy(&StorageTmp->lldpXdot1ConfigVlanNameTable_old);
			StorageTmp->lldpXdot1ConfigVlanNameTable_rsvs = 0;
			StorageTmp->lldpXdot1ConfigVlanNameTable_tsts = 0;
			StorageTmp->lldpXdot1ConfigVlanNameTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXdot1ConfigVlanNameTable_sets == 0)
			revert_lldpXdot1ConfigVlanNameTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigVlanNameTable_old) == NULL)
			break;
		StorageTmp->lldpXdot1ConfigVlanNameTxEnable = StorageOld->lldpXdot1ConfigVlanNameTxEnable;
		if (--StorageTmp->lldpXdot1ConfigVlanNameTable_rsvs == 0)
			lldpXdot1ConfigVlanNameTable_destroy(&StorageTmp->lldpXdot1ConfigVlanNameTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXdot1ConfigProtoVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXdot1ConfigProtoVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXdot1ConfigProtoVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXdot1MIB", "write_lldpXdot1ConfigProtoVlanTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXdot1ConfigProtoVlanTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigProtoVlanTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigProtoVlanTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigProtoVlanTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old) == NULL)
			if (StorageTmp->lldpXdot1ConfigProtoVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old = lldpXdot1ConfigProtoVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXdot1ConfigProtoVlanTable_rsvs++;
		StorageTmp->lldpXdot1ConfigProtoVlanTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXdot1ConfigProtoVlanTable_tsts == 0)
				if ((ret = check_lldpXdot1ConfigProtoVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigProtoVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXdot1ConfigProtoVlanTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXdot1ConfigProtoVlanTable_sets == 0)
				if ((ret = update_lldpXdot1ConfigProtoVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigProtoVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old) != NULL) {
			lldpXdot1ConfigProtoVlanTable_destroy(&StorageTmp->lldpXdot1ConfigProtoVlanTable_old);
			StorageTmp->lldpXdot1ConfigProtoVlanTable_rsvs = 0;
			StorageTmp->lldpXdot1ConfigProtoVlanTable_tsts = 0;
			StorageTmp->lldpXdot1ConfigProtoVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXdot1ConfigProtoVlanTable_sets == 0)
			revert_lldpXdot1ConfigProtoVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtoVlanTable_old) == NULL)
			break;
		StorageTmp->lldpXdot1ConfigProtoVlanTxEnable = StorageOld->lldpXdot1ConfigProtoVlanTxEnable;
		if (--StorageTmp->lldpXdot1ConfigProtoVlanTable_rsvs == 0)
			lldpXdot1ConfigProtoVlanTable_destroy(&StorageTmp->lldpXdot1ConfigProtoVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXdot1ConfigProtocolTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXdot1ConfigProtocolTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXdot1ConfigProtocolTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXdot1MIB", "write_lldpXdot1ConfigProtocolTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXdot1ConfigProtocolTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigProtocolTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigProtocolTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1ConfigProtocolTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old) == NULL)
			if (StorageTmp->lldpXdot1ConfigProtocolTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old = lldpXdot1ConfigProtocolTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXdot1ConfigProtocolTable_rsvs++;
		StorageTmp->lldpXdot1ConfigProtocolTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXdot1ConfigProtocolTable_tsts == 0)
				if ((ret = check_lldpXdot1ConfigProtocolTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigProtocolTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXdot1ConfigProtocolTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXdot1ConfigProtocolTable_sets == 0)
				if ((ret = update_lldpXdot1ConfigProtocolTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1ConfigProtocolTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old) != NULL) {
			lldpXdot1ConfigProtocolTable_destroy(&StorageTmp->lldpXdot1ConfigProtocolTable_old);
			StorageTmp->lldpXdot1ConfigProtocolTable_rsvs = 0;
			StorageTmp->lldpXdot1ConfigProtocolTable_tsts = 0;
			StorageTmp->lldpXdot1ConfigProtocolTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXdot1ConfigProtocolTable_sets == 0)
			revert_lldpXdot1ConfigProtocolTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXdot1ConfigProtocolTable_old) == NULL)
			break;
		StorageTmp->lldpXdot1ConfigProtocolTxEnable = StorageOld->lldpXdot1ConfigProtocolTxEnable;
		if (--StorageTmp->lldpXdot1ConfigProtocolTable_rsvs == 0)
			lldpXdot1ConfigProtocolTable_destroy(&StorageTmp->lldpXdot1ConfigProtocolTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void lldpXdot1MIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
lldpXdot1MIB_loop_handler(int sig)
{
	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1MIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (lldpXdot1MIBold_signal_handler != NULL)
		(*lldpXdot1MIBold_signal_handler) (sig);
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
}

/**
 * @fn void lldpXdot1MIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
lldpXdot1MIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("lldpXdot1MIB", "lldpXdot1MIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("lldpXdot1MIB", "done.\n"));
	return;
}
