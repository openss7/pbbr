/*****************************************************************************

 @(#) src/snmp/ieee8021fqtssmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021fqtssmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021FqtssMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021FqtssMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021FqtssMib_refresh = 1;
volatile int ieee8021FqtssBapTable_refresh = 1;
volatile int ieee8021FqtssTxSelectionAlgorithmTable_refresh = 1;
volatile int ieee8021FqtssSrpRegenOverrideTable_refresh = 1;

/*
 * ieee8021FqtssMib_variables_oid: object identifier for ieee8021FqtssMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021FqtssMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 16 };
oid ieee8021FqtssBapTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 16, 1, 1, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021FqtssCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 16, 2, 1, 1 };
oid ieee8021FqtssBapGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 16, 2, 2, 1 };
oid ieee8021FqtssTxSelectionAlgorithmGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 16, 2, 2, 2 };
oid ieee8021FqtssBoundaryPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 16, 2, 2, 3 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021FqtssMib_variables: tree for ieee8021FqtssMib
 * This variable defines function callbacks and type return information for the ieee8021FqtssMib mib section
 */
struct variable7 ieee8021FqtssMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021FQTSSDELTABANDWIDTH  1
	{(u_char) IEEE8021FQTSSDELTABANDWIDTH, ASN_UNSIGNED, RWRITE, var_ieee8021FqtssBapTable, 5, {1, 1, 1, 1, 2}},
#define   IEEE8021FQTSSOPERIDLESLOPEMS  2
	{(u_char) IEEE8021FQTSSOPERIDLESLOPEMS, ASN_UNSIGNED, RONLY, var_ieee8021FqtssBapTable, 5, {1, 1, 1, 1, 3}},
#define   IEEE8021FQTSSOPERIDLESLOPELS  3
	{(u_char) IEEE8021FQTSSOPERIDLESLOPELS, ASN_UNSIGNED, RONLY, var_ieee8021FqtssBapTable, 5, {1, 1, 1, 1, 4}},
#define   IEEE8021FQTSSADMINIDLESLOPEMS  4
	{(u_char) IEEE8021FQTSSADMINIDLESLOPEMS, ASN_UNSIGNED, RWRITE, var_ieee8021FqtssBapTable, 5, {1, 1, 1, 1, 5}},
#define   IEEE8021FQTSSADMINIDLESLOPELS  5
	{(u_char) IEEE8021FQTSSADMINIDLESLOPELS, ASN_UNSIGNED, RWRITE, var_ieee8021FqtssBapTable, 5, {1, 1, 1, 1, 6}},
#define   IEEE8021FQTSSBAPROWSTATUS  6
	{(u_char) IEEE8021FQTSSBAPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021FqtssBapTable, 5, {1, 1, 1, 1, 7}},
#define   IEEE8021FQTSSTXSELECTIONALGORITHMID  7
	{(u_char) IEEE8021FQTSSTXSELECTIONALGORITHMID, ASN_UNSIGNED, RWRITE, var_ieee8021FqtssTxSelectionAlgorithmTable, 5, {1, 2, 1, 1, 2}},
#define   IEEE8021FQTSSPRIORITYREGENOVERRIDE  8
	{(u_char) IEEE8021FQTSSPRIORITYREGENOVERRIDE, ASN_UNSIGNED, RWRITE, var_ieee8021FqtssSrpRegenOverrideTable, 5, {1, 2, 2, 1, 2}},
#define   IEEE8021FQTSSSRPBOUNDARYPORT  9
	{(u_char) IEEE8021FQTSSSRPBOUNDARYPORT, ASN_INTEGER, RONLY, var_ieee8021FqtssSrpRegenOverrideTable, 5, {1, 2, 2, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct ieee8021FqtssMib_data *ieee8021FqtssMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021FqtssBapTableStorage = NULL;
struct header_complex_index *ieee8021FqtssTxSelectionAlgorithmTableStorage = NULL;
struct header_complex_index *ieee8021FqtssSrpRegenOverrideTableStorage = NULL;

void (*ieee8021FqtssMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021FqtssMib_loop_handler(int);
void ieee8021FqtssMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021FqtssMib(void)
 * @brief ieee8021FqtssMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021FqtssMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021FqtssMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021FqtssMib", "init_ieee8021FqtssMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021FqtssMib", ieee8021FqtssMib_variables, variable7, ieee8021FqtssMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021FqtssMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021FqtssMib", parse_ieee8021FqtssMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021FqtssBapTable", parse_ieee8021FqtssBapTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021FqtssTxSelectionAlgorithmTable", parse_ieee8021FqtssTxSelectionAlgorithmTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021FqtssSrpRegenOverrideTable", parse_ieee8021FqtssSrpRegenOverrideTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021FqtssMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021FqtssBapTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021FqtssTxSelectionAlgorithmTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021FqtssSrpRegenOverrideTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021FqtssMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021FqtssMib_fd_handler, (void *) 1);
	}
	ieee8021FqtssMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021FqtssMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021FqtssMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021FqtssMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021FqtssMib(void)
{
	DEBUGMSGTL(("ieee8021FqtssMib", "deinit_ieee8021FqtssMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021FqtssMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021FqtssMib_variables_oid, sizeof(ieee8021FqtssMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021FqtssMib");
	snmpd_unregister_config_handler("ieee8021FqtssBapTable");
	snmpd_unregister_config_handler("ieee8021FqtssTxSelectionAlgorithmTable");
	snmpd_unregister_config_handler("ieee8021FqtssSrpRegenOverrideTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

int
term_ieee8021FqtssMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021FqtssMib", "term_ieee8021FqtssMib: terminating...  "));
	deinit_ieee8021FqtssMib();
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021FqtssMib_data *ieee8021FqtssMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021FqtssMib.
 *
 * Creates a new ieee8021FqtssMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021FqtssMib.
 */
struct ieee8021FqtssMib_data *
ieee8021FqtssMib_create(void)
{
	struct ieee8021FqtssMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssMib_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021FqtssMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021FqtssMib_data *ieee8021FqtssMib_duplicate(struct ieee8021FqtssMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021FqtssMib_data *
ieee8021FqtssMib_duplicate(struct ieee8021FqtssMib_data *thedata)
{
	struct ieee8021FqtssMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssMib_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021FqtssMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021FqtssMib_destroy(struct ieee8021FqtssMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021FqtssMib.
 * @brief delete a scalars structure from ieee8021FqtssMib.
 *
 * Frees scalars that were previously removed from ieee8021FqtssMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021FqtssMib_destroy(struct ieee8021FqtssMib_data **thedata)
{
	struct ieee8021FqtssMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssMib_add(struct ieee8021FqtssMib_data *thedata)
 * @param thedata the structure representing ieee8021FqtssMib scalars.
 * @brief adds node to the ieee8021FqtssMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021FqtssMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021FqtssMib_add(struct ieee8021FqtssMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssMib_add: adding data...  "));
	if (thedata)
		ieee8021FqtssMibStorage = thedata;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021FqtssMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021FqtssMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021FqtssMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021FqtssMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021FqtssMib_data *StorageTmp = ieee8021FqtssMib_create();

	DEBUGMSGTL(("ieee8021FqtssMib", "parse_ieee8021FqtssMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021FqtssMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/*
 * store_ieee8021FqtssMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021FqtssMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021FqtssMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021FqtssMib", "store_ieee8021FqtssMib: storing data...  "));
	refresh_ieee8021FqtssMib(1);
	if ((StorageTmp = ieee8021FqtssMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021FqtssMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021FqtssMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021FqtssMib(struct ieee8021FqtssMib_data *StorageTmp, struct ieee8021FqtssMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021FqtssMib(struct ieee8021FqtssMib_data *StorageTmp, struct ieee8021FqtssMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021FqtssMib(struct ieee8021FqtssMib_data *StorageTmp, struct ieee8021FqtssMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021FqtssMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021FqtssMib(struct ieee8021FqtssMib_data *StorageTmp, struct ieee8021FqtssMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021FqtssMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021FqtssMib(struct 
 * @fn void revert_ieee8021FqtssMib(struct ieee8021FqtssMib_data *StorageTmp, struct ieee8021FqtssMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021FqtssMib(struct ieee8021FqtssMib_data *StorageTmp, struct ieee8021FqtssMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021FqtssMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021FqtssMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021FqtssMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021FqtssMib(int force)
{
	if (ieee8021FqtssMibStorage == NULL) {
		struct ieee8021FqtssMib_data *StorageNew;

		if ((StorageNew = ieee8021FqtssMib_create()) == NULL)
			return;
		ieee8021FqtssMibStorage = StorageNew;
		ieee8021FqtssMib_refresh = 1;
	}
	if (!force && ieee8021FqtssMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021FqtssMib", "refresh_ieee8021FqtssMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021FqtssMib_refresh = 0;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021FqtssMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021FqtssMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021FqtssMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021FqtssMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021FqtssMib", "var_ieee8021FqtssMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021FqtssMib(0);
	if ((StorageTmp = ieee8021FqtssMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021FqtssMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021FqtssMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021FqtssMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021FqtssBapTable_data *ieee8021FqtssBapTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021FqtssBapTable table.
 *
 * Creates a new ieee8021FqtssBapTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021FqtssBapTable_data *
ieee8021FqtssBapTable_create(void)
{
	struct ieee8021FqtssBapTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssBapTable_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssBapTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021FqtssDeltaBandwidth = 0;
		StorageNew->ieee8021FqtssOperIdleSlopeMs = 0;
		StorageNew->ieee8021FqtssOperIdleSlopeLs = 0;
		StorageNew->ieee8021FqtssAdminIdleSlopeMs = 0;
		StorageNew->ieee8021FqtssAdminIdleSlopeLs = 0;
		StorageNew->ieee8021FqtssBapRowStatus = 0;
		StorageNew->ieee8021FqtssBapRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021FqtssBapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021FqtssBapTable_data *ieee8021FqtssBapTable_duplicate(struct ieee8021FqtssBapTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021FqtssBapTable_data *
ieee8021FqtssBapTable_duplicate(struct ieee8021FqtssBapTable_data *thedata)
{
	struct ieee8021FqtssBapTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssBapTable_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssBapTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021FqtssBapTable_id = thedata->ieee8021FqtssBapTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021FqtssBAPTrafficClass = thedata->ieee8021FqtssBAPTrafficClass;
		StorageNew->ieee8021FqtssDeltaBandwidth = thedata->ieee8021FqtssDeltaBandwidth;
		StorageNew->ieee8021FqtssOperIdleSlopeMs = thedata->ieee8021FqtssOperIdleSlopeMs;
		StorageNew->ieee8021FqtssOperIdleSlopeLs = thedata->ieee8021FqtssOperIdleSlopeLs;
		StorageNew->ieee8021FqtssAdminIdleSlopeMs = thedata->ieee8021FqtssAdminIdleSlopeMs;
		StorageNew->ieee8021FqtssAdminIdleSlopeLs = thedata->ieee8021FqtssAdminIdleSlopeLs;
		StorageNew->ieee8021FqtssBapRowStatus = thedata->ieee8021FqtssBapRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021FqtssBapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021FqtssBapTable_destroy(struct ieee8021FqtssBapTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021FqtssBapTable_destroy(struct ieee8021FqtssBapTable_data **thedata)
{
	struct ieee8021FqtssBapTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssBapTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssBapTable_add(struct ieee8021FqtssBapTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021FqtssBapTable table data set.
 *
 * Adds a table row structure to the ieee8021FqtssBapTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021FqtssBapTable_add(struct ieee8021FqtssBapTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssBapTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021FqtssBAPTrafficClass */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021FqtssBAPTrafficClass, sizeof(thedata->ieee8021FqtssBAPTrafficClass));
		header_complex_add_data(&ieee8021FqtssBapTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssBapTable_del(struct ieee8021FqtssBapTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021FqtssBapTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021FqtssBapTable_del(struct ieee8021FqtssBapTable_data *thedata)
{
	struct ieee8021FqtssBapTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssBapTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021FqtssBapTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021FqtssBapTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021FqtssBapTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021FqtssBapTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021FqtssBapTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021FqtssBapTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021FqtssBapTable_data *StorageTmp = ieee8021FqtssBapTable_create();

	DEBUGMSGTL(("ieee8021FqtssMib", "parse_ieee8021FqtssBapTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssBAPTrafficClass, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssDeltaBandwidth, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssOperIdleSlopeMs, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssOperIdleSlopeLs, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssAdminIdleSlopeMs, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssAdminIdleSlopeLs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021FqtssBapRowStatus, &tmpsize);
	ieee8021FqtssBapTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/*
 * store_ieee8021FqtssBapTable(): store configuraiton file for ieee8021FqtssBapTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021FqtssBapTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021FqtssBapTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021FqtssMib", "store_ieee8021FqtssBapTable: storing data...  "));
	refresh_ieee8021FqtssBapTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021FqtssBapTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021FqtssBapTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021FqtssBapTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssBAPTrafficClass, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssDeltaBandwidth, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssOperIdleSlopeMs, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssOperIdleSlopeLs, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssAdminIdleSlopeMs, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssAdminIdleSlopeLs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021FqtssBapRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021FqtssTxSelectionAlgorithmTable_data *ieee8021FqtssTxSelectionAlgorithmTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021FqtssTxSelectionAlgorithmTable table.
 *
 * Creates a new ieee8021FqtssTxSelectionAlgorithmTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021FqtssTxSelectionAlgorithmTable_data *
ieee8021FqtssTxSelectionAlgorithmTable_create(void)
{
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssTxSelectionAlgorithmTable_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssTxSelectionAlgorithmTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021FqtssTxSelectionAlgorithmID = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021FqtssTxSelectionAlgorithmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021FqtssTxSelectionAlgorithmTable_data *ieee8021FqtssTxSelectionAlgorithmTable_duplicate(struct ieee8021FqtssTxSelectionAlgorithmTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021FqtssTxSelectionAlgorithmTable_data *
ieee8021FqtssTxSelectionAlgorithmTable_duplicate(struct ieee8021FqtssTxSelectionAlgorithmTable_data *thedata)
{
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssTxSelectionAlgorithmTable_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssTxSelectionAlgorithmTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021FqtssTxSelectionAlgorithmTable_id = thedata->ieee8021FqtssTxSelectionAlgorithmTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021FqtssTrafficClass = thedata->ieee8021FqtssTrafficClass;
		StorageNew->ieee8021FqtssTxSelectionAlgorithmID = thedata->ieee8021FqtssTxSelectionAlgorithmID;
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021FqtssTxSelectionAlgorithmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021FqtssTxSelectionAlgorithmTable_destroy(struct ieee8021FqtssTxSelectionAlgorithmTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021FqtssTxSelectionAlgorithmTable_destroy(struct ieee8021FqtssTxSelectionAlgorithmTable_data **thedata)
{
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssTxSelectionAlgorithmTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssTxSelectionAlgorithmTable_add(struct ieee8021FqtssTxSelectionAlgorithmTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021FqtssTxSelectionAlgorithmTable table data set.
 *
 * Adds a table row structure to the ieee8021FqtssTxSelectionAlgorithmTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021FqtssTxSelectionAlgorithmTable_add(struct ieee8021FqtssTxSelectionAlgorithmTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssTxSelectionAlgorithmTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021FqtssTrafficClass */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021FqtssTrafficClass, sizeof(thedata->ieee8021FqtssTrafficClass));
		header_complex_add_data(&ieee8021FqtssTxSelectionAlgorithmTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssTxSelectionAlgorithmTable_del(struct ieee8021FqtssTxSelectionAlgorithmTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021FqtssTxSelectionAlgorithmTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021FqtssTxSelectionAlgorithmTable_del(struct ieee8021FqtssTxSelectionAlgorithmTable_data *thedata)
{
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssTxSelectionAlgorithmTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021FqtssTxSelectionAlgorithmTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021FqtssTxSelectionAlgorithmTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021FqtssTxSelectionAlgorithmTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021FqtssTxSelectionAlgorithmTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021FqtssTxSelectionAlgorithmTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021FqtssTxSelectionAlgorithmTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp = ieee8021FqtssTxSelectionAlgorithmTable_create();

	DEBUGMSGTL(("ieee8021FqtssMib", "parse_ieee8021FqtssTxSelectionAlgorithmTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssTrafficClass, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssTxSelectionAlgorithmID, &tmpsize);
	ieee8021FqtssTxSelectionAlgorithmTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/*
 * store_ieee8021FqtssTxSelectionAlgorithmTable(): store configuraiton file for ieee8021FqtssTxSelectionAlgorithmTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021FqtssTxSelectionAlgorithmTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021FqtssMib", "store_ieee8021FqtssTxSelectionAlgorithmTable: storing data...  "));
	refresh_ieee8021FqtssTxSelectionAlgorithmTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021FqtssTxSelectionAlgorithmTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021FqtssTxSelectionAlgorithmTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021FqtssTxSelectionAlgorithmTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssTrafficClass, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssTxSelectionAlgorithmID, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021FqtssSrpRegenOverrideTable_data *ieee8021FqtssSrpRegenOverrideTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021FqtssSrpRegenOverrideTable table.
 *
 * Creates a new ieee8021FqtssSrpRegenOverrideTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021FqtssSrpRegenOverrideTable_data *
ieee8021FqtssSrpRegenOverrideTable_create(void)
{
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssSrpRegenOverrideTable_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssSrpRegenOverrideTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021FqtssPriorityRegenOverride = 0;
		StorageNew->ieee8021FqtssSrpBoundaryPort = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021FqtssSrpRegenOverrideTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021FqtssSrpRegenOverrideTable_data *ieee8021FqtssSrpRegenOverrideTable_duplicate(struct ieee8021FqtssSrpRegenOverrideTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021FqtssSrpRegenOverrideTable_data *
ieee8021FqtssSrpRegenOverrideTable_duplicate(struct ieee8021FqtssSrpRegenOverrideTable_data *thedata)
{
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021FqtssSrpRegenOverrideTable_data);

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssSrpRegenOverrideTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021FqtssSrpRegenOverrideTable_id = thedata->ieee8021FqtssSrpRegenOverrideTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021FqtssSrClassPriority = thedata->ieee8021FqtssSrClassPriority;
		StorageNew->ieee8021FqtssPriorityRegenOverride = thedata->ieee8021FqtssPriorityRegenOverride;
		StorageNew->ieee8021FqtssSrpBoundaryPort = thedata->ieee8021FqtssSrpBoundaryPort;
	}
      done:
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021FqtssSrpRegenOverrideTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021FqtssSrpRegenOverrideTable_destroy(struct ieee8021FqtssSrpRegenOverrideTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021FqtssSrpRegenOverrideTable_destroy(struct ieee8021FqtssSrpRegenOverrideTable_data **thedata)
{
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssSrpRegenOverrideTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssSrpRegenOverrideTable_add(struct ieee8021FqtssSrpRegenOverrideTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021FqtssSrpRegenOverrideTable table data set.
 *
 * Adds a table row structure to the ieee8021FqtssSrpRegenOverrideTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021FqtssSrpRegenOverrideTable_add(struct ieee8021FqtssSrpRegenOverrideTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssSrpRegenOverrideTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021FqtssSrClassPriority */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021FqtssSrClassPriority, sizeof(thedata->ieee8021FqtssSrClassPriority));
		header_complex_add_data(&ieee8021FqtssSrpRegenOverrideTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021FqtssSrpRegenOverrideTable_del(struct ieee8021FqtssSrpRegenOverrideTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021FqtssSrpRegenOverrideTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021FqtssSrpRegenOverrideTable_del(struct ieee8021FqtssSrpRegenOverrideTable_data *thedata)
{
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssSrpRegenOverrideTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021FqtssSrpRegenOverrideTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021FqtssSrpRegenOverrideTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021FqtssSrpRegenOverrideTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021FqtssSrpRegenOverrideTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021FqtssSrpRegenOverrideTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021FqtssSrpRegenOverrideTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp = ieee8021FqtssSrpRegenOverrideTable_create();

	DEBUGMSGTL(("ieee8021FqtssMib", "parse_ieee8021FqtssSrpRegenOverrideTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssSrClassPriority, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021FqtssPriorityRegenOverride, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021FqtssSrpBoundaryPort, &tmpsize);
	ieee8021FqtssSrpRegenOverrideTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/*
 * store_ieee8021FqtssSrpRegenOverrideTable(): store configuraiton file for ieee8021FqtssSrpRegenOverrideTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021FqtssSrpRegenOverrideTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021FqtssMib", "store_ieee8021FqtssSrpRegenOverrideTable: storing data...  "));
	refresh_ieee8021FqtssSrpRegenOverrideTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021FqtssSrpRegenOverrideTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021FqtssSrpRegenOverrideTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021FqtssSrpRegenOverrideTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssSrClassPriority, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021FqtssPriorityRegenOverride, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021FqtssSrpBoundaryPort, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, struct ieee8021FqtssBapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, struct ieee8021FqtssBapTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, struct ieee8021FqtssBapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021FqtssBapTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, struct ieee8021FqtssBapTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021FqtssBapTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, struct ieee8021FqtssBapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, struct ieee8021FqtssBapTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021FqtssBapTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021FqtssBapTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021FqtssBapTable_data *
refresh_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021FqtssBapTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021FqtssBapTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021FqtssBapTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021FqtssBapTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021FqtssBapTable(int force)
{
	if (!force && ieee8021FqtssBapTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021FqtssBapTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021FqtssBapTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021FqtssBapTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021FqtssMib above.
 */
u_char *
var_ieee8021FqtssBapTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021FqtssBapTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021FqtssMib", "var_ieee8021FqtssBapTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021FqtssBapTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021FqtssBapTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021FqtssBapTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021FQTSSDELTABANDWIDTH:	/* Create */
		*write_method = write_ieee8021FqtssDeltaBandwidth;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssDeltaBandwidth);
		rval = (u_char *) &StorageTmp->ieee8021FqtssDeltaBandwidth;
		break;
	case (u_char) IEEE8021FQTSSOPERIDLESLOPEMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssOperIdleSlopeMs);
		rval = (u_char *) &StorageTmp->ieee8021FqtssOperIdleSlopeMs;
		break;
	case (u_char) IEEE8021FQTSSOPERIDLESLOPELS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssOperIdleSlopeLs);
		rval = (u_char *) &StorageTmp->ieee8021FqtssOperIdleSlopeLs;
		break;
	case (u_char) IEEE8021FQTSSADMINIDLESLOPEMS:	/* Create */
		*write_method = write_ieee8021FqtssAdminIdleSlopeMs;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssAdminIdleSlopeMs);
		rval = (u_char *) &StorageTmp->ieee8021FqtssAdminIdleSlopeMs;
		break;
	case (u_char) IEEE8021FQTSSADMINIDLESLOPELS:	/* Create */
		*write_method = write_ieee8021FqtssAdminIdleSlopeLs;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssAdminIdleSlopeLs);
		rval = (u_char *) &StorageTmp->ieee8021FqtssAdminIdleSlopeLs;
		break;
	case (u_char) IEEE8021FQTSSBAPROWSTATUS:	/* Create */
		*write_method = write_ieee8021FqtssBapRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssBapRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021FqtssBapRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021FqtssTxSelectionAlgorithmTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021FqtssTxSelectionAlgorithmTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021FqtssTxSelectionAlgorithmTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021FqtssTxSelectionAlgorithmTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021FqtssTxSelectionAlgorithmTable_data *
refresh_ieee8021FqtssTxSelectionAlgorithmTable_row(struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021FqtssTxSelectionAlgorithmTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021FqtssTxSelectionAlgorithmTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021FqtssTxSelectionAlgorithmTable(int force)
{
	if (!force && ieee8021FqtssTxSelectionAlgorithmTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021FqtssTxSelectionAlgorithmTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021FqtssTxSelectionAlgorithmTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021FqtssTxSelectionAlgorithmTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021FqtssMib above.
 */
u_char *
var_ieee8021FqtssTxSelectionAlgorithmTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021FqtssMib", "var_ieee8021FqtssTxSelectionAlgorithmTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021FqtssTxSelectionAlgorithmTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021FqtssTxSelectionAlgorithmTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021FqtssTxSelectionAlgorithmTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021FQTSSTXSELECTIONALGORITHMID:	/* ReadWrite */
		*write_method = write_ieee8021FqtssTxSelectionAlgorithmID;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssTxSelectionAlgorithmID);
		rval = (u_char *) &StorageTmp->ieee8021FqtssTxSelectionAlgorithmID;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, struct ieee8021FqtssSrpRegenOverrideTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, struct ieee8021FqtssSrpRegenOverrideTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, struct ieee8021FqtssSrpRegenOverrideTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021FqtssSrpRegenOverrideTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, struct ieee8021FqtssSrpRegenOverrideTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021FqtssSrpRegenOverrideTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, struct ieee8021FqtssSrpRegenOverrideTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, struct ieee8021FqtssSrpRegenOverrideTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021FqtssSrpRegenOverrideTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021FqtssSrpRegenOverrideTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021FqtssSrpRegenOverrideTable_data *
refresh_ieee8021FqtssSrpRegenOverrideTable_row(struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021FqtssSrpRegenOverrideTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021FqtssSrpRegenOverrideTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021FqtssSrpRegenOverrideTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021FqtssSrpRegenOverrideTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021FqtssSrpRegenOverrideTable(int force)
{
	if (!force && ieee8021FqtssSrpRegenOverrideTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021FqtssSrpRegenOverrideTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021FqtssSrpRegenOverrideTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021FqtssSrpRegenOverrideTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021FqtssMib above.
 */
u_char *
var_ieee8021FqtssSrpRegenOverrideTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021FqtssMib", "var_ieee8021FqtssSrpRegenOverrideTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021FqtssSrpRegenOverrideTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021FqtssSrpRegenOverrideTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021FqtssSrpRegenOverrideTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021FQTSSPRIORITYREGENOVERRIDE:	/* ReadWrite */
		*write_method = write_ieee8021FqtssPriorityRegenOverride;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssPriorityRegenOverride);
		rval = (u_char *) &StorageTmp->ieee8021FqtssPriorityRegenOverride;
		break;
	case (u_char) IEEE8021FQTSSSRPBOUNDARYPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021FqtssSrpBoundaryPort);
		rval = (u_char *) &StorageTmp->ieee8021FqtssSrpBoundaryPort;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021FqtssDeltaBandwidth(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021FqtssDeltaBandwidth(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021FqtssBapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021FqtssMib", "write_ieee8021FqtssDeltaBandwidth entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021FqtssBapTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021FqtssBapRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssDeltaBandwidth: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssDeltaBandwidth not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssDeltaBandwidth: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..100000000 */
		if ((0 > set_value || set_value > 100000000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssDeltaBandwidth: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			if (StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old = ieee8021FqtssBapTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021FqtssBapTable_rsvs++;
		StorageTmp->ieee8021FqtssDeltaBandwidth = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021FqtssBapTable_tsts == 0)
				if ((ret = check_ieee8021FqtssBapTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssBapTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021FqtssDeltaBandwidth for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021FqtssBapTable_sets == 0)
				if ((ret = update_ieee8021FqtssBapTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssBapTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
			StorageTmp->ieee8021FqtssBapTable_rsvs = 0;
			StorageTmp->ieee8021FqtssBapTable_tsts = 0;
			StorageTmp->ieee8021FqtssBapTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021FqtssBapTable_sets == 0)
			revert_ieee8021FqtssBapTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			break;
		StorageTmp->ieee8021FqtssDeltaBandwidth = StorageOld->ieee8021FqtssDeltaBandwidth;
		if (--StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
			ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021FqtssAdminIdleSlopeMs(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021FqtssAdminIdleSlopeMs(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021FqtssBapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021FqtssMib", "write_ieee8021FqtssAdminIdleSlopeMs entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021FqtssBapTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021FqtssBapRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssAdminIdleSlopeMs: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssAdminIdleSlopeMs not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssAdminIdleSlopeMs: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			if (StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old = ieee8021FqtssBapTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021FqtssBapTable_rsvs++;
		StorageTmp->ieee8021FqtssAdminIdleSlopeMs = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021FqtssBapTable_tsts == 0)
				if ((ret = check_ieee8021FqtssBapTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssBapTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021FqtssAdminIdleSlopeMs for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021FqtssBapTable_sets == 0)
				if ((ret = update_ieee8021FqtssBapTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssBapTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
			StorageTmp->ieee8021FqtssBapTable_rsvs = 0;
			StorageTmp->ieee8021FqtssBapTable_tsts = 0;
			StorageTmp->ieee8021FqtssBapTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021FqtssBapTable_sets == 0)
			revert_ieee8021FqtssBapTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			break;
		StorageTmp->ieee8021FqtssAdminIdleSlopeMs = StorageOld->ieee8021FqtssAdminIdleSlopeMs;
		if (--StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
			ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021FqtssAdminIdleSlopeLs(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021FqtssAdminIdleSlopeLs(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021FqtssBapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021FqtssMib", "write_ieee8021FqtssAdminIdleSlopeLs entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021FqtssBapTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021FqtssBapRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssAdminIdleSlopeLs: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssAdminIdleSlopeLs not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssAdminIdleSlopeLs: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			if (StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old = ieee8021FqtssBapTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021FqtssBapTable_rsvs++;
		StorageTmp->ieee8021FqtssAdminIdleSlopeLs = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021FqtssBapTable_tsts == 0)
				if ((ret = check_ieee8021FqtssBapTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssBapTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021FqtssAdminIdleSlopeLs for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021FqtssBapTable_sets == 0)
				if ((ret = update_ieee8021FqtssBapTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssBapTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
			ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
			StorageTmp->ieee8021FqtssBapTable_rsvs = 0;
			StorageTmp->ieee8021FqtssBapTable_tsts = 0;
			StorageTmp->ieee8021FqtssBapTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021FqtssBapTable_sets == 0)
			revert_ieee8021FqtssBapTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
			break;
		StorageTmp->ieee8021FqtssAdminIdleSlopeLs = StorageOld->ieee8021FqtssAdminIdleSlopeLs;
		if (--StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
			ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021FqtssTxSelectionAlgorithmID(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021FqtssTxSelectionAlgorithmID(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021FqtssTxSelectionAlgorithmTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021FqtssMib", "write_ieee8021FqtssTxSelectionAlgorithmID entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021FqtssTxSelectionAlgorithmTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssTxSelectionAlgorithmID not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssTxSelectionAlgorithmID: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old) == NULL)
			if (StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old = ieee8021FqtssTxSelectionAlgorithmTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_rsvs++;
		StorageTmp->ieee8021FqtssTxSelectionAlgorithmID = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_tsts == 0)
				if ((ret = check_ieee8021FqtssTxSelectionAlgorithmTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021FqtssTxSelectionAlgorithmID for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_sets == 0)
				if ((ret = update_ieee8021FqtssTxSelectionAlgorithmTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old) != NULL) {
			ieee8021FqtssTxSelectionAlgorithmTable_destroy(&StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old);
			StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_rsvs = 0;
			StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_tsts = 0;
			StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_sets == 0)
			revert_ieee8021FqtssTxSelectionAlgorithmTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old) == NULL)
			break;
		StorageTmp->ieee8021FqtssTxSelectionAlgorithmID = StorageOld->ieee8021FqtssTxSelectionAlgorithmID;
		if (--StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_rsvs == 0)
			ieee8021FqtssTxSelectionAlgorithmTable_destroy(&StorageTmp->ieee8021FqtssTxSelectionAlgorithmTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021FqtssPriorityRegenOverride(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021FqtssPriorityRegenOverride(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021FqtssSrpRegenOverrideTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021FqtssMib", "write_ieee8021FqtssPriorityRegenOverride entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021FqtssSrpRegenOverrideTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssPriorityRegenOverride not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssPriorityRegenOverride: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssPriorityRegenOverride: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old) == NULL)
			if (StorageTmp->ieee8021FqtssSrpRegenOverrideTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old = ieee8021FqtssSrpRegenOverrideTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021FqtssSrpRegenOverrideTable_rsvs++;
		StorageTmp->ieee8021FqtssPriorityRegenOverride = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021FqtssSrpRegenOverrideTable_tsts == 0)
				if ((ret = check_ieee8021FqtssSrpRegenOverrideTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssSrpRegenOverrideTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021FqtssPriorityRegenOverride for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021FqtssSrpRegenOverrideTable_sets == 0)
				if ((ret = update_ieee8021FqtssSrpRegenOverrideTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021FqtssSrpRegenOverrideTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old) != NULL) {
			ieee8021FqtssSrpRegenOverrideTable_destroy(&StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old);
			StorageTmp->ieee8021FqtssSrpRegenOverrideTable_rsvs = 0;
			StorageTmp->ieee8021FqtssSrpRegenOverrideTable_tsts = 0;
			StorageTmp->ieee8021FqtssSrpRegenOverrideTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021FqtssSrpRegenOverrideTable_sets == 0)
			revert_ieee8021FqtssSrpRegenOverrideTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old) == NULL)
			break;
		StorageTmp->ieee8021FqtssPriorityRegenOverride = StorageOld->ieee8021FqtssPriorityRegenOverride;
		if (--StorageTmp->ieee8021FqtssSrpRegenOverrideTable_rsvs == 0)
			ieee8021FqtssSrpRegenOverrideTable_destroy(&StorageTmp->ieee8021FqtssSrpRegenOverrideTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021FqtssBapTable_row(struct ieee8021FqtssBapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021FqtssBapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021FqtssBapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021FqtssBapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021FqtssBapTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021FqtssMib", "write_ieee8021FqtssBapRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021FqtssBapTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssBapRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021FqtssBapRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBaseComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021FqtssBAPTrafficClass */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBaseComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBaseComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBaseComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021FqtssBAPTrafficClass */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021FqtssBAPTrafficClass: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..7 */
			if ((0 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 7)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021FqtssBAPTrafficClass: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021FqtssBapTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021FqtssBapTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBaseComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021FqtssBAPTrafficClass = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021FqtssBapTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021FqtssBapRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021FqtssBapTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
				if (StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old = ieee8021FqtssBapTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021FqtssBapTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021FqtssBapTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021FqtssBapTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021FqtssBapTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021FqtssBapRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021FqtssBapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021FqtssBapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021FqtssBapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021FqtssBapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021FqtssBapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021FqtssBapRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021FqtssBapTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021FqtssBapRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021FqtssBapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021FqtssBapRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021FqtssBapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021FqtssBapTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021FqtssBapRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021FqtssBapRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021FqtssBapRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) != NULL) {
				ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
				StorageTmp->ieee8021FqtssBapTable_rsvs = 0;
				StorageTmp->ieee8021FqtssBapTable_tsts = 0;
				StorageTmp->ieee8021FqtssBapTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021FqtssBapTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021FqtssBapTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021FqtssBapRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021FqtssBapTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021FqtssBapRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021FqtssBapTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021FqtssBapTable_del(StorageNew);
				ieee8021FqtssBapTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021FqtssBapTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021FqtssBapTable_rsvs == 0)
				ieee8021FqtssBapTable_destroy(&StorageTmp->ieee8021FqtssBapTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021FqtssBapTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021FqtssMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021FqtssMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021FqtssMibold_signal_handler != NULL)
		(*ieee8021FqtssMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
}

/**
 * @fn void ieee8021FqtssMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021FqtssMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021FqtssMib", "ieee8021FqtssMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021FqtssMib", "done.\n"));
	return;
}
