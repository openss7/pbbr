/*****************************************************************************

 @(#) src/snmp/ieee8021pbmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021pbmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021PbMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021PbMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021PbMib_refresh = 1;
volatile int ieee8021PbVidTranslationTable_refresh = 1;
volatile int ieee8021PbCVidRegistrationTable_refresh = 1;
volatile int ieee8021PbEdgePortTable_refresh = 1;
volatile int ieee8021PbServicePriorityRegenerationTable_refresh = 1;
volatile int ieee8021PbCnpTable_refresh = 1;
volatile int ieee8021PbPnpTable_refresh = 1;
volatile int ieee8021PbCepTable_refresh = 1;
volatile int ieee8021PbRcapTable_refresh = 1;
volatile int ieee8021PbInternalInterfaceTable_refresh = 1;

/*
 * ieee8021PbMib_variables_oid: object identifier for ieee8021PbMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021PbMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 5 };
oid ieee8021PbVidTranslationTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 1, 1 };
oid ieee8021PbCVidRegistrationTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 2, 1 };
oid ieee8021PbCnpTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 5, 1 };
oid ieee8021PbPnpTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 6, 1 };
oid ieee8021PbCepTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 7, 1 };
oid ieee8021PbRcapTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 8, 1 };
oid ieee8021PbInternalInterfaceTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 1, 9, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021PbVidTranslationGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 1 };
oid ieee8021PbCVidRegistrationGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 2 };
oid ieee8021PbEdgePortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 3 };
oid ieee8021PbServicePriorityRegenerationGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 4 };
oid ieee8021PbDynamicCnpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 5 };
oid ieee8021PbDynamicPepGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 6 };
oid ieee8021PbDynamicCepGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 7 };
oid ieee8021PbDynamicRcapGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 8 };
oid ieee8021PbInternalInterfaceGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 1, 9 };
oid ieee8021PbCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 5, 2, 2, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021PbMib_variables: tree for ieee8021PbMib
 * This variable defines function callbacks and type return information for the ieee8021PbMib mib section
 */
struct variable4 ieee8021PbMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021PBVIDTRANSLATIONRELAYVID  1
	{(u_char) IEEE8021PBVIDTRANSLATIONRELAYVID, ASN_INTEGER, RWRITE, var_ieee8021PbVidTranslationTable, 4, {1, 1, 1, 2}},
#define   IEEE8021PBVIDTRANSLATIONROWSTATUS  2
	{(u_char) IEEE8021PBVIDTRANSLATIONROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbVidTranslationTable, 4, {1, 1, 1, 3}},
#define   IEEE8021PBCVIDREGISTRATIONSVID  3
	{(u_char) IEEE8021PBCVIDREGISTRATIONSVID, ASN_INTEGER, RWRITE, var_ieee8021PbCVidRegistrationTable, 4, {1, 2, 1, 2}},
#define   IEEE8021PBCVIDREGISTRATIONUNTAGGEDPEP  4
	{(u_char) IEEE8021PBCVIDREGISTRATIONUNTAGGEDPEP, ASN_INTEGER, RWRITE, var_ieee8021PbCVidRegistrationTable, 4, {1, 2, 1, 3}},
#define   IEEE8021PBCVIDREGISTRATIONUNTAGGEDCEP  5
	{(u_char) IEEE8021PBCVIDREGISTRATIONUNTAGGEDCEP, ASN_INTEGER, RWRITE, var_ieee8021PbCVidRegistrationTable, 4, {1, 2, 1, 4}},
#define   IEEE8021PBCVIDREGISTRATIONROWSTATUS  6
	{(u_char) IEEE8021PBCVIDREGISTRATIONROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbCVidRegistrationTable, 4, {1, 2, 1, 5}},
#define   IEEE8021PBEDGEPORTPVID  7
	{(u_char) IEEE8021PBEDGEPORTPVID, ASN_INTEGER, RWRITE, var_ieee8021PbEdgePortTable, 4, {1, 3, 1, 2}},
#define   IEEE8021PBEDGEPORTDEFAULTUSERPRIORITY  8
	{(u_char) IEEE8021PBEDGEPORTDEFAULTUSERPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021PbEdgePortTable, 4, {1, 3, 1, 3}},
#define   IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES  9
	{(u_char) IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES, ASN_INTEGER, RWRITE, var_ieee8021PbEdgePortTable, 4, {1, 3, 1, 4}},
#define   IEEE8021PBEDGEPORTENABLEINGRESSFILTERING  10
	{(u_char) IEEE8021PBEDGEPORTENABLEINGRESSFILTERING, ASN_INTEGER, RWRITE, var_ieee8021PbEdgePortTable, 4, {1, 3, 1, 5}},
#define   IEEE8021PBSERVICEPRIORITYREGENERATIONREGENERATEDPRIORITY  11
	{(u_char) IEEE8021PBSERVICEPRIORITYREGENERATIONREGENERATEDPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021PbServicePriorityRegenerationTable, 4, {1, 4, 1, 3}},
#define   IEEE8021PBCNPCCOMPONENTID  12
	{(u_char) IEEE8021PBCNPCCOMPONENTID, ASN_UNSIGNED, RWRITE, var_ieee8021PbCnpTable, 4, {1, 5, 1, 1}},
#define   IEEE8021PBCNPSVID     13
	{(u_char) IEEE8021PBCNPSVID, ASN_INTEGER, RWRITE, var_ieee8021PbCnpTable, 4, {1, 5, 1, 2}},
#define   IEEE8021PBCNPROWSTATUS  14
	{(u_char) IEEE8021PBCNPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbCnpTable, 4, {1, 5, 1, 3}},
#define   IEEE8021PBPNPROWSTATUS  15
	{(u_char) IEEE8021PBPNPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbPnpTable, 4, {1, 6, 1, 1}},
#define   IEEE8021PBCEPCCOMPONENTID  16
	{(u_char) IEEE8021PBCEPCCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021PbCepTable, 4, {1, 7, 1, 1}},
#define   IEEE8021PBCEPCEPPORTNUMBER  17
	{(u_char) IEEE8021PBCEPCEPPORTNUMBER, ASN_UNSIGNED, RONLY, var_ieee8021PbCepTable, 4, {1, 7, 1, 2}},
#define   IEEE8021PBCEPROWSTATUS  18
	{(u_char) IEEE8021PBCEPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbCepTable, 4, {1, 7, 1, 3}},
#define   IEEE8021PBRCAPSCOMPONENTID  19
	{(u_char) IEEE8021PBRCAPSCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021PbRcapTable, 4, {1, 8, 1, 1}},
#define   IEEE8021PBRCAPPORTNUMBER  20
	{(u_char) IEEE8021PBRCAPPORTNUMBER, ASN_UNSIGNED, RONLY, var_ieee8021PbRcapTable, 4, {1, 8, 1, 2}},
#define   IEEE8021PBRCAPROWSTATUS  21
	{(u_char) IEEE8021PBRCAPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbRcapTable, 4, {1, 8, 1, 3}},
#define   IEEE8021PBTIINTERNALPORTNUMBER  22
	{(u_char) IEEE8021PBTIINTERNALPORTNUMBER, ASN_UNSIGNED, RONLY, var_ieee8021PbInternalInterfaceTable, 4, {1, 9, 1, 2}},
#define   IEEE8021PBTIINTERNALPORTTYPE  23
	{(u_char) IEEE8021PBTIINTERNALPORTTYPE, ASN_INTEGER, RWRITE, var_ieee8021PbInternalInterfaceTable, 4, {1, 9, 1, 3}},
#define   IEEE8021PBTIINTERNALSVID  24
	{(u_char) IEEE8021PBTIINTERNALSVID, ASN_INTEGER, RWRITE, var_ieee8021PbInternalInterfaceTable, 4, {1, 9, 1, 4}},
#define   IEEE8021PBTIROWSTATUS  25
	{(u_char) IEEE8021PBTIROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbInternalInterfaceTable, 4, {1, 9, 1, 5}},
};

/* (L = length of the oidsuffix) */
struct ieee8021PbMib_data *ieee8021PbMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021PbVidTranslationTableStorage = NULL;
struct header_complex_index *ieee8021PbCVidRegistrationTableStorage = NULL;
struct header_complex_index *ieee8021PbEdgePortTableStorage = NULL;
struct header_complex_index *ieee8021PbServicePriorityRegenerationTableStorage = NULL;
struct header_complex_index *ieee8021PbCnpTableStorage = NULL;
struct header_complex_index *ieee8021PbPnpTableStorage = NULL;
struct header_complex_index *ieee8021PbCepTableStorage = NULL;
struct header_complex_index *ieee8021PbRcapTableStorage = NULL;
struct header_complex_index *ieee8021PbInternalInterfaceTableStorage = NULL;

void (*ieee8021PbMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021PbMib_loop_handler(int);
void ieee8021PbMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021PbMib(void)
 * @brief ieee8021PbMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021PbMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021PbMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021PbMib", "init_ieee8021PbMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021PbMib", ieee8021PbMib_variables, variable4, ieee8021PbMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021PbMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021PbMib", parse_ieee8021PbMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbVidTranslationTable", parse_ieee8021PbVidTranslationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbCVidRegistrationTable", parse_ieee8021PbCVidRegistrationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbEdgePortTable", parse_ieee8021PbEdgePortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbServicePriorityRegenerationTable", parse_ieee8021PbServicePriorityRegenerationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbCnpTable", parse_ieee8021PbCnpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbPnpTable", parse_ieee8021PbPnpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbCepTable", parse_ieee8021PbCepTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbRcapTable", parse_ieee8021PbRcapTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbInternalInterfaceTable", parse_ieee8021PbInternalInterfaceTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbVidTranslationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbCVidRegistrationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbEdgePortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbServicePriorityRegenerationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbCnpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbPnpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbCepTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbRcapTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbInternalInterfaceTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021PbMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021PbMib_fd_handler, (void *) 1);
	}
	ieee8021PbMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021PbMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021PbMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021PbMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021PbMib(void)
{
	DEBUGMSGTL(("ieee8021PbMib", "deinit_ieee8021PbMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021PbMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021PbMib_variables_oid, sizeof(ieee8021PbMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021PbMib");
	snmpd_unregister_config_handler("ieee8021PbVidTranslationTable");
	snmpd_unregister_config_handler("ieee8021PbCVidRegistrationTable");
	snmpd_unregister_config_handler("ieee8021PbEdgePortTable");
	snmpd_unregister_config_handler("ieee8021PbServicePriorityRegenerationTable");
	snmpd_unregister_config_handler("ieee8021PbCnpTable");
	snmpd_unregister_config_handler("ieee8021PbPnpTable");
	snmpd_unregister_config_handler("ieee8021PbCepTable");
	snmpd_unregister_config_handler("ieee8021PbRcapTable");
	snmpd_unregister_config_handler("ieee8021PbInternalInterfaceTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

int
term_ieee8021PbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021PbMib", "term_ieee8021PbMib: terminating...  "));
	deinit_ieee8021PbMib();
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021PbMib_data *ieee8021PbMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021PbMib.
 *
 * Creates a new ieee8021PbMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021PbMib.
 */
struct ieee8021PbMib_data *
ieee8021PbMib_create(void)
{
	struct ieee8021PbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbMib_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbMib_data *ieee8021PbMib_duplicate(struct ieee8021PbMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021PbMib_data *
ieee8021PbMib_duplicate(struct ieee8021PbMib_data *thedata)
{
	struct ieee8021PbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbMib_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbMib_destroy(struct ieee8021PbMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021PbMib.
 * @brief delete a scalars structure from ieee8021PbMib.
 *
 * Frees scalars that were previously removed from ieee8021PbMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbMib_destroy(struct ieee8021PbMib_data **thedata)
{
	struct ieee8021PbMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbMib_add(struct ieee8021PbMib_data *thedata)
 * @param thedata the structure representing ieee8021PbMib scalars.
 * @brief adds node to the ieee8021PbMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021PbMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021PbMib_add(struct ieee8021PbMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbMib_add: adding data...  "));
	if (thedata)
		ieee8021PbMibStorage = thedata;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021PbMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbMib_data *StorageTmp = ieee8021PbMib_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021PbMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbMib: storing data...  "));
	refresh_ieee8021PbMib(1);
	if ((StorageTmp = ieee8021PbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021PbMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021PbMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021PbMib(struct ieee8021PbMib_data *StorageTmp, struct ieee8021PbMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021PbMib(struct ieee8021PbMib_data *StorageTmp, struct ieee8021PbMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbMib(struct ieee8021PbMib_data *StorageTmp, struct ieee8021PbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021PbMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbMib(struct ieee8021PbMib_data *StorageTmp, struct ieee8021PbMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021PbMib(struct 
 * @fn void revert_ieee8021PbMib(struct ieee8021PbMib_data *StorageTmp, struct ieee8021PbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021PbMib(struct ieee8021PbMib_data *StorageTmp, struct ieee8021PbMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021PbMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021PbMib(int force)
{
	if (ieee8021PbMibStorage == NULL) {
		struct ieee8021PbMib_data *StorageNew;

		if ((StorageNew = ieee8021PbMib_create()) == NULL)
			return;
		ieee8021PbMibStorage = StorageNew;
		ieee8021PbMib_refresh = 1;
	}
	if (!force && ieee8021PbMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021PbMib", "refresh_ieee8021PbMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021PbMib_refresh = 0;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021PbMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021PbMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021PbMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021PbMib(0);
	if ((StorageTmp = ieee8021PbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021PbMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021PbMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021PbMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021PbVidTranslationTable_data *ieee8021PbVidTranslationTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbVidTranslationTable table.
 *
 * Creates a new ieee8021PbVidTranslationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbVidTranslationTable_data *
ieee8021PbVidTranslationTable_create(void)
{
	struct ieee8021PbVidTranslationTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbVidTranslationTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbVidTranslationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbVidTranslationRelayVid = 0;
		StorageNew->ieee8021PbVidTranslationRowStatus = 0;
		StorageNew->ieee8021PbVidTranslationRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbVidTranslationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbVidTranslationTable_data *ieee8021PbVidTranslationTable_duplicate(struct ieee8021PbVidTranslationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbVidTranslationTable_data *
ieee8021PbVidTranslationTable_duplicate(struct ieee8021PbVidTranslationTable_data *thedata)
{
	struct ieee8021PbVidTranslationTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbVidTranslationTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbVidTranslationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbVidTranslationTable_id = thedata->ieee8021PbVidTranslationTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbVidTranslationLocalVid = thedata->ieee8021PbVidTranslationLocalVid;
		StorageNew->ieee8021PbVidTranslationRelayVid = thedata->ieee8021PbVidTranslationRelayVid;
		StorageNew->ieee8021PbVidTranslationRowStatus = thedata->ieee8021PbVidTranslationRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbVidTranslationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbVidTranslationTable_destroy(struct ieee8021PbVidTranslationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbVidTranslationTable_destroy(struct ieee8021PbVidTranslationTable_data **thedata)
{
	struct ieee8021PbVidTranslationTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbVidTranslationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbVidTranslationTable_add(struct ieee8021PbVidTranslationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbVidTranslationTable table data set.
 *
 * Adds a table row structure to the ieee8021PbVidTranslationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbVidTranslationTable_add(struct ieee8021PbVidTranslationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbVidTranslationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021PbVidTranslationLocalVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbVidTranslationLocalVid, sizeof(thedata->ieee8021PbVidTranslationLocalVid));
		header_complex_add_data(&ieee8021PbVidTranslationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbVidTranslationTable_del(struct ieee8021PbVidTranslationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbVidTranslationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbVidTranslationTable_del(struct ieee8021PbVidTranslationTable_data *thedata)
{
	struct ieee8021PbVidTranslationTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbVidTranslationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbVidTranslationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbVidTranslationTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbVidTranslationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbVidTranslationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbVidTranslationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbVidTranslationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbVidTranslationTable_data *StorageTmp = ieee8021PbVidTranslationTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbVidTranslationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbVidTranslationLocalVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbVidTranslationRelayVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbVidTranslationRowStatus, &tmpsize);
	ieee8021PbVidTranslationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbVidTranslationTable(): store configuraiton file for ieee8021PbVidTranslationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbVidTranslationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbVidTranslationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbVidTranslationTable: storing data...  "));
	refresh_ieee8021PbVidTranslationTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbVidTranslationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbVidTranslationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbVidTranslationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbVidTranslationLocalVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbVidTranslationRelayVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbVidTranslationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbCVidRegistrationTable_data *ieee8021PbCVidRegistrationTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbCVidRegistrationTable table.
 *
 * Creates a new ieee8021PbCVidRegistrationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbCVidRegistrationTable_data *
ieee8021PbCVidRegistrationTable_create(void)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbCVidRegistrationTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCVidRegistrationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbCVidRegistrationSVid = 0;
		StorageNew->ieee8021PbCVidRegistrationUntaggedPep = TV_TRUE;
		StorageNew->ieee8021PbCVidRegistrationUntaggedCep = TV_TRUE;
		StorageNew->ieee8021PbCVidRegistrationRowStatus = 0;
		StorageNew->ieee8021PbCVidRegistrationRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbCVidRegistrationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbCVidRegistrationTable_data *ieee8021PbCVidRegistrationTable_duplicate(struct ieee8021PbCVidRegistrationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbCVidRegistrationTable_data *
ieee8021PbCVidRegistrationTable_duplicate(struct ieee8021PbCVidRegistrationTable_data *thedata)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbCVidRegistrationTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCVidRegistrationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbCVidRegistrationTable_id = thedata->ieee8021PbCVidRegistrationTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbCVidRegistrationCVid = thedata->ieee8021PbCVidRegistrationCVid;
		StorageNew->ieee8021PbCVidRegistrationSVid = thedata->ieee8021PbCVidRegistrationSVid;
		StorageNew->ieee8021PbCVidRegistrationUntaggedPep = thedata->ieee8021PbCVidRegistrationUntaggedPep;
		StorageNew->ieee8021PbCVidRegistrationUntaggedCep = thedata->ieee8021PbCVidRegistrationUntaggedCep;
		StorageNew->ieee8021PbCVidRegistrationRowStatus = thedata->ieee8021PbCVidRegistrationRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbCVidRegistrationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbCVidRegistrationTable_destroy(struct ieee8021PbCVidRegistrationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbCVidRegistrationTable_destroy(struct ieee8021PbCVidRegistrationTable_data **thedata)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCVidRegistrationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbCVidRegistrationTable_add(struct ieee8021PbCVidRegistrationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbCVidRegistrationTable table data set.
 *
 * Adds a table row structure to the ieee8021PbCVidRegistrationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbCVidRegistrationTable_add(struct ieee8021PbCVidRegistrationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCVidRegistrationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021PbCVidRegistrationCVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbCVidRegistrationCVid, sizeof(thedata->ieee8021PbCVidRegistrationCVid));
		header_complex_add_data(&ieee8021PbCVidRegistrationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbCVidRegistrationTable_del(struct ieee8021PbCVidRegistrationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbCVidRegistrationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbCVidRegistrationTable_del(struct ieee8021PbCVidRegistrationTable_data *thedata)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCVidRegistrationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbCVidRegistrationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbCVidRegistrationTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbCVidRegistrationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbCVidRegistrationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbCVidRegistrationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbCVidRegistrationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp = ieee8021PbCVidRegistrationTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbCVidRegistrationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCVidRegistrationCVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCVidRegistrationSVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCVidRegistrationUntaggedPep, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCVidRegistrationUntaggedCep, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCVidRegistrationRowStatus, &tmpsize);
	ieee8021PbCVidRegistrationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbCVidRegistrationTable(): store configuraiton file for ieee8021PbCVidRegistrationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbCVidRegistrationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbCVidRegistrationTable: storing data...  "));
	refresh_ieee8021PbCVidRegistrationTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbCVidRegistrationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbCVidRegistrationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbCVidRegistrationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCVidRegistrationCVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCVidRegistrationSVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCVidRegistrationUntaggedPep, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCVidRegistrationUntaggedCep, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCVidRegistrationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbEdgePortTable_data *ieee8021PbEdgePortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbEdgePortTable table.
 *
 * Creates a new ieee8021PbEdgePortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbEdgePortTable_data *
ieee8021PbEdgePortTable_create(void)
{
	struct ieee8021PbEdgePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbEdgePortTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbEdgePortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbEdgePortPVID = 0;
		StorageNew->ieee8021PbEdgePortDefaultUserPriority = 0;
		StorageNew->ieee8021PbEdgePortAcceptableFrameTypes = IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES_ADMITALL;
		StorageNew->ieee8021PbEdgePortEnableIngressFiltering = TV_TRUE;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbEdgePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbEdgePortTable_data *ieee8021PbEdgePortTable_duplicate(struct ieee8021PbEdgePortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbEdgePortTable_data *
ieee8021PbEdgePortTable_duplicate(struct ieee8021PbEdgePortTable_data *thedata)
{
	struct ieee8021PbEdgePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbEdgePortTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbEdgePortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbEdgePortTable_id = thedata->ieee8021PbEdgePortTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbEdgePortSVid = thedata->ieee8021PbEdgePortSVid;
		StorageNew->ieee8021PbEdgePortPVID = thedata->ieee8021PbEdgePortPVID;
		StorageNew->ieee8021PbEdgePortDefaultUserPriority = thedata->ieee8021PbEdgePortDefaultUserPriority;
		StorageNew->ieee8021PbEdgePortAcceptableFrameTypes = thedata->ieee8021PbEdgePortAcceptableFrameTypes;
		StorageNew->ieee8021PbEdgePortEnableIngressFiltering = thedata->ieee8021PbEdgePortEnableIngressFiltering;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbEdgePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbEdgePortTable_destroy(struct ieee8021PbEdgePortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbEdgePortTable_destroy(struct ieee8021PbEdgePortTable_data **thedata)
{
	struct ieee8021PbEdgePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbEdgePortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbEdgePortTable_add(struct ieee8021PbEdgePortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbEdgePortTable table data set.
 *
 * Adds a table row structure to the ieee8021PbEdgePortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbEdgePortTable_add(struct ieee8021PbEdgePortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbEdgePortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021PbEdgePortSVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbEdgePortSVid, sizeof(thedata->ieee8021PbEdgePortSVid));
		header_complex_add_data(&ieee8021PbEdgePortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbEdgePortTable_del(struct ieee8021PbEdgePortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbEdgePortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbEdgePortTable_del(struct ieee8021PbEdgePortTable_data *thedata)
{
	struct ieee8021PbEdgePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbEdgePortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbEdgePortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbEdgePortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbEdgePortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbEdgePortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbEdgePortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbEdgePortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbEdgePortTable_data *StorageTmp = ieee8021PbEdgePortTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbEdgePortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbEdgePortSVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbEdgePortPVID, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbEdgePortDefaultUserPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbEdgePortEnableIngressFiltering, &tmpsize);
	ieee8021PbEdgePortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbEdgePortTable(): store configuraiton file for ieee8021PbEdgePortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbEdgePortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbEdgePortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbEdgePortTable: storing data...  "));
	refresh_ieee8021PbEdgePortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbEdgePortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbEdgePortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbEdgePortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbEdgePortSVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbEdgePortPVID, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbEdgePortDefaultUserPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbEdgePortEnableIngressFiltering, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbServicePriorityRegenerationTable_data *ieee8021PbServicePriorityRegenerationTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbServicePriorityRegenerationTable table.
 *
 * Creates a new ieee8021PbServicePriorityRegenerationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbServicePriorityRegenerationTable_data *
ieee8021PbServicePriorityRegenerationTable_create(void)
{
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbServicePriorityRegenerationTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbServicePriorityRegenerationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbServicePriorityRegenerationRegeneratedPriority = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbServicePriorityRegenerationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbServicePriorityRegenerationTable_data *ieee8021PbServicePriorityRegenerationTable_duplicate(struct ieee8021PbServicePriorityRegenerationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbServicePriorityRegenerationTable_data *
ieee8021PbServicePriorityRegenerationTable_duplicate(struct ieee8021PbServicePriorityRegenerationTable_data *thedata)
{
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbServicePriorityRegenerationTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbServicePriorityRegenerationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbServicePriorityRegenerationTable_id = thedata->ieee8021PbServicePriorityRegenerationTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbServicePriorityRegenerationSVid = thedata->ieee8021PbServicePriorityRegenerationSVid;
		StorageNew->ieee8021PbServicePriorityRegenerationReceivedPriority = thedata->ieee8021PbServicePriorityRegenerationReceivedPriority;
		StorageNew->ieee8021PbServicePriorityRegenerationRegeneratedPriority = thedata->ieee8021PbServicePriorityRegenerationRegeneratedPriority;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbServicePriorityRegenerationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbServicePriorityRegenerationTable_destroy(struct ieee8021PbServicePriorityRegenerationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbServicePriorityRegenerationTable_destroy(struct ieee8021PbServicePriorityRegenerationTable_data **thedata)
{
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbServicePriorityRegenerationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbServicePriorityRegenerationTable_add(struct ieee8021PbServicePriorityRegenerationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbServicePriorityRegenerationTable table data set.
 *
 * Adds a table row structure to the ieee8021PbServicePriorityRegenerationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbServicePriorityRegenerationTable_add(struct ieee8021PbServicePriorityRegenerationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbServicePriorityRegenerationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021PbServicePriorityRegenerationSVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbServicePriorityRegenerationSVid, sizeof(thedata->ieee8021PbServicePriorityRegenerationSVid));
		/* ieee8021PbServicePriorityRegenerationReceivedPriority */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbServicePriorityRegenerationReceivedPriority,
					  sizeof(thedata->ieee8021PbServicePriorityRegenerationReceivedPriority));
		header_complex_add_data(&ieee8021PbServicePriorityRegenerationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbServicePriorityRegenerationTable_del(struct ieee8021PbServicePriorityRegenerationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbServicePriorityRegenerationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbServicePriorityRegenerationTable_del(struct ieee8021PbServicePriorityRegenerationTable_data *thedata)
{
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbServicePriorityRegenerationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbServicePriorityRegenerationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbServicePriorityRegenerationTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbServicePriorityRegenerationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbServicePriorityRegenerationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbServicePriorityRegenerationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbServicePriorityRegenerationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp = ieee8021PbServicePriorityRegenerationTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbServicePriorityRegenerationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbServicePriorityRegenerationSVid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbServicePriorityRegenerationReceivedPriority, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority, &tmpsize);
	ieee8021PbServicePriorityRegenerationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbServicePriorityRegenerationTable(): store configuraiton file for ieee8021PbServicePriorityRegenerationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbServicePriorityRegenerationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbServicePriorityRegenerationTable: storing data...  "));
	refresh_ieee8021PbServicePriorityRegenerationTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbServicePriorityRegenerationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbServicePriorityRegenerationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbServicePriorityRegenerationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbServicePriorityRegenerationSVid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbServicePriorityRegenerationReceivedPriority, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbCnpTable_data *ieee8021PbCnpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbCnpTable table.
 *
 * Creates a new ieee8021PbCnpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbCnpTable_data *
ieee8021PbCnpTable_create(void)
{
	struct ieee8021PbCnpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbCnpTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCnpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbCnpCComponentId = 0;
		StorageNew->ieee8021PbCnpSVid = 0;
		StorageNew->ieee8021PbCnpRowStatus = 0;
		StorageNew->ieee8021PbCnpRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbCnpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbCnpTable_data *ieee8021PbCnpTable_duplicate(struct ieee8021PbCnpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbCnpTable_data *
ieee8021PbCnpTable_duplicate(struct ieee8021PbCnpTable_data *thedata)
{
	struct ieee8021PbCnpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbCnpTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCnpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbCnpTable_id = thedata->ieee8021PbCnpTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbCnpCComponentId = thedata->ieee8021PbCnpCComponentId;
		StorageNew->ieee8021PbCnpSVid = thedata->ieee8021PbCnpSVid;
		StorageNew->ieee8021PbCnpRowStatus = thedata->ieee8021PbCnpRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbCnpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbCnpTable_destroy(struct ieee8021PbCnpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbCnpTable_destroy(struct ieee8021PbCnpTable_data **thedata)
{
	struct ieee8021PbCnpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCnpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbCnpTable_add(struct ieee8021PbCnpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbCnpTable table data set.
 *
 * Adds a table row structure to the ieee8021PbCnpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbCnpTable_add(struct ieee8021PbCnpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCnpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbCnpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbCnpTable_del(struct ieee8021PbCnpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbCnpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbCnpTable_del(struct ieee8021PbCnpTable_data *thedata)
{
	struct ieee8021PbCnpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCnpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbCnpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbCnpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbCnpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbCnpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbCnpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbCnpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbCnpTable_data *StorageTmp = ieee8021PbCnpTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbCnpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbCnpCComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCnpSVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCnpRowStatus, &tmpsize);
	ieee8021PbCnpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbCnpTable(): store configuraiton file for ieee8021PbCnpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbCnpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbCnpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbCnpTable: storing data...  "));
	refresh_ieee8021PbCnpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbCnpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbCnpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbCnpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbCnpCComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCnpSVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCnpRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbPnpTable_data *ieee8021PbPnpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbPnpTable table.
 *
 * Creates a new ieee8021PbPnpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbPnpTable_data *
ieee8021PbPnpTable_create(void)
{
	struct ieee8021PbPnpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbPnpTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbPnpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbPnpRowStatus = 0;
		StorageNew->ieee8021PbPnpRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbPnpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbPnpTable_data *ieee8021PbPnpTable_duplicate(struct ieee8021PbPnpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbPnpTable_data *
ieee8021PbPnpTable_duplicate(struct ieee8021PbPnpTable_data *thedata)
{
	struct ieee8021PbPnpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbPnpTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbPnpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbPnpTable_id = thedata->ieee8021PbPnpTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbPnpRowStatus = thedata->ieee8021PbPnpRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbPnpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbPnpTable_destroy(struct ieee8021PbPnpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbPnpTable_destroy(struct ieee8021PbPnpTable_data **thedata)
{
	struct ieee8021PbPnpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbPnpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbPnpTable_add(struct ieee8021PbPnpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbPnpTable table data set.
 *
 * Adds a table row structure to the ieee8021PbPnpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbPnpTable_add(struct ieee8021PbPnpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbPnpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbPnpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbPnpTable_del(struct ieee8021PbPnpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbPnpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbPnpTable_del(struct ieee8021PbPnpTable_data *thedata)
{
	struct ieee8021PbPnpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbPnpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbPnpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbPnpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbPnpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbPnpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbPnpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbPnpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbPnpTable_data *StorageTmp = ieee8021PbPnpTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbPnpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbPnpRowStatus, &tmpsize);
	ieee8021PbPnpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbPnpTable(): store configuraiton file for ieee8021PbPnpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbPnpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbPnpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbPnpTable: storing data...  "));
	refresh_ieee8021PbPnpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbPnpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbPnpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbPnpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbPnpRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbCepTable_data *ieee8021PbCepTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbCepTable table.
 *
 * Creates a new ieee8021PbCepTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbCepTable_data *
ieee8021PbCepTable_create(void)
{
	struct ieee8021PbCepTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbCepTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCepTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbCepCComponentId = 0;
		StorageNew->ieee8021PbCepCepPortNumber = 0;
		StorageNew->ieee8021PbCepRowStatus = 0;
		StorageNew->ieee8021PbCepRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbCepTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbCepTable_data *ieee8021PbCepTable_duplicate(struct ieee8021PbCepTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbCepTable_data *
ieee8021PbCepTable_duplicate(struct ieee8021PbCepTable_data *thedata)
{
	struct ieee8021PbCepTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbCepTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCepTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbCepTable_id = thedata->ieee8021PbCepTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbCepCComponentId = thedata->ieee8021PbCepCComponentId;
		StorageNew->ieee8021PbCepCepPortNumber = thedata->ieee8021PbCepCepPortNumber;
		StorageNew->ieee8021PbCepRowStatus = thedata->ieee8021PbCepRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbCepTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbCepTable_destroy(struct ieee8021PbCepTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbCepTable_destroy(struct ieee8021PbCepTable_data **thedata)
{
	struct ieee8021PbCepTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCepTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbCepTable_add(struct ieee8021PbCepTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbCepTable table data set.
 *
 * Adds a table row structure to the ieee8021PbCepTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbCepTable_add(struct ieee8021PbCepTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCepTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbCepTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbCepTable_del(struct ieee8021PbCepTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbCepTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbCepTable_del(struct ieee8021PbCepTable_data *thedata)
{
	struct ieee8021PbCepTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbCepTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbCepTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbCepTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbCepTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbCepTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbCepTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbCepTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbCepTable_data *StorageTmp = ieee8021PbCepTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbCepTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbCepCComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbCepCepPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbCepRowStatus, &tmpsize);
	ieee8021PbCepTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbCepTable(): store configuraiton file for ieee8021PbCepTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbCepTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbCepTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbCepTable: storing data...  "));
	refresh_ieee8021PbCepTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbCepTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbCepTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbCepTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbCepCComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbCepCepPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbCepRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbRcapTable_data *ieee8021PbRcapTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbRcapTable table.
 *
 * Creates a new ieee8021PbRcapTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbRcapTable_data *
ieee8021PbRcapTable_create(void)
{
	struct ieee8021PbRcapTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbRcapTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbRcapTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbRcapSComponentId = 0;
		StorageNew->ieee8021PbRcapPortNumber = 0;
		StorageNew->ieee8021PbRcapRowStatus = 0;
		StorageNew->ieee8021PbRcapRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbRcapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbRcapTable_data *ieee8021PbRcapTable_duplicate(struct ieee8021PbRcapTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbRcapTable_data *
ieee8021PbRcapTable_duplicate(struct ieee8021PbRcapTable_data *thedata)
{
	struct ieee8021PbRcapTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbRcapTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbRcapTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbRcapTable_id = thedata->ieee8021PbRcapTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbRcapSComponentId = thedata->ieee8021PbRcapSComponentId;
		StorageNew->ieee8021PbRcapPortNumber = thedata->ieee8021PbRcapPortNumber;
		StorageNew->ieee8021PbRcapRowStatus = thedata->ieee8021PbRcapRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbRcapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbRcapTable_destroy(struct ieee8021PbRcapTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbRcapTable_destroy(struct ieee8021PbRcapTable_data **thedata)
{
	struct ieee8021PbRcapTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbRcapTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbRcapTable_add(struct ieee8021PbRcapTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbRcapTable table data set.
 *
 * Adds a table row structure to the ieee8021PbRcapTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbRcapTable_add(struct ieee8021PbRcapTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbRcapTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbRcapTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbRcapTable_del(struct ieee8021PbRcapTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbRcapTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbRcapTable_del(struct ieee8021PbRcapTable_data *thedata)
{
	struct ieee8021PbRcapTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbRcapTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbRcapTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbRcapTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbRcapTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbRcapTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbRcapTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbRcapTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbRcapTable_data *StorageTmp = ieee8021PbRcapTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbRcapTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbRcapSComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbRcapPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbRcapRowStatus, &tmpsize);
	ieee8021PbRcapTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbRcapTable(): store configuraiton file for ieee8021PbRcapTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbRcapTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbRcapTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbRcapTable: storing data...  "));
	refresh_ieee8021PbRcapTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbRcapTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbRcapTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbRcapTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbRcapSComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbRcapPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbRcapRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbInternalInterfaceTable_data *ieee8021PbInternalInterfaceTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbInternalInterfaceTable table.
 *
 * Creates a new ieee8021PbInternalInterfaceTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbInternalInterfaceTable_data *
ieee8021PbInternalInterfaceTable_create(void)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbInternalInterfaceTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbInternalInterfaceTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbTiInternalPortNumber = 0;
		StorageNew->ieee8021PbTiInternalPortType = 0;
		StorageNew->ieee8021PbTiInternalSVid = 0;
		StorageNew->ieee8021PbTiRowStatus = 0;
		StorageNew->ieee8021PbTiRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbInternalInterfaceTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbInternalInterfaceTable_data *ieee8021PbInternalInterfaceTable_duplicate(struct ieee8021PbInternalInterfaceTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbInternalInterfaceTable_data *
ieee8021PbInternalInterfaceTable_duplicate(struct ieee8021PbInternalInterfaceTable_data *thedata)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbInternalInterfaceTable_data);

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbInternalInterfaceTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbInternalInterfaceTable_id = thedata->ieee8021PbInternalInterfaceTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbTiExternalSVid = thedata->ieee8021PbTiExternalSVid;
		StorageNew->ieee8021PbTiInternalPortNumber = thedata->ieee8021PbTiInternalPortNumber;
		StorageNew->ieee8021PbTiInternalPortType = thedata->ieee8021PbTiInternalPortType;
		StorageNew->ieee8021PbTiInternalSVid = thedata->ieee8021PbTiInternalSVid;
		StorageNew->ieee8021PbTiRowStatus = thedata->ieee8021PbTiRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbInternalInterfaceTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbInternalInterfaceTable_destroy(struct ieee8021PbInternalInterfaceTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbInternalInterfaceTable_destroy(struct ieee8021PbInternalInterfaceTable_data **thedata)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbInternalInterfaceTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbInternalInterfaceTable_add(struct ieee8021PbInternalInterfaceTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbInternalInterfaceTable table data set.
 *
 * Adds a table row structure to the ieee8021PbInternalInterfaceTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbInternalInterfaceTable_add(struct ieee8021PbInternalInterfaceTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbInternalInterfaceTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021PbTiExternalSVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbTiExternalSVid, sizeof(thedata->ieee8021PbTiExternalSVid));
		header_complex_add_data(&ieee8021PbInternalInterfaceTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbInternalInterfaceTable_del(struct ieee8021PbInternalInterfaceTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbInternalInterfaceTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbInternalInterfaceTable_del(struct ieee8021PbInternalInterfaceTable_data *thedata)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbInternalInterfaceTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbInternalInterfaceTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbInternalInterfaceTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbInternalInterfaceTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbInternalInterfaceTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbInternalInterfaceTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbInternalInterfaceTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbInternalInterfaceTable_data *StorageTmp = ieee8021PbInternalInterfaceTable_create();

	DEBUGMSGTL(("ieee8021PbMib", "parse_ieee8021PbInternalInterfaceTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbTiExternalSVid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbTiInternalPortNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbTiInternalPortType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbTiInternalSVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbTiRowStatus, &tmpsize);
	ieee8021PbInternalInterfaceTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/*
 * store_ieee8021PbInternalInterfaceTable(): store configuraiton file for ieee8021PbInternalInterfaceTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbInternalInterfaceTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbInternalInterfaceTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbMib", "store_ieee8021PbInternalInterfaceTable: storing data...  "));
	refresh_ieee8021PbInternalInterfaceTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbInternalInterfaceTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbInternalInterfaceTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbInternalInterfaceTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbTiExternalSVid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbTiInternalPortNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbTiInternalPortType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbTiInternalSVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbTiRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, struct ieee8021PbVidTranslationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, struct ieee8021PbVidTranslationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, struct ieee8021PbVidTranslationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbVidTranslationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, struct ieee8021PbVidTranslationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbVidTranslationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, struct ieee8021PbVidTranslationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, struct ieee8021PbVidTranslationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbVidTranslationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbVidTranslationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbVidTranslationTable_data *
refresh_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbVidTranslationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbVidTranslationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbVidTranslationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbVidTranslationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbVidTranslationTable(int force)
{
	if (!force && ieee8021PbVidTranslationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbVidTranslationTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbVidTranslationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbVidTranslationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbVidTranslationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbVidTranslationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbVidTranslationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbVidTranslationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbVidTranslationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbVidTranslationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBVIDTRANSLATIONRELAYVID:	/* Create */
		*write_method = write_ieee8021PbVidTranslationRelayVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbVidTranslationRelayVid);
		rval = (u_char *) &StorageTmp->ieee8021PbVidTranslationRelayVid;
		break;
	case (u_char) IEEE8021PBVIDTRANSLATIONROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbVidTranslationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbVidTranslationRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbVidTranslationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, struct ieee8021PbCVidRegistrationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, struct ieee8021PbCVidRegistrationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, struct ieee8021PbCVidRegistrationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbCVidRegistrationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, struct ieee8021PbCVidRegistrationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbCVidRegistrationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, struct ieee8021PbCVidRegistrationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, struct ieee8021PbCVidRegistrationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbCVidRegistrationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbCVidRegistrationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbCVidRegistrationTable_data *
refresh_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbCVidRegistrationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbCVidRegistrationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbCVidRegistrationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbCVidRegistrationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbCVidRegistrationTable(int force)
{
	if (!force && ieee8021PbCVidRegistrationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbCVidRegistrationTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbCVidRegistrationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbCVidRegistrationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbCVidRegistrationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbCVidRegistrationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbCVidRegistrationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbCVidRegistrationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbCVidRegistrationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBCVIDREGISTRATIONSVID:	/* Create */
		*write_method = write_ieee8021PbCVidRegistrationSVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCVidRegistrationSVid);
		rval = (u_char *) &StorageTmp->ieee8021PbCVidRegistrationSVid;
		break;
	case (u_char) IEEE8021PBCVIDREGISTRATIONUNTAGGEDPEP:	/* Create */
		*write_method = write_ieee8021PbCVidRegistrationUntaggedPep;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCVidRegistrationUntaggedPep);
		rval = (u_char *) &StorageTmp->ieee8021PbCVidRegistrationUntaggedPep;
		break;
	case (u_char) IEEE8021PBCVIDREGISTRATIONUNTAGGEDCEP:	/* Create */
		*write_method = write_ieee8021PbCVidRegistrationUntaggedCep;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCVidRegistrationUntaggedCep);
		rval = (u_char *) &StorageTmp->ieee8021PbCVidRegistrationUntaggedCep;
		break;
	case (u_char) IEEE8021PBCVIDREGISTRATIONROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbCVidRegistrationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCVidRegistrationRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbCVidRegistrationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, struct ieee8021PbEdgePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, struct ieee8021PbEdgePortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, struct ieee8021PbEdgePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbEdgePortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, struct ieee8021PbEdgePortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbEdgePortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, struct ieee8021PbEdgePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, struct ieee8021PbEdgePortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbEdgePortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbEdgePortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbEdgePortTable_data *
refresh_ieee8021PbEdgePortTable_row(struct ieee8021PbEdgePortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbEdgePortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbEdgePortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbEdgePortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbEdgePortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbEdgePortTable(int force)
{
	if (!force && ieee8021PbEdgePortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbEdgePortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbEdgePortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbEdgePortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbEdgePortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbEdgePortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbEdgePortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbEdgePortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbEdgePortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbEdgePortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBEDGEPORTPVID:	/* ReadWrite */
		*write_method = write_ieee8021PbEdgePortPVID;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbEdgePortPVID);
		rval = (u_char *) &StorageTmp->ieee8021PbEdgePortPVID;
		break;
	case (u_char) IEEE8021PBEDGEPORTDEFAULTUSERPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021PbEdgePortDefaultUserPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbEdgePortDefaultUserPriority);
		rval = (u_char *) &StorageTmp->ieee8021PbEdgePortDefaultUserPriority;
		break;
	case (u_char) IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES:	/* ReadWrite */
		*write_method = write_ieee8021PbEdgePortAcceptableFrameTypes;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes);
		rval = (u_char *) &StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes;
		break;
	case (u_char) IEEE8021PBEDGEPORTENABLEINGRESSFILTERING:	/* ReadWrite */
		*write_method = write_ieee8021PbEdgePortEnableIngressFiltering;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbEdgePortEnableIngressFiltering);
		rval = (u_char *) &StorageTmp->ieee8021PbEdgePortEnableIngressFiltering;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, struct ieee8021PbServicePriorityRegenerationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, struct ieee8021PbServicePriorityRegenerationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, struct ieee8021PbServicePriorityRegenerationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbServicePriorityRegenerationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, struct ieee8021PbServicePriorityRegenerationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbServicePriorityRegenerationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, struct ieee8021PbServicePriorityRegenerationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, struct ieee8021PbServicePriorityRegenerationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbServicePriorityRegenerationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbServicePriorityRegenerationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbServicePriorityRegenerationTable_data *
refresh_ieee8021PbServicePriorityRegenerationTable_row(struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbServicePriorityRegenerationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbServicePriorityRegenerationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbServicePriorityRegenerationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbServicePriorityRegenerationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbServicePriorityRegenerationTable(int force)
{
	if (!force && ieee8021PbServicePriorityRegenerationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbServicePriorityRegenerationTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbServicePriorityRegenerationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbServicePriorityRegenerationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbServicePriorityRegenerationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbServicePriorityRegenerationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbServicePriorityRegenerationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbServicePriorityRegenerationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbServicePriorityRegenerationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBSERVICEPRIORITYREGENERATIONREGENERATEDPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021PbServicePriorityRegenerationRegeneratedPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority);
		rval = (u_char *) &StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, struct ieee8021PbCnpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, struct ieee8021PbCnpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, struct ieee8021PbCnpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbCnpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, struct ieee8021PbCnpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbCnpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, struct ieee8021PbCnpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, struct ieee8021PbCnpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbCnpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbCnpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbCnpTable_data *
refresh_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbCnpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbCnpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbCnpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbCnpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbCnpTable(int force)
{
	if (!force && ieee8021PbCnpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbCnpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbCnpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbCnpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbCnpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbCnpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbCnpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbCnpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbCnpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbCnpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBCNPCCOMPONENTID:	/* Create */
		*write_method = write_ieee8021PbCnpCComponentId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCnpCComponentId);
		rval = (u_char *) &StorageTmp->ieee8021PbCnpCComponentId;
		break;
	case (u_char) IEEE8021PBCNPSVID:	/* Create */
		*write_method = write_ieee8021PbCnpSVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCnpSVid);
		rval = (u_char *) &StorageTmp->ieee8021PbCnpSVid;
		break;
	case (u_char) IEEE8021PBCNPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbCnpRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCnpRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbCnpRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, struct ieee8021PbPnpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, struct ieee8021PbPnpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, struct ieee8021PbPnpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbPnpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, struct ieee8021PbPnpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbPnpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, struct ieee8021PbPnpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, struct ieee8021PbPnpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbPnpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbPnpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbPnpTable_data *
refresh_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbPnpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbPnpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbPnpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbPnpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbPnpTable(int force)
{
	if (!force && ieee8021PbPnpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbPnpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbPnpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbPnpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbPnpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbPnpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbPnpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbPnpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbPnpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbPnpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBPNPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbPnpRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbPnpRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbPnpRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, struct ieee8021PbCepTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, struct ieee8021PbCepTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, struct ieee8021PbCepTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbCepTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, struct ieee8021PbCepTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbCepTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, struct ieee8021PbCepTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, struct ieee8021PbCepTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbCepTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbCepTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbCepTable_data *
refresh_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbCepTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbCepTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbCepTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbCepTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbCepTable(int force)
{
	if (!force && ieee8021PbCepTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbCepTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbCepTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbCepTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbCepTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbCepTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbCepTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbCepTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbCepTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbCepTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBCEPCCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCepCComponentId);
		rval = (u_char *) &StorageTmp->ieee8021PbCepCComponentId;
		break;
	case (u_char) IEEE8021PBCEPCEPPORTNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCepCepPortNumber);
		rval = (u_char *) &StorageTmp->ieee8021PbCepCepPortNumber;
		break;
	case (u_char) IEEE8021PBCEPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbCepRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbCepRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbCepRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, struct ieee8021PbRcapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, struct ieee8021PbRcapTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, struct ieee8021PbRcapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbRcapTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, struct ieee8021PbRcapTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbRcapTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, struct ieee8021PbRcapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, struct ieee8021PbRcapTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbRcapTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbRcapTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbRcapTable_data *
refresh_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbRcapTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbRcapTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbRcapTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbRcapTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbRcapTable(int force)
{
	if (!force && ieee8021PbRcapTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbRcapTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbRcapTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbRcapTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbRcapTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbRcapTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbRcapTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbRcapTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbRcapTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbRcapTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBRCAPSCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbRcapSComponentId);
		rval = (u_char *) &StorageTmp->ieee8021PbRcapSComponentId;
		break;
	case (u_char) IEEE8021PBRCAPPORTNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbRcapPortNumber);
		rval = (u_char *) &StorageTmp->ieee8021PbRcapPortNumber;
		break;
	case (u_char) IEEE8021PBRCAPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbRcapRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbRcapRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbRcapRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, struct ieee8021PbInternalInterfaceTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, struct ieee8021PbInternalInterfaceTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, struct ieee8021PbInternalInterfaceTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbInternalInterfaceTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, struct ieee8021PbInternalInterfaceTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbInternalInterfaceTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, struct ieee8021PbInternalInterfaceTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, struct ieee8021PbInternalInterfaceTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbInternalInterfaceTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbInternalInterfaceTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbInternalInterfaceTable_data *
refresh_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbInternalInterfaceTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbInternalInterfaceTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbInternalInterfaceTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbInternalInterfaceTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbInternalInterfaceTable(int force)
{
	if (!force && ieee8021PbInternalInterfaceTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbInternalInterfaceTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbInternalInterfaceTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbInternalInterfaceTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbMib above.
 */
u_char *
var_ieee8021PbInternalInterfaceTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbMib", "var_ieee8021PbInternalInterfaceTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbInternalInterfaceTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbInternalInterfaceTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbInternalInterfaceTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBTIINTERNALPORTNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbTiInternalPortNumber);
		rval = (u_char *) &StorageTmp->ieee8021PbTiInternalPortNumber;
		break;
	case (u_char) IEEE8021PBTIINTERNALPORTTYPE:	/* Create */
		*write_method = write_ieee8021PbTiInternalPortType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbTiInternalPortType);
		rval = (u_char *) &StorageTmp->ieee8021PbTiInternalPortType;
		break;
	case (u_char) IEEE8021PBTIINTERNALSVID:	/* Create */
		*write_method = write_ieee8021PbTiInternalSVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbTiInternalSVid);
		rval = (u_char *) &StorageTmp->ieee8021PbTiInternalSVid;
		break;
	case (u_char) IEEE8021PBTIROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbTiRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbTiRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbTiRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021PbVidTranslationRelayVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbVidTranslationRelayVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbVidTranslationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbVidTranslationRelayVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbVidTranslationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbVidTranslationRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbVidTranslationRelayVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbVidTranslationRelayVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbVidTranslationRelayVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbVidTranslationRelayVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) == NULL)
			if (StorageTmp->ieee8021PbVidTranslationTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old = ieee8021PbVidTranslationTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbVidTranslationTable_rsvs++;
		StorageTmp->ieee8021PbVidTranslationRelayVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbVidTranslationTable_tsts == 0)
				if ((ret = check_ieee8021PbVidTranslationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbVidTranslationTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbVidTranslationRelayVid for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbVidTranslationTable_sets == 0)
				if ((ret = update_ieee8021PbVidTranslationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbVidTranslationTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) != NULL) {
			ieee8021PbVidTranslationTable_destroy(&StorageTmp->ieee8021PbVidTranslationTable_old);
			StorageTmp->ieee8021PbVidTranslationTable_rsvs = 0;
			StorageTmp->ieee8021PbVidTranslationTable_tsts = 0;
			StorageTmp->ieee8021PbVidTranslationTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbVidTranslationTable_sets == 0)
			revert_ieee8021PbVidTranslationTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbVidTranslationRelayVid = StorageOld->ieee8021PbVidTranslationRelayVid;
		if (--StorageTmp->ieee8021PbVidTranslationTable_rsvs == 0)
			ieee8021PbVidTranslationTable_destroy(&StorageTmp->ieee8021PbVidTranslationTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCVidRegistrationSVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbCVidRegistrationSVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCVidRegistrationSVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbCVidRegistrationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbCVidRegistrationRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationSVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationSVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationSVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationSVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			if (StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old = ieee8021PbCVidRegistrationTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbCVidRegistrationTable_rsvs++;
		StorageTmp->ieee8021PbCVidRegistrationSVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_tsts == 0)
				if ((ret = check_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCVidRegistrationTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbCVidRegistrationSVid for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_sets == 0)
				if ((ret = update_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCVidRegistrationTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
			StorageTmp->ieee8021PbCVidRegistrationTable_rsvs = 0;
			StorageTmp->ieee8021PbCVidRegistrationTable_tsts = 0;
			StorageTmp->ieee8021PbCVidRegistrationTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbCVidRegistrationTable_sets == 0)
			revert_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbCVidRegistrationSVid = StorageOld->ieee8021PbCVidRegistrationSVid;
		if (--StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
			ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCVidRegistrationUntaggedPep(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbCVidRegistrationUntaggedPep(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCVidRegistrationUntaggedPep entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbCVidRegistrationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbCVidRegistrationRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedPep: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedPep not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedPep: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedPep: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			if (StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old = ieee8021PbCVidRegistrationTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbCVidRegistrationTable_rsvs++;
		StorageTmp->ieee8021PbCVidRegistrationUntaggedPep = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_tsts == 0)
				if ((ret = check_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCVidRegistrationTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbCVidRegistrationUntaggedPep for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_sets == 0)
				if ((ret = update_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCVidRegistrationTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
			StorageTmp->ieee8021PbCVidRegistrationTable_rsvs = 0;
			StorageTmp->ieee8021PbCVidRegistrationTable_tsts = 0;
			StorageTmp->ieee8021PbCVidRegistrationTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbCVidRegistrationTable_sets == 0)
			revert_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbCVidRegistrationUntaggedPep = StorageOld->ieee8021PbCVidRegistrationUntaggedPep;
		if (--StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
			ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCVidRegistrationUntaggedCep(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbCVidRegistrationUntaggedCep(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCVidRegistrationUntaggedCep entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbCVidRegistrationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbCVidRegistrationRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedCep: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedCep not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedCep: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationUntaggedCep: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			if (StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old = ieee8021PbCVidRegistrationTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbCVidRegistrationTable_rsvs++;
		StorageTmp->ieee8021PbCVidRegistrationUntaggedCep = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_tsts == 0)
				if ((ret = check_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCVidRegistrationTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbCVidRegistrationUntaggedCep for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_sets == 0)
				if ((ret = update_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCVidRegistrationTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
			ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
			StorageTmp->ieee8021PbCVidRegistrationTable_rsvs = 0;
			StorageTmp->ieee8021PbCVidRegistrationTable_tsts = 0;
			StorageTmp->ieee8021PbCVidRegistrationTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbCVidRegistrationTable_sets == 0)
			revert_ieee8021PbCVidRegistrationTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbCVidRegistrationUntaggedCep = StorageOld->ieee8021PbCVidRegistrationUntaggedCep;
		if (--StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
			ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbEdgePortPVID(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbEdgePortPVID(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbEdgePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbEdgePortPVID entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbEdgePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortPVID not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortPVID: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortPVID: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			if (StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old = ieee8021PbEdgePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbEdgePortTable_rsvs++;
		StorageTmp->ieee8021PbEdgePortPVID = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbEdgePortTable_tsts == 0)
				if ((ret = check_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbEdgePortPVID for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbEdgePortTable_sets == 0)
				if ((ret = update_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
			StorageTmp->ieee8021PbEdgePortTable_rsvs = 0;
			StorageTmp->ieee8021PbEdgePortTable_tsts = 0;
			StorageTmp->ieee8021PbEdgePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbEdgePortTable_sets == 0)
			revert_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbEdgePortPVID = StorageOld->ieee8021PbEdgePortPVID;
		if (--StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbEdgePortDefaultUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbEdgePortDefaultUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbEdgePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbEdgePortDefaultUserPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbEdgePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortDefaultUserPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortDefaultUserPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortDefaultUserPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			if (StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old = ieee8021PbEdgePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbEdgePortTable_rsvs++;
		StorageTmp->ieee8021PbEdgePortDefaultUserPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbEdgePortTable_tsts == 0)
				if ((ret = check_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbEdgePortDefaultUserPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbEdgePortTable_sets == 0)
				if ((ret = update_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
			StorageTmp->ieee8021PbEdgePortTable_rsvs = 0;
			StorageTmp->ieee8021PbEdgePortTable_tsts = 0;
			StorageTmp->ieee8021PbEdgePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbEdgePortTable_sets == 0)
			revert_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbEdgePortDefaultUserPriority = StorageOld->ieee8021PbEdgePortDefaultUserPriority;
		if (--StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbEdgePortAcceptableFrameTypes(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbEdgePortAcceptableFrameTypes(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbEdgePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbEdgePortAcceptableFrameTypes entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbEdgePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortAcceptableFrameTypes not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortAcceptableFrameTypes: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value admitAll */
		switch (set_value) {
		case IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES_ADMITALL:
		case IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES_ADMITUNTAGGEDANDPRIORITY:
		case IEEE8021PBEDGEPORTACCEPTABLEFRAMETYPES_ADMITTAGGED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortAcceptableFrameTypes: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			if (StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old = ieee8021PbEdgePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbEdgePortTable_rsvs++;
		StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbEdgePortTable_tsts == 0)
				if ((ret = check_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbEdgePortTable_sets == 0)
				if ((ret = update_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
			StorageTmp->ieee8021PbEdgePortTable_rsvs = 0;
			StorageTmp->ieee8021PbEdgePortTable_tsts = 0;
			StorageTmp->ieee8021PbEdgePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbEdgePortTable_sets == 0)
			revert_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbEdgePortAcceptableFrameTypes = StorageOld->ieee8021PbEdgePortAcceptableFrameTypes;
		if (--StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbEdgePortEnableIngressFiltering(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbEdgePortEnableIngressFiltering(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbEdgePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbEdgePortEnableIngressFiltering entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbEdgePortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortEnableIngressFiltering not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortEnableIngressFiltering: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbEdgePortEnableIngressFiltering: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			if (StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old = ieee8021PbEdgePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbEdgePortTable_rsvs++;
		StorageTmp->ieee8021PbEdgePortEnableIngressFiltering = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbEdgePortTable_tsts == 0)
				if ((ret = check_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbEdgePortEnableIngressFiltering for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbEdgePortTable_sets == 0)
				if ((ret = update_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbEdgePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) != NULL) {
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
			StorageTmp->ieee8021PbEdgePortTable_rsvs = 0;
			StorageTmp->ieee8021PbEdgePortTable_tsts = 0;
			StorageTmp->ieee8021PbEdgePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbEdgePortTable_sets == 0)
			revert_ieee8021PbEdgePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbEdgePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbEdgePortEnableIngressFiltering = StorageOld->ieee8021PbEdgePortEnableIngressFiltering;
		if (--StorageTmp->ieee8021PbEdgePortTable_rsvs == 0)
			ieee8021PbEdgePortTable_destroy(&StorageTmp->ieee8021PbEdgePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbServicePriorityRegenerationRegeneratedPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbServicePriorityRegenerationRegeneratedPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbServicePriorityRegenerationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbServicePriorityRegenerationRegeneratedPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbServicePriorityRegenerationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbServicePriorityRegenerationRegeneratedPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbServicePriorityRegenerationRegeneratedPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbServicePriorityRegenerationRegeneratedPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old) == NULL)
			if (StorageTmp->ieee8021PbServicePriorityRegenerationTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old = ieee8021PbServicePriorityRegenerationTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbServicePriorityRegenerationTable_rsvs++;
		StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbServicePriorityRegenerationTable_tsts == 0)
				if ((ret = check_ieee8021PbServicePriorityRegenerationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbServicePriorityRegenerationTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority for you to use, and you have just been asked to do something
				   with it.  Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbServicePriorityRegenerationTable_sets == 0)
				if ((ret = update_ieee8021PbServicePriorityRegenerationTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbServicePriorityRegenerationTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old) != NULL) {
			ieee8021PbServicePriorityRegenerationTable_destroy(&StorageTmp->ieee8021PbServicePriorityRegenerationTable_old);
			StorageTmp->ieee8021PbServicePriorityRegenerationTable_rsvs = 0;
			StorageTmp->ieee8021PbServicePriorityRegenerationTable_tsts = 0;
			StorageTmp->ieee8021PbServicePriorityRegenerationTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbServicePriorityRegenerationTable_sets == 0)
			revert_ieee8021PbServicePriorityRegenerationTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbServicePriorityRegenerationTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbServicePriorityRegenerationRegeneratedPriority = StorageOld->ieee8021PbServicePriorityRegenerationRegeneratedPriority;
		if (--StorageTmp->ieee8021PbServicePriorityRegenerationTable_rsvs == 0)
			ieee8021PbServicePriorityRegenerationTable_destroy(&StorageTmp->ieee8021PbServicePriorityRegenerationTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCnpCComponentId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbCnpCComponentId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCnpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCnpCComponentId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbCnpTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbCnpRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpCComponentId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpCComponentId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpCComponentId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..-1 */
		if ((1 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpCComponentId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
			if (StorageTmp->ieee8021PbCnpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old = ieee8021PbCnpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbCnpTable_rsvs++;
		StorageTmp->ieee8021PbCnpCComponentId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbCnpTable_tsts == 0)
				if ((ret = check_ieee8021PbCnpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCnpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbCnpCComponentId for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbCnpTable_sets == 0)
				if ((ret = update_ieee8021PbCnpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCnpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
			ieee8021PbCnpTable_destroy(&StorageTmp->ieee8021PbCnpTable_old);
			StorageTmp->ieee8021PbCnpTable_rsvs = 0;
			StorageTmp->ieee8021PbCnpTable_tsts = 0;
			StorageTmp->ieee8021PbCnpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbCnpTable_sets == 0)
			revert_ieee8021PbCnpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbCnpCComponentId = StorageOld->ieee8021PbCnpCComponentId;
		if (--StorageTmp->ieee8021PbCnpTable_rsvs == 0)
			ieee8021PbCnpTable_destroy(&StorageTmp->ieee8021PbCnpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCnpSVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbCnpSVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCnpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCnpSVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbCnpTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbCnpRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpSVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpSVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpSVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..0 1..4094 */
		if (set_value != 0 && (1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpSVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
			if (StorageTmp->ieee8021PbCnpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old = ieee8021PbCnpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbCnpTable_rsvs++;
		StorageTmp->ieee8021PbCnpSVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbCnpTable_tsts == 0)
				if ((ret = check_ieee8021PbCnpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCnpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbCnpSVid for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbCnpTable_sets == 0)
				if ((ret = update_ieee8021PbCnpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbCnpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
			ieee8021PbCnpTable_destroy(&StorageTmp->ieee8021PbCnpTable_old);
			StorageTmp->ieee8021PbCnpTable_rsvs = 0;
			StorageTmp->ieee8021PbCnpTable_tsts = 0;
			StorageTmp->ieee8021PbCnpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbCnpTable_sets == 0)
			revert_ieee8021PbCnpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbCnpSVid = StorageOld->ieee8021PbCnpSVid;
		if (--StorageTmp->ieee8021PbCnpTable_rsvs == 0)
			ieee8021PbCnpTable_destroy(&StorageTmp->ieee8021PbCnpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbTiInternalPortType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbTiInternalPortType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbTiInternalPortType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbInternalInterfaceTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbTiRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalPortType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalPortType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalPortType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021PBTIINTERNALPORTTYPE_NONE:
		case IEEE8021PBTIINTERNALPORTTYPE_CUSTOMERVLANPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_PROVIDERNETWORKPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_CUSTOMERNETWORKPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_CUSTOMEREDGEPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_CUSTOMERBACKBONEPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_VIRTUALINSTANCEPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_DBRIDGEPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_REMOTECUSTOMERACCESSPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_STATIONFACINGBRIDGEPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_UPLINKACCESSPORT:
		case IEEE8021PBTIINTERNALPORTTYPE_UPLINKRELAYPORT:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalPortType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
			if (StorageTmp->ieee8021PbInternalInterfaceTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old = ieee8021PbInternalInterfaceTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbInternalInterfaceTable_rsvs++;
		StorageTmp->ieee8021PbTiInternalPortType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbInternalInterfaceTable_tsts == 0)
				if ((ret = check_ieee8021PbInternalInterfaceTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbInternalInterfaceTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbTiInternalPortType for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbInternalInterfaceTable_sets == 0)
				if ((ret = update_ieee8021PbInternalInterfaceTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbInternalInterfaceTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
			ieee8021PbInternalInterfaceTable_destroy(&StorageTmp->ieee8021PbInternalInterfaceTable_old);
			StorageTmp->ieee8021PbInternalInterfaceTable_rsvs = 0;
			StorageTmp->ieee8021PbInternalInterfaceTable_tsts = 0;
			StorageTmp->ieee8021PbInternalInterfaceTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbInternalInterfaceTable_sets == 0)
			revert_ieee8021PbInternalInterfaceTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbTiInternalPortType = StorageOld->ieee8021PbTiInternalPortType;
		if (--StorageTmp->ieee8021PbInternalInterfaceTable_rsvs == 0)
			ieee8021PbInternalInterfaceTable_destroy(&StorageTmp->ieee8021PbInternalInterfaceTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbTiInternalSVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbTiInternalSVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbTiInternalSVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbInternalInterfaceTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbTiRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalSVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalSVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalSVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..0 1..4094 */
		if (set_value != 0 && (1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiInternalSVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
			if (StorageTmp->ieee8021PbInternalInterfaceTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old = ieee8021PbInternalInterfaceTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbInternalInterfaceTable_rsvs++;
		StorageTmp->ieee8021PbTiInternalSVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbInternalInterfaceTable_tsts == 0)
				if ((ret = check_ieee8021PbInternalInterfaceTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbInternalInterfaceTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbTiInternalSVid for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbInternalInterfaceTable_sets == 0)
				if ((ret = update_ieee8021PbInternalInterfaceTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbInternalInterfaceTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
			ieee8021PbInternalInterfaceTable_destroy(&StorageTmp->ieee8021PbInternalInterfaceTable_old);
			StorageTmp->ieee8021PbInternalInterfaceTable_rsvs = 0;
			StorageTmp->ieee8021PbInternalInterfaceTable_tsts = 0;
			StorageTmp->ieee8021PbInternalInterfaceTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbInternalInterfaceTable_sets == 0)
			revert_ieee8021PbInternalInterfaceTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbTiInternalSVid = StorageOld->ieee8021PbTiInternalSVid;
		if (--StorageTmp->ieee8021PbInternalInterfaceTable_rsvs == 0)
			ieee8021PbInternalInterfaceTable_destroy(&StorageTmp->ieee8021PbInternalInterfaceTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbVidTranslationTable_row(struct ieee8021PbVidTranslationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbCVidRegistrationTable_row(struct ieee8021PbCVidRegistrationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbCnpTable_row(struct ieee8021PbCnpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbPnpTable_row(struct ieee8021PbPnpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbCepTable_row(struct ieee8021PbCepTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbRcapTable_row(struct ieee8021PbRcapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbInternalInterfaceTable_row(struct ieee8021PbInternalInterfaceTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbVidTranslationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbVidTranslationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbVidTranslationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbVidTranslationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbVidTranslationRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbVidTranslationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbVidTranslationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbVidTranslationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbVidTranslationLocalVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbVidTranslationLocalVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbVidTranslationLocalVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbVidTranslationLocalVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbVidTranslationTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbVidTranslationTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbVidTranslationLocalVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbVidTranslationTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbVidTranslationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbVidTranslationTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) == NULL)
				if (StorageTmp->ieee8021PbVidTranslationTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old = ieee8021PbVidTranslationTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbVidTranslationTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbVidTranslationTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbVidTranslationTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbVidTranslationTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbVidTranslationRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbVidTranslationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbVidTranslationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbVidTranslationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbVidTranslationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbVidTranslationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbVidTranslationRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbVidTranslationTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbVidTranslationRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbVidTranslationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbVidTranslationRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbVidTranslationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbVidTranslationTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbVidTranslationRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbVidTranslationRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbVidTranslationRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) != NULL) {
				ieee8021PbVidTranslationTable_destroy(&StorageTmp->ieee8021PbVidTranslationTable_old);
				StorageTmp->ieee8021PbVidTranslationTable_rsvs = 0;
				StorageTmp->ieee8021PbVidTranslationTable_tsts = 0;
				StorageTmp->ieee8021PbVidTranslationTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbVidTranslationTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbVidTranslationTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbVidTranslationRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbVidTranslationTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbVidTranslationRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbVidTranslationTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbVidTranslationTable_del(StorageNew);
				ieee8021PbVidTranslationTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbVidTranslationTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbVidTranslationTable_rsvs == 0)
				ieee8021PbVidTranslationTable_destroy(&StorageTmp->ieee8021PbVidTranslationTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbVidTranslationTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCVidRegistrationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbCVidRegistrationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCVidRegistrationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbCVidRegistrationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCVidRegistrationRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbCVidRegistrationTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCVidRegistrationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbCVidRegistrationCVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbCVidRegistrationCVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbCVidRegistrationCVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbCVidRegistrationCVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbCVidRegistrationTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbCVidRegistrationTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbCVidRegistrationCVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbCVidRegistrationTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbCVidRegistrationTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
				if (StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old = ieee8021PbCVidRegistrationTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbCVidRegistrationTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbCVidRegistrationTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbCVidRegistrationTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbCVidRegistrationTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbCVidRegistrationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbCVidRegistrationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbCVidRegistrationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbCVidRegistrationRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbCVidRegistrationTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbCVidRegistrationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbCVidRegistrationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbCVidRegistrationTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbCVidRegistrationRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbCVidRegistrationRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbCVidRegistrationRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) != NULL) {
				ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
				StorageTmp->ieee8021PbCVidRegistrationTable_rsvs = 0;
				StorageTmp->ieee8021PbCVidRegistrationTable_tsts = 0;
				StorageTmp->ieee8021PbCVidRegistrationTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbCVidRegistrationTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbCVidRegistrationTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbCVidRegistrationTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbCVidRegistrationRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbCVidRegistrationTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbCVidRegistrationTable_del(StorageNew);
				ieee8021PbCVidRegistrationTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbCVidRegistrationTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbCVidRegistrationTable_rsvs == 0)
				ieee8021PbCVidRegistrationTable_destroy(&StorageTmp->ieee8021PbCVidRegistrationTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbCVidRegistrationTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCnpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbCnpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCnpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbCnpTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCnpRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbCnpTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCnpRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbCnpTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbCnpTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbCnpTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbCnpRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbCnpTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
				if (StorageTmp->ieee8021PbCnpTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old = ieee8021PbCnpTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbCnpTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbCnpTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbCnpTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbCnpTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbCnpRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbCnpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbCnpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbCnpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbCnpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbCnpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbCnpRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbCnpTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbCnpRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbCnpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbCnpRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbCnpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbCnpTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbCnpRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbCnpRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbCnpRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) != NULL) {
				ieee8021PbCnpTable_destroy(&StorageTmp->ieee8021PbCnpTable_old);
				StorageTmp->ieee8021PbCnpTable_rsvs = 0;
				StorageTmp->ieee8021PbCnpTable_tsts = 0;
				StorageTmp->ieee8021PbCnpTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbCnpTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbCnpTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbCnpRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbCnpTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbCnpRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbCnpTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbCnpTable_del(StorageNew);
				ieee8021PbCnpTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbCnpTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbCnpTable_rsvs == 0)
				ieee8021PbCnpTable_destroy(&StorageTmp->ieee8021PbCnpTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbCnpTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbPnpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbPnpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbPnpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbPnpTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbPnpRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbPnpTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbPnpRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbPnpRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbPnpTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbPnpTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbPnpTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbPnpRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbPnpTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbPnpTable_old) == NULL)
				if (StorageTmp->ieee8021PbPnpTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbPnpTable_old = ieee8021PbPnpTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbPnpTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbPnpTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbPnpTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbPnpTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbPnpRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbPnpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbPnpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbPnpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbPnpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbPnpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbPnpRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbPnpTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbPnpRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbPnpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbPnpRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbPnpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbPnpTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbPnpRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbPnpRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbPnpRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbPnpTable_old) != NULL) {
				ieee8021PbPnpTable_destroy(&StorageTmp->ieee8021PbPnpTable_old);
				StorageTmp->ieee8021PbPnpTable_rsvs = 0;
				StorageTmp->ieee8021PbPnpTable_tsts = 0;
				StorageTmp->ieee8021PbPnpTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbPnpTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbPnpTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbPnpRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbPnpTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbPnpRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbPnpTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbPnpTable_del(StorageNew);
				ieee8021PbPnpTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbPnpTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbPnpTable_rsvs == 0)
				ieee8021PbPnpTable_destroy(&StorageTmp->ieee8021PbPnpTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbPnpTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbCepRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbCepRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbCepTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbCepTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbCepRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbCepTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCepRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbCepRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbCepTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbCepTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbCepTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbCepRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbCepTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbCepTable_old) == NULL)
				if (StorageTmp->ieee8021PbCepTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbCepTable_old = ieee8021PbCepTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbCepTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbCepTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbCepTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbCepTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbCepRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbCepTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbCepRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbCepTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbCepRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbCepTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbCepRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbCepTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbCepRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbCepTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbCepRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbCepTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbCepTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbCepRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbCepRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbCepRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbCepTable_old) != NULL) {
				ieee8021PbCepTable_destroy(&StorageTmp->ieee8021PbCepTable_old);
				StorageTmp->ieee8021PbCepTable_rsvs = 0;
				StorageTmp->ieee8021PbCepTable_tsts = 0;
				StorageTmp->ieee8021PbCepTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbCepTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbCepTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbCepRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbCepTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbCepRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbCepTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbCepTable_del(StorageNew);
				ieee8021PbCepTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbCepTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbCepTable_rsvs == 0)
				ieee8021PbCepTable_destroy(&StorageTmp->ieee8021PbCepTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbCepTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbRcapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbRcapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbRcapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbRcapTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbRcapRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbRcapTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbRcapRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbRcapRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbRcapTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbRcapTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbRcapTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbRcapRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbRcapTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbRcapTable_old) == NULL)
				if (StorageTmp->ieee8021PbRcapTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbRcapTable_old = ieee8021PbRcapTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbRcapTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbRcapTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbRcapTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbRcapTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbRcapRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbRcapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbRcapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbRcapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbRcapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbRcapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbRcapRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbRcapTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbRcapRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbRcapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbRcapRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbRcapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbRcapTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbRcapRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbRcapRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbRcapRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbRcapTable_old) != NULL) {
				ieee8021PbRcapTable_destroy(&StorageTmp->ieee8021PbRcapTable_old);
				StorageTmp->ieee8021PbRcapTable_rsvs = 0;
				StorageTmp->ieee8021PbRcapTable_tsts = 0;
				StorageTmp->ieee8021PbRcapTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbRcapTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbRcapTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbRcapRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbRcapTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbRcapRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbRcapTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbRcapTable_del(StorageNew);
				ieee8021PbRcapTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbRcapTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbRcapTable_rsvs == 0)
				ieee8021PbRcapTable_destroy(&StorageTmp->ieee8021PbRcapTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbRcapTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbTiRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbTiRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbInternalInterfaceTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbInternalInterfaceTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbMib", "write_ieee8021PbTiRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbInternalInterfaceTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbTiRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbTiExternalSVid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbTiExternalSVid */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbTiExternalSVid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbTiExternalSVid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbInternalInterfaceTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbInternalInterfaceTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbTiExternalSVid = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbInternalInterfaceTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbTiRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbInternalInterfaceTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
				if (StorageTmp->ieee8021PbInternalInterfaceTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old = ieee8021PbInternalInterfaceTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbInternalInterfaceTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbInternalInterfaceTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbInternalInterfaceTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbInternalInterfaceTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbTiRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbInternalInterfaceTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbTiRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbInternalInterfaceTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbTiRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbInternalInterfaceTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbTiRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbInternalInterfaceTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbTiRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbInternalInterfaceTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbTiRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbInternalInterfaceTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbInternalInterfaceTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbTiRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbTiRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbTiRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) != NULL) {
				ieee8021PbInternalInterfaceTable_destroy(&StorageTmp->ieee8021PbInternalInterfaceTable_old);
				StorageTmp->ieee8021PbInternalInterfaceTable_rsvs = 0;
				StorageTmp->ieee8021PbInternalInterfaceTable_tsts = 0;
				StorageTmp->ieee8021PbInternalInterfaceTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbInternalInterfaceTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbInternalInterfaceTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbTiRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbInternalInterfaceTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbTiRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbInternalInterfaceTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbInternalInterfaceTable_del(StorageNew);
				ieee8021PbInternalInterfaceTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbInternalInterfaceTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbInternalInterfaceTable_rsvs == 0)
				ieee8021PbInternalInterfaceTable_destroy(&StorageTmp->ieee8021PbInternalInterfaceTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbInternalInterfaceTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021PbMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021PbMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021PbMibold_signal_handler != NULL)
		(*ieee8021PbMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
}

/**
 * @fn void ieee8021PbMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021PbMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021PbMib", "ieee8021PbMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021PbMib", "done.\n"));
	return;
}
