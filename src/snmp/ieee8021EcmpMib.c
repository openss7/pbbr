/*****************************************************************************

 @(#) src/snmp/ieee8021ecmpmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021ecmpmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021EcmpMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021EcmpMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021EcmpMib_refresh = 1;
volatile int ieee8021QBridgeEcmpFdbTable_refresh = 1;
volatile int ieee8021EcmpFlowFilterCtlTable_refresh = 1;
volatile int ieee8021EcmpEctStaticTable_refresh = 1;
volatile int ieee8021EcmpTopSrvTable_refresh = 1;
volatile int ieee8021QBridgePortVlanTtlStatisticsTable_refresh = 1;

/*
 * ieee8021EcmpMib_variables_oid: object identifier for ieee8021EcmpMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021EcmpMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 28 };
oid ieee8021EcmpEctStaticTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 1, 3, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021QBridgeEcmpFdbGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 1, 1 };
oid ieee8021EcmpFlowFilterCtlGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 1, 2 };
oid ieee8021EcmpEctStaticGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 1, 3 };
oid ieee8021EcmpTopSrvGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 1, 4 };
oid ieee8021QBridgePortVlanTtlStatisticsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 1, 5 };
oid ieee8021EcmpCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 2, 1 };
oid ieee8021EcmpFlowFilteringCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 28, 2, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021EcmpMib_variables: tree for ieee8021EcmpMib
 * This variable defines function callbacks and type return information for the ieee8021EcmpMib mib section
 */
struct variable4 ieee8021EcmpMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021QBRIDGEECMPFDBPORTLIST  1
	{(u_char) IEEE8021QBRIDGEECMPFDBPORTLIST, ASN_OCTET_STR, RONLY, var_ieee8021QBridgeEcmpFdbTable, 4, {1, 1, 1, 1}},
#define   IEEE8021ECMPFLOWFILTERCTLENABLED  2
	{(u_char) IEEE8021ECMPFLOWFILTERCTLENABLED, ASN_INTEGER, RONLY, var_ieee8021EcmpFlowFilterCtlTable, 4, {1, 2, 1, 2}},
#define   IEEE8021ECMPFLOWFILTERCTLHASHGEN  3
	{(u_char) IEEE8021ECMPFLOWFILTERCTLHASHGEN, ASN_INTEGER, RONLY, var_ieee8021EcmpFlowFilterCtlTable, 4, {1, 2, 1, 3}},
#define   IEEE8021ECMPFLOWFILTERCTLTTL  4
	{(u_char) IEEE8021ECMPFLOWFILTERCTLTTL, ASN_INTEGER, RWRITE, var_ieee8021EcmpFlowFilterCtlTable, 4, {1, 2, 1, 4}},
#define   IEEE8021ECMPECTSTATICENTRYBRIDGEPRIORITY  5
	{(u_char) IEEE8021ECMPECTSTATICENTRYBRIDGEPRIORITY, ASN_OCTET_STR, RWRITE, var_ieee8021EcmpEctStaticTable, 4, {1, 3, 1, 2}},
#define   IEEE8021ECMPECTSTATICENTRYROWSTATUS  6
	{(u_char) IEEE8021ECMPECTSTATICENTRYROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021EcmpEctStaticTable, 4, {1, 3, 1, 3}},
#define   IEEE8021ECMPTOPSRVENTRYTSBIT  7
	{(u_char) IEEE8021ECMPTOPSRVENTRYTSBIT, ASN_INTEGER, RONLY, var_ieee8021EcmpTopSrvTable, 4, {1, 4, 1, 1}},
#define   IEEE8021ECMPTOPSRVENTRYTIEBREAKMASK  8
	{(u_char) IEEE8021ECMPTOPSRVENTRYTIEBREAKMASK, ASN_INTEGER, RONLY, var_ieee8021EcmpTopSrvTable, 4, {1, 4, 1, 2}},
#define   IEEE8021QBRIDGETPVLANPORTTTLDISCARDS  9
	{(u_char) IEEE8021QBRIDGETPVLANPORTTTLDISCARDS, ASN_COUNTER64, RONLY, var_ieee8021QBridgePortVlanTtlStatisticsTable, 4, {1, 5, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct ieee8021EcmpMib_data *ieee8021EcmpMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021QBridgeEcmpFdbTableStorage = NULL;
struct header_complex_index *ieee8021EcmpFlowFilterCtlTableStorage = NULL;
struct header_complex_index *ieee8021EcmpEctStaticTableStorage = NULL;
struct header_complex_index *ieee8021EcmpTopSrvTableStorage = NULL;
struct header_complex_index *ieee8021QBridgePortVlanTtlStatisticsTableStorage = NULL;

void (*ieee8021EcmpMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021EcmpMib_loop_handler(int);
void ieee8021EcmpMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021EcmpMib(void)
 * @brief ieee8021EcmpMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021EcmpMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021EcmpMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021EcmpMib", "init_ieee8021EcmpMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021EcmpMib", ieee8021EcmpMib_variables, variable4, ieee8021EcmpMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021EcmpMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021EcmpMib", parse_ieee8021EcmpMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgeEcmpFdbTable", parse_ieee8021QBridgeEcmpFdbTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021EcmpFlowFilterCtlTable", parse_ieee8021EcmpFlowFilterCtlTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021EcmpEctStaticTable", parse_ieee8021EcmpEctStaticTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021EcmpTopSrvTable", parse_ieee8021EcmpTopSrvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021QBridgePortVlanTtlStatisticsTable", parse_ieee8021QBridgePortVlanTtlStatisticsTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021EcmpMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgeEcmpFdbTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021EcmpFlowFilterCtlTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021EcmpEctStaticTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021EcmpTopSrvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021QBridgePortVlanTtlStatisticsTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021EcmpMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021EcmpMib_fd_handler, (void *) 1);
	}
	ieee8021EcmpMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021EcmpMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021EcmpMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021EcmpMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021EcmpMib(void)
{
	DEBUGMSGTL(("ieee8021EcmpMib", "deinit_ieee8021EcmpMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021EcmpMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021EcmpMib_variables_oid, sizeof(ieee8021EcmpMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021EcmpMib");
	snmpd_unregister_config_handler("ieee8021QBridgeEcmpFdbTable");
	snmpd_unregister_config_handler("ieee8021EcmpFlowFilterCtlTable");
	snmpd_unregister_config_handler("ieee8021EcmpEctStaticTable");
	snmpd_unregister_config_handler("ieee8021EcmpTopSrvTable");
	snmpd_unregister_config_handler("ieee8021QBridgePortVlanTtlStatisticsTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

int
term_ieee8021EcmpMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021EcmpMib", "term_ieee8021EcmpMib: terminating...  "));
	deinit_ieee8021EcmpMib();
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021EcmpMib_data *ieee8021EcmpMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021EcmpMib.
 *
 * Creates a new ieee8021EcmpMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021EcmpMib.
 */
struct ieee8021EcmpMib_data *
ieee8021EcmpMib_create(void)
{
	struct ieee8021EcmpMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpMib_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021EcmpMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021EcmpMib_data *ieee8021EcmpMib_duplicate(struct ieee8021EcmpMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021EcmpMib_data *
ieee8021EcmpMib_duplicate(struct ieee8021EcmpMib_data *thedata)
{
	struct ieee8021EcmpMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpMib_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021EcmpMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021EcmpMib_destroy(struct ieee8021EcmpMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021EcmpMib.
 * @brief delete a scalars structure from ieee8021EcmpMib.
 *
 * Frees scalars that were previously removed from ieee8021EcmpMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021EcmpMib_destroy(struct ieee8021EcmpMib_data **thedata)
{
	struct ieee8021EcmpMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpMib_add(struct ieee8021EcmpMib_data *thedata)
 * @param thedata the structure representing ieee8021EcmpMib scalars.
 * @brief adds node to the ieee8021EcmpMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021EcmpMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021EcmpMib_add(struct ieee8021EcmpMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpMib_add: adding data...  "));
	if (thedata)
		ieee8021EcmpMibStorage = thedata;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021EcmpMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021EcmpMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021EcmpMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021EcmpMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021EcmpMib_data *StorageTmp = ieee8021EcmpMib_create();

	DEBUGMSGTL(("ieee8021EcmpMib", "parse_ieee8021EcmpMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021EcmpMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/*
 * store_ieee8021EcmpMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021EcmpMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021EcmpMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021EcmpMib", "store_ieee8021EcmpMib: storing data...  "));
	refresh_ieee8021EcmpMib(1);
	if ((StorageTmp = ieee8021EcmpMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021EcmpMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021EcmpMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021EcmpMib(struct ieee8021EcmpMib_data *StorageTmp, struct ieee8021EcmpMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021EcmpMib(struct ieee8021EcmpMib_data *StorageTmp, struct ieee8021EcmpMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021EcmpMib(struct ieee8021EcmpMib_data *StorageTmp, struct ieee8021EcmpMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021EcmpMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021EcmpMib(struct ieee8021EcmpMib_data *StorageTmp, struct ieee8021EcmpMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021EcmpMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021EcmpMib(struct 
 * @fn void revert_ieee8021EcmpMib(struct ieee8021EcmpMib_data *StorageTmp, struct ieee8021EcmpMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021EcmpMib(struct ieee8021EcmpMib_data *StorageTmp, struct ieee8021EcmpMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021EcmpMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021EcmpMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021EcmpMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021EcmpMib(int force)
{
	if (ieee8021EcmpMibStorage == NULL) {
		struct ieee8021EcmpMib_data *StorageNew;

		if ((StorageNew = ieee8021EcmpMib_create()) == NULL)
			return;
		ieee8021EcmpMibStorage = StorageNew;
		ieee8021EcmpMib_refresh = 1;
	}
	if (!force && ieee8021EcmpMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021EcmpMib", "refresh_ieee8021EcmpMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021EcmpMib_refresh = 0;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021EcmpMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021EcmpMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021EcmpMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021EcmpMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021EcmpMib", "var_ieee8021EcmpMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021EcmpMib(0);
	if ((StorageTmp = ieee8021EcmpMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021EcmpMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021EcmpMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021EcmpMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021QBridgeEcmpFdbTable_data *ieee8021QBridgeEcmpFdbTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgeEcmpFdbTable table.
 *
 * Creates a new ieee8021QBridgeEcmpFdbTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgeEcmpFdbTable_data *
ieee8021QBridgeEcmpFdbTable_create(void)
{
	struct ieee8021QBridgeEcmpFdbTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeEcmpFdbTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgeEcmpFdbTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeFdbComponentId = 0;
		StorageNew->ieee8021QBridgeFdbId = 0;
		if ((StorageNew->ieee8021QBridgeTpFdbAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeTpFdbAddressLen = 0;
		StorageNew->ieee8021QBridgeTpFdbAddress[StorageNew->ieee8021QBridgeTpFdbAddressLen] = 0;
		if ((StorageNew->ieee8021QBridgeEcmpFdbPortList = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021QBridgeEcmpFdbPortListLen = 0;
		StorageNew->ieee8021QBridgeEcmpFdbPortList[StorageNew->ieee8021QBridgeEcmpFdbPortListLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgeEcmpFdbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgeEcmpFdbTable_data *ieee8021QBridgeEcmpFdbTable_duplicate(struct ieee8021QBridgeEcmpFdbTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgeEcmpFdbTable_data *
ieee8021QBridgeEcmpFdbTable_duplicate(struct ieee8021QBridgeEcmpFdbTable_data *thedata)
{
	struct ieee8021QBridgeEcmpFdbTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgeEcmpFdbTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgeEcmpFdbTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgeEcmpFdbTable_id = thedata->ieee8021QBridgeEcmpFdbTable_id;
		StorageNew->ieee8021QBridgeFdbComponentId = thedata->ieee8021QBridgeFdbComponentId;
		StorageNew->ieee8021QBridgeFdbId = thedata->ieee8021QBridgeFdbId;
		if (!(StorageNew->ieee8021QBridgeTpFdbAddress = malloc(thedata->ieee8021QBridgeTpFdbAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeTpFdbAddress, thedata->ieee8021QBridgeTpFdbAddress, thedata->ieee8021QBridgeTpFdbAddressLen);
		StorageNew->ieee8021QBridgeTpFdbAddressLen = thedata->ieee8021QBridgeTpFdbAddressLen;
		StorageNew->ieee8021QBridgeTpFdbAddress[StorageNew->ieee8021QBridgeTpFdbAddressLen] = 0;
		if (!(StorageNew->ieee8021QBridgeEcmpFdbPortList = malloc(thedata->ieee8021QBridgeEcmpFdbPortListLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021QBridgeEcmpFdbPortList, thedata->ieee8021QBridgeEcmpFdbPortList, thedata->ieee8021QBridgeEcmpFdbPortListLen);
		StorageNew->ieee8021QBridgeEcmpFdbPortListLen = thedata->ieee8021QBridgeEcmpFdbPortListLen;
		StorageNew->ieee8021QBridgeEcmpFdbPortList[StorageNew->ieee8021QBridgeEcmpFdbPortListLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgeEcmpFdbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgeEcmpFdbTable_destroy(struct ieee8021QBridgeEcmpFdbTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgeEcmpFdbTable_destroy(struct ieee8021QBridgeEcmpFdbTable_data **thedata)
{
	struct ieee8021QBridgeEcmpFdbTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgeEcmpFdbTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021QBridgeTpFdbAddress);
		StorageDel->ieee8021QBridgeTpFdbAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021QBridgeEcmpFdbPortList);
		StorageDel->ieee8021QBridgeEcmpFdbPortListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeEcmpFdbTable_add(struct ieee8021QBridgeEcmpFdbTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgeEcmpFdbTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgeEcmpFdbTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgeEcmpFdbTable_add(struct ieee8021QBridgeEcmpFdbTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgeEcmpFdbTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeFdbComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeFdbComponentId, sizeof(thedata->ieee8021QBridgeFdbComponentId));
		/* ieee8021QBridgeFdbId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeFdbId, sizeof(thedata->ieee8021QBridgeFdbId));
		/* ieee8021QBridgeTpFdbAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021QBridgeTpFdbAddress, thedata->ieee8021QBridgeTpFdbAddressLen);
		header_complex_add_data(&ieee8021QBridgeEcmpFdbTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgeEcmpFdbTable_del(struct ieee8021QBridgeEcmpFdbTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgeEcmpFdbTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgeEcmpFdbTable_del(struct ieee8021QBridgeEcmpFdbTable_data *thedata)
{
	struct ieee8021QBridgeEcmpFdbTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgeEcmpFdbTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgeEcmpFdbTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgeEcmpFdbTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgeEcmpFdbTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgeEcmpFdbTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgeEcmpFdbTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgeEcmpFdbTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp = ieee8021QBridgeEcmpFdbTable_create();

	DEBUGMSGTL(("ieee8021EcmpMib", "parse_ieee8021QBridgeEcmpFdbTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeFdbComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeFdbId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021QBridgeTpFdbAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeTpFdbAddress, &StorageTmp->ieee8021QBridgeTpFdbAddressLen);
	if (StorageTmp->ieee8021QBridgeTpFdbAddress == NULL) {
		config_perror("invalid specification for ieee8021QBridgeTpFdbAddress");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021QBridgeEcmpFdbPortList);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021QBridgeEcmpFdbPortList, &StorageTmp->ieee8021QBridgeEcmpFdbPortListLen);
	if (StorageTmp->ieee8021QBridgeEcmpFdbPortList == NULL) {
		config_perror("invalid specification for ieee8021QBridgeEcmpFdbPortList");
		return;
	}
	ieee8021QBridgeEcmpFdbTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/*
 * store_ieee8021QBridgeEcmpFdbTable(): store configuraiton file for ieee8021QBridgeEcmpFdbTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgeEcmpFdbTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021EcmpMib", "store_ieee8021QBridgeEcmpFdbTable: storing data...  "));
	refresh_ieee8021QBridgeEcmpFdbTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgeEcmpFdbTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgeEcmpFdbTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgeEcmpFdbTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeFdbComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeFdbId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeTpFdbAddress, &StorageTmp->ieee8021QBridgeTpFdbAddressLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021QBridgeEcmpFdbPortList, &StorageTmp->ieee8021QBridgeEcmpFdbPortListLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021EcmpFlowFilterCtlTable_data *ieee8021EcmpFlowFilterCtlTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021EcmpFlowFilterCtlTable table.
 *
 * Creates a new ieee8021EcmpFlowFilterCtlTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021EcmpFlowFilterCtlTable_data *
ieee8021EcmpFlowFilterCtlTable_create(void)
{
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpFlowFilterCtlTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpFlowFilterCtlTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021EcmpFlowFilterCtlEnabled = 0;
		StorageNew->ieee8021EcmpFlowFilterCtlHashGen = 0;
		StorageNew->ieee8021EcmpFlowFilterCtlTtl = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021EcmpFlowFilterCtlTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021EcmpFlowFilterCtlTable_data *ieee8021EcmpFlowFilterCtlTable_duplicate(struct ieee8021EcmpFlowFilterCtlTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021EcmpFlowFilterCtlTable_data *
ieee8021EcmpFlowFilterCtlTable_duplicate(struct ieee8021EcmpFlowFilterCtlTable_data *thedata)
{
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpFlowFilterCtlTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpFlowFilterCtlTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021EcmpFlowFilterCtlTable_id = thedata->ieee8021EcmpFlowFilterCtlTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021EcmpFlowFilterCtlVid = thedata->ieee8021EcmpFlowFilterCtlVid;
		StorageNew->ieee8021EcmpFlowFilterCtlEnabled = thedata->ieee8021EcmpFlowFilterCtlEnabled;
		StorageNew->ieee8021EcmpFlowFilterCtlHashGen = thedata->ieee8021EcmpFlowFilterCtlHashGen;
		StorageNew->ieee8021EcmpFlowFilterCtlTtl = thedata->ieee8021EcmpFlowFilterCtlTtl;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021EcmpFlowFilterCtlTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021EcmpFlowFilterCtlTable_destroy(struct ieee8021EcmpFlowFilterCtlTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021EcmpFlowFilterCtlTable_destroy(struct ieee8021EcmpFlowFilterCtlTable_data **thedata)
{
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpFlowFilterCtlTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpFlowFilterCtlTable_add(struct ieee8021EcmpFlowFilterCtlTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021EcmpFlowFilterCtlTable table data set.
 *
 * Adds a table row structure to the ieee8021EcmpFlowFilterCtlTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021EcmpFlowFilterCtlTable_add(struct ieee8021EcmpFlowFilterCtlTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpFlowFilterCtlTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021EcmpFlowFilterCtlVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021EcmpFlowFilterCtlVid, sizeof(thedata->ieee8021EcmpFlowFilterCtlVid));
		header_complex_add_data(&ieee8021EcmpFlowFilterCtlTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpFlowFilterCtlTable_del(struct ieee8021EcmpFlowFilterCtlTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021EcmpFlowFilterCtlTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021EcmpFlowFilterCtlTable_del(struct ieee8021EcmpFlowFilterCtlTable_data *thedata)
{
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpFlowFilterCtlTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021EcmpFlowFilterCtlTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021EcmpFlowFilterCtlTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021EcmpFlowFilterCtlTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021EcmpFlowFilterCtlTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021EcmpFlowFilterCtlTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021EcmpFlowFilterCtlTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp = ieee8021EcmpFlowFilterCtlTable_create();

	DEBUGMSGTL(("ieee8021EcmpMib", "parse_ieee8021EcmpFlowFilterCtlTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpFlowFilterCtlVid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpFlowFilterCtlEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpFlowFilterCtlHashGen, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpFlowFilterCtlTtl, &tmpsize);
	ieee8021EcmpFlowFilterCtlTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/*
 * store_ieee8021EcmpFlowFilterCtlTable(): store configuraiton file for ieee8021EcmpFlowFilterCtlTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021EcmpFlowFilterCtlTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021EcmpMib", "store_ieee8021EcmpFlowFilterCtlTable: storing data...  "));
	refresh_ieee8021EcmpFlowFilterCtlTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021EcmpFlowFilterCtlTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021EcmpFlowFilterCtlTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021EcmpFlowFilterCtlTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpFlowFilterCtlVid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpFlowFilterCtlEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpFlowFilterCtlHashGen, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpFlowFilterCtlTtl, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021EcmpEctStaticTable_data *ieee8021EcmpEctStaticTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021EcmpEctStaticTable table.
 *
 * Creates a new ieee8021EcmpEctStaticTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021EcmpEctStaticTable_data *
ieee8021EcmpEctStaticTable_create(void)
{
	struct ieee8021EcmpEctStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpEctStaticTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpEctStaticTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbTopIx = 0;
		if ((StorageNew->ieee8021EcmpEctStaticEntryBridgePriority = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021EcmpEctStaticEntryBridgePriorityLen = 0;
		StorageNew->ieee8021EcmpEctStaticEntryBridgePriority[StorageNew->ieee8021EcmpEctStaticEntryBridgePriorityLen] = 0;
		StorageNew->ieee8021EcmpEctStaticEntryRowStatus = 0;
		StorageNew->ieee8021EcmpEctStaticEntryRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021EcmpEctStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021EcmpEctStaticTable_data *ieee8021EcmpEctStaticTable_duplicate(struct ieee8021EcmpEctStaticTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021EcmpEctStaticTable_data *
ieee8021EcmpEctStaticTable_duplicate(struct ieee8021EcmpEctStaticTable_data *thedata)
{
	struct ieee8021EcmpEctStaticTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpEctStaticTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpEctStaticTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021EcmpEctStaticTable_id = thedata->ieee8021EcmpEctStaticTable_id;
		StorageNew->ieee8021SpbTopIx = thedata->ieee8021SpbTopIx;
		StorageNew->ieee8021EcmpEctStaticEntryTieBreakMask = thedata->ieee8021EcmpEctStaticEntryTieBreakMask;
		if (!(StorageNew->ieee8021EcmpEctStaticEntryBridgePriority = malloc(thedata->ieee8021EcmpEctStaticEntryBridgePriorityLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021EcmpEctStaticEntryBridgePriority, thedata->ieee8021EcmpEctStaticEntryBridgePriority, thedata->ieee8021EcmpEctStaticEntryBridgePriorityLen);
		StorageNew->ieee8021EcmpEctStaticEntryBridgePriorityLen = thedata->ieee8021EcmpEctStaticEntryBridgePriorityLen;
		StorageNew->ieee8021EcmpEctStaticEntryBridgePriority[StorageNew->ieee8021EcmpEctStaticEntryBridgePriorityLen] = 0;
		StorageNew->ieee8021EcmpEctStaticEntryRowStatus = thedata->ieee8021EcmpEctStaticEntryRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021EcmpEctStaticTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021EcmpEctStaticTable_destroy(struct ieee8021EcmpEctStaticTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021EcmpEctStaticTable_destroy(struct ieee8021EcmpEctStaticTable_data **thedata)
{
	struct ieee8021EcmpEctStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpEctStaticTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021EcmpEctStaticEntryBridgePriority);
		StorageDel->ieee8021EcmpEctStaticEntryBridgePriorityLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpEctStaticTable_add(struct ieee8021EcmpEctStaticTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021EcmpEctStaticTable table data set.
 *
 * Adds a table row structure to the ieee8021EcmpEctStaticTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021EcmpEctStaticTable_add(struct ieee8021EcmpEctStaticTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpEctStaticTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbTopIx, sizeof(thedata->ieee8021SpbTopIx));
		/* ieee8021EcmpEctStaticEntryTieBreakMask */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021EcmpEctStaticEntryTieBreakMask, sizeof(thedata->ieee8021EcmpEctStaticEntryTieBreakMask));
		header_complex_add_data(&ieee8021EcmpEctStaticTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpEctStaticTable_del(struct ieee8021EcmpEctStaticTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021EcmpEctStaticTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021EcmpEctStaticTable_del(struct ieee8021EcmpEctStaticTable_data *thedata)
{
	struct ieee8021EcmpEctStaticTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpEctStaticTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021EcmpEctStaticTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021EcmpEctStaticTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021EcmpEctStaticTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021EcmpEctStaticTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021EcmpEctStaticTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021EcmpEctStaticTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021EcmpEctStaticTable_data *StorageTmp = ieee8021EcmpEctStaticTable_create();

	DEBUGMSGTL(("ieee8021EcmpMib", "parse_ieee8021EcmpEctStaticTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbTopIx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpEctStaticEntryTieBreakMask, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority, &StorageTmp->ieee8021EcmpEctStaticEntryBridgePriorityLen);
	if (StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority == NULL) {
		config_perror("invalid specification for ieee8021EcmpEctStaticEntryBridgePriority");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpEctStaticEntryRowStatus, &tmpsize);
	ieee8021EcmpEctStaticTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/*
 * store_ieee8021EcmpEctStaticTable(): store configuraiton file for ieee8021EcmpEctStaticTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021EcmpEctStaticTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021EcmpEctStaticTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021EcmpMib", "store_ieee8021EcmpEctStaticTable: storing data...  "));
	refresh_ieee8021EcmpEctStaticTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021EcmpEctStaticTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021EcmpEctStaticTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021EcmpEctStaticTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpEctStaticEntryTieBreakMask, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority, &StorageTmp->ieee8021EcmpEctStaticEntryBridgePriorityLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpEctStaticEntryRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021EcmpTopSrvTable_data *ieee8021EcmpTopSrvTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021EcmpTopSrvTable table.
 *
 * Creates a new ieee8021EcmpTopSrvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021EcmpTopSrvTable_data *
ieee8021EcmpTopSrvTable_create(void)
{
	struct ieee8021EcmpTopSrvTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpTopSrvTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpTopSrvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021SpbmTopSrvEntryTopIx = 0;
		if ((StorageNew->ieee8021SpbmTopSrvEntrySysId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbmTopSrvEntrySysIdLen = 0;
		StorageNew->ieee8021SpbmTopSrvEntrySysId[StorageNew->ieee8021SpbmTopSrvEntrySysIdLen] = 0;
		StorageNew->ieee8021SpbmTopSrvEntryIsid = 0;
		StorageNew->ieee8021SpbmTopSrvEntryBaseVid = 0;
		if ((StorageNew->ieee8021SpbmTopSrvEntryMac = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021SpbmTopSrvEntryMacLen = 0;
		StorageNew->ieee8021SpbmTopSrvEntryMac[StorageNew->ieee8021SpbmTopSrvEntryMacLen] = 0;
		StorageNew->ieee8021EcmpTopSrvEntryTsBit = 0;
		StorageNew->ieee8021EcmpTopSrvEntryTieBreakMask = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021EcmpTopSrvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021EcmpTopSrvTable_data *ieee8021EcmpTopSrvTable_duplicate(struct ieee8021EcmpTopSrvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021EcmpTopSrvTable_data *
ieee8021EcmpTopSrvTable_duplicate(struct ieee8021EcmpTopSrvTable_data *thedata)
{
	struct ieee8021EcmpTopSrvTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021EcmpTopSrvTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpTopSrvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021EcmpTopSrvTable_id = thedata->ieee8021EcmpTopSrvTable_id;
		StorageNew->ieee8021SpbmTopSrvEntryTopIx = thedata->ieee8021SpbmTopSrvEntryTopIx;
		if (!(StorageNew->ieee8021SpbmTopSrvEntrySysId = malloc(thedata->ieee8021SpbmTopSrvEntrySysIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmTopSrvEntrySysId, thedata->ieee8021SpbmTopSrvEntrySysId, thedata->ieee8021SpbmTopSrvEntrySysIdLen);
		StorageNew->ieee8021SpbmTopSrvEntrySysIdLen = thedata->ieee8021SpbmTopSrvEntrySysIdLen;
		StorageNew->ieee8021SpbmTopSrvEntrySysId[StorageNew->ieee8021SpbmTopSrvEntrySysIdLen] = 0;
		StorageNew->ieee8021SpbmTopSrvEntryIsid = thedata->ieee8021SpbmTopSrvEntryIsid;
		StorageNew->ieee8021SpbmTopSrvEntryBaseVid = thedata->ieee8021SpbmTopSrvEntryBaseVid;
		if (!(StorageNew->ieee8021SpbmTopSrvEntryMac = malloc(thedata->ieee8021SpbmTopSrvEntryMacLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021SpbmTopSrvEntryMac, thedata->ieee8021SpbmTopSrvEntryMac, thedata->ieee8021SpbmTopSrvEntryMacLen);
		StorageNew->ieee8021SpbmTopSrvEntryMacLen = thedata->ieee8021SpbmTopSrvEntryMacLen;
		StorageNew->ieee8021SpbmTopSrvEntryMac[StorageNew->ieee8021SpbmTopSrvEntryMacLen] = 0;
		StorageNew->ieee8021EcmpTopSrvEntryTsBit = thedata->ieee8021EcmpTopSrvEntryTsBit;
		StorageNew->ieee8021EcmpTopSrvEntryTieBreakMask = thedata->ieee8021EcmpTopSrvEntryTieBreakMask;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021EcmpTopSrvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021EcmpTopSrvTable_destroy(struct ieee8021EcmpTopSrvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021EcmpTopSrvTable_destroy(struct ieee8021EcmpTopSrvTable_data **thedata)
{
	struct ieee8021EcmpTopSrvTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpTopSrvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021SpbmTopSrvEntrySysId);
		StorageDel->ieee8021SpbmTopSrvEntrySysIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021SpbmTopSrvEntryMac);
		StorageDel->ieee8021SpbmTopSrvEntryMacLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpTopSrvTable_add(struct ieee8021EcmpTopSrvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021EcmpTopSrvTable table data set.
 *
 * Adds a table row structure to the ieee8021EcmpTopSrvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021EcmpTopSrvTable_add(struct ieee8021EcmpTopSrvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpTopSrvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021SpbmTopSrvEntryTopIx */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbmTopSrvEntryTopIx, sizeof(thedata->ieee8021SpbmTopSrvEntryTopIx));
		/* ieee8021SpbmTopSrvEntrySysId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbmTopSrvEntrySysId, thedata->ieee8021SpbmTopSrvEntrySysIdLen);
		/* ieee8021SpbmTopSrvEntryIsid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021SpbmTopSrvEntryIsid, sizeof(thedata->ieee8021SpbmTopSrvEntryIsid));
		/* ieee8021SpbmTopSrvEntryBaseVid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021SpbmTopSrvEntryBaseVid, sizeof(thedata->ieee8021SpbmTopSrvEntryBaseVid));
		/* ieee8021SpbmTopSrvEntryMac */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->ieee8021SpbmTopSrvEntryMac, thedata->ieee8021SpbmTopSrvEntryMacLen);
		header_complex_add_data(&ieee8021EcmpTopSrvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021EcmpTopSrvTable_del(struct ieee8021EcmpTopSrvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021EcmpTopSrvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021EcmpTopSrvTable_del(struct ieee8021EcmpTopSrvTable_data *thedata)
{
	struct ieee8021EcmpTopSrvTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpTopSrvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021EcmpTopSrvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021EcmpTopSrvTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021EcmpTopSrvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021EcmpTopSrvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021EcmpTopSrvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021EcmpTopSrvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021EcmpTopSrvTable_data *StorageTmp = ieee8021EcmpTopSrvTable_create();

	DEBUGMSGTL(("ieee8021EcmpMib", "parse_ieee8021EcmpTopSrvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbmTopSrvEntryTopIx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbmTopSrvEntrySysId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmTopSrvEntrySysId, &StorageTmp->ieee8021SpbmTopSrvEntrySysIdLen);
	if (StorageTmp->ieee8021SpbmTopSrvEntrySysId == NULL) {
		config_perror("invalid specification for ieee8021SpbmTopSrvEntrySysId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021SpbmTopSrvEntryIsid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021SpbmTopSrvEntryBaseVid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021SpbmTopSrvEntryMac);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021SpbmTopSrvEntryMac, &StorageTmp->ieee8021SpbmTopSrvEntryMacLen);
	if (StorageTmp->ieee8021SpbmTopSrvEntryMac == NULL) {
		config_perror("invalid specification for ieee8021SpbmTopSrvEntryMac");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpTopSrvEntryTsBit, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021EcmpTopSrvEntryTieBreakMask, &tmpsize);
	ieee8021EcmpTopSrvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/*
 * store_ieee8021EcmpTopSrvTable(): store configuraiton file for ieee8021EcmpTopSrvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021EcmpTopSrvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021EcmpTopSrvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021EcmpMib", "store_ieee8021EcmpTopSrvTable: storing data...  "));
	refresh_ieee8021EcmpTopSrvTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021EcmpTopSrvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021EcmpTopSrvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021EcmpTopSrvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryTopIx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmTopSrvEntrySysId, &StorageTmp->ieee8021SpbmTopSrvEntrySysIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryIsid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryBaseVid, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021SpbmTopSrvEntryMac, &StorageTmp->ieee8021SpbmTopSrvEntryMacLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpTopSrvEntryTsBit, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021EcmpTopSrvEntryTieBreakMask, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021QBridgePortVlanTtlStatisticsTable_data *ieee8021QBridgePortVlanTtlStatisticsTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021QBridgePortVlanTtlStatisticsTable table.
 *
 * Creates a new ieee8021QBridgePortVlanTtlStatisticsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021QBridgePortVlanTtlStatisticsTable_data *
ieee8021QBridgePortVlanTtlStatisticsTable_create(void)
{
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgePortVlanTtlStatisticsTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgePortVlanTtlStatisticsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021QBridgeVlanIndex = 0;
		StorageNew->ieee8021QBridgeTpVlanPortTtlDiscards = (struct counter64) {
		0, 0};
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021QBridgePortVlanTtlStatisticsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021QBridgePortVlanTtlStatisticsTable_data *ieee8021QBridgePortVlanTtlStatisticsTable_duplicate(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021QBridgePortVlanTtlStatisticsTable_data *
ieee8021QBridgePortVlanTtlStatisticsTable_duplicate(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *thedata)
{
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021QBridgePortVlanTtlStatisticsTable_data);

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgePortVlanTtlStatisticsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021QBridgePortVlanTtlStatisticsTable_id = thedata->ieee8021QBridgePortVlanTtlStatisticsTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021QBridgeVlanIndex = thedata->ieee8021QBridgeVlanIndex;
		StorageNew->ieee8021QBridgeTpVlanPortTtlDiscards = thedata->ieee8021QBridgeTpVlanPortTtlDiscards;
	}
      done:
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021QBridgePortVlanTtlStatisticsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021QBridgePortVlanTtlStatisticsTable_destroy(struct ieee8021QBridgePortVlanTtlStatisticsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021QBridgePortVlanTtlStatisticsTable_destroy(struct ieee8021QBridgePortVlanTtlStatisticsTable_data **thedata)
{
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgePortVlanTtlStatisticsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgePortVlanTtlStatisticsTable_add(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021QBridgePortVlanTtlStatisticsTable table data set.
 *
 * Adds a table row structure to the ieee8021QBridgePortVlanTtlStatisticsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021QBridgePortVlanTtlStatisticsTable_add(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgePortVlanTtlStatisticsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021QBridgeVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanIndex, sizeof(thedata->ieee8021QBridgeVlanIndex));
		header_complex_add_data(&ieee8021QBridgePortVlanTtlStatisticsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021QBridgePortVlanTtlStatisticsTable_del(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021QBridgePortVlanTtlStatisticsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021QBridgePortVlanTtlStatisticsTable_del(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *thedata)
{
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021QBridgePortVlanTtlStatisticsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021QBridgePortVlanTtlStatisticsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021QBridgePortVlanTtlStatisticsTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021QBridgePortVlanTtlStatisticsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021QBridgePortVlanTtlStatisticsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021QBridgePortVlanTtlStatisticsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021QBridgePortVlanTtlStatisticsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp = ieee8021QBridgePortVlanTtlStatisticsTable_create();

	DEBUGMSGTL(("ieee8021EcmpMib", "parse_ieee8021QBridgePortVlanTtlStatisticsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021QBridgeTpVlanPortTtlDiscards, &tmpsize);
	ieee8021QBridgePortVlanTtlStatisticsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/*
 * store_ieee8021QBridgePortVlanTtlStatisticsTable(): store configuraiton file for ieee8021QBridgePortVlanTtlStatisticsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021QBridgePortVlanTtlStatisticsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021EcmpMib", "store_ieee8021QBridgePortVlanTtlStatisticsTable: storing data...  "));
	refresh_ieee8021QBridgePortVlanTtlStatisticsTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021QBridgePortVlanTtlStatisticsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021QBridgePortVlanTtlStatisticsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021QBridgePortVlanTtlStatisticsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021QBridgeTpVlanPortTtlDiscards, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, struct ieee8021QBridgeEcmpFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, struct ieee8021QBridgeEcmpFdbTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, struct ieee8021QBridgeEcmpFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgeEcmpFdbTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, struct ieee8021QBridgeEcmpFdbTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgeEcmpFdbTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, struct ieee8021QBridgeEcmpFdbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, struct ieee8021QBridgeEcmpFdbTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgeEcmpFdbTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgeEcmpFdbTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgeEcmpFdbTable_data *
refresh_ieee8021QBridgeEcmpFdbTable_row(struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgeEcmpFdbTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgeEcmpFdbTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgeEcmpFdbTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgeEcmpFdbTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgeEcmpFdbTable(int force)
{
	if (!force && ieee8021QBridgeEcmpFdbTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgeEcmpFdbTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgeEcmpFdbTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgeEcmpFdbTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021EcmpMib above.
 */
u_char *
var_ieee8021QBridgeEcmpFdbTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgeEcmpFdbTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021EcmpMib", "var_ieee8021QBridgeEcmpFdbTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgeEcmpFdbTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgeEcmpFdbTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgeEcmpFdbTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGEECMPFDBPORTLIST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021QBridgeEcmpFdbPortListLen;
		rval = (u_char *) StorageTmp->ieee8021QBridgeEcmpFdbPortList;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, struct ieee8021EcmpFlowFilterCtlTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, struct ieee8021EcmpFlowFilterCtlTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, struct ieee8021EcmpFlowFilterCtlTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021EcmpFlowFilterCtlTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, struct ieee8021EcmpFlowFilterCtlTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021EcmpFlowFilterCtlTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, struct ieee8021EcmpFlowFilterCtlTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, struct ieee8021EcmpFlowFilterCtlTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021EcmpFlowFilterCtlTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021EcmpFlowFilterCtlTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021EcmpFlowFilterCtlTable_data *
refresh_ieee8021EcmpFlowFilterCtlTable_row(struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021EcmpFlowFilterCtlTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021EcmpFlowFilterCtlTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021EcmpFlowFilterCtlTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021EcmpFlowFilterCtlTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021EcmpFlowFilterCtlTable(int force)
{
	if (!force && ieee8021EcmpFlowFilterCtlTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021EcmpFlowFilterCtlTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021EcmpFlowFilterCtlTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021EcmpFlowFilterCtlTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021EcmpMib above.
 */
u_char *
var_ieee8021EcmpFlowFilterCtlTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021EcmpMib", "var_ieee8021EcmpFlowFilterCtlTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021EcmpFlowFilterCtlTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021EcmpFlowFilterCtlTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021EcmpFlowFilterCtlTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021ECMPFLOWFILTERCTLENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021EcmpFlowFilterCtlEnabled);
		rval = (u_char *) &StorageTmp->ieee8021EcmpFlowFilterCtlEnabled;
		break;
	case (u_char) IEEE8021ECMPFLOWFILTERCTLHASHGEN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021EcmpFlowFilterCtlHashGen);
		rval = (u_char *) &StorageTmp->ieee8021EcmpFlowFilterCtlHashGen;
		break;
	case (u_char) IEEE8021ECMPFLOWFILTERCTLTTL:	/* ReadWrite */
		*write_method = write_ieee8021EcmpFlowFilterCtlTtl;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021EcmpFlowFilterCtlTtl);
		rval = (u_char *) &StorageTmp->ieee8021EcmpFlowFilterCtlTtl;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, struct ieee8021EcmpEctStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, struct ieee8021EcmpEctStaticTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, struct ieee8021EcmpEctStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021EcmpEctStaticTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, struct ieee8021EcmpEctStaticTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021EcmpEctStaticTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, struct ieee8021EcmpEctStaticTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, struct ieee8021EcmpEctStaticTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021EcmpEctStaticTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021EcmpEctStaticTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021EcmpEctStaticTable_data *
refresh_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021EcmpEctStaticTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021EcmpEctStaticTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021EcmpEctStaticTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021EcmpEctStaticTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021EcmpEctStaticTable(int force)
{
	if (!force && ieee8021EcmpEctStaticTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021EcmpEctStaticTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021EcmpEctStaticTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021EcmpEctStaticTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021EcmpMib above.
 */
u_char *
var_ieee8021EcmpEctStaticTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021EcmpEctStaticTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021EcmpMib", "var_ieee8021EcmpEctStaticTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021EcmpEctStaticTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021EcmpEctStaticTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021EcmpEctStaticTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021ECMPECTSTATICENTRYBRIDGEPRIORITY:	/* Create */
		*write_method = write_ieee8021EcmpEctStaticEntryBridgePriority;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021EcmpEctStaticEntryBridgePriorityLen;
		rval = (u_char *) StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority;
		break;
	case (u_char) IEEE8021ECMPECTSTATICENTRYROWSTATUS:	/* Create */
		*write_method = write_ieee8021EcmpEctStaticEntryRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021EcmpEctStaticEntryRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021EcmpEctStaticEntryRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, struct ieee8021EcmpTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, struct ieee8021EcmpTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, struct ieee8021EcmpTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021EcmpTopSrvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, struct ieee8021EcmpTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021EcmpTopSrvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, struct ieee8021EcmpTopSrvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, struct ieee8021EcmpTopSrvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021EcmpTopSrvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021EcmpTopSrvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021EcmpTopSrvTable_data *
refresh_ieee8021EcmpTopSrvTable_row(struct ieee8021EcmpTopSrvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021EcmpTopSrvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021EcmpTopSrvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021EcmpTopSrvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021EcmpTopSrvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021EcmpTopSrvTable(int force)
{
	if (!force && ieee8021EcmpTopSrvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021EcmpTopSrvTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021EcmpTopSrvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021EcmpTopSrvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021EcmpMib above.
 */
u_char *
var_ieee8021EcmpTopSrvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021EcmpTopSrvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021EcmpMib", "var_ieee8021EcmpTopSrvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021EcmpTopSrvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021EcmpTopSrvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021EcmpTopSrvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021ECMPTOPSRVENTRYTSBIT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021EcmpTopSrvEntryTsBit);
		rval = (u_char *) &StorageTmp->ieee8021EcmpTopSrvEntryTsBit;
		break;
	case (u_char) IEEE8021ECMPTOPSRVENTRYTIEBREAKMASK:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021EcmpTopSrvEntryTieBreakMask);
		rval = (u_char *) &StorageTmp->ieee8021EcmpTopSrvEntryTieBreakMask;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021QBridgePortVlanTtlStatisticsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021QBridgePortVlanTtlStatisticsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021QBridgePortVlanTtlStatisticsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021QBridgePortVlanTtlStatisticsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021QBridgePortVlanTtlStatisticsTable_data *
refresh_ieee8021QBridgePortVlanTtlStatisticsTable_row(struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021QBridgePortVlanTtlStatisticsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021QBridgePortVlanTtlStatisticsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021QBridgePortVlanTtlStatisticsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021QBridgePortVlanTtlStatisticsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021QBridgePortVlanTtlStatisticsTable(int force)
{
	if (!force && ieee8021QBridgePortVlanTtlStatisticsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021QBridgePortVlanTtlStatisticsTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021QBridgePortVlanTtlStatisticsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021QBridgePortVlanTtlStatisticsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021EcmpMib above.
 */
u_char *
var_ieee8021QBridgePortVlanTtlStatisticsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021QBridgePortVlanTtlStatisticsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021EcmpMib", "var_ieee8021QBridgePortVlanTtlStatisticsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021QBridgePortVlanTtlStatisticsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021QBridgePortVlanTtlStatisticsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021QBridgePortVlanTtlStatisticsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021QBRIDGETPVLANPORTTTLDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021QBridgeTpVlanPortTtlDiscards);
		rval = (u_char *) &StorageTmp->ieee8021QBridgeTpVlanPortTtlDiscards;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021EcmpFlowFilterCtlTtl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021EcmpFlowFilterCtlTtl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021EcmpFlowFilterCtlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021EcmpMib", "write_ieee8021EcmpFlowFilterCtlTtl entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021EcmpFlowFilterCtlTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpFlowFilterCtlTtl not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpFlowFilterCtlTtl: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..63 */
		if ((0 > set_value || set_value > 63)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpFlowFilterCtlTtl: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old) == NULL)
			if (StorageTmp->ieee8021EcmpFlowFilterCtlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old = ieee8021EcmpFlowFilterCtlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021EcmpFlowFilterCtlTable_rsvs++;
		StorageTmp->ieee8021EcmpFlowFilterCtlTtl = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021EcmpFlowFilterCtlTable_tsts == 0)
				if ((ret = check_ieee8021EcmpFlowFilterCtlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021EcmpFlowFilterCtlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021EcmpFlowFilterCtlTtl for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021EcmpFlowFilterCtlTable_sets == 0)
				if ((ret = update_ieee8021EcmpFlowFilterCtlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021EcmpFlowFilterCtlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old) != NULL) {
			ieee8021EcmpFlowFilterCtlTable_destroy(&StorageTmp->ieee8021EcmpFlowFilterCtlTable_old);
			StorageTmp->ieee8021EcmpFlowFilterCtlTable_rsvs = 0;
			StorageTmp->ieee8021EcmpFlowFilterCtlTable_tsts = 0;
			StorageTmp->ieee8021EcmpFlowFilterCtlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021EcmpFlowFilterCtlTable_sets == 0)
			revert_ieee8021EcmpFlowFilterCtlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021EcmpFlowFilterCtlTable_old) == NULL)
			break;
		StorageTmp->ieee8021EcmpFlowFilterCtlTtl = StorageOld->ieee8021EcmpFlowFilterCtlTtl;
		if (--StorageTmp->ieee8021EcmpFlowFilterCtlTable_rsvs == 0)
			ieee8021EcmpFlowFilterCtlTable_destroy(&StorageTmp->ieee8021EcmpFlowFilterCtlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021EcmpEctStaticEntryBridgePriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021EcmpEctStaticEntryBridgePriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021EcmpEctStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021EcmpMib", "write_ieee8021EcmpEctStaticEntryBridgePriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021EcmpEctStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpEctStaticEntryBridgePriority: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpEctStaticEntryBridgePriority not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 2..2 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpEctStaticEntryBridgePriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) == NULL)
			if (StorageTmp->ieee8021EcmpEctStaticTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old = ieee8021EcmpEctStaticTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021EcmpEctStaticTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority);
		StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority = string;
		StorageTmp->ieee8021EcmpEctStaticEntryBridgePriorityLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021EcmpEctStaticTable_tsts == 0)
				if ((ret = check_ieee8021EcmpEctStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021EcmpEctStaticTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021EcmpEctStaticTable_sets == 0)
				if ((ret = update_ieee8021EcmpEctStaticTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021EcmpEctStaticTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) != NULL) {
			ieee8021EcmpEctStaticTable_destroy(&StorageTmp->ieee8021EcmpEctStaticTable_old);
			StorageTmp->ieee8021EcmpEctStaticTable_rsvs = 0;
			StorageTmp->ieee8021EcmpEctStaticTable_tsts = 0;
			StorageTmp->ieee8021EcmpEctStaticTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021EcmpEctStaticTable_sets == 0)
			revert_ieee8021EcmpEctStaticTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) == NULL)
			break;
		if (StorageOld->ieee8021EcmpEctStaticEntryBridgePriority != NULL) {
			SNMP_FREE(StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority);
			StorageTmp->ieee8021EcmpEctStaticEntryBridgePriority = StorageOld->ieee8021EcmpEctStaticEntryBridgePriority;
			StorageTmp->ieee8021EcmpEctStaticEntryBridgePriorityLen = StorageOld->ieee8021EcmpEctStaticEntryBridgePriorityLen;
			StorageOld->ieee8021EcmpEctStaticEntryBridgePriority = NULL;
			StorageOld->ieee8021EcmpEctStaticEntryBridgePriorityLen = 0;
		}
		if (--StorageTmp->ieee8021EcmpEctStaticTable_rsvs == 0)
			ieee8021EcmpEctStaticTable_destroy(&StorageTmp->ieee8021EcmpEctStaticTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021EcmpEctStaticTable_row(struct ieee8021EcmpEctStaticTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021EcmpEctStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021EcmpEctStaticEntryRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021EcmpEctStaticTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021EcmpEctStaticTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021EcmpMib", "write_ieee8021EcmpEctStaticEntryRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021EcmpEctStaticTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpEctStaticEntryRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021EcmpEctStaticEntryRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021SpbTopIx */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021EcmpEctStaticEntryTieBreakMask */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021SpbTopIx */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021SpbTopIx: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021EcmpEctStaticEntryTieBreakMask */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021EcmpEctStaticEntryTieBreakMask: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..15 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 15)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021EcmpEctStaticEntryTieBreakMask: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021EcmpEctStaticTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021EcmpEctStaticTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021SpbTopIx = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021EcmpEctStaticEntryTieBreakMask = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021EcmpEctStaticTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021EcmpEctStaticTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) == NULL)
				if (StorageTmp->ieee8021EcmpEctStaticTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old = ieee8021EcmpEctStaticTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021EcmpEctStaticTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021EcmpEctStaticTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021EcmpEctStaticTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021EcmpEctStaticTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021EcmpEctStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021EcmpEctStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021EcmpEctStaticTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021EcmpEctStaticEntryRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021EcmpEctStaticTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021EcmpEctStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021EcmpEctStaticTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021EcmpEctStaticTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021EcmpEctStaticEntryRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021EcmpEctStaticEntryRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021EcmpEctStaticEntryRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) != NULL) {
				ieee8021EcmpEctStaticTable_destroy(&StorageTmp->ieee8021EcmpEctStaticTable_old);
				StorageTmp->ieee8021EcmpEctStaticTable_rsvs = 0;
				StorageTmp->ieee8021EcmpEctStaticTable_tsts = 0;
				StorageTmp->ieee8021EcmpEctStaticTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021EcmpEctStaticTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021EcmpEctStaticTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021EcmpEctStaticTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021EcmpEctStaticEntryRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021EcmpEctStaticTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021EcmpEctStaticTable_del(StorageNew);
				ieee8021EcmpEctStaticTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021EcmpEctStaticTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021EcmpEctStaticTable_rsvs == 0)
				ieee8021EcmpEctStaticTable_destroy(&StorageTmp->ieee8021EcmpEctStaticTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021EcmpEctStaticTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021EcmpMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021EcmpMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021EcmpMibold_signal_handler != NULL)
		(*ieee8021EcmpMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
}

/**
 * @fn void ieee8021EcmpMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021EcmpMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021EcmpMib", "ieee8021EcmpMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021EcmpMib", "done.\n"));
	return;
}
