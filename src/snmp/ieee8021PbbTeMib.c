/*****************************************************************************

 @(#) src/snmp/ieee8021pbbtemib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021pbbtemib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021PbbTeMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021PbbTeMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021PbbTeMib_refresh = 1;
volatile int ieee8021PbbTeProtectionGroupListTable_refresh = 1;
volatile int ieee8021PbbTeMAShareGroupTable_refresh = 1;
volatile int ieee8021PbbTeTesiTable_refresh = 1;
volatile int ieee8021PbbTeTeSiEspTable_refresh = 1;
volatile int ieee8021PbbTeProtectionGroupConfigTable_refresh = 1;
volatile int ieee8021PbbTeProtectionGroupISidTable_refresh = 1;
volatile int ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_refresh = 1;

/*
 * ieee8021PbbTeMib_variables_oid: object identifier for ieee8021PbbTeMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021PbbTeMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 10 };
oid ieee8021PbbTeProtectionGroupListTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 1, 1, 1 };
oid ieee8021PbbTeTesiTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 1, 3, 1 };
oid ieee8021PbbTeTeSiEspTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 1, 4, 1 };
oid ieee8021PbbTeProtectionGroupISidTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 1, 6, 1 };
oid ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 1, 7, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid ieee8021PbbTeProtectionGroupAdminFailure_oid[10] = { 1, 3, 111, 2, 802, 1, 1, 10, 0, 1 };

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021PbbTeCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 1, 1 };
oid ieee8021PbbTeGroupListGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 1 };
oid ieee8021PbbTeMASharedGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 2 };
oid ieee8021PbbTeTesiGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 3 };
oid ieee8021PbbTeSiEspGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 4 };
oid ieee8021PbbTeProtectionConfigManGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 5 };
oid ieee8021PbbTeProtectionConfigOptGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 6 };
oid ieee8021PbbTeProtectionGroupISidGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 7 };
oid ieee8021PbbTeFdbGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 8 };
oid ieee8021PbbTeNotificationsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 10, 2, 2, 9 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021PbbTeMib_variables: tree for ieee8021PbbTeMib
 * This variable defines function callbacks and type return information for the ieee8021PbbTeMib mib section
 */
struct variable4 ieee8021PbbTeMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021PBBTEPROTECTIONGROUPLISTMD  1
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPLISTMD, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeProtectionGroupListTable, 4, {1, 1, 1, 2}},
#define   IEEE8021PBBTEPROTECTIONGROUPLISTWORKINGMA  2
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPLISTWORKINGMA, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeProtectionGroupListTable, 4, {1, 1, 1, 3}},
#define   IEEE8021PBBTEPROTECTIONGROUPLISTPROTECTIONMA  3
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPLISTPROTECTIONMA, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeProtectionGroupListTable, 4, {1, 1, 1, 4}},
#define   IEEE8021PBBTEPROTECTIONGROUPLISTSTORAGETYPE  4
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPLISTSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupListTable, 4, {1, 1, 1, 5}},
#define   IEEE8021PBBTEPROTECTIONGROUPLISTROWSTATUS  5
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPLISTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupListTable, 4, {1, 1, 1, 6}},
#define   IEEE8021PBBTEMASHAREDGROUPID  6
	{(u_char) IEEE8021PBBTEMASHAREDGROUPID, ASN_UNSIGNED, RONLY, var_ieee8021PbbTeMAShareGroupTable, 4, {1, 2, 1, 2}},
#define   IEEE8021PBBTETESICOMPONENT  7
	{(u_char) IEEE8021PBBTETESICOMPONENT, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeTesiTable, 4, {1, 3, 1, 2}},
#define   IEEE8021PBBTETESIBRIDGEPORT  8
	{(u_char) IEEE8021PBBTETESIBRIDGEPORT, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeTesiTable, 4, {1, 3, 1, 3}},
#define   IEEE8021PBBTETESISTORAGETYPE  9
	{(u_char) IEEE8021PBBTETESISTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeTesiTable, 4, {1, 3, 1, 4}},
#define   IEEE8021PBBTETESIROWSTATUS  10
	{(u_char) IEEE8021PBBTETESIROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbTeTesiTable, 4, {1, 3, 1, 5}},
#define   IEEE8021PBBTETESIESPESP  11
	{(u_char) IEEE8021PBBTETESIESPESP, ASN_OCTET_STR, RWRITE, var_ieee8021PbbTeTeSiEspTable, 4, {1, 4, 1, 2}},
#define   IEEE8021PBBTETESIESPSTORAGETYPE  12
	{(u_char) IEEE8021PBBTETESIESPSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeTeSiEspTable, 4, {1, 4, 1, 3}},
#define   IEEE8021PBBTETESIESPROWSTATUS  13
	{(u_char) IEEE8021PBBTETESIESPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbTeTeSiEspTable, 4, {1, 4, 1, 4}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGSTATE  14
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGSTATE, ASN_INTEGER, RONLY, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 1}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDSTATUS  15
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDSTATUS, ASN_INTEGER, RONLY, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 2}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDLAST  16
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDLAST, ASN_INTEGER, RONLY, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 3}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN  17
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 4}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGACTIVEREQUESTS  18
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGACTIVEREQUESTS, ASN_INTEGER, RONLY, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 5}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGWTR  19
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGWTR, ASN_UNSIGNED, RONLY, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 6}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGHOLDOFF  20
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGHOLDOFF, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 7}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGNOTIFYENABLE  21
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGNOTIFYENABLE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 8}},
#define   IEEE8021PBBTEPROTECTIONGROUPCONFIGSTORAGETYPE  22
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupConfigTable, 4, {1, 5, 1, 9}},
#define   IEEE8021PBBTEPROTECTIONGROUPISIDCOMPONENTID  23
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPISIDCOMPONENTID, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeProtectionGroupISidTable, 4, {1, 6, 1, 2}},
#define   IEEE8021PBBTEPROTECTIONGROUPISIDGROUPID  24
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPISIDGROUPID, ASN_UNSIGNED, RWRITE, var_ieee8021PbbTeProtectionGroupISidTable, 4, {1, 6, 1, 3}},
#define   IEEE8021PBBTEPROTECTIONGROUPISIDSTORAGETYPE  25
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPISIDSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupISidTable, 4, {1, 6, 1, 4}},
#define   IEEE8021PBBTEPROTECTIONGROUPISIDROWSTATUS  26
	{(u_char) IEEE8021PBBTEPROTECTIONGROUPISIDROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbTeProtectionGroupISidTable, 4, {1, 6, 1, 5}},
#define   IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTEGRESSPORTS  27
	{(u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTEGRESSPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable, 4, {1, 7, 1, 2}},
#define   IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTFORBIDDENPORTS  28
	{(u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTFORBIDDENPORTS, ASN_OCTET_STR, RWRITE, var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable, 4, {1, 7, 1, 3}},
#define   IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTSTORAGETYPE  29
	{(u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable, 4, {1, 7, 1, 4}},
#define   IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTROWSTATUS  30
	{(u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable, 4, {1, 7, 1, 5}},
};

/* (L = length of the oidsuffix) */
struct ieee8021PbbTeMib_data *ieee8021PbbTeMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021PbbTeProtectionGroupListTableStorage = NULL;
struct header_complex_index *ieee8021PbbTeMAShareGroupTableStorage = NULL;
struct header_complex_index *ieee8021PbbTeTesiTableStorage = NULL;
struct header_complex_index *ieee8021PbbTeTeSiEspTableStorage = NULL;
struct header_complex_index *ieee8021PbbTeProtectionGroupConfigTableStorage = NULL;
struct header_complex_index *ieee8021PbbTeProtectionGroupISidTableStorage = NULL;
struct header_complex_index *ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage = NULL;

void (*ieee8021PbbTeMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021PbbTeMib_loop_handler(int);
void ieee8021PbbTeMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021PbbTeMib(void)
 * @brief ieee8021PbbTeMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021PbbTeMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021PbbTeMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021PbbTeMib", "init_ieee8021PbbTeMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021PbbTeMib", ieee8021PbbTeMib_variables, variable4, ieee8021PbbTeMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021PbbTeMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021PbbTeMib", parse_ieee8021PbbTeMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeProtectionGroupListTable", parse_ieee8021PbbTeProtectionGroupListTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeMAShareGroupTable", parse_ieee8021PbbTeMAShareGroupTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeTesiTable", parse_ieee8021PbbTeTesiTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeTeSiEspTable", parse_ieee8021PbbTeTeSiEspTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeProtectionGroupConfigTable", parse_ieee8021PbbTeProtectionGroupConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeProtectionGroupISidTable", parse_ieee8021PbbTeProtectionGroupISidTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable", parse_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeProtectionGroupListTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeMAShareGroupTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeTesiTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeTeSiEspTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeProtectionGroupConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeProtectionGroupISidTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021PbbTeMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021PbbTeMib_fd_handler, (void *) 1);
	}
	ieee8021PbbTeMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021PbbTeMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021PbbTeMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021PbbTeMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021PbbTeMib(void)
{
	DEBUGMSGTL(("ieee8021PbbTeMib", "deinit_ieee8021PbbTeMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021PbbTeMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021PbbTeMib_variables_oid, sizeof(ieee8021PbbTeMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021PbbTeMib");
	snmpd_unregister_config_handler("ieee8021PbbTeProtectionGroupListTable");
	snmpd_unregister_config_handler("ieee8021PbbTeMAShareGroupTable");
	snmpd_unregister_config_handler("ieee8021PbbTeTesiTable");
	snmpd_unregister_config_handler("ieee8021PbbTeTeSiEspTable");
	snmpd_unregister_config_handler("ieee8021PbbTeProtectionGroupConfigTable");
	snmpd_unregister_config_handler("ieee8021PbbTeProtectionGroupISidTable");
	snmpd_unregister_config_handler("ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

int
term_ieee8021PbbTeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021PbbTeMib", "term_ieee8021PbbTeMib: terminating...  "));
	deinit_ieee8021PbbTeMib();
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021PbbTeMib_data *ieee8021PbbTeMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021PbbTeMib.
 *
 * Creates a new ieee8021PbbTeMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021PbbTeMib.
 */
struct ieee8021PbbTeMib_data *
ieee8021PbbTeMib_create(void)
{
	struct ieee8021PbbTeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeMib_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeMib_data *ieee8021PbbTeMib_duplicate(struct ieee8021PbbTeMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021PbbTeMib_data *
ieee8021PbbTeMib_duplicate(struct ieee8021PbbTeMib_data *thedata)
{
	struct ieee8021PbbTeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeMib_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeMib_destroy(struct ieee8021PbbTeMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021PbbTeMib.
 * @brief delete a scalars structure from ieee8021PbbTeMib.
 *
 * Frees scalars that were previously removed from ieee8021PbbTeMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeMib_destroy(struct ieee8021PbbTeMib_data **thedata)
{
	struct ieee8021PbbTeMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeMib_add(struct ieee8021PbbTeMib_data *thedata)
 * @param thedata the structure representing ieee8021PbbTeMib scalars.
 * @brief adds node to the ieee8021PbbTeMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021PbbTeMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021PbbTeMib_add(struct ieee8021PbbTeMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMib_add: adding data...  "));
	if (thedata)
		ieee8021PbbTeMibStorage = thedata;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021PbbTeMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeMib_data *StorageTmp = ieee8021PbbTeMib_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021PbbTeMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeMib: storing data...  "));
	refresh_ieee8021PbbTeMib(1);
	if ((StorageTmp = ieee8021PbbTeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021PbbTeMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021PbbTeMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021PbbTeMib(struct ieee8021PbbTeMib_data *StorageTmp, struct ieee8021PbbTeMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021PbbTeMib(struct ieee8021PbbTeMib_data *StorageTmp, struct ieee8021PbbTeMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeMib(struct ieee8021PbbTeMib_data *StorageTmp, struct ieee8021PbbTeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021PbbTeMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeMib(struct ieee8021PbbTeMib_data *StorageTmp, struct ieee8021PbbTeMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021PbbTeMib(struct 
 * @fn void revert_ieee8021PbbTeMib(struct ieee8021PbbTeMib_data *StorageTmp, struct ieee8021PbbTeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021PbbTeMib(struct ieee8021PbbTeMib_data *StorageTmp, struct ieee8021PbbTeMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021PbbTeMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021PbbTeMib(int force)
{
	if (ieee8021PbbTeMibStorage == NULL) {
		struct ieee8021PbbTeMib_data *StorageNew;

		if ((StorageNew = ieee8021PbbTeMib_create()) == NULL)
			return;
		ieee8021PbbTeMibStorage = StorageNew;
		ieee8021PbbTeMib_refresh = 1;
	}
	if (!force && ieee8021PbbTeMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021PbbTeMib", "refresh_ieee8021PbbTeMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021PbbTeMib_refresh = 0;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021PbbTeMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021PbbTeMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021PbbTeMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021PbbTeMib(0);
	if ((StorageTmp = ieee8021PbbTeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021PbbTeMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021PbbTeMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021PbbTeMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021PbbTeProtectionGroupListTable_data *ieee8021PbbTeProtectionGroupListTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeProtectionGroupListTable table.
 *
 * Creates a new ieee8021PbbTeProtectionGroupListTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeProtectionGroupListTable_data *
ieee8021PbbTeProtectionGroupListTable_create(void)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeProtectionGroupListTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupListTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListMD = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListWorkingMA = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListProtectionMA = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021PbbTeProtectionGroupListRowStatus = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeProtectionGroupListTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeProtectionGroupListTable_data *ieee8021PbbTeProtectionGroupListTable_duplicate(struct ieee8021PbbTeProtectionGroupListTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeProtectionGroupListTable_data *
ieee8021PbbTeProtectionGroupListTable_duplicate(struct ieee8021PbbTeProtectionGroupListTable_data *thedata)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeProtectionGroupListTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupListTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeProtectionGroupListTable_id = thedata->ieee8021PbbTeProtectionGroupListTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021PbbTeProtectionGroupListGroupId = thedata->ieee8021PbbTeProtectionGroupListGroupId;
		StorageNew->ieee8021PbbTeProtectionGroupListMD = thedata->ieee8021PbbTeProtectionGroupListMD;
		StorageNew->ieee8021PbbTeProtectionGroupListWorkingMA = thedata->ieee8021PbbTeProtectionGroupListWorkingMA;
		StorageNew->ieee8021PbbTeProtectionGroupListProtectionMA = thedata->ieee8021PbbTeProtectionGroupListProtectionMA;
		StorageNew->ieee8021PbbTeProtectionGroupListStorageType = thedata->ieee8021PbbTeProtectionGroupListStorageType;
		StorageNew->ieee8021PbbTeProtectionGroupListRowStatus = thedata->ieee8021PbbTeProtectionGroupListRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeProtectionGroupListTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupListTable_destroy(struct ieee8021PbbTeProtectionGroupListTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeProtectionGroupListTable_destroy(struct ieee8021PbbTeProtectionGroupListTable_data **thedata)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupListTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupListTable_add(struct ieee8021PbbTeProtectionGroupListTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeProtectionGroupListTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeProtectionGroupListTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeProtectionGroupListTable_add(struct ieee8021PbbTeProtectionGroupListTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupListTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021PbbTeProtectionGroupListGroupId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeProtectionGroupListGroupId, sizeof(thedata->ieee8021PbbTeProtectionGroupListGroupId));
		header_complex_add_data(&ieee8021PbbTeProtectionGroupListTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupListTable_del(struct ieee8021PbbTeProtectionGroupListTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeProtectionGroupListTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeProtectionGroupListTable_del(struct ieee8021PbbTeProtectionGroupListTable_data *thedata)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupListTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeProtectionGroupListTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeProtectionGroupListTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeProtectionGroupListTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeProtectionGroupListTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeProtectionGroupListTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeProtectionGroupListTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = ieee8021PbbTeProtectionGroupListTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeProtectionGroupListTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupListGroupId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupListMD, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupListStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus, &tmpsize);
	ieee8021PbbTeProtectionGroupListTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeProtectionGroupListTable(): store configuraiton file for ieee8021PbbTeProtectionGroupListTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeProtectionGroupListTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeProtectionGroupListTable: storing data...  "));
	refresh_ieee8021PbbTeProtectionGroupListTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeProtectionGroupListTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeProtectionGroupListTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbTeProtectionGroupListStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeProtectionGroupListTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListMD, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbTeMAShareGroupTable_data *ieee8021PbbTeMAShareGroupTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeMAShareGroupTable table.
 *
 * Creates a new ieee8021PbbTeMAShareGroupTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeMAShareGroupTable_data *
ieee8021PbbTeMAShareGroupTable_create(void)
{
	struct ieee8021PbbTeMAShareGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeMAShareGroupTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMAShareGroupTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListGroupId = 0;
		StorageNew->ieee8021PbbTeMASharedGroupId = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeMAShareGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeMAShareGroupTable_data *ieee8021PbbTeMAShareGroupTable_duplicate(struct ieee8021PbbTeMAShareGroupTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeMAShareGroupTable_data *
ieee8021PbbTeMAShareGroupTable_duplicate(struct ieee8021PbbTeMAShareGroupTable_data *thedata)
{
	struct ieee8021PbbTeMAShareGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeMAShareGroupTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMAShareGroupTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeMAShareGroupTable_id = thedata->ieee8021PbbTeMAShareGroupTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021PbbTeProtectionGroupListGroupId = thedata->ieee8021PbbTeProtectionGroupListGroupId;
		StorageNew->ieee8021PbbTeMASharedGroupSubIndex = thedata->ieee8021PbbTeMASharedGroupSubIndex;
		StorageNew->ieee8021PbbTeMASharedGroupId = thedata->ieee8021PbbTeMASharedGroupId;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeMAShareGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeMAShareGroupTable_destroy(struct ieee8021PbbTeMAShareGroupTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeMAShareGroupTable_destroy(struct ieee8021PbbTeMAShareGroupTable_data **thedata)
{
	struct ieee8021PbbTeMAShareGroupTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMAShareGroupTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeMAShareGroupTable_add(struct ieee8021PbbTeMAShareGroupTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeMAShareGroupTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeMAShareGroupTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeMAShareGroupTable_add(struct ieee8021PbbTeMAShareGroupTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMAShareGroupTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021PbbTeProtectionGroupListGroupId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeProtectionGroupListGroupId, sizeof(thedata->ieee8021PbbTeProtectionGroupListGroupId));
		/* ieee8021PbbTeMASharedGroupSubIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeMASharedGroupSubIndex, sizeof(thedata->ieee8021PbbTeMASharedGroupSubIndex));
		header_complex_add_data(&ieee8021PbbTeMAShareGroupTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeMAShareGroupTable_del(struct ieee8021PbbTeMAShareGroupTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeMAShareGroupTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeMAShareGroupTable_del(struct ieee8021PbbTeMAShareGroupTable_data *thedata)
{
	struct ieee8021PbbTeMAShareGroupTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMAShareGroupTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeMAShareGroupTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeMAShareGroupTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeMAShareGroupTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeMAShareGroupTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeMAShareGroupTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeMAShareGroupTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp = ieee8021PbbTeMAShareGroupTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeMAShareGroupTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupListGroupId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeMASharedGroupSubIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeMASharedGroupId, &tmpsize);
	ieee8021PbbTeMAShareGroupTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeMAShareGroupTable(): store configuraiton file for ieee8021PbbTeMAShareGroupTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeMAShareGroupTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeMAShareGroupTable: storing data...  "));
	refresh_ieee8021PbbTeMAShareGroupTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeMAShareGroupTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeMAShareGroupTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeMAShareGroupTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeMASharedGroupSubIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeMASharedGroupId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbTeTesiTable_data *ieee8021PbbTeTesiTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeTesiTable table.
 *
 * Creates a new ieee8021PbbTeTesiTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeTesiTable_data *
ieee8021PbbTeTesiTable_create(void)
{
	struct ieee8021PbbTeTesiTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeTesiTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTesiTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbTeTesiComponent = 0;
		StorageNew->ieee8021PbbTeTesiBridgePort = 0;
		StorageNew->ieee8021PbbTeTesiStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021PbbTeTesiRowStatus = 0;
		StorageNew->ieee8021PbbTeTesiRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeTesiTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeTesiTable_data *ieee8021PbbTeTesiTable_duplicate(struct ieee8021PbbTeTesiTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeTesiTable_data *
ieee8021PbbTeTesiTable_duplicate(struct ieee8021PbbTeTesiTable_data *thedata)
{
	struct ieee8021PbbTeTesiTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeTesiTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTesiTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeTesiTable_id = thedata->ieee8021PbbTeTesiTable_id;
		StorageNew->ieee8021PbbTeTesiId = thedata->ieee8021PbbTeTesiId;
		StorageNew->ieee8021PbbTeTesiComponent = thedata->ieee8021PbbTeTesiComponent;
		StorageNew->ieee8021PbbTeTesiBridgePort = thedata->ieee8021PbbTeTesiBridgePort;
		StorageNew->ieee8021PbbTeTesiStorageType = thedata->ieee8021PbbTeTesiStorageType;
		StorageNew->ieee8021PbbTeTesiRowStatus = thedata->ieee8021PbbTeTesiRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeTesiTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeTesiTable_destroy(struct ieee8021PbbTeTesiTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeTesiTable_destroy(struct ieee8021PbbTeTesiTable_data **thedata)
{
	struct ieee8021PbbTeTesiTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTesiTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeTesiTable_add(struct ieee8021PbbTeTesiTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeTesiTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeTesiTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeTesiTable_add(struct ieee8021PbbTeTesiTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTesiTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbTeTesiId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeTesiId, sizeof(thedata->ieee8021PbbTeTesiId));
		header_complex_add_data(&ieee8021PbbTeTesiTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeTesiTable_del(struct ieee8021PbbTeTesiTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeTesiTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeTesiTable_del(struct ieee8021PbbTeTesiTable_data *thedata)
{
	struct ieee8021PbbTeTesiTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTesiTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeTesiTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeTesiTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeTesiTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeTesiTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeTesiTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeTesiTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeTesiTable_data *StorageTmp = ieee8021PbbTeTesiTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeTesiTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeTesiId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeTesiComponent, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeTesiBridgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeTesiStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeTesiRowStatus, &tmpsize);
	ieee8021PbbTeTesiTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeTesiTable(): store configuraiton file for ieee8021PbbTeTesiTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeTesiTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeTesiTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeTesiTable: storing data...  "));
	refresh_ieee8021PbbTeTesiTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeTesiTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeTesiTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbTeTesiStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeTesiTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeTesiId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeTesiComponent, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeTesiBridgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeTesiStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeTesiRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbTeTeSiEspTable_data *ieee8021PbbTeTeSiEspTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeTeSiEspTable table.
 *
 * Creates a new ieee8021PbbTeTeSiEspTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeTeSiEspTable_data *
ieee8021PbbTeTeSiEspTable_create(void)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeTeSiEspTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTeSiEspTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbTeTesiId = 0;
		if ((StorageNew->ieee8021PbbTeTeSiEspEsp = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbTeTeSiEspEspLen = 0;
		StorageNew->ieee8021PbbTeTeSiEspEsp[StorageNew->ieee8021PbbTeTeSiEspEspLen] = 0;
		StorageNew->ieee8021PbbTeTeSiEspStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021PbbTeTeSiEspRowStatus = 0;
		StorageNew->ieee8021PbbTeTeSiEspRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeTeSiEspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeTeSiEspTable_data *ieee8021PbbTeTeSiEspTable_duplicate(struct ieee8021PbbTeTeSiEspTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeTeSiEspTable_data *
ieee8021PbbTeTeSiEspTable_duplicate(struct ieee8021PbbTeTeSiEspTable_data *thedata)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeTeSiEspTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTeSiEspTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeTeSiEspTable_id = thedata->ieee8021PbbTeTeSiEspTable_id;
		StorageNew->ieee8021PbbTeTesiId = thedata->ieee8021PbbTeTesiId;
		StorageNew->ieee8021PbbTeTeSiEspEspIndex = thedata->ieee8021PbbTeTeSiEspEspIndex;
		if (!(StorageNew->ieee8021PbbTeTeSiEspEsp = malloc(thedata->ieee8021PbbTeTeSiEspEspLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbTeTeSiEspEsp, thedata->ieee8021PbbTeTeSiEspEsp, thedata->ieee8021PbbTeTeSiEspEspLen);
		StorageNew->ieee8021PbbTeTeSiEspEspLen = thedata->ieee8021PbbTeTeSiEspEspLen;
		StorageNew->ieee8021PbbTeTeSiEspEsp[StorageNew->ieee8021PbbTeTeSiEspEspLen] = 0;
		StorageNew->ieee8021PbbTeTeSiEspStorageType = thedata->ieee8021PbbTeTeSiEspStorageType;
		StorageNew->ieee8021PbbTeTeSiEspRowStatus = thedata->ieee8021PbbTeTeSiEspRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeTeSiEspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeTeSiEspTable_destroy(struct ieee8021PbbTeTeSiEspTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeTeSiEspTable_destroy(struct ieee8021PbbTeTeSiEspTable_data **thedata)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTeSiEspTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021PbbTeTeSiEspEsp);
		StorageDel->ieee8021PbbTeTeSiEspEspLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeTeSiEspTable_add(struct ieee8021PbbTeTeSiEspTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeTeSiEspTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeTeSiEspTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeTeSiEspTable_add(struct ieee8021PbbTeTeSiEspTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTeSiEspTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbTeTesiId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeTesiId, sizeof(thedata->ieee8021PbbTeTesiId));
		/* ieee8021PbbTeTeSiEspEspIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeTeSiEspEspIndex, sizeof(thedata->ieee8021PbbTeTeSiEspEspIndex));
		header_complex_add_data(&ieee8021PbbTeTeSiEspTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeTeSiEspTable_del(struct ieee8021PbbTeTeSiEspTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeTeSiEspTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeTeSiEspTable_del(struct ieee8021PbbTeTeSiEspTable_data *thedata)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeTeSiEspTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeTeSiEspTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeTeSiEspTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeTeSiEspTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeTeSiEspTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeTeSiEspTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeTeSiEspTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeTeSiEspTable_data *StorageTmp = ieee8021PbbTeTeSiEspTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeTeSiEspTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeTesiId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeTeSiEspEspIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021PbbTeTeSiEspEsp);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbTeTeSiEspEsp, &StorageTmp->ieee8021PbbTeTeSiEspEspLen);
	if (StorageTmp->ieee8021PbbTeTeSiEspEsp == NULL) {
		config_perror("invalid specification for ieee8021PbbTeTeSiEspEsp");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeTeSiEspStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeTeSiEspRowStatus, &tmpsize);
	ieee8021PbbTeTeSiEspTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeTeSiEspTable(): store configuraiton file for ieee8021PbbTeTeSiEspTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeTeSiEspTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeTeSiEspTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeTeSiEspTable: storing data...  "));
	refresh_ieee8021PbbTeTeSiEspTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeTeSiEspTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeTeSiEspTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbTeTeSiEspStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeTeSiEspTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeTesiId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeTeSiEspEspIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbTeTeSiEspEsp, &StorageTmp->ieee8021PbbTeTeSiEspEspLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeTeSiEspStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeTeSiEspRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbTeProtectionGroupConfigTable_data *ieee8021PbbTeProtectionGroupConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeProtectionGroupConfigTable table.
 *
 * Creates a new ieee8021PbbTeProtectionGroupConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeProtectionGroupConfigTable_data *
ieee8021PbbTeProtectionGroupConfigTable_create(void)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeProtectionGroupConfigTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupConfigTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBaseComponentId = 0;
		StorageNew->ieee8021PbbTeProtectionGroupListGroupId = 0;
		StorageNew->ieee8021PbbTeProtectionGroupConfigState = 0;
		StorageNew->ieee8021PbbTeProtectionGroupConfigCommandStatus = 0;
		StorageNew->ieee8021PbbTeProtectionGroupConfigCommandLast = 0;
		StorageNew->ieee8021PbbTeProtectionGroupConfigCommandAdmin = IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN_CLEAR;
		StorageNew->ieee8021PbbTeProtectionGroupConfigActiveRequests = 0;
		StorageNew->ieee8021PbbTeProtectionGroupConfigWTR = 5;
		StorageNew->ieee8021PbbTeProtectionGroupConfigHoldOff = 0;
		StorageNew->ieee8021PbbTeProtectionGroupConfigNotifyEnable = TV_FALSE;
		StorageNew->ieee8021PbbTeProtectionGroupConfigStorageType = ST_NONVOLATILE;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeProtectionGroupConfigTable_data *ieee8021PbbTeProtectionGroupConfigTable_duplicate(struct ieee8021PbbTeProtectionGroupConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeProtectionGroupConfigTable_data *
ieee8021PbbTeProtectionGroupConfigTable_duplicate(struct ieee8021PbbTeProtectionGroupConfigTable_data *thedata)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeProtectionGroupConfigTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupConfigTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeProtectionGroupConfigTable_id = thedata->ieee8021PbbTeProtectionGroupConfigTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		StorageNew->ieee8021PbbTeProtectionGroupListGroupId = thedata->ieee8021PbbTeProtectionGroupListGroupId;
		StorageNew->ieee8021PbbTeProtectionGroupConfigState = thedata->ieee8021PbbTeProtectionGroupConfigState;
		StorageNew->ieee8021PbbTeProtectionGroupConfigCommandStatus = thedata->ieee8021PbbTeProtectionGroupConfigCommandStatus;
		StorageNew->ieee8021PbbTeProtectionGroupConfigCommandLast = thedata->ieee8021PbbTeProtectionGroupConfigCommandLast;
		StorageNew->ieee8021PbbTeProtectionGroupConfigCommandAdmin = thedata->ieee8021PbbTeProtectionGroupConfigCommandAdmin;
		StorageNew->ieee8021PbbTeProtectionGroupConfigActiveRequests = thedata->ieee8021PbbTeProtectionGroupConfigActiveRequests;
		StorageNew->ieee8021PbbTeProtectionGroupConfigWTR = thedata->ieee8021PbbTeProtectionGroupConfigWTR;
		StorageNew->ieee8021PbbTeProtectionGroupConfigHoldOff = thedata->ieee8021PbbTeProtectionGroupConfigHoldOff;
		StorageNew->ieee8021PbbTeProtectionGroupConfigNotifyEnable = thedata->ieee8021PbbTeProtectionGroupConfigNotifyEnable;
		StorageNew->ieee8021PbbTeProtectionGroupConfigStorageType = thedata->ieee8021PbbTeProtectionGroupConfigStorageType;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupConfigTable_destroy(struct ieee8021PbbTeProtectionGroupConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeProtectionGroupConfigTable_destroy(struct ieee8021PbbTeProtectionGroupConfigTable_data **thedata)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupConfigTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupConfigTable_add(struct ieee8021PbbTeProtectionGroupConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeProtectionGroupConfigTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeProtectionGroupConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeProtectionGroupConfigTable_add(struct ieee8021PbbTeProtectionGroupConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupConfigTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		/* ieee8021PbbTeProtectionGroupListGroupId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeProtectionGroupListGroupId, sizeof(thedata->ieee8021PbbTeProtectionGroupListGroupId));
		header_complex_add_data(&ieee8021PbbTeProtectionGroupConfigTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupConfigTable_del(struct ieee8021PbbTeProtectionGroupConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeProtectionGroupConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeProtectionGroupConfigTable_del(struct ieee8021PbbTeProtectionGroupConfigTable_data *thedata)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupConfigTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeProtectionGroupConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeProtectionGroupConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeProtectionGroupConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeProtectionGroupConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeProtectionGroupConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeProtectionGroupConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp = ieee8021PbbTeProtectionGroupConfigTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeProtectionGroupConfigTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupListGroupId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandLast, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigActiveRequests, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigWTR, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType, &tmpsize);
	ieee8021PbbTeProtectionGroupConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeProtectionGroupConfigTable(): store configuraiton file for ieee8021PbbTeProtectionGroupConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeProtectionGroupConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeProtectionGroupConfigTable: storing data...  "));
	refresh_ieee8021PbbTeProtectionGroupConfigTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeProtectionGroupConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeProtectionGroupConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeProtectionGroupConfigTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupListGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandLast, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigActiveRequests, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigWTR, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbTeProtectionGroupISidTable_data *ieee8021PbbTeProtectionGroupISidTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeProtectionGroupISidTable table.
 *
 * Creates a new ieee8021PbbTeProtectionGroupISidTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeProtectionGroupISidTable_data *
ieee8021PbbTeProtectionGroupISidTable_create(void)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeProtectionGroupISidTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupISidTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbTeProtectionGroupISidComponentId = 0;
		StorageNew->ieee8021PbbTeProtectionGroupISidGroupId = 0;
		StorageNew->ieee8021PbbTeProtectionGroupISidStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021PbbTeProtectionGroupISidRowStatus = 0;
		StorageNew->ieee8021PbbTeProtectionGroupISidRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeProtectionGroupISidTable_data *ieee8021PbbTeProtectionGroupISidTable_duplicate(struct ieee8021PbbTeProtectionGroupISidTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeProtectionGroupISidTable_data *
ieee8021PbbTeProtectionGroupISidTable_duplicate(struct ieee8021PbbTeProtectionGroupISidTable_data *thedata)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeProtectionGroupISidTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupISidTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeProtectionGroupISidTable_id = thedata->ieee8021PbbTeProtectionGroupISidTable_id;
		StorageNew->ieee8021PbbTeProtectionGroupISidIndex = thedata->ieee8021PbbTeProtectionGroupISidIndex;
		StorageNew->ieee8021PbbTeProtectionGroupISidComponentId = thedata->ieee8021PbbTeProtectionGroupISidComponentId;
		StorageNew->ieee8021PbbTeProtectionGroupISidGroupId = thedata->ieee8021PbbTeProtectionGroupISidGroupId;
		StorageNew->ieee8021PbbTeProtectionGroupISidStorageType = thedata->ieee8021PbbTeProtectionGroupISidStorageType;
		StorageNew->ieee8021PbbTeProtectionGroupISidRowStatus = thedata->ieee8021PbbTeProtectionGroupISidRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupISidTable_destroy(struct ieee8021PbbTeProtectionGroupISidTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeProtectionGroupISidTable_destroy(struct ieee8021PbbTeProtectionGroupISidTable_data **thedata)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupISidTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupISidTable_add(struct ieee8021PbbTeProtectionGroupISidTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeProtectionGroupISidTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeProtectionGroupISidTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeProtectionGroupISidTable_add(struct ieee8021PbbTeProtectionGroupISidTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupISidTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbTeProtectionGroupISidIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeProtectionGroupISidIndex, sizeof(thedata->ieee8021PbbTeProtectionGroupISidIndex));
		header_complex_add_data(&ieee8021PbbTeProtectionGroupISidTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeProtectionGroupISidTable_del(struct ieee8021PbbTeProtectionGroupISidTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeProtectionGroupISidTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeProtectionGroupISidTable_del(struct ieee8021PbbTeProtectionGroupISidTable_data *thedata)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeProtectionGroupISidTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeProtectionGroupISidTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeProtectionGroupISidTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeProtectionGroupISidTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeProtectionGroupISidTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeProtectionGroupISidTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeProtectionGroupISidTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp = ieee8021PbbTeProtectionGroupISidTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeProtectionGroupISidTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupISidIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus, &tmpsize);
	ieee8021PbbTeProtectionGroupISidTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeProtectionGroupISidTable(): store configuraiton file for ieee8021PbbTeProtectionGroupISidTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeProtectionGroupISidTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeProtectionGroupISidTable: storing data...  "));
	refresh_ieee8021PbbTeProtectionGroupISidTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeProtectionGroupISidTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeProtectionGroupISidTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeProtectionGroupISidTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupISidIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable table.
 *
 * Creates a new ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *
ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_create(void)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = 0;
		if ((StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen = 0;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts[StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen] = 0;
		if ((StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen = 0;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts[StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen] = 0;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType = ST_NONVOLATILE;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus = 0;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *
ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *thedata)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data);

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_id = thedata->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_id;
		StorageNew->ieee8021QBridgeVlanCurrentComponentId = thedata->ieee8021QBridgeVlanCurrentComponentId;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex = thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex;
		if (!(StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts = malloc(thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts, thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts,
		       thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen);
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen = thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts[StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen] = 0;
		if (!(StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts = malloc(thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts, thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts,
		       thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen);
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen = thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts[StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen] = 0;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType = thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType;
		StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus = thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data **thedata)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts);
		StorageDel->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts);
		StorageDel->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_add(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_add(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021QBridgeVlanCurrentComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021QBridgeVlanCurrentComponentId, sizeof(thedata->ieee8021QBridgeVlanCurrentComponentId));
		/* ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex,
					  sizeof(thedata->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex));
		header_complex_add_data(&ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_del(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_del(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *thedata)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp = ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_create();

	DEBUGMSGTL(("ieee8021PbbTeMib", "parse_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen);
	if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts == NULL) {
		config_perror("invalid specification for ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts);
	line =
	    read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen);
	if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts == NULL) {
		config_perror("invalid specification for ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus, &tmpsize);
	ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/*
 * store_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(): store configuraiton file for ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbTeMib", "store_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable: storing data...  "));
	refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021QBridgeVlanCurrentComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex, &tmpsize);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts,
						   &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts,
						   &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupListTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeProtectionGroupListTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupListTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeProtectionGroupListTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupListTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeProtectionGroupListTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeProtectionGroupListTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeProtectionGroupListTable_data *
refresh_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeProtectionGroupListTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeProtectionGroupListTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeProtectionGroupListTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeProtectionGroupListTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeProtectionGroupListTable(int force)
{
	if (!force && ieee8021PbbTeProtectionGroupListTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeProtectionGroupListTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeProtectionGroupListTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeProtectionGroupListTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeProtectionGroupListTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeProtectionGroupListTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeProtectionGroupListTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeProtectionGroupListTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPLISTMD:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupListMD;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupListMD);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupListMD;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPLISTWORKINGMA:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupListWorkingMA;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPLISTPROTECTIONMA:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupListProtectionMA;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPLISTSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupListStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupListStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupListStorageType;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPLISTROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupListRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, struct ieee8021PbbTeMAShareGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, struct ieee8021PbbTeMAShareGroupTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, struct ieee8021PbbTeMAShareGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeMAShareGroupTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, struct ieee8021PbbTeMAShareGroupTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeMAShareGroupTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, struct ieee8021PbbTeMAShareGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, struct ieee8021PbbTeMAShareGroupTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeMAShareGroupTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeMAShareGroupTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeMAShareGroupTable_data *
refresh_ieee8021PbbTeMAShareGroupTable_row(struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeMAShareGroupTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeMAShareGroupTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeMAShareGroupTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeMAShareGroupTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeMAShareGroupTable(int force)
{
	if (!force && ieee8021PbbTeMAShareGroupTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeMAShareGroupTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeMAShareGroupTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeMAShareGroupTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeMAShareGroupTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeMAShareGroupTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeMAShareGroupTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeMAShareGroupTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeMAShareGroupTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeMAShareGroupTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTEMASHAREDGROUPID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeMASharedGroupId);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeMASharedGroupId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, struct ieee8021PbbTeTesiTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, struct ieee8021PbbTeTesiTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, struct ieee8021PbbTeTesiTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeTesiTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, struct ieee8021PbbTeTesiTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeTesiTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, struct ieee8021PbbTeTesiTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, struct ieee8021PbbTeTesiTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeTesiTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeTesiTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeTesiTable_data *
refresh_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeTesiTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeTesiTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeTesiTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeTesiTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeTesiTable(int force)
{
	if (!force && ieee8021PbbTeTesiTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeTesiTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeTesiTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeTesiTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeTesiTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeTesiTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeTesiTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeTesiTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeTesiTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeTesiTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTETESICOMPONENT:	/* Create */
		*write_method = write_ieee8021PbbTeTesiComponent;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeTesiComponent);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeTesiComponent;
		break;
	case (u_char) IEEE8021PBBTETESIBRIDGEPORT:	/* Create */
		*write_method = write_ieee8021PbbTeTesiBridgePort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeTesiBridgePort);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeTesiBridgePort;
		break;
	case (u_char) IEEE8021PBBTETESISTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbTeTesiStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeTesiStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeTesiStorageType;
		break;
	case (u_char) IEEE8021PBBTETESIROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbTeTesiRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeTesiRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeTesiRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, struct ieee8021PbbTeTeSiEspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, struct ieee8021PbbTeTeSiEspTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, struct ieee8021PbbTeTeSiEspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeTeSiEspTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, struct ieee8021PbbTeTeSiEspTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeTeSiEspTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, struct ieee8021PbbTeTeSiEspTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, struct ieee8021PbbTeTeSiEspTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeTeSiEspTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeTeSiEspTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeTeSiEspTable_data *
refresh_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeTeSiEspTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeTeSiEspTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeTeSiEspTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeTeSiEspTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeTeSiEspTable(int force)
{
	if (!force && ieee8021PbbTeTeSiEspTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeTeSiEspTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeTeSiEspTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeTeSiEspTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeTeSiEspTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeTeSiEspTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeTeSiEspTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeTeSiEspTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeTeSiEspTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTETESIESPESP:	/* Create */
		*write_method = write_ieee8021PbbTeTeSiEspEsp;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbTeTeSiEspEspLen;
		rval = (u_char *) StorageTmp->ieee8021PbbTeTeSiEspEsp;
		break;
	case (u_char) IEEE8021PBBTETESIESPSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbTeTeSiEspStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeTeSiEspStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeTeSiEspStorageType;
		break;
	case (u_char) IEEE8021PBBTETESIESPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbTeTeSiEspRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeTeSiEspRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeTeSiEspRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeProtectionGroupConfigTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeProtectionGroupConfigTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeProtectionGroupConfigTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeProtectionGroupConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeProtectionGroupConfigTable_data *
refresh_ieee8021PbbTeProtectionGroupConfigTable_row(struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeProtectionGroupConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeProtectionGroupConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeProtectionGroupConfigTable(int force)
{
	if (!force && ieee8021PbbTeProtectionGroupConfigTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeProtectionGroupConfigTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeProtectionGroupConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeProtectionGroupConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeProtectionGroupConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeProtectionGroupConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeProtectionGroupConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeProtectionGroupConfigTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigState);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigState;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandStatus;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDLAST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandLast);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandLast;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupConfigCommandAdmin;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGACTIVEREQUESTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigActiveRequests);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigActiveRequests;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGWTR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigWTR);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigWTR;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGHOLDOFF:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupConfigHoldOff;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGNOTIFYENABLE:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupConfigNotifyEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPCONFIGSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupConfigStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupISidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupISidTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupISidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeProtectionGroupISidTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupISidTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeProtectionGroupISidTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupISidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, struct ieee8021PbbTeProtectionGroupISidTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeProtectionGroupISidTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeProtectionGroupISidTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeProtectionGroupISidTable_data *
refresh_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeProtectionGroupISidTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeProtectionGroupISidTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeProtectionGroupISidTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeProtectionGroupISidTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeProtectionGroupISidTable(int force)
{
	if (!force && ieee8021PbbTeProtectionGroupISidTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeProtectionGroupISidTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeProtectionGroupISidTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeProtectionGroupISidTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeProtectionGroupISidTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeProtectionGroupISidTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeProtectionGroupISidTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeProtectionGroupISidTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPISIDCOMPONENTID:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupISidComponentId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPISIDGROUPID:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupISidGroupId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPISIDSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupISidStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType;
		break;
	case (u_char) IEEE8021PBBTEPROTECTIONGROUPISIDROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbTeProtectionGroupISidRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp, struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp,
							     struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp, struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp,
							      struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp, struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp,
							      struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *
refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(int force)
{
	if (!force && ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbTeMib above.
 */
u_char *
var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbTeMib", "var_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTEGRESSPORTS:	/* Create */
		*write_method = write_ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen;
		rval = (u_char *) StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts;
		break;
	case (u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTFORBIDDENPORTS:	/* Create */
		*write_method = write_ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen;
		rval = (u_char *) StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts;
		break;
	case (u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType;
		break;
	case (u_char) IEEE8021PBBTEBRIDGESTATICFORWARDANYUNICASTROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupListMD(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupListMD(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupListMD entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupListTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListMD: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListMD not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListMD: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old = ieee8021PbbTeProtectionGroupListTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupListMD = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupListMD for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupListMD = StorageOld->ieee8021PbbTeProtectionGroupListMD;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupListWorkingMA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupListWorkingMA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupListWorkingMA entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupListTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListWorkingMA: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListWorkingMA not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListWorkingMA: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old = ieee8021PbbTeProtectionGroupListTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupListWorkingMA = StorageOld->ieee8021PbbTeProtectionGroupListWorkingMA;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupListProtectionMA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupListProtectionMA(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupListProtectionMA entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupListTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListProtectionMA: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListProtectionMA not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListProtectionMA: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old = ieee8021PbbTeProtectionGroupListTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA for you to use, and you have just been asked to do something with it.  Note 
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupListProtectionMA = StorageOld->ieee8021PbbTeProtectionGroupListProtectionMA;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupListStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupListStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupListStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupListTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old = ieee8021PbbTeProtectionGroupListTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupListStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupListStorageType for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupListStorageType = StorageOld->ieee8021PbbTeProtectionGroupListStorageType;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTesiComponent(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeTesiComponent(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTesiTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTesiComponent entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeTesiTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeTesiRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiComponent: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiComponent not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiComponent: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..-1 */
		if ((1 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiComponent: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old = ieee8021PbbTeTesiTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeTesiTable_rsvs++;
		StorageTmp->ieee8021PbbTeTesiComponent = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeTesiTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTesiTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeTesiComponent for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeTesiTable_sets == 0)
				if ((ret = update_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTesiTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
			StorageTmp->ieee8021PbbTeTesiTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeTesiTable_tsts = 0;
			StorageTmp->ieee8021PbbTeTesiTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeTesiTable_sets == 0)
			revert_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeTesiComponent = StorageOld->ieee8021PbbTeTesiComponent;
		if (--StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
			ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTesiBridgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeTesiBridgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTesiTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTesiBridgePort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeTesiTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeTesiRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiBridgePort: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiBridgePort not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiBridgePort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..65535 */
		if ((1 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiBridgePort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old = ieee8021PbbTeTesiTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeTesiTable_rsvs++;
		StorageTmp->ieee8021PbbTeTesiBridgePort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeTesiTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTesiTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeTesiBridgePort for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeTesiTable_sets == 0)
				if ((ret = update_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTesiTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
			StorageTmp->ieee8021PbbTeTesiTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeTesiTable_tsts = 0;
			StorageTmp->ieee8021PbbTeTesiTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeTesiTable_sets == 0)
			revert_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeTesiBridgePort = StorageOld->ieee8021PbbTeTesiBridgePort;
		if (--StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
			ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTesiStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeTesiStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTesiTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTesiStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeTesiTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeTesiRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old = ieee8021PbbTeTesiTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeTesiTable_rsvs++;
		StorageTmp->ieee8021PbbTeTesiStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeTesiTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTesiTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeTesiStorageType for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeTesiTable_sets == 0)
				if ((ret = update_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTesiTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
			ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
			StorageTmp->ieee8021PbbTeTesiTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeTesiTable_tsts = 0;
			StorageTmp->ieee8021PbbTeTesiTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeTesiTable_sets == 0)
			revert_ieee8021PbbTeTesiTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeTesiStorageType = StorageOld->ieee8021PbbTeTesiStorageType;
		if (--StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
			ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTeSiEspEsp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeTeSiEspEsp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTeSiEspEsp entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeTeSiEspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeTeSiEspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspEsp: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspEsp not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 14..14 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 14)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspEsp: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old = ieee8021PbbTeTeSiEspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbTeTeSiEspEsp);
		StorageTmp->ieee8021PbbTeTeSiEspEsp = string;
		StorageTmp->ieee8021PbbTeTeSiEspEspLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeTeSiEspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTeSiEspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeTeSiEspEsp for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_sets == 0)
				if ((ret = update_ieee8021PbbTeTeSiEspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTeSiEspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
			ieee8021PbbTeTeSiEspTable_destroy(&StorageTmp->ieee8021PbbTeTeSiEspTable_old);
			StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeTeSiEspTable_tsts = 0;
			StorageTmp->ieee8021PbbTeTeSiEspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeTeSiEspTable_sets == 0)
			revert_ieee8021PbbTeTeSiEspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbTeTeSiEspEsp != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbTeTeSiEspEsp);
			StorageTmp->ieee8021PbbTeTeSiEspEsp = StorageOld->ieee8021PbbTeTeSiEspEsp;
			StorageTmp->ieee8021PbbTeTeSiEspEspLen = StorageOld->ieee8021PbbTeTeSiEspEspLen;
			StorageOld->ieee8021PbbTeTeSiEspEsp = NULL;
			StorageOld->ieee8021PbbTeTeSiEspEspLen = 0;
		}
		if (--StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs == 0)
			ieee8021PbbTeTeSiEspTable_destroy(&StorageTmp->ieee8021PbbTeTeSiEspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTeSiEspStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeTeSiEspStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTeSiEspStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeTeSiEspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeTeSiEspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old = ieee8021PbbTeTeSiEspTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs++;
		StorageTmp->ieee8021PbbTeTeSiEspStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeTeSiEspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTeSiEspTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeTeSiEspStorageType for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_sets == 0)
				if ((ret = update_ieee8021PbbTeTeSiEspTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeTeSiEspTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
			ieee8021PbbTeTeSiEspTable_destroy(&StorageTmp->ieee8021PbbTeTeSiEspTable_old);
			StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeTeSiEspTable_tsts = 0;
			StorageTmp->ieee8021PbbTeTeSiEspTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeTeSiEspTable_sets == 0)
			revert_ieee8021PbbTeTeSiEspTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeTeSiEspStorageType = StorageOld->ieee8021PbbTeTeSiEspStorageType;
		if (--StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs == 0)
			ieee8021PbbTeTeSiEspTable_destroy(&StorageTmp->ieee8021PbbTeTeSiEspTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupConfigCommandAdmin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupConfigCommandAdmin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupConfigCommandAdmin entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupConfigTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigCommandAdmin not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigCommandAdmin: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value clear */
		switch (set_value) {
		case IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN_CLEAR:
		case IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN_LOCKOUTPROTECTION:
		case IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN_FORCESWITCH:
		case IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN_MANUALSWITCHTOPROTECTION:
		case IEEE8021PBBTEPROTECTIONGROUPCONFIGCOMMANDADMIN_MANUALSWITCHTOWORKING:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigCommandAdmin: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old = ieee8021PbbTeProtectionGroupConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigCommandAdmin = StorageOld->ieee8021PbbTeProtectionGroupConfigCommandAdmin;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupConfigHoldOff(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupConfigHoldOff(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupConfigHoldOff entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupConfigTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigHoldOff not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigHoldOff: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* Note: ranges 0..100 */
		if ((0 > set_value || set_value > 100)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigHoldOff: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old = ieee8021PbbTeProtectionGroupConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigHoldOff = StorageOld->ieee8021PbbTeProtectionGroupConfigHoldOff;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupConfigNotifyEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupConfigNotifyEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupConfigNotifyEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupConfigTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigNotifyEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigNotifyEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigNotifyEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old = ieee8021PbbTeProtectionGroupConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigNotifyEnable = StorageOld->ieee8021PbbTeProtectionGroupConfigNotifyEnable;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupConfigStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupConfigStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupConfigStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupConfigTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupConfigStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old = ieee8021PbbTeProtectionGroupConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupConfigStorageType = StorageOld->ieee8021PbbTeProtectionGroupConfigStorageType;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupConfigTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupISidComponentId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupISidComponentId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupISidComponentId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupISidTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidComponentId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidComponentId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidComponentId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..-1 */
		if ((1 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidComponentId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old = ieee8021PbbTeProtectionGroupISidTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupISidComponentId = StorageOld->ieee8021PbbTeProtectionGroupISidComponentId;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupISidGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupISidGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupISidGroupId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupISidTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidGroupId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidGroupId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..-1 */
		if ((1 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old = ieee8021PbbTeProtectionGroupISidTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupISidGroupId = StorageOld->ieee8021PbbTeProtectionGroupISidGroupId;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupISidStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeProtectionGroupISidStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupISidStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupISidTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old = ieee8021PbbTeProtectionGroupISidTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs++;
		StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets == 0)
				if ((ret = update_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts = 0;
			StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets == 0)
			revert_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeProtectionGroupISidStorageType = StorageOld->ieee8021PbbTeProtectionGroupISidStorageType;
		if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old =
				     ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts);
		StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts = string;
		StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts for you to use, and you have just been asked to do something with
				   it.  Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets == 0)
				if ((ret = update_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts = 0;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets == 0)
			revert_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts = StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen = StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen;
			StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPorts = NULL;
			StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastEgressPortsLen = 0;
		}
		if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old =
				     ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts);
		StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts = string;
		StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts for you to use, and you have just been asked to do something
				   with it.  Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets == 0)
				if ((ret = update_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts = 0;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets == 0)
			revert_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts = StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen = StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen;
			StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPorts = NULL;
			StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastForbiddenPortsLen = 0;
		}
		if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old =
				     ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs++;
		StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts == 0)
				if ((ret = check_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType for you to use, and you have just been asked to do something with
				   it.  Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets == 0)
				if ((ret = update_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs = 0;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts = 0;
			StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets == 0)
			revert_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType = StorageOld->ieee8021PbbTeBridgeStaticForwardAnyUnicastStorageType;
		if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbTeProtectionGroupListTable_row(struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbTeTesiTable_row(struct ieee8021PbbTeTesiTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbTeTeSiEspTable_row(struct ieee8021PbbTeTeSiEspTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbTeProtectionGroupISidTable_row(struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupListRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbTeProtectionGroupListRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbTeProtectionGroupListTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupListRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupListTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupListRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBaseComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbbTeProtectionGroupListGroupId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBaseComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBaseComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBaseComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbbTeProtectionGroupListGroupId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeProtectionGroupListGroupId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeProtectionGroupListGroupId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbTeProtectionGroupListTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbTeProtectionGroupListTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBaseComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbbTeProtectionGroupListGroupId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbTeProtectionGroupListTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbTeProtectionGroupListTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
				if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old = ieee8021PbbTeProtectionGroupListTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbTeProtectionGroupListTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbTeProtectionGroupListTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done 
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbTeProtectionGroupListTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbTeProtectionGroupListTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbTeProtectionGroupListRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbTeProtectionGroupListRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbTeProtectionGroupListRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) != NULL) {
				ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
				StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs = 0;
				StorageTmp->ieee8021PbbTeProtectionGroupListTable_tsts = 0;
				StorageTmp->ieee8021PbbTeProtectionGroupListTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbTeProtectionGroupListTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbTeProtectionGroupListTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbTeProtectionGroupListRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbTeProtectionGroupListTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbTeProtectionGroupListTable_del(StorageNew);
				ieee8021PbbTeProtectionGroupListTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupListTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbTeProtectionGroupListTable_rsvs == 0)
				ieee8021PbbTeProtectionGroupListTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupListTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbTeProtectionGroupListTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTesiRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbTeTesiRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTesiTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbTeTesiTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTesiRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbTeTesiTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTesiRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021PbbTeTesiId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021PbbTeTesiId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeTesiId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..42947295 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 42947295)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeTesiId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbTeTesiTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbTeTesiTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021PbbTeTesiId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbTeTesiTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbTeTesiRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbTeTesiTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
				if (StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old = ieee8021PbbTeTesiTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbTeTesiTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbTeTesiTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbTeTesiTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbTeTesiTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbTeTesiRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbTeTesiTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeTesiRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeTesiTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeTesiRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeTesiTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbTeTesiRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbTeTesiTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeTesiRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbTeTesiTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeTesiRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbTeTesiTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbTeTesiTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbTeTesiRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbTeTesiRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbTeTesiRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) != NULL) {
				ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
				StorageTmp->ieee8021PbbTeTesiTable_rsvs = 0;
				StorageTmp->ieee8021PbbTeTesiTable_tsts = 0;
				StorageTmp->ieee8021PbbTeTesiTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbTeTesiTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbTeTesiTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbTeTesiRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbTeTesiTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbTeTesiRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbTeTesiTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbTeTesiTable_del(StorageNew);
				ieee8021PbbTeTesiTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbTeTesiTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbTeTesiTable_rsvs == 0)
				ieee8021PbbTeTesiTable_destroy(&StorageTmp->ieee8021PbbTeTesiTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbTeTesiTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeTeSiEspRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbTeTeSiEspRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeTeSiEspTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbTeTeSiEspTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeTeSiEspRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbTeTeSiEspTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeTeSiEspRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021PbbTeTesiId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbbTeTeSiEspEspIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021PbbTeTesiId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeTesiId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..42947295 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 42947295)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeTesiId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbbTeTeSiEspEspIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeTeSiEspEspIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbTeTeSiEspTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbTeTeSiEspTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021PbbTeTesiId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbbTeTeSiEspEspIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbTeTeSiEspTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbTeTeSiEspTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
				if (StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old = ieee8021PbbTeTeSiEspTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbTeTeSiEspTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbTeTeSiEspTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbTeTeSiEspTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbTeTeSiEspTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeTeSiEspTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeTeSiEspTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbTeTeSiEspRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must
		   be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbTeTeSiEspTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbTeTeSiEspTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbTeTeSiEspTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbTeTeSiEspTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbTeTeSiEspRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbTeTeSiEspRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbTeTeSiEspRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) != NULL) {
				ieee8021PbbTeTeSiEspTable_destroy(&StorageTmp->ieee8021PbbTeTeSiEspTable_old);
				StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs = 0;
				StorageTmp->ieee8021PbbTeTeSiEspTable_tsts = 0;
				StorageTmp->ieee8021PbbTeTeSiEspTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbTeTeSiEspTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbTeTeSiEspTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbTeTeSiEspTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbTeTeSiEspRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbTeTeSiEspTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbTeTeSiEspTable_del(StorageNew);
				ieee8021PbbTeTeSiEspTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbTeTeSiEspTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbTeTeSiEspTable_rsvs == 0)
				ieee8021PbbTeTeSiEspTable_destroy(&StorageTmp->ieee8021PbbTeTeSiEspTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbTeTeSiEspTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeProtectionGroupISidRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbTeProtectionGroupISidRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeProtectionGroupISidTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbTeProtectionGroupISidTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeProtectionGroupISidRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbTeProtectionGroupISidTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeProtectionGroupISidRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021PbbTeProtectionGroupISidIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021PbbTeProtectionGroupISidIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeProtectionGroupISidIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 256..16777214 */
			if ((256 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16777214)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeProtectionGroupISidIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbTeProtectionGroupISidTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbTeProtectionGroupISidTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021PbbTeProtectionGroupISidIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbTeProtectionGroupISidTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbTeProtectionGroupISidTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
				if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old = ieee8021PbbTeProtectionGroupISidTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbTeProtectionGroupISidTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbTeProtectionGroupISidTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done 
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbTeProtectionGroupISidTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbTeProtectionGroupISidTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbTeProtectionGroupISidRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbTeProtectionGroupISidRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbTeProtectionGroupISidRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) != NULL) {
				ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
				StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs = 0;
				StorageTmp->ieee8021PbbTeProtectionGroupISidTable_tsts = 0;
				StorageTmp->ieee8021PbbTeProtectionGroupISidTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbTeProtectionGroupISidTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbTeProtectionGroupISidRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbTeProtectionGroupISidTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbTeProtectionGroupISidTable_del(StorageNew);
				ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbTeProtectionGroupISidTable_rsvs == 0)
				ieee8021PbbTeProtectionGroupISidTable_destroy(&StorageTmp->ieee8021PbbTeProtectionGroupISidTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbTeProtectionGroupISidTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbTeMib", "write_ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021QBridgeVlanCurrentComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021QBridgeVlanCurrentComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanCurrentComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021QBridgeVlanCurrentComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021QBridgeVlanCurrentComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastVlanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbTeBridgeStaticForwardingAnyUnicastTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
				if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old =
					     ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus for you to use, and you have just been asked to do something with it.  Note that
		   anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) != NULL) {
				ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
				StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs = 0;
				StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_tsts = 0;
				StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbTeBridgeStaticForwardAnyUnicastRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_del(StorageNew);
				ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_rsvs == 0)
				ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_destroy(&StorageTmp->ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbTeBridgeStaticForwardingAnyUnicastTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_ieee8021PbbTeProtectionGroupAdminFailure_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	DEBUGMSGTL(("ieee8021PbbTeMib", "send_ieee8021PbbTeProtectionGroupAdminFailure_v2trap: sending trap...  "));
	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = ieee8021PbbTeProtectionGroupAdminFailure_oid;
	trap.val_len = sizeof(ieee8021PbbTeProtectionGroupAdminFailure_oid);
	trap.index = 0;
	send_v2trap(&trap);
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/**
 * @fn void ieee8021PbbTeMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021PbbTeMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021PbbTeMibold_signal_handler != NULL)
		(*ieee8021PbbTeMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
}

/**
 * @fn void ieee8021PbbTeMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021PbbTeMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021PbbTeMib", "ieee8021PbbTeMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021PbbTeMib", "done.\n"));
	return;
}
