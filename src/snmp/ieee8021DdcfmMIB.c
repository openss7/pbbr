/*****************************************************************************

 @(#) src/snmp/ieee8021ddcfmmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021ddcfmmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021DdcfmMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021DdcfmMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021DdcfmMIB_refresh = 1;
volatile int ieee8021DdcfmStackTable_refresh = 1;
volatile int ieee8021DdcfmRrTable_refresh = 1;
volatile int ieee8021DdcfmRFMReceiverTable_refresh = 1;
volatile int ieee8021DdcfmDrTable_refresh = 1;
volatile int ieee8021DdcfmSoTable_refresh = 1;

/*
 * ieee8021DdcfmMIB_variables_oid: object identifier for ieee8021DdcfmMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021DdcfmMIB_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 11 };
oid ieee8021DdcfmRrTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 11, 1, 2, 1, 1 };
oid ieee8021DdcfmRFMReceiverTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 11, 1, 3, 1, 1 };
oid ieee8021DdcfmDrTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 11, 1, 4, 1, 1 };
oid ieee8021DdcfmSoTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 11, 1, 5, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021DdcfmCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 11, 2, 1, 1 };
oid ieee8021DdcfmStackGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 11, 2, 2, 1 };
oid ieee8021DdcfmRrGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 11, 2, 2, 2 };
oid ieee8021DdcfmRFMReceiverGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 11, 2, 2, 3 };
oid ieee8021DdcfmDrGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 11, 2, 2, 4 };
oid ieee8021DdcfmSoGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 11, 2, 2, 5 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021DdcfmMIB_variables: tree for ieee8021DdcfmMIB
 * This variable defines function callbacks and type return information for the ieee8021DdcfmMIB mib section
 */
struct variable7 ieee8021DdcfmMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021DDCFMSTACKRRMDLEVEL  1
	{(u_char) IEEE8021DDCFMSTACKRRMDLEVEL, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 2}},
#define   IEEE8021DDCFMSTACKRRDIRECTION  2
	{(u_char) IEEE8021DDCFMSTACKRRDIRECTION, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 3}},
#define   IEEE8021DDCFMSTACKRFMRECEIVERMDLEVEL  3
	{(u_char) IEEE8021DDCFMSTACKRFMRECEIVERMDLEVEL, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 4}},
#define   IEEE8021DDCFMSTACKDRMDLEVEL  4
	{(u_char) IEEE8021DDCFMSTACKDRMDLEVEL, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 5}},
#define   IEEE8021DDCFMSTACKDRVLANIDORNONE  5
	{(u_char) IEEE8021DDCFMSTACKDRVLANIDORNONE, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 6}},
#define   IEEE8021DDCFMSTACKSFMORIGINATORMDLEVEL  6
	{(u_char) IEEE8021DDCFMSTACKSFMORIGINATORMDLEVEL, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 7}},
#define   IEEE8021DDCFMSTACKSFMORIGINATORVLANIDORNONE  7
	{(u_char) IEEE8021DDCFMSTACKSFMORIGINATORVLANIDORNONE, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 8}},
#define   IEEE8021DDCFMSTACKSFMORIGINATORDIRECTION  8
	{(u_char) IEEE8021DDCFMSTACKSFMORIGINATORDIRECTION, ASN_INTEGER, RONLY, var_ieee8021DdcfmStackTable, 5, {1, 1, 1, 1, 9}},
#define   IEEE8021DDCFMRRPRIMARYVLANIDORNONE  9
	{(u_char) IEEE8021DDCFMRRPRIMARYVLANIDORNONE, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 4}},
#define   IEEE8021DDCFMRRFILTER  10
	{(u_char) IEEE8021DDCFMRRFILTER, ASN_OCTET_STR, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 5}},
#define   IEEE8021DDCFMRRSAMPLINGINTERVAL  11
	{(u_char) IEEE8021DDCFMRRSAMPLINGINTERVAL, ASN_UNSIGNED, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 6}},
#define   IEEE8021DDCFMRRTARGETADDRESS  12
	{(u_char) IEEE8021DDCFMRRTARGETADDRESS, ASN_OCTET_STR, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 7}},
#define   IEEE8021DDCFMRRCONTINUEFLAG  13
	{(u_char) IEEE8021DDCFMRRCONTINUEFLAG, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 8}},
#define   IEEE8021DDCFMRRDURATION  14
	{(u_char) IEEE8021DDCFMRRDURATION, ASN_UNSIGNED, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 9}},
#define   IEEE8021DDCFMRRDURATIONINTIMEFLAG  15
	{(u_char) IEEE8021DDCFMRRDURATIONINTIMEFLAG, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 10}},
#define   IEEE8021DDCFMRRVLANPRIORITY  16
	{(u_char) IEEE8021DDCFMRRVLANPRIORITY, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 11}},
#define   IEEE8021DDCFMRRVLANDROPELIGIBLE  17
	{(u_char) IEEE8021DDCFMRRVLANDROPELIGIBLE, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 12}},
#define   IEEE8021DDCFMRRFLOODINGFLAG  18
	{(u_char) IEEE8021DDCFMRRFLOODINGFLAG, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 13}},
#define   IEEE8021DDCFMRRTRUNCATIONFLAG  19
	{(u_char) IEEE8021DDCFMRRTRUNCATIONFLAG, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 14}},
#define   IEEE8021DDCFMRRACTIVATIONSTATUS  20
	{(u_char) IEEE8021DDCFMRRACTIVATIONSTATUS, ASN_INTEGER, RONLY, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 15}},
#define   IEEE8021DDCFMRRREMAINDURATION  21
	{(u_char) IEEE8021DDCFMRRREMAINDURATION, ASN_UNSIGNED, RONLY, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 16}},
#define   IEEE8021DDCFMRRNEXTRFMTRANSID  22
	{(u_char) IEEE8021DDCFMRRNEXTRFMTRANSID, ASN_UNSIGNED, RONLY, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 17}},
#define   IEEE8021DDCFMRRROWSTATUS  23
	{(u_char) IEEE8021DDCFMRRROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRrTable, 5, {1, 2, 1, 1, 18}},
#define   IEEE8021DDCFMRFMROWSTATUS  24
	{(u_char) IEEE8021DDCFMRFMROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021DdcfmRFMReceiverTable, 5, {1, 3, 1, 1, 3}},
#define   IEEE8021DDCFMDRSFMORIGINATOR  25
	{(u_char) IEEE8021DDCFMDRSFMORIGINATOR, ASN_OCTET_STR, RWRITE, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 4}},
#define   IEEE8021DDCFMDRSOURCEADDRESSSTAYFLAG  26
	{(u_char) IEEE8021DDCFMDRSOURCEADDRESSSTAYFLAG, ASN_INTEGER, RWRITE, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 5}},
#define   IEEE8021DDCFMDRFLOODINGFLAG  27
	{(u_char) IEEE8021DDCFMDRFLOODINGFLAG, ASN_INTEGER, RWRITE, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 6}},
#define   IEEE8021DDCFMDRDURATION  28
	{(u_char) IEEE8021DDCFMDRDURATION, ASN_UNSIGNED, RWRITE, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 7}},
#define   IEEE8021DDCFMDRACTIVATIONSTATUS  29
	{(u_char) IEEE8021DDCFMDRACTIVATIONSTATUS, ASN_INTEGER, RONLY, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 8}},
#define   IEEE8021DDCFMDRREMAINDURATION  30
	{(u_char) IEEE8021DDCFMDRREMAINDURATION, ASN_UNSIGNED, RONLY, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 9}},
#define   IEEE8021DDCFMDRSFMSEQUENCEERRORS  31
	{(u_char) IEEE8021DDCFMDRSFMSEQUENCEERRORS, ASN_UNSIGNED, RONLY, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 10}},
#define   IEEE8021DDCFMDRROWSTATUS  32
	{(u_char) IEEE8021DDCFMDRROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021DdcfmDrTable, 5, {1, 4, 1, 1, 11}},
#define   IEEE8021DDCFMSODRMACADDRESS  33
	{(u_char) IEEE8021DDCFMSODRMACADDRESS, ASN_OCTET_STR, RWRITE, var_ieee8021DdcfmSoTable, 5, {1, 5, 1, 1, 5}},
#define   IEEE8021DDCFMSODURATION  34
	{(u_char) IEEE8021DDCFMSODURATION, ASN_UNSIGNED, RWRITE, var_ieee8021DdcfmSoTable, 5, {1, 5, 1, 1, 6}},
#define   IEEE8021DDCFMSOACTIVATIONSTATUS  35
	{(u_char) IEEE8021DDCFMSOACTIVATIONSTATUS, ASN_INTEGER, RONLY, var_ieee8021DdcfmSoTable, 5, {1, 5, 1, 1, 7}},
#define   IEEE8021DDCFMSOREMAINDURATION  36
	{(u_char) IEEE8021DDCFMSOREMAINDURATION, ASN_UNSIGNED, RONLY, var_ieee8021DdcfmSoTable, 5, {1, 5, 1, 1, 8}},
#define   IEEE8021DDCFMSOROWSTATUS  37
	{(u_char) IEEE8021DDCFMSOROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021DdcfmSoTable, 5, {1, 5, 1, 1, 9}},
};

/* (L = length of the oidsuffix) */
struct ieee8021DdcfmMIB_data *ieee8021DdcfmMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021DdcfmStackTableStorage = NULL;
struct header_complex_index *ieee8021DdcfmRrTableStorage = NULL;
struct header_complex_index *ieee8021DdcfmRFMReceiverTableStorage = NULL;
struct header_complex_index *ieee8021DdcfmDrTableStorage = NULL;
struct header_complex_index *ieee8021DdcfmSoTableStorage = NULL;

void (*ieee8021DdcfmMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021DdcfmMIB_loop_handler(int);
void ieee8021DdcfmMIB_fd_handler(int, void *);

/**
 * @fn void init_ieee8021DdcfmMIB(void)
 * @brief ieee8021DdcfmMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021DdcfmMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021DdcfmMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "init_ieee8021DdcfmMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021DdcfmMIB", ieee8021DdcfmMIB_variables, variable7, ieee8021DdcfmMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021DdcfmMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021DdcfmMIB", parse_ieee8021DdcfmMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021DdcfmStackTable", parse_ieee8021DdcfmStackTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021DdcfmRrTable", parse_ieee8021DdcfmRrTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021DdcfmRFMReceiverTable", parse_ieee8021DdcfmRFMReceiverTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021DdcfmDrTable", parse_ieee8021DdcfmDrTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021DdcfmSoTable", parse_ieee8021DdcfmSoTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021DdcfmMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021DdcfmStackTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021DdcfmRrTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021DdcfmRFMReceiverTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021DdcfmDrTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021DdcfmSoTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021DdcfmMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021DdcfmMIB_fd_handler, (void *) 1);
	}
	ieee8021DdcfmMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021DdcfmMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/**
 * @fn void deinit_ieee8021DdcfmMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021DdcfmMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021DdcfmMIB(void)
{
	DEBUGMSGTL(("ieee8021DdcfmMIB", "deinit_ieee8021DdcfmMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021DdcfmMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021DdcfmMIB_variables_oid, sizeof(ieee8021DdcfmMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021DdcfmMIB");
	snmpd_unregister_config_handler("ieee8021DdcfmStackTable");
	snmpd_unregister_config_handler("ieee8021DdcfmRrTable");
	snmpd_unregister_config_handler("ieee8021DdcfmRFMReceiverTable");
	snmpd_unregister_config_handler("ieee8021DdcfmDrTable");
	snmpd_unregister_config_handler("ieee8021DdcfmSoTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

int
term_ieee8021DdcfmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021DdcfmMIB", "term_ieee8021DdcfmMIB: terminating...  "));
	deinit_ieee8021DdcfmMIB();
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021DdcfmMIB_data *ieee8021DdcfmMIB_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021DdcfmMIB.
 *
 * Creates a new ieee8021DdcfmMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021DdcfmMIB.
 */
struct ieee8021DdcfmMIB_data *
ieee8021DdcfmMIB_create(void)
{
	struct ieee8021DdcfmMIB_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmMIB_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021DdcfmMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021DdcfmMIB_data *ieee8021DdcfmMIB_duplicate(struct ieee8021DdcfmMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021DdcfmMIB_data *
ieee8021DdcfmMIB_duplicate(struct ieee8021DdcfmMIB_data *thedata)
{
	struct ieee8021DdcfmMIB_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmMIB_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021DdcfmMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021DdcfmMIB_destroy(struct ieee8021DdcfmMIB_data **thedata)
 * @param thedata pointer to the data structure in ieee8021DdcfmMIB.
 * @brief delete a scalars structure from ieee8021DdcfmMIB.
 *
 * Frees scalars that were previously removed from ieee8021DdcfmMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021DdcfmMIB_destroy(struct ieee8021DdcfmMIB_data **thedata)
{
	struct ieee8021DdcfmMIB_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmMIB_add(struct ieee8021DdcfmMIB_data *thedata)
 * @param thedata the structure representing ieee8021DdcfmMIB scalars.
 * @brief adds node to the ieee8021DdcfmMIB scalar data set.
 *
 * Adds a scalar structure to the ieee8021DdcfmMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021DdcfmMIB_add(struct ieee8021DdcfmMIB_data *thedata)
{
	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmMIB_add: adding data...  "));
	if (thedata)
		ieee8021DdcfmMIBStorage = thedata;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021DdcfmMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021DdcfmMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021DdcfmMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021DdcfmMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021DdcfmMIB_data *StorageTmp = ieee8021DdcfmMIB_create();

	DEBUGMSGTL(("ieee8021DdcfmMIB", "parse_ieee8021DdcfmMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021DdcfmMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/*
 * store_ieee8021DdcfmMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021DdcfmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021DdcfmMIB_data *StorageTmp;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "store_ieee8021DdcfmMIB: storing data...  "));
	refresh_ieee8021DdcfmMIB(1);
	if ((StorageTmp = ieee8021DdcfmMIBStorage) == NULL) {
		DEBUGMSGTL(("ieee8021DdcfmMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021DdcfmMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021DdcfmMIB(struct ieee8021DdcfmMIB_data *StorageTmp, struct ieee8021DdcfmMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021DdcfmMIB(struct ieee8021DdcfmMIB_data *StorageTmp, struct ieee8021DdcfmMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021DdcfmMIB(struct ieee8021DdcfmMIB_data *StorageTmp, struct ieee8021DdcfmMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021DdcfmMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021DdcfmMIB(struct ieee8021DdcfmMIB_data *StorageTmp, struct ieee8021DdcfmMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021DdcfmMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021DdcfmMIB(struct 
 * @fn void revert_ieee8021DdcfmMIB(struct ieee8021DdcfmMIB_data *StorageTmp, struct ieee8021DdcfmMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021DdcfmMIB(struct ieee8021DdcfmMIB_data *StorageTmp, struct ieee8021DdcfmMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021DdcfmMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021DdcfmMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021DdcfmMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021DdcfmMIB(int force)
{
	if (ieee8021DdcfmMIBStorage == NULL) {
		struct ieee8021DdcfmMIB_data *StorageNew;

		if ((StorageNew = ieee8021DdcfmMIB_create()) == NULL)
			return;
		ieee8021DdcfmMIBStorage = StorageNew;
		ieee8021DdcfmMIB_refresh = 1;
	}
	if (!force && ieee8021DdcfmMIB_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "refresh_ieee8021DdcfmMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021DdcfmMIB_refresh = 0;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021DdcfmMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021DdcfmMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021DdcfmMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021DdcfmMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "var_ieee8021DdcfmMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021DdcfmMIB(0);
	if ((StorageTmp = ieee8021DdcfmMIBStorage) == NULL) {
		DEBUGMSGTL(("ieee8021DdcfmMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021DdcfmMIB", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021DdcfmMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021DdcfmStackTable_data *ieee8021DdcfmStackTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021DdcfmStackTable table.
 *
 * Creates a new ieee8021DdcfmStackTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021DdcfmStackTable_data *
ieee8021DdcfmStackTable_create(void)
{
	struct ieee8021DdcfmStackTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmStackTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmStackTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021DdcfmStackRrMdLevel = 0;
		StorageNew->ieee8021DdcfmStackRrDirection = 0;
		StorageNew->ieee8021DdcfmStackRFMreceiverMdLevel = 0;
		StorageNew->ieee8021DdcfmStackDrMdLevel = 0;
		StorageNew->ieee8021DdcfmStackDrVlanIdOrNone = 0;
		StorageNew->ieee8021DdcfmStackSFMOriginatorMdLevel = 0;
		StorageNew->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone = 0;
		StorageNew->ieee8021DdcfmStackSFMOriginatorDirection = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021DdcfmStackTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021DdcfmStackTable_data *ieee8021DdcfmStackTable_duplicate(struct ieee8021DdcfmStackTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021DdcfmStackTable_data *
ieee8021DdcfmStackTable_duplicate(struct ieee8021DdcfmStackTable_data *thedata)
{
	struct ieee8021DdcfmStackTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmStackTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmStackTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021DdcfmStackTable_id = thedata->ieee8021DdcfmStackTable_id;
		StorageNew->ieee8021DdcfmStackIfIndex = thedata->ieee8021DdcfmStackIfIndex;
		StorageNew->ieee8021DdcfmStackRrMdLevel = thedata->ieee8021DdcfmStackRrMdLevel;
		StorageNew->ieee8021DdcfmStackRrDirection = thedata->ieee8021DdcfmStackRrDirection;
		StorageNew->ieee8021DdcfmStackRFMreceiverMdLevel = thedata->ieee8021DdcfmStackRFMreceiverMdLevel;
		StorageNew->ieee8021DdcfmStackDrMdLevel = thedata->ieee8021DdcfmStackDrMdLevel;
		StorageNew->ieee8021DdcfmStackDrVlanIdOrNone = thedata->ieee8021DdcfmStackDrVlanIdOrNone;
		StorageNew->ieee8021DdcfmStackSFMOriginatorMdLevel = thedata->ieee8021DdcfmStackSFMOriginatorMdLevel;
		StorageNew->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone = thedata->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone;
		StorageNew->ieee8021DdcfmStackSFMOriginatorDirection = thedata->ieee8021DdcfmStackSFMOriginatorDirection;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021DdcfmStackTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021DdcfmStackTable_destroy(struct ieee8021DdcfmStackTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021DdcfmStackTable_destroy(struct ieee8021DdcfmStackTable_data **thedata)
{
	struct ieee8021DdcfmStackTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmStackTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmStackTable_add(struct ieee8021DdcfmStackTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021DdcfmStackTable table data set.
 *
 * Adds a table row structure to the ieee8021DdcfmStackTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021DdcfmStackTable_add(struct ieee8021DdcfmStackTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmStackTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021DdcfmStackIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmStackIfIndex, sizeof(thedata->ieee8021DdcfmStackIfIndex));
		header_complex_add_data(&ieee8021DdcfmStackTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmStackTable_del(struct ieee8021DdcfmStackTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021DdcfmStackTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021DdcfmStackTable_del(struct ieee8021DdcfmStackTable_data *thedata)
{
	struct ieee8021DdcfmStackTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmStackTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021DdcfmStackTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021DdcfmStackTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021DdcfmStackTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021DdcfmStackTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021DdcfmStackTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021DdcfmStackTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021DdcfmStackTable_data *StorageTmp = ieee8021DdcfmStackTable_create();

	DEBUGMSGTL(("ieee8021DdcfmMIB", "parse_ieee8021DdcfmStackTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackRrMdLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackRrDirection, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackRFMreceiverMdLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackDrMdLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackDrVlanIdOrNone, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackSFMOriginatorMdLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmStackSFMOriginatorDirection, &tmpsize);
	ieee8021DdcfmStackTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/*
 * store_ieee8021DdcfmStackTable(): store configuraiton file for ieee8021DdcfmStackTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021DdcfmStackTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021DdcfmStackTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "store_ieee8021DdcfmStackTable: storing data...  "));
	refresh_ieee8021DdcfmStackTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021DdcfmStackTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021DdcfmStackTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021DdcfmStackTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackRrMdLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackRrDirection, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackRFMreceiverMdLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackDrMdLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackDrVlanIdOrNone, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackSFMOriginatorMdLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmStackSFMOriginatorDirection, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021DdcfmRrTable_data *ieee8021DdcfmRrTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021DdcfmRrTable table.
 *
 * Creates a new ieee8021DdcfmRrTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021DdcfmRrTable_data *
ieee8021DdcfmRrTable_create(void)
{
	struct ieee8021DdcfmRrTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmRrTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRrTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021DdcfmRrPrimaryVlanIdOrNone = 0;
		if ((StorageNew->ieee8021DdcfmRrFilter = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021DdcfmRrFilterLen = 0;
		StorageNew->ieee8021DdcfmRrFilter[StorageNew->ieee8021DdcfmRrFilterLen] = 0;
		StorageNew->ieee8021DdcfmRrSamplingInterval = 0;
		if ((StorageNew->ieee8021DdcfmRrTargetAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021DdcfmRrTargetAddressLen = 0;
		StorageNew->ieee8021DdcfmRrTargetAddress[StorageNew->ieee8021DdcfmRrTargetAddressLen] = 0;
		StorageNew->ieee8021DdcfmRrContinueFlag = TV_TRUE;
		StorageNew->ieee8021DdcfmRrDuration = 0;
		StorageNew->ieee8021DdcfmRrDurationInTimeFlag = TV_TRUE;
		StorageNew->ieee8021DdcfmRrVlanPriority = 7;
		StorageNew->ieee8021DdcfmRrVlanDropEligible = TV_FALSE;
		StorageNew->ieee8021DdcfmRrFloodingFlag = TV_TRUE;
		StorageNew->ieee8021DdcfmRrTruncationFlag = TV_TRUE;
		StorageNew->ieee8021DdcfmRrActivationStatus = TV_FALSE;
		StorageNew->ieee8021DdcfmRrRemainDuration = 0;
		StorageNew->ieee8021DdcfmRrNextRfmTransID = 0;
		StorageNew->ieee8021DdcfmRrRowStatus = 0;
		StorageNew->ieee8021DdcfmRrRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021DdcfmRrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021DdcfmRrTable_data *ieee8021DdcfmRrTable_duplicate(struct ieee8021DdcfmRrTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021DdcfmRrTable_data *
ieee8021DdcfmRrTable_duplicate(struct ieee8021DdcfmRrTable_data *thedata)
{
	struct ieee8021DdcfmRrTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmRrTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRrTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021DdcfmRrTable_id = thedata->ieee8021DdcfmRrTable_id;
		StorageNew->ieee8021DdcfmRrIfIndex = thedata->ieee8021DdcfmRrIfIndex;
		StorageNew->ieee8021DdcfmRrMdIndex = thedata->ieee8021DdcfmRrMdIndex;
		StorageNew->ieee8021DdcfmRrDirection = thedata->ieee8021DdcfmRrDirection;
		StorageNew->ieee8021DdcfmRrPrimaryVlanIdOrNone = thedata->ieee8021DdcfmRrPrimaryVlanIdOrNone;
		if (!(StorageNew->ieee8021DdcfmRrFilter = malloc(thedata->ieee8021DdcfmRrFilterLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021DdcfmRrFilter, thedata->ieee8021DdcfmRrFilter, thedata->ieee8021DdcfmRrFilterLen);
		StorageNew->ieee8021DdcfmRrFilterLen = thedata->ieee8021DdcfmRrFilterLen;
		StorageNew->ieee8021DdcfmRrFilter[StorageNew->ieee8021DdcfmRrFilterLen] = 0;
		StorageNew->ieee8021DdcfmRrSamplingInterval = thedata->ieee8021DdcfmRrSamplingInterval;
		if (!(StorageNew->ieee8021DdcfmRrTargetAddress = malloc(thedata->ieee8021DdcfmRrTargetAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021DdcfmRrTargetAddress, thedata->ieee8021DdcfmRrTargetAddress, thedata->ieee8021DdcfmRrTargetAddressLen);
		StorageNew->ieee8021DdcfmRrTargetAddressLen = thedata->ieee8021DdcfmRrTargetAddressLen;
		StorageNew->ieee8021DdcfmRrTargetAddress[StorageNew->ieee8021DdcfmRrTargetAddressLen] = 0;
		StorageNew->ieee8021DdcfmRrContinueFlag = thedata->ieee8021DdcfmRrContinueFlag;
		StorageNew->ieee8021DdcfmRrDuration = thedata->ieee8021DdcfmRrDuration;
		StorageNew->ieee8021DdcfmRrDurationInTimeFlag = thedata->ieee8021DdcfmRrDurationInTimeFlag;
		StorageNew->ieee8021DdcfmRrVlanPriority = thedata->ieee8021DdcfmRrVlanPriority;
		StorageNew->ieee8021DdcfmRrVlanDropEligible = thedata->ieee8021DdcfmRrVlanDropEligible;
		StorageNew->ieee8021DdcfmRrFloodingFlag = thedata->ieee8021DdcfmRrFloodingFlag;
		StorageNew->ieee8021DdcfmRrTruncationFlag = thedata->ieee8021DdcfmRrTruncationFlag;
		StorageNew->ieee8021DdcfmRrActivationStatus = thedata->ieee8021DdcfmRrActivationStatus;
		StorageNew->ieee8021DdcfmRrRemainDuration = thedata->ieee8021DdcfmRrRemainDuration;
		StorageNew->ieee8021DdcfmRrNextRfmTransID = thedata->ieee8021DdcfmRrNextRfmTransID;
		StorageNew->ieee8021DdcfmRrRowStatus = thedata->ieee8021DdcfmRrRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021DdcfmRrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021DdcfmRrTable_destroy(struct ieee8021DdcfmRrTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021DdcfmRrTable_destroy(struct ieee8021DdcfmRrTable_data **thedata)
{
	struct ieee8021DdcfmRrTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRrTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021DdcfmRrFilter);
		StorageDel->ieee8021DdcfmRrFilterLen = 0;
		SNMP_FREE(StorageDel->ieee8021DdcfmRrTargetAddress);
		StorageDel->ieee8021DdcfmRrTargetAddressLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmRrTable_add(struct ieee8021DdcfmRrTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021DdcfmRrTable table data set.
 *
 * Adds a table row structure to the ieee8021DdcfmRrTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021DdcfmRrTable_add(struct ieee8021DdcfmRrTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRrTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021DdcfmRrIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmRrIfIndex, sizeof(thedata->ieee8021DdcfmRrIfIndex));
		/* ieee8021DdcfmRrMdIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmRrMdIndex, sizeof(thedata->ieee8021DdcfmRrMdIndex));
		/* ieee8021DdcfmRrDirection */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmRrDirection, sizeof(thedata->ieee8021DdcfmRrDirection));
		header_complex_add_data(&ieee8021DdcfmRrTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmRrTable_del(struct ieee8021DdcfmRrTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021DdcfmRrTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021DdcfmRrTable_del(struct ieee8021DdcfmRrTable_data *thedata)
{
	struct ieee8021DdcfmRrTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRrTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021DdcfmRrTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021DdcfmRrTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021DdcfmRrTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021DdcfmRrTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021DdcfmRrTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021DdcfmRrTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021DdcfmRrTable_data *StorageTmp = ieee8021DdcfmRrTable_create();

	DEBUGMSGTL(("ieee8021DdcfmMIB", "parse_ieee8021DdcfmRrTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrMdIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrDirection, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021DdcfmRrFilter);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021DdcfmRrFilter, &StorageTmp->ieee8021DdcfmRrFilterLen);
	if (StorageTmp->ieee8021DdcfmRrFilter == NULL) {
		config_perror("invalid specification for ieee8021DdcfmRrFilter");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmRrSamplingInterval, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021DdcfmRrTargetAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021DdcfmRrTargetAddress, &StorageTmp->ieee8021DdcfmRrTargetAddressLen);
	if (StorageTmp->ieee8021DdcfmRrTargetAddress == NULL) {
		config_perror("invalid specification for ieee8021DdcfmRrTargetAddress");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrContinueFlag, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmRrDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrDurationInTimeFlag, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrVlanPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrVlanDropEligible, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrFloodingFlag, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrTruncationFlag, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrActivationStatus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmRrRemainDuration, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmRrNextRfmTransID, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRrRowStatus, &tmpsize);
	ieee8021DdcfmRrTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/*
 * store_ieee8021DdcfmRrTable(): store configuraiton file for ieee8021DdcfmRrTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021DdcfmRrTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021DdcfmRrTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "store_ieee8021DdcfmRrTable: storing data...  "));
	refresh_ieee8021DdcfmRrTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021DdcfmRrTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021DdcfmRrTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021DdcfmRrTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrMdIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrDirection, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021DdcfmRrFilter, &StorageTmp->ieee8021DdcfmRrFilterLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmRrSamplingInterval, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021DdcfmRrTargetAddress, &StorageTmp->ieee8021DdcfmRrTargetAddressLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrContinueFlag, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmRrDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrDurationInTimeFlag, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrVlanPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrVlanDropEligible, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrFloodingFlag, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrTruncationFlag, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrActivationStatus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmRrRemainDuration, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmRrNextRfmTransID, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRrRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021DdcfmRFMReceiverTable_data *ieee8021DdcfmRFMReceiverTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021DdcfmRFMReceiverTable table.
 *
 * Creates a new ieee8021DdcfmRFMReceiverTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021DdcfmRFMReceiverTable_data *
ieee8021DdcfmRFMReceiverTable_create(void)
{
	struct ieee8021DdcfmRFMReceiverTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmRFMReceiverTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRFMReceiverTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021DdcfmRFMRowStatus = 0;
		StorageNew->ieee8021DdcfmRFMRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021DdcfmRFMReceiverTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021DdcfmRFMReceiverTable_data *ieee8021DdcfmRFMReceiverTable_duplicate(struct ieee8021DdcfmRFMReceiverTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021DdcfmRFMReceiverTable_data *
ieee8021DdcfmRFMReceiverTable_duplicate(struct ieee8021DdcfmRFMReceiverTable_data *thedata)
{
	struct ieee8021DdcfmRFMReceiverTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmRFMReceiverTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRFMReceiverTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021DdcfmRFMReceiverTable_id = thedata->ieee8021DdcfmRFMReceiverTable_id;
		StorageNew->ieee8021DdcfmRfmReceiverIfIndex = thedata->ieee8021DdcfmRfmReceiverIfIndex;
		StorageNew->ieee8021DdcfmRfmReceiverMdIndex = thedata->ieee8021DdcfmRfmReceiverMdIndex;
		StorageNew->ieee8021DdcfmRFMRowStatus = thedata->ieee8021DdcfmRFMRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021DdcfmRFMReceiverTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021DdcfmRFMReceiverTable_destroy(struct ieee8021DdcfmRFMReceiverTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021DdcfmRFMReceiverTable_destroy(struct ieee8021DdcfmRFMReceiverTable_data **thedata)
{
	struct ieee8021DdcfmRFMReceiverTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRFMReceiverTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmRFMReceiverTable_add(struct ieee8021DdcfmRFMReceiverTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021DdcfmRFMReceiverTable table data set.
 *
 * Adds a table row structure to the ieee8021DdcfmRFMReceiverTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021DdcfmRFMReceiverTable_add(struct ieee8021DdcfmRFMReceiverTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRFMReceiverTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021DdcfmRfmReceiverIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmRfmReceiverIfIndex, sizeof(thedata->ieee8021DdcfmRfmReceiverIfIndex));
		/* ieee8021DdcfmRfmReceiverMdIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmRfmReceiverMdIndex, sizeof(thedata->ieee8021DdcfmRfmReceiverMdIndex));
		header_complex_add_data(&ieee8021DdcfmRFMReceiverTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmRFMReceiverTable_del(struct ieee8021DdcfmRFMReceiverTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021DdcfmRFMReceiverTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021DdcfmRFMReceiverTable_del(struct ieee8021DdcfmRFMReceiverTable_data *thedata)
{
	struct ieee8021DdcfmRFMReceiverTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmRFMReceiverTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021DdcfmRFMReceiverTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021DdcfmRFMReceiverTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021DdcfmRFMReceiverTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021DdcfmRFMReceiverTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021DdcfmRFMReceiverTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021DdcfmRFMReceiverTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp = ieee8021DdcfmRFMReceiverTable_create();

	DEBUGMSGTL(("ieee8021DdcfmMIB", "parse_ieee8021DdcfmRFMReceiverTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRfmReceiverIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRfmReceiverMdIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmRFMRowStatus, &tmpsize);
	ieee8021DdcfmRFMReceiverTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/*
 * store_ieee8021DdcfmRFMReceiverTable(): store configuraiton file for ieee8021DdcfmRFMReceiverTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021DdcfmRFMReceiverTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "store_ieee8021DdcfmRFMReceiverTable: storing data...  "));
	refresh_ieee8021DdcfmRFMReceiverTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021DdcfmRFMReceiverTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021DdcfmRFMReceiverTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021DdcfmRFMReceiverTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRfmReceiverIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRfmReceiverMdIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmRFMRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021DdcfmDrTable_data *ieee8021DdcfmDrTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021DdcfmDrTable table.
 *
 * Creates a new ieee8021DdcfmDrTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021DdcfmDrTable_data *
ieee8021DdcfmDrTable_create(void)
{
	struct ieee8021DdcfmDrTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmDrTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmDrTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021DdcfmDrSfmOriginator = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021DdcfmDrSfmOriginatorLen = 0;
		StorageNew->ieee8021DdcfmDrSfmOriginator[StorageNew->ieee8021DdcfmDrSfmOriginatorLen] = 0;
		StorageNew->ieee8021DdcfmDrSourceAddressStayFlag = TV_TRUE;
		StorageNew->ieee8021DdcfmDrFloodingFlag = TV_TRUE;
		StorageNew->ieee8021DdcfmDrDuration = 0;
		StorageNew->ieee8021DdcfmDrActivationStatus = TV_FALSE;
		StorageNew->ieee8021DdcfmDrRemainDuration = 0;
		StorageNew->ieee8021DdcfmDrSFMsequenceErrors = 0;
		StorageNew->ieee8021DdcfmDrRowStatus = 0;
		StorageNew->ieee8021DdcfmDrRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021DdcfmDrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021DdcfmDrTable_data *ieee8021DdcfmDrTable_duplicate(struct ieee8021DdcfmDrTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021DdcfmDrTable_data *
ieee8021DdcfmDrTable_duplicate(struct ieee8021DdcfmDrTable_data *thedata)
{
	struct ieee8021DdcfmDrTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmDrTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmDrTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021DdcfmDrTable_id = thedata->ieee8021DdcfmDrTable_id;
		StorageNew->ieee8021DdcfmDrIfIndex = thedata->ieee8021DdcfmDrIfIndex;
		StorageNew->ieee8021DdcfmDrMdIndex = thedata->ieee8021DdcfmDrMdIndex;
		StorageNew->ieee8021DdcfmDrVlanIdOrNone = thedata->ieee8021DdcfmDrVlanIdOrNone;
		if (!(StorageNew->ieee8021DdcfmDrSfmOriginator = malloc(thedata->ieee8021DdcfmDrSfmOriginatorLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021DdcfmDrSfmOriginator, thedata->ieee8021DdcfmDrSfmOriginator, thedata->ieee8021DdcfmDrSfmOriginatorLen);
		StorageNew->ieee8021DdcfmDrSfmOriginatorLen = thedata->ieee8021DdcfmDrSfmOriginatorLen;
		StorageNew->ieee8021DdcfmDrSfmOriginator[StorageNew->ieee8021DdcfmDrSfmOriginatorLen] = 0;
		StorageNew->ieee8021DdcfmDrSourceAddressStayFlag = thedata->ieee8021DdcfmDrSourceAddressStayFlag;
		StorageNew->ieee8021DdcfmDrFloodingFlag = thedata->ieee8021DdcfmDrFloodingFlag;
		StorageNew->ieee8021DdcfmDrDuration = thedata->ieee8021DdcfmDrDuration;
		StorageNew->ieee8021DdcfmDrActivationStatus = thedata->ieee8021DdcfmDrActivationStatus;
		StorageNew->ieee8021DdcfmDrRemainDuration = thedata->ieee8021DdcfmDrRemainDuration;
		StorageNew->ieee8021DdcfmDrSFMsequenceErrors = thedata->ieee8021DdcfmDrSFMsequenceErrors;
		StorageNew->ieee8021DdcfmDrRowStatus = thedata->ieee8021DdcfmDrRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021DdcfmDrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021DdcfmDrTable_destroy(struct ieee8021DdcfmDrTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021DdcfmDrTable_destroy(struct ieee8021DdcfmDrTable_data **thedata)
{
	struct ieee8021DdcfmDrTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmDrTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021DdcfmDrSfmOriginator);
		StorageDel->ieee8021DdcfmDrSfmOriginatorLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmDrTable_add(struct ieee8021DdcfmDrTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021DdcfmDrTable table data set.
 *
 * Adds a table row structure to the ieee8021DdcfmDrTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021DdcfmDrTable_add(struct ieee8021DdcfmDrTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmDrTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021DdcfmDrIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmDrIfIndex, sizeof(thedata->ieee8021DdcfmDrIfIndex));
		/* ieee8021DdcfmDrMdIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmDrMdIndex, sizeof(thedata->ieee8021DdcfmDrMdIndex));
		/* ieee8021DdcfmDrVlanIdOrNone */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmDrVlanIdOrNone, sizeof(thedata->ieee8021DdcfmDrVlanIdOrNone));
		header_complex_add_data(&ieee8021DdcfmDrTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmDrTable_del(struct ieee8021DdcfmDrTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021DdcfmDrTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021DdcfmDrTable_del(struct ieee8021DdcfmDrTable_data *thedata)
{
	struct ieee8021DdcfmDrTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmDrTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021DdcfmDrTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021DdcfmDrTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021DdcfmDrTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021DdcfmDrTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021DdcfmDrTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021DdcfmDrTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021DdcfmDrTable_data *StorageTmp = ieee8021DdcfmDrTable_create();

	DEBUGMSGTL(("ieee8021DdcfmMIB", "parse_ieee8021DdcfmDrTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrMdIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrVlanIdOrNone, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021DdcfmDrSfmOriginator);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021DdcfmDrSfmOriginator, &StorageTmp->ieee8021DdcfmDrSfmOriginatorLen);
	if (StorageTmp->ieee8021DdcfmDrSfmOriginator == NULL) {
		config_perror("invalid specification for ieee8021DdcfmDrSfmOriginator");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrFloodingFlag, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmDrDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrActivationStatus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmDrRemainDuration, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmDrSFMsequenceErrors, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmDrRowStatus, &tmpsize);
	ieee8021DdcfmDrTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/*
 * store_ieee8021DdcfmDrTable(): store configuraiton file for ieee8021DdcfmDrTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021DdcfmDrTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021DdcfmDrTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "store_ieee8021DdcfmDrTable: storing data...  "));
	refresh_ieee8021DdcfmDrTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021DdcfmDrTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021DdcfmDrTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021DdcfmDrTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrMdIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrVlanIdOrNone, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021DdcfmDrSfmOriginator, &StorageTmp->ieee8021DdcfmDrSfmOriginatorLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrFloodingFlag, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmDrDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrActivationStatus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmDrRemainDuration, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmDrSFMsequenceErrors, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmDrRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021DdcfmSoTable_data *ieee8021DdcfmSoTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021DdcfmSoTable table.
 *
 * Creates a new ieee8021DdcfmSoTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021DdcfmSoTable_data *
ieee8021DdcfmSoTable_create(void)
{
	struct ieee8021DdcfmSoTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmSoTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmSoTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021DdcfmSoDrMacAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021DdcfmSoDrMacAddressLen = 0;
		StorageNew->ieee8021DdcfmSoDrMacAddress[StorageNew->ieee8021DdcfmSoDrMacAddressLen] = 0;
		StorageNew->ieee8021DdcfmSoDuration = 0;
		StorageNew->ieee8021DdcfmSoActivationStatus = TV_FALSE;
		StorageNew->ieee8021DdcfmSoRemainDuration = 0;
		StorageNew->ieee8021DdcfmSoRowStatus = 0;
		StorageNew->ieee8021DdcfmSoRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021DdcfmSoTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021DdcfmSoTable_data *ieee8021DdcfmSoTable_duplicate(struct ieee8021DdcfmSoTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021DdcfmSoTable_data *
ieee8021DdcfmSoTable_duplicate(struct ieee8021DdcfmSoTable_data *thedata)
{
	struct ieee8021DdcfmSoTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021DdcfmSoTable_data);

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmSoTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021DdcfmSoTable_id = thedata->ieee8021DdcfmSoTable_id;
		StorageNew->ieee8021DdcfmSfmIfIndex = thedata->ieee8021DdcfmSfmIfIndex;
		StorageNew->ieee8021DdcfmSoMdIndex = thedata->ieee8021DdcfmSoMdIndex;
		StorageNew->ieee8021DdcfmSoVlanIdOrNone = thedata->ieee8021DdcfmSoVlanIdOrNone;
		StorageNew->ieee8021DdcfmSoDirection = thedata->ieee8021DdcfmSoDirection;
		if (!(StorageNew->ieee8021DdcfmSoDrMacAddress = malloc(thedata->ieee8021DdcfmSoDrMacAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021DdcfmSoDrMacAddress, thedata->ieee8021DdcfmSoDrMacAddress, thedata->ieee8021DdcfmSoDrMacAddressLen);
		StorageNew->ieee8021DdcfmSoDrMacAddressLen = thedata->ieee8021DdcfmSoDrMacAddressLen;
		StorageNew->ieee8021DdcfmSoDrMacAddress[StorageNew->ieee8021DdcfmSoDrMacAddressLen] = 0;
		StorageNew->ieee8021DdcfmSoDuration = thedata->ieee8021DdcfmSoDuration;
		StorageNew->ieee8021DdcfmSoActivationStatus = thedata->ieee8021DdcfmSoActivationStatus;
		StorageNew->ieee8021DdcfmSoRemainDuration = thedata->ieee8021DdcfmSoRemainDuration;
		StorageNew->ieee8021DdcfmSoRowStatus = thedata->ieee8021DdcfmSoRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021DdcfmSoTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021DdcfmSoTable_destroy(struct ieee8021DdcfmSoTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021DdcfmSoTable_destroy(struct ieee8021DdcfmSoTable_data **thedata)
{
	struct ieee8021DdcfmSoTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmSoTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021DdcfmSoDrMacAddress);
		StorageDel->ieee8021DdcfmSoDrMacAddressLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmSoTable_add(struct ieee8021DdcfmSoTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021DdcfmSoTable table data set.
 *
 * Adds a table row structure to the ieee8021DdcfmSoTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021DdcfmSoTable_add(struct ieee8021DdcfmSoTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmSoTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021DdcfmSfmIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmSfmIfIndex, sizeof(thedata->ieee8021DdcfmSfmIfIndex));
		/* ieee8021DdcfmSoMdIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmSoMdIndex, sizeof(thedata->ieee8021DdcfmSoMdIndex));
		/* ieee8021DdcfmSoVlanIdOrNone */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmSoVlanIdOrNone, sizeof(thedata->ieee8021DdcfmSoVlanIdOrNone));
		/* ieee8021DdcfmSoDirection */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021DdcfmSoDirection, sizeof(thedata->ieee8021DdcfmSoDirection));
		header_complex_add_data(&ieee8021DdcfmSoTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021DdcfmSoTable_del(struct ieee8021DdcfmSoTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021DdcfmSoTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021DdcfmSoTable_del(struct ieee8021DdcfmSoTable_data *thedata)
{
	struct ieee8021DdcfmSoTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmSoTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021DdcfmSoTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021DdcfmSoTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021DdcfmSoTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021DdcfmSoTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021DdcfmSoTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021DdcfmSoTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021DdcfmSoTable_data *StorageTmp = ieee8021DdcfmSoTable_create();

	DEBUGMSGTL(("ieee8021DdcfmMIB", "parse_ieee8021DdcfmSoTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmSfmIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmSoMdIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmSoVlanIdOrNone, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmSoDirection, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021DdcfmSoDrMacAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021DdcfmSoDrMacAddress, &StorageTmp->ieee8021DdcfmSoDrMacAddressLen);
	if (StorageTmp->ieee8021DdcfmSoDrMacAddress == NULL) {
		config_perror("invalid specification for ieee8021DdcfmSoDrMacAddress");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmSoDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmSoActivationStatus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021DdcfmSoRemainDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021DdcfmSoRowStatus, &tmpsize);
	ieee8021DdcfmSoTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/*
 * store_ieee8021DdcfmSoTable(): store configuraiton file for ieee8021DdcfmSoTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021DdcfmSoTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021DdcfmSoTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "store_ieee8021DdcfmSoTable: storing data...  "));
	refresh_ieee8021DdcfmSoTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021DdcfmSoTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021DdcfmSoTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021DdcfmSoTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmSfmIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmSoMdIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmSoVlanIdOrNone, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmSoDirection, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021DdcfmSoDrMacAddress, &StorageTmp->ieee8021DdcfmSoDrMacAddressLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmSoDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmSoActivationStatus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021DdcfmSoRemainDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021DdcfmSoRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, struct ieee8021DdcfmStackTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, struct ieee8021DdcfmStackTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, struct ieee8021DdcfmStackTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021DdcfmStackTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, struct ieee8021DdcfmStackTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021DdcfmStackTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, struct ieee8021DdcfmStackTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, struct ieee8021DdcfmStackTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021DdcfmStackTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021DdcfmStackTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021DdcfmStackTable_data *
refresh_ieee8021DdcfmStackTable_row(struct ieee8021DdcfmStackTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021DdcfmStackTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021DdcfmStackTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021DdcfmStackTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021DdcfmStackTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021DdcfmStackTable(int force)
{
	if (!force && ieee8021DdcfmStackTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021DdcfmStackTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021DdcfmStackTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021DdcfmStackTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021DdcfmMIB above.
 */
u_char *
var_ieee8021DdcfmStackTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021DdcfmStackTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "var_ieee8021DdcfmStackTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021DdcfmStackTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021DdcfmStackTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021DdcfmStackTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021DDCFMSTACKRRMDLEVEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackRrMdLevel);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackRrMdLevel;
		break;
	case (u_char) IEEE8021DDCFMSTACKRRDIRECTION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackRrDirection);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackRrDirection;
		break;
	case (u_char) IEEE8021DDCFMSTACKRFMRECEIVERMDLEVEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackRFMreceiverMdLevel);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackRFMreceiverMdLevel;
		break;
	case (u_char) IEEE8021DDCFMSTACKDRMDLEVEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackDrMdLevel);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackDrMdLevel;
		break;
	case (u_char) IEEE8021DDCFMSTACKDRVLANIDORNONE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackDrVlanIdOrNone);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackDrVlanIdOrNone;
		break;
	case (u_char) IEEE8021DDCFMSTACKSFMORIGINATORMDLEVEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackSFMOriginatorMdLevel);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackSFMOriginatorMdLevel;
		break;
	case (u_char) IEEE8021DDCFMSTACKSFMORIGINATORVLANIDORNONE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackSFMOriginatorVlanIdOrNone;
		break;
	case (u_char) IEEE8021DDCFMSTACKSFMORIGINATORDIRECTION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmStackSFMOriginatorDirection);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmStackSFMOriginatorDirection;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, struct ieee8021DdcfmRrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, struct ieee8021DdcfmRrTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, struct ieee8021DdcfmRrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021DdcfmRrTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, struct ieee8021DdcfmRrTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021DdcfmRrTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, struct ieee8021DdcfmRrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, struct ieee8021DdcfmRrTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021DdcfmRrTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021DdcfmRrTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021DdcfmRrTable_data *
refresh_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021DdcfmRrTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021DdcfmRrTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021DdcfmRrTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021DdcfmRrTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021DdcfmRrTable(int force)
{
	if (!force && ieee8021DdcfmRrTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021DdcfmRrTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021DdcfmRrTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021DdcfmRrTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021DdcfmMIB above.
 */
u_char *
var_ieee8021DdcfmRrTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "var_ieee8021DdcfmRrTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021DdcfmRrTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021DdcfmRrTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021DDCFMRRPRIMARYVLANIDORNONE:	/* Create */
		*write_method = write_ieee8021DdcfmRrPrimaryVlanIdOrNone;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone;
		break;
	case (u_char) IEEE8021DDCFMRRFILTER:	/* Create */
		*write_method = write_ieee8021DdcfmRrFilter;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021DdcfmRrFilterLen;
		rval = (u_char *) StorageTmp->ieee8021DdcfmRrFilter;
		break;
	case (u_char) IEEE8021DDCFMRRSAMPLINGINTERVAL:	/* Create */
		*write_method = write_ieee8021DdcfmRrSamplingInterval;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrSamplingInterval);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrSamplingInterval;
		break;
	case (u_char) IEEE8021DDCFMRRTARGETADDRESS:	/* Create */
		*write_method = write_ieee8021DdcfmRrTargetAddress;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021DdcfmRrTargetAddressLen;
		rval = (u_char *) StorageTmp->ieee8021DdcfmRrTargetAddress;
		break;
	case (u_char) IEEE8021DDCFMRRCONTINUEFLAG:	/* Create */
		*write_method = write_ieee8021DdcfmRrContinueFlag;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrContinueFlag);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrContinueFlag;
		break;
	case (u_char) IEEE8021DDCFMRRDURATION:	/* Create */
		*write_method = write_ieee8021DdcfmRrDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrDuration);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrDuration;
		break;
	case (u_char) IEEE8021DDCFMRRDURATIONINTIMEFLAG:	/* Create */
		*write_method = write_ieee8021DdcfmRrDurationInTimeFlag;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrDurationInTimeFlag);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrDurationInTimeFlag;
		break;
	case (u_char) IEEE8021DDCFMRRVLANPRIORITY:	/* Create */
		*write_method = write_ieee8021DdcfmRrVlanPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrVlanPriority);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrVlanPriority;
		break;
	case (u_char) IEEE8021DDCFMRRVLANDROPELIGIBLE:	/* Create */
		*write_method = write_ieee8021DdcfmRrVlanDropEligible;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrVlanDropEligible);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrVlanDropEligible;
		break;
	case (u_char) IEEE8021DDCFMRRFLOODINGFLAG:	/* Create */
		*write_method = write_ieee8021DdcfmRrFloodingFlag;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrFloodingFlag);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrFloodingFlag;
		break;
	case (u_char) IEEE8021DDCFMRRTRUNCATIONFLAG:	/* Create */
		*write_method = write_ieee8021DdcfmRrTruncationFlag;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrTruncationFlag);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrTruncationFlag;
		break;
	case (u_char) IEEE8021DDCFMRRACTIVATIONSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrActivationStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrActivationStatus;
		break;
	case (u_char) IEEE8021DDCFMRRREMAINDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrRemainDuration);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrRemainDuration;
		break;
	case (u_char) IEEE8021DDCFMRRNEXTRFMTRANSID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrNextRfmTransID);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrNextRfmTransID;
		break;
	case (u_char) IEEE8021DDCFMRRROWSTATUS:	/* Create */
		*write_method = write_ieee8021DdcfmRrRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRrRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRrRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, struct ieee8021DdcfmRFMReceiverTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, struct ieee8021DdcfmRFMReceiverTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, struct ieee8021DdcfmRFMReceiverTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021DdcfmRFMReceiverTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, struct ieee8021DdcfmRFMReceiverTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021DdcfmRFMReceiverTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, struct ieee8021DdcfmRFMReceiverTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, struct ieee8021DdcfmRFMReceiverTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021DdcfmRFMReceiverTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021DdcfmRFMReceiverTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021DdcfmRFMReceiverTable_data *
refresh_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021DdcfmRFMReceiverTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021DdcfmRFMReceiverTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021DdcfmRFMReceiverTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021DdcfmRFMReceiverTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021DdcfmRFMReceiverTable(int force)
{
	if (!force && ieee8021DdcfmRFMReceiverTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021DdcfmRFMReceiverTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021DdcfmRFMReceiverTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021DdcfmRFMReceiverTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021DdcfmMIB above.
 */
u_char *
var_ieee8021DdcfmRFMReceiverTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "var_ieee8021DdcfmRFMReceiverTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021DdcfmRFMReceiverTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021DdcfmRFMReceiverTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021DdcfmRFMReceiverTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021DDCFMRFMROWSTATUS:	/* Create */
		*write_method = write_ieee8021DdcfmRFMRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmRFMRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmRFMRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, struct ieee8021DdcfmDrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, struct ieee8021DdcfmDrTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, struct ieee8021DdcfmDrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021DdcfmDrTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, struct ieee8021DdcfmDrTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021DdcfmDrTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, struct ieee8021DdcfmDrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, struct ieee8021DdcfmDrTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021DdcfmDrTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021DdcfmDrTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021DdcfmDrTable_data *
refresh_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021DdcfmDrTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021DdcfmDrTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021DdcfmDrTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021DdcfmDrTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021DdcfmDrTable(int force)
{
	if (!force && ieee8021DdcfmDrTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021DdcfmDrTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021DdcfmDrTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021DdcfmDrTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021DdcfmMIB above.
 */
u_char *
var_ieee8021DdcfmDrTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021DdcfmDrTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "var_ieee8021DdcfmDrTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021DdcfmDrTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021DdcfmDrTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021DdcfmDrTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021DDCFMDRSFMORIGINATOR:	/* Create */
		*write_method = write_ieee8021DdcfmDrSfmOriginator;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021DdcfmDrSfmOriginatorLen;
		rval = (u_char *) StorageTmp->ieee8021DdcfmDrSfmOriginator;
		break;
	case (u_char) IEEE8021DDCFMDRSOURCEADDRESSSTAYFLAG:	/* Create */
		*write_method = write_ieee8021DdcfmDrSourceAddressStayFlag;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag;
		break;
	case (u_char) IEEE8021DDCFMDRFLOODINGFLAG:	/* Create */
		*write_method = write_ieee8021DdcfmDrFloodingFlag;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrFloodingFlag);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrFloodingFlag;
		break;
	case (u_char) IEEE8021DDCFMDRDURATION:	/* Create */
		*write_method = write_ieee8021DdcfmDrDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrDuration);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrDuration;
		break;
	case (u_char) IEEE8021DDCFMDRACTIVATIONSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrActivationStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrActivationStatus;
		break;
	case (u_char) IEEE8021DDCFMDRREMAINDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrRemainDuration);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrRemainDuration;
		break;
	case (u_char) IEEE8021DDCFMDRSFMSEQUENCEERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrSFMsequenceErrors);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrSFMsequenceErrors;
		break;
	case (u_char) IEEE8021DDCFMDRROWSTATUS:	/* Create */
		*write_method = write_ieee8021DdcfmDrRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmDrRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmDrRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, struct ieee8021DdcfmSoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, struct ieee8021DdcfmSoTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, struct ieee8021DdcfmSoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021DdcfmSoTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, struct ieee8021DdcfmSoTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021DdcfmSoTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, struct ieee8021DdcfmSoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, struct ieee8021DdcfmSoTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021DdcfmSoTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021DdcfmSoTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021DdcfmSoTable_data *
refresh_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021DdcfmSoTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021DdcfmSoTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021DdcfmSoTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021DdcfmSoTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021DdcfmSoTable(int force)
{
	if (!force && ieee8021DdcfmSoTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021DdcfmSoTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021DdcfmSoTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021DdcfmSoTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021DdcfmMIB above.
 */
u_char *
var_ieee8021DdcfmSoTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021DdcfmSoTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "var_ieee8021DdcfmSoTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021DdcfmSoTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021DdcfmSoTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021DdcfmSoTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021DDCFMSODRMACADDRESS:	/* Create */
		*write_method = write_ieee8021DdcfmSoDrMacAddress;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021DdcfmSoDrMacAddressLen;
		rval = (u_char *) StorageTmp->ieee8021DdcfmSoDrMacAddress;
		break;
	case (u_char) IEEE8021DDCFMSODURATION:	/* Create */
		*write_method = write_ieee8021DdcfmSoDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmSoDuration);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmSoDuration;
		break;
	case (u_char) IEEE8021DDCFMSOACTIVATIONSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmSoActivationStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmSoActivationStatus;
		break;
	case (u_char) IEEE8021DDCFMSOREMAINDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmSoRemainDuration);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmSoRemainDuration;
		break;
	case (u_char) IEEE8021DDCFMSOROWSTATUS:	/* Create */
		*write_method = write_ieee8021DdcfmSoRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021DdcfmSoRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021DdcfmSoRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021DdcfmRrPrimaryVlanIdOrNone(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrPrimaryVlanIdOrNone(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrPrimaryVlanIdOrNone entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrPrimaryVlanIdOrNone: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrPrimaryVlanIdOrNone not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrPrimaryVlanIdOrNone: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* Note: ranges 0..0 1..4094 */
		if (set_value != 0 && (1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrPrimaryVlanIdOrNone: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrPrimaryVlanIdOrNone = StorageOld->ieee8021DdcfmRrPrimaryVlanIdOrNone;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrFilter(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrFilter(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrFilter entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFilter: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFilter not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..1500 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 1500))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFilter: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021DdcfmRrFilter);
		StorageTmp->ieee8021DdcfmRrFilter = string;
		StorageTmp->ieee8021DdcfmRrFilterLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrFilter for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		if (StorageOld->ieee8021DdcfmRrFilter != NULL) {
			SNMP_FREE(StorageTmp->ieee8021DdcfmRrFilter);
			StorageTmp->ieee8021DdcfmRrFilter = StorageOld->ieee8021DdcfmRrFilter;
			StorageTmp->ieee8021DdcfmRrFilterLen = StorageOld->ieee8021DdcfmRrFilterLen;
			StorageOld->ieee8021DdcfmRrFilter = NULL;
			StorageOld->ieee8021DdcfmRrFilterLen = 0;
		}
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrSamplingInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrSamplingInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrSamplingInterval entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrSamplingInterval: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrSamplingInterval not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrSamplingInterval: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrSamplingInterval = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrSamplingInterval for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrSamplingInterval = StorageOld->ieee8021DdcfmRrSamplingInterval;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrTargetAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrTargetAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrTargetAddress entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTargetAddress: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTargetAddress not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTargetAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021DdcfmRrTargetAddress);
		StorageTmp->ieee8021DdcfmRrTargetAddress = string;
		StorageTmp->ieee8021DdcfmRrTargetAddressLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrTargetAddress for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		if (StorageOld->ieee8021DdcfmRrTargetAddress != NULL) {
			SNMP_FREE(StorageTmp->ieee8021DdcfmRrTargetAddress);
			StorageTmp->ieee8021DdcfmRrTargetAddress = StorageOld->ieee8021DdcfmRrTargetAddress;
			StorageTmp->ieee8021DdcfmRrTargetAddressLen = StorageOld->ieee8021DdcfmRrTargetAddressLen;
			StorageOld->ieee8021DdcfmRrTargetAddress = NULL;
			StorageOld->ieee8021DdcfmRrTargetAddressLen = 0;
		}
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrContinueFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrContinueFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrContinueFlag entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrContinueFlag: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrContinueFlag not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrContinueFlag: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrContinueFlag: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrContinueFlag = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrContinueFlag for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrContinueFlag = StorageOld->ieee8021DdcfmRrContinueFlag;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDuration not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrDuration for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrDuration = StorageOld->ieee8021DdcfmRrDuration;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrDurationInTimeFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrDurationInTimeFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrDurationInTimeFlag entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDurationInTimeFlag: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDurationInTimeFlag not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDurationInTimeFlag: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrDurationInTimeFlag: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrDurationInTimeFlag = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrDurationInTimeFlag for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrDurationInTimeFlag = StorageOld->ieee8021DdcfmRrDurationInTimeFlag;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrVlanPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrVlanPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrVlanPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanPriority: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanPriority not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 7 */
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrVlanPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrVlanPriority for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrVlanPriority = StorageOld->ieee8021DdcfmRrVlanPriority;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrVlanDropEligible(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrVlanDropEligible(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrVlanDropEligible entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanDropEligible: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanDropEligible not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanDropEligible: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrVlanDropEligible: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrVlanDropEligible = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrVlanDropEligible for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrVlanDropEligible = StorageOld->ieee8021DdcfmRrVlanDropEligible;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrFloodingFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrFloodingFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrFloodingFlag entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFloodingFlag: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFloodingFlag not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFloodingFlag: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrFloodingFlag: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrFloodingFlag = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrFloodingFlag for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrFloodingFlag = StorageOld->ieee8021DdcfmRrFloodingFlag;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrTruncationFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmRrTruncationFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrTruncationFlag entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmRrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTruncationFlag: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTruncationFlag not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTruncationFlag: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrTruncationFlag: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmRrTable_rsvs++;
		StorageTmp->ieee8021DdcfmRrTruncationFlag = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmRrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrTruncationFlag for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmRrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmRrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmRrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmRrTable_sets == 0)
			revert_ieee8021DdcfmRrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmRrTruncationFlag = StorageOld->ieee8021DdcfmRrTruncationFlag;
		if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
			ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmDrSfmOriginator(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmDrSfmOriginator(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmDrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmDrSfmOriginator entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmDrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmDrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSfmOriginator: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSfmOriginator not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSfmOriginator: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old = ieee8021DdcfmDrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmDrTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021DdcfmDrSfmOriginator);
		StorageTmp->ieee8021DdcfmDrSfmOriginator = string;
		StorageTmp->ieee8021DdcfmDrSfmOriginatorLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmDrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmDrSfmOriginator for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmDrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
			StorageTmp->ieee8021DdcfmDrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmDrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmDrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmDrTable_sets == 0)
			revert_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		if (StorageOld->ieee8021DdcfmDrSfmOriginator != NULL) {
			SNMP_FREE(StorageTmp->ieee8021DdcfmDrSfmOriginator);
			StorageTmp->ieee8021DdcfmDrSfmOriginator = StorageOld->ieee8021DdcfmDrSfmOriginator;
			StorageTmp->ieee8021DdcfmDrSfmOriginatorLen = StorageOld->ieee8021DdcfmDrSfmOriginatorLen;
			StorageOld->ieee8021DdcfmDrSfmOriginator = NULL;
			StorageOld->ieee8021DdcfmDrSfmOriginatorLen = 0;
		}
		if (--StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmDrSourceAddressStayFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmDrSourceAddressStayFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmDrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmDrSourceAddressStayFlag entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmDrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmDrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSourceAddressStayFlag: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSourceAddressStayFlag not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSourceAddressStayFlag: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrSourceAddressStayFlag: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old = ieee8021DdcfmDrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmDrTable_rsvs++;
		StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmDrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmDrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
			StorageTmp->ieee8021DdcfmDrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmDrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmDrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmDrTable_sets == 0)
			revert_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmDrSourceAddressStayFlag = StorageOld->ieee8021DdcfmDrSourceAddressStayFlag;
		if (--StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmDrFloodingFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmDrFloodingFlag(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmDrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmDrFloodingFlag entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmDrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmDrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrFloodingFlag: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrFloodingFlag not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrFloodingFlag: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrFloodingFlag: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old = ieee8021DdcfmDrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmDrTable_rsvs++;
		StorageTmp->ieee8021DdcfmDrFloodingFlag = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmDrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmDrFloodingFlag for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmDrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
			StorageTmp->ieee8021DdcfmDrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmDrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmDrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmDrTable_sets == 0)
			revert_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmDrFloodingFlag = StorageOld->ieee8021DdcfmDrFloodingFlag;
		if (--StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmDrDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmDrDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmDrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmDrDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmDrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmDrRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrDuration not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old = ieee8021DdcfmDrTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmDrTable_rsvs++;
		StorageTmp->ieee8021DdcfmDrDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmDrTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmDrDuration for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmDrTable_sets == 0)
				if ((ret = update_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmDrTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
			StorageTmp->ieee8021DdcfmDrTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmDrTable_tsts = 0;
			StorageTmp->ieee8021DdcfmDrTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmDrTable_sets == 0)
			revert_ieee8021DdcfmDrTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmDrDuration = StorageOld->ieee8021DdcfmDrDuration;
		if (--StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
			ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmSoDrMacAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmSoDrMacAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmSoTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmSoDrMacAddress entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmSoTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmSoRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoDrMacAddress: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoDrMacAddress not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoDrMacAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmSoTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old = ieee8021DdcfmSoTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmSoTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021DdcfmSoDrMacAddress);
		StorageTmp->ieee8021DdcfmSoDrMacAddress = string;
		StorageTmp->ieee8021DdcfmSoDrMacAddressLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmSoTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmSoTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmSoTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmSoDrMacAddress for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmSoTable_sets == 0)
				if ((ret = update_ieee8021DdcfmSoTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmSoTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
			ieee8021DdcfmSoTable_destroy(&StorageTmp->ieee8021DdcfmSoTable_old);
			StorageTmp->ieee8021DdcfmSoTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmSoTable_tsts = 0;
			StorageTmp->ieee8021DdcfmSoTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmSoTable_sets == 0)
			revert_ieee8021DdcfmSoTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
			break;
		if (StorageOld->ieee8021DdcfmSoDrMacAddress != NULL) {
			SNMP_FREE(StorageTmp->ieee8021DdcfmSoDrMacAddress);
			StorageTmp->ieee8021DdcfmSoDrMacAddress = StorageOld->ieee8021DdcfmSoDrMacAddress;
			StorageTmp->ieee8021DdcfmSoDrMacAddressLen = StorageOld->ieee8021DdcfmSoDrMacAddressLen;
			StorageOld->ieee8021DdcfmSoDrMacAddress = NULL;
			StorageOld->ieee8021DdcfmSoDrMacAddressLen = 0;
		}
		if (--StorageTmp->ieee8021DdcfmSoTable_rsvs == 0)
			ieee8021DdcfmSoTable_destroy(&StorageTmp->ieee8021DdcfmSoTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmSoDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021DdcfmSoDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmSoTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmSoDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021DdcfmSoTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021DdcfmSoRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoDuration not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
			if (StorageTmp->ieee8021DdcfmSoTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old = ieee8021DdcfmSoTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021DdcfmSoTable_rsvs++;
		StorageTmp->ieee8021DdcfmSoDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021DdcfmSoTable_tsts == 0)
				if ((ret = check_ieee8021DdcfmSoTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmSoTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021DdcfmSoDuration for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021DdcfmSoTable_sets == 0)
				if ((ret = update_ieee8021DdcfmSoTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021DdcfmSoTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
			ieee8021DdcfmSoTable_destroy(&StorageTmp->ieee8021DdcfmSoTable_old);
			StorageTmp->ieee8021DdcfmSoTable_rsvs = 0;
			StorageTmp->ieee8021DdcfmSoTable_tsts = 0;
			StorageTmp->ieee8021DdcfmSoTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021DdcfmSoTable_sets == 0)
			revert_ieee8021DdcfmSoTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
			break;
		StorageTmp->ieee8021DdcfmSoDuration = StorageOld->ieee8021DdcfmSoDuration;
		if (--StorageTmp->ieee8021DdcfmSoTable_rsvs == 0)
			ieee8021DdcfmSoTable_destroy(&StorageTmp->ieee8021DdcfmSoTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021DdcfmRrTable_row(struct ieee8021DdcfmRrTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021DdcfmRFMReceiverTable_row(struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021DdcfmDrTable_row(struct ieee8021DdcfmDrTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021DdcfmSoTable_row(struct ieee8021DdcfmSoTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRrRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021DdcfmRrRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021DdcfmRrTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRrRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021DdcfmRrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRrRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021DdcfmRrIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmRrMdIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmRrDirection */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021DdcfmRrIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRrIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRrIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmRrMdIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRrMdIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..7 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 7)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRrMdIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmRrDirection */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRrDirection: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			switch ((long) *vp->val.integer) {
			case IEEE8021DDCFMRRDIRECTION_DOWN:
			case IEEE8021DDCFMRRDIRECTION_UP:
				break;
			default:
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRrDirection: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021DdcfmRrTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021DdcfmRrTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021DdcfmRrIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmRrMdIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmRrDirection = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021DdcfmRrTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021DdcfmRrRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021DdcfmRrTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
				if (StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old = ieee8021DdcfmRrTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021DdcfmRrTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021DdcfmRrTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021DdcfmRrTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021DdcfmRrTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021DdcfmRrRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021DdcfmRrTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmRrRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmRrTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmRrRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmRrTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021DdcfmRrRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021DdcfmRrTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmRrRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021DdcfmRrTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmRrRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021DdcfmRrTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021DdcfmRrTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021DdcfmRrRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021DdcfmRrRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021DdcfmRrRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) != NULL) {
				ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
				StorageTmp->ieee8021DdcfmRrTable_rsvs = 0;
				StorageTmp->ieee8021DdcfmRrTable_tsts = 0;
				StorageTmp->ieee8021DdcfmRrTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021DdcfmRrTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021DdcfmRrTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021DdcfmRrRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021DdcfmRrTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021DdcfmRrRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021DdcfmRrTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021DdcfmRrTable_del(StorageNew);
				ieee8021DdcfmRrTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021DdcfmRrTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021DdcfmRrTable_rsvs == 0)
				ieee8021DdcfmRrTable_destroy(&StorageTmp->ieee8021DdcfmRrTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021DdcfmRrTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmRFMRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021DdcfmRFMRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmRFMReceiverTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021DdcfmRFMReceiverTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmRFMRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021DdcfmRFMReceiverTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRFMRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmRFMRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021DdcfmRfmReceiverIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmRfmReceiverMdIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021DdcfmRfmReceiverIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRfmReceiverIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRfmReceiverIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmRfmReceiverMdIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRfmReceiverMdIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..7 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 7)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmRfmReceiverMdIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021DdcfmRFMReceiverTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021DdcfmRFMReceiverTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021DdcfmRfmReceiverIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmRfmReceiverMdIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021DdcfmRFMReceiverTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021DdcfmRFMRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021DdcfmRFMReceiverTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021DdcfmRFMReceiverTable_old) == NULL)
				if (StorageTmp->ieee8021DdcfmRFMReceiverTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021DdcfmRFMReceiverTable_old = ieee8021DdcfmRFMReceiverTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021DdcfmRFMReceiverTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021DdcfmRFMReceiverTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021DdcfmRFMReceiverTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021DdcfmRFMReceiverTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021DdcfmRFMRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021DdcfmRFMReceiverTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmRFMRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmRFMReceiverTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmRFMRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmRFMReceiverTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021DdcfmRFMRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021DdcfmRFMReceiverTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmRFMRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021DdcfmRFMReceiverTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmRFMRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021DdcfmRFMReceiverTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021DdcfmRFMReceiverTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021DdcfmRFMRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021DdcfmRFMRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021DdcfmRFMRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021DdcfmRFMReceiverTable_old) != NULL) {
				ieee8021DdcfmRFMReceiverTable_destroy(&StorageTmp->ieee8021DdcfmRFMReceiverTable_old);
				StorageTmp->ieee8021DdcfmRFMReceiverTable_rsvs = 0;
				StorageTmp->ieee8021DdcfmRFMReceiverTable_tsts = 0;
				StorageTmp->ieee8021DdcfmRFMReceiverTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021DdcfmRFMReceiverTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021DdcfmRFMReceiverTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021DdcfmRFMRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021DdcfmRFMReceiverTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021DdcfmRFMRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021DdcfmRFMReceiverTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021DdcfmRFMReceiverTable_del(StorageNew);
				ieee8021DdcfmRFMReceiverTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021DdcfmRFMReceiverTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021DdcfmRFMReceiverTable_rsvs == 0)
				ieee8021DdcfmRFMReceiverTable_destroy(&StorageTmp->ieee8021DdcfmRFMReceiverTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021DdcfmRFMReceiverTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmDrRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021DdcfmDrRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmDrTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021DdcfmDrTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmDrRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021DdcfmDrTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmDrRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021DdcfmDrIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmDrMdIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmDrVlanIdOrNone */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021DdcfmDrIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmDrIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmDrIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmDrMdIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmDrMdIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..7 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 7)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmDrMdIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmDrVlanIdOrNone */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmDrVlanIdOrNone: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..0 1..4094 */
			if ((long) *vp->val.integer != 0 && (1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmDrVlanIdOrNone: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021DdcfmDrTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021DdcfmDrTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021DdcfmDrIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmDrMdIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmDrVlanIdOrNone = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021DdcfmDrTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021DdcfmDrRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021DdcfmDrTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
				if (StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old = ieee8021DdcfmDrTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021DdcfmDrTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021DdcfmDrTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021DdcfmDrTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021DdcfmDrTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021DdcfmDrRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021DdcfmDrTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmDrRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmDrTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmDrRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmDrTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021DdcfmDrRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021DdcfmDrTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmDrRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021DdcfmDrTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmDrRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021DdcfmDrTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021DdcfmDrTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021DdcfmDrRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021DdcfmDrRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021DdcfmDrRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) != NULL) {
				ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
				StorageTmp->ieee8021DdcfmDrTable_rsvs = 0;
				StorageTmp->ieee8021DdcfmDrTable_tsts = 0;
				StorageTmp->ieee8021DdcfmDrTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021DdcfmDrTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021DdcfmDrTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021DdcfmDrRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021DdcfmDrTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021DdcfmDrRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021DdcfmDrTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021DdcfmDrTable_del(StorageNew);
				ieee8021DdcfmDrTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021DdcfmDrTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021DdcfmDrTable_rsvs == 0)
				ieee8021DdcfmDrTable_destroy(&StorageTmp->ieee8021DdcfmDrTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021DdcfmDrTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021DdcfmSoRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021DdcfmSoRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021DdcfmSoTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021DdcfmSoTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021DdcfmMIB", "write_ieee8021DdcfmSoRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021DdcfmSoTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021DdcfmSoRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021DdcfmSfmIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmSoMdIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmSoVlanIdOrNone */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021DdcfmSoDirection */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021DdcfmSfmIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSfmIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSfmIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmSoMdIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSoMdIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..7 */
			if ((0 > (long) *vp->val.integer || (long) *vp->val.integer > 7)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSoMdIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmSoVlanIdOrNone */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSoVlanIdOrNone: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..0 1..4094 */
			if ((long) *vp->val.integer != 0 && (1 > (long) *vp->val.integer || (long) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSoVlanIdOrNone: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021DdcfmSoDirection */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSoDirection: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			switch ((long) *vp->val.integer) {
			case IEEE8021DDCFMSODIRECTION_DOWN:
			case IEEE8021DDCFMSODIRECTION_UP:
				break;
			default:
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021DdcfmSoDirection: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021DdcfmSoTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021DdcfmSoTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021DdcfmSfmIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmSoMdIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmSoVlanIdOrNone = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021DdcfmSoDirection = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021DdcfmSoTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021DdcfmSoRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021DdcfmSoTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
				if (StorageTmp->ieee8021DdcfmSoTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old = ieee8021DdcfmSoTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021DdcfmSoTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021DdcfmSoTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021DdcfmSoTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021DdcfmSoTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021DdcfmSoRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021DdcfmSoTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmSoRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmSoTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021DdcfmSoRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021DdcfmSoTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021DdcfmSoRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021DdcfmSoTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmSoRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021DdcfmSoTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021DdcfmSoRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021DdcfmSoTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021DdcfmSoTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021DdcfmSoRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021DdcfmSoRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021DdcfmSoRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) != NULL) {
				ieee8021DdcfmSoTable_destroy(&StorageTmp->ieee8021DdcfmSoTable_old);
				StorageTmp->ieee8021DdcfmSoTable_rsvs = 0;
				StorageTmp->ieee8021DdcfmSoTable_tsts = 0;
				StorageTmp->ieee8021DdcfmSoTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021DdcfmSoTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021DdcfmSoTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021DdcfmSoRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021DdcfmSoTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021DdcfmSoRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021DdcfmSoTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021DdcfmSoTable_del(StorageNew);
				ieee8021DdcfmSoTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021DdcfmSoTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021DdcfmSoTable_rsvs == 0)
				ieee8021DdcfmSoTable_destroy(&StorageTmp->ieee8021DdcfmSoTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021DdcfmSoTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021DdcfmMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021DdcfmMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021DdcfmMIBold_signal_handler != NULL)
		(*ieee8021DdcfmMIBold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
}

/**
 * @fn void ieee8021DdcfmMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021DdcfmMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021DdcfmMIB", "ieee8021DdcfmMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021DdcfmMIB", "done.\n"));
	return;
}
