/*****************************************************************************

 @(#) src/snmp/lldpxdot1evbextensions.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/lldpxdot1evbextensions.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "lldpXDot1EvbExtensions.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "lldpXDot1EvbExtensions";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int lldpXDot1EvbExtensions_refresh = 1;
volatile int lldpXdot1EvbConfigEvbTable_refresh = 1;
volatile int lldpXdot1EvbConfigCdcpTable_refresh = 1;
volatile int lldpV2Xdot1LocEvbTlvTable_refresh = 1;
volatile int lldpV2Xdot1LocCdcpTlvTable_refresh = 1;
volatile int lldpV2Xdot1RemEvbTlvTable_refresh = 1;
volatile int lldpV2Xdot1RemCdcpTlvTable_refresh = 1;

/*
 * lldpXDot1EvbExtensions_variables_oid: object identifier for lldpXDot1EvbExtensions
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid lldpXDot1EvbExtensions_variables_oid[13] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 7, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid lldpXdot1EvbCompliance_oid[16] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 7, 1, 2, 1, 1 };
oid lldpXdot1EvbGroup_oid[16] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 7, 1, 2, 2, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 lldpXDot1EvbExtensions_variables: tree for lldpXDot1EvbExtensions
 * This variable defines function callbacks and type return information for the lldpXDot1EvbExtensions mib section
 */
struct variable7 lldpXDot1EvbExtensions_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   LLDPXDOT1EVBCONFIGEVBTXENABLE  1
	{(u_char) LLDPXDOT1EVBCONFIGEVBTXENABLE, ASN_INTEGER, RWRITE, var_lldpXdot1EvbConfigEvbTable, 6, {1, 1, 1, 1, 1, 1}},
#define   LLDPXDOT1EVBCONFIGCDCPTXENABLE  2
	{(u_char) LLDPXDOT1EVBCONFIGCDCPTXENABLE, ASN_INTEGER, RWRITE, var_lldpXdot1EvbConfigCdcpTable, 6, {1, 1, 1, 2, 1, 1}},
#define   LLDPV2XDOT1LOCEVBTLVSTRING  3
	{(u_char) LLDPV2XDOT1LOCEVBTLVSTRING, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1LocEvbTlvTable, 6, {1, 1, 2, 1, 1, 1}},
#define   LLDPV2XDOT1LOCCDCPTLVSTRING  4
	{(u_char) LLDPV2XDOT1LOCCDCPTLVSTRING, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1LocCdcpTlvTable, 6, {1, 1, 2, 2, 1, 1}},
#define   LLDPV2XDOT1REMEVBTLVSTRING  5
	{(u_char) LLDPV2XDOT1REMEVBTLVSTRING, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1RemEvbTlvTable, 6, {1, 1, 3, 1, 1, 1}},
#define   LLDPV2XDOT1REMCDCPTLVSTRING  6
	{(u_char) LLDPV2XDOT1REMCDCPTLVSTRING, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1RemCdcpTlvTable, 6, {1, 1, 3, 2, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct lldpXDot1EvbExtensions_data *lldpXDot1EvbExtensionsStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *lldpXdot1EvbConfigEvbTableStorage = NULL;
struct header_complex_index *lldpXdot1EvbConfigCdcpTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocEvbTlvTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocCdcpTlvTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemEvbTlvTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemCdcpTlvTableStorage = NULL;

void (*lldpXDot1EvbExtensionsold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void lldpXDot1EvbExtensions_loop_handler(int);
void lldpXDot1EvbExtensions_fd_handler(int, void *);

/**
 * @fn void init_lldpXDot1EvbExtensions(void)
 * @brief lldpXDot1EvbExtensions initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (lldpXDot1EvbExtensions_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_lldpXDot1EvbExtensions(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "init_lldpXDot1EvbExtensions: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("lldpXDot1EvbExtensions", lldpXDot1EvbExtensions_variables, variable7, lldpXDot1EvbExtensions_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_lldpXDot1EvbExtensions, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("lldpXDot1EvbExtensions", parse_lldpXDot1EvbExtensions, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1EvbConfigEvbTable", parse_lldpXdot1EvbConfigEvbTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpXdot1EvbConfigCdcpTable", parse_lldpXdot1EvbConfigCdcpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocEvbTlvTable", parse_lldpV2Xdot1LocEvbTlvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocCdcpTlvTable", parse_lldpV2Xdot1LocCdcpTlvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemEvbTlvTable", parse_lldpV2Xdot1RemEvbTlvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemCdcpTlvTable", parse_lldpV2Xdot1RemCdcpTlvTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXDot1EvbExtensions, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1EvbConfigEvbTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpXdot1EvbConfigCdcpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocEvbTlvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocCdcpTlvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemEvbTlvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemCdcpTlvTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, lldpXDot1EvbExtensions_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, lldpXDot1EvbExtensions_fd_handler, (void *) 1);
	}
	lldpXDot1EvbExtensionsold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &lldpXDot1EvbExtensions_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/**
 * @fn void deinit_lldpXDot1EvbExtensions(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (lldpXDot1EvbExtensions_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_lldpXDot1EvbExtensions(void)
{
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "deinit_lldpXDot1EvbExtensions: deinitializating...  "));
	external_signal_handler[SIGCHLD] = lldpXDot1EvbExtensionsold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(lldpXDot1EvbExtensions_variables_oid, sizeof(lldpXDot1EvbExtensions_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("lldpXDot1EvbExtensions");
	snmpd_unregister_config_handler("lldpXdot1EvbConfigEvbTable");
	snmpd_unregister_config_handler("lldpXdot1EvbConfigCdcpTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocEvbTlvTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocCdcpTlvTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemEvbTlvTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemCdcpTlvTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

int
term_lldpXDot1EvbExtensions(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "term_lldpXDot1EvbExtensions: terminating...  "));
	deinit_lldpXDot1EvbExtensions();
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return 0;
}

/**
 * @fn struct lldpXDot1EvbExtensions_data *lldpXDot1EvbExtensions_create(void)
 * @brief create a fresh data structure representing scalars in lldpXDot1EvbExtensions.
 *
 * Creates a new lldpXDot1EvbExtensions_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in lldpXDot1EvbExtensions.
 */
struct lldpXDot1EvbExtensions_data *
lldpXDot1EvbExtensions_create(void)
{
	struct lldpXDot1EvbExtensions_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXDot1EvbExtensions_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXDot1EvbExtensions_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXDot1EvbExtensions_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXDot1EvbExtensions_data *lldpXDot1EvbExtensions_duplicate(struct lldpXDot1EvbExtensions_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct lldpXDot1EvbExtensions_data *
lldpXDot1EvbExtensions_duplicate(struct lldpXDot1EvbExtensions_data *thedata)
{
	struct lldpXDot1EvbExtensions_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXDot1EvbExtensions_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXDot1EvbExtensions_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXDot1EvbExtensions_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXDot1EvbExtensions_destroy(struct lldpXDot1EvbExtensions_data **thedata)
 * @param thedata pointer to the data structure in lldpXDot1EvbExtensions.
 * @brief delete a scalars structure from lldpXDot1EvbExtensions.
 *
 * Frees scalars that were previously removed from lldpXDot1EvbExtensions.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXDot1EvbExtensions_destroy(struct lldpXDot1EvbExtensions_data **thedata)
{
	struct lldpXDot1EvbExtensions_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXDot1EvbExtensions_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXDot1EvbExtensions_add(struct lldpXDot1EvbExtensions_data *thedata)
 * @param thedata the structure representing lldpXDot1EvbExtensions scalars.
 * @brief adds node to the lldpXDot1EvbExtensions scalar data set.
 *
 * Adds a scalar structure to the lldpXDot1EvbExtensions data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
lldpXDot1EvbExtensions_add(struct lldpXDot1EvbExtensions_data *thedata)
{
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXDot1EvbExtensions_add: adding data...  "));
	if (thedata)
		lldpXDot1EvbExtensionsStorage = thedata;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXDot1EvbExtensions(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXDot1EvbExtensions entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXDot1EvbExtensions).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_lldpXDot1EvbExtensions(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXDot1EvbExtensions_data *StorageTmp = lldpXDot1EvbExtensions_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpXDot1EvbExtensions: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	lldpXDot1EvbExtensions_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpXDot1EvbExtensions(): stores .conf file entries needed to configure the mib.
 */
int
store_lldpXDot1EvbExtensions(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXDot1EvbExtensions_data *StorageTmp;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpXDot1EvbExtensions: storing data...  "));
	refresh_lldpXDot1EvbExtensions(1);
	if ((StorageTmp = lldpXDot1EvbExtensionsStorage) == NULL) {
		DEBUGMSGTL(("lldpXDot1EvbExtensions", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "lldpXDot1EvbExtensions ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpXDot1EvbExtensions(struct lldpXDot1EvbExtensions_data *StorageTmp, struct lldpXDot1EvbExtensions_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_lldpXDot1EvbExtensions(struct lldpXDot1EvbExtensions_data *StorageTmp, struct lldpXDot1EvbExtensions_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXDot1EvbExtensions(struct lldpXDot1EvbExtensions_data *StorageTmp, struct lldpXDot1EvbExtensions_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_lldpXDot1EvbExtensions()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXDot1EvbExtensions(struct lldpXDot1EvbExtensions_data *StorageTmp, struct lldpXDot1EvbExtensions_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXDot1EvbExtensions_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_lldpXDot1EvbExtensions(struct 
 * @fn void revert_lldpXDot1EvbExtensions(struct lldpXDot1EvbExtensions_data *StorageTmp, struct lldpXDot1EvbExtensions_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_lldpXDot1EvbExtensions(struct lldpXDot1EvbExtensions_data *StorageTmp, struct lldpXDot1EvbExtensions_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXDot1EvbExtensions(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXDot1EvbExtensions(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of lldpXDot1EvbExtensions.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_lldpXDot1EvbExtensions(int force)
{
	if (lldpXDot1EvbExtensionsStorage == NULL) {
		struct lldpXDot1EvbExtensions_data *StorageNew;

		if ((StorageNew = lldpXDot1EvbExtensions_create()) == NULL)
			return;
		lldpXDot1EvbExtensionsStorage = StorageNew;
		lldpXDot1EvbExtensions_refresh = 1;
	}
	if (!force && lldpXDot1EvbExtensions_refresh == 0)
		return;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "refresh_lldpXDot1EvbExtensions: refreshing...  "));
	/* XXX: Update scalars as required here... */
	lldpXDot1EvbExtensions_refresh = 0;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/**
 * @fn u_char * var_lldpXDot1EvbExtensions(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in lldpXDot1EvbExtensions.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_lldpXDot1EvbExtensions(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXDot1EvbExtensions_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpXDot1EvbExtensions: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_lldpXDot1EvbExtensions(0);
	if ((StorageTmp = lldpXDot1EvbExtensionsStorage) == NULL) {
		DEBUGMSGTL(("lldpXDot1EvbExtensions", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("lldpXDot1EvbExtensions", "found.\n"));
	else
		DEBUGMSGTL(("lldpXDot1EvbExtensions", "not found.\n"));
	return (rval);
}

/**
 * @fn struct lldpXdot1EvbConfigEvbTable_data *lldpXdot1EvbConfigEvbTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1EvbConfigEvbTable table.
 *
 * Creates a new lldpXdot1EvbConfigEvbTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1EvbConfigEvbTable_data *
lldpXdot1EvbConfigEvbTable_create(void)
{
	struct lldpXdot1EvbConfigEvbTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1EvbConfigEvbTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigEvbTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2PortConfigIfIndex = 0;
		StorageNew->lldpV2PortConfigDestAddressIndex = 0;
		StorageNew->lldpXdot1EvbConfigEvbTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1EvbConfigEvbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1EvbConfigEvbTable_data *lldpXdot1EvbConfigEvbTable_duplicate(struct lldpXdot1EvbConfigEvbTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1EvbConfigEvbTable_data *
lldpXdot1EvbConfigEvbTable_duplicate(struct lldpXdot1EvbConfigEvbTable_data *thedata)
{
	struct lldpXdot1EvbConfigEvbTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1EvbConfigEvbTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigEvbTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1EvbConfigEvbTable_id = thedata->lldpXdot1EvbConfigEvbTable_id;
		StorageNew->lldpV2PortConfigIfIndex = thedata->lldpV2PortConfigIfIndex;
		StorageNew->lldpV2PortConfigDestAddressIndex = thedata->lldpV2PortConfigDestAddressIndex;
		StorageNew->lldpXdot1EvbConfigEvbTxEnable = thedata->lldpXdot1EvbConfigEvbTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1EvbConfigEvbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1EvbConfigEvbTable_destroy(struct lldpXdot1EvbConfigEvbTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1EvbConfigEvbTable_destroy(struct lldpXdot1EvbConfigEvbTable_data **thedata)
{
	struct lldpXdot1EvbConfigEvbTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigEvbTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1EvbConfigEvbTable_add(struct lldpXdot1EvbConfigEvbTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1EvbConfigEvbTable table data set.
 *
 * Adds a table row structure to the lldpXdot1EvbConfigEvbTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1EvbConfigEvbTable_add(struct lldpXdot1EvbConfigEvbTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigEvbTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2PortConfigIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2PortConfigIfIndex, sizeof(thedata->lldpV2PortConfigIfIndex));
		/* lldpV2PortConfigDestAddressIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2PortConfigDestAddressIndex, sizeof(thedata->lldpV2PortConfigDestAddressIndex));
		header_complex_add_data(&lldpXdot1EvbConfigEvbTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1EvbConfigEvbTable_del(struct lldpXdot1EvbConfigEvbTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1EvbConfigEvbTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1EvbConfigEvbTable_del(struct lldpXdot1EvbConfigEvbTable_data *thedata)
{
	struct lldpXdot1EvbConfigEvbTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigEvbTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1EvbConfigEvbTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1EvbConfigEvbTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1EvbConfigEvbTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1EvbConfigEvbTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1EvbConfigEvbTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1EvbConfigEvbTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1EvbConfigEvbTable_data *StorageTmp = lldpXdot1EvbConfigEvbTable_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpXdot1EvbConfigEvbTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1EvbConfigEvbTxEnable, &tmpsize);
	lldpXdot1EvbConfigEvbTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpXdot1EvbConfigEvbTable(): store configuraiton file for lldpXdot1EvbConfigEvbTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1EvbConfigEvbTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1EvbConfigEvbTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpXdot1EvbConfigEvbTable: storing data...  "));
	refresh_lldpXdot1EvbConfigEvbTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1EvbConfigEvbTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1EvbConfigEvbTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1EvbConfigEvbTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1EvbConfigEvbTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpXdot1EvbConfigCdcpTable_data *lldpXdot1EvbConfigCdcpTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpXdot1EvbConfigCdcpTable table.
 *
 * Creates a new lldpXdot1EvbConfigCdcpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpXdot1EvbConfigCdcpTable_data *
lldpXdot1EvbConfigCdcpTable_create(void)
{
	struct lldpXdot1EvbConfigCdcpTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1EvbConfigCdcpTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigCdcpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2PortConfigIfIndex = 0;
		StorageNew->lldpV2PortConfigDestAddressIndex = 0;
		StorageNew->lldpXdot1EvbConfigCdcpTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpXdot1EvbConfigCdcpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpXdot1EvbConfigCdcpTable_data *lldpXdot1EvbConfigCdcpTable_duplicate(struct lldpXdot1EvbConfigCdcpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpXdot1EvbConfigCdcpTable_data *
lldpXdot1EvbConfigCdcpTable_duplicate(struct lldpXdot1EvbConfigCdcpTable_data *thedata)
{
	struct lldpXdot1EvbConfigCdcpTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpXdot1EvbConfigCdcpTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigCdcpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpXdot1EvbConfigCdcpTable_id = thedata->lldpXdot1EvbConfigCdcpTable_id;
		StorageNew->lldpV2PortConfigIfIndex = thedata->lldpV2PortConfigIfIndex;
		StorageNew->lldpV2PortConfigDestAddressIndex = thedata->lldpV2PortConfigDestAddressIndex;
		StorageNew->lldpXdot1EvbConfigCdcpTxEnable = thedata->lldpXdot1EvbConfigCdcpTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpXdot1EvbConfigCdcpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpXdot1EvbConfigCdcpTable_destroy(struct lldpXdot1EvbConfigCdcpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpXdot1EvbConfigCdcpTable_destroy(struct lldpXdot1EvbConfigCdcpTable_data **thedata)
{
	struct lldpXdot1EvbConfigCdcpTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigCdcpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1EvbConfigCdcpTable_add(struct lldpXdot1EvbConfigCdcpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpXdot1EvbConfigCdcpTable table data set.
 *
 * Adds a table row structure to the lldpXdot1EvbConfigCdcpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpXdot1EvbConfigCdcpTable_add(struct lldpXdot1EvbConfigCdcpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigCdcpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2PortConfigIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2PortConfigIfIndex, sizeof(thedata->lldpV2PortConfigIfIndex));
		/* lldpV2PortConfigDestAddressIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2PortConfigDestAddressIndex, sizeof(thedata->lldpV2PortConfigDestAddressIndex));
		header_complex_add_data(&lldpXdot1EvbConfigCdcpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpXdot1EvbConfigCdcpTable_del(struct lldpXdot1EvbConfigCdcpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpXdot1EvbConfigCdcpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpXdot1EvbConfigCdcpTable_del(struct lldpXdot1EvbConfigCdcpTable_data *thedata)
{
	struct lldpXdot1EvbConfigCdcpTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXdot1EvbConfigCdcpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpXdot1EvbConfigCdcpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpXdot1EvbConfigCdcpTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpXdot1EvbConfigCdcpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpXdot1EvbConfigCdcpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpXdot1EvbConfigCdcpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpXdot1EvbConfigCdcpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp = lldpXdot1EvbConfigCdcpTable_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpXdot1EvbConfigCdcpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpXdot1EvbConfigCdcpTxEnable, &tmpsize);
	lldpXdot1EvbConfigCdcpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpXdot1EvbConfigCdcpTable(): store configuraiton file for lldpXdot1EvbConfigCdcpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpXdot1EvbConfigCdcpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpXdot1EvbConfigCdcpTable: storing data...  "));
	refresh_lldpXdot1EvbConfigCdcpTable(1);
	(void) tmpsize;
	for (hcindex = lldpXdot1EvbConfigCdcpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpXdot1EvbConfigCdcpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpXdot1EvbConfigCdcpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpXdot1EvbConfigCdcpTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocEvbTlvTable_data *lldpV2Xdot1LocEvbTlvTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocEvbTlvTable table.
 *
 * Creates a new lldpV2Xdot1LocEvbTlvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocEvbTlvTable_data *
lldpV2Xdot1LocEvbTlvTable_create(void)
{
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocEvbTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocEvbTlvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		if ((StorageNew->lldpV2Xdot1LocEvbTlvString = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1LocEvbTlvStringLen = 0;
		StorageNew->lldpV2Xdot1LocEvbTlvString[StorageNew->lldpV2Xdot1LocEvbTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocEvbTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocEvbTlvTable_data *lldpV2Xdot1LocEvbTlvTable_duplicate(struct lldpV2Xdot1LocEvbTlvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocEvbTlvTable_data *
lldpV2Xdot1LocEvbTlvTable_duplicate(struct lldpV2Xdot1LocEvbTlvTable_data *thedata)
{
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocEvbTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocEvbTlvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocEvbTlvTable_id = thedata->lldpV2Xdot1LocEvbTlvTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		if (!(StorageNew->lldpV2Xdot1LocEvbTlvString = malloc(thedata->lldpV2Xdot1LocEvbTlvStringLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1LocEvbTlvString, thedata->lldpV2Xdot1LocEvbTlvString, thedata->lldpV2Xdot1LocEvbTlvStringLen);
		StorageNew->lldpV2Xdot1LocEvbTlvStringLen = thedata->lldpV2Xdot1LocEvbTlvStringLen;
		StorageNew->lldpV2Xdot1LocEvbTlvString[StorageNew->lldpV2Xdot1LocEvbTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocEvbTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocEvbTlvTable_destroy(struct lldpV2Xdot1LocEvbTlvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocEvbTlvTable_destroy(struct lldpV2Xdot1LocEvbTlvTable_data **thedata)
{
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocEvbTlvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1LocEvbTlvString);
		StorageDel->lldpV2Xdot1LocEvbTlvStringLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocEvbTlvTable_add(struct lldpV2Xdot1LocEvbTlvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocEvbTlvTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocEvbTlvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocEvbTlvTable_add(struct lldpV2Xdot1LocEvbTlvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocEvbTlvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1LocEvbTlvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocEvbTlvTable_del(struct lldpV2Xdot1LocEvbTlvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocEvbTlvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocEvbTlvTable_del(struct lldpV2Xdot1LocEvbTlvTable_data *thedata)
{
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocEvbTlvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocEvbTlvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocEvbTlvTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocEvbTlvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocEvbTlvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocEvbTlvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocEvbTlvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp = lldpV2Xdot1LocEvbTlvTable_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpV2Xdot1LocEvbTlvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1LocEvbTlvString);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1LocEvbTlvString, &StorageTmp->lldpV2Xdot1LocEvbTlvStringLen);
	if (StorageTmp->lldpV2Xdot1LocEvbTlvString == NULL) {
		config_perror("invalid specification for lldpV2Xdot1LocEvbTlvString");
		return;
	}
	lldpV2Xdot1LocEvbTlvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocEvbTlvTable(): store configuraiton file for lldpV2Xdot1LocEvbTlvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocEvbTlvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpV2Xdot1LocEvbTlvTable: storing data...  "));
	refresh_lldpV2Xdot1LocEvbTlvTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocEvbTlvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocEvbTlvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocEvbTlvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1LocEvbTlvString, &StorageTmp->lldpV2Xdot1LocEvbTlvStringLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocCdcpTlvTable_data *lldpV2Xdot1LocCdcpTlvTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocCdcpTlvTable table.
 *
 * Creates a new lldpV2Xdot1LocCdcpTlvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocCdcpTlvTable_data *
lldpV2Xdot1LocCdcpTlvTable_create(void)
{
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocCdcpTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocCdcpTlvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		if ((StorageNew->lldpV2Xdot1LocCdcpTlvString = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1LocCdcpTlvStringLen = 0;
		StorageNew->lldpV2Xdot1LocCdcpTlvString[StorageNew->lldpV2Xdot1LocCdcpTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocCdcpTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocCdcpTlvTable_data *lldpV2Xdot1LocCdcpTlvTable_duplicate(struct lldpV2Xdot1LocCdcpTlvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocCdcpTlvTable_data *
lldpV2Xdot1LocCdcpTlvTable_duplicate(struct lldpV2Xdot1LocCdcpTlvTable_data *thedata)
{
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocCdcpTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocCdcpTlvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocCdcpTlvTable_id = thedata->lldpV2Xdot1LocCdcpTlvTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		if (!(StorageNew->lldpV2Xdot1LocCdcpTlvString = malloc(thedata->lldpV2Xdot1LocCdcpTlvStringLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1LocCdcpTlvString, thedata->lldpV2Xdot1LocCdcpTlvString, thedata->lldpV2Xdot1LocCdcpTlvStringLen);
		StorageNew->lldpV2Xdot1LocCdcpTlvStringLen = thedata->lldpV2Xdot1LocCdcpTlvStringLen;
		StorageNew->lldpV2Xdot1LocCdcpTlvString[StorageNew->lldpV2Xdot1LocCdcpTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocCdcpTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocCdcpTlvTable_destroy(struct lldpV2Xdot1LocCdcpTlvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocCdcpTlvTable_destroy(struct lldpV2Xdot1LocCdcpTlvTable_data **thedata)
{
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocCdcpTlvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1LocCdcpTlvString);
		StorageDel->lldpV2Xdot1LocCdcpTlvStringLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocCdcpTlvTable_add(struct lldpV2Xdot1LocCdcpTlvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocCdcpTlvTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocCdcpTlvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocCdcpTlvTable_add(struct lldpV2Xdot1LocCdcpTlvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocCdcpTlvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1LocCdcpTlvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocCdcpTlvTable_del(struct lldpV2Xdot1LocCdcpTlvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocCdcpTlvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocCdcpTlvTable_del(struct lldpV2Xdot1LocCdcpTlvTable_data *thedata)
{
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1LocCdcpTlvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocCdcpTlvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocCdcpTlvTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocCdcpTlvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocCdcpTlvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocCdcpTlvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocCdcpTlvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp = lldpV2Xdot1LocCdcpTlvTable_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpV2Xdot1LocCdcpTlvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1LocCdcpTlvString);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1LocCdcpTlvString, &StorageTmp->lldpV2Xdot1LocCdcpTlvStringLen);
	if (StorageTmp->lldpV2Xdot1LocCdcpTlvString == NULL) {
		config_perror("invalid specification for lldpV2Xdot1LocCdcpTlvString");
		return;
	}
	lldpV2Xdot1LocCdcpTlvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocCdcpTlvTable(): store configuraiton file for lldpV2Xdot1LocCdcpTlvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocCdcpTlvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpV2Xdot1LocCdcpTlvTable: storing data...  "));
	refresh_lldpV2Xdot1LocCdcpTlvTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocCdcpTlvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocCdcpTlvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocCdcpTlvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1LocCdcpTlvString, &StorageTmp->lldpV2Xdot1LocCdcpTlvStringLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemEvbTlvTable_data *lldpV2Xdot1RemEvbTlvTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemEvbTlvTable table.
 *
 * Creates a new lldpV2Xdot1RemEvbTlvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemEvbTlvTable_data *
lldpV2Xdot1RemEvbTlvTable_create(void)
{
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemEvbTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemEvbTlvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if ((StorageNew->lldpV2Xdot1RemEvbTlvString = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1RemEvbTlvStringLen = 0;
		StorageNew->lldpV2Xdot1RemEvbTlvString[StorageNew->lldpV2Xdot1RemEvbTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemEvbTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemEvbTlvTable_data *lldpV2Xdot1RemEvbTlvTable_duplicate(struct lldpV2Xdot1RemEvbTlvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemEvbTlvTable_data *
lldpV2Xdot1RemEvbTlvTable_duplicate(struct lldpV2Xdot1RemEvbTlvTable_data *thedata)
{
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemEvbTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemEvbTlvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemEvbTlvTable_id = thedata->lldpV2Xdot1RemEvbTlvTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		if (!(StorageNew->lldpV2Xdot1RemEvbTlvString = malloc(thedata->lldpV2Xdot1RemEvbTlvStringLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1RemEvbTlvString, thedata->lldpV2Xdot1RemEvbTlvString, thedata->lldpV2Xdot1RemEvbTlvStringLen);
		StorageNew->lldpV2Xdot1RemEvbTlvStringLen = thedata->lldpV2Xdot1RemEvbTlvStringLen;
		StorageNew->lldpV2Xdot1RemEvbTlvString[StorageNew->lldpV2Xdot1RemEvbTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemEvbTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemEvbTlvTable_destroy(struct lldpV2Xdot1RemEvbTlvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemEvbTlvTable_destroy(struct lldpV2Xdot1RemEvbTlvTable_data **thedata)
{
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemEvbTlvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1RemEvbTlvString);
		StorageDel->lldpV2Xdot1RemEvbTlvStringLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemEvbTlvTable_add(struct lldpV2Xdot1RemEvbTlvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemEvbTlvTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemEvbTlvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemEvbTlvTable_add(struct lldpV2Xdot1RemEvbTlvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemEvbTlvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot1RemEvbTlvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemEvbTlvTable_del(struct lldpV2Xdot1RemEvbTlvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemEvbTlvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemEvbTlvTable_del(struct lldpV2Xdot1RemEvbTlvTable_data *thedata)
{
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemEvbTlvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemEvbTlvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemEvbTlvTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemEvbTlvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemEvbTlvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemEvbTlvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemEvbTlvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp = lldpV2Xdot1RemEvbTlvTable_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpV2Xdot1RemEvbTlvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1RemEvbTlvString);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1RemEvbTlvString, &StorageTmp->lldpV2Xdot1RemEvbTlvStringLen);
	if (StorageTmp->lldpV2Xdot1RemEvbTlvString == NULL) {
		config_perror("invalid specification for lldpV2Xdot1RemEvbTlvString");
		return;
	}
	lldpV2Xdot1RemEvbTlvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemEvbTlvTable(): store configuraiton file for lldpV2Xdot1RemEvbTlvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemEvbTlvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpV2Xdot1RemEvbTlvTable: storing data...  "));
	refresh_lldpV2Xdot1RemEvbTlvTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemEvbTlvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemEvbTlvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemEvbTlvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1RemEvbTlvString, &StorageTmp->lldpV2Xdot1RemEvbTlvStringLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemCdcpTlvTable_data *lldpV2Xdot1RemCdcpTlvTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemCdcpTlvTable table.
 *
 * Creates a new lldpV2Xdot1RemCdcpTlvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemCdcpTlvTable_data *
lldpV2Xdot1RemCdcpTlvTable_create(void)
{
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemCdcpTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemCdcpTlvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if ((StorageNew->lldpV2Xdot1RemCdcpTlvString = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1RemCdcpTlvStringLen = 0;
		StorageNew->lldpV2Xdot1RemCdcpTlvString[StorageNew->lldpV2Xdot1RemCdcpTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemCdcpTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemCdcpTlvTable_data *lldpV2Xdot1RemCdcpTlvTable_duplicate(struct lldpV2Xdot1RemCdcpTlvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemCdcpTlvTable_data *
lldpV2Xdot1RemCdcpTlvTable_duplicate(struct lldpV2Xdot1RemCdcpTlvTable_data *thedata)
{
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemCdcpTlvTable_data);

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemCdcpTlvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemCdcpTlvTable_id = thedata->lldpV2Xdot1RemCdcpTlvTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		if (!(StorageNew->lldpV2Xdot1RemCdcpTlvString = malloc(thedata->lldpV2Xdot1RemCdcpTlvStringLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1RemCdcpTlvString, thedata->lldpV2Xdot1RemCdcpTlvString, thedata->lldpV2Xdot1RemCdcpTlvStringLen);
		StorageNew->lldpV2Xdot1RemCdcpTlvStringLen = thedata->lldpV2Xdot1RemCdcpTlvStringLen;
		StorageNew->lldpV2Xdot1RemCdcpTlvString[StorageNew->lldpV2Xdot1RemCdcpTlvStringLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemCdcpTlvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemCdcpTlvTable_destroy(struct lldpV2Xdot1RemCdcpTlvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemCdcpTlvTable_destroy(struct lldpV2Xdot1RemCdcpTlvTable_data **thedata)
{
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemCdcpTlvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1RemCdcpTlvString);
		StorageDel->lldpV2Xdot1RemCdcpTlvStringLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemCdcpTlvTable_add(struct lldpV2Xdot1RemCdcpTlvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemCdcpTlvTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemCdcpTlvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemCdcpTlvTable_add(struct lldpV2Xdot1RemCdcpTlvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemCdcpTlvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot1RemCdcpTlvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemCdcpTlvTable_del(struct lldpV2Xdot1RemCdcpTlvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemCdcpTlvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemCdcpTlvTable_del(struct lldpV2Xdot1RemCdcpTlvTable_data *thedata)
{
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageDel;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpV2Xdot1RemCdcpTlvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemCdcpTlvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemCdcpTlvTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemCdcpTlvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemCdcpTlvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemCdcpTlvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemCdcpTlvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp = lldpV2Xdot1RemCdcpTlvTable_create();

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "parse_lldpV2Xdot1RemCdcpTlvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1RemCdcpTlvString);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1RemCdcpTlvString, &StorageTmp->lldpV2Xdot1RemCdcpTlvStringLen);
	if (StorageTmp->lldpV2Xdot1RemCdcpTlvString == NULL) {
		config_perror("invalid specification for lldpV2Xdot1RemCdcpTlvString");
		return;
	}
	lldpV2Xdot1RemCdcpTlvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemCdcpTlvTable(): store configuraiton file for lldpV2Xdot1RemCdcpTlvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemCdcpTlvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "store_lldpV2Xdot1RemCdcpTlvTable: storing data...  "));
	refresh_lldpV2Xdot1RemCdcpTlvTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemCdcpTlvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemCdcpTlvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemCdcpTlvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1RemCdcpTlvString, &StorageTmp->lldpV2Xdot1RemCdcpTlvStringLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, struct lldpXdot1EvbConfigEvbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, struct lldpXdot1EvbConfigEvbTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, struct lldpXdot1EvbConfigEvbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1EvbConfigEvbTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, struct lldpXdot1EvbConfigEvbTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1EvbConfigEvbTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, struct lldpXdot1EvbConfigEvbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, struct lldpXdot1EvbConfigEvbTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1EvbConfigEvbTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1EvbConfigEvbTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1EvbConfigEvbTable_data *
refresh_lldpXdot1EvbConfigEvbTable_row(struct lldpXdot1EvbConfigEvbTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1EvbConfigEvbTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1EvbConfigEvbTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1EvbConfigEvbTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1EvbConfigEvbTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1EvbConfigEvbTable(int force)
{
	if (!force && lldpXdot1EvbConfigEvbTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1EvbConfigEvbTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1EvbConfigEvbTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1EvbConfigEvbTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXDot1EvbExtensions above.
 */
u_char *
var_lldpXdot1EvbConfigEvbTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1EvbConfigEvbTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpXdot1EvbConfigEvbTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1EvbConfigEvbTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1EvbConfigEvbTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1EvbConfigEvbTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1EVBCONFIGEVBTXENABLE:	/* ReadWrite */
		*write_method = write_lldpXdot1EvbConfigEvbTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1EvbConfigEvbTxEnable);
		rval = (u_char *) &StorageTmp->lldpXdot1EvbConfigEvbTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, struct lldpXdot1EvbConfigCdcpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, struct lldpXdot1EvbConfigCdcpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, struct lldpXdot1EvbConfigCdcpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpXdot1EvbConfigCdcpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, struct lldpXdot1EvbConfigCdcpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpXdot1EvbConfigCdcpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, struct lldpXdot1EvbConfigCdcpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, struct lldpXdot1EvbConfigCdcpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpXdot1EvbConfigCdcpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpXdot1EvbConfigCdcpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpXdot1EvbConfigCdcpTable_data *
refresh_lldpXdot1EvbConfigCdcpTable_row(struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpXdot1EvbConfigCdcpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpXdot1EvbConfigCdcpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpXdot1EvbConfigCdcpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpXdot1EvbConfigCdcpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpXdot1EvbConfigCdcpTable(int force)
{
	if (!force && lldpXdot1EvbConfigCdcpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpXdot1EvbConfigCdcpTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpXdot1EvbConfigCdcpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpXdot1EvbConfigCdcpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXDot1EvbExtensions above.
 */
u_char *
var_lldpXdot1EvbConfigCdcpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpXdot1EvbConfigCdcpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpXdot1EvbConfigCdcpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpXdot1EvbConfigCdcpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpXdot1EvbConfigCdcpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPXDOT1EVBCONFIGCDCPTXENABLE:	/* ReadWrite */
		*write_method = write_lldpXdot1EvbConfigCdcpTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpXdot1EvbConfigCdcpTxEnable);
		rval = (u_char *) &StorageTmp->lldpXdot1EvbConfigCdcpTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1LocEvbTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1LocEvbTlvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1LocEvbTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocEvbTlvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1LocEvbTlvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocEvbTlvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1LocEvbTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1LocEvbTlvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocEvbTlvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocEvbTlvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocEvbTlvTable_data *
refresh_lldpV2Xdot1LocEvbTlvTable_row(struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocEvbTlvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocEvbTlvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocEvbTlvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocEvbTlvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocEvbTlvTable(int force)
{
	if (!force && lldpV2Xdot1LocEvbTlvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocEvbTlvTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocEvbTlvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocEvbTlvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXDot1EvbExtensions above.
 */
u_char *
var_lldpV2Xdot1LocEvbTlvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocEvbTlvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpV2Xdot1LocEvbTlvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocEvbTlvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocEvbTlvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocEvbTlvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCEVBTLVSTRING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1LocEvbTlvStringLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1LocEvbTlvString;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1LocCdcpTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1LocCdcpTlvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1LocCdcpTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocCdcpTlvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1LocCdcpTlvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocCdcpTlvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1LocCdcpTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1LocCdcpTlvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocCdcpTlvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocCdcpTlvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocCdcpTlvTable_data *
refresh_lldpV2Xdot1LocCdcpTlvTable_row(struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocCdcpTlvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocCdcpTlvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocCdcpTlvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocCdcpTlvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocCdcpTlvTable(int force)
{
	if (!force && lldpV2Xdot1LocCdcpTlvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocCdcpTlvTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocCdcpTlvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocCdcpTlvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXDot1EvbExtensions above.
 */
u_char *
var_lldpV2Xdot1LocCdcpTlvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocCdcpTlvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpV2Xdot1LocCdcpTlvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocCdcpTlvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocCdcpTlvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocCdcpTlvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCCDCPTLVSTRING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1LocCdcpTlvStringLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1LocCdcpTlvString;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1RemEvbTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1RemEvbTlvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1RemEvbTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemEvbTlvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1RemEvbTlvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemEvbTlvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1RemEvbTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, struct lldpV2Xdot1RemEvbTlvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemEvbTlvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemEvbTlvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemEvbTlvTable_data *
refresh_lldpV2Xdot1RemEvbTlvTable_row(struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemEvbTlvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemEvbTlvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemEvbTlvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemEvbTlvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemEvbTlvTable(int force)
{
	if (!force && lldpV2Xdot1RemEvbTlvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemEvbTlvTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemEvbTlvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemEvbTlvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXDot1EvbExtensions above.
 */
u_char *
var_lldpV2Xdot1RemEvbTlvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemEvbTlvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpV2Xdot1RemEvbTlvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemEvbTlvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemEvbTlvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemEvbTlvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMEVBTLVSTRING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1RemEvbTlvStringLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1RemEvbTlvString;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1RemCdcpTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1RemCdcpTlvTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1RemCdcpTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemCdcpTlvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1RemCdcpTlvTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemCdcpTlvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1RemCdcpTlvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, struct lldpV2Xdot1RemCdcpTlvTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemCdcpTlvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemCdcpTlvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemCdcpTlvTable_data *
refresh_lldpV2Xdot1RemCdcpTlvTable_row(struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemCdcpTlvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemCdcpTlvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemCdcpTlvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemCdcpTlvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemCdcpTlvTable(int force)
{
	if (!force && lldpV2Xdot1RemCdcpTlvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemCdcpTlvTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemCdcpTlvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemCdcpTlvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpXDot1EvbExtensions above.
 */
u_char *
var_lldpV2Xdot1RemCdcpTlvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemCdcpTlvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "var_lldpV2Xdot1RemCdcpTlvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemCdcpTlvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemCdcpTlvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemCdcpTlvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMCDCPTLVSTRING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1RemCdcpTlvStringLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1RemCdcpTlvString;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_lldpXdot1EvbConfigEvbTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXdot1EvbConfigEvbTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXdot1EvbConfigEvbTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 19;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "write_lldpXdot1EvbConfigEvbTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXdot1EvbConfigEvbTableStorage, NULL, &name[19], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1EvbConfigEvbTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1EvbConfigEvbTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1EvbConfigEvbTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old) == NULL)
			if (StorageTmp->lldpXdot1EvbConfigEvbTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old = lldpXdot1EvbConfigEvbTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXdot1EvbConfigEvbTable_rsvs++;
		StorageTmp->lldpXdot1EvbConfigEvbTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXdot1EvbConfigEvbTable_tsts == 0)
				if ((ret = check_lldpXdot1EvbConfigEvbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1EvbConfigEvbTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXdot1EvbConfigEvbTxEnable for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXdot1EvbConfigEvbTable_sets == 0)
				if ((ret = update_lldpXdot1EvbConfigEvbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1EvbConfigEvbTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old) != NULL) {
			lldpXdot1EvbConfigEvbTable_destroy(&StorageTmp->lldpXdot1EvbConfigEvbTable_old);
			StorageTmp->lldpXdot1EvbConfigEvbTable_rsvs = 0;
			StorageTmp->lldpXdot1EvbConfigEvbTable_tsts = 0;
			StorageTmp->lldpXdot1EvbConfigEvbTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXdot1EvbConfigEvbTable_sets == 0)
			revert_lldpXdot1EvbConfigEvbTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigEvbTable_old) == NULL)
			break;
		StorageTmp->lldpXdot1EvbConfigEvbTxEnable = StorageOld->lldpXdot1EvbConfigEvbTxEnable;
		if (--StorageTmp->lldpXdot1EvbConfigEvbTable_rsvs == 0)
			lldpXdot1EvbConfigEvbTable_destroy(&StorageTmp->lldpXdot1EvbConfigEvbTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpXdot1EvbConfigCdcpTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpXdot1EvbConfigCdcpTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpXdot1EvbConfigCdcpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 19;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpXDot1EvbExtensions", "write_lldpXdot1EvbConfigCdcpTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpXdot1EvbConfigCdcpTableStorage, NULL, &name[19], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1EvbConfigCdcpTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1EvbConfigCdcpTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpXdot1EvbConfigCdcpTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old) == NULL)
			if (StorageTmp->lldpXdot1EvbConfigCdcpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old = lldpXdot1EvbConfigCdcpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpXdot1EvbConfigCdcpTable_rsvs++;
		StorageTmp->lldpXdot1EvbConfigCdcpTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpXdot1EvbConfigCdcpTable_tsts == 0)
				if ((ret = check_lldpXdot1EvbConfigCdcpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1EvbConfigCdcpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpXdot1EvbConfigCdcpTxEnable for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpXdot1EvbConfigCdcpTable_sets == 0)
				if ((ret = update_lldpXdot1EvbConfigCdcpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpXdot1EvbConfigCdcpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old) != NULL) {
			lldpXdot1EvbConfigCdcpTable_destroy(&StorageTmp->lldpXdot1EvbConfigCdcpTable_old);
			StorageTmp->lldpXdot1EvbConfigCdcpTable_rsvs = 0;
			StorageTmp->lldpXdot1EvbConfigCdcpTable_tsts = 0;
			StorageTmp->lldpXdot1EvbConfigCdcpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpXdot1EvbConfigCdcpTable_sets == 0)
			revert_lldpXdot1EvbConfigCdcpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpXdot1EvbConfigCdcpTable_old) == NULL)
			break;
		StorageTmp->lldpXdot1EvbConfigCdcpTxEnable = StorageOld->lldpXdot1EvbConfigCdcpTxEnable;
		if (--StorageTmp->lldpXdot1EvbConfigCdcpTable_rsvs == 0)
			lldpXdot1EvbConfigCdcpTable_destroy(&StorageTmp->lldpXdot1EvbConfigCdcpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void lldpXDot1EvbExtensions_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
lldpXDot1EvbExtensions_loop_handler(int sig)
{
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXDot1EvbExtensions_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (lldpXDot1EvbExtensionsold_signal_handler != NULL)
		(*lldpXDot1EvbExtensionsold_signal_handler) (sig);
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
}

/**
 * @fn void lldpXDot1EvbExtensions_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
lldpXDot1EvbExtensions_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "lldpXDot1EvbExtensions_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("lldpXDot1EvbExtensions", "done.\n"));
	return;
}
