/*****************************************************************************

 @(#) src/snmp/ieee8021mstpmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021mstpmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021MstpMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021MstpMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021MstpMib_refresh = 1;
volatile int ieee8021MstpCistTable_refresh = 1;
volatile int ieee8021MstpTable_refresh = 1;
volatile int ieee8021MstpCistPortTable_refresh = 1;
volatile int ieee8021MstpPortTable_refresh = 1;
volatile int ieee8021MstpFidToMstiTable_refresh = 1;
volatile int ieee8021MstpVlanTable_refresh = 1;
volatile int ieee8021MstpConfigIdTable_refresh = 1;
volatile int ieee8021MstpCistPortExtensionTable_refresh = 1;
volatile int ieee8021MstpFidToMstiV2Table_refresh = 1;
volatile int ieee8021MstpVlanV2Table_refresh = 1;

/*
 * ieee8021MstpMib_variables_oid: object identifier for ieee8021MstpMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021MstpMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 6 };
oid ieee8021MstpTable_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 1, 2, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021MstpCistGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 1 };
oid ieee8021MstpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 2 };
oid ieee8021MstpCistPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 3 };
oid ieee8021MstpPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 4 };
oid ieee8021MstpFidToMstiGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 5 };
oid ieee8021MstpVlanGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 6 };
oid ieee8021MstpConfigIdGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 7 };
oid ieee8021MstpCistPortExtensionGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 8 };
oid ieee8021MstpFidToMstiV2Group_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 9 };
oid ieee8021MstpVlanV2Group_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 1, 10 };
oid ieee8021MstpCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 6, 2, 2, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ieee8021MstpMib_variables: tree for ieee8021MstpMib
 * This variable defines function callbacks and type return information for the ieee8021MstpMib mib section
 */
struct variable4 ieee8021MstpMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021MSTPCISTBRIDGEIDENTIFIER  1
	{(u_char) IEEE8021MSTPCISTBRIDGEIDENTIFIER, ASN_OCTET_STR, RONLY, var_ieee8021MstpCistTable, 4, {1, 1, 1, 2}},
#define   IEEE8021MSTPCISTTOPOLOGYCHANGE  2
	{(u_char) IEEE8021MSTPCISTTOPOLOGYCHANGE, ASN_INTEGER, RONLY, var_ieee8021MstpCistTable, 4, {1, 1, 1, 3}},
#define   IEEE8021MSTPCISTREGIONALROOTIDENTIFIER  3
	{(u_char) IEEE8021MSTPCISTREGIONALROOTIDENTIFIER, ASN_OCTET_STR, RONLY, var_ieee8021MstpCistTable, 4, {1, 1, 1, 4}},
#define   IEEE8021MSTPCISTPATHCOST  4
	{(u_char) IEEE8021MSTPCISTPATHCOST, ASN_UNSIGNED, RONLY, var_ieee8021MstpCistTable, 4, {1, 1, 1, 5}},
#define   IEEE8021MSTPCISTMAXHOPS  5
	{(u_char) IEEE8021MSTPCISTMAXHOPS, ASN_INTEGER, RWRITE, var_ieee8021MstpCistTable, 4, {1, 1, 1, 6}},
#define   IEEE8021MSTPBRIDGEID  6
	{(u_char) IEEE8021MSTPBRIDGEID, ASN_OCTET_STR, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 3}},
#define   IEEE8021MSTPTIMESINCETOPOLOGYCHANGE  7
	{(u_char) IEEE8021MSTPTIMESINCETOPOLOGYCHANGE, ASN_TIMETICKS, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 4}},
#define   IEEE8021MSTPTOPOLOGYCHANGES  8
	{(u_char) IEEE8021MSTPTOPOLOGYCHANGES, ASN_COUNTER64, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 5}},
#define   IEEE8021MSTPTOPOLOGYCHANGE  9
	{(u_char) IEEE8021MSTPTOPOLOGYCHANGE, ASN_INTEGER, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 6}},
#define   IEEE8021MSTPDESIGNATEDROOT  10
	{(u_char) IEEE8021MSTPDESIGNATEDROOT, ASN_OCTET_STR, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 7}},
#define   IEEE8021MSTPROOTPATHCOST  11
	{(u_char) IEEE8021MSTPROOTPATHCOST, ASN_INTEGER, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 8}},
#define   IEEE8021MSTPROOTPORT  12
	{(u_char) IEEE8021MSTPROOTPORT, ASN_UNSIGNED, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 9}},
#define   IEEE8021MSTPBRIDGEPRIORITY  13
	{(u_char) IEEE8021MSTPBRIDGEPRIORITY, ASN_INTEGER, RWRITE, var_ieee8021MstpTable, 4, {1, 2, 1, 10}},
#define   IEEE8021MSTPVIDS0     14
	{(u_char) IEEE8021MSTPVIDS0, ASN_OCTET_STR, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 11}},
#define   IEEE8021MSTPVIDS1     15
	{(u_char) IEEE8021MSTPVIDS1, ASN_OCTET_STR, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 12}},
#define   IEEE8021MSTPVIDS2     16
	{(u_char) IEEE8021MSTPVIDS2, ASN_OCTET_STR, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 13}},
#define   IEEE8021MSTPVIDS3     17
	{(u_char) IEEE8021MSTPVIDS3, ASN_OCTET_STR, RONLY, var_ieee8021MstpTable, 4, {1, 2, 1, 14}},
#define   IEEE8021MSTPROWSTATUS  18
	{(u_char) IEEE8021MSTPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021MstpTable, 4, {1, 2, 1, 15}},
#define   IEEE8021MSTPCISTPORTUPTIME  19
	{(u_char) IEEE8021MSTPCISTPORTUPTIME, ASN_TIMETICKS, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 3}},
#define   IEEE8021MSTPCISTPORTADMINPATHCOST  20
	{(u_char) IEEE8021MSTPCISTPORTADMINPATHCOST, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 4}},
#define   IEEE8021MSTPCISTPORTDESIGNATEDROOT  21
	{(u_char) IEEE8021MSTPCISTPORTDESIGNATEDROOT, ASN_OCTET_STR, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 5}},
#define   IEEE8021MSTPCISTPORTTOPOLOGYCHANGEACK  22
	{(u_char) IEEE8021MSTPCISTPORTTOPOLOGYCHANGEACK, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 6}},
#define   IEEE8021MSTPCISTPORTHELLOTIME  23
	{(u_char) IEEE8021MSTPCISTPORTHELLOTIME, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 7}},
#define   IEEE8021MSTPCISTPORTADMINEDGEPORT  24
	{(u_char) IEEE8021MSTPCISTPORTADMINEDGEPORT, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 8}},
#define   IEEE8021MSTPCISTPORTOPEREDGEPORT  25
	{(u_char) IEEE8021MSTPCISTPORTOPEREDGEPORT, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 9}},
#define   IEEE8021MSTPCISTPORTMACENABLED  26
	{(u_char) IEEE8021MSTPCISTPORTMACENABLED, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 10}},
#define   IEEE8021MSTPCISTPORTMACOPERATIONAL  27
	{(u_char) IEEE8021MSTPCISTPORTMACOPERATIONAL, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 11}},
#define   IEEE8021MSTPCISTPORTRESTRICTEDROLE  28
	{(u_char) IEEE8021MSTPCISTPORTRESTRICTEDROLE, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 12}},
#define   IEEE8021MSTPCISTPORTRESTRICTEDTCN  29
	{(u_char) IEEE8021MSTPCISTPORTRESTRICTEDTCN, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 13}},
#define   IEEE8021MSTPCISTPORTROLE  30
	{(u_char) IEEE8021MSTPCISTPORTROLE, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 14}},
#define   IEEE8021MSTPCISTPORTDISPUTED  31
	{(u_char) IEEE8021MSTPCISTPORTDISPUTED, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 15}},
#define   IEEE8021MSTPCISTPORTCISTREGIONALROOTID  32
	{(u_char) IEEE8021MSTPCISTPORTCISTREGIONALROOTID, ASN_OCTET_STR, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 16}},
#define   IEEE8021MSTPCISTPORTCISTPATHCOST  33
	{(u_char) IEEE8021MSTPCISTPORTCISTPATHCOST, ASN_UNSIGNED, RONLY, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 17}},
#define   IEEE8021MSTPCISTPORTPROTOCOLMIGRATION  34
	{(u_char) IEEE8021MSTPCISTPORTPROTOCOLMIGRATION, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 18}},
#define   IEEE8021MSTPCISTPORTENABLEBPDURX  35
	{(u_char) IEEE8021MSTPCISTPORTENABLEBPDURX, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 19}},
#define   IEEE8021MSTPCISTPORTENABLEBPDUTX  36
	{(u_char) IEEE8021MSTPCISTPORTENABLEBPDUTX, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 20}},
#define   IEEE8021MSTPCISTPORTPSEUDOROOTID  37
	{(u_char) IEEE8021MSTPCISTPORTPSEUDOROOTID, ASN_OCTET_STR, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 21}},
#define   IEEE8021MSTPCISTPORTISL2GP  38
	{(u_char) IEEE8021MSTPCISTPORTISL2GP, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortTable, 4, {1, 3, 1, 22}},
#define   IEEE8021MSTPPORTUPTIME  39
	{(u_char) IEEE8021MSTPPORTUPTIME, ASN_TIMETICKS, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 4}},
#define   IEEE8021MSTPPORTSTATE  40
	{(u_char) IEEE8021MSTPPORTSTATE, ASN_INTEGER, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 5}},
#define   IEEE8021MSTPPORTPRIORITY  41
	{(u_char) IEEE8021MSTPPORTPRIORITY, ASN_INTEGER, RWRITE, var_ieee8021MstpPortTable, 4, {1, 4, 1, 6}},
#define   IEEE8021MSTPPORTPATHCOST  42
	{(u_char) IEEE8021MSTPPORTPATHCOST, ASN_INTEGER, RWRITE, var_ieee8021MstpPortTable, 4, {1, 4, 1, 7}},
#define   IEEE8021MSTPPORTDESIGNATEDROOT  43
	{(u_char) IEEE8021MSTPPORTDESIGNATEDROOT, ASN_OCTET_STR, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 8}},
#define   IEEE8021MSTPPORTDESIGNATEDCOST  44
	{(u_char) IEEE8021MSTPPORTDESIGNATEDCOST, ASN_INTEGER, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 9}},
#define   IEEE8021MSTPPORTDESIGNATEDBRIDGE  45
	{(u_char) IEEE8021MSTPPORTDESIGNATEDBRIDGE, ASN_OCTET_STR, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 10}},
#define   IEEE8021MSTPPORTDESIGNATEDPORT  46
	{(u_char) IEEE8021MSTPPORTDESIGNATEDPORT, ASN_UNSIGNED, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 11}},
#define   IEEE8021MSTPPORTROLE  47
	{(u_char) IEEE8021MSTPPORTROLE, ASN_INTEGER, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 12}},
#define   IEEE8021MSTPPORTDISPUTED  48
	{(u_char) IEEE8021MSTPPORTDISPUTED, ASN_INTEGER, RONLY, var_ieee8021MstpPortTable, 4, {1, 4, 1, 13}},
#define   IEEE8021MSTPFIDTOMSTIMSTID  49
	{(u_char) IEEE8021MSTPFIDTOMSTIMSTID, ASN_UNSIGNED, RWRITE, var_ieee8021MstpFidToMstiTable, 4, {1, 5, 1, 3}},
#define   IEEE8021MSTPVLANMSTID  50
	{(u_char) IEEE8021MSTPVLANMSTID, ASN_UNSIGNED, RONLY, var_ieee8021MstpVlanTable, 4, {1, 6, 1, 3}},
#define   IEEE8021MSTPCONFIGIDFORMATSELECTOR  51
	{(u_char) IEEE8021MSTPCONFIGIDFORMATSELECTOR, ASN_INTEGER, RWRITE, var_ieee8021MstpConfigIdTable, 4, {1, 7, 1, 2}},
#define   IEEE8021MSTPCONFIGURATIONNAME  52
	{(u_char) IEEE8021MSTPCONFIGURATIONNAME, ASN_OCTET_STR, RWRITE, var_ieee8021MstpConfigIdTable, 4, {1, 7, 1, 3}},
#define   IEEE8021MSTPREVISIONLEVEL  53
	{(u_char) IEEE8021MSTPREVISIONLEVEL, ASN_UNSIGNED, RWRITE, var_ieee8021MstpConfigIdTable, 4, {1, 7, 1, 4}},
#define   IEEE8021MSTPCONFIGURATIONDIGEST  54
	{(u_char) IEEE8021MSTPCONFIGURATIONDIGEST, ASN_OCTET_STR, RONLY, var_ieee8021MstpConfigIdTable, 4, {1, 7, 1, 5}},
#define   IEEE8021MSTPCISTPORTAUTOEDGEPORT  55
	{(u_char) IEEE8021MSTPCISTPORTAUTOEDGEPORT, ASN_INTEGER, RWRITE, var_ieee8021MstpCistPortExtensionTable, 4, {1, 8, 1, 1}},
#define   IEEE8021MSTPCISTPORTAUTOISOLATEPORT  56
	{(u_char) IEEE8021MSTPCISTPORTAUTOISOLATEPORT, ASN_INTEGER, RONLY, var_ieee8021MstpCistPortExtensionTable, 4, {1, 8, 1, 2}},
#define   IEEE8021MSTPFIDTOMSTIV2MSTID  57
	{(u_char) IEEE8021MSTPFIDTOMSTIV2MSTID, ASN_UNSIGNED, RWRITE, var_ieee8021MstpFidToMstiV2Table, 4, {1, 9, 1, 3}},
#define   IEEE8021MSTPVLANV2MSTID  58
	{(u_char) IEEE8021MSTPVLANV2MSTID, ASN_UNSIGNED, RONLY, var_ieee8021MstpVlanV2Table, 4, {1, 10, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct ieee8021MstpMib_data *ieee8021MstpMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021MstpCistTableStorage = NULL;
struct header_complex_index *ieee8021MstpTableStorage = NULL;
struct header_complex_index *ieee8021MstpCistPortTableStorage = NULL;
struct header_complex_index *ieee8021MstpPortTableStorage = NULL;
struct header_complex_index *ieee8021MstpFidToMstiTableStorage = NULL;
struct header_complex_index *ieee8021MstpVlanTableStorage = NULL;
struct header_complex_index *ieee8021MstpConfigIdTableStorage = NULL;
struct header_complex_index *ieee8021MstpCistPortExtensionTableStorage = NULL;
struct header_complex_index *ieee8021MstpFidToMstiV2TableStorage = NULL;
struct header_complex_index *ieee8021MstpVlanV2TableStorage = NULL;

void (*ieee8021MstpMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021MstpMib_loop_handler(int);
void ieee8021MstpMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021MstpMib(void)
 * @brief ieee8021MstpMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021MstpMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021MstpMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021MstpMib", "init_ieee8021MstpMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021MstpMib", ieee8021MstpMib_variables, variable4, ieee8021MstpMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021MstpMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021MstpMib", parse_ieee8021MstpMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpCistTable", parse_ieee8021MstpCistTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpTable", parse_ieee8021MstpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpCistPortTable", parse_ieee8021MstpCistPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpPortTable", parse_ieee8021MstpPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpFidToMstiTable", parse_ieee8021MstpFidToMstiTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpVlanTable", parse_ieee8021MstpVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpConfigIdTable", parse_ieee8021MstpConfigIdTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpCistPortExtensionTable", parse_ieee8021MstpCistPortExtensionTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpFidToMstiV2Table", parse_ieee8021MstpFidToMstiV2Table, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021MstpVlanV2Table", parse_ieee8021MstpVlanV2Table, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpCistTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpCistPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpFidToMstiTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpConfigIdTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpCistPortExtensionTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpFidToMstiV2Table, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021MstpVlanV2Table, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021MstpMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021MstpMib_fd_handler, (void *) 1);
	}
	ieee8021MstpMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021MstpMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021MstpMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021MstpMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021MstpMib(void)
{
	DEBUGMSGTL(("ieee8021MstpMib", "deinit_ieee8021MstpMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021MstpMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021MstpMib_variables_oid, sizeof(ieee8021MstpMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021MstpMib");
	snmpd_unregister_config_handler("ieee8021MstpCistTable");
	snmpd_unregister_config_handler("ieee8021MstpTable");
	snmpd_unregister_config_handler("ieee8021MstpCistPortTable");
	snmpd_unregister_config_handler("ieee8021MstpPortTable");
	snmpd_unregister_config_handler("ieee8021MstpFidToMstiTable");
	snmpd_unregister_config_handler("ieee8021MstpVlanTable");
	snmpd_unregister_config_handler("ieee8021MstpConfigIdTable");
	snmpd_unregister_config_handler("ieee8021MstpCistPortExtensionTable");
	snmpd_unregister_config_handler("ieee8021MstpFidToMstiV2Table");
	snmpd_unregister_config_handler("ieee8021MstpVlanV2Table");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

int
term_ieee8021MstpMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021MstpMib", "term_ieee8021MstpMib: terminating...  "));
	deinit_ieee8021MstpMib();
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021MstpMib_data *ieee8021MstpMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021MstpMib.
 *
 * Creates a new ieee8021MstpMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021MstpMib.
 */
struct ieee8021MstpMib_data *
ieee8021MstpMib_create(void)
{
	struct ieee8021MstpMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpMib_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpMib_data *ieee8021MstpMib_duplicate(struct ieee8021MstpMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021MstpMib_data *
ieee8021MstpMib_duplicate(struct ieee8021MstpMib_data *thedata)
{
	struct ieee8021MstpMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpMib_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpMib_destroy(struct ieee8021MstpMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021MstpMib.
 * @brief delete a scalars structure from ieee8021MstpMib.
 *
 * Frees scalars that were previously removed from ieee8021MstpMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpMib_destroy(struct ieee8021MstpMib_data **thedata)
{
	struct ieee8021MstpMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpMib_add(struct ieee8021MstpMib_data *thedata)
 * @param thedata the structure representing ieee8021MstpMib scalars.
 * @brief adds node to the ieee8021MstpMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021MstpMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021MstpMib_add(struct ieee8021MstpMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpMib_add: adding data...  "));
	if (thedata)
		ieee8021MstpMibStorage = thedata;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021MstpMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpMib_data *StorageTmp = ieee8021MstpMib_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021MstpMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpMib: storing data...  "));
	refresh_ieee8021MstpMib(1);
	if ((StorageTmp = ieee8021MstpMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021MstpMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021MstpMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021MstpMib(struct ieee8021MstpMib_data *StorageTmp, struct ieee8021MstpMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021MstpMib(struct ieee8021MstpMib_data *StorageTmp, struct ieee8021MstpMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpMib(struct ieee8021MstpMib_data *StorageTmp, struct ieee8021MstpMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021MstpMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpMib(struct ieee8021MstpMib_data *StorageTmp, struct ieee8021MstpMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021MstpMib(struct 
 * @fn void revert_ieee8021MstpMib(struct ieee8021MstpMib_data *StorageTmp, struct ieee8021MstpMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021MstpMib(struct ieee8021MstpMib_data *StorageTmp, struct ieee8021MstpMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021MstpMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021MstpMib(int force)
{
	if (ieee8021MstpMibStorage == NULL) {
		struct ieee8021MstpMib_data *StorageNew;

		if ((StorageNew = ieee8021MstpMib_create()) == NULL)
			return;
		ieee8021MstpMibStorage = StorageNew;
		ieee8021MstpMib_refresh = 1;
	}
	if (!force && ieee8021MstpMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021MstpMib", "refresh_ieee8021MstpMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021MstpMib_refresh = 0;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021MstpMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021MstpMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021MstpMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021MstpMib(0);
	if ((StorageTmp = ieee8021MstpMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021MstpMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021MstpMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021MstpMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021MstpCistTable_data *ieee8021MstpCistTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpCistTable table.
 *
 * Creates a new ieee8021MstpCistTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpCistTable_data *
ieee8021MstpCistTable_create(void)
{
	struct ieee8021MstpCistTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpCistTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021MstpCistBridgeIdentifier = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpCistBridgeIdentifierLen = 0;
		StorageNew->ieee8021MstpCistBridgeIdentifier[StorageNew->ieee8021MstpCistBridgeIdentifierLen] = 0;
		StorageNew->ieee8021MstpCistTopologyChange = 0;
		if ((StorageNew->ieee8021MstpCistRegionalRootIdentifier = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpCistRegionalRootIdentifierLen = 0;
		StorageNew->ieee8021MstpCistRegionalRootIdentifier[StorageNew->ieee8021MstpCistRegionalRootIdentifierLen] = 0;
		StorageNew->ieee8021MstpCistPathCost = 0;
		StorageNew->ieee8021MstpCistMaxHops = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpCistTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpCistTable_data *ieee8021MstpCistTable_duplicate(struct ieee8021MstpCistTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpCistTable_data *
ieee8021MstpCistTable_duplicate(struct ieee8021MstpCistTable_data *thedata)
{
	struct ieee8021MstpCistTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpCistTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpCistTable_id = thedata->ieee8021MstpCistTable_id;
		StorageNew->ieee8021MstpCistComponentId = thedata->ieee8021MstpCistComponentId;
		if (!(StorageNew->ieee8021MstpCistBridgeIdentifier = malloc(thedata->ieee8021MstpCistBridgeIdentifierLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpCistBridgeIdentifier, thedata->ieee8021MstpCistBridgeIdentifier, thedata->ieee8021MstpCistBridgeIdentifierLen);
		StorageNew->ieee8021MstpCistBridgeIdentifierLen = thedata->ieee8021MstpCistBridgeIdentifierLen;
		StorageNew->ieee8021MstpCistBridgeIdentifier[StorageNew->ieee8021MstpCistBridgeIdentifierLen] = 0;
		StorageNew->ieee8021MstpCistTopologyChange = thedata->ieee8021MstpCistTopologyChange;
		if (!(StorageNew->ieee8021MstpCistRegionalRootIdentifier = malloc(thedata->ieee8021MstpCistRegionalRootIdentifierLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpCistRegionalRootIdentifier, thedata->ieee8021MstpCistRegionalRootIdentifier, thedata->ieee8021MstpCistRegionalRootIdentifierLen);
		StorageNew->ieee8021MstpCistRegionalRootIdentifierLen = thedata->ieee8021MstpCistRegionalRootIdentifierLen;
		StorageNew->ieee8021MstpCistRegionalRootIdentifier[StorageNew->ieee8021MstpCistRegionalRootIdentifierLen] = 0;
		StorageNew->ieee8021MstpCistPathCost = thedata->ieee8021MstpCistPathCost;
		StorageNew->ieee8021MstpCistMaxHops = thedata->ieee8021MstpCistMaxHops;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpCistTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpCistTable_destroy(struct ieee8021MstpCistTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpCistTable_destroy(struct ieee8021MstpCistTable_data **thedata)
{
	struct ieee8021MstpCistTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021MstpCistBridgeIdentifier);
		StorageDel->ieee8021MstpCistBridgeIdentifierLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpCistRegionalRootIdentifier);
		StorageDel->ieee8021MstpCistRegionalRootIdentifierLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpCistTable_add(struct ieee8021MstpCistTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpCistTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpCistTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpCistTable_add(struct ieee8021MstpCistTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpCistComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpCistComponentId, sizeof(thedata->ieee8021MstpCistComponentId));
		header_complex_add_data(&ieee8021MstpCistTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpCistTable_del(struct ieee8021MstpCistTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpCistTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpCistTable_del(struct ieee8021MstpCistTable_data *thedata)
{
	struct ieee8021MstpCistTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpCistTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpCistTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpCistTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpCistTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpCistTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpCistTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpCistTable_data *StorageTmp = ieee8021MstpCistTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpCistTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistComponentId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpCistBridgeIdentifier);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpCistBridgeIdentifier, &StorageTmp->ieee8021MstpCistBridgeIdentifierLen);
	if (StorageTmp->ieee8021MstpCistBridgeIdentifier == NULL) {
		config_perror("invalid specification for ieee8021MstpCistBridgeIdentifier");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistTopologyChange, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpCistRegionalRootIdentifier);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpCistRegionalRootIdentifier, &StorageTmp->ieee8021MstpCistRegionalRootIdentifierLen);
	if (StorageTmp->ieee8021MstpCistRegionalRootIdentifier == NULL) {
		config_perror("invalid specification for ieee8021MstpCistRegionalRootIdentifier");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistPathCost, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistMaxHops, &tmpsize);
	ieee8021MstpCistTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpCistTable(): store configuraiton file for ieee8021MstpCistTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpCistTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpCistTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpCistTable: storing data...  "));
	refresh_ieee8021MstpCistTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpCistTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpCistTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpCistTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpCistBridgeIdentifier, &StorageTmp->ieee8021MstpCistBridgeIdentifierLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistTopologyChange, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpCistRegionalRootIdentifier, &StorageTmp->ieee8021MstpCistRegionalRootIdentifierLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistPathCost, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistMaxHops, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpTable_data *ieee8021MstpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpTable table.
 *
 * Creates a new ieee8021MstpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpTable_data *
ieee8021MstpTable_create(void)
{
	struct ieee8021MstpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021MstpBridgeId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpBridgeIdLen = 0;
		StorageNew->ieee8021MstpBridgeId[StorageNew->ieee8021MstpBridgeIdLen] = 0;
		StorageNew->ieee8021MstpTimeSinceTopologyChange = 0;
		StorageNew->ieee8021MstpTopologyChanges = (struct counter64) {
		0, 0};
		StorageNew->ieee8021MstpTopologyChange = 0;
		if ((StorageNew->ieee8021MstpDesignatedRoot = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpDesignatedRootLen = 0;
		StorageNew->ieee8021MstpDesignatedRoot[StorageNew->ieee8021MstpDesignatedRootLen] = 0;
		StorageNew->ieee8021MstpRootPathCost = 0;
		StorageNew->ieee8021MstpRootPort = 0;
		StorageNew->ieee8021MstpBridgePriority = 0;
		if ((StorageNew->ieee8021MstpVids0 = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpVids0Len = 0;
		StorageNew->ieee8021MstpVids0[StorageNew->ieee8021MstpVids0Len] = 0;
		if ((StorageNew->ieee8021MstpVids1 = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpVids1Len = 0;
		StorageNew->ieee8021MstpVids1[StorageNew->ieee8021MstpVids1Len] = 0;
		if ((StorageNew->ieee8021MstpVids2 = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpVids2Len = 0;
		StorageNew->ieee8021MstpVids2[StorageNew->ieee8021MstpVids2Len] = 0;
		if ((StorageNew->ieee8021MstpVids3 = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpVids3Len = 0;
		StorageNew->ieee8021MstpVids3[StorageNew->ieee8021MstpVids3Len] = 0;
		StorageNew->ieee8021MstpRowStatus = 0;
		StorageNew->ieee8021MstpRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpTable_data *ieee8021MstpTable_duplicate(struct ieee8021MstpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpTable_data *
ieee8021MstpTable_duplicate(struct ieee8021MstpTable_data *thedata)
{
	struct ieee8021MstpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpTable_id = thedata->ieee8021MstpTable_id;
		StorageNew->ieee8021MstpComponentId = thedata->ieee8021MstpComponentId;
		StorageNew->ieee8021MstpId = thedata->ieee8021MstpId;
		if (!(StorageNew->ieee8021MstpBridgeId = malloc(thedata->ieee8021MstpBridgeIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpBridgeId, thedata->ieee8021MstpBridgeId, thedata->ieee8021MstpBridgeIdLen);
		StorageNew->ieee8021MstpBridgeIdLen = thedata->ieee8021MstpBridgeIdLen;
		StorageNew->ieee8021MstpBridgeId[StorageNew->ieee8021MstpBridgeIdLen] = 0;
		StorageNew->ieee8021MstpTimeSinceTopologyChange = thedata->ieee8021MstpTimeSinceTopologyChange;
		StorageNew->ieee8021MstpTopologyChanges = thedata->ieee8021MstpTopologyChanges;
		StorageNew->ieee8021MstpTopologyChange = thedata->ieee8021MstpTopologyChange;
		if (!(StorageNew->ieee8021MstpDesignatedRoot = malloc(thedata->ieee8021MstpDesignatedRootLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpDesignatedRoot, thedata->ieee8021MstpDesignatedRoot, thedata->ieee8021MstpDesignatedRootLen);
		StorageNew->ieee8021MstpDesignatedRootLen = thedata->ieee8021MstpDesignatedRootLen;
		StorageNew->ieee8021MstpDesignatedRoot[StorageNew->ieee8021MstpDesignatedRootLen] = 0;
		StorageNew->ieee8021MstpRootPathCost = thedata->ieee8021MstpRootPathCost;
		StorageNew->ieee8021MstpRootPort = thedata->ieee8021MstpRootPort;
		StorageNew->ieee8021MstpBridgePriority = thedata->ieee8021MstpBridgePriority;
		if (!(StorageNew->ieee8021MstpVids0 = malloc(thedata->ieee8021MstpVids0Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpVids0, thedata->ieee8021MstpVids0, thedata->ieee8021MstpVids0Len);
		StorageNew->ieee8021MstpVids0Len = thedata->ieee8021MstpVids0Len;
		StorageNew->ieee8021MstpVids0[StorageNew->ieee8021MstpVids0Len] = 0;
		if (!(StorageNew->ieee8021MstpVids1 = malloc(thedata->ieee8021MstpVids1Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpVids1, thedata->ieee8021MstpVids1, thedata->ieee8021MstpVids1Len);
		StorageNew->ieee8021MstpVids1Len = thedata->ieee8021MstpVids1Len;
		StorageNew->ieee8021MstpVids1[StorageNew->ieee8021MstpVids1Len] = 0;
		if (!(StorageNew->ieee8021MstpVids2 = malloc(thedata->ieee8021MstpVids2Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpVids2, thedata->ieee8021MstpVids2, thedata->ieee8021MstpVids2Len);
		StorageNew->ieee8021MstpVids2Len = thedata->ieee8021MstpVids2Len;
		StorageNew->ieee8021MstpVids2[StorageNew->ieee8021MstpVids2Len] = 0;
		if (!(StorageNew->ieee8021MstpVids3 = malloc(thedata->ieee8021MstpVids3Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpVids3, thedata->ieee8021MstpVids3, thedata->ieee8021MstpVids3Len);
		StorageNew->ieee8021MstpVids3Len = thedata->ieee8021MstpVids3Len;
		StorageNew->ieee8021MstpVids3[StorageNew->ieee8021MstpVids3Len] = 0;
		StorageNew->ieee8021MstpRowStatus = thedata->ieee8021MstpRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpTable_destroy(struct ieee8021MstpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpTable_destroy(struct ieee8021MstpTable_data **thedata)
{
	struct ieee8021MstpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021MstpBridgeId);
		StorageDel->ieee8021MstpBridgeIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpDesignatedRoot);
		StorageDel->ieee8021MstpDesignatedRootLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpVids0);
		StorageDel->ieee8021MstpVids0Len = 0;
		SNMP_FREE(StorageDel->ieee8021MstpVids1);
		StorageDel->ieee8021MstpVids1Len = 0;
		SNMP_FREE(StorageDel->ieee8021MstpVids2);
		StorageDel->ieee8021MstpVids2Len = 0;
		SNMP_FREE(StorageDel->ieee8021MstpVids3);
		StorageDel->ieee8021MstpVids3Len = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpTable_add(struct ieee8021MstpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpTable_add(struct ieee8021MstpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpComponentId, sizeof(thedata->ieee8021MstpComponentId));
		/* ieee8021MstpId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpId, sizeof(thedata->ieee8021MstpId));
		header_complex_add_data(&ieee8021MstpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpTable_del(struct ieee8021MstpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpTable_del(struct ieee8021MstpTable_data *thedata)
{
	struct ieee8021MstpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpTable_data *StorageTmp = ieee8021MstpTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpBridgeId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpBridgeId, &StorageTmp->ieee8021MstpBridgeIdLen);
	if (StorageTmp->ieee8021MstpBridgeId == NULL) {
		config_perror("invalid specification for ieee8021MstpBridgeId");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021MstpTimeSinceTopologyChange, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021MstpTopologyChanges, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpTopologyChange, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpDesignatedRoot);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpDesignatedRoot, &StorageTmp->ieee8021MstpDesignatedRootLen);
	if (StorageTmp->ieee8021MstpDesignatedRoot == NULL) {
		config_perror("invalid specification for ieee8021MstpDesignatedRoot");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpRootPathCost, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpRootPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpBridgePriority, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpVids0);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpVids0, &StorageTmp->ieee8021MstpVids0Len);
	if (StorageTmp->ieee8021MstpVids0 == NULL) {
		config_perror("invalid specification for ieee8021MstpVids0");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021MstpVids1);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpVids1, &StorageTmp->ieee8021MstpVids1Len);
	if (StorageTmp->ieee8021MstpVids1 == NULL) {
		config_perror("invalid specification for ieee8021MstpVids1");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021MstpVids2);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpVids2, &StorageTmp->ieee8021MstpVids2Len);
	if (StorageTmp->ieee8021MstpVids2 == NULL) {
		config_perror("invalid specification for ieee8021MstpVids2");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021MstpVids3);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpVids3, &StorageTmp->ieee8021MstpVids3Len);
	if (StorageTmp->ieee8021MstpVids3 == NULL) {
		config_perror("invalid specification for ieee8021MstpVids3");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpRowStatus, &tmpsize);
	ieee8021MstpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpTable(): store configuraiton file for ieee8021MstpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpTable: storing data...  "));
	refresh_ieee8021MstpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpBridgeId, &StorageTmp->ieee8021MstpBridgeIdLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021MstpTimeSinceTopologyChange, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021MstpTopologyChanges, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpTopologyChange, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpDesignatedRoot, &StorageTmp->ieee8021MstpDesignatedRootLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpRootPathCost, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpRootPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpBridgePriority, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpVids0, &StorageTmp->ieee8021MstpVids0Len);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpVids1, &StorageTmp->ieee8021MstpVids1Len);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpVids2, &StorageTmp->ieee8021MstpVids2Len);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpVids3, &StorageTmp->ieee8021MstpVids3Len);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpCistPortTable_data *ieee8021MstpCistPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpCistPortTable table.
 *
 * Creates a new ieee8021MstpCistPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpCistPortTable_data *
ieee8021MstpCistPortTable_create(void)
{
	struct ieee8021MstpCistPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpCistPortTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpCistPortUptime = 0;
		StorageNew->ieee8021MstpCistPortAdminPathCost = 0;
		if ((StorageNew->ieee8021MstpCistPortDesignatedRoot = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpCistPortDesignatedRootLen = 0;
		StorageNew->ieee8021MstpCistPortDesignatedRoot[StorageNew->ieee8021MstpCistPortDesignatedRootLen] = 0;
		StorageNew->ieee8021MstpCistPortTopologyChangeAck = 0;
		StorageNew->ieee8021MstpCistPortHelloTime = 0;
		StorageNew->ieee8021MstpCistPortAdminEdgePort = TV_TRUE;
		StorageNew->ieee8021MstpCistPortOperEdgePort = 0;
		StorageNew->ieee8021MstpCistPortMacEnabled = 0;
		StorageNew->ieee8021MstpCistPortMacOperational = 0;
		StorageNew->ieee8021MstpCistPortRestrictedRole = 0;
		StorageNew->ieee8021MstpCistPortRestrictedTcn = 0;
		StorageNew->ieee8021MstpCistPortRole = 0;
		StorageNew->ieee8021MstpCistPortDisputed = 0;
		if ((StorageNew->ieee8021MstpCistPortCistRegionalRootId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpCistPortCistRegionalRootIdLen = 0;
		StorageNew->ieee8021MstpCistPortCistRegionalRootId[StorageNew->ieee8021MstpCistPortCistRegionalRootIdLen] = 0;
		StorageNew->ieee8021MstpCistPortCistPathCost = 0;
		StorageNew->ieee8021MstpCistPortProtocolMigration = 0;
		StorageNew->ieee8021MstpCistPortEnableBPDURx = TV_TRUE;
		StorageNew->ieee8021MstpCistPortEnableBPDUTx = TV_TRUE;
		if ((StorageNew->ieee8021MstpCistPortPseudoRootId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpCistPortPseudoRootIdLen = 0;
		StorageNew->ieee8021MstpCistPortPseudoRootId[StorageNew->ieee8021MstpCistPortPseudoRootIdLen] = 0;
		StorageNew->ieee8021MstpCistPortIsL2Gp = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpCistPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpCistPortTable_data *ieee8021MstpCistPortTable_duplicate(struct ieee8021MstpCistPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpCistPortTable_data *
ieee8021MstpCistPortTable_duplicate(struct ieee8021MstpCistPortTable_data *thedata)
{
	struct ieee8021MstpCistPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpCistPortTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpCistPortTable_id = thedata->ieee8021MstpCistPortTable_id;
		StorageNew->ieee8021MstpCistPortComponentId = thedata->ieee8021MstpCistPortComponentId;
		StorageNew->ieee8021MstpCistPortNum = thedata->ieee8021MstpCistPortNum;
		StorageNew->ieee8021MstpCistPortUptime = thedata->ieee8021MstpCistPortUptime;
		StorageNew->ieee8021MstpCistPortAdminPathCost = thedata->ieee8021MstpCistPortAdminPathCost;
		if (!(StorageNew->ieee8021MstpCistPortDesignatedRoot = malloc(thedata->ieee8021MstpCistPortDesignatedRootLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpCistPortDesignatedRoot, thedata->ieee8021MstpCistPortDesignatedRoot, thedata->ieee8021MstpCistPortDesignatedRootLen);
		StorageNew->ieee8021MstpCistPortDesignatedRootLen = thedata->ieee8021MstpCistPortDesignatedRootLen;
		StorageNew->ieee8021MstpCistPortDesignatedRoot[StorageNew->ieee8021MstpCistPortDesignatedRootLen] = 0;
		StorageNew->ieee8021MstpCistPortTopologyChangeAck = thedata->ieee8021MstpCistPortTopologyChangeAck;
		StorageNew->ieee8021MstpCistPortHelloTime = thedata->ieee8021MstpCistPortHelloTime;
		StorageNew->ieee8021MstpCistPortAdminEdgePort = thedata->ieee8021MstpCistPortAdminEdgePort;
		StorageNew->ieee8021MstpCistPortOperEdgePort = thedata->ieee8021MstpCistPortOperEdgePort;
		StorageNew->ieee8021MstpCistPortMacEnabled = thedata->ieee8021MstpCistPortMacEnabled;
		StorageNew->ieee8021MstpCistPortMacOperational = thedata->ieee8021MstpCistPortMacOperational;
		StorageNew->ieee8021MstpCistPortRestrictedRole = thedata->ieee8021MstpCistPortRestrictedRole;
		StorageNew->ieee8021MstpCistPortRestrictedTcn = thedata->ieee8021MstpCistPortRestrictedTcn;
		StorageNew->ieee8021MstpCistPortRole = thedata->ieee8021MstpCistPortRole;
		StorageNew->ieee8021MstpCistPortDisputed = thedata->ieee8021MstpCistPortDisputed;
		if (!(StorageNew->ieee8021MstpCistPortCistRegionalRootId = malloc(thedata->ieee8021MstpCistPortCistRegionalRootIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpCistPortCistRegionalRootId, thedata->ieee8021MstpCistPortCistRegionalRootId, thedata->ieee8021MstpCistPortCistRegionalRootIdLen);
		StorageNew->ieee8021MstpCistPortCistRegionalRootIdLen = thedata->ieee8021MstpCistPortCistRegionalRootIdLen;
		StorageNew->ieee8021MstpCistPortCistRegionalRootId[StorageNew->ieee8021MstpCistPortCistRegionalRootIdLen] = 0;
		StorageNew->ieee8021MstpCistPortCistPathCost = thedata->ieee8021MstpCistPortCistPathCost;
		StorageNew->ieee8021MstpCistPortProtocolMigration = thedata->ieee8021MstpCistPortProtocolMigration;
		StorageNew->ieee8021MstpCistPortEnableBPDURx = thedata->ieee8021MstpCistPortEnableBPDURx;
		StorageNew->ieee8021MstpCistPortEnableBPDUTx = thedata->ieee8021MstpCistPortEnableBPDUTx;
		if (!(StorageNew->ieee8021MstpCistPortPseudoRootId = malloc(thedata->ieee8021MstpCistPortPseudoRootIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpCistPortPseudoRootId, thedata->ieee8021MstpCistPortPseudoRootId, thedata->ieee8021MstpCistPortPseudoRootIdLen);
		StorageNew->ieee8021MstpCistPortPseudoRootIdLen = thedata->ieee8021MstpCistPortPseudoRootIdLen;
		StorageNew->ieee8021MstpCistPortPseudoRootId[StorageNew->ieee8021MstpCistPortPseudoRootIdLen] = 0;
		StorageNew->ieee8021MstpCistPortIsL2Gp = thedata->ieee8021MstpCistPortIsL2Gp;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpCistPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpCistPortTable_destroy(struct ieee8021MstpCistPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpCistPortTable_destroy(struct ieee8021MstpCistPortTable_data **thedata)
{
	struct ieee8021MstpCistPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021MstpCistPortDesignatedRoot);
		StorageDel->ieee8021MstpCistPortDesignatedRootLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpCistPortCistRegionalRootId);
		StorageDel->ieee8021MstpCistPortCistRegionalRootIdLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpCistPortPseudoRootId);
		StorageDel->ieee8021MstpCistPortPseudoRootIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpCistPortTable_add(struct ieee8021MstpCistPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpCistPortTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpCistPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpCistPortTable_add(struct ieee8021MstpCistPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpCistPortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpCistPortComponentId, sizeof(thedata->ieee8021MstpCistPortComponentId));
		/* ieee8021MstpCistPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpCistPortNum, sizeof(thedata->ieee8021MstpCistPortNum));
		header_complex_add_data(&ieee8021MstpCistPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpCistPortTable_del(struct ieee8021MstpCistPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpCistPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpCistPortTable_del(struct ieee8021MstpCistPortTable_data *thedata)
{
	struct ieee8021MstpCistPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpCistPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpCistPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpCistPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpCistPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpCistPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpCistPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpCistPortTable_data *StorageTmp = ieee8021MstpCistPortTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpCistPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistPortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistPortNum, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021MstpCistPortUptime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortAdminPathCost, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpCistPortDesignatedRoot);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpCistPortDesignatedRoot, &StorageTmp->ieee8021MstpCistPortDesignatedRootLen);
	if (StorageTmp->ieee8021MstpCistPortDesignatedRoot == NULL) {
		config_perror("invalid specification for ieee8021MstpCistPortDesignatedRoot");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortTopologyChangeAck, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortHelloTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortAdminEdgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortOperEdgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortMacEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortMacOperational, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortRestrictedRole, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortRestrictedTcn, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortRole, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortDisputed, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpCistPortCistRegionalRootId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpCistPortCistRegionalRootId, &StorageTmp->ieee8021MstpCistPortCistRegionalRootIdLen);
	if (StorageTmp->ieee8021MstpCistPortCistRegionalRootId == NULL) {
		config_perror("invalid specification for ieee8021MstpCistPortCistRegionalRootId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistPortCistPathCost, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortProtocolMigration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortEnableBPDURx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortEnableBPDUTx, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpCistPortPseudoRootId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpCistPortPseudoRootId, &StorageTmp->ieee8021MstpCistPortPseudoRootIdLen);
	if (StorageTmp->ieee8021MstpCistPortPseudoRootId == NULL) {
		config_perror("invalid specification for ieee8021MstpCistPortPseudoRootId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortIsL2Gp, &tmpsize);
	ieee8021MstpCistPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpCistPortTable(): store configuraiton file for ieee8021MstpCistPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpCistPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpCistPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpCistPortTable: storing data...  "));
	refresh_ieee8021MstpCistPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpCistPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpCistPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpCistPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistPortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021MstpCistPortUptime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortAdminPathCost, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpCistPortDesignatedRoot, &StorageTmp->ieee8021MstpCistPortDesignatedRootLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortTopologyChangeAck, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortHelloTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortAdminEdgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortOperEdgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortMacEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortMacOperational, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortRestrictedRole, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortRestrictedTcn, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortRole, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortDisputed, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpCistPortCistRegionalRootId, &StorageTmp->ieee8021MstpCistPortCistRegionalRootIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistPortCistPathCost, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortProtocolMigration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortEnableBPDURx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortEnableBPDUTx, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpCistPortPseudoRootId, &StorageTmp->ieee8021MstpCistPortPseudoRootIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortIsL2Gp, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpPortTable_data *ieee8021MstpPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpPortTable table.
 *
 * Creates a new ieee8021MstpPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpPortTable_data *
ieee8021MstpPortTable_create(void)
{
	struct ieee8021MstpPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpPortTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpPortUptime = 0;
		StorageNew->ieee8021MstpPortState = 0;
		StorageNew->ieee8021MstpPortPriority = 0;
		StorageNew->ieee8021MstpPortPathCost = 0;
		if ((StorageNew->ieee8021MstpPortDesignatedRoot = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpPortDesignatedRootLen = 0;
		StorageNew->ieee8021MstpPortDesignatedRoot[StorageNew->ieee8021MstpPortDesignatedRootLen] = 0;
		StorageNew->ieee8021MstpPortDesignatedCost = 0;
		if ((StorageNew->ieee8021MstpPortDesignatedBridge = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpPortDesignatedBridgeLen = 0;
		StorageNew->ieee8021MstpPortDesignatedBridge[StorageNew->ieee8021MstpPortDesignatedBridgeLen] = 0;
		StorageNew->ieee8021MstpPortDesignatedPort = 0;
		StorageNew->ieee8021MstpPortRole = 0;
		StorageNew->ieee8021MstpPortDisputed = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpPortTable_data *ieee8021MstpPortTable_duplicate(struct ieee8021MstpPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpPortTable_data *
ieee8021MstpPortTable_duplicate(struct ieee8021MstpPortTable_data *thedata)
{
	struct ieee8021MstpPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpPortTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpPortTable_id = thedata->ieee8021MstpPortTable_id;
		StorageNew->ieee8021MstpPortComponentId = thedata->ieee8021MstpPortComponentId;
		StorageNew->ieee8021MstpPortMstId = thedata->ieee8021MstpPortMstId;
		StorageNew->ieee8021MstpPortNum = thedata->ieee8021MstpPortNum;
		StorageNew->ieee8021MstpPortUptime = thedata->ieee8021MstpPortUptime;
		StorageNew->ieee8021MstpPortState = thedata->ieee8021MstpPortState;
		StorageNew->ieee8021MstpPortPriority = thedata->ieee8021MstpPortPriority;
		StorageNew->ieee8021MstpPortPathCost = thedata->ieee8021MstpPortPathCost;
		if (!(StorageNew->ieee8021MstpPortDesignatedRoot = malloc(thedata->ieee8021MstpPortDesignatedRootLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpPortDesignatedRoot, thedata->ieee8021MstpPortDesignatedRoot, thedata->ieee8021MstpPortDesignatedRootLen);
		StorageNew->ieee8021MstpPortDesignatedRootLen = thedata->ieee8021MstpPortDesignatedRootLen;
		StorageNew->ieee8021MstpPortDesignatedRoot[StorageNew->ieee8021MstpPortDesignatedRootLen] = 0;
		StorageNew->ieee8021MstpPortDesignatedCost = thedata->ieee8021MstpPortDesignatedCost;
		if (!(StorageNew->ieee8021MstpPortDesignatedBridge = malloc(thedata->ieee8021MstpPortDesignatedBridgeLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpPortDesignatedBridge, thedata->ieee8021MstpPortDesignatedBridge, thedata->ieee8021MstpPortDesignatedBridgeLen);
		StorageNew->ieee8021MstpPortDesignatedBridgeLen = thedata->ieee8021MstpPortDesignatedBridgeLen;
		StorageNew->ieee8021MstpPortDesignatedBridge[StorageNew->ieee8021MstpPortDesignatedBridgeLen] = 0;
		StorageNew->ieee8021MstpPortDesignatedPort = thedata->ieee8021MstpPortDesignatedPort;
		StorageNew->ieee8021MstpPortRole = thedata->ieee8021MstpPortRole;
		StorageNew->ieee8021MstpPortDisputed = thedata->ieee8021MstpPortDisputed;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpPortTable_destroy(struct ieee8021MstpPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpPortTable_destroy(struct ieee8021MstpPortTable_data **thedata)
{
	struct ieee8021MstpPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021MstpPortDesignatedRoot);
		StorageDel->ieee8021MstpPortDesignatedRootLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpPortDesignatedBridge);
		StorageDel->ieee8021MstpPortDesignatedBridgeLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpPortTable_add(struct ieee8021MstpPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpPortTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpPortTable_add(struct ieee8021MstpPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpPortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpPortComponentId, sizeof(thedata->ieee8021MstpPortComponentId));
		/* ieee8021MstpPortMstId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpPortMstId, sizeof(thedata->ieee8021MstpPortMstId));
		/* ieee8021MstpPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpPortNum, sizeof(thedata->ieee8021MstpPortNum));
		header_complex_add_data(&ieee8021MstpPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpPortTable_del(struct ieee8021MstpPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpPortTable_del(struct ieee8021MstpPortTable_data *thedata)
{
	struct ieee8021MstpPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpPortTable_data *StorageTmp = ieee8021MstpPortTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpPortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpPortMstId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpPortNum, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->ieee8021MstpPortUptime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpPortState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpPortPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpPortPathCost, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpPortDesignatedRoot);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpPortDesignatedRoot, &StorageTmp->ieee8021MstpPortDesignatedRootLen);
	if (StorageTmp->ieee8021MstpPortDesignatedRoot == NULL) {
		config_perror("invalid specification for ieee8021MstpPortDesignatedRoot");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpPortDesignatedCost, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpPortDesignatedBridge);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpPortDesignatedBridge, &StorageTmp->ieee8021MstpPortDesignatedBridgeLen);
	if (StorageTmp->ieee8021MstpPortDesignatedBridge == NULL) {
		config_perror("invalid specification for ieee8021MstpPortDesignatedBridge");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpPortDesignatedPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpPortRole, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpPortDisputed, &tmpsize);
	ieee8021MstpPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpPortTable(): store configuraiton file for ieee8021MstpPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpPortTable: storing data...  "));
	refresh_ieee8021MstpPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpPortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpPortMstId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->ieee8021MstpPortUptime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpPortState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpPortPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpPortPathCost, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpPortDesignatedRoot, &StorageTmp->ieee8021MstpPortDesignatedRootLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpPortDesignatedCost, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpPortDesignatedBridge, &StorageTmp->ieee8021MstpPortDesignatedBridgeLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpPortDesignatedPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpPortRole, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpPortDisputed, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpFidToMstiTable_data *ieee8021MstpFidToMstiTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpFidToMstiTable table.
 *
 * Creates a new ieee8021MstpFidToMstiTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpFidToMstiTable_data *
ieee8021MstpFidToMstiTable_create(void)
{
	struct ieee8021MstpFidToMstiTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpFidToMstiTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpFidToMstiMstId = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpFidToMstiTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpFidToMstiTable_data *ieee8021MstpFidToMstiTable_duplicate(struct ieee8021MstpFidToMstiTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpFidToMstiTable_data *
ieee8021MstpFidToMstiTable_duplicate(struct ieee8021MstpFidToMstiTable_data *thedata)
{
	struct ieee8021MstpFidToMstiTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpFidToMstiTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpFidToMstiTable_id = thedata->ieee8021MstpFidToMstiTable_id;
		StorageNew->ieee8021MstpFidToMstiComponentId = thedata->ieee8021MstpFidToMstiComponentId;
		StorageNew->ieee8021MstpFidToMstiFid = thedata->ieee8021MstpFidToMstiFid;
		StorageNew->ieee8021MstpFidToMstiMstId = thedata->ieee8021MstpFidToMstiMstId;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpFidToMstiTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpFidToMstiTable_destroy(struct ieee8021MstpFidToMstiTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpFidToMstiTable_destroy(struct ieee8021MstpFidToMstiTable_data **thedata)
{
	struct ieee8021MstpFidToMstiTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpFidToMstiTable_add(struct ieee8021MstpFidToMstiTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpFidToMstiTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpFidToMstiTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpFidToMstiTable_add(struct ieee8021MstpFidToMstiTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpFidToMstiComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpFidToMstiComponentId, sizeof(thedata->ieee8021MstpFidToMstiComponentId));
		/* ieee8021MstpFidToMstiFid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpFidToMstiFid, sizeof(thedata->ieee8021MstpFidToMstiFid));
		header_complex_add_data(&ieee8021MstpFidToMstiTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpFidToMstiTable_del(struct ieee8021MstpFidToMstiTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpFidToMstiTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpFidToMstiTable_del(struct ieee8021MstpFidToMstiTable_data *thedata)
{
	struct ieee8021MstpFidToMstiTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpFidToMstiTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpFidToMstiTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpFidToMstiTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpFidToMstiTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpFidToMstiTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpFidToMstiTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpFidToMstiTable_data *StorageTmp = ieee8021MstpFidToMstiTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpFidToMstiTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpFidToMstiComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpFidToMstiFid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpFidToMstiMstId, &tmpsize);
	ieee8021MstpFidToMstiTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpFidToMstiTable(): store configuraiton file for ieee8021MstpFidToMstiTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpFidToMstiTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpFidToMstiTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpFidToMstiTable: storing data...  "));
	refresh_ieee8021MstpFidToMstiTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpFidToMstiTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpFidToMstiTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpFidToMstiTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpFidToMstiComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpFidToMstiFid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpFidToMstiMstId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpVlanTable_data *ieee8021MstpVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpVlanTable table.
 *
 * Creates a new ieee8021MstpVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpVlanTable_data *
ieee8021MstpVlanTable_create(void)
{
	struct ieee8021MstpVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpVlanTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpVlanMstId = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpVlanTable_data *ieee8021MstpVlanTable_duplicate(struct ieee8021MstpVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpVlanTable_data *
ieee8021MstpVlanTable_duplicate(struct ieee8021MstpVlanTable_data *thedata)
{
	struct ieee8021MstpVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpVlanTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpVlanTable_id = thedata->ieee8021MstpVlanTable_id;
		StorageNew->ieee8021MstpVlanComponentId = thedata->ieee8021MstpVlanComponentId;
		StorageNew->ieee8021MstpVlanId = thedata->ieee8021MstpVlanId;
		StorageNew->ieee8021MstpVlanMstId = thedata->ieee8021MstpVlanMstId;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpVlanTable_destroy(struct ieee8021MstpVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpVlanTable_destroy(struct ieee8021MstpVlanTable_data **thedata)
{
	struct ieee8021MstpVlanTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpVlanTable_add(struct ieee8021MstpVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpVlanTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpVlanTable_add(struct ieee8021MstpVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpVlanComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpVlanComponentId, sizeof(thedata->ieee8021MstpVlanComponentId));
		/* ieee8021MstpVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpVlanId, sizeof(thedata->ieee8021MstpVlanId));
		header_complex_add_data(&ieee8021MstpVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpVlanTable_del(struct ieee8021MstpVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpVlanTable_del(struct ieee8021MstpVlanTable_data *thedata)
{
	struct ieee8021MstpVlanTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpVlanTable_data *StorageTmp = ieee8021MstpVlanTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpVlanComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpVlanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpVlanMstId, &tmpsize);
	ieee8021MstpVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpVlanTable(): store configuraiton file for ieee8021MstpVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpVlanTable: storing data...  "));
	refresh_ieee8021MstpVlanTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpVlanComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpVlanMstId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpConfigIdTable_data *ieee8021MstpConfigIdTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpConfigIdTable table.
 *
 * Creates a new ieee8021MstpConfigIdTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpConfigIdTable_data *
ieee8021MstpConfigIdTable_create(void)
{
	struct ieee8021MstpConfigIdTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpConfigIdTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpConfigIdTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpConfigIdFormatSelector = 0;
		if ((StorageNew->ieee8021MstpConfigurationName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpConfigurationNameLen = 0;
		StorageNew->ieee8021MstpConfigurationName[StorageNew->ieee8021MstpConfigurationNameLen] = 0;
		StorageNew->ieee8021MstpRevisionLevel = 0;
		if ((StorageNew->ieee8021MstpConfigurationDigest = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021MstpConfigurationDigestLen = 0;
		StorageNew->ieee8021MstpConfigurationDigest[StorageNew->ieee8021MstpConfigurationDigestLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpConfigIdTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpConfigIdTable_data *ieee8021MstpConfigIdTable_duplicate(struct ieee8021MstpConfigIdTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpConfigIdTable_data *
ieee8021MstpConfigIdTable_duplicate(struct ieee8021MstpConfigIdTable_data *thedata)
{
	struct ieee8021MstpConfigIdTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpConfigIdTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpConfigIdTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpConfigIdTable_id = thedata->ieee8021MstpConfigIdTable_id;
		StorageNew->ieee8021MstpConfigIdComponentId = thedata->ieee8021MstpConfigIdComponentId;
		StorageNew->ieee8021MstpConfigIdFormatSelector = thedata->ieee8021MstpConfigIdFormatSelector;
		if (!(StorageNew->ieee8021MstpConfigurationName = malloc(thedata->ieee8021MstpConfigurationNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpConfigurationName, thedata->ieee8021MstpConfigurationName, thedata->ieee8021MstpConfigurationNameLen);
		StorageNew->ieee8021MstpConfigurationNameLen = thedata->ieee8021MstpConfigurationNameLen;
		StorageNew->ieee8021MstpConfigurationName[StorageNew->ieee8021MstpConfigurationNameLen] = 0;
		StorageNew->ieee8021MstpRevisionLevel = thedata->ieee8021MstpRevisionLevel;
		if (!(StorageNew->ieee8021MstpConfigurationDigest = malloc(thedata->ieee8021MstpConfigurationDigestLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021MstpConfigurationDigest, thedata->ieee8021MstpConfigurationDigest, thedata->ieee8021MstpConfigurationDigestLen);
		StorageNew->ieee8021MstpConfigurationDigestLen = thedata->ieee8021MstpConfigurationDigestLen;
		StorageNew->ieee8021MstpConfigurationDigest[StorageNew->ieee8021MstpConfigurationDigestLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpConfigIdTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpConfigIdTable_destroy(struct ieee8021MstpConfigIdTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpConfigIdTable_destroy(struct ieee8021MstpConfigIdTable_data **thedata)
{
	struct ieee8021MstpConfigIdTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpConfigIdTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021MstpConfigurationName);
		StorageDel->ieee8021MstpConfigurationNameLen = 0;
		SNMP_FREE(StorageDel->ieee8021MstpConfigurationDigest);
		StorageDel->ieee8021MstpConfigurationDigestLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpConfigIdTable_add(struct ieee8021MstpConfigIdTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpConfigIdTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpConfigIdTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpConfigIdTable_add(struct ieee8021MstpConfigIdTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpConfigIdTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpConfigIdComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpConfigIdComponentId, sizeof(thedata->ieee8021MstpConfigIdComponentId));
		header_complex_add_data(&ieee8021MstpConfigIdTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpConfigIdTable_del(struct ieee8021MstpConfigIdTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpConfigIdTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpConfigIdTable_del(struct ieee8021MstpConfigIdTable_data *thedata)
{
	struct ieee8021MstpConfigIdTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpConfigIdTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpConfigIdTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpConfigIdTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpConfigIdTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpConfigIdTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpConfigIdTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpConfigIdTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpConfigIdTable_data *StorageTmp = ieee8021MstpConfigIdTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpConfigIdTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpConfigIdComponentId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpConfigIdFormatSelector, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpConfigurationName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpConfigurationName, &StorageTmp->ieee8021MstpConfigurationNameLen);
	if (StorageTmp->ieee8021MstpConfigurationName == NULL) {
		config_perror("invalid specification for ieee8021MstpConfigurationName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpRevisionLevel, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021MstpConfigurationDigest);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021MstpConfigurationDigest, &StorageTmp->ieee8021MstpConfigurationDigestLen);
	if (StorageTmp->ieee8021MstpConfigurationDigest == NULL) {
		config_perror("invalid specification for ieee8021MstpConfigurationDigest");
		return;
	}
	ieee8021MstpConfigIdTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpConfigIdTable(): store configuraiton file for ieee8021MstpConfigIdTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpConfigIdTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpConfigIdTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpConfigIdTable: storing data...  "));
	refresh_ieee8021MstpConfigIdTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpConfigIdTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpConfigIdTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpConfigIdTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpConfigIdComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpConfigIdFormatSelector, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpConfigurationName, &StorageTmp->ieee8021MstpConfigurationNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpRevisionLevel, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021MstpConfigurationDigest, &StorageTmp->ieee8021MstpConfigurationDigestLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpCistPortExtensionTable_data *ieee8021MstpCistPortExtensionTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpCistPortExtensionTable table.
 *
 * Creates a new ieee8021MstpCistPortExtensionTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpCistPortExtensionTable_data *
ieee8021MstpCistPortExtensionTable_create(void)
{
	struct ieee8021MstpCistPortExtensionTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpCistPortExtensionTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortExtensionTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpCistPortComponentId = 0;
		StorageNew->ieee8021MstpCistPortNum = 0;
		StorageNew->ieee8021MstpCistPortAutoEdgePort = 0;
		StorageNew->ieee8021MstpCistPortAutoIsolatePort = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpCistPortExtensionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpCistPortExtensionTable_data *ieee8021MstpCistPortExtensionTable_duplicate(struct ieee8021MstpCistPortExtensionTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpCistPortExtensionTable_data *
ieee8021MstpCistPortExtensionTable_duplicate(struct ieee8021MstpCistPortExtensionTable_data *thedata)
{
	struct ieee8021MstpCistPortExtensionTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpCistPortExtensionTable_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortExtensionTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpCistPortExtensionTable_id = thedata->ieee8021MstpCistPortExtensionTable_id;
		StorageNew->ieee8021MstpCistPortComponentId = thedata->ieee8021MstpCistPortComponentId;
		StorageNew->ieee8021MstpCistPortNum = thedata->ieee8021MstpCistPortNum;
		StorageNew->ieee8021MstpCistPortAutoEdgePort = thedata->ieee8021MstpCistPortAutoEdgePort;
		StorageNew->ieee8021MstpCistPortAutoIsolatePort = thedata->ieee8021MstpCistPortAutoIsolatePort;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpCistPortExtensionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpCistPortExtensionTable_destroy(struct ieee8021MstpCistPortExtensionTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpCistPortExtensionTable_destroy(struct ieee8021MstpCistPortExtensionTable_data **thedata)
{
	struct ieee8021MstpCistPortExtensionTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortExtensionTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpCistPortExtensionTable_add(struct ieee8021MstpCistPortExtensionTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpCistPortExtensionTable table data set.
 *
 * Adds a table row structure to the ieee8021MstpCistPortExtensionTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpCistPortExtensionTable_add(struct ieee8021MstpCistPortExtensionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortExtensionTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpCistPortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpCistPortComponentId, sizeof(thedata->ieee8021MstpCistPortComponentId));
		/* ieee8021MstpCistPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpCistPortNum, sizeof(thedata->ieee8021MstpCistPortNum));
		header_complex_add_data(&ieee8021MstpCistPortExtensionTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpCistPortExtensionTable_del(struct ieee8021MstpCistPortExtensionTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpCistPortExtensionTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpCistPortExtensionTable_del(struct ieee8021MstpCistPortExtensionTable_data *thedata)
{
	struct ieee8021MstpCistPortExtensionTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpCistPortExtensionTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpCistPortExtensionTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpCistPortExtensionTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpCistPortExtensionTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpCistPortExtensionTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpCistPortExtensionTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpCistPortExtensionTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpCistPortExtensionTable_data *StorageTmp = ieee8021MstpCistPortExtensionTable_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpCistPortExtensionTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistPortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpCistPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortAutoEdgePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021MstpCistPortAutoIsolatePort, &tmpsize);
	ieee8021MstpCistPortExtensionTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpCistPortExtensionTable(): store configuraiton file for ieee8021MstpCistPortExtensionTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpCistPortExtensionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpCistPortExtensionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpCistPortExtensionTable: storing data...  "));
	refresh_ieee8021MstpCistPortExtensionTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpCistPortExtensionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpCistPortExtensionTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpCistPortExtensionTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistPortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpCistPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortAutoEdgePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021MstpCistPortAutoIsolatePort, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpFidToMstiV2Table_data *ieee8021MstpFidToMstiV2Table_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpFidToMstiV2Table table.
 *
 * Creates a new ieee8021MstpFidToMstiV2Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpFidToMstiV2Table_data *
ieee8021MstpFidToMstiV2Table_create(void)
{
	struct ieee8021MstpFidToMstiV2Table_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpFidToMstiV2Table_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiV2Table_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpFidToMstiV2MstId = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpFidToMstiV2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpFidToMstiV2Table_data *ieee8021MstpFidToMstiV2Table_duplicate(struct ieee8021MstpFidToMstiV2Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpFidToMstiV2Table_data *
ieee8021MstpFidToMstiV2Table_duplicate(struct ieee8021MstpFidToMstiV2Table_data *thedata)
{
	struct ieee8021MstpFidToMstiV2Table_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpFidToMstiV2Table_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiV2Table_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpFidToMstiV2Table_id = thedata->ieee8021MstpFidToMstiV2Table_id;
		StorageNew->ieee8021MstpFidToMstiV2ComponentId = thedata->ieee8021MstpFidToMstiV2ComponentId;
		StorageNew->ieee8021MstpFidToMstiV2Fid = thedata->ieee8021MstpFidToMstiV2Fid;
		StorageNew->ieee8021MstpFidToMstiV2MstId = thedata->ieee8021MstpFidToMstiV2MstId;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpFidToMstiV2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpFidToMstiV2Table_destroy(struct ieee8021MstpFidToMstiV2Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpFidToMstiV2Table_destroy(struct ieee8021MstpFidToMstiV2Table_data **thedata)
{
	struct ieee8021MstpFidToMstiV2Table_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiV2Table_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpFidToMstiV2Table_add(struct ieee8021MstpFidToMstiV2Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpFidToMstiV2Table table data set.
 *
 * Adds a table row structure to the ieee8021MstpFidToMstiV2Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpFidToMstiV2Table_add(struct ieee8021MstpFidToMstiV2Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiV2Table_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpFidToMstiV2ComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpFidToMstiV2ComponentId, sizeof(thedata->ieee8021MstpFidToMstiV2ComponentId));
		/* ieee8021MstpFidToMstiV2Fid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpFidToMstiV2Fid, sizeof(thedata->ieee8021MstpFidToMstiV2Fid));
		header_complex_add_data(&ieee8021MstpFidToMstiV2TableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpFidToMstiV2Table_del(struct ieee8021MstpFidToMstiV2Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpFidToMstiV2Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpFidToMstiV2Table_del(struct ieee8021MstpFidToMstiV2Table_data *thedata)
{
	struct ieee8021MstpFidToMstiV2Table_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpFidToMstiV2Table_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpFidToMstiV2TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpFidToMstiV2TableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpFidToMstiV2Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpFidToMstiV2Table entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpFidToMstiV2Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpFidToMstiV2Table(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpFidToMstiV2Table_data *StorageTmp = ieee8021MstpFidToMstiV2Table_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpFidToMstiV2Table: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpFidToMstiV2ComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpFidToMstiV2Fid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpFidToMstiV2MstId, &tmpsize);
	ieee8021MstpFidToMstiV2Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpFidToMstiV2Table(): store configuraiton file for ieee8021MstpFidToMstiV2Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpFidToMstiV2Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpFidToMstiV2Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpFidToMstiV2Table: storing data...  "));
	refresh_ieee8021MstpFidToMstiV2Table(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpFidToMstiV2TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpFidToMstiV2Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpFidToMstiV2Table ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpFidToMstiV2ComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpFidToMstiV2Fid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpFidToMstiV2MstId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021MstpVlanV2Table_data *ieee8021MstpVlanV2Table_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021MstpVlanV2Table table.
 *
 * Creates a new ieee8021MstpVlanV2Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021MstpVlanV2Table_data *
ieee8021MstpVlanV2Table_create(void)
{
	struct ieee8021MstpVlanV2Table_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpVlanV2Table_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanV2Table_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021MstpVlanV2MstId = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021MstpVlanV2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021MstpVlanV2Table_data *ieee8021MstpVlanV2Table_duplicate(struct ieee8021MstpVlanV2Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021MstpVlanV2Table_data *
ieee8021MstpVlanV2Table_duplicate(struct ieee8021MstpVlanV2Table_data *thedata)
{
	struct ieee8021MstpVlanV2Table_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021MstpVlanV2Table_data);

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanV2Table_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021MstpVlanV2Table_id = thedata->ieee8021MstpVlanV2Table_id;
		StorageNew->ieee8021MstpVlanV2ComponentId = thedata->ieee8021MstpVlanV2ComponentId;
		StorageNew->ieee8021MstpVlanV2Id = thedata->ieee8021MstpVlanV2Id;
		StorageNew->ieee8021MstpVlanV2MstId = thedata->ieee8021MstpVlanV2MstId;
	}
      done:
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021MstpVlanV2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021MstpVlanV2Table_destroy(struct ieee8021MstpVlanV2Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021MstpVlanV2Table_destroy(struct ieee8021MstpVlanV2Table_data **thedata)
{
	struct ieee8021MstpVlanV2Table_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanV2Table_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpVlanV2Table_add(struct ieee8021MstpVlanV2Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021MstpVlanV2Table table data set.
 *
 * Adds a table row structure to the ieee8021MstpVlanV2Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021MstpVlanV2Table_add(struct ieee8021MstpVlanV2Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanV2Table_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021MstpVlanV2ComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpVlanV2ComponentId, sizeof(thedata->ieee8021MstpVlanV2ComponentId));
		/* ieee8021MstpVlanV2Id */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021MstpVlanV2Id, sizeof(thedata->ieee8021MstpVlanV2Id));
		header_complex_add_data(&ieee8021MstpVlanV2TableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021MstpVlanV2Table_del(struct ieee8021MstpVlanV2Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021MstpVlanV2Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021MstpVlanV2Table_del(struct ieee8021MstpVlanV2Table_data *thedata)
{
	struct ieee8021MstpVlanV2Table_data *StorageDel;

	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpVlanV2Table_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021MstpVlanV2TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021MstpVlanV2TableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021MstpVlanV2Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021MstpVlanV2Table entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021MstpVlanV2Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021MstpVlanV2Table(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021MstpVlanV2Table_data *StorageTmp = ieee8021MstpVlanV2Table_create();

	DEBUGMSGTL(("ieee8021MstpMib", "parse_ieee8021MstpVlanV2Table: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpVlanV2ComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpVlanV2Id, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021MstpVlanV2MstId, &tmpsize);
	ieee8021MstpVlanV2Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/*
 * store_ieee8021MstpVlanV2Table(): store configuraiton file for ieee8021MstpVlanV2Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021MstpVlanV2Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021MstpVlanV2Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021MstpMib", "store_ieee8021MstpVlanV2Table: storing data...  "));
	refresh_ieee8021MstpVlanV2Table(1);
	(void) tmpsize;
	for (hcindex = ieee8021MstpVlanV2TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021MstpVlanV2Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021MstpVlanV2Table ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpVlanV2ComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpVlanV2Id, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021MstpVlanV2MstId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, struct ieee8021MstpCistTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, struct ieee8021MstpCistTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, struct ieee8021MstpCistTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpCistTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, struct ieee8021MstpCistTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpCistTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, struct ieee8021MstpCistTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, struct ieee8021MstpCistTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpCistTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpCistTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpCistTable_data *
refresh_ieee8021MstpCistTable_row(struct ieee8021MstpCistTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpCistTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpCistTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpCistTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpCistTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpCistTable(int force)
{
	if (!force && ieee8021MstpCistTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpCistTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpCistTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpCistTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpCistTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpCistTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpCistTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpCistTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpCistTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpCistTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPCISTBRIDGEIDENTIFIER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpCistBridgeIdentifierLen;
		rval = (u_char *) StorageTmp->ieee8021MstpCistBridgeIdentifier;
		break;
	case (u_char) IEEE8021MSTPCISTTOPOLOGYCHANGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistTopologyChange);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistTopologyChange;
		break;
	case (u_char) IEEE8021MSTPCISTREGIONALROOTIDENTIFIER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpCistRegionalRootIdentifierLen;
		rval = (u_char *) StorageTmp->ieee8021MstpCistRegionalRootIdentifier;
		break;
	case (u_char) IEEE8021MSTPCISTPATHCOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPathCost);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPathCost;
		break;
	case (u_char) IEEE8021MSTPCISTMAXHOPS:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistMaxHops;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistMaxHops);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistMaxHops;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, struct ieee8021MstpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, struct ieee8021MstpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, struct ieee8021MstpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, struct ieee8021MstpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, struct ieee8021MstpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, struct ieee8021MstpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpTable_data *
refresh_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpTable(int force)
{
	if (!force && ieee8021MstpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPBRIDGEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpBridgeIdLen;
		rval = (u_char *) StorageTmp->ieee8021MstpBridgeId;
		break;
	case (u_char) IEEE8021MSTPTIMESINCETOPOLOGYCHANGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpTimeSinceTopologyChange);
		rval = (u_char *) &StorageTmp->ieee8021MstpTimeSinceTopologyChange;
		break;
	case (u_char) IEEE8021MSTPTOPOLOGYCHANGES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpTopologyChanges);
		rval = (u_char *) &StorageTmp->ieee8021MstpTopologyChanges;
		break;
	case (u_char) IEEE8021MSTPTOPOLOGYCHANGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpTopologyChange);
		rval = (u_char *) &StorageTmp->ieee8021MstpTopologyChange;
		break;
	case (u_char) IEEE8021MSTPDESIGNATEDROOT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpDesignatedRootLen;
		rval = (u_char *) StorageTmp->ieee8021MstpDesignatedRoot;
		break;
	case (u_char) IEEE8021MSTPROOTPATHCOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpRootPathCost);
		rval = (u_char *) &StorageTmp->ieee8021MstpRootPathCost;
		break;
	case (u_char) IEEE8021MSTPROOTPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpRootPort);
		rval = (u_char *) &StorageTmp->ieee8021MstpRootPort;
		break;
	case (u_char) IEEE8021MSTPBRIDGEPRIORITY:	/* Create */
		*write_method = write_ieee8021MstpBridgePriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpBridgePriority);
		rval = (u_char *) &StorageTmp->ieee8021MstpBridgePriority;
		break;
	case (u_char) IEEE8021MSTPVIDS0:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpVids0Len;
		rval = (u_char *) StorageTmp->ieee8021MstpVids0;
		break;
	case (u_char) IEEE8021MSTPVIDS1:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpVids1Len;
		rval = (u_char *) StorageTmp->ieee8021MstpVids1;
		break;
	case (u_char) IEEE8021MSTPVIDS2:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpVids2Len;
		rval = (u_char *) StorageTmp->ieee8021MstpVids2;
		break;
	case (u_char) IEEE8021MSTPVIDS3:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpVids3Len;
		rval = (u_char *) StorageTmp->ieee8021MstpVids3;
		break;
	case (u_char) IEEE8021MSTPROWSTATUS:	/* Create */
		*write_method = write_ieee8021MstpRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021MstpRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, struct ieee8021MstpCistPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, struct ieee8021MstpCistPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, struct ieee8021MstpCistPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpCistPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, struct ieee8021MstpCistPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpCistPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, struct ieee8021MstpCistPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, struct ieee8021MstpCistPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpCistPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpCistPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpCistPortTable_data *
refresh_ieee8021MstpCistPortTable_row(struct ieee8021MstpCistPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpCistPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpCistPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpCistPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpCistPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpCistPortTable(int force)
{
	if (!force && ieee8021MstpCistPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpCistPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpCistPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpCistPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpCistPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpCistPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpCistPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpCistPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPCISTPORTUPTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortUptime);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortUptime;
		break;
	case (u_char) IEEE8021MSTPCISTPORTADMINPATHCOST:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortAdminPathCost;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortAdminPathCost);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortAdminPathCost;
		break;
	case (u_char) IEEE8021MSTPCISTPORTDESIGNATEDROOT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpCistPortDesignatedRootLen;
		rval = (u_char *) StorageTmp->ieee8021MstpCistPortDesignatedRoot;
		break;
	case (u_char) IEEE8021MSTPCISTPORTTOPOLOGYCHANGEACK:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortTopologyChangeAck);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortTopologyChangeAck;
		break;
	case (u_char) IEEE8021MSTPCISTPORTHELLOTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortHelloTime);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortHelloTime;
		break;
	case (u_char) IEEE8021MSTPCISTPORTADMINEDGEPORT:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortAdminEdgePort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortAdminEdgePort);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortAdminEdgePort;
		break;
	case (u_char) IEEE8021MSTPCISTPORTOPEREDGEPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortOperEdgePort);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortOperEdgePort;
		break;
	case (u_char) IEEE8021MSTPCISTPORTMACENABLED:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortMacEnabled;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortMacEnabled);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortMacEnabled;
		break;
	case (u_char) IEEE8021MSTPCISTPORTMACOPERATIONAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortMacOperational);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortMacOperational;
		break;
	case (u_char) IEEE8021MSTPCISTPORTRESTRICTEDROLE:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortRestrictedRole;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortRestrictedRole);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortRestrictedRole;
		break;
	case (u_char) IEEE8021MSTPCISTPORTRESTRICTEDTCN:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortRestrictedTcn;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortRestrictedTcn);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortRestrictedTcn;
		break;
	case (u_char) IEEE8021MSTPCISTPORTROLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortRole);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortRole;
		break;
	case (u_char) IEEE8021MSTPCISTPORTDISPUTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortDisputed);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortDisputed;
		break;
	case (u_char) IEEE8021MSTPCISTPORTCISTREGIONALROOTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpCistPortCistRegionalRootIdLen;
		rval = (u_char *) StorageTmp->ieee8021MstpCistPortCistRegionalRootId;
		break;
	case (u_char) IEEE8021MSTPCISTPORTCISTPATHCOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortCistPathCost);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortCistPathCost;
		break;
	case (u_char) IEEE8021MSTPCISTPORTPROTOCOLMIGRATION:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortProtocolMigration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortProtocolMigration);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortProtocolMigration;
		break;
	case (u_char) IEEE8021MSTPCISTPORTENABLEBPDURX:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortEnableBPDURx;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortEnableBPDURx);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortEnableBPDURx;
		break;
	case (u_char) IEEE8021MSTPCISTPORTENABLEBPDUTX:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortEnableBPDUTx;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortEnableBPDUTx);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortEnableBPDUTx;
		break;
	case (u_char) IEEE8021MSTPCISTPORTPSEUDOROOTID:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortPseudoRootId;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpCistPortPseudoRootIdLen;
		rval = (u_char *) StorageTmp->ieee8021MstpCistPortPseudoRootId;
		break;
	case (u_char) IEEE8021MSTPCISTPORTISL2GP:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortIsL2Gp;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortIsL2Gp);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortIsL2Gp;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, struct ieee8021MstpPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, struct ieee8021MstpPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, struct ieee8021MstpPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, struct ieee8021MstpPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, struct ieee8021MstpPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, struct ieee8021MstpPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpPortTable_data *
refresh_ieee8021MstpPortTable_row(struct ieee8021MstpPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpPortTable(int force)
{
	if (!force && ieee8021MstpPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPPORTUPTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortUptime);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortUptime;
		break;
	case (u_char) IEEE8021MSTPPORTSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortState);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortState;
		break;
	case (u_char) IEEE8021MSTPPORTPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021MstpPortPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortPriority);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortPriority;
		break;
	case (u_char) IEEE8021MSTPPORTPATHCOST:	/* ReadWrite */
		*write_method = write_ieee8021MstpPortPathCost;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortPathCost);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortPathCost;
		break;
	case (u_char) IEEE8021MSTPPORTDESIGNATEDROOT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpPortDesignatedRootLen;
		rval = (u_char *) StorageTmp->ieee8021MstpPortDesignatedRoot;
		break;
	case (u_char) IEEE8021MSTPPORTDESIGNATEDCOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortDesignatedCost);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortDesignatedCost;
		break;
	case (u_char) IEEE8021MSTPPORTDESIGNATEDBRIDGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpPortDesignatedBridgeLen;
		rval = (u_char *) StorageTmp->ieee8021MstpPortDesignatedBridge;
		break;
	case (u_char) IEEE8021MSTPPORTDESIGNATEDPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortDesignatedPort);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortDesignatedPort;
		break;
	case (u_char) IEEE8021MSTPPORTROLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortRole);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortRole;
		break;
	case (u_char) IEEE8021MSTPPORTDISPUTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpPortDisputed);
		rval = (u_char *) &StorageTmp->ieee8021MstpPortDisputed;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, struct ieee8021MstpFidToMstiTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, struct ieee8021MstpFidToMstiTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, struct ieee8021MstpFidToMstiTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpFidToMstiTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, struct ieee8021MstpFidToMstiTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpFidToMstiTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, struct ieee8021MstpFidToMstiTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, struct ieee8021MstpFidToMstiTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpFidToMstiTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpFidToMstiTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpFidToMstiTable_data *
refresh_ieee8021MstpFidToMstiTable_row(struct ieee8021MstpFidToMstiTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpFidToMstiTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpFidToMstiTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpFidToMstiTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpFidToMstiTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpFidToMstiTable(int force)
{
	if (!force && ieee8021MstpFidToMstiTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpFidToMstiTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpFidToMstiTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpFidToMstiTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpFidToMstiTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpFidToMstiTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpFidToMstiTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpFidToMstiTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpFidToMstiTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpFidToMstiTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPFIDTOMSTIMSTID:	/* ReadWrite */
		*write_method = write_ieee8021MstpFidToMstiMstId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpFidToMstiMstId);
		rval = (u_char *) &StorageTmp->ieee8021MstpFidToMstiMstId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, struct ieee8021MstpVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, struct ieee8021MstpVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, struct ieee8021MstpVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, struct ieee8021MstpVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, struct ieee8021MstpVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, struct ieee8021MstpVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpVlanTable_data *
refresh_ieee8021MstpVlanTable_row(struct ieee8021MstpVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpVlanTable(int force)
{
	if (!force && ieee8021MstpVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPVLANMSTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpVlanMstId);
		rval = (u_char *) &StorageTmp->ieee8021MstpVlanMstId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, struct ieee8021MstpConfigIdTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, struct ieee8021MstpConfigIdTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, struct ieee8021MstpConfigIdTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpConfigIdTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, struct ieee8021MstpConfigIdTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpConfigIdTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, struct ieee8021MstpConfigIdTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, struct ieee8021MstpConfigIdTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpConfigIdTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpConfigIdTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpConfigIdTable_data *
refresh_ieee8021MstpConfigIdTable_row(struct ieee8021MstpConfigIdTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpConfigIdTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpConfigIdTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpConfigIdTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpConfigIdTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpConfigIdTable(int force)
{
	if (!force && ieee8021MstpConfigIdTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpConfigIdTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpConfigIdTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpConfigIdTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpConfigIdTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpConfigIdTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpConfigIdTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpConfigIdTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpConfigIdTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpConfigIdTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPCONFIGIDFORMATSELECTOR:	/* ReadWrite */
		*write_method = write_ieee8021MstpConfigIdFormatSelector;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpConfigIdFormatSelector);
		rval = (u_char *) &StorageTmp->ieee8021MstpConfigIdFormatSelector;
		break;
	case (u_char) IEEE8021MSTPCONFIGURATIONNAME:	/* ReadWrite */
		*write_method = write_ieee8021MstpConfigurationName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpConfigurationNameLen;
		rval = (u_char *) StorageTmp->ieee8021MstpConfigurationName;
		break;
	case (u_char) IEEE8021MSTPREVISIONLEVEL:	/* ReadWrite */
		*write_method = write_ieee8021MstpRevisionLevel;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpRevisionLevel);
		rval = (u_char *) &StorageTmp->ieee8021MstpRevisionLevel;
		break;
	case (u_char) IEEE8021MSTPCONFIGURATIONDIGEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021MstpConfigurationDigestLen;
		rval = (u_char *) StorageTmp->ieee8021MstpConfigurationDigest;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, struct ieee8021MstpCistPortExtensionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, struct ieee8021MstpCistPortExtensionTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, struct ieee8021MstpCistPortExtensionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpCistPortExtensionTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, struct ieee8021MstpCistPortExtensionTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpCistPortExtensionTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, struct ieee8021MstpCistPortExtensionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, struct ieee8021MstpCistPortExtensionTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpCistPortExtensionTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpCistPortExtensionTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpCistPortExtensionTable_data *
refresh_ieee8021MstpCistPortExtensionTable_row(struct ieee8021MstpCistPortExtensionTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpCistPortExtensionTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpCistPortExtensionTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpCistPortExtensionTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpCistPortExtensionTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpCistPortExtensionTable(int force)
{
	if (!force && ieee8021MstpCistPortExtensionTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpCistPortExtensionTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpCistPortExtensionTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpCistPortExtensionTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpCistPortExtensionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpCistPortExtensionTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpCistPortExtensionTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpCistPortExtensionTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpCistPortExtensionTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpCistPortExtensionTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPCISTPORTAUTOEDGEPORT:	/* ReadWrite */
		*write_method = write_ieee8021MstpCistPortAutoEdgePort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortAutoEdgePort);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortAutoEdgePort;
		break;
	case (u_char) IEEE8021MSTPCISTPORTAUTOISOLATEPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpCistPortAutoIsolatePort);
		rval = (u_char *) &StorageTmp->ieee8021MstpCistPortAutoIsolatePort;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, struct ieee8021MstpFidToMstiV2Table_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, struct ieee8021MstpFidToMstiV2Table_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, struct ieee8021MstpFidToMstiV2Table_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpFidToMstiV2Table_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, struct ieee8021MstpFidToMstiV2Table_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpFidToMstiV2Table_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, struct ieee8021MstpFidToMstiV2Table_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, struct ieee8021MstpFidToMstiV2Table_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpFidToMstiV2Table_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpFidToMstiV2Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpFidToMstiV2Table_data *
refresh_ieee8021MstpFidToMstiV2Table_row(struct ieee8021MstpFidToMstiV2Table_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpFidToMstiV2Table_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpFidToMstiV2Table_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpFidToMstiV2Table(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpFidToMstiV2Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpFidToMstiV2Table(int force)
{
	if (!force && ieee8021MstpFidToMstiV2Table_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpFidToMstiV2Table_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpFidToMstiV2Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpFidToMstiV2Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpFidToMstiV2Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpFidToMstiV2Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpFidToMstiV2Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpFidToMstiV2Table(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpFidToMstiV2TableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpFidToMstiV2Table_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPFIDTOMSTIV2MSTID:	/* ReadWrite */
		*write_method = write_ieee8021MstpFidToMstiV2MstId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpFidToMstiV2MstId);
		rval = (u_char *) &StorageTmp->ieee8021MstpFidToMstiV2MstId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, struct ieee8021MstpVlanV2Table_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, struct ieee8021MstpVlanV2Table_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, struct ieee8021MstpVlanV2Table_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021MstpVlanV2Table_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, struct ieee8021MstpVlanV2Table_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021MstpVlanV2Table_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, struct ieee8021MstpVlanV2Table_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, struct ieee8021MstpVlanV2Table_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021MstpVlanV2Table_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021MstpVlanV2Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021MstpVlanV2Table_data *
refresh_ieee8021MstpVlanV2Table_row(struct ieee8021MstpVlanV2Table_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021MstpVlanV2Table_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021MstpVlanV2Table_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021MstpVlanV2Table(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021MstpVlanV2Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021MstpVlanV2Table(int force)
{
	if (!force && ieee8021MstpVlanV2Table_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021MstpVlanV2Table_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021MstpVlanV2Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021MstpVlanV2Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021MstpMib above.
 */
u_char *
var_ieee8021MstpVlanV2Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021MstpVlanV2Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021MstpMib", "var_ieee8021MstpVlanV2Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021MstpVlanV2Table(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021MstpVlanV2TableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021MstpVlanV2Table_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021MSTPVLANV2MSTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021MstpVlanV2MstId);
		rval = (u_char *) &StorageTmp->ieee8021MstpVlanV2MstId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021MstpCistMaxHops(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistMaxHops(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistMaxHops entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistMaxHops not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistMaxHops: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 6..40 */
		if ((6 > set_value || set_value > 40)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistMaxHops: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old = ieee8021MstpCistTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistTable_rsvs++;
		StorageTmp->ieee8021MstpCistMaxHops = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistMaxHops for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistTable_sets == 0)
				if ((ret = update_ieee8021MstpCistTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old) != NULL) {
			ieee8021MstpCistTable_destroy(&StorageTmp->ieee8021MstpCistTable_old);
			StorageTmp->ieee8021MstpCistTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistTable_tsts = 0;
			StorageTmp->ieee8021MstpCistTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistTable_sets == 0)
			revert_ieee8021MstpCistTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistMaxHops = StorageOld->ieee8021MstpCistMaxHops;
		if (--StorageTmp->ieee8021MstpCistTable_rsvs == 0)
			ieee8021MstpCistTable_destroy(&StorageTmp->ieee8021MstpCistTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpBridgePriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpBridgePriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpBridgePriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021MstpRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpBridgePriority: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpBridgePriority not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpBridgePriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..61440 */
		if ((0 > set_value || set_value > 61440)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpBridgePriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpTable_old) == NULL)
			if (StorageTmp->ieee8021MstpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpTable_old = ieee8021MstpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpTable_rsvs++;
		StorageTmp->ieee8021MstpBridgePriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpTable_tsts == 0)
				if ((ret = check_ieee8021MstpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpBridgePriority for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpTable_sets == 0)
				if ((ret = update_ieee8021MstpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpTable_old) != NULL) {
			ieee8021MstpTable_destroy(&StorageTmp->ieee8021MstpTable_old);
			StorageTmp->ieee8021MstpTable_rsvs = 0;
			StorageTmp->ieee8021MstpTable_tsts = 0;
			StorageTmp->ieee8021MstpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpTable_sets == 0)
			revert_ieee8021MstpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpBridgePriority = StorageOld->ieee8021MstpBridgePriority;
		if (--StorageTmp->ieee8021MstpTable_rsvs == 0)
			ieee8021MstpTable_destroy(&StorageTmp->ieee8021MstpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortAdminPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortAdminPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortAdminPathCost entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAdminPathCost not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAdminPathCost: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..200000000 */
		if ((0 > set_value || set_value > 200000000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAdminPathCost: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortAdminPathCost = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortAdminPathCost for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortAdminPathCost = StorageOld->ieee8021MstpCistPortAdminPathCost;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortAdminEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortAdminEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortAdminEdgePort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAdminEdgePort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAdminEdgePort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAdminEdgePort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortAdminEdgePort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortAdminEdgePort for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortAdminEdgePort = StorageOld->ieee8021MstpCistPortAdminEdgePort;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortMacEnabled(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortMacEnabled(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortMacEnabled entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortMacEnabled not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortMacEnabled: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortMacEnabled: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortMacEnabled = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortMacEnabled for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortMacEnabled = StorageOld->ieee8021MstpCistPortMacEnabled;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortRestrictedRole(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortRestrictedRole(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortRestrictedRole entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortRestrictedRole not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortRestrictedRole: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortRestrictedRole: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortRestrictedRole = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortRestrictedRole for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortRestrictedRole = StorageOld->ieee8021MstpCistPortRestrictedRole;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortRestrictedTcn(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortRestrictedTcn(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortRestrictedTcn entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortRestrictedTcn not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortRestrictedTcn: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortRestrictedTcn: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortRestrictedTcn = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortRestrictedTcn for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortRestrictedTcn = StorageOld->ieee8021MstpCistPortRestrictedTcn;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortProtocolMigration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortProtocolMigration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortProtocolMigration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortProtocolMigration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortProtocolMigration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortProtocolMigration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortProtocolMigration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortProtocolMigration for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortProtocolMigration = StorageOld->ieee8021MstpCistPortProtocolMigration;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortEnableBPDURx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortEnableBPDURx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortEnableBPDURx entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortEnableBPDURx not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortEnableBPDURx: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortEnableBPDURx: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortEnableBPDURx = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortEnableBPDURx for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortEnableBPDURx = StorageOld->ieee8021MstpCistPortEnableBPDURx;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortEnableBPDUTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortEnableBPDUTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortEnableBPDUTx entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortEnableBPDUTx not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortEnableBPDUTx: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortEnableBPDUTx: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortEnableBPDUTx = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortEnableBPDUTx for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortEnableBPDUTx = StorageOld->ieee8021MstpCistPortEnableBPDUTx;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortPseudoRootId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortPseudoRootId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortPseudoRootId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortPseudoRootId not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 8..8 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 8)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortPseudoRootId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021MstpCistPortPseudoRootId);
		StorageTmp->ieee8021MstpCistPortPseudoRootId = string;
		StorageTmp->ieee8021MstpCistPortPseudoRootIdLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortPseudoRootId for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		if (StorageOld->ieee8021MstpCistPortPseudoRootId != NULL) {
			SNMP_FREE(StorageTmp->ieee8021MstpCistPortPseudoRootId);
			StorageTmp->ieee8021MstpCistPortPseudoRootId = StorageOld->ieee8021MstpCistPortPseudoRootId;
			StorageTmp->ieee8021MstpCistPortPseudoRootIdLen = StorageOld->ieee8021MstpCistPortPseudoRootIdLen;
			StorageOld->ieee8021MstpCistPortPseudoRootId = NULL;
			StorageOld->ieee8021MstpCistPortPseudoRootIdLen = 0;
		}
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortIsL2Gp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortIsL2Gp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortIsL2Gp entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortIsL2Gp not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortIsL2Gp: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortIsL2Gp: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old = ieee8021MstpCistPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortIsL2Gp = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortIsL2Gp for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) != NULL) {
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
			StorageTmp->ieee8021MstpCistPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortTable_sets == 0)
			revert_ieee8021MstpCistPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortIsL2Gp = StorageOld->ieee8021MstpCistPortIsL2Gp;
		if (--StorageTmp->ieee8021MstpCistPortTable_rsvs == 0)
			ieee8021MstpCistPortTable_destroy(&StorageTmp->ieee8021MstpCistPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpPortPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpPortPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpPortPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpPortPriority not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpPortPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..240 */
		if ((0 > set_value || set_value > 240)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpPortPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old = ieee8021MstpPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpPortTable_rsvs++;
		StorageTmp->ieee8021MstpPortPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpPortPriority for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpPortTable_sets == 0)
				if ((ret = update_ieee8021MstpPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) != NULL) {
			ieee8021MstpPortTable_destroy(&StorageTmp->ieee8021MstpPortTable_old);
			StorageTmp->ieee8021MstpPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpPortTable_tsts = 0;
			StorageTmp->ieee8021MstpPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpPortTable_sets == 0)
			revert_ieee8021MstpPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpPortPriority = StorageOld->ieee8021MstpPortPriority;
		if (--StorageTmp->ieee8021MstpPortTable_rsvs == 0)
			ieee8021MstpPortTable_destroy(&StorageTmp->ieee8021MstpPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpPortPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpPortPathCost(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpPortPathCost entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpPortTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpPortPathCost not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpPortPathCost: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..200000000 */
		if ((1 > set_value || set_value > 200000000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpPortPathCost: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) == NULL)
			if (StorageTmp->ieee8021MstpPortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old = ieee8021MstpPortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpPortTable_rsvs++;
		StorageTmp->ieee8021MstpPortPathCost = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpPortTable_tsts == 0)
				if ((ret = check_ieee8021MstpPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpPortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpPortPathCost for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpPortTable_sets == 0)
				if ((ret = update_ieee8021MstpPortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpPortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) != NULL) {
			ieee8021MstpPortTable_destroy(&StorageTmp->ieee8021MstpPortTable_old);
			StorageTmp->ieee8021MstpPortTable_rsvs = 0;
			StorageTmp->ieee8021MstpPortTable_tsts = 0;
			StorageTmp->ieee8021MstpPortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpPortTable_sets == 0)
			revert_ieee8021MstpPortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpPortTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpPortPathCost = StorageOld->ieee8021MstpPortPathCost;
		if (--StorageTmp->ieee8021MstpPortTable_rsvs == 0)
			ieee8021MstpPortTable_destroy(&StorageTmp->ieee8021MstpPortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpFidToMstiMstId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpFidToMstiMstId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpFidToMstiTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpFidToMstiMstId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpFidToMstiTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpFidToMstiMstId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpFidToMstiMstId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpFidToMstiMstId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old) == NULL)
			if (StorageTmp->ieee8021MstpFidToMstiTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old = ieee8021MstpFidToMstiTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpFidToMstiTable_rsvs++;
		StorageTmp->ieee8021MstpFidToMstiMstId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpFidToMstiTable_tsts == 0)
				if ((ret = check_ieee8021MstpFidToMstiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpFidToMstiTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpFidToMstiMstId for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpFidToMstiTable_sets == 0)
				if ((ret = update_ieee8021MstpFidToMstiTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpFidToMstiTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old) != NULL) {
			ieee8021MstpFidToMstiTable_destroy(&StorageTmp->ieee8021MstpFidToMstiTable_old);
			StorageTmp->ieee8021MstpFidToMstiTable_rsvs = 0;
			StorageTmp->ieee8021MstpFidToMstiTable_tsts = 0;
			StorageTmp->ieee8021MstpFidToMstiTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpFidToMstiTable_sets == 0)
			revert_ieee8021MstpFidToMstiTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpFidToMstiMstId = StorageOld->ieee8021MstpFidToMstiMstId;
		if (--StorageTmp->ieee8021MstpFidToMstiTable_rsvs == 0)
			ieee8021MstpFidToMstiTable_destroy(&StorageTmp->ieee8021MstpFidToMstiTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpConfigIdFormatSelector(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpConfigIdFormatSelector(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpConfigIdTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpConfigIdFormatSelector entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpConfigIdTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpConfigIdFormatSelector not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpConfigIdFormatSelector: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..0 */
		if (set_value != 0) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpConfigIdFormatSelector: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			if (StorageTmp->ieee8021MstpConfigIdTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old = ieee8021MstpConfigIdTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpConfigIdTable_rsvs++;
		StorageTmp->ieee8021MstpConfigIdFormatSelector = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpConfigIdTable_tsts == 0)
				if ((ret = check_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpConfigIdTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpConfigIdFormatSelector for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpConfigIdTable_sets == 0)
				if ((ret = update_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpConfigIdTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			ieee8021MstpConfigIdTable_destroy(&StorageTmp->ieee8021MstpConfigIdTable_old);
			StorageTmp->ieee8021MstpConfigIdTable_rsvs = 0;
			StorageTmp->ieee8021MstpConfigIdTable_tsts = 0;
			StorageTmp->ieee8021MstpConfigIdTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpConfigIdTable_sets == 0)
			revert_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpConfigIdFormatSelector = StorageOld->ieee8021MstpConfigIdFormatSelector;
		if (--StorageTmp->ieee8021MstpConfigIdTable_rsvs == 0)
			ieee8021MstpConfigIdTable_destroy(&StorageTmp->ieee8021MstpConfigIdTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpConfigurationName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpConfigurationName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpConfigIdTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpConfigurationName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpConfigIdTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpConfigurationName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 32..32 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 32)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpConfigurationName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			if (StorageTmp->ieee8021MstpConfigIdTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old = ieee8021MstpConfigIdTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpConfigIdTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021MstpConfigurationName);
		StorageTmp->ieee8021MstpConfigurationName = string;
		StorageTmp->ieee8021MstpConfigurationNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpConfigIdTable_tsts == 0)
				if ((ret = check_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpConfigIdTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpConfigurationName for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpConfigIdTable_sets == 0)
				if ((ret = update_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpConfigIdTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			ieee8021MstpConfigIdTable_destroy(&StorageTmp->ieee8021MstpConfigIdTable_old);
			StorageTmp->ieee8021MstpConfigIdTable_rsvs = 0;
			StorageTmp->ieee8021MstpConfigIdTable_tsts = 0;
			StorageTmp->ieee8021MstpConfigIdTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpConfigIdTable_sets == 0)
			revert_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			break;
		if (StorageOld->ieee8021MstpConfigurationName != NULL) {
			SNMP_FREE(StorageTmp->ieee8021MstpConfigurationName);
			StorageTmp->ieee8021MstpConfigurationName = StorageOld->ieee8021MstpConfigurationName;
			StorageTmp->ieee8021MstpConfigurationNameLen = StorageOld->ieee8021MstpConfigurationNameLen;
			StorageOld->ieee8021MstpConfigurationName = NULL;
			StorageOld->ieee8021MstpConfigurationNameLen = 0;
		}
		if (--StorageTmp->ieee8021MstpConfigIdTable_rsvs == 0)
			ieee8021MstpConfigIdTable_destroy(&StorageTmp->ieee8021MstpConfigIdTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpRevisionLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpRevisionLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpConfigIdTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpRevisionLevel entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpConfigIdTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpRevisionLevel not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpRevisionLevel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..65535 */
		if ((0 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpRevisionLevel: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			if (StorageTmp->ieee8021MstpConfigIdTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old = ieee8021MstpConfigIdTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpConfigIdTable_rsvs++;
		StorageTmp->ieee8021MstpRevisionLevel = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpConfigIdTable_tsts == 0)
				if ((ret = check_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpConfigIdTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpRevisionLevel for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpConfigIdTable_sets == 0)
				if ((ret = update_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpConfigIdTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) != NULL) {
			ieee8021MstpConfigIdTable_destroy(&StorageTmp->ieee8021MstpConfigIdTable_old);
			StorageTmp->ieee8021MstpConfigIdTable_rsvs = 0;
			StorageTmp->ieee8021MstpConfigIdTable_tsts = 0;
			StorageTmp->ieee8021MstpConfigIdTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpConfigIdTable_sets == 0)
			revert_ieee8021MstpConfigIdTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpConfigIdTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpRevisionLevel = StorageOld->ieee8021MstpRevisionLevel;
		if (--StorageTmp->ieee8021MstpConfigIdTable_rsvs == 0)
			ieee8021MstpConfigIdTable_destroy(&StorageTmp->ieee8021MstpConfigIdTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpCistPortAutoEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpCistPortAutoEdgePort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpCistPortExtensionTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpCistPortAutoEdgePort entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpCistPortExtensionTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAutoEdgePort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAutoEdgePort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpCistPortAutoEdgePort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old) == NULL)
			if (StorageTmp->ieee8021MstpCistPortExtensionTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old = ieee8021MstpCistPortExtensionTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpCistPortExtensionTable_rsvs++;
		StorageTmp->ieee8021MstpCistPortAutoEdgePort = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpCistPortExtensionTable_tsts == 0)
				if ((ret = check_ieee8021MstpCistPortExtensionTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortExtensionTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpCistPortAutoEdgePort for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpCistPortExtensionTable_sets == 0)
				if ((ret = update_ieee8021MstpCistPortExtensionTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpCistPortExtensionTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old) != NULL) {
			ieee8021MstpCistPortExtensionTable_destroy(&StorageTmp->ieee8021MstpCistPortExtensionTable_old);
			StorageTmp->ieee8021MstpCistPortExtensionTable_rsvs = 0;
			StorageTmp->ieee8021MstpCistPortExtensionTable_tsts = 0;
			StorageTmp->ieee8021MstpCistPortExtensionTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpCistPortExtensionTable_sets == 0)
			revert_ieee8021MstpCistPortExtensionTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpCistPortExtensionTable_old) == NULL)
			break;
		StorageTmp->ieee8021MstpCistPortAutoEdgePort = StorageOld->ieee8021MstpCistPortAutoEdgePort;
		if (--StorageTmp->ieee8021MstpCistPortExtensionTable_rsvs == 0)
			ieee8021MstpCistPortExtensionTable_destroy(&StorageTmp->ieee8021MstpCistPortExtensionTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpFidToMstiV2MstId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021MstpFidToMstiV2MstId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpFidToMstiV2Table_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpFidToMstiV2MstId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021MstpFidToMstiV2TableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpFidToMstiV2MstId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpFidToMstiV2MstId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..4095 */
		if ((0 > set_value || set_value > 4095)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpFidToMstiV2MstId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old) == NULL)
			if (StorageTmp->ieee8021MstpFidToMstiV2Table_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old = ieee8021MstpFidToMstiV2Table_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021MstpFidToMstiV2Table_rsvs++;
		StorageTmp->ieee8021MstpFidToMstiV2MstId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021MstpFidToMstiV2Table_tsts == 0)
				if ((ret = check_ieee8021MstpFidToMstiV2Table_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpFidToMstiV2Table_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021MstpFidToMstiV2MstId for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021MstpFidToMstiV2Table_sets == 0)
				if ((ret = update_ieee8021MstpFidToMstiV2Table_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021MstpFidToMstiV2Table_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old) != NULL) {
			ieee8021MstpFidToMstiV2Table_destroy(&StorageTmp->ieee8021MstpFidToMstiV2Table_old);
			StorageTmp->ieee8021MstpFidToMstiV2Table_rsvs = 0;
			StorageTmp->ieee8021MstpFidToMstiV2Table_tsts = 0;
			StorageTmp->ieee8021MstpFidToMstiV2Table_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021MstpFidToMstiV2Table_sets == 0)
			revert_ieee8021MstpFidToMstiV2Table_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021MstpFidToMstiV2Table_old) == NULL)
			break;
		StorageTmp->ieee8021MstpFidToMstiV2MstId = StorageOld->ieee8021MstpFidToMstiV2MstId;
		if (--StorageTmp->ieee8021MstpFidToMstiV2Table_rsvs == 0)
			ieee8021MstpFidToMstiV2Table_destroy(&StorageTmp->ieee8021MstpFidToMstiV2Table_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021MstpTable_row(struct ieee8021MstpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021MstpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021MstpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021MstpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021MstpTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021MstpMib", "write_ieee8021MstpRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021MstpTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021MstpRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021MstpComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021MstpId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021MstpComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021MstpComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021MstpComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021MstpId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021MstpId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4094 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4094)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021MstpId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021MstpTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021MstpTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021MstpComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021MstpId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021MstpTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021MstpRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021MstpTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021MstpTable_old) == NULL)
				if (StorageTmp->ieee8021MstpTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021MstpTable_old = ieee8021MstpTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021MstpTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021MstpTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021MstpTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021MstpTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021MstpRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021MstpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021MstpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021MstpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021MstpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021MstpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021MstpRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021MstpTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021MstpRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021MstpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021MstpRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021MstpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021MstpTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021MstpRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021MstpRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021MstpRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021MstpTable_old) != NULL) {
				ieee8021MstpTable_destroy(&StorageTmp->ieee8021MstpTable_old);
				StorageTmp->ieee8021MstpTable_rsvs = 0;
				StorageTmp->ieee8021MstpTable_tsts = 0;
				StorageTmp->ieee8021MstpTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021MstpTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021MstpTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021MstpRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021MstpTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021MstpRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021MstpTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021MstpTable_del(StorageNew);
				ieee8021MstpTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021MstpTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021MstpTable_rsvs == 0)
				ieee8021MstpTable_destroy(&StorageTmp->ieee8021MstpTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021MstpTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021MstpMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021MstpMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021MstpMibold_signal_handler != NULL)
		(*ieee8021MstpMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
}

/**
 * @fn void ieee8021MstpMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021MstpMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021MstpMib", "ieee8021MstpMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021MstpMib", "done.\n"));
	return;
}
