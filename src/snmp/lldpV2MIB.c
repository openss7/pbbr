/*****************************************************************************

 @(#) src/snmp/lldpv2mib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/lldpv2mib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "lldpV2MIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "lldpV2MIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int lldpV2MIB_refresh = 1;
volatile int lldpV2PortConfigTable_refresh = 1;
volatile int lldpV2DestAddressTable_refresh = 1;
volatile int lldpV2ManAddrConfigTxPortsTable_refresh = 1;
volatile int lldpV2StatsTxPortTable_refresh = 1;
volatile int lldpV2StatsRxPortTable_refresh = 1;
volatile int lldpV2LocPortTable_refresh = 1;
volatile int lldpV2LocManAddrTable_refresh = 1;
volatile int lldpV2RemTable_refresh = 1;
volatile int lldpV2RemManAddrTable_refresh = 1;
volatile int lldpV2RemUnknownTLVTable_refresh = 1;
volatile int lldpV2RemOrgDefInfoTable_refresh = 1;

/*
 * lldpV2MIB_variables_oid: object identifier for lldpV2MIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid lldpV2MIB_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 13 };
oid lldpV2ManAddrConfigTxPortsTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 1, 10, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid lldpV2RemTablesChange_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 0, 0, 1 };

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid lldpV2TxRxCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 1, 1 };
oid lldpV2TxCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 1, 2 };
oid lldpV2RxCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 1, 3 };
oid lldpV2ConfigGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 1 };
oid lldpV2ConfigRxGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 2 };
oid lldpV2ConfigTxGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 3 };
oid lldpV2StatsRxGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 4 };
oid lldpV2StatsTxGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 5 };
oid lldpV2LocSysGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 6 };
oid lldpV2RemSysGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 7 };
oid lldpV2NotificationsGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 2, 2, 8 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 lldpV2MIB_variables: tree for lldpV2MIB
 * This variable defines function callbacks and type return information for the lldpV2MIB mib section
 */
struct variable7 lldpV2MIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   LLDPV2MESSAGETXINTERVAL  1
	{(u_char) LLDPV2MESSAGETXINTERVAL, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 1}},
#define   LLDPV2MESSAGETXHOLDMULTIPLIER  2
	{(u_char) LLDPV2MESSAGETXHOLDMULTIPLIER, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 2}},
#define   LLDPV2REINITDELAY     3
	{(u_char) LLDPV2REINITDELAY, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 3}},
#define   LLDPV2NOTIFICATIONINTERVAL  4
	{(u_char) LLDPV2NOTIFICATIONINTERVAL, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 4}},
#define   LLDPV2TXCREDITMAX     5
	{(u_char) LLDPV2TXCREDITMAX, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 5}},
#define   LLDPV2MESSAGEFASTTX   6
	{(u_char) LLDPV2MESSAGEFASTTX, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 6}},
#define   LLDPV2TXFASTINIT      7
	{(u_char) LLDPV2TXFASTINIT, ASN_UNSIGNED, RWRITE, var_lldpV2MIB, 3, {1, 1, 7}},
#define   LLDPV2PORTCONFIGADMINSTATUS  8
	{(u_char) LLDPV2PORTCONFIGADMINSTATUS, ASN_INTEGER, RWRITE, var_lldpV2PortConfigTable, 5, {1, 1, 8, 1, 3}},
#define   LLDPV2PORTCONFIGNOTIFICATIONENABLE  9
	{(u_char) LLDPV2PORTCONFIGNOTIFICATIONENABLE, ASN_INTEGER, RWRITE, var_lldpV2PortConfigTable, 5, {1, 1, 8, 1, 4}},
#define   LLDPV2PORTCONFIGTLVSTXENABLE  10
	{(u_char) LLDPV2PORTCONFIGTLVSTXENABLE, ASN_OCTET_STR, RWRITE, var_lldpV2PortConfigTable, 5, {1, 1, 8, 1, 5}},
#define   LLDPV2DESTMACADDRESS  11
	{(u_char) LLDPV2DESTMACADDRESS, ASN_OCTET_STR, RONLY, var_lldpV2DestAddressTable, 5, {1, 1, 9, 1, 2}},
#define   LLDPV2MANADDRCONFIGTXENABLE  12
	{(u_char) LLDPV2MANADDRCONFIGTXENABLE, ASN_INTEGER, RWRITE, var_lldpV2ManAddrConfigTxPortsTable, 5, {1, 1, 10, 1, 5}},
#define   LLDPV2MANADDRCONFIGROWSTATUS  13
	{(u_char) LLDPV2MANADDRCONFIGROWSTATUS, ASN_INTEGER, RWRITE, var_lldpV2ManAddrConfigTxPortsTable, 5, {1, 1, 10, 1, 6}},
#define   LLDPV2STATSREMTABLESLASTCHANGETIME  14
	{(u_char) LLDPV2STATSREMTABLESLASTCHANGETIME, ASN_TIMETICKS, RONLY, var_lldpV2MIB, 3, {1, 2, 1}},
#define   LLDPV2STATSREMTABLESINSERTS  15
	{(u_char) LLDPV2STATSREMTABLESINSERTS, ASN_GAUGE, RONLY, var_lldpV2MIB, 3, {1, 2, 2}},
#define   LLDPV2STATSREMTABLESDELETES  16
	{(u_char) LLDPV2STATSREMTABLESDELETES, ASN_GAUGE, RONLY, var_lldpV2MIB, 3, {1, 2, 3}},
#define   LLDPV2STATSREMTABLESDROPS  17
	{(u_char) LLDPV2STATSREMTABLESDROPS, ASN_GAUGE, RONLY, var_lldpV2MIB, 3, {1, 2, 4}},
#define   LLDPV2STATSREMTABLESAGEOUTS  18
	{(u_char) LLDPV2STATSREMTABLESAGEOUTS, ASN_GAUGE, RONLY, var_lldpV2MIB, 3, {1, 2, 5}},
#define   LLDPV2STATSTXPORTFRAMESTOTAL  19
	{(u_char) LLDPV2STATSTXPORTFRAMESTOTAL, ASN_COUNTER, RONLY, var_lldpV2StatsTxPortTable, 5, {1, 2, 6, 1, 3}},
#define   LLDPV2STATSTXLLDPDULENGTHERRORS  20
	{(u_char) LLDPV2STATSTXLLDPDULENGTHERRORS, ASN_COUNTER, RONLY, var_lldpV2StatsTxPortTable, 5, {1, 2, 6, 1, 4}},
#define   LLDPV2STATSRXPORTFRAMESDISCARDEDTOTAL  21
	{(u_char) LLDPV2STATSRXPORTFRAMESDISCARDEDTOTAL, ASN_COUNTER, RONLY, var_lldpV2StatsRxPortTable, 5, {1, 2, 7, 1, 3}},
#define   LLDPV2STATSRXPORTFRAMESERRORS  22
	{(u_char) LLDPV2STATSRXPORTFRAMESERRORS, ASN_COUNTER, RONLY, var_lldpV2StatsRxPortTable, 5, {1, 2, 7, 1, 4}},
#define   LLDPV2STATSRXPORTFRAMESTOTAL  23
	{(u_char) LLDPV2STATSRXPORTFRAMESTOTAL, ASN_COUNTER, RONLY, var_lldpV2StatsRxPortTable, 5, {1, 2, 7, 1, 5}},
#define   LLDPV2STATSRXPORTTLVSDISCARDEDTOTAL  24
	{(u_char) LLDPV2STATSRXPORTTLVSDISCARDEDTOTAL, ASN_COUNTER, RONLY, var_lldpV2StatsRxPortTable, 5, {1, 2, 7, 1, 6}},
#define   LLDPV2STATSRXPORTTLVSUNRECOGNIZEDTOTAL  25
	{(u_char) LLDPV2STATSRXPORTTLVSUNRECOGNIZEDTOTAL, ASN_COUNTER, RONLY, var_lldpV2StatsRxPortTable, 5, {1, 2, 7, 1, 7}},
#define   LLDPV2STATSRXPORTAGEOUTSTOTAL  26
	{(u_char) LLDPV2STATSRXPORTAGEOUTSTOTAL, ASN_GAUGE, RONLY, var_lldpV2StatsRxPortTable, 5, {1, 2, 7, 1, 8}},
#define   LLDPV2LOCCHASSISIDSUBTYPE  27
	{(u_char) LLDPV2LOCCHASSISIDSUBTYPE, ASN_INTEGER, RONLY, var_lldpV2MIB, 3, {1, 3, 1}},
#define   LLDPV2LOCCHASSISID    28
	{(u_char) LLDPV2LOCCHASSISID, ASN_OCTET_STR, RONLY, var_lldpV2MIB, 3, {1, 3, 2}},
#define   LLDPV2LOCSYSNAME      29
	{(u_char) LLDPV2LOCSYSNAME, ASN_OCTET_STR, RONLY, var_lldpV2MIB, 3, {1, 3, 3}},
#define   LLDPV2LOCSYSDESC      30
	{(u_char) LLDPV2LOCSYSDESC, ASN_OCTET_STR, RONLY, var_lldpV2MIB, 3, {1, 3, 4}},
#define   LLDPV2LOCSYSCAPSUPPORTED  31
	{(u_char) LLDPV2LOCSYSCAPSUPPORTED, ASN_OCTET_STR, RONLY, var_lldpV2MIB, 3, {1, 3, 5}},
#define   LLDPV2LOCSYSCAPENABLED  32
	{(u_char) LLDPV2LOCSYSCAPENABLED, ASN_OCTET_STR, RONLY, var_lldpV2MIB, 3, {1, 3, 6}},
#define   LLDPV2LOCPORTIDSUBTYPE  33
	{(u_char) LLDPV2LOCPORTIDSUBTYPE, ASN_INTEGER, RONLY, var_lldpV2LocPortTable, 5, {1, 3, 7, 1, 2}},
#define   LLDPV2LOCPORTID       34
	{(u_char) LLDPV2LOCPORTID, ASN_OCTET_STR, RONLY, var_lldpV2LocPortTable, 5, {1, 3, 7, 1, 3}},
#define   LLDPV2LOCPORTDESC     35
	{(u_char) LLDPV2LOCPORTDESC, ASN_OCTET_STR, RONLY, var_lldpV2LocPortTable, 5, {1, 3, 7, 1, 4}},
#define   LLDPV2LOCMANADDRLEN   36
	{(u_char) LLDPV2LOCMANADDRLEN, ASN_UNSIGNED, RONLY, var_lldpV2LocManAddrTable, 5, {1, 3, 8, 1, 3}},
#define   LLDPV2LOCMANADDRIFSUBTYPE  37
	{(u_char) LLDPV2LOCMANADDRIFSUBTYPE, ASN_INTEGER, RONLY, var_lldpV2LocManAddrTable, 5, {1, 3, 8, 1, 4}},
#define   LLDPV2LOCMANADDRIFID  38
	{(u_char) LLDPV2LOCMANADDRIFID, ASN_UNSIGNED, RONLY, var_lldpV2LocManAddrTable, 5, {1, 3, 8, 1, 5}},
#define   LLDPV2LOCMANADDROID   39
	{(u_char) LLDPV2LOCMANADDROID, ASN_OBJECT_ID, RONLY, var_lldpV2LocManAddrTable, 5, {1, 3, 8, 1, 6}},
#define   LLDPV2REMCHASSISIDSUBTYPE  40
	{(u_char) LLDPV2REMCHASSISIDSUBTYPE, ASN_INTEGER, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 5}},
#define   LLDPV2REMCHASSISID    41
	{(u_char) LLDPV2REMCHASSISID, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 6}},
#define   LLDPV2REMPORTIDSUBTYPE  42
	{(u_char) LLDPV2REMPORTIDSUBTYPE, ASN_INTEGER, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 7}},
#define   LLDPV2REMPORTID       43
	{(u_char) LLDPV2REMPORTID, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 8}},
#define   LLDPV2REMPORTDESC     44
	{(u_char) LLDPV2REMPORTDESC, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 9}},
#define   LLDPV2REMSYSNAME      45
	{(u_char) LLDPV2REMSYSNAME, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 10}},
#define   LLDPV2REMSYSDESC      46
	{(u_char) LLDPV2REMSYSDESC, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 11}},
#define   LLDPV2REMSYSCAPSUPPORTED  47
	{(u_char) LLDPV2REMSYSCAPSUPPORTED, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 12}},
#define   LLDPV2REMSYSCAPENABLED  48
	{(u_char) LLDPV2REMSYSCAPENABLED, ASN_OCTET_STR, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 13}},
#define   LLDPV2REMREMOTECHANGES  49
	{(u_char) LLDPV2REMREMOTECHANGES, ASN_INTEGER, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 14}},
#define   LLDPV2REMTOOMANYNEIGHBORS  50
	{(u_char) LLDPV2REMTOOMANYNEIGHBORS, ASN_INTEGER, RONLY, var_lldpV2RemTable, 5, {1, 4, 1, 1, 15}},
#define   LLDPV2REMMANADDRIFSUBTYPE  51
	{(u_char) LLDPV2REMMANADDRIFSUBTYPE, ASN_INTEGER, RONLY, var_lldpV2RemManAddrTable, 5, {1, 4, 2, 1, 3}},
#define   LLDPV2REMMANADDRIFID  52
	{(u_char) LLDPV2REMMANADDRIFID, ASN_UNSIGNED, RONLY, var_lldpV2RemManAddrTable, 5, {1, 4, 2, 1, 4}},
#define   LLDPV2REMMANADDROID   53
	{(u_char) LLDPV2REMMANADDROID, ASN_OBJECT_ID, RONLY, var_lldpV2RemManAddrTable, 5, {1, 4, 2, 1, 5}},
#define   LLDPV2REMUNKNOWNTLVINFO  54
	{(u_char) LLDPV2REMUNKNOWNTLVINFO, ASN_OCTET_STR, RONLY, var_lldpV2RemUnknownTLVTable, 5, {1, 4, 3, 1, 2}},
#define   LLDPV2REMORGDEFINFO   55
	{(u_char) LLDPV2REMORGDEFINFO, ASN_OCTET_STR, RONLY, var_lldpV2RemOrgDefInfoTable, 5, {1, 4, 4, 1, 4}},
};

/* (L = length of the oidsuffix) */
struct lldpV2MIB_data *lldpV2MIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *lldpV2PortConfigTableStorage = NULL;
struct header_complex_index *lldpV2DestAddressTableStorage = NULL;
struct header_complex_index *lldpV2ManAddrConfigTxPortsTableStorage = NULL;
struct header_complex_index *lldpV2StatsTxPortTableStorage = NULL;
struct header_complex_index *lldpV2StatsRxPortTableStorage = NULL;
struct header_complex_index *lldpV2LocPortTableStorage = NULL;
struct header_complex_index *lldpV2LocManAddrTableStorage = NULL;
struct header_complex_index *lldpV2RemTableStorage = NULL;
struct header_complex_index *lldpV2RemManAddrTableStorage = NULL;
struct header_complex_index *lldpV2RemUnknownTLVTableStorage = NULL;
struct header_complex_index *lldpV2RemOrgDefInfoTableStorage = NULL;

void (*lldpV2MIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void lldpV2MIB_loop_handler(int);
void lldpV2MIB_fd_handler(int, void *);

/**
 * @fn void init_lldpV2MIB(void)
 * @brief lldpV2MIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (lldpV2MIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_lldpV2MIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("lldpV2MIB", "init_lldpV2MIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("lldpV2MIB", lldpV2MIB_variables, variable7, lldpV2MIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_lldpV2MIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("lldpV2MIB", parse_lldpV2MIB, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2PortConfigTable", parse_lldpV2PortConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2DestAddressTable", parse_lldpV2DestAddressTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2ManAddrConfigTxPortsTable", parse_lldpV2ManAddrConfigTxPortsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2StatsTxPortTable", parse_lldpV2StatsTxPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2StatsRxPortTable", parse_lldpV2StatsRxPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2LocPortTable", parse_lldpV2LocPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2LocManAddrTable", parse_lldpV2LocManAddrTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2RemTable", parse_lldpV2RemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2RemManAddrTable", parse_lldpV2RemManAddrTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2RemUnknownTLVTable", parse_lldpV2RemUnknownTLVTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2RemOrgDefInfoTable", parse_lldpV2RemOrgDefInfoTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2MIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2PortConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2DestAddressTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2ManAddrConfigTxPortsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2StatsTxPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2StatsRxPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2LocPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2LocManAddrTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2RemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2RemManAddrTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2RemUnknownTLVTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2RemOrgDefInfoTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, lldpV2MIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, lldpV2MIB_fd_handler, (void *) 1);
	}
	lldpV2MIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &lldpV2MIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/**
 * @fn void deinit_lldpV2MIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (lldpV2MIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_lldpV2MIB(void)
{
	DEBUGMSGTL(("lldpV2MIB", "deinit_lldpV2MIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = lldpV2MIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(lldpV2MIB_variables_oid, sizeof(lldpV2MIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("lldpV2MIB");
	snmpd_unregister_config_handler("lldpV2PortConfigTable");
	snmpd_unregister_config_handler("lldpV2DestAddressTable");
	snmpd_unregister_config_handler("lldpV2ManAddrConfigTxPortsTable");
	snmpd_unregister_config_handler("lldpV2StatsTxPortTable");
	snmpd_unregister_config_handler("lldpV2StatsRxPortTable");
	snmpd_unregister_config_handler("lldpV2LocPortTable");
	snmpd_unregister_config_handler("lldpV2LocManAddrTable");
	snmpd_unregister_config_handler("lldpV2RemTable");
	snmpd_unregister_config_handler("lldpV2RemManAddrTable");
	snmpd_unregister_config_handler("lldpV2RemUnknownTLVTable");
	snmpd_unregister_config_handler("lldpV2RemOrgDefInfoTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

int
term_lldpV2MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("lldpV2MIB", "term_lldpV2MIB: terminating...  "));
	deinit_lldpV2MIB();
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return 0;
}

/**
 * @fn struct lldpV2MIB_data *lldpV2MIB_create(void)
 * @brief create a fresh data structure representing scalars in lldpV2MIB.
 *
 * Creates a new lldpV2MIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in lldpV2MIB.
 */
struct lldpV2MIB_data *
lldpV2MIB_create(void)
{
	struct lldpV2MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2MIB_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2MIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->lldpV2MessageTxInterval = 30;
		StorageNew->lldpV2MessageTxHoldMultiplier = 4;
		StorageNew->lldpV2ReinitDelay = 2;
		StorageNew->lldpV2NotificationInterval = 30;
		StorageNew->lldpV2TxCreditMax = 5;
		StorageNew->lldpV2MessageFastTx = 1;
		StorageNew->lldpV2TxFastInit = 4;
		StorageNew->lldpV2StatsRemTablesLastChangeTime = 0;
		StorageNew->lldpV2StatsRemTablesInserts = 0;
		StorageNew->lldpV2StatsRemTablesDeletes = 0;
		StorageNew->lldpV2StatsRemTablesDrops = 0;
		StorageNew->lldpV2StatsRemTablesAgeouts = 0;
		StorageNew->lldpV2LocChassisIdSubtype = 0;
		if ((StorageNew->lldpV2LocChassisId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2LocChassisIdLen = 0;
		StorageNew->lldpV2LocChassisId[StorageNew->lldpV2LocChassisIdLen] = 0;
		if ((StorageNew->lldpV2LocSysName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2LocSysNameLen = 0;
		StorageNew->lldpV2LocSysName[StorageNew->lldpV2LocSysNameLen] = 0;
		if ((StorageNew->lldpV2LocSysDesc = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2LocSysDescLen = 0;
		StorageNew->lldpV2LocSysDesc[StorageNew->lldpV2LocSysDescLen] = 0;
		if (memdup((u_char **) &StorageNew->lldpV2LocSysCapSupported, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2LocSysCapSupportedLen = 2;
		if (memdup((u_char **) &StorageNew->lldpV2LocSysCapEnabled, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2LocSysCapEnabledLen = 2;

	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2MIB_data *lldpV2MIB_duplicate(struct lldpV2MIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct lldpV2MIB_data *
lldpV2MIB_duplicate(struct lldpV2MIB_data *thedata)
{
	struct lldpV2MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2MIB_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2MIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2MessageTxInterval = thedata->lldpV2MessageTxInterval;
		StorageNew->lldpV2MessageTxHoldMultiplier = thedata->lldpV2MessageTxHoldMultiplier;
		StorageNew->lldpV2ReinitDelay = thedata->lldpV2ReinitDelay;
		StorageNew->lldpV2NotificationInterval = thedata->lldpV2NotificationInterval;
		StorageNew->lldpV2TxCreditMax = thedata->lldpV2TxCreditMax;
		StorageNew->lldpV2MessageFastTx = thedata->lldpV2MessageFastTx;
		StorageNew->lldpV2TxFastInit = thedata->lldpV2TxFastInit;
		StorageNew->lldpV2StatsRemTablesLastChangeTime = thedata->lldpV2StatsRemTablesLastChangeTime;
		StorageNew->lldpV2StatsRemTablesInserts = thedata->lldpV2StatsRemTablesInserts;
		StorageNew->lldpV2StatsRemTablesDeletes = thedata->lldpV2StatsRemTablesDeletes;
		StorageNew->lldpV2StatsRemTablesDrops = thedata->lldpV2StatsRemTablesDrops;
		StorageNew->lldpV2StatsRemTablesAgeouts = thedata->lldpV2StatsRemTablesAgeouts;
		StorageNew->lldpV2LocChassisIdSubtype = thedata->lldpV2LocChassisIdSubtype;
		if (!(StorageNew->lldpV2LocChassisId = malloc(thedata->lldpV2LocChassisIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocChassisId, thedata->lldpV2LocChassisId, thedata->lldpV2LocChassisIdLen);
		StorageNew->lldpV2LocChassisIdLen = thedata->lldpV2LocChassisIdLen;
		StorageNew->lldpV2LocChassisId[StorageNew->lldpV2LocChassisIdLen] = 0;
		if (!(StorageNew->lldpV2LocSysName = malloc(thedata->lldpV2LocSysNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocSysName, thedata->lldpV2LocSysName, thedata->lldpV2LocSysNameLen);
		StorageNew->lldpV2LocSysNameLen = thedata->lldpV2LocSysNameLen;
		StorageNew->lldpV2LocSysName[StorageNew->lldpV2LocSysNameLen] = 0;
		if (!(StorageNew->lldpV2LocSysDesc = malloc(thedata->lldpV2LocSysDescLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocSysDesc, thedata->lldpV2LocSysDesc, thedata->lldpV2LocSysDescLen);
		StorageNew->lldpV2LocSysDescLen = thedata->lldpV2LocSysDescLen;
		StorageNew->lldpV2LocSysDesc[StorageNew->lldpV2LocSysDescLen] = 0;
		if (!(StorageNew->lldpV2LocSysCapSupported = malloc(thedata->lldpV2LocSysCapSupportedLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocSysCapSupported, thedata->lldpV2LocSysCapSupported, thedata->lldpV2LocSysCapSupportedLen);
		StorageNew->lldpV2LocSysCapSupportedLen = thedata->lldpV2LocSysCapSupportedLen;
		StorageNew->lldpV2LocSysCapSupported[StorageNew->lldpV2LocSysCapSupportedLen] = 0;
		if (!(StorageNew->lldpV2LocSysCapEnabled = malloc(thedata->lldpV2LocSysCapEnabledLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocSysCapEnabled, thedata->lldpV2LocSysCapEnabled, thedata->lldpV2LocSysCapEnabledLen);
		StorageNew->lldpV2LocSysCapEnabledLen = thedata->lldpV2LocSysCapEnabledLen;
		StorageNew->lldpV2LocSysCapEnabled[StorageNew->lldpV2LocSysCapEnabledLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2MIB_destroy(struct lldpV2MIB_data **thedata)
 * @param thedata pointer to the data structure in lldpV2MIB.
 * @brief delete a scalars structure from lldpV2MIB.
 *
 * Frees scalars that were previously removed from lldpV2MIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2MIB_destroy(struct lldpV2MIB_data **thedata)
{
	struct lldpV2MIB_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2MIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2LocChassisId);
		StorageDel->lldpV2LocChassisIdLen = 0;
		SNMP_FREE(StorageDel->lldpV2LocSysName);
		StorageDel->lldpV2LocSysNameLen = 0;
		SNMP_FREE(StorageDel->lldpV2LocSysDesc);
		StorageDel->lldpV2LocSysDescLen = 0;
		SNMP_FREE(StorageDel->lldpV2LocSysCapSupported);
		StorageDel->lldpV2LocSysCapSupportedLen = 0;
		SNMP_FREE(StorageDel->lldpV2LocSysCapEnabled);
		StorageDel->lldpV2LocSysCapEnabledLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2MIB_add(struct lldpV2MIB_data *thedata)
 * @param thedata the structure representing lldpV2MIB scalars.
 * @brief adds node to the lldpV2MIB scalar data set.
 *
 * Adds a scalar structure to the lldpV2MIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
lldpV2MIB_add(struct lldpV2MIB_data *thedata)
{
	DEBUGMSGTL(("lldpV2MIB", "lldpV2MIB_add: adding data...  "));
	if (thedata)
		lldpV2MIBStorage = thedata;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2MIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2MIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2MIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_lldpV2MIB(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2MIB_data *StorageTmp = lldpV2MIB_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2MIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2MessageTxInterval, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2MessageTxHoldMultiplier, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2ReinitDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2NotificationInterval, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2TxCreditMax, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2MessageFastTx, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2TxFastInit, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2StatsRemTablesLastChangeTime, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpV2StatsRemTablesInserts, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpV2StatsRemTablesDeletes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpV2StatsRemTablesDrops, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpV2StatsRemTablesAgeouts, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocChassisIdSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2LocChassisId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocChassisId, &StorageTmp->lldpV2LocChassisIdLen);
	if (StorageTmp->lldpV2LocChassisId == NULL) {
		config_perror("invalid specification for lldpV2LocChassisId");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2LocSysName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocSysName, &StorageTmp->lldpV2LocSysNameLen);
	if (StorageTmp->lldpV2LocSysName == NULL) {
		config_perror("invalid specification for lldpV2LocSysName");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2LocSysDesc);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocSysDesc, &StorageTmp->lldpV2LocSysDescLen);
	if (StorageTmp->lldpV2LocSysDesc == NULL) {
		config_perror("invalid specification for lldpV2LocSysDesc");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2LocSysCapSupported);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocSysCapSupported, &StorageTmp->lldpV2LocSysCapSupportedLen);
	if (StorageTmp->lldpV2LocSysCapSupported == NULL) {
		config_perror("invalid specification for lldpV2LocSysCapSupported");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2LocSysCapEnabled);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocSysCapEnabled, &StorageTmp->lldpV2LocSysCapEnabledLen);
	if (StorageTmp->lldpV2LocSysCapEnabled == NULL) {
		config_perror("invalid specification for lldpV2LocSysCapEnabled");
		return;
	}
	lldpV2MIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2MIB(): stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2MIB_data *StorageTmp;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2MIB: storing data...  "));
	refresh_lldpV2MIB(1);
	if ((StorageTmp = lldpV2MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpV2MIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "lldpV2MIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2MessageTxInterval, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2MessageTxHoldMultiplier, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2ReinitDelay, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2NotificationInterval, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2TxCreditMax, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2MessageFastTx, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2TxFastInit, &tmpsize);
		cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2StatsRemTablesLastChangeTime, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpV2StatsRemTablesInserts, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpV2StatsRemTablesDeletes, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpV2StatsRemTablesDrops, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpV2StatsRemTablesAgeouts, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocChassisIdSubtype, &tmpsize);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocChassisId, &StorageTmp->lldpV2LocChassisIdLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocSysName, &StorageTmp->lldpV2LocSysNameLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocSysDesc, &StorageTmp->lldpV2LocSysDescLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocSysCapSupported, &StorageTmp->lldpV2LocSysCapSupportedLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocSysCapEnabled, &StorageTmp->lldpV2LocSysCapEnabledLen);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpV2MIB(struct lldpV2MIB_data *StorageTmp, struct lldpV2MIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_lldpV2MIB(struct lldpV2MIB_data *StorageTmp, struct lldpV2MIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2MIB(struct lldpV2MIB_data *StorageTmp, struct lldpV2MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_lldpV2MIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2MIB(struct lldpV2MIB_data *StorageTmp, struct lldpV2MIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2MIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_lldpV2MIB(struct 
 * @fn void revert_lldpV2MIB(struct lldpV2MIB_data *StorageTmp, struct lldpV2MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_lldpV2MIB(struct lldpV2MIB_data *StorageTmp, struct lldpV2MIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2MIB(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2MIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of lldpV2MIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_lldpV2MIB(int force)
{
	if (lldpV2MIBStorage == NULL) {
		struct lldpV2MIB_data *StorageNew;

		if ((StorageNew = lldpV2MIB_create()) == NULL)
			return;
		lldpV2MIBStorage = StorageNew;
		lldpV2MIB_refresh = 1;
	}
	if (!force && lldpV2MIB_refresh == 0)
		return;
	DEBUGMSGTL(("lldpV2MIB", "refresh_lldpV2MIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	lldpV2MIB_refresh = 0;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/**
 * @fn u_char * var_lldpV2MIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in lldpV2MIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_lldpV2MIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2MIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2MIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_lldpV2MIB(0);
	if ((StorageTmp = lldpV2MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpV2MIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2MESSAGETXINTERVAL:	/* ReadWrite */
		*write_method = write_lldpV2MessageTxInterval;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2MessageTxInterval);
		rval = (u_char *) &StorageTmp->lldpV2MessageTxInterval;
		break;
	case (u_char) LLDPV2MESSAGETXHOLDMULTIPLIER:	/* ReadWrite */
		*write_method = write_lldpV2MessageTxHoldMultiplier;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2MessageTxHoldMultiplier);
		rval = (u_char *) &StorageTmp->lldpV2MessageTxHoldMultiplier;
		break;
	case (u_char) LLDPV2REINITDELAY:	/* ReadWrite */
		*write_method = write_lldpV2ReinitDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2ReinitDelay);
		rval = (u_char *) &StorageTmp->lldpV2ReinitDelay;
		break;
	case (u_char) LLDPV2NOTIFICATIONINTERVAL:	/* ReadWrite */
		*write_method = write_lldpV2NotificationInterval;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2NotificationInterval);
		rval = (u_char *) &StorageTmp->lldpV2NotificationInterval;
		break;
	case (u_char) LLDPV2TXCREDITMAX:	/* ReadWrite */
		*write_method = write_lldpV2TxCreditMax;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2TxCreditMax);
		rval = (u_char *) &StorageTmp->lldpV2TxCreditMax;
		break;
	case (u_char) LLDPV2MESSAGEFASTTX:	/* ReadWrite */
		*write_method = write_lldpV2MessageFastTx;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2MessageFastTx);
		rval = (u_char *) &StorageTmp->lldpV2MessageFastTx;
		break;
	case (u_char) LLDPV2TXFASTINIT:	/* ReadWrite */
		*write_method = write_lldpV2TxFastInit;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2TxFastInit);
		rval = (u_char *) &StorageTmp->lldpV2TxFastInit;
		break;
	case (u_char) LLDPV2STATSREMTABLESLASTCHANGETIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRemTablesLastChangeTime);
		rval = (u_char *) &StorageTmp->lldpV2StatsRemTablesLastChangeTime;
		break;
	case (u_char) LLDPV2STATSREMTABLESINSERTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRemTablesInserts);
		rval = (u_char *) &StorageTmp->lldpV2StatsRemTablesInserts;
		break;
	case (u_char) LLDPV2STATSREMTABLESDELETES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRemTablesDeletes);
		rval = (u_char *) &StorageTmp->lldpV2StatsRemTablesDeletes;
		break;
	case (u_char) LLDPV2STATSREMTABLESDROPS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRemTablesDrops);
		rval = (u_char *) &StorageTmp->lldpV2StatsRemTablesDrops;
		break;
	case (u_char) LLDPV2STATSREMTABLESAGEOUTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRemTablesAgeouts);
		rval = (u_char *) &StorageTmp->lldpV2StatsRemTablesAgeouts;
		break;
	case (u_char) LLDPV2LOCCHASSISIDSUBTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2LocChassisIdSubtype);
		rval = (u_char *) &StorageTmp->lldpV2LocChassisIdSubtype;
		break;
	case (u_char) LLDPV2LOCCHASSISID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocChassisIdLen;
		rval = (u_char *) StorageTmp->lldpV2LocChassisId;
		break;
	case (u_char) LLDPV2LOCSYSNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocSysNameLen;
		rval = (u_char *) StorageTmp->lldpV2LocSysName;
		break;
	case (u_char) LLDPV2LOCSYSDESC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocSysDescLen;
		rval = (u_char *) StorageTmp->lldpV2LocSysDesc;
		break;
	case (u_char) LLDPV2LOCSYSCAPSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocSysCapSupportedLen;
		rval = (u_char *) StorageTmp->lldpV2LocSysCapSupported;
		break;
	case (u_char) LLDPV2LOCSYSCAPENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocSysCapEnabledLen;
		rval = (u_char *) StorageTmp->lldpV2LocSysCapEnabled;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("lldpV2MIB", "found.\n"));
	else
		DEBUGMSGTL(("lldpV2MIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct lldpV2PortConfigTable_data *lldpV2PortConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2PortConfigTable table.
 *
 * Creates a new lldpV2PortConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2PortConfigTable_data *
lldpV2PortConfigTable_create(void)
{
	struct lldpV2PortConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2PortConfigTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2PortConfigTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2PortConfigAdminStatus = LLDPV2PORTCONFIGADMINSTATUS_TXANDRX;
		StorageNew->lldpV2PortConfigNotificationEnable = TV_FALSE;
		if (memdup((u_char **) &StorageNew->lldpV2PortConfigTLVsTxEnable, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2PortConfigTLVsTxEnableLen = 1;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2PortConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2PortConfigTable_data *lldpV2PortConfigTable_duplicate(struct lldpV2PortConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2PortConfigTable_data *
lldpV2PortConfigTable_duplicate(struct lldpV2PortConfigTable_data *thedata)
{
	struct lldpV2PortConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2PortConfigTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2PortConfigTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2PortConfigTable_id = thedata->lldpV2PortConfigTable_id;
		StorageNew->lldpV2PortConfigIfIndex = thedata->lldpV2PortConfigIfIndex;
		StorageNew->lldpV2PortConfigDestAddressIndex = thedata->lldpV2PortConfigDestAddressIndex;
		StorageNew->lldpV2PortConfigAdminStatus = thedata->lldpV2PortConfigAdminStatus;
		StorageNew->lldpV2PortConfigNotificationEnable = thedata->lldpV2PortConfigNotificationEnable;
		if (!(StorageNew->lldpV2PortConfigTLVsTxEnable = malloc(thedata->lldpV2PortConfigTLVsTxEnableLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2PortConfigTLVsTxEnable, thedata->lldpV2PortConfigTLVsTxEnable, thedata->lldpV2PortConfigTLVsTxEnableLen);
		StorageNew->lldpV2PortConfigTLVsTxEnableLen = thedata->lldpV2PortConfigTLVsTxEnableLen;
		StorageNew->lldpV2PortConfigTLVsTxEnable[StorageNew->lldpV2PortConfigTLVsTxEnableLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2PortConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2PortConfigTable_destroy(struct lldpV2PortConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2PortConfigTable_destroy(struct lldpV2PortConfigTable_data **thedata)
{
	struct lldpV2PortConfigTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2PortConfigTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2PortConfigTLVsTxEnable);
		StorageDel->lldpV2PortConfigTLVsTxEnableLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2PortConfigTable_add(struct lldpV2PortConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2PortConfigTable table data set.
 *
 * Adds a table row structure to the lldpV2PortConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2PortConfigTable_add(struct lldpV2PortConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2PortConfigTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2PortConfigIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2PortConfigIfIndex, sizeof(thedata->lldpV2PortConfigIfIndex));
		/* lldpV2PortConfigDestAddressIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2PortConfigDestAddressIndex, sizeof(thedata->lldpV2PortConfigDestAddressIndex));
		header_complex_add_data(&lldpV2PortConfigTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2PortConfigTable_del(struct lldpV2PortConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2PortConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2PortConfigTable_del(struct lldpV2PortConfigTable_data *thedata)
{
	struct lldpV2PortConfigTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2PortConfigTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2PortConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2PortConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2PortConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2PortConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2PortConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2PortConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2PortConfigTable_data *StorageTmp = lldpV2PortConfigTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2PortConfigTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigAdminStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigNotificationEnable, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2PortConfigTLVsTxEnable);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2PortConfigTLVsTxEnable, &StorageTmp->lldpV2PortConfigTLVsTxEnableLen);
	if (StorageTmp->lldpV2PortConfigTLVsTxEnable == NULL) {
		config_perror("invalid specification for lldpV2PortConfigTLVsTxEnable");
		return;
	}
	lldpV2PortConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2PortConfigTable(): store configuraiton file for lldpV2PortConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2PortConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2PortConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2PortConfigTable: storing data...  "));
	refresh_lldpV2PortConfigTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2PortConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2PortConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2PortConfigTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigAdminStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigNotificationEnable, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2PortConfigTLVsTxEnable, &StorageTmp->lldpV2PortConfigTLVsTxEnableLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2DestAddressTable_data *lldpV2DestAddressTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2DestAddressTable table.
 *
 * Creates a new lldpV2DestAddressTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2DestAddressTable_data *
lldpV2DestAddressTable_create(void)
{
	struct lldpV2DestAddressTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2DestAddressTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2DestAddressTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->lldpV2DestMacAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2DestMacAddressLen = 0;
		StorageNew->lldpV2DestMacAddress[StorageNew->lldpV2DestMacAddressLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2DestAddressTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2DestAddressTable_data *lldpV2DestAddressTable_duplicate(struct lldpV2DestAddressTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2DestAddressTable_data *
lldpV2DestAddressTable_duplicate(struct lldpV2DestAddressTable_data *thedata)
{
	struct lldpV2DestAddressTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2DestAddressTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2DestAddressTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2DestAddressTable_id = thedata->lldpV2DestAddressTable_id;
		StorageNew->lldpV2AddressTableIndex = thedata->lldpV2AddressTableIndex;
		if (!(StorageNew->lldpV2DestMacAddress = malloc(thedata->lldpV2DestMacAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2DestMacAddress, thedata->lldpV2DestMacAddress, thedata->lldpV2DestMacAddressLen);
		StorageNew->lldpV2DestMacAddressLen = thedata->lldpV2DestMacAddressLen;
		StorageNew->lldpV2DestMacAddress[StorageNew->lldpV2DestMacAddressLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2DestAddressTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2DestAddressTable_destroy(struct lldpV2DestAddressTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2DestAddressTable_destroy(struct lldpV2DestAddressTable_data **thedata)
{
	struct lldpV2DestAddressTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2DestAddressTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2DestMacAddress);
		StorageDel->lldpV2DestMacAddressLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2DestAddressTable_add(struct lldpV2DestAddressTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2DestAddressTable table data set.
 *
 * Adds a table row structure to the lldpV2DestAddressTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2DestAddressTable_add(struct lldpV2DestAddressTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2DestAddressTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2AddressTableIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2AddressTableIndex, sizeof(thedata->lldpV2AddressTableIndex));
		header_complex_add_data(&lldpV2DestAddressTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2DestAddressTable_del(struct lldpV2DestAddressTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2DestAddressTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2DestAddressTable_del(struct lldpV2DestAddressTable_data *thedata)
{
	struct lldpV2DestAddressTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2DestAddressTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2DestAddressTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2DestAddressTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2DestAddressTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2DestAddressTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2DestAddressTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2DestAddressTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2DestAddressTable_data *StorageTmp = lldpV2DestAddressTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2DestAddressTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2AddressTableIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2DestMacAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2DestMacAddress, &StorageTmp->lldpV2DestMacAddressLen);
	if (StorageTmp->lldpV2DestMacAddress == NULL) {
		config_perror("invalid specification for lldpV2DestMacAddress");
		return;
	}
	lldpV2DestAddressTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2DestAddressTable(): store configuraiton file for lldpV2DestAddressTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2DestAddressTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2DestAddressTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2DestAddressTable: storing data...  "));
	refresh_lldpV2DestAddressTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2DestAddressTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2DestAddressTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2DestAddressTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2AddressTableIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2DestMacAddress, &StorageTmp->lldpV2DestMacAddressLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2ManAddrConfigTxPortsTable_data *lldpV2ManAddrConfigTxPortsTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2ManAddrConfigTxPortsTable table.
 *
 * Creates a new lldpV2ManAddrConfigTxPortsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2ManAddrConfigTxPortsTable_data *
lldpV2ManAddrConfigTxPortsTable_create(void)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2ManAddrConfigTxPortsTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2ManAddrConfigTxPortsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2ManAddrConfigTxEnable = TV_FALSE;
		StorageNew->lldpV2ManAddrConfigRowStatus = 0;
		StorageNew->lldpV2ManAddrConfigRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2ManAddrConfigTxPortsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2ManAddrConfigTxPortsTable_data *lldpV2ManAddrConfigTxPortsTable_duplicate(struct lldpV2ManAddrConfigTxPortsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2ManAddrConfigTxPortsTable_data *
lldpV2ManAddrConfigTxPortsTable_duplicate(struct lldpV2ManAddrConfigTxPortsTable_data *thedata)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2ManAddrConfigTxPortsTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2ManAddrConfigTxPortsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2ManAddrConfigTxPortsTable_id = thedata->lldpV2ManAddrConfigTxPortsTable_id;
		StorageNew->lldpV2ManAddrConfigIfIndex = thedata->lldpV2ManAddrConfigIfIndex;
		StorageNew->lldpV2ManAddrConfigDestAddressIndex = thedata->lldpV2ManAddrConfigDestAddressIndex;
		StorageNew->lldpV2ManAddrConfigLocManAddrSubtype = thedata->lldpV2ManAddrConfigLocManAddrSubtype;
		if (!(StorageNew->lldpV2ManAddrConfigLocManAddr = malloc(thedata->lldpV2ManAddrConfigLocManAddrLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2ManAddrConfigLocManAddr, thedata->lldpV2ManAddrConfigLocManAddr, thedata->lldpV2ManAddrConfigLocManAddrLen);
		StorageNew->lldpV2ManAddrConfigLocManAddrLen = thedata->lldpV2ManAddrConfigLocManAddrLen;
		StorageNew->lldpV2ManAddrConfigLocManAddr[StorageNew->lldpV2ManAddrConfigLocManAddrLen] = 0;
		StorageNew->lldpV2ManAddrConfigTxEnable = thedata->lldpV2ManAddrConfigTxEnable;
		StorageNew->lldpV2ManAddrConfigRowStatus = thedata->lldpV2ManAddrConfigRowStatus;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2ManAddrConfigTxPortsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2ManAddrConfigTxPortsTable_destroy(struct lldpV2ManAddrConfigTxPortsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2ManAddrConfigTxPortsTable_destroy(struct lldpV2ManAddrConfigTxPortsTable_data **thedata)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2ManAddrConfigTxPortsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2ManAddrConfigLocManAddr);
		StorageDel->lldpV2ManAddrConfigLocManAddrLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2ManAddrConfigTxPortsTable_add(struct lldpV2ManAddrConfigTxPortsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2ManAddrConfigTxPortsTable table data set.
 *
 * Adds a table row structure to the lldpV2ManAddrConfigTxPortsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2ManAddrConfigTxPortsTable_add(struct lldpV2ManAddrConfigTxPortsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2ManAddrConfigTxPortsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2ManAddrConfigIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2ManAddrConfigIfIndex, sizeof(thedata->lldpV2ManAddrConfigIfIndex));
		/* lldpV2ManAddrConfigDestAddressIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2ManAddrConfigDestAddressIndex, sizeof(thedata->lldpV2ManAddrConfigDestAddressIndex));
		/* lldpV2ManAddrConfigLocManAddrSubtype */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2ManAddrConfigLocManAddrSubtype, sizeof(thedata->lldpV2ManAddrConfigLocManAddrSubtype));
		/* lldpV2ManAddrConfigLocManAddr */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->lldpV2ManAddrConfigLocManAddr, thedata->lldpV2ManAddrConfigLocManAddrLen);
		header_complex_add_data(&lldpV2ManAddrConfigTxPortsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2ManAddrConfigTxPortsTable_del(struct lldpV2ManAddrConfigTxPortsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2ManAddrConfigTxPortsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2ManAddrConfigTxPortsTable_del(struct lldpV2ManAddrConfigTxPortsTable_data *thedata)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2ManAddrConfigTxPortsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2ManAddrConfigTxPortsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2ManAddrConfigTxPortsTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2ManAddrConfigTxPortsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2ManAddrConfigTxPortsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2ManAddrConfigTxPortsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2ManAddrConfigTxPortsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp = lldpV2ManAddrConfigTxPortsTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2ManAddrConfigTxPortsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2ManAddrConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2ManAddrConfigDestAddressIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2ManAddrConfigLocManAddrSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2ManAddrConfigLocManAddr);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2ManAddrConfigLocManAddr, &StorageTmp->lldpV2ManAddrConfigLocManAddrLen);
	if (StorageTmp->lldpV2ManAddrConfigLocManAddr == NULL) {
		config_perror("invalid specification for lldpV2ManAddrConfigLocManAddr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2ManAddrConfigTxEnable, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2ManAddrConfigRowStatus, &tmpsize);
	lldpV2ManAddrConfigTxPortsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2ManAddrConfigTxPortsTable(): store configuraiton file for lldpV2ManAddrConfigTxPortsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2ManAddrConfigTxPortsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2ManAddrConfigTxPortsTable: storing data...  "));
	refresh_lldpV2ManAddrConfigTxPortsTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2ManAddrConfigTxPortsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2ManAddrConfigTxPortsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2ManAddrConfigTxPortsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2ManAddrConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2ManAddrConfigDestAddressIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2ManAddrConfigLocManAddrSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2ManAddrConfigLocManAddr, &StorageTmp->lldpV2ManAddrConfigLocManAddrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2ManAddrConfigTxEnable, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2ManAddrConfigRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2StatsTxPortTable_data *lldpV2StatsTxPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2StatsTxPortTable table.
 *
 * Creates a new lldpV2StatsTxPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2StatsTxPortTable_data *
lldpV2StatsTxPortTable_create(void)
{
	struct lldpV2StatsTxPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2StatsTxPortTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsTxPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2StatsTxPortFramesTotal = 0;
		StorageNew->lldpV2StatsTxLLDPDULengthErrors = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2StatsTxPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2StatsTxPortTable_data *lldpV2StatsTxPortTable_duplicate(struct lldpV2StatsTxPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2StatsTxPortTable_data *
lldpV2StatsTxPortTable_duplicate(struct lldpV2StatsTxPortTable_data *thedata)
{
	struct lldpV2StatsTxPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2StatsTxPortTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsTxPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2StatsTxPortTable_id = thedata->lldpV2StatsTxPortTable_id;
		StorageNew->lldpV2StatsTxIfIndex = thedata->lldpV2StatsTxIfIndex;
		StorageNew->lldpV2StatsTxDestMACAddress = thedata->lldpV2StatsTxDestMACAddress;
		StorageNew->lldpV2StatsTxPortFramesTotal = thedata->lldpV2StatsTxPortFramesTotal;
		StorageNew->lldpV2StatsTxLLDPDULengthErrors = thedata->lldpV2StatsTxLLDPDULengthErrors;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2StatsTxPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2StatsTxPortTable_destroy(struct lldpV2StatsTxPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2StatsTxPortTable_destroy(struct lldpV2StatsTxPortTable_data **thedata)
{
	struct lldpV2StatsTxPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsTxPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2StatsTxPortTable_add(struct lldpV2StatsTxPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2StatsTxPortTable table data set.
 *
 * Adds a table row structure to the lldpV2StatsTxPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2StatsTxPortTable_add(struct lldpV2StatsTxPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsTxPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2StatsTxIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2StatsTxIfIndex, sizeof(thedata->lldpV2StatsTxIfIndex));
		/* lldpV2StatsTxDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2StatsTxDestMACAddress, sizeof(thedata->lldpV2StatsTxDestMACAddress));
		header_complex_add_data(&lldpV2StatsTxPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2StatsTxPortTable_del(struct lldpV2StatsTxPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2StatsTxPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2StatsTxPortTable_del(struct lldpV2StatsTxPortTable_data *thedata)
{
	struct lldpV2StatsTxPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsTxPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2StatsTxPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2StatsTxPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2StatsTxPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2StatsTxPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2StatsTxPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2StatsTxPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2StatsTxPortTable_data *StorageTmp = lldpV2StatsTxPortTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2StatsTxPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2StatsTxIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2StatsTxDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsTxPortFramesTotal, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsTxLLDPDULengthErrors, &tmpsize);
	lldpV2StatsTxPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2StatsTxPortTable(): store configuraiton file for lldpV2StatsTxPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2StatsTxPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2StatsTxPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2StatsTxPortTable: storing data...  "));
	refresh_lldpV2StatsTxPortTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2StatsTxPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2StatsTxPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2StatsTxPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2StatsTxIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2StatsTxDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsTxPortFramesTotal, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsTxLLDPDULengthErrors, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2StatsRxPortTable_data *lldpV2StatsRxPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2StatsRxPortTable table.
 *
 * Creates a new lldpV2StatsRxPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2StatsRxPortTable_data *
lldpV2StatsRxPortTable_create(void)
{
	struct lldpV2StatsRxPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2StatsRxPortTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsRxPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2StatsRxPortFramesDiscardedTotal = 0;
		StorageNew->lldpV2StatsRxPortFramesErrors = 0;
		StorageNew->lldpV2StatsRxPortFramesTotal = 0;
		StorageNew->lldpV2StatsRxPortTLVsDiscardedTotal = 0;
		StorageNew->lldpV2StatsRxPortTLVsUnrecognizedTotal = 0;
		StorageNew->lldpV2StatsRxPortAgeoutsTotal = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2StatsRxPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2StatsRxPortTable_data *lldpV2StatsRxPortTable_duplicate(struct lldpV2StatsRxPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2StatsRxPortTable_data *
lldpV2StatsRxPortTable_duplicate(struct lldpV2StatsRxPortTable_data *thedata)
{
	struct lldpV2StatsRxPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2StatsRxPortTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsRxPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2StatsRxPortTable_id = thedata->lldpV2StatsRxPortTable_id;
		StorageNew->lldpV2StatsRxDestIfIndex = thedata->lldpV2StatsRxDestIfIndex;
		StorageNew->lldpV2StatsRxDestMACAddress = thedata->lldpV2StatsRxDestMACAddress;
		StorageNew->lldpV2StatsRxPortFramesDiscardedTotal = thedata->lldpV2StatsRxPortFramesDiscardedTotal;
		StorageNew->lldpV2StatsRxPortFramesErrors = thedata->lldpV2StatsRxPortFramesErrors;
		StorageNew->lldpV2StatsRxPortFramesTotal = thedata->lldpV2StatsRxPortFramesTotal;
		StorageNew->lldpV2StatsRxPortTLVsDiscardedTotal = thedata->lldpV2StatsRxPortTLVsDiscardedTotal;
		StorageNew->lldpV2StatsRxPortTLVsUnrecognizedTotal = thedata->lldpV2StatsRxPortTLVsUnrecognizedTotal;
		StorageNew->lldpV2StatsRxPortAgeoutsTotal = thedata->lldpV2StatsRxPortAgeoutsTotal;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2StatsRxPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2StatsRxPortTable_destroy(struct lldpV2StatsRxPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2StatsRxPortTable_destroy(struct lldpV2StatsRxPortTable_data **thedata)
{
	struct lldpV2StatsRxPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsRxPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2StatsRxPortTable_add(struct lldpV2StatsRxPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2StatsRxPortTable table data set.
 *
 * Adds a table row structure to the lldpV2StatsRxPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2StatsRxPortTable_add(struct lldpV2StatsRxPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsRxPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2StatsRxDestIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2StatsRxDestIfIndex, sizeof(thedata->lldpV2StatsRxDestIfIndex));
		/* lldpV2StatsRxDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2StatsRxDestMACAddress, sizeof(thedata->lldpV2StatsRxDestMACAddress));
		header_complex_add_data(&lldpV2StatsRxPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2StatsRxPortTable_del(struct lldpV2StatsRxPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2StatsRxPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2StatsRxPortTable_del(struct lldpV2StatsRxPortTable_data *thedata)
{
	struct lldpV2StatsRxPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2StatsRxPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2StatsRxPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2StatsRxPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2StatsRxPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2StatsRxPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2StatsRxPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2StatsRxPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2StatsRxPortTable_data *StorageTmp = lldpV2StatsRxPortTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2StatsRxPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2StatsRxDestIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2StatsRxDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsRxPortFramesDiscardedTotal, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsRxPortFramesErrors, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsRxPortFramesTotal, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsRxPortTLVsDiscardedTotal, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->lldpV2StatsRxPortTLVsUnrecognizedTotal, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->lldpV2StatsRxPortAgeoutsTotal, &tmpsize);
	lldpV2StatsRxPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2StatsRxPortTable(): store configuraiton file for lldpV2StatsRxPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2StatsRxPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2StatsRxPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2StatsRxPortTable: storing data...  "));
	refresh_lldpV2StatsRxPortTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2StatsRxPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2StatsRxPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2StatsRxPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2StatsRxDestIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2StatsRxDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsRxPortFramesDiscardedTotal, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsRxPortFramesErrors, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsRxPortFramesTotal, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsRxPortTLVsDiscardedTotal, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->lldpV2StatsRxPortTLVsUnrecognizedTotal, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->lldpV2StatsRxPortAgeoutsTotal, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2LocPortTable_data *lldpV2LocPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2LocPortTable table.
 *
 * Creates a new lldpV2LocPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2LocPortTable_data *
lldpV2LocPortTable_create(void)
{
	struct lldpV2LocPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2LocPortTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIdSubtype = 0;
		if ((StorageNew->lldpV2LocPortId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2LocPortIdLen = 0;
		StorageNew->lldpV2LocPortId[StorageNew->lldpV2LocPortIdLen] = 0;
		if ((StorageNew->lldpV2LocPortDesc = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2LocPortDescLen = 0;
		StorageNew->lldpV2LocPortDesc[StorageNew->lldpV2LocPortDescLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2LocPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2LocPortTable_data *lldpV2LocPortTable_duplicate(struct lldpV2LocPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2LocPortTable_data *
lldpV2LocPortTable_duplicate(struct lldpV2LocPortTable_data *thedata)
{
	struct lldpV2LocPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2LocPortTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2LocPortTable_id = thedata->lldpV2LocPortTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2LocPortIdSubtype = thedata->lldpV2LocPortIdSubtype;
		if (!(StorageNew->lldpV2LocPortId = malloc(thedata->lldpV2LocPortIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocPortId, thedata->lldpV2LocPortId, thedata->lldpV2LocPortIdLen);
		StorageNew->lldpV2LocPortIdLen = thedata->lldpV2LocPortIdLen;
		StorageNew->lldpV2LocPortId[StorageNew->lldpV2LocPortIdLen] = 0;
		if (!(StorageNew->lldpV2LocPortDesc = malloc(thedata->lldpV2LocPortDescLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocPortDesc, thedata->lldpV2LocPortDesc, thedata->lldpV2LocPortDescLen);
		StorageNew->lldpV2LocPortDescLen = thedata->lldpV2LocPortDescLen;
		StorageNew->lldpV2LocPortDesc[StorageNew->lldpV2LocPortDescLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2LocPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2LocPortTable_destroy(struct lldpV2LocPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2LocPortTable_destroy(struct lldpV2LocPortTable_data **thedata)
{
	struct lldpV2LocPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2LocPortId);
		StorageDel->lldpV2LocPortIdLen = 0;
		SNMP_FREE(StorageDel->lldpV2LocPortDesc);
		StorageDel->lldpV2LocPortDescLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2LocPortTable_add(struct lldpV2LocPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2LocPortTable table data set.
 *
 * Adds a table row structure to the lldpV2LocPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2LocPortTable_add(struct lldpV2LocPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2LocPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2LocPortTable_del(struct lldpV2LocPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2LocPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2LocPortTable_del(struct lldpV2LocPortTable_data *thedata)
{
	struct lldpV2LocPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2LocPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2LocPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2LocPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2LocPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2LocPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2LocPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2LocPortTable_data *StorageTmp = lldpV2LocPortTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2LocPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIdSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2LocPortId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocPortId, &StorageTmp->lldpV2LocPortIdLen);
	if (StorageTmp->lldpV2LocPortId == NULL) {
		config_perror("invalid specification for lldpV2LocPortId");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2LocPortDesc);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocPortDesc, &StorageTmp->lldpV2LocPortDescLen);
	if (StorageTmp->lldpV2LocPortDesc == NULL) {
		config_perror("invalid specification for lldpV2LocPortDesc");
		return;
	}
	lldpV2LocPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2LocPortTable(): store configuraiton file for lldpV2LocPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2LocPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2LocPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2LocPortTable: storing data...  "));
	refresh_lldpV2LocPortTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2LocPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2LocPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2LocPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIdSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocPortId, &StorageTmp->lldpV2LocPortIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocPortDesc, &StorageTmp->lldpV2LocPortDescLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2LocManAddrTable_data *lldpV2LocManAddrTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2LocManAddrTable table.
 *
 * Creates a new lldpV2LocManAddrTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2LocManAddrTable_data *
lldpV2LocManAddrTable_create(void)
{
	struct lldpV2LocManAddrTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2LocManAddrTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocManAddrTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocManAddrLen = 0;
		StorageNew->lldpV2LocManAddrIfSubtype = 0;
		StorageNew->lldpV2LocManAddrIfId = 0;
		if ((StorageNew->lldpV2LocManAddrOID = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->lldpV2LocManAddrOIDLen = 2;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2LocManAddrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2LocManAddrTable_data *lldpV2LocManAddrTable_duplicate(struct lldpV2LocManAddrTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2LocManAddrTable_data *
lldpV2LocManAddrTable_duplicate(struct lldpV2LocManAddrTable_data *thedata)
{
	struct lldpV2LocManAddrTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2LocManAddrTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocManAddrTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2LocManAddrTable_id = thedata->lldpV2LocManAddrTable_id;
		StorageNew->lldpV2LocManAddrSubtype = thedata->lldpV2LocManAddrSubtype;
		if (!(StorageNew->lldpV2LocManAddr = malloc(thedata->lldpV2LocManAddrLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2LocManAddr, thedata->lldpV2LocManAddr, thedata->lldpV2LocManAddrLen);
		StorageNew->lldpV2LocManAddrLen = thedata->lldpV2LocManAddrLen;
		StorageNew->lldpV2LocManAddr[StorageNew->lldpV2LocManAddrLen] = 0;
		StorageNew->lldpV2LocManAddrLen = thedata->lldpV2LocManAddrLen;
		StorageNew->lldpV2LocManAddrIfSubtype = thedata->lldpV2LocManAddrIfSubtype;
		StorageNew->lldpV2LocManAddrIfId = thedata->lldpV2LocManAddrIfId;
		if (!(StorageNew->lldpV2LocManAddrOID = snmp_duplicate_objid(thedata->lldpV2LocManAddrOID, thedata->lldpV2LocManAddrOIDLen / sizeof(oid))))
			goto destroy;
		StorageNew->lldpV2LocManAddrOIDLen = thedata->lldpV2LocManAddrOIDLen;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2LocManAddrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2LocManAddrTable_destroy(struct lldpV2LocManAddrTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2LocManAddrTable_destroy(struct lldpV2LocManAddrTable_data **thedata)
{
	struct lldpV2LocManAddrTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocManAddrTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2LocManAddr);
		StorageDel->lldpV2LocManAddrLen = 0;
		SNMP_FREE(StorageDel->lldpV2LocManAddrOID);
		StorageDel->lldpV2LocManAddrOIDLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2LocManAddrTable_add(struct lldpV2LocManAddrTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2LocManAddrTable table data set.
 *
 * Adds a table row structure to the lldpV2LocManAddrTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2LocManAddrTable_add(struct lldpV2LocManAddrTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocManAddrTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocManAddrSubtype */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocManAddrSubtype, sizeof(thedata->lldpV2LocManAddrSubtype));
		/* lldpV2LocManAddr */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->lldpV2LocManAddr, thedata->lldpV2LocManAddrLen);
		header_complex_add_data(&lldpV2LocManAddrTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2LocManAddrTable_del(struct lldpV2LocManAddrTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2LocManAddrTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2LocManAddrTable_del(struct lldpV2LocManAddrTable_data *thedata)
{
	struct lldpV2LocManAddrTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2LocManAddrTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2LocManAddrTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2LocManAddrTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2LocManAddrTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2LocManAddrTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2LocManAddrTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2LocManAddrTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2LocManAddrTable_data *StorageTmp = lldpV2LocManAddrTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2LocManAddrTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocManAddrSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2LocManAddr);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2LocManAddr, &StorageTmp->lldpV2LocManAddrLen);
	if (StorageTmp->lldpV2LocManAddr == NULL) {
		config_perror("invalid specification for lldpV2LocManAddr");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2LocManAddrLen, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocManAddrIfSubtype, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2LocManAddrIfId, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2LocManAddrOID);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->lldpV2LocManAddrOID, &StorageTmp->lldpV2LocManAddrOIDLen);
	if (StorageTmp->lldpV2LocManAddrOID == NULL) {
		config_perror("invalid specification for lldpV2LocManAddrOID");
		return;
	}
	lldpV2LocManAddrTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2LocManAddrTable(): store configuraiton file for lldpV2LocManAddrTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2LocManAddrTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2LocManAddrTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2LocManAddrTable: storing data...  "));
	refresh_lldpV2LocManAddrTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2LocManAddrTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2LocManAddrTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2LocManAddrTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocManAddrSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2LocManAddr, &StorageTmp->lldpV2LocManAddrLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2LocManAddrLen, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocManAddrIfSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2LocManAddrIfId, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->lldpV2LocManAddrOID, &StorageTmp->lldpV2LocManAddrOIDLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2RemTable_data *lldpV2RemTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2RemTable table.
 *
 * Creates a new lldpV2RemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2RemTable_data *
lldpV2RemTable_create(void)
{
	struct lldpV2RemTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemChassisIdSubtype = 0;
		if ((StorageNew->lldpV2RemChassisId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemChassisIdLen = 0;
		StorageNew->lldpV2RemChassisId[StorageNew->lldpV2RemChassisIdLen] = 0;
		StorageNew->lldpV2RemPortIdSubtype = 0;
		if ((StorageNew->lldpV2RemPortId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemPortIdLen = 0;
		StorageNew->lldpV2RemPortId[StorageNew->lldpV2RemPortIdLen] = 0;
		if ((StorageNew->lldpV2RemPortDesc = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemPortDescLen = 0;
		StorageNew->lldpV2RemPortDesc[StorageNew->lldpV2RemPortDescLen] = 0;
		if ((StorageNew->lldpV2RemSysName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemSysNameLen = 0;
		StorageNew->lldpV2RemSysName[StorageNew->lldpV2RemSysNameLen] = 0;
		if ((StorageNew->lldpV2RemSysDesc = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemSysDescLen = 0;
		StorageNew->lldpV2RemSysDesc[StorageNew->lldpV2RemSysDescLen] = 0;
		if (memdup((u_char **) &StorageNew->lldpV2RemSysCapSupported, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2RemSysCapSupportedLen = 2;
		if (memdup((u_char **) &StorageNew->lldpV2RemSysCapEnabled, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2RemSysCapEnabledLen = 2;
		StorageNew->lldpV2RemRemoteChanges = 0;
		StorageNew->lldpV2RemTooManyNeighbors = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2RemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2RemTable_data *lldpV2RemTable_duplicate(struct lldpV2RemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2RemTable_data *
lldpV2RemTable_duplicate(struct lldpV2RemTable_data *thedata)
{
	struct lldpV2RemTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2RemTable_id = thedata->lldpV2RemTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2RemChassisIdSubtype = thedata->lldpV2RemChassisIdSubtype;
		if (!(StorageNew->lldpV2RemChassisId = malloc(thedata->lldpV2RemChassisIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemChassisId, thedata->lldpV2RemChassisId, thedata->lldpV2RemChassisIdLen);
		StorageNew->lldpV2RemChassisIdLen = thedata->lldpV2RemChassisIdLen;
		StorageNew->lldpV2RemChassisId[StorageNew->lldpV2RemChassisIdLen] = 0;
		StorageNew->lldpV2RemPortIdSubtype = thedata->lldpV2RemPortIdSubtype;
		if (!(StorageNew->lldpV2RemPortId = malloc(thedata->lldpV2RemPortIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemPortId, thedata->lldpV2RemPortId, thedata->lldpV2RemPortIdLen);
		StorageNew->lldpV2RemPortIdLen = thedata->lldpV2RemPortIdLen;
		StorageNew->lldpV2RemPortId[StorageNew->lldpV2RemPortIdLen] = 0;
		if (!(StorageNew->lldpV2RemPortDesc = malloc(thedata->lldpV2RemPortDescLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemPortDesc, thedata->lldpV2RemPortDesc, thedata->lldpV2RemPortDescLen);
		StorageNew->lldpV2RemPortDescLen = thedata->lldpV2RemPortDescLen;
		StorageNew->lldpV2RemPortDesc[StorageNew->lldpV2RemPortDescLen] = 0;
		if (!(StorageNew->lldpV2RemSysName = malloc(thedata->lldpV2RemSysNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemSysName, thedata->lldpV2RemSysName, thedata->lldpV2RemSysNameLen);
		StorageNew->lldpV2RemSysNameLen = thedata->lldpV2RemSysNameLen;
		StorageNew->lldpV2RemSysName[StorageNew->lldpV2RemSysNameLen] = 0;
		if (!(StorageNew->lldpV2RemSysDesc = malloc(thedata->lldpV2RemSysDescLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemSysDesc, thedata->lldpV2RemSysDesc, thedata->lldpV2RemSysDescLen);
		StorageNew->lldpV2RemSysDescLen = thedata->lldpV2RemSysDescLen;
		StorageNew->lldpV2RemSysDesc[StorageNew->lldpV2RemSysDescLen] = 0;
		if (!(StorageNew->lldpV2RemSysCapSupported = malloc(thedata->lldpV2RemSysCapSupportedLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemSysCapSupported, thedata->lldpV2RemSysCapSupported, thedata->lldpV2RemSysCapSupportedLen);
		StorageNew->lldpV2RemSysCapSupportedLen = thedata->lldpV2RemSysCapSupportedLen;
		StorageNew->lldpV2RemSysCapSupported[StorageNew->lldpV2RemSysCapSupportedLen] = 0;
		if (!(StorageNew->lldpV2RemSysCapEnabled = malloc(thedata->lldpV2RemSysCapEnabledLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemSysCapEnabled, thedata->lldpV2RemSysCapEnabled, thedata->lldpV2RemSysCapEnabledLen);
		StorageNew->lldpV2RemSysCapEnabledLen = thedata->lldpV2RemSysCapEnabledLen;
		StorageNew->lldpV2RemSysCapEnabled[StorageNew->lldpV2RemSysCapEnabledLen] = 0;
		StorageNew->lldpV2RemRemoteChanges = thedata->lldpV2RemRemoteChanges;
		StorageNew->lldpV2RemTooManyNeighbors = thedata->lldpV2RemTooManyNeighbors;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2RemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2RemTable_destroy(struct lldpV2RemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2RemTable_destroy(struct lldpV2RemTable_data **thedata)
{
	struct lldpV2RemTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2RemChassisId);
		StorageDel->lldpV2RemChassisIdLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemPortId);
		StorageDel->lldpV2RemPortIdLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemPortDesc);
		StorageDel->lldpV2RemPortDescLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemSysName);
		StorageDel->lldpV2RemSysNameLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemSysDesc);
		StorageDel->lldpV2RemSysDescLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemSysCapSupported);
		StorageDel->lldpV2RemSysCapSupportedLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemSysCapEnabled);
		StorageDel->lldpV2RemSysCapEnabledLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemTable_add(struct lldpV2RemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2RemTable table data set.
 *
 * Adds a table row structure to the lldpV2RemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2RemTable_add(struct lldpV2RemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2RemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemTable_del(struct lldpV2RemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2RemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2RemTable_del(struct lldpV2RemTable_data *thedata)
{
	struct lldpV2RemTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2RemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2RemTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2RemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2RemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2RemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2RemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2RemTable_data *StorageTmp = lldpV2RemTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2RemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemChassisIdSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemChassisId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemChassisId, &StorageTmp->lldpV2RemChassisIdLen);
	if (StorageTmp->lldpV2RemChassisId == NULL) {
		config_perror("invalid specification for lldpV2RemChassisId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemPortIdSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemPortId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemPortId, &StorageTmp->lldpV2RemPortIdLen);
	if (StorageTmp->lldpV2RemPortId == NULL) {
		config_perror("invalid specification for lldpV2RemPortId");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2RemPortDesc);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemPortDesc, &StorageTmp->lldpV2RemPortDescLen);
	if (StorageTmp->lldpV2RemPortDesc == NULL) {
		config_perror("invalid specification for lldpV2RemPortDesc");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2RemSysName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemSysName, &StorageTmp->lldpV2RemSysNameLen);
	if (StorageTmp->lldpV2RemSysName == NULL) {
		config_perror("invalid specification for lldpV2RemSysName");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2RemSysDesc);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemSysDesc, &StorageTmp->lldpV2RemSysDescLen);
	if (StorageTmp->lldpV2RemSysDesc == NULL) {
		config_perror("invalid specification for lldpV2RemSysDesc");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2RemSysCapSupported);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemSysCapSupported, &StorageTmp->lldpV2RemSysCapSupportedLen);
	if (StorageTmp->lldpV2RemSysCapSupported == NULL) {
		config_perror("invalid specification for lldpV2RemSysCapSupported");
		return;
	}
	SNMP_FREE(StorageTmp->lldpV2RemSysCapEnabled);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemSysCapEnabled, &StorageTmp->lldpV2RemSysCapEnabledLen);
	if (StorageTmp->lldpV2RemSysCapEnabled == NULL) {
		config_perror("invalid specification for lldpV2RemSysCapEnabled");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemRemoteChanges, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemTooManyNeighbors, &tmpsize);
	lldpV2RemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2RemTable(): store configuraiton file for lldpV2RemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2RemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2RemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2RemTable: storing data...  "));
	refresh_lldpV2RemTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2RemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2RemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2RemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemChassisIdSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemChassisId, &StorageTmp->lldpV2RemChassisIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemPortIdSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemPortId, &StorageTmp->lldpV2RemPortIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemPortDesc, &StorageTmp->lldpV2RemPortDescLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemSysName, &StorageTmp->lldpV2RemSysNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemSysDesc, &StorageTmp->lldpV2RemSysDescLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemSysCapSupported, &StorageTmp->lldpV2RemSysCapSupportedLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemSysCapEnabled, &StorageTmp->lldpV2RemSysCapEnabledLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemRemoteChanges, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemTooManyNeighbors, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2RemManAddrTable_data *lldpV2RemManAddrTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2RemManAddrTable table.
 *
 * Creates a new lldpV2RemManAddrTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2RemManAddrTable_data *
lldpV2RemManAddrTable_create(void)
{
	struct lldpV2RemManAddrTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemManAddrTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemManAddrTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		StorageNew->lldpV2RemManAddrIfSubtype = 0;
		StorageNew->lldpV2RemManAddrIfId = 0;
		if ((StorageNew->lldpV2RemManAddrOID = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemManAddrOIDLen = 2;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2RemManAddrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2RemManAddrTable_data *lldpV2RemManAddrTable_duplicate(struct lldpV2RemManAddrTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2RemManAddrTable_data *
lldpV2RemManAddrTable_duplicate(struct lldpV2RemManAddrTable_data *thedata)
{
	struct lldpV2RemManAddrTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemManAddrTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemManAddrTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2RemManAddrTable_id = thedata->lldpV2RemManAddrTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2RemManAddrSubtype = thedata->lldpV2RemManAddrSubtype;
		if (!(StorageNew->lldpV2RemManAddr = malloc(thedata->lldpV2RemManAddrLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemManAddr, thedata->lldpV2RemManAddr, thedata->lldpV2RemManAddrLen);
		StorageNew->lldpV2RemManAddrLen = thedata->lldpV2RemManAddrLen;
		StorageNew->lldpV2RemManAddr[StorageNew->lldpV2RemManAddrLen] = 0;
		StorageNew->lldpV2RemManAddrIfSubtype = thedata->lldpV2RemManAddrIfSubtype;
		StorageNew->lldpV2RemManAddrIfId = thedata->lldpV2RemManAddrIfId;
		if (!(StorageNew->lldpV2RemManAddrOID = snmp_duplicate_objid(thedata->lldpV2RemManAddrOID, thedata->lldpV2RemManAddrOIDLen / sizeof(oid))))
			goto destroy;
		StorageNew->lldpV2RemManAddrOIDLen = thedata->lldpV2RemManAddrOIDLen;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2RemManAddrTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2RemManAddrTable_destroy(struct lldpV2RemManAddrTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2RemManAddrTable_destroy(struct lldpV2RemManAddrTable_data **thedata)
{
	struct lldpV2RemManAddrTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemManAddrTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2RemManAddr);
		StorageDel->lldpV2RemManAddrLen = 0;
		SNMP_FREE(StorageDel->lldpV2RemManAddrOID);
		StorageDel->lldpV2RemManAddrOIDLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemManAddrTable_add(struct lldpV2RemManAddrTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2RemManAddrTable table data set.
 *
 * Adds a table row structure to the lldpV2RemManAddrTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2RemManAddrTable_add(struct lldpV2RemManAddrTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemManAddrTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		/* lldpV2RemManAddrSubtype */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemManAddrSubtype, sizeof(thedata->lldpV2RemManAddrSubtype));
		/* lldpV2RemManAddr */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->lldpV2RemManAddr, thedata->lldpV2RemManAddrLen);
		header_complex_add_data(&lldpV2RemManAddrTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemManAddrTable_del(struct lldpV2RemManAddrTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2RemManAddrTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2RemManAddrTable_del(struct lldpV2RemManAddrTable_data *thedata)
{
	struct lldpV2RemManAddrTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemManAddrTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2RemManAddrTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2RemManAddrTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2RemManAddrTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2RemManAddrTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2RemManAddrTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2RemManAddrTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2RemManAddrTable_data *StorageTmp = lldpV2RemManAddrTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2RemManAddrTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemManAddrSubtype, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemManAddr);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemManAddr, &StorageTmp->lldpV2RemManAddrLen);
	if (StorageTmp->lldpV2RemManAddr == NULL) {
		config_perror("invalid specification for lldpV2RemManAddr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemManAddrIfSubtype, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemManAddrIfId, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemManAddrOID);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->lldpV2RemManAddrOID, &StorageTmp->lldpV2RemManAddrOIDLen);
	if (StorageTmp->lldpV2RemManAddrOID == NULL) {
		config_perror("invalid specification for lldpV2RemManAddrOID");
		return;
	}
	lldpV2RemManAddrTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2RemManAddrTable(): store configuraiton file for lldpV2RemManAddrTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2RemManAddrTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2RemManAddrTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2RemManAddrTable: storing data...  "));
	refresh_lldpV2RemManAddrTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2RemManAddrTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2RemManAddrTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2RemManAddrTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemManAddrSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemManAddr, &StorageTmp->lldpV2RemManAddrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemManAddrIfSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemManAddrIfId, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->lldpV2RemManAddrOID, &StorageTmp->lldpV2RemManAddrOIDLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2RemUnknownTLVTable_data *lldpV2RemUnknownTLVTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2RemUnknownTLVTable table.
 *
 * Creates a new lldpV2RemUnknownTLVTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2RemUnknownTLVTable_data *
lldpV2RemUnknownTLVTable_create(void)
{
	struct lldpV2RemUnknownTLVTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemUnknownTLVTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemUnknownTLVTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if ((StorageNew->lldpV2RemUnknownTLVInfo = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemUnknownTLVInfoLen = 0;
		StorageNew->lldpV2RemUnknownTLVInfo[StorageNew->lldpV2RemUnknownTLVInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2RemUnknownTLVTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2RemUnknownTLVTable_data *lldpV2RemUnknownTLVTable_duplicate(struct lldpV2RemUnknownTLVTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2RemUnknownTLVTable_data *
lldpV2RemUnknownTLVTable_duplicate(struct lldpV2RemUnknownTLVTable_data *thedata)
{
	struct lldpV2RemUnknownTLVTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemUnknownTLVTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemUnknownTLVTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2RemUnknownTLVTable_id = thedata->lldpV2RemUnknownTLVTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2RemUnknownTLVType = thedata->lldpV2RemUnknownTLVType;
		if (!(StorageNew->lldpV2RemUnknownTLVInfo = malloc(thedata->lldpV2RemUnknownTLVInfoLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemUnknownTLVInfo, thedata->lldpV2RemUnknownTLVInfo, thedata->lldpV2RemUnknownTLVInfoLen);
		StorageNew->lldpV2RemUnknownTLVInfoLen = thedata->lldpV2RemUnknownTLVInfoLen;
		StorageNew->lldpV2RemUnknownTLVInfo[StorageNew->lldpV2RemUnknownTLVInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2RemUnknownTLVTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2RemUnknownTLVTable_destroy(struct lldpV2RemUnknownTLVTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2RemUnknownTLVTable_destroy(struct lldpV2RemUnknownTLVTable_data **thedata)
{
	struct lldpV2RemUnknownTLVTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemUnknownTLVTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2RemUnknownTLVInfo);
		StorageDel->lldpV2RemUnknownTLVInfoLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemUnknownTLVTable_add(struct lldpV2RemUnknownTLVTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2RemUnknownTLVTable table data set.
 *
 * Adds a table row structure to the lldpV2RemUnknownTLVTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2RemUnknownTLVTable_add(struct lldpV2RemUnknownTLVTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemUnknownTLVTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		/* lldpV2RemUnknownTLVType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemUnknownTLVType, sizeof(thedata->lldpV2RemUnknownTLVType));
		header_complex_add_data(&lldpV2RemUnknownTLVTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemUnknownTLVTable_del(struct lldpV2RemUnknownTLVTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2RemUnknownTLVTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2RemUnknownTLVTable_del(struct lldpV2RemUnknownTLVTable_data *thedata)
{
	struct lldpV2RemUnknownTLVTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemUnknownTLVTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2RemUnknownTLVTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2RemUnknownTLVTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2RemUnknownTLVTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2RemUnknownTLVTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2RemUnknownTLVTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2RemUnknownTLVTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2RemUnknownTLVTable_data *StorageTmp = lldpV2RemUnknownTLVTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2RemUnknownTLVTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemUnknownTLVType, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemUnknownTLVInfo);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemUnknownTLVInfo, &StorageTmp->lldpV2RemUnknownTLVInfoLen);
	if (StorageTmp->lldpV2RemUnknownTLVInfo == NULL) {
		config_perror("invalid specification for lldpV2RemUnknownTLVInfo");
		return;
	}
	lldpV2RemUnknownTLVTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2RemUnknownTLVTable(): store configuraiton file for lldpV2RemUnknownTLVTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2RemUnknownTLVTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2RemUnknownTLVTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2RemUnknownTLVTable: storing data...  "));
	refresh_lldpV2RemUnknownTLVTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2RemUnknownTLVTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2RemUnknownTLVTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2RemUnknownTLVTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemUnknownTLVType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemUnknownTLVInfo, &StorageTmp->lldpV2RemUnknownTLVInfoLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2RemOrgDefInfoTable_data *lldpV2RemOrgDefInfoTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2RemOrgDefInfoTable table.
 *
 * Creates a new lldpV2RemOrgDefInfoTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2RemOrgDefInfoTable_data *
lldpV2RemOrgDefInfoTable_create(void)
{
	struct lldpV2RemOrgDefInfoTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemOrgDefInfoTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemOrgDefInfoTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if ((StorageNew->lldpV2RemOrgDefInfo = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2RemOrgDefInfoLen = 0;
		StorageNew->lldpV2RemOrgDefInfo[StorageNew->lldpV2RemOrgDefInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2RemOrgDefInfoTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2RemOrgDefInfoTable_data *lldpV2RemOrgDefInfoTable_duplicate(struct lldpV2RemOrgDefInfoTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2RemOrgDefInfoTable_data *
lldpV2RemOrgDefInfoTable_duplicate(struct lldpV2RemOrgDefInfoTable_data *thedata)
{
	struct lldpV2RemOrgDefInfoTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2RemOrgDefInfoTable_data);

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemOrgDefInfoTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2RemOrgDefInfoTable_id = thedata->lldpV2RemOrgDefInfoTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		if (!(StorageNew->lldpV2RemOrgDefInfoOUI = malloc(thedata->lldpV2RemOrgDefInfoOUILen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemOrgDefInfoOUI, thedata->lldpV2RemOrgDefInfoOUI, thedata->lldpV2RemOrgDefInfoOUILen);
		StorageNew->lldpV2RemOrgDefInfoOUILen = thedata->lldpV2RemOrgDefInfoOUILen;
		StorageNew->lldpV2RemOrgDefInfoOUI[StorageNew->lldpV2RemOrgDefInfoOUILen] = 0;
		StorageNew->lldpV2RemOrgDefInfoSubtype = thedata->lldpV2RemOrgDefInfoSubtype;
		StorageNew->lldpV2RemOrgDefInfoIndex = thedata->lldpV2RemOrgDefInfoIndex;
		if (!(StorageNew->lldpV2RemOrgDefInfo = malloc(thedata->lldpV2RemOrgDefInfoLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2RemOrgDefInfo, thedata->lldpV2RemOrgDefInfo, thedata->lldpV2RemOrgDefInfoLen);
		StorageNew->lldpV2RemOrgDefInfoLen = thedata->lldpV2RemOrgDefInfoLen;
		StorageNew->lldpV2RemOrgDefInfo[StorageNew->lldpV2RemOrgDefInfoLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2RemOrgDefInfoTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2RemOrgDefInfoTable_destroy(struct lldpV2RemOrgDefInfoTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2RemOrgDefInfoTable_destroy(struct lldpV2RemOrgDefInfoTable_data **thedata)
{
	struct lldpV2RemOrgDefInfoTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemOrgDefInfoTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2RemOrgDefInfoOUI);
		StorageDel->lldpV2RemOrgDefInfoOUILen = 0;
		SNMP_FREE(StorageDel->lldpV2RemOrgDefInfo);
		StorageDel->lldpV2RemOrgDefInfoLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemOrgDefInfoTable_add(struct lldpV2RemOrgDefInfoTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2RemOrgDefInfoTable table data set.
 *
 * Adds a table row structure to the lldpV2RemOrgDefInfoTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2RemOrgDefInfoTable_add(struct lldpV2RemOrgDefInfoTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemOrgDefInfoTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		/* lldpV2RemOrgDefInfoOUI */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->lldpV2RemOrgDefInfoOUI, thedata->lldpV2RemOrgDefInfoOUILen);
		/* lldpV2RemOrgDefInfoSubtype */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemOrgDefInfoSubtype, sizeof(thedata->lldpV2RemOrgDefInfoSubtype));
		/* lldpV2RemOrgDefInfoIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemOrgDefInfoIndex, sizeof(thedata->lldpV2RemOrgDefInfoIndex));
		header_complex_add_data(&lldpV2RemOrgDefInfoTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2RemOrgDefInfoTable_del(struct lldpV2RemOrgDefInfoTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2RemOrgDefInfoTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2RemOrgDefInfoTable_del(struct lldpV2RemOrgDefInfoTable_data *thedata)
{
	struct lldpV2RemOrgDefInfoTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2MIB", "lldpV2RemOrgDefInfoTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2RemOrgDefInfoTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2RemOrgDefInfoTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2RemOrgDefInfoTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2RemOrgDefInfoTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2RemOrgDefInfoTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2RemOrgDefInfoTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2RemOrgDefInfoTable_data *StorageTmp = lldpV2RemOrgDefInfoTable_create();

	DEBUGMSGTL(("lldpV2MIB", "parse_lldpV2RemOrgDefInfoTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemOrgDefInfoOUI);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemOrgDefInfoOUI, &StorageTmp->lldpV2RemOrgDefInfoOUILen);
	if (StorageTmp->lldpV2RemOrgDefInfoOUI == NULL) {
		config_perror("invalid specification for lldpV2RemOrgDefInfoOUI");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemOrgDefInfoSubtype, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemOrgDefInfoIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2RemOrgDefInfo);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2RemOrgDefInfo, &StorageTmp->lldpV2RemOrgDefInfoLen);
	if (StorageTmp->lldpV2RemOrgDefInfo == NULL) {
		config_perror("invalid specification for lldpV2RemOrgDefInfo");
		return;
	}
	lldpV2RemOrgDefInfoTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/*
 * store_lldpV2RemOrgDefInfoTable(): store configuraiton file for lldpV2RemOrgDefInfoTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2RemOrgDefInfoTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2RemOrgDefInfoTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2MIB", "store_lldpV2RemOrgDefInfoTable: storing data...  "));
	refresh_lldpV2RemOrgDefInfoTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2RemOrgDefInfoTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2RemOrgDefInfoTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2RemOrgDefInfoTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemOrgDefInfoOUI, &StorageTmp->lldpV2RemOrgDefInfoOUILen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemOrgDefInfoSubtype, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemOrgDefInfoIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2RemOrgDefInfo, &StorageTmp->lldpV2RemOrgDefInfoLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, struct lldpV2PortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, struct lldpV2PortConfigTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, struct lldpV2PortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2PortConfigTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, struct lldpV2PortConfigTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2PortConfigTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, struct lldpV2PortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, struct lldpV2PortConfigTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2PortConfigTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2PortConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2PortConfigTable_data *
refresh_lldpV2PortConfigTable_row(struct lldpV2PortConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2PortConfigTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2PortConfigTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2PortConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2PortConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2PortConfigTable(int force)
{
	if (!force && lldpV2PortConfigTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2PortConfigTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2PortConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2PortConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2PortConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2PortConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2PortConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2PortConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2PortConfigTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2PortConfigTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2PORTCONFIGADMINSTATUS:	/* ReadWrite */
		*write_method = write_lldpV2PortConfigAdminStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2PortConfigAdminStatus);
		rval = (u_char *) &StorageTmp->lldpV2PortConfigAdminStatus;
		break;
	case (u_char) LLDPV2PORTCONFIGNOTIFICATIONENABLE:	/* ReadWrite */
		*write_method = write_lldpV2PortConfigNotificationEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2PortConfigNotificationEnable);
		rval = (u_char *) &StorageTmp->lldpV2PortConfigNotificationEnable;
		break;
	case (u_char) LLDPV2PORTCONFIGTLVSTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2PortConfigTLVsTxEnable;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2PortConfigTLVsTxEnableLen;
		rval = (u_char *) StorageTmp->lldpV2PortConfigTLVsTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, struct lldpV2DestAddressTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, struct lldpV2DestAddressTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, struct lldpV2DestAddressTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2DestAddressTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, struct lldpV2DestAddressTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2DestAddressTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, struct lldpV2DestAddressTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, struct lldpV2DestAddressTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2DestAddressTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2DestAddressTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2DestAddressTable_data *
refresh_lldpV2DestAddressTable_row(struct lldpV2DestAddressTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2DestAddressTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2DestAddressTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2DestAddressTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2DestAddressTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2DestAddressTable(int force)
{
	if (!force && lldpV2DestAddressTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2DestAddressTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2DestAddressTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2DestAddressTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2DestAddressTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2DestAddressTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2DestAddressTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2DestAddressTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2DestAddressTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2DestAddressTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2DESTMACADDRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2DestMacAddressLen;
		rval = (u_char *) StorageTmp->lldpV2DestMacAddress;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, struct lldpV2ManAddrConfigTxPortsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, struct lldpV2ManAddrConfigTxPortsTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, struct lldpV2ManAddrConfigTxPortsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2ManAddrConfigTxPortsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, struct lldpV2ManAddrConfigTxPortsTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2ManAddrConfigTxPortsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, struct lldpV2ManAddrConfigTxPortsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, struct lldpV2ManAddrConfigTxPortsTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2ManAddrConfigTxPortsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2ManAddrConfigTxPortsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2ManAddrConfigTxPortsTable_data *
refresh_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2ManAddrConfigTxPortsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2ManAddrConfigTxPortsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2ManAddrConfigTxPortsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2ManAddrConfigTxPortsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2ManAddrConfigTxPortsTable(int force)
{
	if (!force && lldpV2ManAddrConfigTxPortsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2ManAddrConfigTxPortsTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2ManAddrConfigTxPortsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2ManAddrConfigTxPortsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2ManAddrConfigTxPortsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2ManAddrConfigTxPortsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2ManAddrConfigTxPortsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2ManAddrConfigTxPortsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2MANADDRCONFIGTXENABLE:	/* Create */
		*write_method = write_lldpV2ManAddrConfigTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2ManAddrConfigTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2ManAddrConfigTxEnable;
		break;
	case (u_char) LLDPV2MANADDRCONFIGROWSTATUS:	/* Create */
		*write_method = write_lldpV2ManAddrConfigRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2ManAddrConfigRowStatus);
		rval = (u_char *) &StorageTmp->lldpV2ManAddrConfigRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, struct lldpV2StatsTxPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, struct lldpV2StatsTxPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, struct lldpV2StatsTxPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2StatsTxPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, struct lldpV2StatsTxPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2StatsTxPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, struct lldpV2StatsTxPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, struct lldpV2StatsTxPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2StatsTxPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2StatsTxPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2StatsTxPortTable_data *
refresh_lldpV2StatsTxPortTable_row(struct lldpV2StatsTxPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2StatsTxPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2StatsTxPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2StatsTxPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2StatsTxPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2StatsTxPortTable(int force)
{
	if (!force && lldpV2StatsTxPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2StatsTxPortTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2StatsTxPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2StatsTxPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2StatsTxPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2StatsTxPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2StatsTxPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2StatsTxPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2StatsTxPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2StatsTxPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2STATSTXPORTFRAMESTOTAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsTxPortFramesTotal);
		rval = (u_char *) &StorageTmp->lldpV2StatsTxPortFramesTotal;
		break;
	case (u_char) LLDPV2STATSTXLLDPDULENGTHERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsTxLLDPDULengthErrors);
		rval = (u_char *) &StorageTmp->lldpV2StatsTxLLDPDULengthErrors;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, struct lldpV2StatsRxPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, struct lldpV2StatsRxPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, struct lldpV2StatsRxPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2StatsRxPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, struct lldpV2StatsRxPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2StatsRxPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, struct lldpV2StatsRxPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, struct lldpV2StatsRxPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2StatsRxPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2StatsRxPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2StatsRxPortTable_data *
refresh_lldpV2StatsRxPortTable_row(struct lldpV2StatsRxPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2StatsRxPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2StatsRxPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2StatsRxPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2StatsRxPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2StatsRxPortTable(int force)
{
	if (!force && lldpV2StatsRxPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2StatsRxPortTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2StatsRxPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2StatsRxPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2StatsRxPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2StatsRxPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2StatsRxPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2StatsRxPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2StatsRxPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2StatsRxPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2STATSRXPORTFRAMESDISCARDEDTOTAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRxPortFramesDiscardedTotal);
		rval = (u_char *) &StorageTmp->lldpV2StatsRxPortFramesDiscardedTotal;
		break;
	case (u_char) LLDPV2STATSRXPORTFRAMESERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRxPortFramesErrors);
		rval = (u_char *) &StorageTmp->lldpV2StatsRxPortFramesErrors;
		break;
	case (u_char) LLDPV2STATSRXPORTFRAMESTOTAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRxPortFramesTotal);
		rval = (u_char *) &StorageTmp->lldpV2StatsRxPortFramesTotal;
		break;
	case (u_char) LLDPV2STATSRXPORTTLVSDISCARDEDTOTAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRxPortTLVsDiscardedTotal);
		rval = (u_char *) &StorageTmp->lldpV2StatsRxPortTLVsDiscardedTotal;
		break;
	case (u_char) LLDPV2STATSRXPORTTLVSUNRECOGNIZEDTOTAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRxPortTLVsUnrecognizedTotal);
		rval = (u_char *) &StorageTmp->lldpV2StatsRxPortTLVsUnrecognizedTotal;
		break;
	case (u_char) LLDPV2STATSRXPORTAGEOUTSTOTAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2StatsRxPortAgeoutsTotal);
		rval = (u_char *) &StorageTmp->lldpV2StatsRxPortAgeoutsTotal;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, struct lldpV2LocPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, struct lldpV2LocPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, struct lldpV2LocPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2LocPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, struct lldpV2LocPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2LocPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, struct lldpV2LocPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, struct lldpV2LocPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2LocPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2LocPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2LocPortTable_data *
refresh_lldpV2LocPortTable_row(struct lldpV2LocPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2LocPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2LocPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2LocPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2LocPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2LocPortTable(int force)
{
	if (!force && lldpV2LocPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2LocPortTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2LocPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2LocPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2LocPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2LocPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2LocPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2LocPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2LocPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2LocPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2LOCPORTIDSUBTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2LocPortIdSubtype);
		rval = (u_char *) &StorageTmp->lldpV2LocPortIdSubtype;
		break;
	case (u_char) LLDPV2LOCPORTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocPortIdLen;
		rval = (u_char *) StorageTmp->lldpV2LocPortId;
		break;
	case (u_char) LLDPV2LOCPORTDESC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocPortDescLen;
		rval = (u_char *) StorageTmp->lldpV2LocPortDesc;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, struct lldpV2LocManAddrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, struct lldpV2LocManAddrTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, struct lldpV2LocManAddrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2LocManAddrTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, struct lldpV2LocManAddrTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2LocManAddrTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, struct lldpV2LocManAddrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, struct lldpV2LocManAddrTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2LocManAddrTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2LocManAddrTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2LocManAddrTable_data *
refresh_lldpV2LocManAddrTable_row(struct lldpV2LocManAddrTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2LocManAddrTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2LocManAddrTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2LocManAddrTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2LocManAddrTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2LocManAddrTable(int force)
{
	if (!force && lldpV2LocManAddrTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2LocManAddrTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2LocManAddrTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2LocManAddrTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2LocManAddrTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2LocManAddrTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2LocManAddrTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2LocManAddrTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2LocManAddrTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2LocManAddrTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2LOCMANADDRLEN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2LocManAddrLen);
		rval = (u_char *) &StorageTmp->lldpV2LocManAddrLen;
		break;
	case (u_char) LLDPV2LOCMANADDRIFSUBTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2LocManAddrIfSubtype);
		rval = (u_char *) &StorageTmp->lldpV2LocManAddrIfSubtype;
		break;
	case (u_char) LLDPV2LOCMANADDRIFID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2LocManAddrIfId);
		rval = (u_char *) &StorageTmp->lldpV2LocManAddrIfId;
		break;
	case (u_char) LLDPV2LOCMANADDROID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2LocManAddrOIDLen * sizeof(oid);
		rval = (u_char *) StorageTmp->lldpV2LocManAddrOID;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, struct lldpV2RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, struct lldpV2RemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, struct lldpV2RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2RemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, struct lldpV2RemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2RemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, struct lldpV2RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, struct lldpV2RemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2RemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2RemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2RemTable_data *
refresh_lldpV2RemTable_row(struct lldpV2RemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2RemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2RemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2RemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2RemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2RemTable(int force)
{
	if (!force && lldpV2RemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2RemTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2RemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2RemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2RemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2RemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2RemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2RemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2RemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2RemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2REMCHASSISIDSUBTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2RemChassisIdSubtype);
		rval = (u_char *) &StorageTmp->lldpV2RemChassisIdSubtype;
		break;
	case (u_char) LLDPV2REMCHASSISID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemChassisIdLen;
		rval = (u_char *) StorageTmp->lldpV2RemChassisId;
		break;
	case (u_char) LLDPV2REMPORTIDSUBTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2RemPortIdSubtype);
		rval = (u_char *) &StorageTmp->lldpV2RemPortIdSubtype;
		break;
	case (u_char) LLDPV2REMPORTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemPortIdLen;
		rval = (u_char *) StorageTmp->lldpV2RemPortId;
		break;
	case (u_char) LLDPV2REMPORTDESC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemPortDescLen;
		rval = (u_char *) StorageTmp->lldpV2RemPortDesc;
		break;
	case (u_char) LLDPV2REMSYSNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemSysNameLen;
		rval = (u_char *) StorageTmp->lldpV2RemSysName;
		break;
	case (u_char) LLDPV2REMSYSDESC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemSysDescLen;
		rval = (u_char *) StorageTmp->lldpV2RemSysDesc;
		break;
	case (u_char) LLDPV2REMSYSCAPSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemSysCapSupportedLen;
		rval = (u_char *) StorageTmp->lldpV2RemSysCapSupported;
		break;
	case (u_char) LLDPV2REMSYSCAPENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemSysCapEnabledLen;
		rval = (u_char *) StorageTmp->lldpV2RemSysCapEnabled;
		break;
	case (u_char) LLDPV2REMREMOTECHANGES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2RemRemoteChanges);
		rval = (u_char *) &StorageTmp->lldpV2RemRemoteChanges;
		break;
	case (u_char) LLDPV2REMTOOMANYNEIGHBORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2RemTooManyNeighbors);
		rval = (u_char *) &StorageTmp->lldpV2RemTooManyNeighbors;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, struct lldpV2RemManAddrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, struct lldpV2RemManAddrTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, struct lldpV2RemManAddrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2RemManAddrTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, struct lldpV2RemManAddrTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2RemManAddrTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, struct lldpV2RemManAddrTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, struct lldpV2RemManAddrTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2RemManAddrTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2RemManAddrTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2RemManAddrTable_data *
refresh_lldpV2RemManAddrTable_row(struct lldpV2RemManAddrTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2RemManAddrTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2RemManAddrTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2RemManAddrTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2RemManAddrTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2RemManAddrTable(int force)
{
	if (!force && lldpV2RemManAddrTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2RemManAddrTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2RemManAddrTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2RemManAddrTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2RemManAddrTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2RemManAddrTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2RemManAddrTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2RemManAddrTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2RemManAddrTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2RemManAddrTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2REMMANADDRIFSUBTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2RemManAddrIfSubtype);
		rval = (u_char *) &StorageTmp->lldpV2RemManAddrIfSubtype;
		break;
	case (u_char) LLDPV2REMMANADDRIFID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2RemManAddrIfId);
		rval = (u_char *) &StorageTmp->lldpV2RemManAddrIfId;
		break;
	case (u_char) LLDPV2REMMANADDROID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemManAddrOIDLen * sizeof(oid);
		rval = (u_char *) StorageTmp->lldpV2RemManAddrOID;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, struct lldpV2RemUnknownTLVTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, struct lldpV2RemUnknownTLVTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, struct lldpV2RemUnknownTLVTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2RemUnknownTLVTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, struct lldpV2RemUnknownTLVTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2RemUnknownTLVTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, struct lldpV2RemUnknownTLVTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, struct lldpV2RemUnknownTLVTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2RemUnknownTLVTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2RemUnknownTLVTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2RemUnknownTLVTable_data *
refresh_lldpV2RemUnknownTLVTable_row(struct lldpV2RemUnknownTLVTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2RemUnknownTLVTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2RemUnknownTLVTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2RemUnknownTLVTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2RemUnknownTLVTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2RemUnknownTLVTable(int force)
{
	if (!force && lldpV2RemUnknownTLVTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2RemUnknownTLVTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2RemUnknownTLVTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2RemUnknownTLVTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2RemUnknownTLVTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2RemUnknownTLVTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2RemUnknownTLVTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2RemUnknownTLVTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2RemUnknownTLVTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2RemUnknownTLVTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2REMUNKNOWNTLVINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemUnknownTLVInfoLen;
		rval = (u_char *) StorageTmp->lldpV2RemUnknownTLVInfo;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, struct lldpV2RemOrgDefInfoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, struct lldpV2RemOrgDefInfoTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, struct lldpV2RemOrgDefInfoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2RemOrgDefInfoTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, struct lldpV2RemOrgDefInfoTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2RemOrgDefInfoTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, struct lldpV2RemOrgDefInfoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, struct lldpV2RemOrgDefInfoTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2RemOrgDefInfoTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2RemOrgDefInfoTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2RemOrgDefInfoTable_data *
refresh_lldpV2RemOrgDefInfoTable_row(struct lldpV2RemOrgDefInfoTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2RemOrgDefInfoTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2RemOrgDefInfoTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2RemOrgDefInfoTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2RemOrgDefInfoTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2RemOrgDefInfoTable(int force)
{
	if (!force && lldpV2RemOrgDefInfoTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2RemOrgDefInfoTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2RemOrgDefInfoTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2RemOrgDefInfoTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2MIB above.
 */
u_char *
var_lldpV2RemOrgDefInfoTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2RemOrgDefInfoTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2MIB", "var_lldpV2RemOrgDefInfoTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2RemOrgDefInfoTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2RemOrgDefInfoTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2RemOrgDefInfoTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2REMORGDEFINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2RemOrgDefInfoLen;
		rval = (u_char *) StorageTmp->lldpV2RemOrgDefInfo;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_lldpV2PortConfigAdminStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2PortConfigAdminStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2PortConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2PortConfigAdminStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2PortConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigAdminStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigAdminStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value txAndRx */
		switch (set_value) {
		case LLDPV2PORTCONFIGADMINSTATUS_TXONLY:
		case LLDPV2PORTCONFIGADMINSTATUS_RXONLY:
		case LLDPV2PORTCONFIGADMINSTATUS_TXANDRX:
		case LLDPV2PORTCONFIGADMINSTATUS_DISABLED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigAdminStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			if (StorageTmp->lldpV2PortConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old = lldpV2PortConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2PortConfigTable_rsvs++;
		StorageTmp->lldpV2PortConfigAdminStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2PortConfigTable_tsts == 0)
				if ((ret = check_lldpV2PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2PortConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2PortConfigAdminStatus for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2PortConfigTable_sets == 0)
				if ((ret = update_lldpV2PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2PortConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			lldpV2PortConfigTable_destroy(&StorageTmp->lldpV2PortConfigTable_old);
			StorageTmp->lldpV2PortConfigTable_rsvs = 0;
			StorageTmp->lldpV2PortConfigTable_tsts = 0;
			StorageTmp->lldpV2PortConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2PortConfigTable_sets == 0)
			revert_lldpV2PortConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			break;
		StorageTmp->lldpV2PortConfigAdminStatus = StorageOld->lldpV2PortConfigAdminStatus;
		if (--StorageTmp->lldpV2PortConfigTable_rsvs == 0)
			lldpV2PortConfigTable_destroy(&StorageTmp->lldpV2PortConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2PortConfigNotificationEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2PortConfigNotificationEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2PortConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2PortConfigNotificationEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2PortConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigNotificationEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigNotificationEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigNotificationEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			if (StorageTmp->lldpV2PortConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old = lldpV2PortConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2PortConfigTable_rsvs++;
		StorageTmp->lldpV2PortConfigNotificationEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2PortConfigTable_tsts == 0)
				if ((ret = check_lldpV2PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2PortConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2PortConfigNotificationEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2PortConfigTable_sets == 0)
				if ((ret = update_lldpV2PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2PortConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			lldpV2PortConfigTable_destroy(&StorageTmp->lldpV2PortConfigTable_old);
			StorageTmp->lldpV2PortConfigTable_rsvs = 0;
			StorageTmp->lldpV2PortConfigTable_tsts = 0;
			StorageTmp->lldpV2PortConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2PortConfigTable_sets == 0)
			revert_lldpV2PortConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			break;
		StorageTmp->lldpV2PortConfigNotificationEnable = StorageOld->lldpV2PortConfigNotificationEnable;
		if (--StorageTmp->lldpV2PortConfigTable_rsvs == 0)
			lldpV2PortConfigTable_destroy(&StorageTmp->lldpV2PortConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2PortConfigTLVsTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2PortConfigTLVsTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2PortConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2PortConfigTLVsTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2PortConfigTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigTLVsTxEnable not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigTLVsTxEnable: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2PortConfigTLVsTxEnable: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			if (StorageTmp->lldpV2PortConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old = lldpV2PortConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2PortConfigTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->lldpV2PortConfigTLVsTxEnable);
		StorageTmp->lldpV2PortConfigTLVsTxEnable = string;
		StorageTmp->lldpV2PortConfigTLVsTxEnableLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2PortConfigTable_tsts == 0)
				if ((ret = check_lldpV2PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2PortConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2PortConfigTLVsTxEnable for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2PortConfigTable_sets == 0)
				if ((ret = update_lldpV2PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2PortConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) != NULL) {
			lldpV2PortConfigTable_destroy(&StorageTmp->lldpV2PortConfigTable_old);
			StorageTmp->lldpV2PortConfigTable_rsvs = 0;
			StorageTmp->lldpV2PortConfigTable_tsts = 0;
			StorageTmp->lldpV2PortConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2PortConfigTable_sets == 0)
			revert_lldpV2PortConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2PortConfigTable_old) == NULL)
			break;
		if (StorageOld->lldpV2PortConfigTLVsTxEnable != NULL) {
			SNMP_FREE(StorageTmp->lldpV2PortConfigTLVsTxEnable);
			StorageTmp->lldpV2PortConfigTLVsTxEnable = StorageOld->lldpV2PortConfigTLVsTxEnable;
			StorageTmp->lldpV2PortConfigTLVsTxEnableLen = StorageOld->lldpV2PortConfigTLVsTxEnableLen;
			StorageOld->lldpV2PortConfigTLVsTxEnable = NULL;
			StorageOld->lldpV2PortConfigTLVsTxEnableLen = 0;
		}
		if (--StorageTmp->lldpV2PortConfigTable_rsvs == 0)
			lldpV2PortConfigTable_destroy(&StorageTmp->lldpV2PortConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2ManAddrConfigTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2ManAddrConfigTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2ManAddrConfigTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2ManAddrConfigTxPortsTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->lldpV2ManAddrConfigRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ManAddrConfigTxEnable: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ManAddrConfigTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ManAddrConfigTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ManAddrConfigTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) == NULL)
			if (StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old = lldpV2ManAddrConfigTxPortsTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs++;
		StorageTmp->lldpV2ManAddrConfigTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2ManAddrConfigTxPortsTable_tsts == 0)
				if ((ret = check_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2ManAddrConfigTxPortsTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2ManAddrConfigTxEnable for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2ManAddrConfigTxPortsTable_sets == 0)
				if ((ret = update_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2ManAddrConfigTxPortsTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) != NULL) {
			lldpV2ManAddrConfigTxPortsTable_destroy(&StorageTmp->lldpV2ManAddrConfigTxPortsTable_old);
			StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs = 0;
			StorageTmp->lldpV2ManAddrConfigTxPortsTable_tsts = 0;
			StorageTmp->lldpV2ManAddrConfigTxPortsTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2ManAddrConfigTxPortsTable_sets == 0)
			revert_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) == NULL)
			break;
		StorageTmp->lldpV2ManAddrConfigTxEnable = StorageOld->lldpV2ManAddrConfigTxEnable;
		if (--StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs == 0)
			lldpV2ManAddrConfigTxPortsTable_destroy(&StorageTmp->lldpV2ManAddrConfigTxPortsTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2MessageTxInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2MessageTxInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2MessageTxInterval entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageTxInterval not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageTxInterval: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 30 */
		/* Note: ranges 5..32768 */
		if ((5 > set_value || set_value > 32768)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageTxInterval: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2MessageTxInterval = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2MessageTxInterval for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2MessageTxInterval = StorageOld->lldpV2MessageTxInterval;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2MessageTxHoldMultiplier(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2MessageTxHoldMultiplier(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2MessageTxHoldMultiplier entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageTxHoldMultiplier not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageTxHoldMultiplier: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		/* Note: ranges 2..10 */
		if ((2 > set_value || set_value > 10)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageTxHoldMultiplier: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2MessageTxHoldMultiplier = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2MessageTxHoldMultiplier for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2MessageTxHoldMultiplier = StorageOld->lldpV2MessageTxHoldMultiplier;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2ReinitDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2ReinitDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2ReinitDelay entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ReinitDelay not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ReinitDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2 */
		/* Note: ranges 1..10 */
		if ((1 > set_value || set_value > 10)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ReinitDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2ReinitDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2ReinitDelay for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2ReinitDelay = StorageOld->lldpV2ReinitDelay;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2NotificationInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2NotificationInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2NotificationInterval entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2NotificationInterval not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2NotificationInterval: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 30 */
		/* Note: ranges 5..3600 */
		if ((5 > set_value || set_value > 3600)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2NotificationInterval: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2NotificationInterval = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2NotificationInterval for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2NotificationInterval = StorageOld->lldpV2NotificationInterval;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2TxCreditMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2TxCreditMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2TxCreditMax entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2TxCreditMax not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2TxCreditMax: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 5 */
		/* Note: ranges 1..100 */
		if ((1 > set_value || set_value > 100)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2TxCreditMax: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2TxCreditMax = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2TxCreditMax for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2TxCreditMax = StorageOld->lldpV2TxCreditMax;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2MessageFastTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2MessageFastTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2MessageFastTx entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageFastTx not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageFastTx: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		/* Note: ranges 1..3600 */
		if ((1 > set_value || set_value > 3600)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2MessageFastTx: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2MessageFastTx = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2MessageFastTx for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2MessageFastTx = StorageOld->lldpV2MessageFastTx;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2TxFastInit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_lldpV2TxFastInit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2MIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2TxFastInit entering action=%d...  \n", action));
	if ((StorageTmp = lldpV2MIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2TxFastInit not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2TxFastInit: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		/* Note: ranges 1..8 */
		if ((1 > set_value || set_value > 8)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2TxFastInit: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			if (StorageTmp->lldpV2MIB_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2MIB_old = lldpV2MIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2MIB_rsvs++;
		StorageTmp->lldpV2TxFastInit = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->lldpV2MIB_tsts == 0)
				if ((ret = check_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2TxFastInit for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2MIB_sets == 0)
				if ((ret = update_lldpV2MIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2MIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) != NULL) {
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
			StorageTmp->lldpV2MIB_rsvs = 0;
			StorageTmp->lldpV2MIB_tsts = 0;
			StorageTmp->lldpV2MIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2MIB_tsts == 0)
			revert_lldpV2MIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2MIB_old) == NULL)
			break;
		StorageTmp->lldpV2TxFastInit = StorageOld->lldpV2TxFastInit;
		if (--StorageTmp->lldpV2MIB_rsvs == 0)
			lldpV2MIB_destroy(&StorageTmp->lldpV2MIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_lldpV2ManAddrConfigTxPortsTable_row(struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2ManAddrConfigRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_lldpV2ManAddrConfigRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2ManAddrConfigTxPortsTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct lldpV2ManAddrConfigTxPortsTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("lldpV2MIB", "write_lldpV2ManAddrConfigRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(lldpV2ManAddrConfigTxPortsTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ManAddrConfigRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2ManAddrConfigRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* lldpV2ManAddrConfigIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* lldpV2ManAddrConfigDestAddressIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* lldpV2ManAddrConfigLocManAddrSubtype */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* lldpV2ManAddrConfigLocManAddr */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* lldpV2ManAddrConfigIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* lldpV2ManAddrConfigDestAddressIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigDestAddressIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..4096 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4096)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigDestAddressIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* lldpV2ManAddrConfigLocManAddrSubtype */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigLocManAddrSubtype: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			switch ((long) *vp->val.integer) {
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_OTHER:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_IPV4:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_IPV6:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_NSAP:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_HDLC:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_BBN1822:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_ALL802:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_E163:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_E164:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_F69:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_X121:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_IPX:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_APPLETALK:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_DECNETIV:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_BANYANVINES:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_E164WITHNSAP:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_DNS:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_DISTINGUISHEDNAME:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_ASNUMBER:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_XTPOVERIPV4:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_XTPOVERIPV6:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_XTPNATIVEMODEXTP:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_FIBRECHANNELWWPN:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_FIBRECHANNELWWNN:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_GWID:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_AFI:
			case LLDPV2MANADDRCONFIGLOCMANADDRSUBTYPE_RESERVED:
				break;
			default:
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigLocManAddrSubtype: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* lldpV2ManAddrConfigLocManAddr */
			/* Note: ranges 1..31 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 31))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index lldpV2ManAddrConfigLocManAddr: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = lldpV2ManAddrConfigTxPortsTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->lldpV2ManAddrConfigTxPortsTable_rsvs = 1;
			vp = vars;
			StorageNew->lldpV2ManAddrConfigIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->lldpV2ManAddrConfigDestAddressIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->lldpV2ManAddrConfigLocManAddrSubtype = (long) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->lldpV2ManAddrConfigLocManAddr, vp->val.string, vp->val_len);
			StorageNew->lldpV2ManAddrConfigLocManAddrLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&lldpV2ManAddrConfigTxPortsTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->lldpV2ManAddrConfigRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->lldpV2ManAddrConfigTxPortsTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) == NULL)
				if (StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs == 0)
					if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old = lldpV2ManAddrConfigTxPortsTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->lldpV2ManAddrConfigTxPortsTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				lldpV2ManAddrConfigTxPortsTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_lldpV2ManAddrConfigTxPortsTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->lldpV2ManAddrConfigRowStatus != RS_ACTIVE)
				if ((ret = can_act_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->lldpV2ManAddrConfigRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->lldpV2ManAddrConfigRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->lldpV2ManAddrConfigRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_lldpV2ManAddrConfigTxPortsTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->lldpV2ManAddrConfigRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->lldpV2ManAddrConfigRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_lldpV2ManAddrConfigTxPortsTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->lldpV2ManAddrConfigRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->lldpV2ManAddrConfigRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->lldpV2ManAddrConfigRowStatus = set_value;
			if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) != NULL) {
				lldpV2ManAddrConfigTxPortsTable_destroy(&StorageTmp->lldpV2ManAddrConfigTxPortsTable_old);
				StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs = 0;
				StorageTmp->lldpV2ManAddrConfigTxPortsTable_tsts = 0;
				StorageTmp->lldpV2ManAddrConfigTxPortsTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			lldpV2ManAddrConfigTxPortsTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_lldpV2ManAddrConfigTxPortsTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->lldpV2ManAddrConfigRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->lldpV2ManAddrConfigRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_lldpV2ManAddrConfigTxPortsTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				lldpV2ManAddrConfigTxPortsTable_del(StorageNew);
				lldpV2ManAddrConfigTxPortsTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->lldpV2ManAddrConfigTxPortsTable_old) == NULL)
				break;
			if (--StorageTmp->lldpV2ManAddrConfigTxPortsTable_rsvs == 0)
				lldpV2ManAddrConfigTxPortsTable_destroy(&StorageTmp->lldpV2ManAddrConfigTxPortsTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				lldpV2ManAddrConfigTxPortsTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_lldpV2RemTablesChange_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	DEBUGMSGTL(("lldpV2MIB", "send_lldpV2RemTablesChange_v2trap: sending trap...  "));
	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = lldpV2RemTablesChange_oid;
	trap.val_len = sizeof(lldpV2RemTablesChange_oid);
	trap.index = 0;
	send_v2trap(&trap);
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/**
 * @fn void lldpV2MIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
lldpV2MIB_loop_handler(int sig)
{
	DEBUGMSGTL(("lldpV2MIB", "lldpV2MIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (lldpV2MIBold_signal_handler != NULL)
		(*lldpV2MIBold_signal_handler) (sig);
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
}

/**
 * @fn void lldpV2MIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
lldpV2MIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("lldpV2MIB", "lldpV2MIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("lldpV2MIB", "done.\n"));
	return;
}
