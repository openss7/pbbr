/*****************************************************************************

 @(#) src/snmp/ieee8021pbbmib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021pbbmib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021PbbMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021PbbMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021PbbMib_refresh = 1;
volatile int ieee8021PbbVipTable_refresh = 1;
volatile int ieee8021PbbISidToVipTable_refresh = 1;
volatile int ieee8021PbbPipTable_refresh = 1;
volatile int ieee8021PbbPipPriorityTable_refresh = 1;
volatile int ieee8021PbbPipDecodingTable_refresh = 1;
volatile int ieee8021PbbPipEncodingTable_refresh = 1;
volatile int ieee8021PbbVipToPipMappingTable_refresh = 1;
volatile int ieee8021PbbCBPServiceMappingTable_refresh = 1;
volatile int ieee8021PbbCbpTable_refresh = 1;

/*
 * ieee8021PbbMib_variables_oid: object identifier for ieee8021PbbMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021PbbMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 9 };
oid ieee8021PbbVipTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 9, 1, 1, 2, 1 };
oid ieee8021PbbPipTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 9, 1, 1, 4, 1 };
oid ieee8021PbbVipToPipMappingTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 9, 1, 1, 8, 1 };
oid ieee8021PbbCBPServiceMappingTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 9, 1, 1, 9, 1 };
oid ieee8021PbbCbpTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 9, 1, 1, 10, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021PbbBackboneEdgeBridgeGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 1 };
oid ieee8021PbbVipGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 2 };
oid ieee8021PbbPipGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 3 };
oid ieee8021PbbDefaultPriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 4 };
oid ieee8021PbbPipDecodingGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 5 };
oid ieee8021PbbPipEncodingGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 6 };
oid ieee8021PbbVipToPipMappingGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 7 };
oid ieee8021PbbCBPServiceMappingGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 8 };
oid ieee8021PbbDynamicCbpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 9 };
oid ieee8021PbbVipPbbTeGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 1, 10 };
oid ieee8021PbbCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 2, 1 };
oid ieee8021PbbWIthPbbTeCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 9, 2, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021PbbMib_variables: tree for ieee8021PbbMib
 * This variable defines function callbacks and type return information for the ieee8021PbbMib mib section
 */
struct variable7 ieee8021PbbMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021PBBBACKBONEEDGEBRIDGEADDRESS  1
	{(u_char) IEEE8021PBBBACKBONEEDGEBRIDGEADDRESS, ASN_OCTET_STR, RONLY, var_ieee8021PbbMib, 4, {1, 1, 1, 1}},
#define   IEEE8021PBBBACKBONEEDGEBRIDGENAME  2
	{(u_char) IEEE8021PBBBACKBONEEDGEBRIDGENAME, ASN_OCTET_STR, RWRITE, var_ieee8021PbbMib, 4, {1, 1, 1, 2}},
#define   IEEE8021PBBNUMBEROFICOMPONENTS  3
	{(u_char) IEEE8021PBBNUMBEROFICOMPONENTS, ASN_UNSIGNED, RONLY, var_ieee8021PbbMib, 4, {1, 1, 1, 3}},
#define   IEEE8021PBBNUMBEROFBCOMPONENTS  4
	{(u_char) IEEE8021PBBNUMBEROFBCOMPONENTS, ASN_UNSIGNED, RONLY, var_ieee8021PbbMib, 4, {1, 1, 1, 4}},
#define   IEEE8021PBBNUMBEROFBEBPORTS  5
	{(u_char) IEEE8021PBBNUMBEROFBEBPORTS, ASN_UNSIGNED, RONLY, var_ieee8021PbbMib, 4, {1, 1, 1, 5}},
#define   IEEE8021PBBNEXTAVAILABLEPIPIFINDEX  6
	{(u_char) IEEE8021PBBNEXTAVAILABLEPIPIFINDEX, ASN_INTEGER, RONLY, var_ieee8021PbbMib, 4, {1, 1, 1, 6}},
#define   IEEE8021PBBVIPPIPIFINDEX  7
	{(u_char) IEEE8021PBBVIPPIPIFINDEX, ASN_INTEGER, RONLY, var_ieee8021PbbVipTable, 5, {1, 1, 2, 1, 1}},
#define   IEEE8021PBBVIPISID    8
	{(u_char) IEEE8021PBBVIPISID, ASN_UNSIGNED, RWRITE, var_ieee8021PbbVipTable, 5, {1, 1, 2, 1, 2}},
#define   IEEE8021PBBVIPDEFAULTDSTBMAC  9
	{(u_char) IEEE8021PBBVIPDEFAULTDSTBMAC, ASN_OCTET_STR, RONLY, var_ieee8021PbbVipTable, 5, {1, 1, 2, 1, 3}},
#define   IEEE8021PBBVIPTYPE    10
	{(u_char) IEEE8021PBBVIPTYPE, ASN_OCTET_STR, RWRITE, var_ieee8021PbbVipTable, 5, {1, 1, 2, 1, 4}},
#define   IEEE8021PBBVIPROWSTATUS  11
	{(u_char) IEEE8021PBBVIPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbVipTable, 5, {1, 1, 2, 1, 5}},
#define   IEEE8021PBBVIPENABLECONNECTIONID  12
	{(u_char) IEEE8021PBBVIPENABLECONNECTIONID, ASN_INTEGER, RWRITE, var_ieee8021PbbVipTable, 5, {1, 1, 2, 1, 6}},
#define   IEEE8021PBBISIDTOVIPCOMPONENTID  13
	{(u_char) IEEE8021PBBISIDTOVIPCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021PbbISidToVipTable, 5, {1, 1, 3, 1, 2}},
#define   IEEE8021PBBISIDTOVIPPORT  14
	{(u_char) IEEE8021PBBISIDTOVIPPORT, ASN_UNSIGNED, RONLY, var_ieee8021PbbISidToVipTable, 5, {1, 1, 3, 1, 3}},
#define   IEEE8021PBBPIPBMACADDRESS  15
	{(u_char) IEEE8021PBBPIPBMACADDRESS, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 2}},
#define   IEEE8021PBBPIPNAME    16
	{(u_char) IEEE8021PBBPIPNAME, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 3}},
#define   IEEE8021PBBPIPCOMPONENTID  17
	{(u_char) IEEE8021PBBPIPCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 4}},
#define   IEEE8021PBBPIPVIPMAP  18
	{(u_char) IEEE8021PBBPIPVIPMAP, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 5}},
#define   IEEE8021PBBPIPVIPMAP1  19
	{(u_char) IEEE8021PBBPIPVIPMAP1, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 6}},
#define   IEEE8021PBBPIPVIPMAP2  20
	{(u_char) IEEE8021PBBPIPVIPMAP2, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 7}},
#define   IEEE8021PBBPIPVIPMAP3  21
	{(u_char) IEEE8021PBBPIPVIPMAP3, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 8}},
#define   IEEE8021PBBPIPVIPMAP4  22
	{(u_char) IEEE8021PBBPIPVIPMAP4, ASN_OCTET_STR, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 9}},
#define   IEEE8021PBBPIPROWSTATUS  23
	{(u_char) IEEE8021PBBPIPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbPipTable, 5, {1, 1, 4, 1, 10}},
#define   IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION  24
	{(u_char) IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION, ASN_INTEGER, RWRITE, var_ieee8021PbbPipPriorityTable, 5, {1, 1, 5, 1, 1}},
#define   IEEE8021PBBPIPUSEDEI  25
	{(u_char) IEEE8021PBBPIPUSEDEI, ASN_INTEGER, RWRITE, var_ieee8021PbbPipPriorityTable, 5, {1, 1, 5, 1, 2}},
#define   IEEE8021PBBPIPREQUIREDROPENCODING  26
	{(u_char) IEEE8021PBBPIPREQUIREDROPENCODING, ASN_INTEGER, RWRITE, var_ieee8021PbbPipPriorityTable, 5, {1, 1, 5, 1, 3}},
#define   IEEE8021PBBPIPDECODINGPRIORITY  27
	{(u_char) IEEE8021PBBPIPDECODINGPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021PbbPipDecodingTable, 5, {1, 1, 6, 1, 3}},
#define   IEEE8021PBBPIPDECODINGDROPELIGIBLE  28
	{(u_char) IEEE8021PBBPIPDECODINGDROPELIGIBLE, ASN_INTEGER, RWRITE, var_ieee8021PbbPipDecodingTable, 5, {1, 1, 6, 1, 4}},
#define   IEEE8021PBBPIPENCODINGPRIORITY  29
	{(u_char) IEEE8021PBBPIPENCODINGPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021PbbPipEncodingTable, 5, {1, 1, 7, 1, 4}},
#define   IEEE8021PBBVIPTOPIPMAPPINGPIPIFINDEX  30
	{(u_char) IEEE8021PBBVIPTOPIPMAPPINGPIPIFINDEX, ASN_INTEGER, RWRITE, var_ieee8021PbbVipToPipMappingTable, 5, {1, 1, 8, 1, 1}},
#define   IEEE8021PBBVIPTOPIPMAPPINGSTORAGETYPE  31
	{(u_char) IEEE8021PBBVIPTOPIPMAPPINGSTORAGETYPE, ASN_INTEGER, RWRITE, var_ieee8021PbbVipToPipMappingTable, 5, {1, 1, 8, 1, 2}},
#define   IEEE8021PBBVIPTOPIPMAPPINGROWSTATUS  32
	{(u_char) IEEE8021PBBVIPTOPIPMAPPINGROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbVipToPipMappingTable, 5, {1, 1, 8, 1, 3}},
#define   IEEE8021PBBCBPSERVICEMAPPINGBVID  33
	{(u_char) IEEE8021PBBCBPSERVICEMAPPINGBVID, ASN_INTEGER, RWRITE, var_ieee8021PbbCBPServiceMappingTable, 5, {1, 1, 9, 1, 2}},
#define   IEEE8021PBBCBPSERVICEMAPPINGDEFAULTBACKBONEDEST  34
	{(u_char) IEEE8021PBBCBPSERVICEMAPPINGDEFAULTBACKBONEDEST, ASN_OCTET_STR, RWRITE, var_ieee8021PbbCBPServiceMappingTable, 5, {1, 1, 9, 1, 3}},
#define   IEEE8021PBBCBPSERVICEMAPPINGTYPE  35
	{(u_char) IEEE8021PBBCBPSERVICEMAPPINGTYPE, ASN_OCTET_STR, RWRITE, var_ieee8021PbbCBPServiceMappingTable, 5, {1, 1, 9, 1, 4}},
#define   IEEE8021PBBCBPSERVICEMAPPINGLOCALSID  36
	{(u_char) IEEE8021PBBCBPSERVICEMAPPINGLOCALSID, ASN_UNSIGNED, RWRITE, var_ieee8021PbbCBPServiceMappingTable, 5, {1, 1, 9, 1, 5}},
#define   IEEE8021PBBCBPSERVICEMAPPINGROWSTATUS  37
	{(u_char) IEEE8021PBBCBPSERVICEMAPPINGROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbCBPServiceMappingTable, 5, {1, 1, 9, 1, 6}},
#define   IEEE8021PBBCBPROWSTATUS  38
	{(u_char) IEEE8021PBBCBPROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021PbbCbpTable, 5, {1, 1, 10, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct ieee8021PbbMib_data *ieee8021PbbMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021PbbVipTableStorage = NULL;
struct header_complex_index *ieee8021PbbISidToVipTableStorage = NULL;
struct header_complex_index *ieee8021PbbPipTableStorage = NULL;
struct header_complex_index *ieee8021PbbPipPriorityTableStorage = NULL;
struct header_complex_index *ieee8021PbbPipDecodingTableStorage = NULL;
struct header_complex_index *ieee8021PbbPipEncodingTableStorage = NULL;
struct header_complex_index *ieee8021PbbVipToPipMappingTableStorage = NULL;
struct header_complex_index *ieee8021PbbCBPServiceMappingTableStorage = NULL;
struct header_complex_index *ieee8021PbbCbpTableStorage = NULL;

void (*ieee8021PbbMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021PbbMib_loop_handler(int);
void ieee8021PbbMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021PbbMib(void)
 * @brief ieee8021PbbMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021PbbMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021PbbMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021PbbMib", "init_ieee8021PbbMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021PbbMib", ieee8021PbbMib_variables, variable7, ieee8021PbbMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021PbbMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021PbbMib", parse_ieee8021PbbMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbVipTable", parse_ieee8021PbbVipTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbISidToVipTable", parse_ieee8021PbbISidToVipTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbPipTable", parse_ieee8021PbbPipTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbPipPriorityTable", parse_ieee8021PbbPipPriorityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbPipDecodingTable", parse_ieee8021PbbPipDecodingTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbPipEncodingTable", parse_ieee8021PbbPipEncodingTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbVipToPipMappingTable", parse_ieee8021PbbVipToPipMappingTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbCBPServiceMappingTable", parse_ieee8021PbbCBPServiceMappingTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021PbbCbpTable", parse_ieee8021PbbCbpTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbVipTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbISidToVipTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbPipTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbPipPriorityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbPipDecodingTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbPipEncodingTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbVipToPipMappingTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbCBPServiceMappingTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021PbbCbpTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021PbbMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021PbbMib_fd_handler, (void *) 1);
	}
	ieee8021PbbMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021PbbMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021PbbMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021PbbMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021PbbMib(void)
{
	DEBUGMSGTL(("ieee8021PbbMib", "deinit_ieee8021PbbMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021PbbMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021PbbMib_variables_oid, sizeof(ieee8021PbbMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021PbbMib");
	snmpd_unregister_config_handler("ieee8021PbbVipTable");
	snmpd_unregister_config_handler("ieee8021PbbISidToVipTable");
	snmpd_unregister_config_handler("ieee8021PbbPipTable");
	snmpd_unregister_config_handler("ieee8021PbbPipPriorityTable");
	snmpd_unregister_config_handler("ieee8021PbbPipDecodingTable");
	snmpd_unregister_config_handler("ieee8021PbbPipEncodingTable");
	snmpd_unregister_config_handler("ieee8021PbbVipToPipMappingTable");
	snmpd_unregister_config_handler("ieee8021PbbCBPServiceMappingTable");
	snmpd_unregister_config_handler("ieee8021PbbCbpTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

int
term_ieee8021PbbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021PbbMib", "term_ieee8021PbbMib: terminating...  "));
	deinit_ieee8021PbbMib();
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021PbbMib_data *ieee8021PbbMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021PbbMib.
 *
 * Creates a new ieee8021PbbMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021PbbMib.
 */
struct ieee8021PbbMib_data *
ieee8021PbbMib_create(void)
{
	struct ieee8021PbbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbMib_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		if ((StorageNew->ieee8021PbbBackboneEdgeBridgeAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbBackboneEdgeBridgeAddressLen = 0;
		StorageNew->ieee8021PbbBackboneEdgeBridgeAddress[StorageNew->ieee8021PbbBackboneEdgeBridgeAddressLen] = 0;
		if ((StorageNew->ieee8021PbbBackboneEdgeBridgeName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbBackboneEdgeBridgeNameLen = 0;
		StorageNew->ieee8021PbbBackboneEdgeBridgeName[StorageNew->ieee8021PbbBackboneEdgeBridgeNameLen] = 0;
		StorageNew->ieee8021PbbNumberOfIComponents = 0;
		StorageNew->ieee8021PbbNumberOfBComponents = 0;
		StorageNew->ieee8021PbbNumberOfBebPorts = 0;
		StorageNew->ieee8021PbbNextAvailablePipIfIndex = 0;

	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbMib_data *ieee8021PbbMib_duplicate(struct ieee8021PbbMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021PbbMib_data *
ieee8021PbbMib_duplicate(struct ieee8021PbbMib_data *thedata)
{
	struct ieee8021PbbMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbMib_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		if (!(StorageNew->ieee8021PbbBackboneEdgeBridgeAddress = malloc(thedata->ieee8021PbbBackboneEdgeBridgeAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbBackboneEdgeBridgeAddress, thedata->ieee8021PbbBackboneEdgeBridgeAddress, thedata->ieee8021PbbBackboneEdgeBridgeAddressLen);
		StorageNew->ieee8021PbbBackboneEdgeBridgeAddressLen = thedata->ieee8021PbbBackboneEdgeBridgeAddressLen;
		StorageNew->ieee8021PbbBackboneEdgeBridgeAddress[StorageNew->ieee8021PbbBackboneEdgeBridgeAddressLen] = 0;
		if (!(StorageNew->ieee8021PbbBackboneEdgeBridgeName = malloc(thedata->ieee8021PbbBackboneEdgeBridgeNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbBackboneEdgeBridgeName, thedata->ieee8021PbbBackboneEdgeBridgeName, thedata->ieee8021PbbBackboneEdgeBridgeNameLen);
		StorageNew->ieee8021PbbBackboneEdgeBridgeNameLen = thedata->ieee8021PbbBackboneEdgeBridgeNameLen;
		StorageNew->ieee8021PbbBackboneEdgeBridgeName[StorageNew->ieee8021PbbBackboneEdgeBridgeNameLen] = 0;
		StorageNew->ieee8021PbbNumberOfIComponents = thedata->ieee8021PbbNumberOfIComponents;
		StorageNew->ieee8021PbbNumberOfBComponents = thedata->ieee8021PbbNumberOfBComponents;
		StorageNew->ieee8021PbbNumberOfBebPorts = thedata->ieee8021PbbNumberOfBebPorts;
		StorageNew->ieee8021PbbNextAvailablePipIfIndex = thedata->ieee8021PbbNextAvailablePipIfIndex;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbMib_destroy(struct ieee8021PbbMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021PbbMib.
 * @brief delete a scalars structure from ieee8021PbbMib.
 *
 * Frees scalars that were previously removed from ieee8021PbbMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbMib_destroy(struct ieee8021PbbMib_data **thedata)
{
	struct ieee8021PbbMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021PbbBackboneEdgeBridgeAddress);
		StorageDel->ieee8021PbbBackboneEdgeBridgeAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbBackboneEdgeBridgeName);
		StorageDel->ieee8021PbbBackboneEdgeBridgeNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbMib_add(struct ieee8021PbbMib_data *thedata)
 * @param thedata the structure representing ieee8021PbbMib scalars.
 * @brief adds node to the ieee8021PbbMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021PbbMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021PbbMib_add(struct ieee8021PbbMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbMib_add: adding data...  "));
	if (thedata)
		ieee8021PbbMibStorage = thedata;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021PbbMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbMib_data *StorageTmp = ieee8021PbbMib_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	SNMP_FREE(StorageTmp->ieee8021PbbBackboneEdgeBridgeAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbBackboneEdgeBridgeAddress, &StorageTmp->ieee8021PbbBackboneEdgeBridgeAddressLen);
	if (StorageTmp->ieee8021PbbBackboneEdgeBridgeAddress == NULL) {
		config_perror("invalid specification for ieee8021PbbBackboneEdgeBridgeAddress");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbBackboneEdgeBridgeName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbBackboneEdgeBridgeName, &StorageTmp->ieee8021PbbBackboneEdgeBridgeNameLen);
	if (StorageTmp->ieee8021PbbBackboneEdgeBridgeName == NULL) {
		config_perror("invalid specification for ieee8021PbbBackboneEdgeBridgeName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbNumberOfIComponents, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbNumberOfBComponents, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbNumberOfBebPorts, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbNextAvailablePipIfIndex, &tmpsize);
	ieee8021PbbMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbMib: storing data...  "));
	refresh_ieee8021PbbMib(1);
	if ((StorageTmp = ieee8021PbbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021PbbMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021PbbMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbBackboneEdgeBridgeAddress, &StorageTmp->ieee8021PbbBackboneEdgeBridgeAddressLen);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbBackboneEdgeBridgeName, &StorageTmp->ieee8021PbbBackboneEdgeBridgeNameLen);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbNumberOfIComponents, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbNumberOfBComponents, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbNumberOfBebPorts, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbNextAvailablePipIfIndex, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021PbbMib(struct ieee8021PbbMib_data *StorageTmp, struct ieee8021PbbMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021PbbMib(struct ieee8021PbbMib_data *StorageTmp, struct ieee8021PbbMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbMib(struct ieee8021PbbMib_data *StorageTmp, struct ieee8021PbbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021PbbMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbMib(struct ieee8021PbbMib_data *StorageTmp, struct ieee8021PbbMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021PbbMib(struct 
 * @fn void revert_ieee8021PbbMib(struct ieee8021PbbMib_data *StorageTmp, struct ieee8021PbbMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021PbbMib(struct ieee8021PbbMib_data *StorageTmp, struct ieee8021PbbMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021PbbMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021PbbMib(int force)
{
	if (ieee8021PbbMibStorage == NULL) {
		struct ieee8021PbbMib_data *StorageNew;

		if ((StorageNew = ieee8021PbbMib_create()) == NULL)
			return;
		ieee8021PbbMibStorage = StorageNew;
		ieee8021PbbMib_refresh = 1;
	}
	if (!force && ieee8021PbbMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021PbbMib", "refresh_ieee8021PbbMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021PbbMib_refresh = 0;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021PbbMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021PbbMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021PbbMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021PbbMib(0);
	if ((StorageTmp = ieee8021PbbMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021PbbMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBBACKBONEEDGEBRIDGEADDRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbBackboneEdgeBridgeAddressLen;
		rval = (u_char *) StorageTmp->ieee8021PbbBackboneEdgeBridgeAddress;
		break;
	case (u_char) IEEE8021PBBBACKBONEEDGEBRIDGENAME:	/* ReadWrite */
		*write_method = write_ieee8021PbbBackboneEdgeBridgeName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbBackboneEdgeBridgeNameLen;
		rval = (u_char *) StorageTmp->ieee8021PbbBackboneEdgeBridgeName;
		break;
	case (u_char) IEEE8021PBBNUMBEROFICOMPONENTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbNumberOfIComponents);
		rval = (u_char *) &StorageTmp->ieee8021PbbNumberOfIComponents;
		break;
	case (u_char) IEEE8021PBBNUMBEROFBCOMPONENTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbNumberOfBComponents);
		rval = (u_char *) &StorageTmp->ieee8021PbbNumberOfBComponents;
		break;
	case (u_char) IEEE8021PBBNUMBEROFBEBPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbNumberOfBebPorts);
		rval = (u_char *) &StorageTmp->ieee8021PbbNumberOfBebPorts;
		break;
	case (u_char) IEEE8021PBBNEXTAVAILABLEPIPIFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbNextAvailablePipIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021PbbNextAvailablePipIfIndex;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021PbbMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021PbbMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021PbbVipTable_data *ieee8021PbbVipTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbVipTable table.
 *
 * Creates a new ieee8021PbbVipTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbVipTable_data *
ieee8021PbbVipTable_create(void)
{
	struct ieee8021PbbVipTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbVipTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbbVipPipIfIndex = 0;
		StorageNew->ieee8021PbbVipISid = 1;
		/* StorageNew->ieee8021PbbVipDefaultDstBMAC = (uint8_t *) strdup(133127208961); *//* DEFVAL 133127208961 */
		/* StorageNew->ieee8021PbbVipDefaultDstBMACLen = strlen(133127208961); *//* DEFVAL 133127208961 */
		if (memdup((u_char **) &StorageNew->ieee8021PbbVipType, (u_char *) "\xC0", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021PbbVipTypeLen = 1;
		StorageNew->ieee8021PbbVipRowStatus = 0;
		StorageNew->ieee8021PbbVipEnableConnectionId = TV_TRUE;
		StorageNew->ieee8021PbbVipRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbVipTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbVipTable_data *ieee8021PbbVipTable_duplicate(struct ieee8021PbbVipTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbVipTable_data *
ieee8021PbbVipTable_duplicate(struct ieee8021PbbVipTable_data *thedata)
{
	struct ieee8021PbbVipTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbVipTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbVipTable_id = thedata->ieee8021PbbVipTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbbVipPipIfIndex = thedata->ieee8021PbbVipPipIfIndex;
		StorageNew->ieee8021PbbVipISid = thedata->ieee8021PbbVipISid;
		if (!(StorageNew->ieee8021PbbVipDefaultDstBMAC = malloc(thedata->ieee8021PbbVipDefaultDstBMACLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbVipDefaultDstBMAC, thedata->ieee8021PbbVipDefaultDstBMAC, thedata->ieee8021PbbVipDefaultDstBMACLen);
		StorageNew->ieee8021PbbVipDefaultDstBMACLen = thedata->ieee8021PbbVipDefaultDstBMACLen;
		StorageNew->ieee8021PbbVipDefaultDstBMAC[StorageNew->ieee8021PbbVipDefaultDstBMACLen] = 0;
		if (!(StorageNew->ieee8021PbbVipType = malloc(thedata->ieee8021PbbVipTypeLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbVipType, thedata->ieee8021PbbVipType, thedata->ieee8021PbbVipTypeLen);
		StorageNew->ieee8021PbbVipTypeLen = thedata->ieee8021PbbVipTypeLen;
		StorageNew->ieee8021PbbVipType[StorageNew->ieee8021PbbVipTypeLen] = 0;
		StorageNew->ieee8021PbbVipRowStatus = thedata->ieee8021PbbVipRowStatus;
		StorageNew->ieee8021PbbVipEnableConnectionId = thedata->ieee8021PbbVipEnableConnectionId;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbVipTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbVipTable_destroy(struct ieee8021PbbVipTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbVipTable_destroy(struct ieee8021PbbVipTable_data **thedata)
{
	struct ieee8021PbbVipTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021PbbVipDefaultDstBMAC);
		StorageDel->ieee8021PbbVipDefaultDstBMACLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbVipType);
		StorageDel->ieee8021PbbVipTypeLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbVipTable_add(struct ieee8021PbbVipTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbVipTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbVipTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbVipTable_add(struct ieee8021PbbVipTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbbVipTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbVipTable_del(struct ieee8021PbbVipTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbVipTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbVipTable_del(struct ieee8021PbbVipTable_data *thedata)
{
	struct ieee8021PbbVipTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbVipTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbVipTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbVipTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbVipTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbVipTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbVipTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbVipTable_data *StorageTmp = ieee8021PbbVipTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbVipTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbVipPipIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbVipISid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021PbbVipDefaultDstBMAC);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbVipDefaultDstBMAC, &StorageTmp->ieee8021PbbVipDefaultDstBMACLen);
	if (StorageTmp->ieee8021PbbVipDefaultDstBMAC == NULL) {
		config_perror("invalid specification for ieee8021PbbVipDefaultDstBMAC");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbVipType);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbVipType, &StorageTmp->ieee8021PbbVipTypeLen);
	if (StorageTmp->ieee8021PbbVipType == NULL) {
		config_perror("invalid specification for ieee8021PbbVipType");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbVipRowStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbVipEnableConnectionId, &tmpsize);
	ieee8021PbbVipTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbVipTable(): store configuraiton file for ieee8021PbbVipTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbVipTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbVipTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbVipTable: storing data...  "));
	refresh_ieee8021PbbVipTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbVipTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbVipTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbVipTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbVipPipIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbVipISid, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbVipDefaultDstBMAC, &StorageTmp->ieee8021PbbVipDefaultDstBMACLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbVipType, &StorageTmp->ieee8021PbbVipTypeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbVipRowStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbVipEnableConnectionId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbISidToVipTable_data *ieee8021PbbISidToVipTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbISidToVipTable table.
 *
 * Creates a new ieee8021PbbISidToVipTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbISidToVipTable_data *
ieee8021PbbISidToVipTable_create(void)
{
	struct ieee8021PbbISidToVipTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbISidToVipTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbISidToVipTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbISidToVipComponentId = 0;
		StorageNew->ieee8021PbbISidToVipPort = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbISidToVipTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbISidToVipTable_data *ieee8021PbbISidToVipTable_duplicate(struct ieee8021PbbISidToVipTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbISidToVipTable_data *
ieee8021PbbISidToVipTable_duplicate(struct ieee8021PbbISidToVipTable_data *thedata)
{
	struct ieee8021PbbISidToVipTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbISidToVipTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbISidToVipTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbISidToVipTable_id = thedata->ieee8021PbbISidToVipTable_id;
		StorageNew->ieee8021PbbISidToVipISid = thedata->ieee8021PbbISidToVipISid;
		StorageNew->ieee8021PbbISidToVipComponentId = thedata->ieee8021PbbISidToVipComponentId;
		StorageNew->ieee8021PbbISidToVipPort = thedata->ieee8021PbbISidToVipPort;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbISidToVipTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbISidToVipTable_destroy(struct ieee8021PbbISidToVipTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbISidToVipTable_destroy(struct ieee8021PbbISidToVipTable_data **thedata)
{
	struct ieee8021PbbISidToVipTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbISidToVipTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbISidToVipTable_add(struct ieee8021PbbISidToVipTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbISidToVipTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbISidToVipTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbISidToVipTable_add(struct ieee8021PbbISidToVipTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbISidToVipTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbISidToVipISid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbISidToVipISid, sizeof(thedata->ieee8021PbbISidToVipISid));
		header_complex_add_data(&ieee8021PbbISidToVipTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbISidToVipTable_del(struct ieee8021PbbISidToVipTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbISidToVipTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbISidToVipTable_del(struct ieee8021PbbISidToVipTable_data *thedata)
{
	struct ieee8021PbbISidToVipTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbISidToVipTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbISidToVipTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbISidToVipTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbISidToVipTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbISidToVipTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbISidToVipTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbISidToVipTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbISidToVipTable_data *StorageTmp = ieee8021PbbISidToVipTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbISidToVipTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbISidToVipISid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbISidToVipComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbISidToVipPort, &tmpsize);
	ieee8021PbbISidToVipTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbISidToVipTable(): store configuraiton file for ieee8021PbbISidToVipTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbISidToVipTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbISidToVipTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbISidToVipTable: storing data...  "));
	refresh_ieee8021PbbISidToVipTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbISidToVipTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbISidToVipTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbISidToVipTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbISidToVipISid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbISidToVipComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbISidToVipPort, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbPipTable_data *ieee8021PbbPipTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbPipTable table.
 *
 * Creates a new ieee8021PbbPipTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbPipTable_data *
ieee8021PbbPipTable_create(void)
{
	struct ieee8021PbbPipTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021PbbPipBMACAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipBMACAddressLen = 0;
		StorageNew->ieee8021PbbPipBMACAddress[StorageNew->ieee8021PbbPipBMACAddressLen] = 0;
		if ((StorageNew->ieee8021PbbPipName = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipNameLen = strlen("");
		StorageNew->ieee8021PbbPipComponentId = 0;
		if ((StorageNew->ieee8021PbbPipVipMap = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipVipMapLen = strlen("");
		if ((StorageNew->ieee8021PbbPipVipMap1 = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipVipMap1Len = strlen("");
		if ((StorageNew->ieee8021PbbPipVipMap2 = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipVipMap2Len = strlen("");
		if ((StorageNew->ieee8021PbbPipVipMap3 = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipVipMap3Len = strlen("");
		if ((StorageNew->ieee8021PbbPipVipMap4 = (uint8_t *) strdup("")) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbPipVipMap4Len = strlen("");
		StorageNew->ieee8021PbbPipRowStatus = 0;
		StorageNew->ieee8021PbbPipRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbPipTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbPipTable_data *ieee8021PbbPipTable_duplicate(struct ieee8021PbbPipTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbPipTable_data *
ieee8021PbbPipTable_duplicate(struct ieee8021PbbPipTable_data *thedata)
{
	struct ieee8021PbbPipTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbPipTable_id = thedata->ieee8021PbbPipTable_id;
		StorageNew->ieee8021PbbPipIfIndex = thedata->ieee8021PbbPipIfIndex;
		if (!(StorageNew->ieee8021PbbPipBMACAddress = malloc(thedata->ieee8021PbbPipBMACAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipBMACAddress, thedata->ieee8021PbbPipBMACAddress, thedata->ieee8021PbbPipBMACAddressLen);
		StorageNew->ieee8021PbbPipBMACAddressLen = thedata->ieee8021PbbPipBMACAddressLen;
		StorageNew->ieee8021PbbPipBMACAddress[StorageNew->ieee8021PbbPipBMACAddressLen] = 0;
		if (!(StorageNew->ieee8021PbbPipName = malloc(thedata->ieee8021PbbPipNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipName, thedata->ieee8021PbbPipName, thedata->ieee8021PbbPipNameLen);
		StorageNew->ieee8021PbbPipNameLen = thedata->ieee8021PbbPipNameLen;
		StorageNew->ieee8021PbbPipName[StorageNew->ieee8021PbbPipNameLen] = 0;
		StorageNew->ieee8021PbbPipComponentId = thedata->ieee8021PbbPipComponentId;
		if (!(StorageNew->ieee8021PbbPipVipMap = malloc(thedata->ieee8021PbbPipVipMapLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipVipMap, thedata->ieee8021PbbPipVipMap, thedata->ieee8021PbbPipVipMapLen);
		StorageNew->ieee8021PbbPipVipMapLen = thedata->ieee8021PbbPipVipMapLen;
		StorageNew->ieee8021PbbPipVipMap[StorageNew->ieee8021PbbPipVipMapLen] = 0;
		if (!(StorageNew->ieee8021PbbPipVipMap1 = malloc(thedata->ieee8021PbbPipVipMap1Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipVipMap1, thedata->ieee8021PbbPipVipMap1, thedata->ieee8021PbbPipVipMap1Len);
		StorageNew->ieee8021PbbPipVipMap1Len = thedata->ieee8021PbbPipVipMap1Len;
		StorageNew->ieee8021PbbPipVipMap1[StorageNew->ieee8021PbbPipVipMap1Len] = 0;
		if (!(StorageNew->ieee8021PbbPipVipMap2 = malloc(thedata->ieee8021PbbPipVipMap2Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipVipMap2, thedata->ieee8021PbbPipVipMap2, thedata->ieee8021PbbPipVipMap2Len);
		StorageNew->ieee8021PbbPipVipMap2Len = thedata->ieee8021PbbPipVipMap2Len;
		StorageNew->ieee8021PbbPipVipMap2[StorageNew->ieee8021PbbPipVipMap2Len] = 0;
		if (!(StorageNew->ieee8021PbbPipVipMap3 = malloc(thedata->ieee8021PbbPipVipMap3Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipVipMap3, thedata->ieee8021PbbPipVipMap3, thedata->ieee8021PbbPipVipMap3Len);
		StorageNew->ieee8021PbbPipVipMap3Len = thedata->ieee8021PbbPipVipMap3Len;
		StorageNew->ieee8021PbbPipVipMap3[StorageNew->ieee8021PbbPipVipMap3Len] = 0;
		if (!(StorageNew->ieee8021PbbPipVipMap4 = malloc(thedata->ieee8021PbbPipVipMap4Len + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbPipVipMap4, thedata->ieee8021PbbPipVipMap4, thedata->ieee8021PbbPipVipMap4Len);
		StorageNew->ieee8021PbbPipVipMap4Len = thedata->ieee8021PbbPipVipMap4Len;
		StorageNew->ieee8021PbbPipVipMap4[StorageNew->ieee8021PbbPipVipMap4Len] = 0;
		StorageNew->ieee8021PbbPipRowStatus = thedata->ieee8021PbbPipRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbPipTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbPipTable_destroy(struct ieee8021PbbPipTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbPipTable_destroy(struct ieee8021PbbPipTable_data **thedata)
{
	struct ieee8021PbbPipTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021PbbPipBMACAddress);
		StorageDel->ieee8021PbbPipBMACAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbPipName);
		StorageDel->ieee8021PbbPipNameLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbPipVipMap);
		StorageDel->ieee8021PbbPipVipMapLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbPipVipMap1);
		StorageDel->ieee8021PbbPipVipMap1Len = 0;
		SNMP_FREE(StorageDel->ieee8021PbbPipVipMap2);
		StorageDel->ieee8021PbbPipVipMap2Len = 0;
		SNMP_FREE(StorageDel->ieee8021PbbPipVipMap3);
		StorageDel->ieee8021PbbPipVipMap3Len = 0;
		SNMP_FREE(StorageDel->ieee8021PbbPipVipMap4);
		StorageDel->ieee8021PbbPipVipMap4Len = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipTable_add(struct ieee8021PbbPipTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbPipTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbPipTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbPipTable_add(struct ieee8021PbbPipTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbPipIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipIfIndex, sizeof(thedata->ieee8021PbbPipIfIndex));
		header_complex_add_data(&ieee8021PbbPipTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipTable_del(struct ieee8021PbbPipTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbPipTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbPipTable_del(struct ieee8021PbbPipTable_data *thedata)
{
	struct ieee8021PbbPipTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbPipTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbPipTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbPipTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbPipTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbPipTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbPipTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbPipTable_data *StorageTmp = ieee8021PbbPipTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbPipTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021PbbPipBMACAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipBMACAddress, &StorageTmp->ieee8021PbbPipBMACAddressLen);
	if (StorageTmp->ieee8021PbbPipBMACAddress == NULL) {
		config_perror("invalid specification for ieee8021PbbPipBMACAddress");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbPipName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipName, &StorageTmp->ieee8021PbbPipNameLen);
	if (StorageTmp->ieee8021PbbPipName == NULL) {
		config_perror("invalid specification for ieee8021PbbPipName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbPipComponentId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipVipMap, &StorageTmp->ieee8021PbbPipVipMapLen);
	if (StorageTmp->ieee8021PbbPipVipMap == NULL) {
		config_perror("invalid specification for ieee8021PbbPipVipMap");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap1);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipVipMap1, &StorageTmp->ieee8021PbbPipVipMap1Len);
	if (StorageTmp->ieee8021PbbPipVipMap1 == NULL) {
		config_perror("invalid specification for ieee8021PbbPipVipMap1");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap2);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipVipMap2, &StorageTmp->ieee8021PbbPipVipMap2Len);
	if (StorageTmp->ieee8021PbbPipVipMap2 == NULL) {
		config_perror("invalid specification for ieee8021PbbPipVipMap2");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap3);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipVipMap3, &StorageTmp->ieee8021PbbPipVipMap3Len);
	if (StorageTmp->ieee8021PbbPipVipMap3 == NULL) {
		config_perror("invalid specification for ieee8021PbbPipVipMap3");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap4);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbPipVipMap4, &StorageTmp->ieee8021PbbPipVipMap4Len);
	if (StorageTmp->ieee8021PbbPipVipMap4 == NULL) {
		config_perror("invalid specification for ieee8021PbbPipVipMap4");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipRowStatus, &tmpsize);
	ieee8021PbbPipTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbPipTable(): store configuraiton file for ieee8021PbbPipTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbPipTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbPipTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbPipTable: storing data...  "));
	refresh_ieee8021PbbPipTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbPipTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbPipTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbPipTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipBMACAddress, &StorageTmp->ieee8021PbbPipBMACAddressLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipName, &StorageTmp->ieee8021PbbPipNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbPipComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipVipMap, &StorageTmp->ieee8021PbbPipVipMapLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipVipMap1, &StorageTmp->ieee8021PbbPipVipMap1Len);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipVipMap2, &StorageTmp->ieee8021PbbPipVipMap2Len);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipVipMap3, &StorageTmp->ieee8021PbbPipVipMap3Len);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbPipVipMap4, &StorageTmp->ieee8021PbbPipVipMap4Len);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbPipPriorityTable_data *ieee8021PbbPipPriorityTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbPipPriorityTable table.
 *
 * Creates a new ieee8021PbbPipPriorityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbPipPriorityTable_data *
ieee8021PbbPipPriorityTable_create(void)
{
	struct ieee8021PbbPipPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipPriorityTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipPriorityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbPipIfIndex = 0;
		StorageNew->ieee8021PbbPipPriorityCodePointSelection = 0;
		StorageNew->ieee8021PbbPipUseDEI = 0;
		StorageNew->ieee8021PbbPipRequireDropEncoding = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbPipPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbPipPriorityTable_data *ieee8021PbbPipPriorityTable_duplicate(struct ieee8021PbbPipPriorityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbPipPriorityTable_data *
ieee8021PbbPipPriorityTable_duplicate(struct ieee8021PbbPipPriorityTable_data *thedata)
{
	struct ieee8021PbbPipPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipPriorityTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipPriorityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbPipPriorityTable_id = thedata->ieee8021PbbPipPriorityTable_id;
		StorageNew->ieee8021PbbPipIfIndex = thedata->ieee8021PbbPipIfIndex;
		StorageNew->ieee8021PbbPipPriorityCodePointSelection = thedata->ieee8021PbbPipPriorityCodePointSelection;
		StorageNew->ieee8021PbbPipUseDEI = thedata->ieee8021PbbPipUseDEI;
		StorageNew->ieee8021PbbPipRequireDropEncoding = thedata->ieee8021PbbPipRequireDropEncoding;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbPipPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbPipPriorityTable_destroy(struct ieee8021PbbPipPriorityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbPipPriorityTable_destroy(struct ieee8021PbbPipPriorityTable_data **thedata)
{
	struct ieee8021PbbPipPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipPriorityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipPriorityTable_add(struct ieee8021PbbPipPriorityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbPipPriorityTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbPipPriorityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbPipPriorityTable_add(struct ieee8021PbbPipPriorityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipPriorityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbPipIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipIfIndex, sizeof(thedata->ieee8021PbbPipIfIndex));
		header_complex_add_data(&ieee8021PbbPipPriorityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipPriorityTable_del(struct ieee8021PbbPipPriorityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbPipPriorityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbPipPriorityTable_del(struct ieee8021PbbPipPriorityTable_data *thedata)
{
	struct ieee8021PbbPipPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipPriorityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbPipPriorityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbPipPriorityTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbPipPriorityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbPipPriorityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbPipPriorityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbPipPriorityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbPipPriorityTable_data *StorageTmp = ieee8021PbbPipPriorityTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbPipPriorityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipPriorityCodePointSelection, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipUseDEI, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipRequireDropEncoding, &tmpsize);
	ieee8021PbbPipPriorityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbPipPriorityTable(): store configuraiton file for ieee8021PbbPipPriorityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbPipPriorityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbPipPriorityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbPipPriorityTable: storing data...  "));
	refresh_ieee8021PbbPipPriorityTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbPipPriorityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbPipPriorityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbPipPriorityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipPriorityCodePointSelection, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipUseDEI, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipRequireDropEncoding, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbPipDecodingTable_data *ieee8021PbbPipDecodingTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbPipDecodingTable table.
 *
 * Creates a new ieee8021PbbPipDecodingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbPipDecodingTable_data *
ieee8021PbbPipDecodingTable_create(void)
{
	struct ieee8021PbbPipDecodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipDecodingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipDecodingTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbPipIfIndex = 0;
		StorageNew->ieee8021PbbPipDecodingPriority = 0;
		StorageNew->ieee8021PbbPipDecodingDropEligible = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbPipDecodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbPipDecodingTable_data *ieee8021PbbPipDecodingTable_duplicate(struct ieee8021PbbPipDecodingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbPipDecodingTable_data *
ieee8021PbbPipDecodingTable_duplicate(struct ieee8021PbbPipDecodingTable_data *thedata)
{
	struct ieee8021PbbPipDecodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipDecodingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipDecodingTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbPipDecodingTable_id = thedata->ieee8021PbbPipDecodingTable_id;
		StorageNew->ieee8021PbbPipIfIndex = thedata->ieee8021PbbPipIfIndex;
		StorageNew->ieee8021PbbPipDecodingPriorityCodePointRow = thedata->ieee8021PbbPipDecodingPriorityCodePointRow;
		StorageNew->ieee8021PbbPipDecodingPriorityCodePoint = thedata->ieee8021PbbPipDecodingPriorityCodePoint;
		StorageNew->ieee8021PbbPipDecodingPriority = thedata->ieee8021PbbPipDecodingPriority;
		StorageNew->ieee8021PbbPipDecodingDropEligible = thedata->ieee8021PbbPipDecodingDropEligible;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbPipDecodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbPipDecodingTable_destroy(struct ieee8021PbbPipDecodingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbPipDecodingTable_destroy(struct ieee8021PbbPipDecodingTable_data **thedata)
{
	struct ieee8021PbbPipDecodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipDecodingTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipDecodingTable_add(struct ieee8021PbbPipDecodingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbPipDecodingTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbPipDecodingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbPipDecodingTable_add(struct ieee8021PbbPipDecodingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipDecodingTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbPipIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipIfIndex, sizeof(thedata->ieee8021PbbPipIfIndex));
		/* ieee8021PbbPipDecodingPriorityCodePointRow */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipDecodingPriorityCodePointRow, sizeof(thedata->ieee8021PbbPipDecodingPriorityCodePointRow));
		/* ieee8021PbbPipDecodingPriorityCodePoint */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipDecodingPriorityCodePoint, sizeof(thedata->ieee8021PbbPipDecodingPriorityCodePoint));
		header_complex_add_data(&ieee8021PbbPipDecodingTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipDecodingTable_del(struct ieee8021PbbPipDecodingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbPipDecodingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbPipDecodingTable_del(struct ieee8021PbbPipDecodingTable_data *thedata)
{
	struct ieee8021PbbPipDecodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipDecodingTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbPipDecodingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbPipDecodingTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbPipDecodingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbPipDecodingTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbPipDecodingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbPipDecodingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbPipDecodingTable_data *StorageTmp = ieee8021PbbPipDecodingTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbPipDecodingTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipDecodingPriorityCodePointRow, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipDecodingPriorityCodePoint, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbPipDecodingPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipDecodingDropEligible, &tmpsize);
	ieee8021PbbPipDecodingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbPipDecodingTable(): store configuraiton file for ieee8021PbbPipDecodingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbPipDecodingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbPipDecodingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbPipDecodingTable: storing data...  "));
	refresh_ieee8021PbbPipDecodingTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbPipDecodingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbPipDecodingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbPipDecodingTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipDecodingPriorityCodePointRow, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipDecodingPriorityCodePoint, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbPipDecodingPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipDecodingDropEligible, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbPipEncodingTable_data *ieee8021PbbPipEncodingTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbPipEncodingTable table.
 *
 * Creates a new ieee8021PbbPipEncodingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbPipEncodingTable_data *
ieee8021PbbPipEncodingTable_create(void)
{
	struct ieee8021PbbPipEncodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipEncodingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipEncodingTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021PbbPipIfIndex = 0;
		StorageNew->ieee8021PbbPipEncodingPriority = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbPipEncodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbPipEncodingTable_data *ieee8021PbbPipEncodingTable_duplicate(struct ieee8021PbbPipEncodingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbPipEncodingTable_data *
ieee8021PbbPipEncodingTable_duplicate(struct ieee8021PbbPipEncodingTable_data *thedata)
{
	struct ieee8021PbbPipEncodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbPipEncodingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipEncodingTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbPipEncodingTable_id = thedata->ieee8021PbbPipEncodingTable_id;
		StorageNew->ieee8021PbbPipIfIndex = thedata->ieee8021PbbPipIfIndex;
		StorageNew->ieee8021PbbPipEncodingPriorityCodePointRow = thedata->ieee8021PbbPipEncodingPriorityCodePointRow;
		StorageNew->ieee8021PbbPipEncodingPriorityCodePoint = thedata->ieee8021PbbPipEncodingPriorityCodePoint;
		StorageNew->ieee8021PbbPipEncodingDropEligible = thedata->ieee8021PbbPipEncodingDropEligible;
		StorageNew->ieee8021PbbPipEncodingPriority = thedata->ieee8021PbbPipEncodingPriority;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbPipEncodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbPipEncodingTable_destroy(struct ieee8021PbbPipEncodingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbPipEncodingTable_destroy(struct ieee8021PbbPipEncodingTable_data **thedata)
{
	struct ieee8021PbbPipEncodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipEncodingTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipEncodingTable_add(struct ieee8021PbbPipEncodingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbPipEncodingTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbPipEncodingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbPipEncodingTable_add(struct ieee8021PbbPipEncodingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipEncodingTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021PbbPipIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipIfIndex, sizeof(thedata->ieee8021PbbPipIfIndex));
		/* ieee8021PbbPipEncodingPriorityCodePointRow */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipEncodingPriorityCodePointRow, sizeof(thedata->ieee8021PbbPipEncodingPriorityCodePointRow));
		/* ieee8021PbbPipEncodingPriorityCodePoint */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipEncodingPriorityCodePoint, sizeof(thedata->ieee8021PbbPipEncodingPriorityCodePoint));
		/* ieee8021PbbPipEncodingDropEligible */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021PbbPipEncodingDropEligible, sizeof(thedata->ieee8021PbbPipEncodingDropEligible));
		header_complex_add_data(&ieee8021PbbPipEncodingTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbPipEncodingTable_del(struct ieee8021PbbPipEncodingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbPipEncodingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbPipEncodingTable_del(struct ieee8021PbbPipEncodingTable_data *thedata)
{
	struct ieee8021PbbPipEncodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbPipEncodingTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbPipEncodingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbPipEncodingTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbPipEncodingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbPipEncodingTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbPipEncodingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbPipEncodingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbPipEncodingTable_data *StorageTmp = ieee8021PbbPipEncodingTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbPipEncodingTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipEncodingPriorityCodePointRow, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipEncodingPriorityCodePoint, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbPipEncodingDropEligible, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbPipEncodingPriority, &tmpsize);
	ieee8021PbbPipEncodingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbPipEncodingTable(): store configuraiton file for ieee8021PbbPipEncodingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbPipEncodingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbPipEncodingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbPipEncodingTable: storing data...  "));
	refresh_ieee8021PbbPipEncodingTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbPipEncodingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbPipEncodingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbPipEncodingTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipEncodingPriorityCodePointRow, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipEncodingPriorityCodePoint, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbPipEncodingDropEligible, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbPipEncodingPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbVipToPipMappingTable_data *ieee8021PbbVipToPipMappingTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbVipToPipMappingTable table.
 *
 * Creates a new ieee8021PbbVipToPipMappingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbVipToPipMappingTable_data *
ieee8021PbbVipToPipMappingTable_create(void)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbVipToPipMappingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipToPipMappingTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbbVipToPipMappingPipIfIndex = 0;
		StorageNew->ieee8021PbbVipToPipMappingStorageType = 0;
		StorageNew->ieee8021PbbVipToPipMappingRowStatus = 0;
		StorageNew->ieee8021PbbVipToPipMappingRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbVipToPipMappingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbVipToPipMappingTable_data *ieee8021PbbVipToPipMappingTable_duplicate(struct ieee8021PbbVipToPipMappingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbVipToPipMappingTable_data *
ieee8021PbbVipToPipMappingTable_duplicate(struct ieee8021PbbVipToPipMappingTable_data *thedata)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbVipToPipMappingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipToPipMappingTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbVipToPipMappingTable_id = thedata->ieee8021PbbVipToPipMappingTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbbVipToPipMappingPipIfIndex = thedata->ieee8021PbbVipToPipMappingPipIfIndex;
		StorageNew->ieee8021PbbVipToPipMappingStorageType = thedata->ieee8021PbbVipToPipMappingStorageType;
		StorageNew->ieee8021PbbVipToPipMappingRowStatus = thedata->ieee8021PbbVipToPipMappingRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbVipToPipMappingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbVipToPipMappingTable_destroy(struct ieee8021PbbVipToPipMappingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbVipToPipMappingTable_destroy(struct ieee8021PbbVipToPipMappingTable_data **thedata)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipToPipMappingTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbVipToPipMappingTable_add(struct ieee8021PbbVipToPipMappingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbVipToPipMappingTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbVipToPipMappingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbVipToPipMappingTable_add(struct ieee8021PbbVipToPipMappingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipToPipMappingTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbbVipToPipMappingTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbVipToPipMappingTable_del(struct ieee8021PbbVipToPipMappingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbVipToPipMappingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbVipToPipMappingTable_del(struct ieee8021PbbVipToPipMappingTable_data *thedata)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbVipToPipMappingTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbVipToPipMappingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbVipToPipMappingTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbVipToPipMappingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbVipToPipMappingTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbVipToPipMappingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbVipToPipMappingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbVipToPipMappingTable_data *StorageTmp = ieee8021PbbVipToPipMappingTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbVipToPipMappingTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbVipToPipMappingStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbVipToPipMappingRowStatus, &tmpsize);
	ieee8021PbbVipToPipMappingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbVipToPipMappingTable(): store configuraiton file for ieee8021PbbVipToPipMappingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbVipToPipMappingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbVipToPipMappingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbVipToPipMappingTable: storing data...  "));
	refresh_ieee8021PbbVipToPipMappingTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbVipToPipMappingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbVipToPipMappingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->ieee8021PbbVipToPipMappingStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbVipToPipMappingTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbVipToPipMappingStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbVipToPipMappingRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbCBPServiceMappingTable_data *ieee8021PbbCBPServiceMappingTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbCBPServiceMappingTable table.
 *
 * Creates a new ieee8021PbbCBPServiceMappingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbCBPServiceMappingTable_data *
ieee8021PbbCBPServiceMappingTable_create(void)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbCBPServiceMappingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCBPServiceMappingTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbbCBPServiceMappingBVid = 0;
		if ((StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDest = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen = 0;
		StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDest[StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen] = 0;
		if (memdup((u_char **) &StorageNew->ieee8021PbbCBPServiceMappingType, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021PbbCBPServiceMappingTypeLen = 1;
		StorageNew->ieee8021PbbCBPServiceMappingLocalSid = 1;
		StorageNew->ieee8021PbbCBPServiceMappingRowStatus = 0;
		StorageNew->ieee8021PbbCBPServiceMappingRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbCBPServiceMappingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbCBPServiceMappingTable_data *ieee8021PbbCBPServiceMappingTable_duplicate(struct ieee8021PbbCBPServiceMappingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbCBPServiceMappingTable_data *
ieee8021PbbCBPServiceMappingTable_duplicate(struct ieee8021PbbCBPServiceMappingTable_data *thedata)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbCBPServiceMappingTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCBPServiceMappingTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbCBPServiceMappingTable_id = thedata->ieee8021PbbCBPServiceMappingTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbbCBPServiceMappingBackboneSid = thedata->ieee8021PbbCBPServiceMappingBackboneSid;
		StorageNew->ieee8021PbbCBPServiceMappingBVid = thedata->ieee8021PbbCBPServiceMappingBVid;
		if (!(StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDest = malloc(thedata->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDest, thedata->ieee8021PbbCBPServiceMappingDefaultBackboneDest,
		       thedata->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen);
		StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen = thedata->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen;
		StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDest[StorageNew->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen] = 0;
		if (!(StorageNew->ieee8021PbbCBPServiceMappingType = malloc(thedata->ieee8021PbbCBPServiceMappingTypeLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021PbbCBPServiceMappingType, thedata->ieee8021PbbCBPServiceMappingType, thedata->ieee8021PbbCBPServiceMappingTypeLen);
		StorageNew->ieee8021PbbCBPServiceMappingTypeLen = thedata->ieee8021PbbCBPServiceMappingTypeLen;
		StorageNew->ieee8021PbbCBPServiceMappingType[StorageNew->ieee8021PbbCBPServiceMappingTypeLen] = 0;
		StorageNew->ieee8021PbbCBPServiceMappingLocalSid = thedata->ieee8021PbbCBPServiceMappingLocalSid;
		StorageNew->ieee8021PbbCBPServiceMappingRowStatus = thedata->ieee8021PbbCBPServiceMappingRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbCBPServiceMappingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbCBPServiceMappingTable_destroy(struct ieee8021PbbCBPServiceMappingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbCBPServiceMappingTable_destroy(struct ieee8021PbbCBPServiceMappingTable_data **thedata)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCBPServiceMappingTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021PbbCBPServiceMappingDefaultBackboneDest);
		StorageDel->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen = 0;
		SNMP_FREE(StorageDel->ieee8021PbbCBPServiceMappingType);
		StorageDel->ieee8021PbbCBPServiceMappingTypeLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbCBPServiceMappingTable_add(struct ieee8021PbbCBPServiceMappingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbCBPServiceMappingTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbCBPServiceMappingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbCBPServiceMappingTable_add(struct ieee8021PbbCBPServiceMappingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCBPServiceMappingTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021PbbCBPServiceMappingBackboneSid */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021PbbCBPServiceMappingBackboneSid, sizeof(thedata->ieee8021PbbCBPServiceMappingBackboneSid));
		header_complex_add_data(&ieee8021PbbCBPServiceMappingTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbCBPServiceMappingTable_del(struct ieee8021PbbCBPServiceMappingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbCBPServiceMappingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbCBPServiceMappingTable_del(struct ieee8021PbbCBPServiceMappingTable_data *thedata)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCBPServiceMappingTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbCBPServiceMappingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbCBPServiceMappingTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbCBPServiceMappingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbCBPServiceMappingTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbCBPServiceMappingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbCBPServiceMappingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = ieee8021PbbCBPServiceMappingTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbCBPServiceMappingTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbCBPServiceMappingBackboneSid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbCBPServiceMappingBVid, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest, &StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen);
	if (StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest == NULL) {
		config_perror("invalid specification for ieee8021PbbCBPServiceMappingDefaultBackboneDest");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021PbbCBPServiceMappingType);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021PbbCBPServiceMappingType, &StorageTmp->ieee8021PbbCBPServiceMappingTypeLen);
	if (StorageTmp->ieee8021PbbCBPServiceMappingType == NULL) {
		config_perror("invalid specification for ieee8021PbbCBPServiceMappingType");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021PbbCBPServiceMappingLocalSid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbCBPServiceMappingRowStatus, &tmpsize);
	ieee8021PbbCBPServiceMappingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbCBPServiceMappingTable(): store configuraiton file for ieee8021PbbCBPServiceMappingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbCBPServiceMappingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbCBPServiceMappingTable: storing data...  "));
	refresh_ieee8021PbbCBPServiceMappingTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbCBPServiceMappingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbCBPServiceMappingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbCBPServiceMappingTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbCBPServiceMappingBackboneSid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbCBPServiceMappingBVid, &tmpsize);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest, &StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021PbbCBPServiceMappingType, &StorageTmp->ieee8021PbbCBPServiceMappingTypeLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021PbbCBPServiceMappingLocalSid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbCBPServiceMappingRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021PbbCbpTable_data *ieee8021PbbCbpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021PbbCbpTable table.
 *
 * Creates a new ieee8021PbbCbpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021PbbCbpTable_data *
ieee8021PbbCbpTable_create(void)
{
	struct ieee8021PbbCbpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbCbpTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCbpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021PbbCbpRowStatus = 0;
		StorageNew->ieee8021PbbCbpRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021PbbCbpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021PbbCbpTable_data *ieee8021PbbCbpTable_duplicate(struct ieee8021PbbCbpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021PbbCbpTable_data *
ieee8021PbbCbpTable_duplicate(struct ieee8021PbbCbpTable_data *thedata)
{
	struct ieee8021PbbCbpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021PbbCbpTable_data);

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCbpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021PbbCbpTable_id = thedata->ieee8021PbbCbpTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021PbbCbpRowStatus = thedata->ieee8021PbbCbpRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021PbbCbpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021PbbCbpTable_destroy(struct ieee8021PbbCbpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021PbbCbpTable_destroy(struct ieee8021PbbCbpTable_data **thedata)
{
	struct ieee8021PbbCbpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCbpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbCbpTable_add(struct ieee8021PbbCbpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021PbbCbpTable table data set.
 *
 * Adds a table row structure to the ieee8021PbbCbpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021PbbCbpTable_add(struct ieee8021PbbCbpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCbpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021PbbCbpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021PbbCbpTable_del(struct ieee8021PbbCbpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021PbbCbpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021PbbCbpTable_del(struct ieee8021PbbCbpTable_data *thedata)
{
	struct ieee8021PbbCbpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbCbpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021PbbCbpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021PbbCbpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021PbbCbpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021PbbCbpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021PbbCbpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021PbbCbpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021PbbCbpTable_data *StorageTmp = ieee8021PbbCbpTable_create();

	DEBUGMSGTL(("ieee8021PbbMib", "parse_ieee8021PbbCbpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021PbbCbpRowStatus, &tmpsize);
	ieee8021PbbCbpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/*
 * store_ieee8021PbbCbpTable(): store configuraiton file for ieee8021PbbCbpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021PbbCbpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021PbbCbpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021PbbMib", "store_ieee8021PbbCbpTable: storing data...  "));
	refresh_ieee8021PbbCbpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021PbbCbpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021PbbCbpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021PbbCbpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021PbbCbpRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, struct ieee8021PbbVipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, struct ieee8021PbbVipTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, struct ieee8021PbbVipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbVipTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, struct ieee8021PbbVipTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbVipTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, struct ieee8021PbbVipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, struct ieee8021PbbVipTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbVipTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbVipTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbVipTable_data *
refresh_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbVipTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbVipTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbVipTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbVipTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbVipTable(int force)
{
	if (!force && ieee8021PbbVipTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbVipTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbVipTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbVipTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbVipTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbVipTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbVipTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbVipTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbVipTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbVipTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBVIPPIPIFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipPipIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipPipIfIndex;
		break;
	case (u_char) IEEE8021PBBVIPISID:	/* Create */
		*write_method = write_ieee8021PbbVipISid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipISid);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipISid;
		break;
	case (u_char) IEEE8021PBBVIPDEFAULTDSTBMAC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbVipDefaultDstBMACLen;
		rval = (u_char *) StorageTmp->ieee8021PbbVipDefaultDstBMAC;
		break;
	case (u_char) IEEE8021PBBVIPTYPE:	/* Create */
		*write_method = write_ieee8021PbbVipType;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbVipTypeLen;
		rval = (u_char *) StorageTmp->ieee8021PbbVipType;
		break;
	case (u_char) IEEE8021PBBVIPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbVipRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipRowStatus;
		break;
	case (u_char) IEEE8021PBBVIPENABLECONNECTIONID:	/* Create */
		*write_method = write_ieee8021PbbVipEnableConnectionId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipEnableConnectionId);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipEnableConnectionId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, struct ieee8021PbbISidToVipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, struct ieee8021PbbISidToVipTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, struct ieee8021PbbISidToVipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbISidToVipTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, struct ieee8021PbbISidToVipTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbISidToVipTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, struct ieee8021PbbISidToVipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, struct ieee8021PbbISidToVipTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbISidToVipTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbISidToVipTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbISidToVipTable_data *
refresh_ieee8021PbbISidToVipTable_row(struct ieee8021PbbISidToVipTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbISidToVipTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbISidToVipTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbISidToVipTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbISidToVipTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbISidToVipTable(int force)
{
	if (!force && ieee8021PbbISidToVipTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbISidToVipTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbISidToVipTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbISidToVipTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbISidToVipTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbISidToVipTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbISidToVipTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbISidToVipTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbISidToVipTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbISidToVipTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBISIDTOVIPCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbISidToVipComponentId);
		rval = (u_char *) &StorageTmp->ieee8021PbbISidToVipComponentId;
		break;
	case (u_char) IEEE8021PBBISIDTOVIPPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbISidToVipPort);
		rval = (u_char *) &StorageTmp->ieee8021PbbISidToVipPort;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, struct ieee8021PbbPipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, struct ieee8021PbbPipTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, struct ieee8021PbbPipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbPipTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, struct ieee8021PbbPipTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbPipTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, struct ieee8021PbbPipTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, struct ieee8021PbbPipTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbPipTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbPipTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbPipTable_data *
refresh_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbPipTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbPipTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbPipTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbPipTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbPipTable(int force)
{
	if (!force && ieee8021PbbPipTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbPipTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbPipTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbPipTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbPipTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbPipTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbPipTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbPipTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbPipTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBPIPBMACADDRESS:	/* Create */
		*write_method = write_ieee8021PbbPipBMACAddress;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipBMACAddressLen;
		rval = (u_char *) StorageTmp->ieee8021PbbPipBMACAddress;
		break;
	case (u_char) IEEE8021PBBPIPNAME:	/* Create */
		*write_method = write_ieee8021PbbPipName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipNameLen;
		rval = (u_char *) StorageTmp->ieee8021PbbPipName;
		break;
	case (u_char) IEEE8021PBBPIPCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipComponentId);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipComponentId;
		break;
	case (u_char) IEEE8021PBBPIPVIPMAP:	/* Create */
		*write_method = write_ieee8021PbbPipVipMap;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipVipMapLen;
		rval = (u_char *) StorageTmp->ieee8021PbbPipVipMap;
		break;
	case (u_char) IEEE8021PBBPIPVIPMAP1:	/* Create */
		*write_method = write_ieee8021PbbPipVipMap1;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipVipMap1Len;
		rval = (u_char *) StorageTmp->ieee8021PbbPipVipMap1;
		break;
	case (u_char) IEEE8021PBBPIPVIPMAP2:	/* Create */
		*write_method = write_ieee8021PbbPipVipMap2;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipVipMap2Len;
		rval = (u_char *) StorageTmp->ieee8021PbbPipVipMap2;
		break;
	case (u_char) IEEE8021PBBPIPVIPMAP3:	/* Create */
		*write_method = write_ieee8021PbbPipVipMap3;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipVipMap3Len;
		rval = (u_char *) StorageTmp->ieee8021PbbPipVipMap3;
		break;
	case (u_char) IEEE8021PBBPIPVIPMAP4:	/* Create */
		*write_method = write_ieee8021PbbPipVipMap4;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbPipVipMap4Len;
		rval = (u_char *) StorageTmp->ieee8021PbbPipVipMap4;
		break;
	case (u_char) IEEE8021PBBPIPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbPipRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, struct ieee8021PbbPipPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, struct ieee8021PbbPipPriorityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, struct ieee8021PbbPipPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbPipPriorityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, struct ieee8021PbbPipPriorityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbPipPriorityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, struct ieee8021PbbPipPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, struct ieee8021PbbPipPriorityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbPipPriorityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbPipPriorityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbPipPriorityTable_data *
refresh_ieee8021PbbPipPriorityTable_row(struct ieee8021PbbPipPriorityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbPipPriorityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbPipPriorityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbPipPriorityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbPipPriorityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbPipPriorityTable(int force)
{
	if (!force && ieee8021PbbPipPriorityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbPipPriorityTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbPipPriorityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbPipPriorityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbPipPriorityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbPipPriorityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbPipPriorityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbPipPriorityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbPipPriorityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbPipPriorityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION:	/* ReadWrite */
		*write_method = write_ieee8021PbbPipPriorityCodePointSelection;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipPriorityCodePointSelection);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipPriorityCodePointSelection;
		break;
	case (u_char) IEEE8021PBBPIPUSEDEI:	/* ReadWrite */
		*write_method = write_ieee8021PbbPipUseDEI;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipUseDEI);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipUseDEI;
		break;
	case (u_char) IEEE8021PBBPIPREQUIREDROPENCODING:	/* ReadWrite */
		*write_method = write_ieee8021PbbPipRequireDropEncoding;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipRequireDropEncoding);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipRequireDropEncoding;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, struct ieee8021PbbPipDecodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, struct ieee8021PbbPipDecodingTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, struct ieee8021PbbPipDecodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbPipDecodingTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, struct ieee8021PbbPipDecodingTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbPipDecodingTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, struct ieee8021PbbPipDecodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, struct ieee8021PbbPipDecodingTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbPipDecodingTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbPipDecodingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbPipDecodingTable_data *
refresh_ieee8021PbbPipDecodingTable_row(struct ieee8021PbbPipDecodingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbPipDecodingTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbPipDecodingTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbPipDecodingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbPipDecodingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbPipDecodingTable(int force)
{
	if (!force && ieee8021PbbPipDecodingTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbPipDecodingTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbPipDecodingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbPipDecodingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbPipDecodingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbPipDecodingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbPipDecodingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbPipDecodingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbPipDecodingTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbPipDecodingTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBPIPDECODINGPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021PbbPipDecodingPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipDecodingPriority);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipDecodingPriority;
		break;
	case (u_char) IEEE8021PBBPIPDECODINGDROPELIGIBLE:	/* ReadWrite */
		*write_method = write_ieee8021PbbPipDecodingDropEligible;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipDecodingDropEligible);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipDecodingDropEligible;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, struct ieee8021PbbPipEncodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, struct ieee8021PbbPipEncodingTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, struct ieee8021PbbPipEncodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbPipEncodingTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, struct ieee8021PbbPipEncodingTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbPipEncodingTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, struct ieee8021PbbPipEncodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, struct ieee8021PbbPipEncodingTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbPipEncodingTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbPipEncodingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbPipEncodingTable_data *
refresh_ieee8021PbbPipEncodingTable_row(struct ieee8021PbbPipEncodingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbPipEncodingTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbPipEncodingTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbPipEncodingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbPipEncodingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbPipEncodingTable(int force)
{
	if (!force && ieee8021PbbPipEncodingTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbPipEncodingTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbPipEncodingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbPipEncodingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbPipEncodingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbPipEncodingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbPipEncodingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbPipEncodingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbPipEncodingTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbPipEncodingTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBPIPENCODINGPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021PbbPipEncodingPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbPipEncodingPriority);
		rval = (u_char *) &StorageTmp->ieee8021PbbPipEncodingPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, struct ieee8021PbbVipToPipMappingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, struct ieee8021PbbVipToPipMappingTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, struct ieee8021PbbVipToPipMappingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbVipToPipMappingTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, struct ieee8021PbbVipToPipMappingTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbVipToPipMappingTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, struct ieee8021PbbVipToPipMappingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, struct ieee8021PbbVipToPipMappingTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbVipToPipMappingTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbVipToPipMappingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbVipToPipMappingTable_data *
refresh_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbVipToPipMappingTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbVipToPipMappingTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbVipToPipMappingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbVipToPipMappingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbVipToPipMappingTable(int force)
{
	if (!force && ieee8021PbbVipToPipMappingTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbVipToPipMappingTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbVipToPipMappingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbVipToPipMappingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbVipToPipMappingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbVipToPipMappingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbVipToPipMappingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbVipToPipMappingTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbVipToPipMappingTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBVIPTOPIPMAPPINGPIPIFINDEX:	/* Create */
		*write_method = write_ieee8021PbbVipToPipMappingPipIfIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex;
		break;
	case (u_char) IEEE8021PBBVIPTOPIPMAPPINGSTORAGETYPE:	/* Create */
		*write_method = write_ieee8021PbbVipToPipMappingStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipToPipMappingStorageType);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipToPipMappingStorageType;
		break;
	case (u_char) IEEE8021PBBVIPTOPIPMAPPINGROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbVipToPipMappingRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbVipToPipMappingRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbVipToPipMappingRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, struct ieee8021PbbCBPServiceMappingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, struct ieee8021PbbCBPServiceMappingTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, struct ieee8021PbbCBPServiceMappingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbCBPServiceMappingTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, struct ieee8021PbbCBPServiceMappingTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbCBPServiceMappingTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, struct ieee8021PbbCBPServiceMappingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, struct ieee8021PbbCBPServiceMappingTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbCBPServiceMappingTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbCBPServiceMappingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbCBPServiceMappingTable_data *
refresh_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbCBPServiceMappingTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbCBPServiceMappingTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbCBPServiceMappingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbCBPServiceMappingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbCBPServiceMappingTable(int force)
{
	if (!force && ieee8021PbbCBPServiceMappingTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbCBPServiceMappingTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbCBPServiceMappingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbCBPServiceMappingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbCBPServiceMappingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbCBPServiceMappingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbCBPServiceMappingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbCBPServiceMappingTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBCBPSERVICEMAPPINGBVID:	/* Create */
		*write_method = write_ieee8021PbbCBPServiceMappingBVid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbCBPServiceMappingBVid);
		rval = (u_char *) &StorageTmp->ieee8021PbbCBPServiceMappingBVid;
		break;
	case (u_char) IEEE8021PBBCBPSERVICEMAPPINGDEFAULTBACKBONEDEST:	/* Create */
		*write_method = write_ieee8021PbbCBPServiceMappingDefaultBackboneDest;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen;
		rval = (u_char *) StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest;
		break;
	case (u_char) IEEE8021PBBCBPSERVICEMAPPINGTYPE:	/* Create */
		*write_method = write_ieee8021PbbCBPServiceMappingType;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021PbbCBPServiceMappingTypeLen;
		rval = (u_char *) StorageTmp->ieee8021PbbCBPServiceMappingType;
		break;
	case (u_char) IEEE8021PBBCBPSERVICEMAPPINGLOCALSID:	/* Create */
		*write_method = write_ieee8021PbbCBPServiceMappingLocalSid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbCBPServiceMappingLocalSid);
		rval = (u_char *) &StorageTmp->ieee8021PbbCBPServiceMappingLocalSid;
		break;
	case (u_char) IEEE8021PBBCBPSERVICEMAPPINGROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbCBPServiceMappingRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbCBPServiceMappingRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbCBPServiceMappingRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, struct ieee8021PbbCbpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, struct ieee8021PbbCbpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, struct ieee8021PbbCbpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021PbbCbpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, struct ieee8021PbbCbpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021PbbCbpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, struct ieee8021PbbCbpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, struct ieee8021PbbCbpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021PbbCbpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021PbbCbpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021PbbCbpTable_data *
refresh_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021PbbCbpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021PbbCbpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021PbbCbpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021PbbCbpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021PbbCbpTable(int force)
{
	if (!force && ieee8021PbbCbpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021PbbCbpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021PbbCbpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021PbbCbpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021PbbMib above.
 */
u_char *
var_ieee8021PbbCbpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021PbbCbpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021PbbMib", "var_ieee8021PbbCbpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021PbbCbpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021PbbCbpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021PbbCbpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021PBBCBPROWSTATUS:	/* Create */
		*write_method = write_ieee8021PbbCbpRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021PbbCbpRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021PbbCbpRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021PbbVipISid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbVipISid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipISid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbVipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbVipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipISid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipISid not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipISid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		/* Note: ranges 1..1 256..16777214 */
		if (set_value != 1 && (256 > set_value || set_value > 16777214)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipISid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbVipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old = ieee8021PbbVipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbVipTable_rsvs++;
		StorageTmp->ieee8021PbbVipISid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbVipTable_tsts == 0)
				if ((ret = check_ieee8021PbbVipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbVipISid for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbVipTable_sets == 0)
				if ((ret = update_ieee8021PbbVipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
			StorageTmp->ieee8021PbbVipTable_rsvs = 0;
			StorageTmp->ieee8021PbbVipTable_tsts = 0;
			StorageTmp->ieee8021PbbVipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbVipTable_sets == 0)
			revert_ieee8021PbbVipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbVipISid = StorageOld->ieee8021PbbVipISid;
		if (--StorageTmp->ieee8021PbbVipTable_rsvs == 0)
			ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbVipType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbVipType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbVipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbVipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipType not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipType: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipType: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { ingress , egress } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbVipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old = ieee8021PbbVipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbVipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbVipType);
		StorageTmp->ieee8021PbbVipType = string;
		StorageTmp->ieee8021PbbVipTypeLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbVipTable_tsts == 0)
				if ((ret = check_ieee8021PbbVipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbVipType for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbVipTable_sets == 0)
				if ((ret = update_ieee8021PbbVipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
			StorageTmp->ieee8021PbbVipTable_rsvs = 0;
			StorageTmp->ieee8021PbbVipTable_tsts = 0;
			StorageTmp->ieee8021PbbVipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbVipTable_sets == 0)
			revert_ieee8021PbbVipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbVipType != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbVipType);
			StorageTmp->ieee8021PbbVipType = StorageOld->ieee8021PbbVipType;
			StorageTmp->ieee8021PbbVipTypeLen = StorageOld->ieee8021PbbVipTypeLen;
			StorageOld->ieee8021PbbVipType = NULL;
			StorageOld->ieee8021PbbVipTypeLen = 0;
		}
		if (--StorageTmp->ieee8021PbbVipTable_rsvs == 0)
			ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbVipEnableConnectionId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbVipEnableConnectionId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipEnableConnectionId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbVipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbVipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipEnableConnectionId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipEnableConnectionId not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipEnableConnectionId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipEnableConnectionId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbVipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old = ieee8021PbbVipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbVipTable_rsvs++;
		StorageTmp->ieee8021PbbVipEnableConnectionId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbVipTable_tsts == 0)
				if ((ret = check_ieee8021PbbVipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbVipEnableConnectionId for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbVipTable_sets == 0)
				if ((ret = update_ieee8021PbbVipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
			ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
			StorageTmp->ieee8021PbbVipTable_rsvs = 0;
			StorageTmp->ieee8021PbbVipTable_tsts = 0;
			StorageTmp->ieee8021PbbVipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbVipTable_sets == 0)
			revert_ieee8021PbbVipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbVipEnableConnectionId = StorageOld->ieee8021PbbVipEnableConnectionId;
		if (--StorageTmp->ieee8021PbbVipTable_rsvs == 0)
			ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipBMACAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipBMACAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipBMACAddress entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipBMACAddress: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipBMACAddress not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipBMACAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipBMACAddress);
		StorageTmp->ieee8021PbbPipBMACAddress = string;
		StorageTmp->ieee8021PbbPipBMACAddressLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipBMACAddress for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipBMACAddress != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipBMACAddress);
			StorageTmp->ieee8021PbbPipBMACAddress = StorageOld->ieee8021PbbPipBMACAddress;
			StorageTmp->ieee8021PbbPipBMACAddressLen = StorageOld->ieee8021PbbPipBMACAddressLen;
			StorageOld->ieee8021PbbPipBMACAddress = NULL;
			StorageOld->ieee8021PbbPipBMACAddressLen = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipName);
		StorageTmp->ieee8021PbbPipName = string;
		StorageTmp->ieee8021PbbPipNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipName for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipName != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipName);
			StorageTmp->ieee8021PbbPipName = StorageOld->ieee8021PbbPipName;
			StorageTmp->ieee8021PbbPipNameLen = StorageOld->ieee8021PbbPipNameLen;
			StorageOld->ieee8021PbbPipName = NULL;
			StorageOld->ieee8021PbbPipNameLen = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipVipMap(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipVipMap(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipVipMap entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..512 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 512))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap);
		StorageTmp->ieee8021PbbPipVipMap = string;
		StorageTmp->ieee8021PbbPipVipMapLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipVipMap for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipVipMap != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap);
			StorageTmp->ieee8021PbbPipVipMap = StorageOld->ieee8021PbbPipVipMap;
			StorageTmp->ieee8021PbbPipVipMapLen = StorageOld->ieee8021PbbPipVipMapLen;
			StorageOld->ieee8021PbbPipVipMap = NULL;
			StorageOld->ieee8021PbbPipVipMapLen = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipVipMap1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipVipMap1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipVipMap1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap1 not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..2049 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 2049))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap1);
		StorageTmp->ieee8021PbbPipVipMap1 = string;
		StorageTmp->ieee8021PbbPipVipMap1Len = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipVipMap1 for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipVipMap1 != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap1);
			StorageTmp->ieee8021PbbPipVipMap1 = StorageOld->ieee8021PbbPipVipMap1;
			StorageTmp->ieee8021PbbPipVipMap1Len = StorageOld->ieee8021PbbPipVipMap1Len;
			StorageOld->ieee8021PbbPipVipMap1 = NULL;
			StorageOld->ieee8021PbbPipVipMap1Len = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipVipMap2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipVipMap2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipVipMap2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap2 not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..2048 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 2048))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap2);
		StorageTmp->ieee8021PbbPipVipMap2 = string;
		StorageTmp->ieee8021PbbPipVipMap2Len = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipVipMap2 for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipVipMap2 != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap2);
			StorageTmp->ieee8021PbbPipVipMap2 = StorageOld->ieee8021PbbPipVipMap2;
			StorageTmp->ieee8021PbbPipVipMap2Len = StorageOld->ieee8021PbbPipVipMap2Len;
			StorageOld->ieee8021PbbPipVipMap2 = NULL;
			StorageOld->ieee8021PbbPipVipMap2Len = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipVipMap3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipVipMap3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipVipMap3 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap3 not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..2048 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 2048))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap3);
		StorageTmp->ieee8021PbbPipVipMap3 = string;
		StorageTmp->ieee8021PbbPipVipMap3Len = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipVipMap3 for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipVipMap3 != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap3);
			StorageTmp->ieee8021PbbPipVipMap3 = StorageOld->ieee8021PbbPipVipMap3;
			StorageTmp->ieee8021PbbPipVipMap3Len = StorageOld->ieee8021PbbPipVipMap3Len;
			StorageOld->ieee8021PbbPipVipMap3 = NULL;
			StorageOld->ieee8021PbbPipVipMap3Len = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipVipMap4(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipVipMap4(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipVipMap4 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbPipRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap4: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap4 not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..1537 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 1537))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipVipMap4: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap4);
		StorageTmp->ieee8021PbbPipVipMap4 = string;
		StorageTmp->ieee8021PbbPipVipMap4Len = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipVipMap4 for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipTable_sets == 0)
				if ((ret = update_ieee8021PbbPipTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			StorageTmp->ieee8021PbbPipTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipTable_tsts = 0;
			StorageTmp->ieee8021PbbPipTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipTable_sets == 0)
			revert_ieee8021PbbPipTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbPipVipMap4 != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbPipVipMap4);
			StorageTmp->ieee8021PbbPipVipMap4 = StorageOld->ieee8021PbbPipVipMap4;
			StorageTmp->ieee8021PbbPipVipMap4Len = StorageOld->ieee8021PbbPipVipMap4Len;
			StorageOld->ieee8021PbbPipVipMap4 = NULL;
			StorageOld->ieee8021PbbPipVipMap4Len = 0;
		}
		if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
			ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipPriorityCodePointSelection(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipPriorityCodePointSelection(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipPriorityCodePointSelection entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipPriorityCodePointSelection not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipPriorityCodePointSelection: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION_CODEPOINT8P0D:
		case IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION_CODEPOINT7P1D:
		case IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION_CODEPOINT6P2D:
		case IEEE8021PBBPIPPRIORITYCODEPOINTSELECTION_CODEPOINT5P3D:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipPriorityCodePointSelection: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old = ieee8021PbbPipPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipPriorityTable_rsvs++;
		StorageTmp->ieee8021PbbPipPriorityCodePointSelection = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipPriorityTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipPriorityCodePointSelection for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipPriorityTable_sets == 0)
				if ((ret = update_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			ieee8021PbbPipPriorityTable_destroy(&StorageTmp->ieee8021PbbPipPriorityTable_old);
			StorageTmp->ieee8021PbbPipPriorityTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipPriorityTable_tsts = 0;
			StorageTmp->ieee8021PbbPipPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipPriorityTable_sets == 0)
			revert_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbPipPriorityCodePointSelection = StorageOld->ieee8021PbbPipPriorityCodePointSelection;
		if (--StorageTmp->ieee8021PbbPipPriorityTable_rsvs == 0)
			ieee8021PbbPipPriorityTable_destroy(&StorageTmp->ieee8021PbbPipPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipUseDEI(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipUseDEI(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipUseDEI entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipUseDEI not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipUseDEI: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipUseDEI: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old = ieee8021PbbPipPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipPriorityTable_rsvs++;
		StorageTmp->ieee8021PbbPipUseDEI = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipPriorityTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipUseDEI for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipPriorityTable_sets == 0)
				if ((ret = update_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			ieee8021PbbPipPriorityTable_destroy(&StorageTmp->ieee8021PbbPipPriorityTable_old);
			StorageTmp->ieee8021PbbPipPriorityTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipPriorityTable_tsts = 0;
			StorageTmp->ieee8021PbbPipPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipPriorityTable_sets == 0)
			revert_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbPipUseDEI = StorageOld->ieee8021PbbPipUseDEI;
		if (--StorageTmp->ieee8021PbbPipPriorityTable_rsvs == 0)
			ieee8021PbbPipPriorityTable_destroy(&StorageTmp->ieee8021PbbPipPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipRequireDropEncoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipRequireDropEncoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipRequireDropEncoding entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipRequireDropEncoding not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipRequireDropEncoding: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipRequireDropEncoding: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old = ieee8021PbbPipPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipPriorityTable_rsvs++;
		StorageTmp->ieee8021PbbPipRequireDropEncoding = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipPriorityTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipRequireDropEncoding for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipPriorityTable_sets == 0)
				if ((ret = update_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) != NULL) {
			ieee8021PbbPipPriorityTable_destroy(&StorageTmp->ieee8021PbbPipPriorityTable_old);
			StorageTmp->ieee8021PbbPipPriorityTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipPriorityTable_tsts = 0;
			StorageTmp->ieee8021PbbPipPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipPriorityTable_sets == 0)
			revert_ieee8021PbbPipPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbPipRequireDropEncoding = StorageOld->ieee8021PbbPipRequireDropEncoding;
		if (--StorageTmp->ieee8021PbbPipPriorityTable_rsvs == 0)
			ieee8021PbbPipPriorityTable_destroy(&StorageTmp->ieee8021PbbPipPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipDecodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipDecodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipDecodingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipDecodingPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipDecodingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipDecodingPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipDecodingPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipDecodingPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipDecodingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old = ieee8021PbbPipDecodingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipDecodingTable_rsvs++;
		StorageTmp->ieee8021PbbPipDecodingPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipDecodingTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipDecodingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipDecodingPriority for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipDecodingTable_sets == 0)
				if ((ret = update_ieee8021PbbPipDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipDecodingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) != NULL) {
			ieee8021PbbPipDecodingTable_destroy(&StorageTmp->ieee8021PbbPipDecodingTable_old);
			StorageTmp->ieee8021PbbPipDecodingTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipDecodingTable_tsts = 0;
			StorageTmp->ieee8021PbbPipDecodingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipDecodingTable_sets == 0)
			revert_ieee8021PbbPipDecodingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbPipDecodingPriority = StorageOld->ieee8021PbbPipDecodingPriority;
		if (--StorageTmp->ieee8021PbbPipDecodingTable_rsvs == 0)
			ieee8021PbbPipDecodingTable_destroy(&StorageTmp->ieee8021PbbPipDecodingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipDecodingDropEligible(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipDecodingDropEligible(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipDecodingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipDecodingDropEligible entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipDecodingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipDecodingDropEligible not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipDecodingDropEligible: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipDecodingDropEligible: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipDecodingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old = ieee8021PbbPipDecodingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipDecodingTable_rsvs++;
		StorageTmp->ieee8021PbbPipDecodingDropEligible = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipDecodingTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipDecodingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipDecodingDropEligible for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipDecodingTable_sets == 0)
				if ((ret = update_ieee8021PbbPipDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipDecodingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) != NULL) {
			ieee8021PbbPipDecodingTable_destroy(&StorageTmp->ieee8021PbbPipDecodingTable_old);
			StorageTmp->ieee8021PbbPipDecodingTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipDecodingTable_tsts = 0;
			StorageTmp->ieee8021PbbPipDecodingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipDecodingTable_sets == 0)
			revert_ieee8021PbbPipDecodingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipDecodingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbPipDecodingDropEligible = StorageOld->ieee8021PbbPipDecodingDropEligible;
		if (--StorageTmp->ieee8021PbbPipDecodingTable_rsvs == 0)
			ieee8021PbbPipDecodingTable_destroy(&StorageTmp->ieee8021PbbPipDecodingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipEncodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbPipEncodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipEncodingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipEncodingPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbPipEncodingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipEncodingPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipEncodingPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipEncodingPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbPipEncodingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old = ieee8021PbbPipEncodingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbPipEncodingTable_rsvs++;
		StorageTmp->ieee8021PbbPipEncodingPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbPipEncodingTable_tsts == 0)
				if ((ret = check_ieee8021PbbPipEncodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipEncodingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbPipEncodingPriority for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbPipEncodingTable_sets == 0)
				if ((ret = update_ieee8021PbbPipEncodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbPipEncodingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old) != NULL) {
			ieee8021PbbPipEncodingTable_destroy(&StorageTmp->ieee8021PbbPipEncodingTable_old);
			StorageTmp->ieee8021PbbPipEncodingTable_rsvs = 0;
			StorageTmp->ieee8021PbbPipEncodingTable_tsts = 0;
			StorageTmp->ieee8021PbbPipEncodingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbPipEncodingTable_sets == 0)
			revert_ieee8021PbbPipEncodingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbPipEncodingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbPipEncodingPriority = StorageOld->ieee8021PbbPipEncodingPriority;
		if (--StorageTmp->ieee8021PbbPipEncodingTable_rsvs == 0)
			ieee8021PbbPipEncodingTable_destroy(&StorageTmp->ieee8021PbbPipEncodingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbVipToPipMappingPipIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbVipToPipMappingPipIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipToPipMappingPipIfIndex entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbVipToPipMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbVipToPipMappingRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingPipIfIndex: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingPipIfIndex not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingPipIfIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..2147483647 */
		if ((1 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingPipIfIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old = ieee8021PbbVipToPipMappingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs++;
		StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_tsts == 0)
				if ((ret = check_ieee8021PbbVipToPipMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipToPipMappingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_sets == 0)
				if ((ret = update_ieee8021PbbVipToPipMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipToPipMappingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
			ieee8021PbbVipToPipMappingTable_destroy(&StorageTmp->ieee8021PbbVipToPipMappingTable_old);
			StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs = 0;
			StorageTmp->ieee8021PbbVipToPipMappingTable_tsts = 0;
			StorageTmp->ieee8021PbbVipToPipMappingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbVipToPipMappingTable_sets == 0)
			revert_ieee8021PbbVipToPipMappingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbVipToPipMappingPipIfIndex = StorageOld->ieee8021PbbVipToPipMappingPipIfIndex;
		if (--StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs == 0)
			ieee8021PbbVipToPipMappingTable_destroy(&StorageTmp->ieee8021PbbVipToPipMappingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbVipToPipMappingStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbVipToPipMappingStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipToPipMappingStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbVipToPipMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbVipToPipMappingRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old = ieee8021PbbVipToPipMappingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs++;
		StorageTmp->ieee8021PbbVipToPipMappingStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_tsts == 0)
				if ((ret = check_ieee8021PbbVipToPipMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipToPipMappingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbVipToPipMappingStorageType for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_sets == 0)
				if ((ret = update_ieee8021PbbVipToPipMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbVipToPipMappingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
			ieee8021PbbVipToPipMappingTable_destroy(&StorageTmp->ieee8021PbbVipToPipMappingTable_old);
			StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs = 0;
			StorageTmp->ieee8021PbbVipToPipMappingTable_tsts = 0;
			StorageTmp->ieee8021PbbVipToPipMappingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbVipToPipMappingTable_sets == 0)
			revert_ieee8021PbbVipToPipMappingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbVipToPipMappingStorageType = StorageOld->ieee8021PbbVipToPipMappingStorageType;
		if (--StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs == 0)
			ieee8021PbbVipToPipMappingTable_destroy(&StorageTmp->ieee8021PbbVipToPipMappingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbCBPServiceMappingBVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbCBPServiceMappingBVid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbCBPServiceMappingBVid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbCBPServiceMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingBVid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingBVid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingBVid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4094 */
		if ((1 > set_value || set_value > 4094)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingBVid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old = ieee8021PbbCBPServiceMappingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs++;
		StorageTmp->ieee8021PbbCBPServiceMappingBVid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts == 0)
				if ((ret = check_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbCBPServiceMappingBVid for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
				if ((ret = update_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
			revert_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbCBPServiceMappingBVid = StorageOld->ieee8021PbbCBPServiceMappingBVid;
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbCBPServiceMappingDefaultBackboneDest(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbCBPServiceMappingDefaultBackboneDest(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbCBPServiceMappingDefaultBackboneDest entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbCBPServiceMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingDefaultBackboneDest: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingDefaultBackboneDest not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingDefaultBackboneDest: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old = ieee8021PbbCBPServiceMappingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest);
		StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest = string;
		StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts == 0)
				if ((ret = check_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
				if ((ret = update_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
			revert_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbCBPServiceMappingDefaultBackboneDest != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest);
			StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDest = StorageOld->ieee8021PbbCBPServiceMappingDefaultBackboneDest;
			StorageTmp->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen = StorageOld->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen;
			StorageOld->ieee8021PbbCBPServiceMappingDefaultBackboneDest = NULL;
			StorageOld->ieee8021PbbCBPServiceMappingDefaultBackboneDestLen = 0;
		}
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbCBPServiceMappingType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbCBPServiceMappingType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbCBPServiceMappingType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbCBPServiceMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingType not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingType: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingType: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old = ieee8021PbbCBPServiceMappingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbCBPServiceMappingType);
		StorageTmp->ieee8021PbbCBPServiceMappingType = string;
		StorageTmp->ieee8021PbbCBPServiceMappingTypeLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts == 0)
				if ((ret = check_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbCBPServiceMappingType for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
				if ((ret = update_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
			revert_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbCBPServiceMappingType != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbCBPServiceMappingType);
			StorageTmp->ieee8021PbbCBPServiceMappingType = StorageOld->ieee8021PbbCBPServiceMappingType;
			StorageTmp->ieee8021PbbCBPServiceMappingTypeLen = StorageOld->ieee8021PbbCBPServiceMappingTypeLen;
			StorageOld->ieee8021PbbCBPServiceMappingType = NULL;
			StorageOld->ieee8021PbbCBPServiceMappingTypeLen = 0;
		}
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbCBPServiceMappingLocalSid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021PbbCBPServiceMappingLocalSid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbCBPServiceMappingLocalSid entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021PbbCBPServiceMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingLocalSid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingLocalSid not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingLocalSid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		/* Note: ranges 1..1 256..16777214 */
		if (set_value != 1 && (256 > set_value || set_value > 16777214)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingLocalSid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old = ieee8021PbbCBPServiceMappingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs++;
		StorageTmp->ieee8021PbbCBPServiceMappingLocalSid = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts == 0)
				if ((ret = check_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbCBPServiceMappingLocalSid for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
				if ((ret = update_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
			StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts = 0;
			StorageTmp->ieee8021PbbCBPServiceMappingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_sets == 0)
			revert_ieee8021PbbCBPServiceMappingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
			break;
		StorageTmp->ieee8021PbbCBPServiceMappingLocalSid = StorageOld->ieee8021PbbCBPServiceMappingLocalSid;
		if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbBackboneEdgeBridgeName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_ieee8021PbbBackboneEdgeBridgeName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbMib_data *StorageTmp = NULL, *StorageOld = NULL;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbBackboneEdgeBridgeName entering action=%d...  \n", action));
	if ((StorageTmp = ieee8021PbbMibStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbBackboneEdgeBridgeName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbBackboneEdgeBridgeName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbMib_old) == NULL)
			if (StorageTmp->ieee8021PbbMib_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021PbbMib_old = ieee8021PbbMib_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021PbbMib_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021PbbBackboneEdgeBridgeName);
		StorageTmp->ieee8021PbbBackboneEdgeBridgeName = string;
		StorageTmp->ieee8021PbbBackboneEdgeBridgeNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021PbbMib_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->ieee8021PbbMib_tsts == 0)
				if ((ret = check_ieee8021PbbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbMib_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021PbbBackboneEdgeBridgeName for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbMib_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021PbbMib_sets == 0)
				if ((ret = update_ieee8021PbbMib(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021PbbMib_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021PbbMib_old) != NULL) {
			ieee8021PbbMib_destroy(&StorageTmp->ieee8021PbbMib_old);
			StorageTmp->ieee8021PbbMib_rsvs = 0;
			StorageTmp->ieee8021PbbMib_tsts = 0;
			StorageTmp->ieee8021PbbMib_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021PbbMib_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021PbbMib_tsts == 0)
			revert_ieee8021PbbMib(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021PbbMib_old) == NULL)
			break;
		if (StorageOld->ieee8021PbbBackboneEdgeBridgeName != NULL) {
			SNMP_FREE(StorageTmp->ieee8021PbbBackboneEdgeBridgeName);
			StorageTmp->ieee8021PbbBackboneEdgeBridgeName = StorageOld->ieee8021PbbBackboneEdgeBridgeName;
			StorageTmp->ieee8021PbbBackboneEdgeBridgeNameLen = StorageOld->ieee8021PbbBackboneEdgeBridgeNameLen;
			StorageOld->ieee8021PbbBackboneEdgeBridgeName = NULL;
			StorageOld->ieee8021PbbBackboneEdgeBridgeNameLen = 0;
		}
		if (--StorageTmp->ieee8021PbbMib_rsvs == 0)
			ieee8021PbbMib_destroy(&StorageTmp->ieee8021PbbMib_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbVipTable_row(struct ieee8021PbbVipTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbPipTable_row(struct ieee8021PbbPipTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbVipToPipMappingTable_row(struct ieee8021PbbVipToPipMappingTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbCBPServiceMappingTable_row(struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021PbbCbpTable_row(struct ieee8021PbbCbpTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbVipRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbVipRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbVipTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbVipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbVipTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbVipTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbVipTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbVipRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbVipTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
				if (StorageTmp->ieee8021PbbVipTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old = ieee8021PbbVipTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbVipTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbVipTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbVipTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbVipTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbVipRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbVipTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbVipRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbVipTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbVipRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbVipTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbVipRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbVipTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbVipRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbVipTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbVipRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbVipTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbVipTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbVipRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbVipRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbVipRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) != NULL) {
				ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
				StorageTmp->ieee8021PbbVipTable_rsvs = 0;
				StorageTmp->ieee8021PbbVipTable_tsts = 0;
				StorageTmp->ieee8021PbbVipTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbVipTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbVipTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbVipRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbVipTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbVipRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbVipTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbVipTable_del(StorageNew);
				ieee8021PbbVipTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbVipTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbVipTable_rsvs == 0)
				ieee8021PbbVipTable_destroy(&StorageTmp->ieee8021PbbVipTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbVipTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbPipRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbPipRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbPipTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbPipTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbPipRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbPipTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbPipRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021PbbPipIfIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021PbbPipIfIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbPipIfIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbPipIfIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbPipTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbPipTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021PbbPipIfIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbPipTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbPipRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbPipTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
				if (StorageTmp->ieee8021PbbPipTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old = ieee8021PbbPipTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbPipTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbPipTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbPipTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbPipTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbPipRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbPipTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbPipRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbPipTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbPipRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbPipTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbPipRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbPipTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbPipRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbPipTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbPipRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbPipTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbPipTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbPipRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbPipRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbPipRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) != NULL) {
				ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
				StorageTmp->ieee8021PbbPipTable_rsvs = 0;
				StorageTmp->ieee8021PbbPipTable_tsts = 0;
				StorageTmp->ieee8021PbbPipTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbPipTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbPipTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbPipRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbPipTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbPipRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbPipTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbPipTable_del(StorageNew);
				ieee8021PbbPipTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbPipTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbPipTable_rsvs == 0)
				ieee8021PbbPipTable_destroy(&StorageTmp->ieee8021PbbPipTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbPipTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbVipToPipMappingRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbVipToPipMappingRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbVipToPipMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbVipToPipMappingTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbVipToPipMappingRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbVipToPipMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbVipToPipMappingRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbVipToPipMappingTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbVipToPipMappingTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbVipToPipMappingTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbVipToPipMappingTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
				if (StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old = ieee8021PbbVipToPipMappingTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbVipToPipMappingTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbVipToPipMappingTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbVipToPipMappingTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbVipToPipMappingTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbVipToPipMappingTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbVipToPipMappingTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbVipToPipMappingRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbVipToPipMappingTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbVipToPipMappingTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbVipToPipMappingTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbVipToPipMappingTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbVipToPipMappingRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbVipToPipMappingRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbVipToPipMappingRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) != NULL) {
				ieee8021PbbVipToPipMappingTable_destroy(&StorageTmp->ieee8021PbbVipToPipMappingTable_old);
				StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs = 0;
				StorageTmp->ieee8021PbbVipToPipMappingTable_tsts = 0;
				StorageTmp->ieee8021PbbVipToPipMappingTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbVipToPipMappingTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbVipToPipMappingTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbVipToPipMappingTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbVipToPipMappingRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbVipToPipMappingTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbVipToPipMappingTable_del(StorageNew);
				ieee8021PbbVipToPipMappingTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbVipToPipMappingTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbVipToPipMappingTable_rsvs == 0)
				ieee8021PbbVipToPipMappingTable_destroy(&StorageTmp->ieee8021PbbVipToPipMappingTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbVipToPipMappingTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbCBPServiceMappingRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbCBPServiceMappingRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbCBPServiceMappingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbCBPServiceMappingTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbCBPServiceMappingRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbCBPServiceMappingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCBPServiceMappingRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021PbbCBPServiceMappingBackboneSid */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021PbbCBPServiceMappingBackboneSid */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbCBPServiceMappingBackboneSid: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 256..16777214 */
			if ((256 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16777214)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021PbbCBPServiceMappingBackboneSid: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbCBPServiceMappingTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbCBPServiceMappingTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021PbbCBPServiceMappingBackboneSid = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbCBPServiceMappingTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbCBPServiceMappingTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
				if (StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old = ieee8021PbbCBPServiceMappingTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbCBPServiceMappingTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbCBPServiceMappingTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbCBPServiceMappingTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbCBPServiceMappingTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbCBPServiceMappingTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbCBPServiceMappingTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbCBPServiceMappingRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbCBPServiceMappingTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbCBPServiceMappingTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbCBPServiceMappingTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbCBPServiceMappingTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbCBPServiceMappingRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbCBPServiceMappingRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbCBPServiceMappingRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) != NULL) {
				ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
				StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs = 0;
				StorageTmp->ieee8021PbbCBPServiceMappingTable_tsts = 0;
				StorageTmp->ieee8021PbbCBPServiceMappingTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbCBPServiceMappingTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbCBPServiceMappingTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbCBPServiceMappingTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbCBPServiceMappingRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbCBPServiceMappingTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbCBPServiceMappingTable_del(StorageNew);
				ieee8021PbbCBPServiceMappingTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbCBPServiceMappingTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbCBPServiceMappingTable_rsvs == 0)
				ieee8021PbbCBPServiceMappingTable_destroy(&StorageTmp->ieee8021PbbCBPServiceMappingTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbCBPServiceMappingTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021PbbCbpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021PbbCbpRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021PbbCbpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021PbbCbpTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021PbbMib", "write_ieee8021PbbCbpRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021PbbCbpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCbpRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021PbbCbpRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021PbbCbpTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021PbbCbpTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021PbbCbpTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021PbbCbpRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021PbbCbpTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021PbbCbpTable_old) == NULL)
				if (StorageTmp->ieee8021PbbCbpTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021PbbCbpTable_old = ieee8021PbbCbpTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021PbbCbpTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021PbbCbpTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021PbbCbpTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021PbbCbpTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021PbbCbpRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021PbbCbpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbCbpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbCbpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021PbbCbpRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021PbbCbpTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021PbbCbpRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021PbbCbpTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbCbpRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021PbbCbpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021PbbCbpRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021PbbCbpTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021PbbCbpTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021PbbCbpRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021PbbCbpRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021PbbCbpRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021PbbCbpTable_old) != NULL) {
				ieee8021PbbCbpTable_destroy(&StorageTmp->ieee8021PbbCbpTable_old);
				StorageTmp->ieee8021PbbCbpTable_rsvs = 0;
				StorageTmp->ieee8021PbbCbpTable_tsts = 0;
				StorageTmp->ieee8021PbbCbpTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021PbbCbpTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021PbbCbpTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021PbbCbpRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021PbbCbpTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021PbbCbpRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021PbbCbpTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021PbbCbpTable_del(StorageNew);
				ieee8021PbbCbpTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021PbbCbpTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021PbbCbpTable_rsvs == 0)
				ieee8021PbbCbpTable_destroy(&StorageTmp->ieee8021PbbCbpTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021PbbCbpTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021PbbMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021PbbMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021PbbMibold_signal_handler != NULL)
		(*ieee8021PbbMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
}

/**
 * @fn void ieee8021PbbMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021PbbMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021PbbMib", "ieee8021PbbMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021PbbMib", "done.\n"));
	return;
}
