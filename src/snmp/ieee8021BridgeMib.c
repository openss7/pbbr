/*****************************************************************************

 @(#) src/snmp/ieee8021bridgemib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/ieee8021bridgemib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ieee8021BridgeMib.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "ieee8021BridgeMib";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int ieee8021BridgeMib_refresh = 1;
volatile int ieee8021BridgeBaseTable_refresh = 1;
volatile int ieee8021BridgeBasePortTable_refresh = 1;
volatile int ieee8021BridgeBaseIfToPortTable_refresh = 1;
volatile int ieee8021BridgePhyPortTable_refresh = 1;
volatile int ieee8021BridgeTpPortTable_refresh = 1;
volatile int ieee8021BridgePortPriorityTable_refresh = 1;
volatile int ieee8021BridgeUserPriorityRegenTable_refresh = 1;
volatile int ieee8021BridgeTrafficClassTable_refresh = 1;
volatile int ieee8021BridgePortOutboundAccessPriorityTable_refresh = 1;
volatile int ieee8021BridgePortDecodingTable_refresh = 1;
volatile int ieee8021BridgePortEncodingTable_refresh = 1;
volatile int ieee8021BridgeServiceAccessPriorityTable_refresh = 1;
volatile int ieee8021BridgePortMrpTable_refresh = 1;
volatile int ieee8021BridgePortMmrpTable_refresh = 1;
volatile int ieee8021BridgeILanIfTable_refresh = 1;
volatile int ieee8021BridgeDot1dPortTable_refresh = 1;

/*
 * ieee8021BridgeMib_variables_oid: object identifier for ieee8021BridgeMib
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ieee8021BridgeMib_variables_oid[8] = { 1, 3, 111, 2, 802, 1, 1, 2 };
oid ieee8021BridgeBaseTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 2, 1, 1, 1, 1 };
oid ieee8021BridgeILanIfTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 2, 1, 6, 1, 1 };
oid ieee8021BridgeDot1dPortTable_variables_oid[12] = { 1, 3, 111, 2, 802, 1, 1, 2, 1, 7, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ieee8021BridgeCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 1, 1 };
oid ieee8021BridgePriorityAndMulticastFilteringCompliance_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 1, 2 };
oid ieee8021BridgeCompliance1_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 1, 3 };
oid ieee8021BridgeBaseBridgeGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 1 };
oid ieee8021BridgeBasePortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 2 };
oid ieee8021BridgeCapGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 3 };
oid ieee8021BridgeDeviceMmrpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 4 };
oid ieee8021BridgeTpPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 6 };
oid ieee8021BridgeDevicePriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 7 };
oid ieee8021BridgeDefaultPriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 8 };
oid ieee8021BridgeRegenPriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 9 };
oid ieee8021BridgePriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 10 };
oid ieee8021BridgeAccessPriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 11 };
oid ieee8021BridgePortMrpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 12 };
oid ieee8021BridgePortMmrpGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 13 };
oid ieee8021BridgePortDecodingGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 14 };
oid ieee8021BridgePortEncodingGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 15 };
oid ieee8021BridgeServiceAccessPriorityGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 16 };
oid ieee8021BridgeInternalLANGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 17 };
oid ieee8021BridgeCreatableBaseBridgeGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 18 };
oid ieee8021BridgeDot1dDynamicPortCreationGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 19 };
oid ieee8021BridgeBaseIfToPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 20 };
oid ieee8021BridgePhyPortGroup_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 2, 2, 2, 21 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 ieee8021BridgeMib_variables: tree for ieee8021BridgeMib
 * This variable defines function callbacks and type return information for the ieee8021BridgeMib mib section
 */
struct variable7 ieee8021BridgeMib_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   IEEE8021BRIDGEBASEBRIDGEADDRESS  1
	{(u_char) IEEE8021BRIDGEBASEBRIDGEADDRESS, ASN_OCTET_STR, RWRITE, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 2}},
#define   IEEE8021BRIDGEBASENUMPORTS  2
	{(u_char) IEEE8021BRIDGEBASENUMPORTS, ASN_INTEGER, RONLY, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 3}},
#define   IEEE8021BRIDGEBASECOMPONENTTYPE  3
	{(u_char) IEEE8021BRIDGEBASECOMPONENTTYPE, ASN_INTEGER, RWRITE, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 4}},
#define   IEEE8021BRIDGEBASEDEVICECAPABILITIES  4
	{(u_char) IEEE8021BRIDGEBASEDEVICECAPABILITIES, ASN_OCTET_STR, RWRITE, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 5}},
#define   IEEE8021BRIDGEBASETRAFFICCLASSESENABLED  5
	{(u_char) IEEE8021BRIDGEBASETRAFFICCLASSESENABLED, ASN_INTEGER, RWRITE, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 6}},
#define   IEEE8021BRIDGEBASEMMRPENABLEDSTATUS  6
	{(u_char) IEEE8021BRIDGEBASEMMRPENABLEDSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 7}},
#define   IEEE8021BRIDGEBASEROWSTATUS  7
	{(u_char) IEEE8021BRIDGEBASEROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgeBaseTable, 5, {1, 1, 1, 1, 8}},
#define   IEEE8021BRIDGEBASEPORTIFINDEX  8
	{(u_char) IEEE8021BRIDGEBASEPORTIFINDEX, ASN_INTEGER, RWRITE, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 3}},
#define   IEEE8021BRIDGEBASEPORTDELAYEXCEEDEDDISCARDS  9
	{(u_char) IEEE8021BRIDGEBASEPORTDELAYEXCEEDEDDISCARDS, ASN_COUNTER64, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 4}},
#define   IEEE8021BRIDGEBASEPORTMTUEXCEEDEDDISCARDS  10
	{(u_char) IEEE8021BRIDGEBASEPORTMTUEXCEEDEDDISCARDS, ASN_COUNTER64, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 5}},
#define   IEEE8021BRIDGEBASEPORTCAPABILITIES  11
	{(u_char) IEEE8021BRIDGEBASEPORTCAPABILITIES, ASN_OCTET_STR, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 6}},
#define   IEEE8021BRIDGEBASEPORTTYPECAPABILITIES  12
	{(u_char) IEEE8021BRIDGEBASEPORTTYPECAPABILITIES, ASN_OCTET_STR, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 7}},
#define   IEEE8021BRIDGEBASEPORTTYPE  13
	{(u_char) IEEE8021BRIDGEBASEPORTTYPE, ASN_INTEGER, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 8}},
#define   IEEE8021BRIDGEBASEPORTEXTERNAL  14
	{(u_char) IEEE8021BRIDGEBASEPORTEXTERNAL, ASN_INTEGER, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 9}},
#define   IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT  15
	{(u_char) IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT, ASN_INTEGER, RWRITE, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 10}},
#define   IEEE8021BRIDGEBASEPORTOPERPOINTTOPOINT  16
	{(u_char) IEEE8021BRIDGEBASEPORTOPERPOINTTOPOINT, ASN_INTEGER, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 11}},
#define   IEEE8021BRIDGEBASEPORTNAME  17
	{(u_char) IEEE8021BRIDGEBASEPORTNAME, ASN_OCTET_STR, RONLY, var_ieee8021BridgeBasePortTable, 5, {1, 1, 4, 1, 12}},
#define   IEEE8021BRIDGEBASEIFINDEXCOMPONENTID  19
	{(u_char) IEEE8021BRIDGEBASEIFINDEXCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021BridgeBaseIfToPortTable, 5, {1, 1, 5, 1, 1}},
#define   IEEE8021BRIDGEBASEIFINDEXPORT  20
	{(u_char) IEEE8021BRIDGEBASEIFINDEXPORT, ASN_UNSIGNED, RONLY, var_ieee8021BridgeBaseIfToPortTable, 5, {1, 1, 5, 1, 2}},
#define   IEEE8021BRIDGEPHYPORTIFINDEX  21
	{(u_char) IEEE8021BRIDGEPHYPORTIFINDEX, ASN_INTEGER, RONLY, var_ieee8021BridgePhyPortTable, 5, {1, 1, 6, 1, 2}},
#define   IEEE8021BRIDGEPHYMACADDRESS  22
	{(u_char) IEEE8021BRIDGEPHYMACADDRESS, ASN_OCTET_STR, RONLY, var_ieee8021BridgePhyPortTable, 5, {1, 1, 6, 1, 3}},
#define   IEEE8021BRIDGEPHYPORTTOCOMPONENTID  23
	{(u_char) IEEE8021BRIDGEPHYPORTTOCOMPONENTID, ASN_UNSIGNED, RONLY, var_ieee8021BridgePhyPortTable, 5, {1, 1, 6, 1, 4}},
#define   IEEE8021BRIDGEPHYPORTTOINTERNALPORT  24
	{(u_char) IEEE8021BRIDGEPHYPORTTOINTERNALPORT, ASN_UNSIGNED, RONLY, var_ieee8021BridgePhyPortTable, 5, {1, 1, 6, 1, 5}},
#define   IEEE8021BRIDGETPPORTMAXINFO  25
	{(u_char) IEEE8021BRIDGETPPORTMAXINFO, ASN_INTEGER, RONLY, var_ieee8021BridgeTpPortTable, 5, {1, 2, 1, 1, 3}},
#define   IEEE8021BRIDGETPPORTINFRAMES  26
	{(u_char) IEEE8021BRIDGETPPORTINFRAMES, ASN_COUNTER64, RONLY, var_ieee8021BridgeTpPortTable, 5, {1, 2, 1, 1, 4}},
#define   IEEE8021BRIDGETPPORTOUTFRAMES  27
	{(u_char) IEEE8021BRIDGETPPORTOUTFRAMES, ASN_COUNTER64, RONLY, var_ieee8021BridgeTpPortTable, 5, {1, 2, 1, 1, 5}},
#define   IEEE8021BRIDGETPPORTINDISCARDS  28
	{(u_char) IEEE8021BRIDGETPPORTINDISCARDS, ASN_COUNTER64, RONLY, var_ieee8021BridgeTpPortTable, 5, {1, 2, 1, 1, 6}},
#define   IEEE8021BRIDGEPORTDEFAULTUSERPRIORITY  29
	{(u_char) IEEE8021BRIDGEPORTDEFAULTUSERPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021BridgePortPriorityTable, 5, {1, 3, 1, 1, 1}},
#define   IEEE8021BRIDGEPORTNUMTRAFFICCLASSES  30
	{(u_char) IEEE8021BRIDGEPORTNUMTRAFFICCLASSES, ASN_INTEGER, RWRITE, var_ieee8021BridgePortPriorityTable, 5, {1, 3, 1, 1, 2}},
#define   IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION  31
	{(u_char) IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION, ASN_INTEGER, RWRITE, var_ieee8021BridgePortPriorityTable, 5, {1, 3, 1, 1, 3}},
#define   IEEE8021BRIDGEPORTUSEDEI  32
	{(u_char) IEEE8021BRIDGEPORTUSEDEI, ASN_INTEGER, RWRITE, var_ieee8021BridgePortPriorityTable, 5, {1, 3, 1, 1, 4}},
#define   IEEE8021BRIDGEPORTREQUIREDROPENCODING  33
	{(u_char) IEEE8021BRIDGEPORTREQUIREDROPENCODING, ASN_INTEGER, RWRITE, var_ieee8021BridgePortPriorityTable, 5, {1, 3, 1, 1, 5}},
#define   IEEE8021BRIDGEPORTSERVICEACCESSPRIORITYSELECTION  34
	{(u_char) IEEE8021BRIDGEPORTSERVICEACCESSPRIORITYSELECTION, ASN_INTEGER, RWRITE, var_ieee8021BridgePortPriorityTable, 5, {1, 3, 1, 1, 6}},
#define   IEEE8021BRIDGEREGENUSERPRIORITY  35
	{(u_char) IEEE8021BRIDGEREGENUSERPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeUserPriorityRegenTable, 5, {1, 3, 2, 1, 2}},
#define   IEEE8021BRIDGETRAFFICCLASS  36
	{(u_char) IEEE8021BRIDGETRAFFICCLASS, ASN_INTEGER, RWRITE, var_ieee8021BridgeTrafficClassTable, 5, {1, 3, 3, 1, 2}},
#define   IEEE8021BRIDGEPORTOUTBOUNDACCESSPRIORITY  38
	{(u_char) IEEE8021BRIDGEPORTOUTBOUNDACCESSPRIORITY, ASN_UNSIGNED, RONLY, var_ieee8021BridgePortOutboundAccessPriorityTable, 5, {1, 3, 4, 1, 1}},
#define   IEEE8021BRIDGEPORTDECODINGPRIORITY  39
	{(u_char) IEEE8021BRIDGEPORTDECODINGPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021BridgePortDecodingTable, 5, {1, 3, 5, 1, 5}},
#define   IEEE8021BRIDGEPORTDECODINGDROPELIGIBLE  40
	{(u_char) IEEE8021BRIDGEPORTDECODINGDROPELIGIBLE, ASN_INTEGER, RWRITE, var_ieee8021BridgePortDecodingTable, 5, {1, 3, 5, 1, 6}},
#define   IEEE8021BRIDGEPORTENCODINGPRIORITY  41
	{(u_char) IEEE8021BRIDGEPORTENCODINGPRIORITY, ASN_UNSIGNED, RWRITE, var_ieee8021BridgePortEncodingTable, 5, {1, 3, 6, 1, 6}},
#define   IEEE8021BRIDGESERVICEACCESSPRIORITYVALUE  42
	{(u_char) IEEE8021BRIDGESERVICEACCESSPRIORITYVALUE, ASN_UNSIGNED, RWRITE, var_ieee8021BridgeServiceAccessPriorityTable, 5, {1, 3, 7, 1, 4}},
#define   IEEE8021BRIDGEPORTMRPJOINTIME  43
	{(u_char) IEEE8021BRIDGEPORTMRPJOINTIME, ASN_INTEGER, RWRITE, var_ieee8021BridgePortMrpTable, 5, {1, 4, 1, 1, 1}},
#define   IEEE8021BRIDGEPORTMRPLEAVETIME  44
	{(u_char) IEEE8021BRIDGEPORTMRPLEAVETIME, ASN_INTEGER, RWRITE, var_ieee8021BridgePortMrpTable, 5, {1, 4, 1, 1, 2}},
#define   IEEE8021BRIDGEPORTMRPLEAVEALLTIME  45
	{(u_char) IEEE8021BRIDGEPORTMRPLEAVEALLTIME, ASN_INTEGER, RWRITE, var_ieee8021BridgePortMrpTable, 5, {1, 4, 1, 1, 3}},
#define   IEEE8021BRIDGEPORTMMRPENABLEDSTATUS  46
	{(u_char) IEEE8021BRIDGEPORTMMRPENABLEDSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgePortMmrpTable, 5, {1, 5, 1, 1, 1}},
#define   IEEE8021BRIDGEPORTMMRPFAILEDREGISTRATIONS  47
	{(u_char) IEEE8021BRIDGEPORTMMRPFAILEDREGISTRATIONS, ASN_COUNTER64, RONLY, var_ieee8021BridgePortMmrpTable, 5, {1, 5, 1, 1, 2}},
#define   IEEE8021BRIDGEPORTMMRPLASTPDUORIGIN  48
	{(u_char) IEEE8021BRIDGEPORTMMRPLASTPDUORIGIN, ASN_OCTET_STR, RONLY, var_ieee8021BridgePortMmrpTable, 5, {1, 5, 1, 1, 3}},
#define   IEEE8021BRIDGEPORTRESTRICTEDGROUPREGISTRATION  49
	{(u_char) IEEE8021BRIDGEPORTRESTRICTEDGROUPREGISTRATION, ASN_INTEGER, RWRITE, var_ieee8021BridgePortMmrpTable, 5, {1, 5, 1, 1, 4}},
#define   IEEE8021BRIDGEILANIFROWSTATUS  51
	{(u_char) IEEE8021BRIDGEILANIFROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgeILanIfTable, 5, {1, 6, 1, 1, 1}},
#define   IEEE8021BRIDGEDOT1DPORTROWSTATUS  52
	{(u_char) IEEE8021BRIDGEDOT1DPORTROWSTATUS, ASN_INTEGER, RWRITE, var_ieee8021BridgeDot1dPortTable, 5, {1, 7, 1, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct ieee8021BridgeMib_data *ieee8021BridgeMibStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *ieee8021BridgeBaseTableStorage = NULL;
struct header_complex_index *ieee8021BridgeBasePortTableStorage = NULL;
struct header_complex_index *ieee8021BridgeBaseIfToPortTableStorage = NULL;
struct header_complex_index *ieee8021BridgePhyPortTableStorage = NULL;
struct header_complex_index *ieee8021BridgeTpPortTableStorage = NULL;
struct header_complex_index *ieee8021BridgePortPriorityTableStorage = NULL;
struct header_complex_index *ieee8021BridgeUserPriorityRegenTableStorage = NULL;
struct header_complex_index *ieee8021BridgeTrafficClassTableStorage = NULL;
struct header_complex_index *ieee8021BridgePortOutboundAccessPriorityTableStorage = NULL;
struct header_complex_index *ieee8021BridgePortDecodingTableStorage = NULL;
struct header_complex_index *ieee8021BridgePortEncodingTableStorage = NULL;
struct header_complex_index *ieee8021BridgeServiceAccessPriorityTableStorage = NULL;
struct header_complex_index *ieee8021BridgePortMrpTableStorage = NULL;
struct header_complex_index *ieee8021BridgePortMmrpTableStorage = NULL;
struct header_complex_index *ieee8021BridgeILanIfTableStorage = NULL;
struct header_complex_index *ieee8021BridgeDot1dPortTableStorage = NULL;

void (*ieee8021BridgeMibold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ieee8021BridgeMib_loop_handler(int);
void ieee8021BridgeMib_fd_handler(int, void *);

/**
 * @fn void init_ieee8021BridgeMib(void)
 * @brief ieee8021BridgeMib initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ieee8021BridgeMib_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ieee8021BridgeMib(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ieee8021BridgeMib", "init_ieee8021BridgeMib: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ieee8021BridgeMib", ieee8021BridgeMib_variables, variable7, ieee8021BridgeMib_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ieee8021BridgeMib, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ieee8021BridgeMib", parse_ieee8021BridgeMib, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeBaseTable", parse_ieee8021BridgeBaseTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeBasePortTable", parse_ieee8021BridgeBasePortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeBaseIfToPortTable", parse_ieee8021BridgeBaseIfToPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePhyPortTable", parse_ieee8021BridgePhyPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeTpPortTable", parse_ieee8021BridgeTpPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePortPriorityTable", parse_ieee8021BridgePortPriorityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeUserPriorityRegenTable", parse_ieee8021BridgeUserPriorityRegenTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeTrafficClassTable", parse_ieee8021BridgeTrafficClassTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePortOutboundAccessPriorityTable", parse_ieee8021BridgePortOutboundAccessPriorityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePortDecodingTable", parse_ieee8021BridgePortDecodingTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePortEncodingTable", parse_ieee8021BridgePortEncodingTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeServiceAccessPriorityTable", parse_ieee8021BridgeServiceAccessPriorityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePortMrpTable", parse_ieee8021BridgePortMrpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgePortMmrpTable", parse_ieee8021BridgePortMmrpTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeILanIfTable", parse_ieee8021BridgeILanIfTable, NULL, "HELP STRING");
	snmpd_register_config_handler("ieee8021BridgeDot1dPortTable", parse_ieee8021BridgeDot1dPortTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeMib, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeBaseTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeBasePortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeBaseIfToPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePhyPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeTpPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePortPriorityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeUserPriorityRegenTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeTrafficClassTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePortOutboundAccessPriorityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePortDecodingTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePortEncodingTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeServiceAccessPriorityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePortMrpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgePortMmrpTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeILanIfTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ieee8021BridgeDot1dPortTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, ieee8021BridgeMib_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, ieee8021BridgeMib_fd_handler, (void *) 1);
	}
	ieee8021BridgeMibold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ieee8021BridgeMib_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/**
 * @fn void deinit_ieee8021BridgeMib(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ieee8021BridgeMib_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ieee8021BridgeMib(void)
{
	DEBUGMSGTL(("ieee8021BridgeMib", "deinit_ieee8021BridgeMib: deinitializating...  "));
	external_signal_handler[SIGCHLD] = ieee8021BridgeMibold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(ieee8021BridgeMib_variables_oid, sizeof(ieee8021BridgeMib_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ieee8021BridgeMib");
	snmpd_unregister_config_handler("ieee8021BridgeBaseTable");
	snmpd_unregister_config_handler("ieee8021BridgeBasePortTable");
	snmpd_unregister_config_handler("ieee8021BridgeBaseIfToPortTable");
	snmpd_unregister_config_handler("ieee8021BridgePhyPortTable");
	snmpd_unregister_config_handler("ieee8021BridgeTpPortTable");
	snmpd_unregister_config_handler("ieee8021BridgePortPriorityTable");
	snmpd_unregister_config_handler("ieee8021BridgeUserPriorityRegenTable");
	snmpd_unregister_config_handler("ieee8021BridgeTrafficClassTable");
	snmpd_unregister_config_handler("ieee8021BridgePortOutboundAccessPriorityTable");
	snmpd_unregister_config_handler("ieee8021BridgePortDecodingTable");
	snmpd_unregister_config_handler("ieee8021BridgePortEncodingTable");
	snmpd_unregister_config_handler("ieee8021BridgeServiceAccessPriorityTable");
	snmpd_unregister_config_handler("ieee8021BridgePortMrpTable");
	snmpd_unregister_config_handler("ieee8021BridgePortMmrpTable");
	snmpd_unregister_config_handler("ieee8021BridgeILanIfTable");
	snmpd_unregister_config_handler("ieee8021BridgeDot1dPortTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

int
term_ieee8021BridgeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("ieee8021BridgeMib", "term_ieee8021BridgeMib: terminating...  "));
	deinit_ieee8021BridgeMib();
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return 0;
}

/**
 * @fn struct ieee8021BridgeMib_data *ieee8021BridgeMib_create(void)
 * @brief create a fresh data structure representing scalars in ieee8021BridgeMib.
 *
 * Creates a new ieee8021BridgeMib_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ieee8021BridgeMib.
 */
struct ieee8021BridgeMib_data *
ieee8021BridgeMib_create(void)
{
	struct ieee8021BridgeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeMib_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeMib_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeMib_data *ieee8021BridgeMib_duplicate(struct ieee8021BridgeMib_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct ieee8021BridgeMib_data *
ieee8021BridgeMib_duplicate(struct ieee8021BridgeMib_data *thedata)
{
	struct ieee8021BridgeMib_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeMib_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeMib_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeMib_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeMib_destroy(struct ieee8021BridgeMib_data **thedata)
 * @param thedata pointer to the data structure in ieee8021BridgeMib.
 * @brief delete a scalars structure from ieee8021BridgeMib.
 *
 * Frees scalars that were previously removed from ieee8021BridgeMib.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeMib_destroy(struct ieee8021BridgeMib_data **thedata)
{
	struct ieee8021BridgeMib_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeMib_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeMib_add(struct ieee8021BridgeMib_data *thedata)
 * @param thedata the structure representing ieee8021BridgeMib scalars.
 * @brief adds node to the ieee8021BridgeMib scalar data set.
 *
 * Adds a scalar structure to the ieee8021BridgeMib data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ieee8021BridgeMib_add(struct ieee8021BridgeMib_data *thedata)
{
	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeMib_add: adding data...  "));
	if (thedata)
		ieee8021BridgeMibStorage = thedata;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeMib(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeMib entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeMib).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ieee8021BridgeMib(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeMib_data *StorageTmp = ieee8021BridgeMib_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeMib: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ieee8021BridgeMib_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeMib(): stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeMib(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeMib_data *StorageTmp;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeMib: storing data...  "));
	refresh_ieee8021BridgeMib(1);
	if ((StorageTmp = ieee8021BridgeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021BridgeMib", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ieee8021BridgeMib ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_ieee8021BridgeMib(struct ieee8021BridgeMib_data *StorageTmp, struct ieee8021BridgeMib_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_ieee8021BridgeMib(struct ieee8021BridgeMib_data *StorageTmp, struct ieee8021BridgeMib_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeMib(struct ieee8021BridgeMib_data *StorageTmp, struct ieee8021BridgeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_ieee8021BridgeMib()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeMib(struct ieee8021BridgeMib_data *StorageTmp, struct ieee8021BridgeMib_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeMib_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_ieee8021BridgeMib(struct 
 * @fn void revert_ieee8021BridgeMib(struct ieee8021BridgeMib_data *StorageTmp, struct ieee8021BridgeMib_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_ieee8021BridgeMib(struct ieee8021BridgeMib_data *StorageTmp, struct ieee8021BridgeMib_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeMib(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeMib(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ieee8021BridgeMib.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ieee8021BridgeMib(int force)
{
	if (ieee8021BridgeMibStorage == NULL) {
		struct ieee8021BridgeMib_data *StorageNew;

		if ((StorageNew = ieee8021BridgeMib_create()) == NULL)
			return;
		ieee8021BridgeMibStorage = StorageNew;
		ieee8021BridgeMib_refresh = 1;
	}
	if (!force && ieee8021BridgeMib_refresh == 0)
		return;
	DEBUGMSGTL(("ieee8021BridgeMib", "refresh_ieee8021BridgeMib: refreshing...  "));
	/* XXX: Update scalars as required here... */
	ieee8021BridgeMib_refresh = 0;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/**
 * @fn u_char * var_ieee8021BridgeMib(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ieee8021BridgeMib.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ieee8021BridgeMib(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeMib_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeMib: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ieee8021BridgeMib(0);
	if ((StorageTmp = ieee8021BridgeMibStorage) == NULL) {
		DEBUGMSGTL(("ieee8021BridgeMib", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("ieee8021BridgeMib", "found.\n"));
	else
		DEBUGMSGTL(("ieee8021BridgeMib", "not found.\n"));
	return (rval);
}

/**
 * @fn struct ieee8021BridgeBaseTable_data *ieee8021BridgeBaseTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeBaseTable table.
 *
 * Creates a new ieee8021BridgeBaseTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeBaseTable_data *
ieee8021BridgeBaseTable_create(void)
{
	struct ieee8021BridgeBaseTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeBaseTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->ieee8021BridgeBaseBridgeAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgeBaseBridgeAddressLen = 0;
		StorageNew->ieee8021BridgeBaseBridgeAddress[StorageNew->ieee8021BridgeBaseBridgeAddressLen] = 0;
		StorageNew->ieee8021BridgeBaseNumPorts = 0;
		StorageNew->ieee8021BridgeBaseComponentType = 0;
		if (memdup((u_char **) &StorageNew->ieee8021BridgeBaseDeviceCapabilities, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021BridgeBaseDeviceCapabilitiesLen = 1;
		StorageNew->ieee8021BridgeBaseTrafficClassesEnabled = TV_TRUE;
		StorageNew->ieee8021BridgeBaseMmrpEnabledStatus = TV_TRUE;
		StorageNew->ieee8021BridgeBaseRowStatus = 0;
		StorageNew->ieee8021BridgeBaseRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeBaseTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeBaseTable_data *ieee8021BridgeBaseTable_duplicate(struct ieee8021BridgeBaseTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeBaseTable_data *
ieee8021BridgeBaseTable_duplicate(struct ieee8021BridgeBaseTable_data *thedata)
{
	struct ieee8021BridgeBaseTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeBaseTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeBaseTable_id = thedata->ieee8021BridgeBaseTable_id;
		StorageNew->ieee8021BridgeBaseComponentId = thedata->ieee8021BridgeBaseComponentId;
		if (!(StorageNew->ieee8021BridgeBaseBridgeAddress = malloc(thedata->ieee8021BridgeBaseBridgeAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeBaseBridgeAddress, thedata->ieee8021BridgeBaseBridgeAddress, thedata->ieee8021BridgeBaseBridgeAddressLen);
		StorageNew->ieee8021BridgeBaseBridgeAddressLen = thedata->ieee8021BridgeBaseBridgeAddressLen;
		StorageNew->ieee8021BridgeBaseBridgeAddress[StorageNew->ieee8021BridgeBaseBridgeAddressLen] = 0;
		StorageNew->ieee8021BridgeBaseNumPorts = thedata->ieee8021BridgeBaseNumPorts;
		StorageNew->ieee8021BridgeBaseComponentType = thedata->ieee8021BridgeBaseComponentType;
		if (!(StorageNew->ieee8021BridgeBaseDeviceCapabilities = malloc(thedata->ieee8021BridgeBaseDeviceCapabilitiesLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeBaseDeviceCapabilities, thedata->ieee8021BridgeBaseDeviceCapabilities, thedata->ieee8021BridgeBaseDeviceCapabilitiesLen);
		StorageNew->ieee8021BridgeBaseDeviceCapabilitiesLen = thedata->ieee8021BridgeBaseDeviceCapabilitiesLen;
		StorageNew->ieee8021BridgeBaseDeviceCapabilities[StorageNew->ieee8021BridgeBaseDeviceCapabilitiesLen] = 0;
		StorageNew->ieee8021BridgeBaseTrafficClassesEnabled = thedata->ieee8021BridgeBaseTrafficClassesEnabled;
		StorageNew->ieee8021BridgeBaseMmrpEnabledStatus = thedata->ieee8021BridgeBaseMmrpEnabledStatus;
		StorageNew->ieee8021BridgeBaseRowStatus = thedata->ieee8021BridgeBaseRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeBaseTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeBaseTable_destroy(struct ieee8021BridgeBaseTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeBaseTable_destroy(struct ieee8021BridgeBaseTable_data **thedata)
{
	struct ieee8021BridgeBaseTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021BridgeBaseBridgeAddress);
		StorageDel->ieee8021BridgeBaseBridgeAddressLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeBaseDeviceCapabilities);
		StorageDel->ieee8021BridgeBaseDeviceCapabilitiesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeBaseTable_add(struct ieee8021BridgeBaseTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeBaseTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeBaseTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeBaseTable_add(struct ieee8021BridgeBaseTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBaseComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBaseComponentId, sizeof(thedata->ieee8021BridgeBaseComponentId));
		header_complex_add_data(&ieee8021BridgeBaseTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeBaseTable_del(struct ieee8021BridgeBaseTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeBaseTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeBaseTable_del(struct ieee8021BridgeBaseTable_data *thedata)
{
	struct ieee8021BridgeBaseTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeBaseTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeBaseTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeBaseTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeBaseTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeBaseTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeBaseTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeBaseTable_data *StorageTmp = ieee8021BridgeBaseTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeBaseTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeBaseBridgeAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeBaseBridgeAddress, &StorageTmp->ieee8021BridgeBaseBridgeAddressLen);
	if (StorageTmp->ieee8021BridgeBaseBridgeAddress == NULL) {
		config_perror("invalid specification for ieee8021BridgeBaseBridgeAddress");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBaseNumPorts, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBaseComponentType, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeBaseDeviceCapabilities);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeBaseDeviceCapabilities, &StorageTmp->ieee8021BridgeBaseDeviceCapabilitiesLen);
	if (StorageTmp->ieee8021BridgeBaseDeviceCapabilities == NULL) {
		config_perror("invalid specification for ieee8021BridgeBaseDeviceCapabilities");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBaseRowStatus, &tmpsize);
	ieee8021BridgeBaseTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeBaseTable(): store configuraiton file for ieee8021BridgeBaseTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeBaseTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeBaseTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeBaseTable: storing data...  "));
	refresh_ieee8021BridgeBaseTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeBaseTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeBaseTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeBaseTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeBaseBridgeAddress, &StorageTmp->ieee8021BridgeBaseBridgeAddressLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBaseNumPorts, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBaseComponentType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeBaseDeviceCapabilities, &StorageTmp->ieee8021BridgeBaseDeviceCapabilitiesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBaseRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeBasePortTable_data *ieee8021BridgeBasePortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeBasePortTable table.
 *
 * Creates a new ieee8021BridgeBasePortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeBasePortTable_data *
ieee8021BridgeBasePortTable_create(void)
{
	struct ieee8021BridgeBasePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeBasePortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBasePortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortIfIndex = 0;
		StorageNew->ieee8021BridgeBasePortDelayExceededDiscards = (struct counter64) {
		0, 0};
		StorageNew->ieee8021BridgeBasePortMtuExceededDiscards = (struct counter64) {
		0, 0};
		if (memdup((u_char **) &StorageNew->ieee8021BridgeBasePortCapabilities, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021BridgeBasePortCapabilitiesLen = 1;
		if (memdup((u_char **) &StorageNew->ieee8021BridgeBasePortTypeCapabilities, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->ieee8021BridgeBasePortTypeCapabilitiesLen = 2;
		StorageNew->ieee8021BridgeBasePortType = 0;
		StorageNew->ieee8021BridgeBasePortExternal = 0;
		StorageNew->ieee8021BridgeBasePortAdminPointToPoint = IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT_FORCEFALSE;
		StorageNew->ieee8021BridgeBasePortOperPointToPoint = 0;
		if ((StorageNew->ieee8021BridgeBasePortName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgeBasePortNameLen = 0;
		StorageNew->ieee8021BridgeBasePortName[StorageNew->ieee8021BridgeBasePortNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeBasePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeBasePortTable_data *ieee8021BridgeBasePortTable_duplicate(struct ieee8021BridgeBasePortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeBasePortTable_data *
ieee8021BridgeBasePortTable_duplicate(struct ieee8021BridgeBasePortTable_data *thedata)
{
	struct ieee8021BridgeBasePortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeBasePortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBasePortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeBasePortTable_id = thedata->ieee8021BridgeBasePortTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgeBasePortIfIndex = thedata->ieee8021BridgeBasePortIfIndex;
		StorageNew->ieee8021BridgeBasePortDelayExceededDiscards = thedata->ieee8021BridgeBasePortDelayExceededDiscards;
		StorageNew->ieee8021BridgeBasePortMtuExceededDiscards = thedata->ieee8021BridgeBasePortMtuExceededDiscards;
		if (!(StorageNew->ieee8021BridgeBasePortCapabilities = malloc(thedata->ieee8021BridgeBasePortCapabilitiesLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeBasePortCapabilities, thedata->ieee8021BridgeBasePortCapabilities, thedata->ieee8021BridgeBasePortCapabilitiesLen);
		StorageNew->ieee8021BridgeBasePortCapabilitiesLen = thedata->ieee8021BridgeBasePortCapabilitiesLen;
		StorageNew->ieee8021BridgeBasePortCapabilities[StorageNew->ieee8021BridgeBasePortCapabilitiesLen] = 0;
		if (!(StorageNew->ieee8021BridgeBasePortTypeCapabilities = malloc(thedata->ieee8021BridgeBasePortTypeCapabilitiesLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeBasePortTypeCapabilities, thedata->ieee8021BridgeBasePortTypeCapabilities, thedata->ieee8021BridgeBasePortTypeCapabilitiesLen);
		StorageNew->ieee8021BridgeBasePortTypeCapabilitiesLen = thedata->ieee8021BridgeBasePortTypeCapabilitiesLen;
		StorageNew->ieee8021BridgeBasePortTypeCapabilities[StorageNew->ieee8021BridgeBasePortTypeCapabilitiesLen] = 0;
		StorageNew->ieee8021BridgeBasePortType = thedata->ieee8021BridgeBasePortType;
		StorageNew->ieee8021BridgeBasePortExternal = thedata->ieee8021BridgeBasePortExternal;
		StorageNew->ieee8021BridgeBasePortAdminPointToPoint = thedata->ieee8021BridgeBasePortAdminPointToPoint;
		StorageNew->ieee8021BridgeBasePortOperPointToPoint = thedata->ieee8021BridgeBasePortOperPointToPoint;
		if (!(StorageNew->ieee8021BridgeBasePortName = malloc(thedata->ieee8021BridgeBasePortNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgeBasePortName, thedata->ieee8021BridgeBasePortName, thedata->ieee8021BridgeBasePortNameLen);
		StorageNew->ieee8021BridgeBasePortNameLen = thedata->ieee8021BridgeBasePortNameLen;
		StorageNew->ieee8021BridgeBasePortName[StorageNew->ieee8021BridgeBasePortNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeBasePortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeBasePortTable_destroy(struct ieee8021BridgeBasePortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeBasePortTable_destroy(struct ieee8021BridgeBasePortTable_data **thedata)
{
	struct ieee8021BridgeBasePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBasePortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021BridgeBasePortCapabilities);
		StorageDel->ieee8021BridgeBasePortCapabilitiesLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeBasePortTypeCapabilities);
		StorageDel->ieee8021BridgeBasePortTypeCapabilitiesLen = 0;
		SNMP_FREE(StorageDel->ieee8021BridgeBasePortName);
		StorageDel->ieee8021BridgeBasePortNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeBasePortTable_add(struct ieee8021BridgeBasePortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeBasePortTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeBasePortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeBasePortTable_add(struct ieee8021BridgeBasePortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBasePortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021BridgeBasePortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeBasePortTable_del(struct ieee8021BridgeBasePortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeBasePortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeBasePortTable_del(struct ieee8021BridgeBasePortTable_data *thedata)
{
	struct ieee8021BridgeBasePortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBasePortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeBasePortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeBasePortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeBasePortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeBasePortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeBasePortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeBasePortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeBasePortTable_data *StorageTmp = ieee8021BridgeBasePortTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeBasePortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBasePortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021BridgeBasePortDelayExceededDiscards, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021BridgeBasePortMtuExceededDiscards, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeBasePortCapabilities);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeBasePortCapabilities, &StorageTmp->ieee8021BridgeBasePortCapabilitiesLen);
	if (StorageTmp->ieee8021BridgeBasePortCapabilities == NULL) {
		config_perror("invalid specification for ieee8021BridgeBasePortCapabilities");
		return;
	}
	SNMP_FREE(StorageTmp->ieee8021BridgeBasePortTypeCapabilities);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeBasePortTypeCapabilities, &StorageTmp->ieee8021BridgeBasePortTypeCapabilitiesLen);
	if (StorageTmp->ieee8021BridgeBasePortTypeCapabilities == NULL) {
		config_perror("invalid specification for ieee8021BridgeBasePortTypeCapabilities");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBasePortType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBasePortExternal, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBasePortAdminPointToPoint, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeBasePortOperPointToPoint, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgeBasePortName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgeBasePortName, &StorageTmp->ieee8021BridgeBasePortNameLen);
	if (StorageTmp->ieee8021BridgeBasePortName == NULL) {
		config_perror("invalid specification for ieee8021BridgeBasePortName");
		return;
	}
	ieee8021BridgeBasePortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeBasePortTable(): store configuraiton file for ieee8021BridgeBasePortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeBasePortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeBasePortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeBasePortTable: storing data...  "));
	refresh_ieee8021BridgeBasePortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeBasePortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeBasePortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeBasePortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBasePortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021BridgeBasePortDelayExceededDiscards, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021BridgeBasePortMtuExceededDiscards, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeBasePortCapabilities, &StorageTmp->ieee8021BridgeBasePortCapabilitiesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeBasePortTypeCapabilities, &StorageTmp->ieee8021BridgeBasePortTypeCapabilitiesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBasePortType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBasePortExternal, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBasePortAdminPointToPoint, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeBasePortOperPointToPoint, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgeBasePortName, &StorageTmp->ieee8021BridgeBasePortNameLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeBaseIfToPortTable_data *ieee8021BridgeBaseIfToPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeBaseIfToPortTable table.
 *
 * Creates a new ieee8021BridgeBaseIfToPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeBaseIfToPortTable_data *
ieee8021BridgeBaseIfToPortTable_create(void)
{
	struct ieee8021BridgeBaseIfToPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeBaseIfToPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseIfToPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ifIndex = 0;
		StorageNew->ieee8021BridgeBaseIfIndexComponentId = 0;
		StorageNew->ieee8021BridgeBaseIfIndexPort = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeBaseIfToPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeBaseIfToPortTable_data *ieee8021BridgeBaseIfToPortTable_duplicate(struct ieee8021BridgeBaseIfToPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeBaseIfToPortTable_data *
ieee8021BridgeBaseIfToPortTable_duplicate(struct ieee8021BridgeBaseIfToPortTable_data *thedata)
{
	struct ieee8021BridgeBaseIfToPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeBaseIfToPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseIfToPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeBaseIfToPortTable_id = thedata->ieee8021BridgeBaseIfToPortTable_id;
		StorageNew->ifIndex = thedata->ifIndex;
		StorageNew->ieee8021BridgeBaseIfIndexComponentId = thedata->ieee8021BridgeBaseIfIndexComponentId;
		StorageNew->ieee8021BridgeBaseIfIndexPort = thedata->ieee8021BridgeBaseIfIndexPort;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeBaseIfToPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeBaseIfToPortTable_destroy(struct ieee8021BridgeBaseIfToPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeBaseIfToPortTable_destroy(struct ieee8021BridgeBaseIfToPortTable_data **thedata)
{
	struct ieee8021BridgeBaseIfToPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseIfToPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeBaseIfToPortTable_add(struct ieee8021BridgeBaseIfToPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeBaseIfToPortTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeBaseIfToPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeBaseIfToPortTable_add(struct ieee8021BridgeBaseIfToPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseIfToPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ifIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ifIndex, sizeof(thedata->ifIndex));
		header_complex_add_data(&ieee8021BridgeBaseIfToPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeBaseIfToPortTable_del(struct ieee8021BridgeBaseIfToPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeBaseIfToPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeBaseIfToPortTable_del(struct ieee8021BridgeBaseIfToPortTable_data *thedata)
{
	struct ieee8021BridgeBaseIfToPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeBaseIfToPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeBaseIfToPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeBaseIfToPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeBaseIfToPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeBaseIfToPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeBaseIfToPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeBaseIfToPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp = ieee8021BridgeBaseIfToPortTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeBaseIfToPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ifIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseIfIndexComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBaseIfIndexPort, &tmpsize);
	ieee8021BridgeBaseIfToPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeBaseIfToPortTable(): store configuraiton file for ieee8021BridgeBaseIfToPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeBaseIfToPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeBaseIfToPortTable: storing data...  "));
	refresh_ieee8021BridgeBaseIfToPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeBaseIfToPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeBaseIfToPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeBaseIfToPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ifIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseIfIndexComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBaseIfIndexPort, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePhyPortTable_data *ieee8021BridgePhyPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePhyPortTable table.
 *
 * Creates a new ieee8021BridgePhyPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePhyPortTable_data *
ieee8021BridgePhyPortTable_create(void)
{
	struct ieee8021BridgePhyPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePhyPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePhyPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgePhyPortIfIndex = 0;
		if ((StorageNew->ieee8021BridgePhyMacAddress = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgePhyMacAddressLen = 0;
		StorageNew->ieee8021BridgePhyMacAddress[StorageNew->ieee8021BridgePhyMacAddressLen] = 0;
		StorageNew->ieee8021BridgePhyPortToComponentId = 0;
		StorageNew->ieee8021BridgePhyPortToInternalPort = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePhyPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePhyPortTable_data *ieee8021BridgePhyPortTable_duplicate(struct ieee8021BridgePhyPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePhyPortTable_data *
ieee8021BridgePhyPortTable_duplicate(struct ieee8021BridgePhyPortTable_data *thedata)
{
	struct ieee8021BridgePhyPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePhyPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePhyPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePhyPortTable_id = thedata->ieee8021BridgePhyPortTable_id;
		StorageNew->ieee8021BridgePhyPort = thedata->ieee8021BridgePhyPort;
		StorageNew->ieee8021BridgePhyPortIfIndex = thedata->ieee8021BridgePhyPortIfIndex;
		if (!(StorageNew->ieee8021BridgePhyMacAddress = malloc(thedata->ieee8021BridgePhyMacAddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgePhyMacAddress, thedata->ieee8021BridgePhyMacAddress, thedata->ieee8021BridgePhyMacAddressLen);
		StorageNew->ieee8021BridgePhyMacAddressLen = thedata->ieee8021BridgePhyMacAddressLen;
		StorageNew->ieee8021BridgePhyMacAddress[StorageNew->ieee8021BridgePhyMacAddressLen] = 0;
		StorageNew->ieee8021BridgePhyPortToComponentId = thedata->ieee8021BridgePhyPortToComponentId;
		StorageNew->ieee8021BridgePhyPortToInternalPort = thedata->ieee8021BridgePhyPortToInternalPort;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePhyPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePhyPortTable_destroy(struct ieee8021BridgePhyPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePhyPortTable_destroy(struct ieee8021BridgePhyPortTable_data **thedata)
{
	struct ieee8021BridgePhyPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePhyPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021BridgePhyMacAddress);
		StorageDel->ieee8021BridgePhyMacAddressLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePhyPortTable_add(struct ieee8021BridgePhyPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePhyPortTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePhyPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePhyPortTable_add(struct ieee8021BridgePhyPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePhyPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgePhyPort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePhyPort, sizeof(thedata->ieee8021BridgePhyPort));
		header_complex_add_data(&ieee8021BridgePhyPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePhyPortTable_del(struct ieee8021BridgePhyPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePhyPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePhyPortTable_del(struct ieee8021BridgePhyPortTable_data *thedata)
{
	struct ieee8021BridgePhyPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePhyPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePhyPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePhyPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePhyPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePhyPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePhyPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePhyPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePhyPortTable_data *StorageTmp = ieee8021BridgePhyPortTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePhyPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePhyPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePhyPortIfIndex, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgePhyMacAddress);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgePhyMacAddress, &StorageTmp->ieee8021BridgePhyMacAddressLen);
	if (StorageTmp->ieee8021BridgePhyMacAddress == NULL) {
		config_perror("invalid specification for ieee8021BridgePhyMacAddress");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePhyPortToComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePhyPortToInternalPort, &tmpsize);
	ieee8021BridgePhyPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePhyPortTable(): store configuraiton file for ieee8021BridgePhyPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePhyPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePhyPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePhyPortTable: storing data...  "));
	refresh_ieee8021BridgePhyPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePhyPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePhyPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePhyPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePhyPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePhyPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgePhyMacAddress, &StorageTmp->ieee8021BridgePhyMacAddressLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePhyPortToComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePhyPortToInternalPort, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeTpPortTable_data *ieee8021BridgeTpPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeTpPortTable table.
 *
 * Creates a new ieee8021BridgeTpPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeTpPortTable_data *
ieee8021BridgeTpPortTable_create(void)
{
	struct ieee8021BridgeTpPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeTpPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTpPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeTpPortMaxInfo = 0;
		StorageNew->ieee8021BridgeTpPortInFrames = (struct counter64) {
		0, 0};
		StorageNew->ieee8021BridgeTpPortOutFrames = (struct counter64) {
		0, 0};
		StorageNew->ieee8021BridgeTpPortInDiscards = (struct counter64) {
		0, 0};
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeTpPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeTpPortTable_data *ieee8021BridgeTpPortTable_duplicate(struct ieee8021BridgeTpPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeTpPortTable_data *
ieee8021BridgeTpPortTable_duplicate(struct ieee8021BridgeTpPortTable_data *thedata)
{
	struct ieee8021BridgeTpPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeTpPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTpPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeTpPortTable_id = thedata->ieee8021BridgeTpPortTable_id;
		StorageNew->ieee8021BridgeTpPortComponentId = thedata->ieee8021BridgeTpPortComponentId;
		StorageNew->ieee8021BridgeTpPort = thedata->ieee8021BridgeTpPort;
		StorageNew->ieee8021BridgeTpPortMaxInfo = thedata->ieee8021BridgeTpPortMaxInfo;
		StorageNew->ieee8021BridgeTpPortInFrames = thedata->ieee8021BridgeTpPortInFrames;
		StorageNew->ieee8021BridgeTpPortOutFrames = thedata->ieee8021BridgeTpPortOutFrames;
		StorageNew->ieee8021BridgeTpPortInDiscards = thedata->ieee8021BridgeTpPortInDiscards;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeTpPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeTpPortTable_destroy(struct ieee8021BridgeTpPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeTpPortTable_destroy(struct ieee8021BridgeTpPortTable_data **thedata)
{
	struct ieee8021BridgeTpPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTpPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeTpPortTable_add(struct ieee8021BridgeTpPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeTpPortTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeTpPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeTpPortTable_add(struct ieee8021BridgeTpPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTpPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeTpPortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeTpPortComponentId, sizeof(thedata->ieee8021BridgeTpPortComponentId));
		/* ieee8021BridgeTpPort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeTpPort, sizeof(thedata->ieee8021BridgeTpPort));
		header_complex_add_data(&ieee8021BridgeTpPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeTpPortTable_del(struct ieee8021BridgeTpPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeTpPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeTpPortTable_del(struct ieee8021BridgeTpPortTable_data *thedata)
{
	struct ieee8021BridgeTpPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTpPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeTpPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeTpPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeTpPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeTpPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeTpPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeTpPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeTpPortTable_data *StorageTmp = ieee8021BridgeTpPortTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeTpPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeTpPortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeTpPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeTpPortMaxInfo, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021BridgeTpPortInFrames, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021BridgeTpPortOutFrames, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021BridgeTpPortInDiscards, &tmpsize);
	ieee8021BridgeTpPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeTpPortTable(): store configuraiton file for ieee8021BridgeTpPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeTpPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeTpPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeTpPortTable: storing data...  "));
	refresh_ieee8021BridgeTpPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeTpPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeTpPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeTpPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeTpPortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeTpPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeTpPortMaxInfo, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021BridgeTpPortInFrames, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021BridgeTpPortOutFrames, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021BridgeTpPortInDiscards, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePortPriorityTable_data *ieee8021BridgePortPriorityTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePortPriorityTable table.
 *
 * Creates a new ieee8021BridgePortPriorityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePortPriorityTable_data *
ieee8021BridgePortPriorityTable_create(void)
{
	struct ieee8021BridgePortPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortPriorityTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortPriorityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgePortDefaultUserPriority = 0;
		StorageNew->ieee8021BridgePortNumTrafficClasses = 0;
		StorageNew->ieee8021BridgePortPriorityCodePointSelection = 0;
		StorageNew->ieee8021BridgePortUseDEI = 0;
		StorageNew->ieee8021BridgePortRequireDropEncoding = TV_FALSE;
		StorageNew->ieee8021BridgePortServiceAccessPrioritySelection = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePortPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePortPriorityTable_data *ieee8021BridgePortPriorityTable_duplicate(struct ieee8021BridgePortPriorityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePortPriorityTable_data *
ieee8021BridgePortPriorityTable_duplicate(struct ieee8021BridgePortPriorityTable_data *thedata)
{
	struct ieee8021BridgePortPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortPriorityTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortPriorityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePortPriorityTable_id = thedata->ieee8021BridgePortPriorityTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgePortDefaultUserPriority = thedata->ieee8021BridgePortDefaultUserPriority;
		StorageNew->ieee8021BridgePortNumTrafficClasses = thedata->ieee8021BridgePortNumTrafficClasses;
		StorageNew->ieee8021BridgePortPriorityCodePointSelection = thedata->ieee8021BridgePortPriorityCodePointSelection;
		StorageNew->ieee8021BridgePortUseDEI = thedata->ieee8021BridgePortUseDEI;
		StorageNew->ieee8021BridgePortRequireDropEncoding = thedata->ieee8021BridgePortRequireDropEncoding;
		StorageNew->ieee8021BridgePortServiceAccessPrioritySelection = thedata->ieee8021BridgePortServiceAccessPrioritySelection;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePortPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePortPriorityTable_destroy(struct ieee8021BridgePortPriorityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePortPriorityTable_destroy(struct ieee8021BridgePortPriorityTable_data **thedata)
{
	struct ieee8021BridgePortPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortPriorityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortPriorityTable_add(struct ieee8021BridgePortPriorityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePortPriorityTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePortPriorityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePortPriorityTable_add(struct ieee8021BridgePortPriorityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortPriorityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021BridgePortPriorityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortPriorityTable_del(struct ieee8021BridgePortPriorityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePortPriorityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePortPriorityTable_del(struct ieee8021BridgePortPriorityTable_data *thedata)
{
	struct ieee8021BridgePortPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortPriorityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePortPriorityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePortPriorityTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePortPriorityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePortPriorityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePortPriorityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePortPriorityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = ieee8021BridgePortPriorityTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePortPriorityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortDefaultUserPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortNumTrafficClasses, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortPriorityCodePointSelection, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortUseDEI, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortRequireDropEncoding, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection, &tmpsize);
	ieee8021BridgePortPriorityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePortPriorityTable(): store configuraiton file for ieee8021BridgePortPriorityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePortPriorityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePortPriorityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePortPriorityTable: storing data...  "));
	refresh_ieee8021BridgePortPriorityTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePortPriorityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePortPriorityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePortPriorityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortDefaultUserPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortNumTrafficClasses, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortPriorityCodePointSelection, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortUseDEI, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortRequireDropEncoding, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeUserPriorityRegenTable_data *ieee8021BridgeUserPriorityRegenTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeUserPriorityRegenTable table.
 *
 * Creates a new ieee8021BridgeUserPriorityRegenTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeUserPriorityRegenTable_data *
ieee8021BridgeUserPriorityRegenTable_create(void)
{
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeUserPriorityRegenTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeUserPriorityRegenTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgeRegenUserPriority = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeUserPriorityRegenTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeUserPriorityRegenTable_data *ieee8021BridgeUserPriorityRegenTable_duplicate(struct ieee8021BridgeUserPriorityRegenTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeUserPriorityRegenTable_data *
ieee8021BridgeUserPriorityRegenTable_duplicate(struct ieee8021BridgeUserPriorityRegenTable_data *thedata)
{
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeUserPriorityRegenTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeUserPriorityRegenTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeUserPriorityRegenTable_id = thedata->ieee8021BridgeUserPriorityRegenTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgeUserPriority = thedata->ieee8021BridgeUserPriority;
		StorageNew->ieee8021BridgeRegenUserPriority = thedata->ieee8021BridgeRegenUserPriority;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeUserPriorityRegenTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeUserPriorityRegenTable_destroy(struct ieee8021BridgeUserPriorityRegenTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeUserPriorityRegenTable_destroy(struct ieee8021BridgeUserPriorityRegenTable_data **thedata)
{
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeUserPriorityRegenTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeUserPriorityRegenTable_add(struct ieee8021BridgeUserPriorityRegenTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeUserPriorityRegenTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeUserPriorityRegenTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeUserPriorityRegenTable_add(struct ieee8021BridgeUserPriorityRegenTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeUserPriorityRegenTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021BridgeUserPriority */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeUserPriority, sizeof(thedata->ieee8021BridgeUserPriority));
		header_complex_add_data(&ieee8021BridgeUserPriorityRegenTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeUserPriorityRegenTable_del(struct ieee8021BridgeUserPriorityRegenTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeUserPriorityRegenTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeUserPriorityRegenTable_del(struct ieee8021BridgeUserPriorityRegenTable_data *thedata)
{
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeUserPriorityRegenTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeUserPriorityRegenTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeUserPriorityRegenTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeUserPriorityRegenTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeUserPriorityRegenTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeUserPriorityRegenTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeUserPriorityRegenTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp = ieee8021BridgeUserPriorityRegenTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeUserPriorityRegenTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeUserPriority, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeRegenUserPriority, &tmpsize);
	ieee8021BridgeUserPriorityRegenTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeUserPriorityRegenTable(): store configuraiton file for ieee8021BridgeUserPriorityRegenTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeUserPriorityRegenTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeUserPriorityRegenTable: storing data...  "));
	refresh_ieee8021BridgeUserPriorityRegenTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeUserPriorityRegenTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeUserPriorityRegenTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeUserPriorityRegenTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeUserPriority, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeRegenUserPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeTrafficClassTable_data *ieee8021BridgeTrafficClassTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeTrafficClassTable table.
 *
 * Creates a new ieee8021BridgeTrafficClassTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeTrafficClassTable_data *
ieee8021BridgeTrafficClassTable_create(void)
{
	struct ieee8021BridgeTrafficClassTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeTrafficClassTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTrafficClassTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgeTrafficClass = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeTrafficClassTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeTrafficClassTable_data *ieee8021BridgeTrafficClassTable_duplicate(struct ieee8021BridgeTrafficClassTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeTrafficClassTable_data *
ieee8021BridgeTrafficClassTable_duplicate(struct ieee8021BridgeTrafficClassTable_data *thedata)
{
	struct ieee8021BridgeTrafficClassTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeTrafficClassTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTrafficClassTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeTrafficClassTable_id = thedata->ieee8021BridgeTrafficClassTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgeTrafficClassPriority = thedata->ieee8021BridgeTrafficClassPriority;
		StorageNew->ieee8021BridgeTrafficClass = thedata->ieee8021BridgeTrafficClass;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeTrafficClassTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeTrafficClassTable_destroy(struct ieee8021BridgeTrafficClassTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeTrafficClassTable_destroy(struct ieee8021BridgeTrafficClassTable_data **thedata)
{
	struct ieee8021BridgeTrafficClassTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTrafficClassTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeTrafficClassTable_add(struct ieee8021BridgeTrafficClassTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeTrafficClassTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeTrafficClassTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeTrafficClassTable_add(struct ieee8021BridgeTrafficClassTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTrafficClassTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021BridgeTrafficClassPriority */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeTrafficClassPriority, sizeof(thedata->ieee8021BridgeTrafficClassPriority));
		header_complex_add_data(&ieee8021BridgeTrafficClassTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeTrafficClassTable_del(struct ieee8021BridgeTrafficClassTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeTrafficClassTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeTrafficClassTable_del(struct ieee8021BridgeTrafficClassTable_data *thedata)
{
	struct ieee8021BridgeTrafficClassTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeTrafficClassTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeTrafficClassTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeTrafficClassTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeTrafficClassTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeTrafficClassTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeTrafficClassTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeTrafficClassTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeTrafficClassTable_data *StorageTmp = ieee8021BridgeTrafficClassTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeTrafficClassTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeTrafficClassPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeTrafficClass, &tmpsize);
	ieee8021BridgeTrafficClassTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeTrafficClassTable(): store configuraiton file for ieee8021BridgeTrafficClassTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeTrafficClassTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeTrafficClassTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeTrafficClassTable: storing data...  "));
	refresh_ieee8021BridgeTrafficClassTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeTrafficClassTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeTrafficClassTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeTrafficClassTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeTrafficClassPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeTrafficClass, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePortOutboundAccessPriorityTable_data *ieee8021BridgePortOutboundAccessPriorityTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePortOutboundAccessPriorityTable table.
 *
 * Creates a new ieee8021BridgePortOutboundAccessPriorityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePortOutboundAccessPriorityTable_data *
ieee8021BridgePortOutboundAccessPriorityTable_create(void)
{
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortOutboundAccessPriorityTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortOutboundAccessPriorityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgeRegenUserPriority = 0;
		StorageNew->ieee8021BridgePortOutboundAccessPriority = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePortOutboundAccessPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePortOutboundAccessPriorityTable_data *ieee8021BridgePortOutboundAccessPriorityTable_duplicate(struct ieee8021BridgePortOutboundAccessPriorityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePortOutboundAccessPriorityTable_data *
ieee8021BridgePortOutboundAccessPriorityTable_duplicate(struct ieee8021BridgePortOutboundAccessPriorityTable_data *thedata)
{
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortOutboundAccessPriorityTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortOutboundAccessPriorityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePortOutboundAccessPriorityTable_id = thedata->ieee8021BridgePortOutboundAccessPriorityTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgeRegenUserPriority = thedata->ieee8021BridgeRegenUserPriority;
		StorageNew->ieee8021BridgePortOutboundAccessPriority = thedata->ieee8021BridgePortOutboundAccessPriority;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePortOutboundAccessPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePortOutboundAccessPriorityTable_destroy(struct ieee8021BridgePortOutboundAccessPriorityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePortOutboundAccessPriorityTable_destroy(struct ieee8021BridgePortOutboundAccessPriorityTable_data **thedata)
{
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortOutboundAccessPriorityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortOutboundAccessPriorityTable_add(struct ieee8021BridgePortOutboundAccessPriorityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePortOutboundAccessPriorityTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePortOutboundAccessPriorityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePortOutboundAccessPriorityTable_add(struct ieee8021BridgePortOutboundAccessPriorityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortOutboundAccessPriorityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		/* ieee8021BridgeRegenUserPriority */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeRegenUserPriority, sizeof(thedata->ieee8021BridgeRegenUserPriority));
		header_complex_add_data(&ieee8021BridgePortOutboundAccessPriorityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortOutboundAccessPriorityTable_del(struct ieee8021BridgePortOutboundAccessPriorityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePortOutboundAccessPriorityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePortOutboundAccessPriorityTable_del(struct ieee8021BridgePortOutboundAccessPriorityTable_data *thedata)
{
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortOutboundAccessPriorityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePortOutboundAccessPriorityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePortOutboundAccessPriorityTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePortOutboundAccessPriorityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePortOutboundAccessPriorityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePortOutboundAccessPriorityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePortOutboundAccessPriorityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp = ieee8021BridgePortOutboundAccessPriorityTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePortOutboundAccessPriorityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeRegenUserPriority, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortOutboundAccessPriority, &tmpsize);
	ieee8021BridgePortOutboundAccessPriorityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePortOutboundAccessPriorityTable(): store configuraiton file for ieee8021BridgePortOutboundAccessPriorityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePortOutboundAccessPriorityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePortOutboundAccessPriorityTable: storing data...  "));
	refresh_ieee8021BridgePortOutboundAccessPriorityTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePortOutboundAccessPriorityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePortOutboundAccessPriorityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePortOutboundAccessPriorityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeRegenUserPriority, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortOutboundAccessPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePortDecodingTable_data *ieee8021BridgePortDecodingTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePortDecodingTable table.
 *
 * Creates a new ieee8021BridgePortDecodingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePortDecodingTable_data *
ieee8021BridgePortDecodingTable_create(void)
{
	struct ieee8021BridgePortDecodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortDecodingTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortDecodingTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgePortDecodingPriority = 0;
		StorageNew->ieee8021BridgePortDecodingDropEligible = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePortDecodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePortDecodingTable_data *ieee8021BridgePortDecodingTable_duplicate(struct ieee8021BridgePortDecodingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePortDecodingTable_data *
ieee8021BridgePortDecodingTable_duplicate(struct ieee8021BridgePortDecodingTable_data *thedata)
{
	struct ieee8021BridgePortDecodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortDecodingTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortDecodingTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePortDecodingTable_id = thedata->ieee8021BridgePortDecodingTable_id;
		StorageNew->ieee8021BridgePortDecodingComponentId = thedata->ieee8021BridgePortDecodingComponentId;
		StorageNew->ieee8021BridgePortDecodingPortNum = thedata->ieee8021BridgePortDecodingPortNum;
		StorageNew->ieee8021BridgePortDecodingPriorityCodePointRow = thedata->ieee8021BridgePortDecodingPriorityCodePointRow;
		StorageNew->ieee8021BridgePortDecodingPriorityCodePoint = thedata->ieee8021BridgePortDecodingPriorityCodePoint;
		StorageNew->ieee8021BridgePortDecodingPriority = thedata->ieee8021BridgePortDecodingPriority;
		StorageNew->ieee8021BridgePortDecodingDropEligible = thedata->ieee8021BridgePortDecodingDropEligible;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePortDecodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePortDecodingTable_destroy(struct ieee8021BridgePortDecodingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePortDecodingTable_destroy(struct ieee8021BridgePortDecodingTable_data **thedata)
{
	struct ieee8021BridgePortDecodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortDecodingTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortDecodingTable_add(struct ieee8021BridgePortDecodingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePortDecodingTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePortDecodingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePortDecodingTable_add(struct ieee8021BridgePortDecodingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortDecodingTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgePortDecodingComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePortDecodingComponentId, sizeof(thedata->ieee8021BridgePortDecodingComponentId));
		/* ieee8021BridgePortDecodingPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePortDecodingPortNum, sizeof(thedata->ieee8021BridgePortDecodingPortNum));
		/* ieee8021BridgePortDecodingPriorityCodePointRow */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgePortDecodingPriorityCodePointRow,
					  sizeof(thedata->ieee8021BridgePortDecodingPriorityCodePointRow));
		/* ieee8021BridgePortDecodingPriorityCodePoint */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgePortDecodingPriorityCodePoint, sizeof(thedata->ieee8021BridgePortDecodingPriorityCodePoint));
		header_complex_add_data(&ieee8021BridgePortDecodingTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortDecodingTable_del(struct ieee8021BridgePortDecodingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePortDecodingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePortDecodingTable_del(struct ieee8021BridgePortDecodingTable_data *thedata)
{
	struct ieee8021BridgePortDecodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortDecodingTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePortDecodingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePortDecodingTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePortDecodingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePortDecodingTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePortDecodingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePortDecodingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePortDecodingTable_data *StorageTmp = ieee8021BridgePortDecodingTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePortDecodingTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortDecodingComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortDecodingPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortDecodingPriorityCodePointRow, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortDecodingPriorityCodePoint, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortDecodingPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortDecodingDropEligible, &tmpsize);
	ieee8021BridgePortDecodingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePortDecodingTable(): store configuraiton file for ieee8021BridgePortDecodingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePortDecodingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePortDecodingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePortDecodingTable: storing data...  "));
	refresh_ieee8021BridgePortDecodingTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePortDecodingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePortDecodingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePortDecodingTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortDecodingComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortDecodingPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortDecodingPriorityCodePointRow, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortDecodingPriorityCodePoint, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortDecodingPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortDecodingDropEligible, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePortEncodingTable_data *ieee8021BridgePortEncodingTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePortEncodingTable table.
 *
 * Creates a new ieee8021BridgePortEncodingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePortEncodingTable_data *
ieee8021BridgePortEncodingTable_create(void)
{
	struct ieee8021BridgePortEncodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortEncodingTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortEncodingTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgePortEncodingPriority = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePortEncodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePortEncodingTable_data *ieee8021BridgePortEncodingTable_duplicate(struct ieee8021BridgePortEncodingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePortEncodingTable_data *
ieee8021BridgePortEncodingTable_duplicate(struct ieee8021BridgePortEncodingTable_data *thedata)
{
	struct ieee8021BridgePortEncodingTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortEncodingTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortEncodingTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePortEncodingTable_id = thedata->ieee8021BridgePortEncodingTable_id;
		StorageNew->ieee8021BridgePortEncodingComponentId = thedata->ieee8021BridgePortEncodingComponentId;
		StorageNew->ieee8021BridgePortEncodingPortNum = thedata->ieee8021BridgePortEncodingPortNum;
		StorageNew->ieee8021BridgePortEncodingPriorityCodePointRow = thedata->ieee8021BridgePortEncodingPriorityCodePointRow;
		StorageNew->ieee8021BridgePortEncodingPriorityCodePoint = thedata->ieee8021BridgePortEncodingPriorityCodePoint;
		StorageNew->ieee8021BridgePortEncodingDropEligible = thedata->ieee8021BridgePortEncodingDropEligible;
		StorageNew->ieee8021BridgePortEncodingPriority = thedata->ieee8021BridgePortEncodingPriority;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePortEncodingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePortEncodingTable_destroy(struct ieee8021BridgePortEncodingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePortEncodingTable_destroy(struct ieee8021BridgePortEncodingTable_data **thedata)
{
	struct ieee8021BridgePortEncodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortEncodingTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortEncodingTable_add(struct ieee8021BridgePortEncodingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePortEncodingTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePortEncodingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePortEncodingTable_add(struct ieee8021BridgePortEncodingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortEncodingTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgePortEncodingComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePortEncodingComponentId, sizeof(thedata->ieee8021BridgePortEncodingComponentId));
		/* ieee8021BridgePortEncodingPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgePortEncodingPortNum, sizeof(thedata->ieee8021BridgePortEncodingPortNum));
		/* ieee8021BridgePortEncodingPriorityCodePointRow */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgePortEncodingPriorityCodePointRow,
					  sizeof(thedata->ieee8021BridgePortEncodingPriorityCodePointRow));
		/* ieee8021BridgePortEncodingPriorityCodePoint */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgePortEncodingPriorityCodePoint, sizeof(thedata->ieee8021BridgePortEncodingPriorityCodePoint));
		/* ieee8021BridgePortEncodingDropEligible */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ieee8021BridgePortEncodingDropEligible, sizeof(thedata->ieee8021BridgePortEncodingDropEligible));
		header_complex_add_data(&ieee8021BridgePortEncodingTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortEncodingTable_del(struct ieee8021BridgePortEncodingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePortEncodingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePortEncodingTable_del(struct ieee8021BridgePortEncodingTable_data *thedata)
{
	struct ieee8021BridgePortEncodingTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortEncodingTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePortEncodingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePortEncodingTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePortEncodingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePortEncodingTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePortEncodingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePortEncodingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePortEncodingTable_data *StorageTmp = ieee8021BridgePortEncodingTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePortEncodingTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortEncodingComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortEncodingPortNum, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortEncodingPriorityCodePointRow, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortEncodingPriorityCodePoint, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortEncodingDropEligible, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgePortEncodingPriority, &tmpsize);
	ieee8021BridgePortEncodingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePortEncodingTable(): store configuraiton file for ieee8021BridgePortEncodingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePortEncodingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePortEncodingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePortEncodingTable: storing data...  "));
	refresh_ieee8021BridgePortEncodingTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePortEncodingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePortEncodingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePortEncodingTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortEncodingComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortEncodingPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortEncodingPriorityCodePointRow, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortEncodingPriorityCodePoint, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortEncodingDropEligible, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgePortEncodingPriority, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeServiceAccessPriorityTable_data *ieee8021BridgeServiceAccessPriorityTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeServiceAccessPriorityTable table.
 *
 * Creates a new ieee8021BridgeServiceAccessPriorityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeServiceAccessPriorityTable_data *
ieee8021BridgeServiceAccessPriorityTable_create(void)
{
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeServiceAccessPriorityTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeServiceAccessPriorityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeServiceAccessPriorityValue = 0;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeServiceAccessPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeServiceAccessPriorityTable_data *ieee8021BridgeServiceAccessPriorityTable_duplicate(struct ieee8021BridgeServiceAccessPriorityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeServiceAccessPriorityTable_data *
ieee8021BridgeServiceAccessPriorityTable_duplicate(struct ieee8021BridgeServiceAccessPriorityTable_data *thedata)
{
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeServiceAccessPriorityTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeServiceAccessPriorityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeServiceAccessPriorityTable_id = thedata->ieee8021BridgeServiceAccessPriorityTable_id;
		StorageNew->ieee8021BridgeServiceAccessPriorityComponentId = thedata->ieee8021BridgeServiceAccessPriorityComponentId;
		StorageNew->ieee8021BridgeServiceAccessPriorityPortNum = thedata->ieee8021BridgeServiceAccessPriorityPortNum;
		StorageNew->ieee8021BridgeServiceAccessPriorityReceived = thedata->ieee8021BridgeServiceAccessPriorityReceived;
		StorageNew->ieee8021BridgeServiceAccessPriorityValue = thedata->ieee8021BridgeServiceAccessPriorityValue;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeServiceAccessPriorityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeServiceAccessPriorityTable_destroy(struct ieee8021BridgeServiceAccessPriorityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeServiceAccessPriorityTable_destroy(struct ieee8021BridgeServiceAccessPriorityTable_data **thedata)
{
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeServiceAccessPriorityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeServiceAccessPriorityTable_add(struct ieee8021BridgeServiceAccessPriorityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeServiceAccessPriorityTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeServiceAccessPriorityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeServiceAccessPriorityTable_add(struct ieee8021BridgeServiceAccessPriorityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeServiceAccessPriorityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeServiceAccessPriorityComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeServiceAccessPriorityComponentId,
					  sizeof(thedata->ieee8021BridgeServiceAccessPriorityComponentId));
		/* ieee8021BridgeServiceAccessPriorityPortNum */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeServiceAccessPriorityPortNum, sizeof(thedata->ieee8021BridgeServiceAccessPriorityPortNum));
		/* ieee8021BridgeServiceAccessPriorityReceived */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeServiceAccessPriorityReceived, sizeof(thedata->ieee8021BridgeServiceAccessPriorityReceived));
		header_complex_add_data(&ieee8021BridgeServiceAccessPriorityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeServiceAccessPriorityTable_del(struct ieee8021BridgeServiceAccessPriorityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeServiceAccessPriorityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeServiceAccessPriorityTable_del(struct ieee8021BridgeServiceAccessPriorityTable_data *thedata)
{
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeServiceAccessPriorityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeServiceAccessPriorityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeServiceAccessPriorityTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeServiceAccessPriorityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeServiceAccessPriorityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeServiceAccessPriorityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeServiceAccessPriorityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp = ieee8021BridgeServiceAccessPriorityTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeServiceAccessPriorityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeServiceAccessPriorityComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeServiceAccessPriorityPortNum, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeServiceAccessPriorityReceived, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeServiceAccessPriorityValue, &tmpsize);
	ieee8021BridgeServiceAccessPriorityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeServiceAccessPriorityTable(): store configuraiton file for ieee8021BridgeServiceAccessPriorityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeServiceAccessPriorityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeServiceAccessPriorityTable: storing data...  "));
	refresh_ieee8021BridgeServiceAccessPriorityTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeServiceAccessPriorityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeServiceAccessPriorityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeServiceAccessPriorityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeServiceAccessPriorityComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeServiceAccessPriorityPortNum, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeServiceAccessPriorityReceived, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeServiceAccessPriorityValue, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePortMrpTable_data *ieee8021BridgePortMrpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePortMrpTable table.
 *
 * Creates a new ieee8021BridgePortMrpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePortMrpTable_data *
ieee8021BridgePortMrpTable_create(void)
{
	struct ieee8021BridgePortMrpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortMrpTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMrpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgePortMrpJoinTime = 20;
		StorageNew->ieee8021BridgePortMrpLeaveTime = 60;
		StorageNew->ieee8021BridgePortMrpLeaveAllTime = 1000;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePortMrpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePortMrpTable_data *ieee8021BridgePortMrpTable_duplicate(struct ieee8021BridgePortMrpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePortMrpTable_data *
ieee8021BridgePortMrpTable_duplicate(struct ieee8021BridgePortMrpTable_data *thedata)
{
	struct ieee8021BridgePortMrpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortMrpTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMrpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePortMrpTable_id = thedata->ieee8021BridgePortMrpTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgePortMrpJoinTime = thedata->ieee8021BridgePortMrpJoinTime;
		StorageNew->ieee8021BridgePortMrpLeaveTime = thedata->ieee8021BridgePortMrpLeaveTime;
		StorageNew->ieee8021BridgePortMrpLeaveAllTime = thedata->ieee8021BridgePortMrpLeaveAllTime;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePortMrpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePortMrpTable_destroy(struct ieee8021BridgePortMrpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePortMrpTable_destroy(struct ieee8021BridgePortMrpTable_data **thedata)
{
	struct ieee8021BridgePortMrpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMrpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortMrpTable_add(struct ieee8021BridgePortMrpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePortMrpTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePortMrpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePortMrpTable_add(struct ieee8021BridgePortMrpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMrpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021BridgePortMrpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortMrpTable_del(struct ieee8021BridgePortMrpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePortMrpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePortMrpTable_del(struct ieee8021BridgePortMrpTable_data *thedata)
{
	struct ieee8021BridgePortMrpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMrpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePortMrpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePortMrpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePortMrpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePortMrpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePortMrpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePortMrpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePortMrpTable_data *StorageTmp = ieee8021BridgePortMrpTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePortMrpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortMrpJoinTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortMrpLeaveTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortMrpLeaveAllTime, &tmpsize);
	ieee8021BridgePortMrpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePortMrpTable(): store configuraiton file for ieee8021BridgePortMrpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePortMrpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePortMrpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePortMrpTable: storing data...  "));
	refresh_ieee8021BridgePortMrpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePortMrpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePortMrpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePortMrpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortMrpJoinTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortMrpLeaveTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortMrpLeaveAllTime, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgePortMmrpTable_data *ieee8021BridgePortMmrpTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgePortMmrpTable table.
 *
 * Creates a new ieee8021BridgePortMmrpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgePortMmrpTable_data *
ieee8021BridgePortMmrpTable_create(void)
{
	struct ieee8021BridgePortMmrpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortMmrpTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMmrpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgePortMmrpEnabledStatus = TV_TRUE;
		StorageNew->ieee8021BridgePortMmrpFailedRegistrations = (struct counter64) {
		0, 0};
		if ((StorageNew->ieee8021BridgePortMmrpLastPduOrigin = malloc(1)) == NULL)
			goto nomem;
		StorageNew->ieee8021BridgePortMmrpLastPduOriginLen = 0;
		StorageNew->ieee8021BridgePortMmrpLastPduOrigin[StorageNew->ieee8021BridgePortMmrpLastPduOriginLen] = 0;
		StorageNew->ieee8021BridgePortRestrictedGroupRegistration = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgePortMmrpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgePortMmrpTable_data *ieee8021BridgePortMmrpTable_duplicate(struct ieee8021BridgePortMmrpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgePortMmrpTable_data *
ieee8021BridgePortMmrpTable_duplicate(struct ieee8021BridgePortMmrpTable_data *thedata)
{
	struct ieee8021BridgePortMmrpTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgePortMmrpTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMmrpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgePortMmrpTable_id = thedata->ieee8021BridgePortMmrpTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgePortMmrpEnabledStatus = thedata->ieee8021BridgePortMmrpEnabledStatus;
		StorageNew->ieee8021BridgePortMmrpFailedRegistrations = thedata->ieee8021BridgePortMmrpFailedRegistrations;
		if (!(StorageNew->ieee8021BridgePortMmrpLastPduOrigin = malloc(thedata->ieee8021BridgePortMmrpLastPduOriginLen + 1)))
			goto destroy;
		memcpy(StorageNew->ieee8021BridgePortMmrpLastPduOrigin, thedata->ieee8021BridgePortMmrpLastPduOrigin, thedata->ieee8021BridgePortMmrpLastPduOriginLen);
		StorageNew->ieee8021BridgePortMmrpLastPduOriginLen = thedata->ieee8021BridgePortMmrpLastPduOriginLen;
		StorageNew->ieee8021BridgePortMmrpLastPduOrigin[StorageNew->ieee8021BridgePortMmrpLastPduOriginLen] = 0;
		StorageNew->ieee8021BridgePortRestrictedGroupRegistration = thedata->ieee8021BridgePortRestrictedGroupRegistration;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgePortMmrpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgePortMmrpTable_destroy(struct ieee8021BridgePortMmrpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgePortMmrpTable_destroy(struct ieee8021BridgePortMmrpTable_data **thedata)
{
	struct ieee8021BridgePortMmrpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMmrpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->ieee8021BridgePortMmrpLastPduOrigin);
		StorageDel->ieee8021BridgePortMmrpLastPduOriginLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortMmrpTable_add(struct ieee8021BridgePortMmrpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgePortMmrpTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgePortMmrpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgePortMmrpTable_add(struct ieee8021BridgePortMmrpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMmrpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021BridgePortMmrpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgePortMmrpTable_del(struct ieee8021BridgePortMmrpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgePortMmrpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgePortMmrpTable_del(struct ieee8021BridgePortMmrpTable_data *thedata)
{
	struct ieee8021BridgePortMmrpTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgePortMmrpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgePortMmrpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgePortMmrpTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgePortMmrpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgePortMmrpTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgePortMmrpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgePortMmrpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgePortMmrpTable_data *StorageTmp = ieee8021BridgePortMmrpTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgePortMmrpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortMmrpEnabledStatus, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->ieee8021BridgePortMmrpFailedRegistrations, &tmpsize);
	SNMP_FREE(StorageTmp->ieee8021BridgePortMmrpLastPduOrigin);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ieee8021BridgePortMmrpLastPduOrigin, &StorageTmp->ieee8021BridgePortMmrpLastPduOriginLen);
	if (StorageTmp->ieee8021BridgePortMmrpLastPduOrigin == NULL) {
		config_perror("invalid specification for ieee8021BridgePortMmrpLastPduOrigin");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgePortRestrictedGroupRegistration, &tmpsize);
	ieee8021BridgePortMmrpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgePortMmrpTable(): store configuraiton file for ieee8021BridgePortMmrpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgePortMmrpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgePortMmrpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgePortMmrpTable: storing data...  "));
	refresh_ieee8021BridgePortMmrpTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgePortMmrpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgePortMmrpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgePortMmrpTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortMmrpEnabledStatus, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->ieee8021BridgePortMmrpFailedRegistrations, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ieee8021BridgePortMmrpLastPduOrigin, &StorageTmp->ieee8021BridgePortMmrpLastPduOriginLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgePortRestrictedGroupRegistration, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeILanIfTable_data *ieee8021BridgeILanIfTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeILanIfTable table.
 *
 * Creates a new ieee8021BridgeILanIfTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeILanIfTable_data *
ieee8021BridgeILanIfTable_create(void)
{
	struct ieee8021BridgeILanIfTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeILanIfTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeILanIfTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ifIndex = 0;
		StorageNew->ieee8021BridgeILanIfRowStatus = 0;
		StorageNew->ieee8021BridgeILanIfRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeILanIfTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeILanIfTable_data *ieee8021BridgeILanIfTable_duplicate(struct ieee8021BridgeILanIfTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeILanIfTable_data *
ieee8021BridgeILanIfTable_duplicate(struct ieee8021BridgeILanIfTable_data *thedata)
{
	struct ieee8021BridgeILanIfTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeILanIfTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeILanIfTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeILanIfTable_id = thedata->ieee8021BridgeILanIfTable_id;
		StorageNew->ifIndex = thedata->ifIndex;
		StorageNew->ieee8021BridgeILanIfRowStatus = thedata->ieee8021BridgeILanIfRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeILanIfTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeILanIfTable_destroy(struct ieee8021BridgeILanIfTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeILanIfTable_destroy(struct ieee8021BridgeILanIfTable_data **thedata)
{
	struct ieee8021BridgeILanIfTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeILanIfTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeILanIfTable_add(struct ieee8021BridgeILanIfTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeILanIfTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeILanIfTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeILanIfTable_add(struct ieee8021BridgeILanIfTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeILanIfTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ifIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ifIndex, sizeof(thedata->ifIndex));
		header_complex_add_data(&ieee8021BridgeILanIfTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeILanIfTable_del(struct ieee8021BridgeILanIfTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeILanIfTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeILanIfTable_del(struct ieee8021BridgeILanIfTable_data *thedata)
{
	struct ieee8021BridgeILanIfTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeILanIfTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeILanIfTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeILanIfTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeILanIfTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeILanIfTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeILanIfTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeILanIfTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeILanIfTable_data *StorageTmp = ieee8021BridgeILanIfTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeILanIfTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ifIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeILanIfRowStatus, &tmpsize);
	ieee8021BridgeILanIfTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeILanIfTable(): store configuraiton file for ieee8021BridgeILanIfTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeILanIfTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeILanIfTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeILanIfTable: storing data...  "));
	refresh_ieee8021BridgeILanIfTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeILanIfTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeILanIfTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeILanIfTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ifIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeILanIfRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct ieee8021BridgeDot1dPortTable_data *ieee8021BridgeDot1dPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the ieee8021BridgeDot1dPortTable table.
 *
 * Creates a new ieee8021BridgeDot1dPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct ieee8021BridgeDot1dPortTable_data *
ieee8021BridgeDot1dPortTable_create(void)
{
	struct ieee8021BridgeDot1dPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeDot1dPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeDot1dPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ieee8021BridgeBasePortComponentId = 0;
		StorageNew->ieee8021BridgeBasePort = 0;
		StorageNew->ieee8021BridgeDot1dPortRowStatus = 0;
		StorageNew->ieee8021BridgeDot1dPortRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	ieee8021BridgeDot1dPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct ieee8021BridgeDot1dPortTable_data *ieee8021BridgeDot1dPortTable_duplicate(struct ieee8021BridgeDot1dPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct ieee8021BridgeDot1dPortTable_data *
ieee8021BridgeDot1dPortTable_duplicate(struct ieee8021BridgeDot1dPortTable_data *thedata)
{
	struct ieee8021BridgeDot1dPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(ieee8021BridgeDot1dPortTable_data);

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeDot1dPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->ieee8021BridgeDot1dPortTable_id = thedata->ieee8021BridgeDot1dPortTable_id;
		StorageNew->ieee8021BridgeBasePortComponentId = thedata->ieee8021BridgeBasePortComponentId;
		StorageNew->ieee8021BridgeBasePort = thedata->ieee8021BridgeBasePort;
		StorageNew->ieee8021BridgeDot1dPortRowStatus = thedata->ieee8021BridgeDot1dPortRowStatus;
	}
      done:
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	ieee8021BridgeDot1dPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int ieee8021BridgeDot1dPortTable_destroy(struct ieee8021BridgeDot1dPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ieee8021BridgeDot1dPortTable_destroy(struct ieee8021BridgeDot1dPortTable_data **thedata)
{
	struct ieee8021BridgeDot1dPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeDot1dPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeDot1dPortTable_add(struct ieee8021BridgeDot1dPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the ieee8021BridgeDot1dPortTable table data set.
 *
 * Adds a table row structure to the ieee8021BridgeDot1dPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
ieee8021BridgeDot1dPortTable_add(struct ieee8021BridgeDot1dPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeDot1dPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* ieee8021BridgeBasePortComponentId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePortComponentId, sizeof(thedata->ieee8021BridgeBasePortComponentId));
		/* ieee8021BridgeBasePort */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->ieee8021BridgeBasePort, sizeof(thedata->ieee8021BridgeBasePort));
		header_complex_add_data(&ieee8021BridgeDot1dPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ieee8021BridgeDot1dPortTable_del(struct ieee8021BridgeDot1dPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the ieee8021BridgeDot1dPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
ieee8021BridgeDot1dPortTable_del(struct ieee8021BridgeDot1dPortTable_data *thedata)
{
	struct ieee8021BridgeDot1dPortTable_data *StorageDel;

	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeDot1dPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(ieee8021BridgeDot1dPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&ieee8021BridgeDot1dPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ieee8021BridgeDot1dPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ieee8021BridgeDot1dPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case ieee8021BridgeDot1dPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_ieee8021BridgeDot1dPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct ieee8021BridgeDot1dPortTable_data *StorageTmp = ieee8021BridgeDot1dPortTable_create();

	DEBUGMSGTL(("ieee8021BridgeMib", "parse_ieee8021BridgeDot1dPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ieee8021BridgeDot1dPortRowStatus, &tmpsize);
	ieee8021BridgeDot1dPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/*
 * store_ieee8021BridgeDot1dPortTable(): store configuraiton file for ieee8021BridgeDot1dPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_ieee8021BridgeDot1dPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ieee8021BridgeDot1dPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("ieee8021BridgeMib", "store_ieee8021BridgeDot1dPortTable: storing data...  "));
	refresh_ieee8021BridgeDot1dPortTable(1);
	(void) tmpsize;
	for (hcindex = ieee8021BridgeDot1dPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct ieee8021BridgeDot1dPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "ieee8021BridgeDot1dPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePortComponentId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ieee8021BridgeBasePort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ieee8021BridgeDot1dPortRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, struct ieee8021BridgeBaseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, struct ieee8021BridgeBaseTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, struct ieee8021BridgeBaseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeBaseTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, struct ieee8021BridgeBaseTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeBaseTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, struct ieee8021BridgeBaseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, struct ieee8021BridgeBaseTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeBaseTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeBaseTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeBaseTable_data *
refresh_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeBaseTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeBaseTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeBaseTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeBaseTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeBaseTable(int force)
{
	if (!force && ieee8021BridgeBaseTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeBaseTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeBaseTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeBaseTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeBaseTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeBaseTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeBaseTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeBaseTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEBASEBRIDGEADDRESS:	/* Create */
		*write_method = write_ieee8021BridgeBaseBridgeAddress;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeBaseBridgeAddressLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeBaseBridgeAddress;
		break;
	case (u_char) IEEE8021BRIDGEBASENUMPORTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseNumPorts);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseNumPorts;
		break;
	case (u_char) IEEE8021BRIDGEBASECOMPONENTTYPE:	/* Create */
		*write_method = write_ieee8021BridgeBaseComponentType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseComponentType);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseComponentType;
		break;
	case (u_char) IEEE8021BRIDGEBASEDEVICECAPABILITIES:	/* Create */
		*write_method = write_ieee8021BridgeBaseDeviceCapabilities;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeBaseDeviceCapabilitiesLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeBaseDeviceCapabilities;
		break;
	case (u_char) IEEE8021BRIDGEBASETRAFFICCLASSESENABLED:	/* Create */
		*write_method = write_ieee8021BridgeBaseTrafficClassesEnabled;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled;
		break;
	case (u_char) IEEE8021BRIDGEBASEMMRPENABLEDSTATUS:	/* Create */
		*write_method = write_ieee8021BridgeBaseMmrpEnabledStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus;
		break;
	case (u_char) IEEE8021BRIDGEBASEROWSTATUS:	/* Create */
		*write_method = write_ieee8021BridgeBaseRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, struct ieee8021BridgeBasePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, struct ieee8021BridgeBasePortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, struct ieee8021BridgeBasePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeBasePortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, struct ieee8021BridgeBasePortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeBasePortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, struct ieee8021BridgeBasePortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, struct ieee8021BridgeBasePortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeBasePortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeBasePortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeBasePortTable_data *
refresh_ieee8021BridgeBasePortTable_row(struct ieee8021BridgeBasePortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeBasePortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeBasePortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeBasePortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeBasePortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeBasePortTable(int force)
{
	if (!force && ieee8021BridgeBasePortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeBasePortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeBasePortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeBasePortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeBasePortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeBasePortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeBasePortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeBasePortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeBasePortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeBasePortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEBASEPORTIFINDEX:	/* ReadWrite */
		*write_method = write_ieee8021BridgeBasePortIfIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortIfIndex;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTDELAYEXCEEDEDDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortDelayExceededDiscards);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortDelayExceededDiscards;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTMTUEXCEEDEDDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortMtuExceededDiscards);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortMtuExceededDiscards;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTCAPABILITIES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeBasePortCapabilitiesLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeBasePortCapabilities;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTTYPECAPABILITIES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeBasePortTypeCapabilitiesLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeBasePortTypeCapabilities;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortType);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortType;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTEXTERNAL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortExternal);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortExternal;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT:	/* ReadWrite */
		*write_method = write_ieee8021BridgeBasePortAdminPointToPoint;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortAdminPointToPoint);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortAdminPointToPoint;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTOPERPOINTTOPOINT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBasePortOperPointToPoint);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBasePortOperPointToPoint;
		break;
	case (u_char) IEEE8021BRIDGEBASEPORTNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgeBasePortNameLen;
		rval = (u_char *) StorageTmp->ieee8021BridgeBasePortName;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, struct ieee8021BridgeBaseIfToPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, struct ieee8021BridgeBaseIfToPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, struct ieee8021BridgeBaseIfToPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeBaseIfToPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, struct ieee8021BridgeBaseIfToPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeBaseIfToPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, struct ieee8021BridgeBaseIfToPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, struct ieee8021BridgeBaseIfToPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeBaseIfToPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeBaseIfToPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeBaseIfToPortTable_data *
refresh_ieee8021BridgeBaseIfToPortTable_row(struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeBaseIfToPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeBaseIfToPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeBaseIfToPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeBaseIfToPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeBaseIfToPortTable(int force)
{
	if (!force && ieee8021BridgeBaseIfToPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeBaseIfToPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeBaseIfToPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeBaseIfToPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeBaseIfToPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeBaseIfToPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeBaseIfToPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeBaseIfToPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeBaseIfToPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeBaseIfToPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEBASEIFINDEXCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseIfIndexComponentId);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseIfIndexComponentId;
		break;
	case (u_char) IEEE8021BRIDGEBASEIFINDEXPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeBaseIfIndexPort);
		rval = (u_char *) &StorageTmp->ieee8021BridgeBaseIfIndexPort;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, struct ieee8021BridgePhyPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, struct ieee8021BridgePhyPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, struct ieee8021BridgePhyPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePhyPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, struct ieee8021BridgePhyPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePhyPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, struct ieee8021BridgePhyPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, struct ieee8021BridgePhyPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePhyPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePhyPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePhyPortTable_data *
refresh_ieee8021BridgePhyPortTable_row(struct ieee8021BridgePhyPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePhyPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePhyPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePhyPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePhyPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePhyPortTable(int force)
{
	if (!force && ieee8021BridgePhyPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePhyPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePhyPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePhyPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePhyPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePhyPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePhyPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePhyPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePhyPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePhyPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPHYPORTIFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePhyPortIfIndex);
		rval = (u_char *) &StorageTmp->ieee8021BridgePhyPortIfIndex;
		break;
	case (u_char) IEEE8021BRIDGEPHYMACADDRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgePhyMacAddressLen;
		rval = (u_char *) StorageTmp->ieee8021BridgePhyMacAddress;
		break;
	case (u_char) IEEE8021BRIDGEPHYPORTTOCOMPONENTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePhyPortToComponentId);
		rval = (u_char *) &StorageTmp->ieee8021BridgePhyPortToComponentId;
		break;
	case (u_char) IEEE8021BRIDGEPHYPORTTOINTERNALPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePhyPortToInternalPort);
		rval = (u_char *) &StorageTmp->ieee8021BridgePhyPortToInternalPort;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, struct ieee8021BridgeTpPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, struct ieee8021BridgeTpPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, struct ieee8021BridgeTpPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeTpPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, struct ieee8021BridgeTpPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeTpPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, struct ieee8021BridgeTpPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, struct ieee8021BridgeTpPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeTpPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeTpPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeTpPortTable_data *
refresh_ieee8021BridgeTpPortTable_row(struct ieee8021BridgeTpPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeTpPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeTpPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeTpPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeTpPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeTpPortTable(int force)
{
	if (!force && ieee8021BridgeTpPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeTpPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeTpPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeTpPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeTpPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeTpPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeTpPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeTpPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeTpPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeTpPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGETPPORTMAXINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeTpPortMaxInfo);
		rval = (u_char *) &StorageTmp->ieee8021BridgeTpPortMaxInfo;
		break;
	case (u_char) IEEE8021BRIDGETPPORTINFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeTpPortInFrames);
		rval = (u_char *) &StorageTmp->ieee8021BridgeTpPortInFrames;
		break;
	case (u_char) IEEE8021BRIDGETPPORTOUTFRAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeTpPortOutFrames);
		rval = (u_char *) &StorageTmp->ieee8021BridgeTpPortOutFrames;
		break;
	case (u_char) IEEE8021BRIDGETPPORTINDISCARDS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeTpPortInDiscards);
		rval = (u_char *) &StorageTmp->ieee8021BridgeTpPortInDiscards;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, struct ieee8021BridgePortPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, struct ieee8021BridgePortPriorityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, struct ieee8021BridgePortPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePortPriorityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, struct ieee8021BridgePortPriorityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePortPriorityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, struct ieee8021BridgePortPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, struct ieee8021BridgePortPriorityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePortPriorityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePortPriorityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePortPriorityTable_data *
refresh_ieee8021BridgePortPriorityTable_row(struct ieee8021BridgePortPriorityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePortPriorityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePortPriorityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePortPriorityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePortPriorityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePortPriorityTable(int force)
{
	if (!force && ieee8021BridgePortPriorityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePortPriorityTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePortPriorityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePortPriorityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePortPriorityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePortPriorityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePortPriorityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePortPriorityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPORTDEFAULTUSERPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortDefaultUserPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortDefaultUserPriority);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortDefaultUserPriority;
		break;
	case (u_char) IEEE8021BRIDGEPORTNUMTRAFFICCLASSES:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortNumTrafficClasses;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortNumTrafficClasses);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortNumTrafficClasses;
		break;
	case (u_char) IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortPriorityCodePointSelection;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortPriorityCodePointSelection);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortPriorityCodePointSelection;
		break;
	case (u_char) IEEE8021BRIDGEPORTUSEDEI:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortUseDEI;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortUseDEI);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortUseDEI;
		break;
	case (u_char) IEEE8021BRIDGEPORTREQUIREDROPENCODING:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortRequireDropEncoding;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortRequireDropEncoding);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortRequireDropEncoding;
		break;
	case (u_char) IEEE8021BRIDGEPORTSERVICEACCESSPRIORITYSELECTION:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortServiceAccessPrioritySelection;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, struct ieee8021BridgeUserPriorityRegenTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, struct ieee8021BridgeUserPriorityRegenTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, struct ieee8021BridgeUserPriorityRegenTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeUserPriorityRegenTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, struct ieee8021BridgeUserPriorityRegenTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeUserPriorityRegenTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, struct ieee8021BridgeUserPriorityRegenTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, struct ieee8021BridgeUserPriorityRegenTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeUserPriorityRegenTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeUserPriorityRegenTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeUserPriorityRegenTable_data *
refresh_ieee8021BridgeUserPriorityRegenTable_row(struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeUserPriorityRegenTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeUserPriorityRegenTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeUserPriorityRegenTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeUserPriorityRegenTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeUserPriorityRegenTable(int force)
{
	if (!force && ieee8021BridgeUserPriorityRegenTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeUserPriorityRegenTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeUserPriorityRegenTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeUserPriorityRegenTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeUserPriorityRegenTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeUserPriorityRegenTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeUserPriorityRegenTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeUserPriorityRegenTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeUserPriorityRegenTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEREGENUSERPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021BridgeRegenUserPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeRegenUserPriority);
		rval = (u_char *) &StorageTmp->ieee8021BridgeRegenUserPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, struct ieee8021BridgeTrafficClassTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, struct ieee8021BridgeTrafficClassTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, struct ieee8021BridgeTrafficClassTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeTrafficClassTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, struct ieee8021BridgeTrafficClassTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeTrafficClassTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, struct ieee8021BridgeTrafficClassTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, struct ieee8021BridgeTrafficClassTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeTrafficClassTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeTrafficClassTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeTrafficClassTable_data *
refresh_ieee8021BridgeTrafficClassTable_row(struct ieee8021BridgeTrafficClassTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeTrafficClassTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeTrafficClassTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeTrafficClassTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeTrafficClassTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeTrafficClassTable(int force)
{
	if (!force && ieee8021BridgeTrafficClassTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeTrafficClassTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeTrafficClassTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeTrafficClassTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeTrafficClassTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeTrafficClassTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeTrafficClassTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeTrafficClassTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeTrafficClassTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeTrafficClassTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGETRAFFICCLASS:	/* ReadWrite */
		*write_method = write_ieee8021BridgeTrafficClass;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeTrafficClass);
		rval = (u_char *) &StorageTmp->ieee8021BridgeTrafficClass;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePortOutboundAccessPriorityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePortOutboundAccessPriorityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePortOutboundAccessPriorityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePortOutboundAccessPriorityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePortOutboundAccessPriorityTable_data *
refresh_ieee8021BridgePortOutboundAccessPriorityTable_row(struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePortOutboundAccessPriorityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePortOutboundAccessPriorityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePortOutboundAccessPriorityTable(int force)
{
	if (!force && ieee8021BridgePortOutboundAccessPriorityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePortOutboundAccessPriorityTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePortOutboundAccessPriorityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePortOutboundAccessPriorityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePortOutboundAccessPriorityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePortOutboundAccessPriorityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePortOutboundAccessPriorityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePortOutboundAccessPriorityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePortOutboundAccessPriorityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPORTOUTBOUNDACCESSPRIORITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortOutboundAccessPriority);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortOutboundAccessPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, struct ieee8021BridgePortDecodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, struct ieee8021BridgePortDecodingTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, struct ieee8021BridgePortDecodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePortDecodingTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, struct ieee8021BridgePortDecodingTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePortDecodingTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, struct ieee8021BridgePortDecodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, struct ieee8021BridgePortDecodingTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePortDecodingTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePortDecodingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePortDecodingTable_data *
refresh_ieee8021BridgePortDecodingTable_row(struct ieee8021BridgePortDecodingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePortDecodingTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePortDecodingTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePortDecodingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePortDecodingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePortDecodingTable(int force)
{
	if (!force && ieee8021BridgePortDecodingTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePortDecodingTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePortDecodingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePortDecodingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePortDecodingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePortDecodingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePortDecodingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePortDecodingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePortDecodingTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePortDecodingTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPORTDECODINGPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortDecodingPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortDecodingPriority);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortDecodingPriority;
		break;
	case (u_char) IEEE8021BRIDGEPORTDECODINGDROPELIGIBLE:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortDecodingDropEligible;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortDecodingDropEligible);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortDecodingDropEligible;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, struct ieee8021BridgePortEncodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, struct ieee8021BridgePortEncodingTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, struct ieee8021BridgePortEncodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePortEncodingTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, struct ieee8021BridgePortEncodingTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePortEncodingTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, struct ieee8021BridgePortEncodingTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, struct ieee8021BridgePortEncodingTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePortEncodingTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePortEncodingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePortEncodingTable_data *
refresh_ieee8021BridgePortEncodingTable_row(struct ieee8021BridgePortEncodingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePortEncodingTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePortEncodingTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePortEncodingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePortEncodingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePortEncodingTable(int force)
{
	if (!force && ieee8021BridgePortEncodingTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePortEncodingTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePortEncodingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePortEncodingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePortEncodingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePortEncodingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePortEncodingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePortEncodingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePortEncodingTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePortEncodingTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPORTENCODINGPRIORITY:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortEncodingPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortEncodingPriority);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortEncodingPriority;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, struct ieee8021BridgeServiceAccessPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, struct ieee8021BridgeServiceAccessPriorityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, struct ieee8021BridgeServiceAccessPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeServiceAccessPriorityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, struct ieee8021BridgeServiceAccessPriorityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeServiceAccessPriorityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, struct ieee8021BridgeServiceAccessPriorityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, struct ieee8021BridgeServiceAccessPriorityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeServiceAccessPriorityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeServiceAccessPriorityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeServiceAccessPriorityTable_data *
refresh_ieee8021BridgeServiceAccessPriorityTable_row(struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeServiceAccessPriorityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeServiceAccessPriorityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeServiceAccessPriorityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeServiceAccessPriorityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeServiceAccessPriorityTable(int force)
{
	if (!force && ieee8021BridgeServiceAccessPriorityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeServiceAccessPriorityTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeServiceAccessPriorityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeServiceAccessPriorityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeServiceAccessPriorityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeServiceAccessPriorityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeServiceAccessPriorityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeServiceAccessPriorityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeServiceAccessPriorityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGESERVICEACCESSPRIORITYVALUE:	/* ReadWrite */
		*write_method = write_ieee8021BridgeServiceAccessPriorityValue;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeServiceAccessPriorityValue);
		rval = (u_char *) &StorageTmp->ieee8021BridgeServiceAccessPriorityValue;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, struct ieee8021BridgePortMrpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, struct ieee8021BridgePortMrpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, struct ieee8021BridgePortMrpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePortMrpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, struct ieee8021BridgePortMrpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePortMrpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, struct ieee8021BridgePortMrpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, struct ieee8021BridgePortMrpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePortMrpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePortMrpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePortMrpTable_data *
refresh_ieee8021BridgePortMrpTable_row(struct ieee8021BridgePortMrpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePortMrpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePortMrpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePortMrpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePortMrpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePortMrpTable(int force)
{
	if (!force && ieee8021BridgePortMrpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePortMrpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePortMrpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePortMrpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePortMrpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePortMrpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePortMrpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePortMrpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePortMrpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePortMrpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPORTMRPJOINTIME:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortMrpJoinTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortMrpJoinTime);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortMrpJoinTime;
		break;
	case (u_char) IEEE8021BRIDGEPORTMRPLEAVETIME:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortMrpLeaveTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortMrpLeaveTime);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortMrpLeaveTime;
		break;
	case (u_char) IEEE8021BRIDGEPORTMRPLEAVEALLTIME:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortMrpLeaveAllTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortMrpLeaveAllTime);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortMrpLeaveAllTime;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, struct ieee8021BridgePortMmrpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, struct ieee8021BridgePortMmrpTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, struct ieee8021BridgePortMmrpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgePortMmrpTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, struct ieee8021BridgePortMmrpTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgePortMmrpTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, struct ieee8021BridgePortMmrpTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, struct ieee8021BridgePortMmrpTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgePortMmrpTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgePortMmrpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgePortMmrpTable_data *
refresh_ieee8021BridgePortMmrpTable_row(struct ieee8021BridgePortMmrpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgePortMmrpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgePortMmrpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgePortMmrpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgePortMmrpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgePortMmrpTable(int force)
{
	if (!force && ieee8021BridgePortMmrpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgePortMmrpTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgePortMmrpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgePortMmrpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgePortMmrpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgePortMmrpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgePortMmrpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgePortMmrpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgePortMmrpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgePortMmrpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEPORTMMRPENABLEDSTATUS:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortMmrpEnabledStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortMmrpEnabledStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortMmrpEnabledStatus;
		break;
	case (u_char) IEEE8021BRIDGEPORTMMRPFAILEDREGISTRATIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortMmrpFailedRegistrations);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortMmrpFailedRegistrations;
		break;
	case (u_char) IEEE8021BRIDGEPORTMMRPLASTPDUORIGIN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ieee8021BridgePortMmrpLastPduOriginLen;
		rval = (u_char *) StorageTmp->ieee8021BridgePortMmrpLastPduOrigin;
		break;
	case (u_char) IEEE8021BRIDGEPORTRESTRICTEDGROUPREGISTRATION:	/* ReadWrite */
		*write_method = write_ieee8021BridgePortRestrictedGroupRegistration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgePortRestrictedGroupRegistration);
		rval = (u_char *) &StorageTmp->ieee8021BridgePortRestrictedGroupRegistration;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, struct ieee8021BridgeILanIfTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, struct ieee8021BridgeILanIfTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, struct ieee8021BridgeILanIfTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeILanIfTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, struct ieee8021BridgeILanIfTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeILanIfTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, struct ieee8021BridgeILanIfTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, struct ieee8021BridgeILanIfTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeILanIfTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeILanIfTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeILanIfTable_data *
refresh_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeILanIfTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeILanIfTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeILanIfTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeILanIfTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeILanIfTable(int force)
{
	if (!force && ieee8021BridgeILanIfTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeILanIfTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeILanIfTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeILanIfTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeILanIfTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeILanIfTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeILanIfTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeILanIfTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeILanIfTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeILanIfTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEILANIFROWSTATUS:	/* Create */
		*write_method = write_ieee8021BridgeILanIfRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeILanIfRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgeILanIfRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, struct ieee8021BridgeDot1dPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, struct ieee8021BridgeDot1dPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, struct ieee8021BridgeDot1dPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_ieee8021BridgeDot1dPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, struct ieee8021BridgeDot1dPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	ieee8021BridgeDot1dPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, struct ieee8021BridgeDot1dPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, struct ieee8021BridgeDot1dPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_ieee8021BridgeDot1dPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the ieee8021BridgeDot1dPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct ieee8021BridgeDot1dPortTable_data *
refresh_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->ieee8021BridgeDot1dPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->ieee8021BridgeDot1dPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_ieee8021BridgeDot1dPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the ieee8021BridgeDot1dPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_ieee8021BridgeDot1dPortTable(int force)
{
	if (!force && ieee8021BridgeDot1dPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	ieee8021BridgeDot1dPortTable_refresh = 0;
}

/**
 * @fn u_char *var_ieee8021BridgeDot1dPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in ieee8021BridgeDot1dPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ieee8021BridgeMib above.
 */
u_char *
var_ieee8021BridgeDot1dPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ieee8021BridgeDot1dPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ieee8021BridgeMib", "var_ieee8021BridgeDot1dPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_ieee8021BridgeDot1dPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(ieee8021BridgeDot1dPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_ieee8021BridgeDot1dPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) IEEE8021BRIDGEDOT1DPORTROWSTATUS:	/* Create */
		*write_method = write_ieee8021BridgeDot1dPortRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ieee8021BridgeDot1dPortRowStatus);
		rval = (u_char *) &StorageTmp->ieee8021BridgeDot1dPortRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_ieee8021BridgeBaseBridgeAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBaseBridgeAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBaseBridgeAddress entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeBaseRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseBridgeAddress: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseBridgeAddress not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 6..6 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 6)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseBridgeAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old = ieee8021BridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBaseTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021BridgeBaseBridgeAddress);
		StorageTmp->ieee8021BridgeBaseBridgeAddress = string;
		StorageTmp->ieee8021BridgeBaseBridgeAddressLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBaseBridgeAddress for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
			StorageTmp->ieee8021BridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021BridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBaseTable_sets == 0)
			revert_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		if (StorageOld->ieee8021BridgeBaseBridgeAddress != NULL) {
			SNMP_FREE(StorageTmp->ieee8021BridgeBaseBridgeAddress);
			StorageTmp->ieee8021BridgeBaseBridgeAddress = StorageOld->ieee8021BridgeBaseBridgeAddress;
			StorageTmp->ieee8021BridgeBaseBridgeAddressLen = StorageOld->ieee8021BridgeBaseBridgeAddressLen;
			StorageOld->ieee8021BridgeBaseBridgeAddress = NULL;
			StorageOld->ieee8021BridgeBaseBridgeAddressLen = 0;
		}
		if (--StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBaseComponentType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBaseComponentType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBaseComponentType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeBaseRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseComponentType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseComponentType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseComponentType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021BRIDGEBASECOMPONENTTYPE_ICOMPONENT:
		case IEEE8021BRIDGEBASECOMPONENTTYPE_BCOMPONENT:
		case IEEE8021BRIDGEBASECOMPONENTTYPE_CVLANCOMPONENT:
		case IEEE8021BRIDGEBASECOMPONENTTYPE_SVLANCOMPONENT:
		case IEEE8021BRIDGEBASECOMPONENTTYPE_DBRIDGECOMPONENT:
		case IEEE8021BRIDGEBASECOMPONENTTYPE_ERCOMPONENT:
		case IEEE8021BRIDGEBASECOMPONENTTYPE_TCOMPONENT:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseComponentType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old = ieee8021BridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBaseTable_rsvs++;
		StorageTmp->ieee8021BridgeBaseComponentType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBaseComponentType for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
			StorageTmp->ieee8021BridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021BridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBaseTable_sets == 0)
			revert_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeBaseComponentType = StorageOld->ieee8021BridgeBaseComponentType;
		if (--StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBaseDeviceCapabilities(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBaseDeviceCapabilities(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBaseDeviceCapabilities entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeBaseRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseDeviceCapabilities: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseDeviceCapabilities not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseDeviceCapabilities: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseDeviceCapabilities: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old = ieee8021BridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBaseTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->ieee8021BridgeBaseDeviceCapabilities);
		StorageTmp->ieee8021BridgeBaseDeviceCapabilities = string;
		StorageTmp->ieee8021BridgeBaseDeviceCapabilitiesLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBaseDeviceCapabilities for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
			StorageTmp->ieee8021BridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021BridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBaseTable_sets == 0)
			revert_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		if (StorageOld->ieee8021BridgeBaseDeviceCapabilities != NULL) {
			SNMP_FREE(StorageTmp->ieee8021BridgeBaseDeviceCapabilities);
			StorageTmp->ieee8021BridgeBaseDeviceCapabilities = StorageOld->ieee8021BridgeBaseDeviceCapabilities;
			StorageTmp->ieee8021BridgeBaseDeviceCapabilitiesLen = StorageOld->ieee8021BridgeBaseDeviceCapabilitiesLen;
			StorageOld->ieee8021BridgeBaseDeviceCapabilities = NULL;
			StorageOld->ieee8021BridgeBaseDeviceCapabilitiesLen = 0;
		}
		if (--StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBaseTrafficClassesEnabled(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBaseTrafficClassesEnabled(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBaseTrafficClassesEnabled entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeBaseRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseTrafficClassesEnabled: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseTrafficClassesEnabled not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseTrafficClassesEnabled: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseTrafficClassesEnabled: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old = ieee8021BridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBaseTable_rsvs++;
		StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
			StorageTmp->ieee8021BridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021BridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBaseTable_sets == 0)
			revert_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeBaseTrafficClassesEnabled = StorageOld->ieee8021BridgeBaseTrafficClassesEnabled;
		if (--StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBaseMmrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBaseMmrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBaseMmrpEnabledStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->ieee8021BridgeBaseRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseMmrpEnabledStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseMmrpEnabledStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseMmrpEnabledStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseMmrpEnabledStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old = ieee8021BridgeBaseTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBaseTable_rsvs++;
		StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBaseTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBaseTable_sets == 0)
				if ((ret = update_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBaseTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
			StorageTmp->ieee8021BridgeBaseTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBaseTable_tsts = 0;
			StorageTmp->ieee8021BridgeBaseTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBaseTable_sets == 0)
			revert_ieee8021BridgeBaseTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeBaseMmrpEnabledStatus = StorageOld->ieee8021BridgeBaseMmrpEnabledStatus;
		if (--StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
			ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBasePortIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBasePortIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBasePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBasePortIfIndex entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBasePortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBasePortIfIndex not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBasePortIfIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBasePortIfIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBasePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old = ieee8021BridgeBasePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBasePortTable_rsvs++;
		StorageTmp->ieee8021BridgeBasePortIfIndex = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBasePortTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBasePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBasePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBasePortIfIndex for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBasePortTable_sets == 0)
				if ((ret = update_ieee8021BridgeBasePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBasePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) != NULL) {
			ieee8021BridgeBasePortTable_destroy(&StorageTmp->ieee8021BridgeBasePortTable_old);
			StorageTmp->ieee8021BridgeBasePortTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBasePortTable_tsts = 0;
			StorageTmp->ieee8021BridgeBasePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBasePortTable_sets == 0)
			revert_ieee8021BridgeBasePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeBasePortIfIndex = StorageOld->ieee8021BridgeBasePortIfIndex;
		if (--StorageTmp->ieee8021BridgeBasePortTable_rsvs == 0)
			ieee8021BridgeBasePortTable_destroy(&StorageTmp->ieee8021BridgeBasePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBasePortAdminPointToPoint(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeBasePortAdminPointToPoint(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBasePortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBasePortAdminPointToPoint entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeBasePortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBasePortAdminPointToPoint not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBasePortAdminPointToPoint: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value forceFalse */
		switch (set_value) {
		case IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT_FORCETRUE:
		case IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT_FORCEFALSE:
		case IEEE8021BRIDGEBASEPORTADMINPOINTTOPOINT_AUTO:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBasePortAdminPointToPoint: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeBasePortTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old = ieee8021BridgeBasePortTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeBasePortTable_rsvs++;
		StorageTmp->ieee8021BridgeBasePortAdminPointToPoint = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeBasePortTable_tsts == 0)
				if ((ret = check_ieee8021BridgeBasePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBasePortTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeBasePortAdminPointToPoint for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeBasePortTable_sets == 0)
				if ((ret = update_ieee8021BridgeBasePortTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeBasePortTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) != NULL) {
			ieee8021BridgeBasePortTable_destroy(&StorageTmp->ieee8021BridgeBasePortTable_old);
			StorageTmp->ieee8021BridgeBasePortTable_rsvs = 0;
			StorageTmp->ieee8021BridgeBasePortTable_tsts = 0;
			StorageTmp->ieee8021BridgeBasePortTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeBasePortTable_sets == 0)
			revert_ieee8021BridgeBasePortTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeBasePortTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeBasePortAdminPointToPoint = StorageOld->ieee8021BridgeBasePortAdminPointToPoint;
		if (--StorageTmp->ieee8021BridgeBasePortTable_rsvs == 0)
			ieee8021BridgeBasePortTable_destroy(&StorageTmp->ieee8021BridgeBasePortTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortDefaultUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortDefaultUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortDefaultUserPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDefaultUserPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDefaultUserPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDefaultUserPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old = ieee8021BridgePortPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgePortDefaultUserPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortDefaultUserPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
			revert_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortDefaultUserPriority = StorageOld->ieee8021BridgePortDefaultUserPriority;
		if (--StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortNumTrafficClasses(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortNumTrafficClasses(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortNumTrafficClasses entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortNumTrafficClasses not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortNumTrafficClasses: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..8 */
		if ((1 > set_value || set_value > 8)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortNumTrafficClasses: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old = ieee8021BridgePortPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgePortNumTrafficClasses = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortNumTrafficClasses for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
			revert_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortNumTrafficClasses = StorageOld->ieee8021BridgePortNumTrafficClasses;
		if (--StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortPriorityCodePointSelection(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortPriorityCodePointSelection(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortPriorityCodePointSelection entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortPriorityCodePointSelection not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortPriorityCodePointSelection: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION_CODEPOINT8P0D:
		case IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION_CODEPOINT7P1D:
		case IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION_CODEPOINT6P2D:
		case IEEE8021BRIDGEPORTPRIORITYCODEPOINTSELECTION_CODEPOINT5P3D:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortPriorityCodePointSelection: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old = ieee8021BridgePortPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgePortPriorityCodePointSelection = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortPriorityCodePointSelection for you to use, and you have just been asked to do something with it.  Note 
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
			revert_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortPriorityCodePointSelection = StorageOld->ieee8021BridgePortPriorityCodePointSelection;
		if (--StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortUseDEI(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortUseDEI(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortUseDEI entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortUseDEI not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortUseDEI: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortUseDEI: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old = ieee8021BridgePortPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgePortUseDEI = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortUseDEI for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
			revert_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortUseDEI = StorageOld->ieee8021BridgePortUseDEI;
		if (--StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortRequireDropEncoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortRequireDropEncoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortRequireDropEncoding entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortRequireDropEncoding not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortRequireDropEncoding: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortRequireDropEncoding: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old = ieee8021BridgePortPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgePortRequireDropEncoding = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortRequireDropEncoding for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
			revert_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortRequireDropEncoding = StorageOld->ieee8021BridgePortRequireDropEncoding;
		if (--StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortServiceAccessPrioritySelection(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortServiceAccessPrioritySelection(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortServiceAccessPrioritySelection entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortServiceAccessPrioritySelection not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortServiceAccessPrioritySelection: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortServiceAccessPrioritySelection: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old = ieee8021BridgePortPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) != NULL) {
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
			StorageTmp->ieee8021BridgePortPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortPriorityTable_sets == 0)
			revert_ieee8021BridgePortPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortServiceAccessPrioritySelection = StorageOld->ieee8021BridgePortServiceAccessPrioritySelection;
		if (--StorageTmp->ieee8021BridgePortPriorityTable_rsvs == 0)
			ieee8021BridgePortPriorityTable_destroy(&StorageTmp->ieee8021BridgePortPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeRegenUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeRegenUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeUserPriorityRegenTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeRegenUserPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeUserPriorityRegenTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeRegenUserPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeRegenUserPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeRegenUserPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeUserPriorityRegenTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old = ieee8021BridgeUserPriorityRegenTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeUserPriorityRegenTable_rsvs++;
		StorageTmp->ieee8021BridgeRegenUserPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeUserPriorityRegenTable_tsts == 0)
				if ((ret = check_ieee8021BridgeUserPriorityRegenTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeUserPriorityRegenTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeRegenUserPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeUserPriorityRegenTable_sets == 0)
				if ((ret = update_ieee8021BridgeUserPriorityRegenTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeUserPriorityRegenTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old) != NULL) {
			ieee8021BridgeUserPriorityRegenTable_destroy(&StorageTmp->ieee8021BridgeUserPriorityRegenTable_old);
			StorageTmp->ieee8021BridgeUserPriorityRegenTable_rsvs = 0;
			StorageTmp->ieee8021BridgeUserPriorityRegenTable_tsts = 0;
			StorageTmp->ieee8021BridgeUserPriorityRegenTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeUserPriorityRegenTable_sets == 0)
			revert_ieee8021BridgeUserPriorityRegenTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeUserPriorityRegenTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeRegenUserPriority = StorageOld->ieee8021BridgeRegenUserPriority;
		if (--StorageTmp->ieee8021BridgeUserPriorityRegenTable_rsvs == 0)
			ieee8021BridgeUserPriorityRegenTable_destroy(&StorageTmp->ieee8021BridgeUserPriorityRegenTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeTrafficClass(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeTrafficClass(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeTrafficClassTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeTrafficClass entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeTrafficClassTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeTrafficClass not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeTrafficClass: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeTrafficClass: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeTrafficClassTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old = ieee8021BridgeTrafficClassTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeTrafficClassTable_rsvs++;
		StorageTmp->ieee8021BridgeTrafficClass = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeTrafficClassTable_tsts == 0)
				if ((ret = check_ieee8021BridgeTrafficClassTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeTrafficClassTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeTrafficClass for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeTrafficClassTable_sets == 0)
				if ((ret = update_ieee8021BridgeTrafficClassTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeTrafficClassTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old) != NULL) {
			ieee8021BridgeTrafficClassTable_destroy(&StorageTmp->ieee8021BridgeTrafficClassTable_old);
			StorageTmp->ieee8021BridgeTrafficClassTable_rsvs = 0;
			StorageTmp->ieee8021BridgeTrafficClassTable_tsts = 0;
			StorageTmp->ieee8021BridgeTrafficClassTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeTrafficClassTable_sets == 0)
			revert_ieee8021BridgeTrafficClassTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeTrafficClassTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeTrafficClass = StorageOld->ieee8021BridgeTrafficClass;
		if (--StorageTmp->ieee8021BridgeTrafficClassTable_rsvs == 0)
			ieee8021BridgeTrafficClassTable_destroy(&StorageTmp->ieee8021BridgeTrafficClassTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeRegenUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeRegenUserPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortOutboundAccessPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeRegenUserPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortOutboundAccessPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeRegenUserPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeRegenUserPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeRegenUserPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old = ieee8021BridgePortOutboundAccessPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgeRegenUserPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortOutboundAccessPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeRegenUserPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgePortOutboundAccessPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old) != NULL) {
			ieee8021BridgePortOutboundAccessPriorityTable_destroy(&StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old);
			StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_sets == 0)
			revert_ieee8021BridgePortOutboundAccessPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeRegenUserPriority = StorageOld->ieee8021BridgeRegenUserPriority;
		if (--StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_rsvs == 0)
			ieee8021BridgePortOutboundAccessPriorityTable_destroy(&StorageTmp->ieee8021BridgePortOutboundAccessPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortDecodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortDecodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortDecodingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortDecodingPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortDecodingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDecodingPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDecodingPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDecodingPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortDecodingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old = ieee8021BridgePortDecodingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortDecodingTable_rsvs++;
		StorageTmp->ieee8021BridgePortDecodingPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortDecodingTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortDecodingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortDecodingPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortDecodingTable_sets == 0)
				if ((ret = update_ieee8021BridgePortDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortDecodingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) != NULL) {
			ieee8021BridgePortDecodingTable_destroy(&StorageTmp->ieee8021BridgePortDecodingTable_old);
			StorageTmp->ieee8021BridgePortDecodingTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortDecodingTable_tsts = 0;
			StorageTmp->ieee8021BridgePortDecodingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortDecodingTable_sets == 0)
			revert_ieee8021BridgePortDecodingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortDecodingPriority = StorageOld->ieee8021BridgePortDecodingPriority;
		if (--StorageTmp->ieee8021BridgePortDecodingTable_rsvs == 0)
			ieee8021BridgePortDecodingTable_destroy(&StorageTmp->ieee8021BridgePortDecodingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortDecodingDropEligible(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortDecodingDropEligible(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortDecodingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortDecodingDropEligible entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortDecodingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDecodingDropEligible not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDecodingDropEligible: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortDecodingDropEligible: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortDecodingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old = ieee8021BridgePortDecodingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortDecodingTable_rsvs++;
		StorageTmp->ieee8021BridgePortDecodingDropEligible = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortDecodingTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortDecodingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortDecodingDropEligible for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortDecodingTable_sets == 0)
				if ((ret = update_ieee8021BridgePortDecodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortDecodingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) != NULL) {
			ieee8021BridgePortDecodingTable_destroy(&StorageTmp->ieee8021BridgePortDecodingTable_old);
			StorageTmp->ieee8021BridgePortDecodingTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortDecodingTable_tsts = 0;
			StorageTmp->ieee8021BridgePortDecodingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortDecodingTable_sets == 0)
			revert_ieee8021BridgePortDecodingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortDecodingTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortDecodingDropEligible = StorageOld->ieee8021BridgePortDecodingDropEligible;
		if (--StorageTmp->ieee8021BridgePortDecodingTable_rsvs == 0)
			ieee8021BridgePortDecodingTable_destroy(&StorageTmp->ieee8021BridgePortDecodingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortEncodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortEncodingPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortEncodingTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortEncodingPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortEncodingTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortEncodingPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortEncodingPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortEncodingPriority: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortEncodingTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old = ieee8021BridgePortEncodingTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortEncodingTable_rsvs++;
		StorageTmp->ieee8021BridgePortEncodingPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortEncodingTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortEncodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortEncodingTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortEncodingPriority for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortEncodingTable_sets == 0)
				if ((ret = update_ieee8021BridgePortEncodingTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortEncodingTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old) != NULL) {
			ieee8021BridgePortEncodingTable_destroy(&StorageTmp->ieee8021BridgePortEncodingTable_old);
			StorageTmp->ieee8021BridgePortEncodingTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortEncodingTable_tsts = 0;
			StorageTmp->ieee8021BridgePortEncodingTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortEncodingTable_sets == 0)
			revert_ieee8021BridgePortEncodingTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortEncodingTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortEncodingPriority = StorageOld->ieee8021BridgePortEncodingPriority;
		if (--StorageTmp->ieee8021BridgePortEncodingTable_rsvs == 0)
			ieee8021BridgePortEncodingTable_destroy(&StorageTmp->ieee8021BridgePortEncodingTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeServiceAccessPriorityValue(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgeServiceAccessPriorityValue(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeServiceAccessPriorityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeServiceAccessPriorityValue entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgeServiceAccessPriorityTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeServiceAccessPriorityValue not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeServiceAccessPriorityValue: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..7 */
		if ((0 > set_value || set_value > 7)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeServiceAccessPriorityValue: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old) == NULL)
			if (StorageTmp->ieee8021BridgeServiceAccessPriorityTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old = ieee8021BridgeServiceAccessPriorityTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgeServiceAccessPriorityTable_rsvs++;
		StorageTmp->ieee8021BridgeServiceAccessPriorityValue = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgeServiceAccessPriorityTable_tsts == 0)
				if ((ret = check_ieee8021BridgeServiceAccessPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeServiceAccessPriorityTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgeServiceAccessPriorityValue for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgeServiceAccessPriorityTable_sets == 0)
				if ((ret = update_ieee8021BridgeServiceAccessPriorityTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgeServiceAccessPriorityTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old) != NULL) {
			ieee8021BridgeServiceAccessPriorityTable_destroy(&StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old);
			StorageTmp->ieee8021BridgeServiceAccessPriorityTable_rsvs = 0;
			StorageTmp->ieee8021BridgeServiceAccessPriorityTable_tsts = 0;
			StorageTmp->ieee8021BridgeServiceAccessPriorityTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgeServiceAccessPriorityTable_sets == 0)
			revert_ieee8021BridgeServiceAccessPriorityTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgeServiceAccessPriorityValue = StorageOld->ieee8021BridgeServiceAccessPriorityValue;
		if (--StorageTmp->ieee8021BridgeServiceAccessPriorityTable_rsvs == 0)
			ieee8021BridgeServiceAccessPriorityTable_destroy(&StorageTmp->ieee8021BridgeServiceAccessPriorityTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortMrpJoinTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortMrpJoinTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortMrpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortMrpJoinTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortMrpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpJoinTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpJoinTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpJoinTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortMrpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old = ieee8021BridgePortMrpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortMrpTable_rsvs++;
		StorageTmp->ieee8021BridgePortMrpJoinTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortMrpTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMrpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortMrpJoinTime for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortMrpTable_sets == 0)
				if ((ret = update_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMrpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			ieee8021BridgePortMrpTable_destroy(&StorageTmp->ieee8021BridgePortMrpTable_old);
			StorageTmp->ieee8021BridgePortMrpTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortMrpTable_tsts = 0;
			StorageTmp->ieee8021BridgePortMrpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortMrpTable_sets == 0)
			revert_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortMrpJoinTime = StorageOld->ieee8021BridgePortMrpJoinTime;
		if (--StorageTmp->ieee8021BridgePortMrpTable_rsvs == 0)
			ieee8021BridgePortMrpTable_destroy(&StorageTmp->ieee8021BridgePortMrpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortMrpLeaveTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortMrpLeaveTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortMrpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortMrpLeaveTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortMrpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpLeaveTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpLeaveTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 60 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpLeaveTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortMrpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old = ieee8021BridgePortMrpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortMrpTable_rsvs++;
		StorageTmp->ieee8021BridgePortMrpLeaveTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortMrpTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMrpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortMrpLeaveTime for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortMrpTable_sets == 0)
				if ((ret = update_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMrpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			ieee8021BridgePortMrpTable_destroy(&StorageTmp->ieee8021BridgePortMrpTable_old);
			StorageTmp->ieee8021BridgePortMrpTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortMrpTable_tsts = 0;
			StorageTmp->ieee8021BridgePortMrpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortMrpTable_sets == 0)
			revert_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortMrpLeaveTime = StorageOld->ieee8021BridgePortMrpLeaveTime;
		if (--StorageTmp->ieee8021BridgePortMrpTable_rsvs == 0)
			ieee8021BridgePortMrpTable_destroy(&StorageTmp->ieee8021BridgePortMrpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortMrpLeaveAllTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortMrpLeaveAllTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortMrpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortMrpLeaveAllTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortMrpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpLeaveAllTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpLeaveAllTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMrpLeaveAllTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortMrpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old = ieee8021BridgePortMrpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortMrpTable_rsvs++;
		StorageTmp->ieee8021BridgePortMrpLeaveAllTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortMrpTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMrpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortMrpLeaveAllTime for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortMrpTable_sets == 0)
				if ((ret = update_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMrpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) != NULL) {
			ieee8021BridgePortMrpTable_destroy(&StorageTmp->ieee8021BridgePortMrpTable_old);
			StorageTmp->ieee8021BridgePortMrpTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortMrpTable_tsts = 0;
			StorageTmp->ieee8021BridgePortMrpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortMrpTable_sets == 0)
			revert_ieee8021BridgePortMrpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMrpTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortMrpLeaveAllTime = StorageOld->ieee8021BridgePortMrpLeaveAllTime;
		if (--StorageTmp->ieee8021BridgePortMrpTable_rsvs == 0)
			ieee8021BridgePortMrpTable_destroy(&StorageTmp->ieee8021BridgePortMrpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortMmrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortMmrpEnabledStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortMmrpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortMmrpEnabledStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortMmrpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMmrpEnabledStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMmrpEnabledStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortMmrpEnabledStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortMmrpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old = ieee8021BridgePortMmrpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortMmrpTable_rsvs++;
		StorageTmp->ieee8021BridgePortMmrpEnabledStatus = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortMmrpTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortMmrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMmrpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortMmrpEnabledStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortMmrpTable_sets == 0)
				if ((ret = update_ieee8021BridgePortMmrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMmrpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) != NULL) {
			ieee8021BridgePortMmrpTable_destroy(&StorageTmp->ieee8021BridgePortMmrpTable_old);
			StorageTmp->ieee8021BridgePortMmrpTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortMmrpTable_tsts = 0;
			StorageTmp->ieee8021BridgePortMmrpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortMmrpTable_sets == 0)
			revert_ieee8021BridgePortMmrpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortMmrpEnabledStatus = StorageOld->ieee8021BridgePortMmrpEnabledStatus;
		if (--StorageTmp->ieee8021BridgePortMmrpTable_rsvs == 0)
			ieee8021BridgePortMmrpTable_destroy(&StorageTmp->ieee8021BridgePortMmrpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgePortRestrictedGroupRegistration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_ieee8021BridgePortRestrictedGroupRegistration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgePortMmrpTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 13;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgePortRestrictedGroupRegistration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(ieee8021BridgePortMmrpTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortRestrictedGroupRegistration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortRestrictedGroupRegistration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgePortRestrictedGroupRegistration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) == NULL)
			if (StorageTmp->ieee8021BridgePortMmrpTable_rsvs == 0)
				if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old = ieee8021BridgePortMmrpTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->ieee8021BridgePortMmrpTable_rsvs++;
		StorageTmp->ieee8021BridgePortRestrictedGroupRegistration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->ieee8021BridgePortMmrpTable_tsts == 0)
				if ((ret = check_ieee8021BridgePortMmrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMmrpTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->ieee8021BridgePortRestrictedGroupRegistration for you to use, and you have just been asked to do something with it.
				   Note that anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->ieee8021BridgePortMmrpTable_sets == 0)
				if ((ret = update_ieee8021BridgePortMmrpTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->ieee8021BridgePortMmrpTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) != NULL) {
			ieee8021BridgePortMmrpTable_destroy(&StorageTmp->ieee8021BridgePortMmrpTable_old);
			StorageTmp->ieee8021BridgePortMmrpTable_rsvs = 0;
			StorageTmp->ieee8021BridgePortMmrpTable_tsts = 0;
			StorageTmp->ieee8021BridgePortMmrpTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->ieee8021BridgePortMmrpTable_sets == 0)
			revert_ieee8021BridgePortMmrpTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->ieee8021BridgePortMmrpTable_old) == NULL)
			break;
		StorageTmp->ieee8021BridgePortRestrictedGroupRegistration = StorageOld->ieee8021BridgePortRestrictedGroupRegistration;
		if (--StorageTmp->ieee8021BridgePortMmrpTable_rsvs == 0)
			ieee8021BridgePortMmrpTable_destroy(&StorageTmp->ieee8021BridgePortMmrpTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021BridgeBaseTable_row(struct ieee8021BridgeBaseTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021BridgeILanIfTable_row(struct ieee8021BridgeILanIfTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_ieee8021BridgeDot1dPortTable_row(struct ieee8021BridgeDot1dPortTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeBaseRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021BridgeBaseRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeBaseTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021BridgeBaseTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeBaseRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021BridgeBaseTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeBaseRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBaseComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBaseComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBaseComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBaseComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021BridgeBaseTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021BridgeBaseTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBaseComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021BridgeBaseTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021BridgeBaseRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021BridgeBaseTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
				if (StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old = ieee8021BridgeBaseTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021BridgeBaseTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021BridgeBaseTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021BridgeBaseTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021BridgeBaseTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021BridgeBaseRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021BridgeBaseTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeBaseRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeBaseTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeBaseRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeBaseTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021BridgeBaseRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021BridgeBaseTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeBaseRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021BridgeBaseTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeBaseRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021BridgeBaseTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021BridgeBaseTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021BridgeBaseRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021BridgeBaseRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021BridgeBaseRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) != NULL) {
				ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
				StorageTmp->ieee8021BridgeBaseTable_rsvs = 0;
				StorageTmp->ieee8021BridgeBaseTable_tsts = 0;
				StorageTmp->ieee8021BridgeBaseTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021BridgeBaseTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021BridgeBaseTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021BridgeBaseRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021BridgeBaseTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021BridgeBaseRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021BridgeBaseTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021BridgeBaseTable_del(StorageNew);
				ieee8021BridgeBaseTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021BridgeBaseTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021BridgeBaseTable_rsvs == 0)
				ieee8021BridgeBaseTable_destroy(&StorageTmp->ieee8021BridgeBaseTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021BridgeBaseTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeILanIfRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021BridgeILanIfRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeILanIfTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021BridgeILanIfTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeILanIfRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021BridgeILanIfTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeILanIfRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeILanIfRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ifIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ifIndex */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ifIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (long) *vp->val.integer || (long) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ifIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021BridgeILanIfTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021BridgeILanIfTable_rsvs = 1;
			vp = vars;
			StorageNew->ifIndex = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021BridgeILanIfTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021BridgeILanIfRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021BridgeILanIfTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021BridgeILanIfTable_old) == NULL)
				if (StorageTmp->ieee8021BridgeILanIfTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021BridgeILanIfTable_old = ieee8021BridgeILanIfTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021BridgeILanIfTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021BridgeILanIfTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021BridgeILanIfTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021BridgeILanIfTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021BridgeILanIfRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021BridgeILanIfTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeILanIfRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeILanIfTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeILanIfRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeILanIfTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021BridgeILanIfRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must
		   be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021BridgeILanIfTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeILanIfRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021BridgeILanIfTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeILanIfRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021BridgeILanIfTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021BridgeILanIfTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021BridgeILanIfRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021BridgeILanIfRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021BridgeILanIfRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021BridgeILanIfTable_old) != NULL) {
				ieee8021BridgeILanIfTable_destroy(&StorageTmp->ieee8021BridgeILanIfTable_old);
				StorageTmp->ieee8021BridgeILanIfTable_rsvs = 0;
				StorageTmp->ieee8021BridgeILanIfTable_tsts = 0;
				StorageTmp->ieee8021BridgeILanIfTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021BridgeILanIfTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021BridgeILanIfTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021BridgeILanIfRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021BridgeILanIfTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021BridgeILanIfRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021BridgeILanIfTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021BridgeILanIfTable_del(StorageNew);
				ieee8021BridgeILanIfTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021BridgeILanIfTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021BridgeILanIfTable_rsvs == 0)
				ieee8021BridgeILanIfTable_destroy(&StorageTmp->ieee8021BridgeILanIfTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021BridgeILanIfTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_ieee8021BridgeDot1dPortRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_ieee8021BridgeDot1dPortRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct ieee8021BridgeDot1dPortTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct ieee8021BridgeDot1dPortTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 13;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("ieee8021BridgeMib", "write_ieee8021BridgeDot1dPortRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(ieee8021BridgeDot1dPortTableStorage, NULL, &name[13], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeDot1dPortRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to ieee8021BridgeDot1dPortRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* ieee8021BridgeBasePortComponentId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* ieee8021BridgeBasePort */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[13]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* ieee8021BridgeBasePortComponentId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..-1 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > -1)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePortComponentId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* ieee8021BridgeBasePort */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..65535 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 65535)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index ieee8021BridgeBasePort: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = ieee8021BridgeDot1dPortTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->ieee8021BridgeDot1dPortTable_rsvs = 1;
			vp = vars;
			StorageNew->ieee8021BridgeBasePortComponentId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->ieee8021BridgeBasePort = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&ieee8021BridgeDot1dPortTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->ieee8021BridgeDot1dPortTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->ieee8021BridgeDot1dPortTable_old) == NULL)
				if (StorageTmp->ieee8021BridgeDot1dPortTable_rsvs == 0)
					if ((StorageOld = StorageTmp->ieee8021BridgeDot1dPortTable_old = ieee8021BridgeDot1dPortTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->ieee8021BridgeDot1dPortTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->ieee8021BridgeDot1dPortTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				ieee8021BridgeDot1dPortTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_ieee8021BridgeDot1dPortTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus != RS_ACTIVE)
				if ((ret = can_act_ieee8021BridgeDot1dPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeDot1dPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_ieee8021BridgeDot1dPortTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->ieee8021BridgeDot1dPortRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_ieee8021BridgeDot1dPortTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_ieee8021BridgeDot1dPortTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_ieee8021BridgeDot1dPortTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_ieee8021BridgeDot1dPortTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->ieee8021BridgeDot1dPortRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->ieee8021BridgeDot1dPortRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->ieee8021BridgeDot1dPortRowStatus = set_value;
			if ((StorageOld = StorageTmp->ieee8021BridgeDot1dPortTable_old) != NULL) {
				ieee8021BridgeDot1dPortTable_destroy(&StorageTmp->ieee8021BridgeDot1dPortTable_old);
				StorageTmp->ieee8021BridgeDot1dPortTable_rsvs = 0;
				StorageTmp->ieee8021BridgeDot1dPortTable_tsts = 0;
				StorageTmp->ieee8021BridgeDot1dPortTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			ieee8021BridgeDot1dPortTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_ieee8021BridgeDot1dPortTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_ieee8021BridgeDot1dPortTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->ieee8021BridgeDot1dPortRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_ieee8021BridgeDot1dPortTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				ieee8021BridgeDot1dPortTable_del(StorageNew);
				ieee8021BridgeDot1dPortTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->ieee8021BridgeDot1dPortTable_old) == NULL)
				break;
			if (--StorageTmp->ieee8021BridgeDot1dPortTable_rsvs == 0)
				ieee8021BridgeDot1dPortTable_destroy(&StorageTmp->ieee8021BridgeDot1dPortTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				ieee8021BridgeDot1dPortTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void ieee8021BridgeMib_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ieee8021BridgeMib_loop_handler(int sig)
{
	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeMib_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (ieee8021BridgeMibold_signal_handler != NULL)
		(*ieee8021BridgeMibold_signal_handler) (sig);
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
}

/**
 * @fn void ieee8021BridgeMib_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ieee8021BridgeMib_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("ieee8021BridgeMib", "ieee8021BridgeMib_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("ieee8021BridgeMib", "done.\n"));
	return;
}
