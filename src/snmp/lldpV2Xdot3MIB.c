/*****************************************************************************

 @(#) src/snmp/lldpv2xdot3mib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/lldpv2xdot3mib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "lldpV2Xdot3MIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "lldpV2Xdot3MIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int lldpV2Xdot3MIB_refresh = 1;
volatile int lldpV2Xdot3PortConfigTable_refresh = 1;
volatile int lldpV2Xdot3LocPortTable_refresh = 1;
volatile int lldpV2Xdot3LocPowerTable_refresh = 1;
volatile int lldpV2Xdot3LocMaxFrameSizeTable_refresh = 1;
volatile int lldpV2Xdot3RemPortTable_refresh = 1;
volatile int lldpV2Xdot3RemPowerTable_refresh = 1;
volatile int lldpV2Xdot3RemMaxFrameSizeTable_refresh = 1;

/*
 * lldpV2Xdot3MIB_variables_oid: object identifier for lldpV2Xdot3MIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid lldpV2Xdot3MIB_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid lldpV2Xdot3TxRxCompliance_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623, 2, 1, 1 };
oid lldpV2Xdot3TxCompliance_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623, 2, 1, 2 };
oid lldpV2Xdot3RxCompliance_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623, 2, 1, 3 };
oid lldpV2Xdot3ConfigGroup_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623, 2, 2, 1 };
oid lldpV2Xdot3LocSysGroup_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623, 2, 2, 2 };
oid lldpV2Xdot3RemSysGroup_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 4623, 2, 2, 3 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 lldpV2Xdot3MIB_variables: tree for lldpV2Xdot3MIB
 * This variable defines function callbacks and type return information for the lldpV2Xdot3MIB mib section
 */
struct variable7 lldpV2Xdot3MIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   LLDPV2XDOT3PORTCONFIGTLVSTXENABLE  1
	{(u_char) LLDPV2XDOT3PORTCONFIGTLVSTXENABLE, ASN_OCTET_STR, RWRITE, var_lldpV2Xdot3PortConfigTable, 5, {1, 1, 1, 1, 1}},
#define   LLDPV2XDOT3LOCPORTAUTONEGSUPPORTED  2
	{(u_char) LLDPV2XDOT3LOCPORTAUTONEGSUPPORTED, ASN_INTEGER, RONLY, var_lldpV2Xdot3LocPortTable, 5, {1, 2, 1, 1, 1}},
#define   LLDPV2XDOT3LOCPORTAUTONEGENABLED  3
	{(u_char) LLDPV2XDOT3LOCPORTAUTONEGENABLED, ASN_INTEGER, RONLY, var_lldpV2Xdot3LocPortTable, 5, {1, 2, 1, 1, 2}},
#define   LLDPV2XDOT3LOCPORTAUTONEGADVERTISEDCAP  4
	{(u_char) LLDPV2XDOT3LOCPORTAUTONEGADVERTISEDCAP, ASN_OCTET_STR, RONLY, var_lldpV2Xdot3LocPortTable, 5, {1, 2, 1, 1, 3}},
#define   LLDPV2XDOT3LOCPORTOPERMAUTYPE  5
	{(u_char) LLDPV2XDOT3LOCPORTOPERMAUTYPE, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3LocPortTable, 5, {1, 2, 1, 1, 4}},
#define   LLDPV2XDOT3LOCPOWERPORTCLASS  6
	{(u_char) LLDPV2XDOT3LOCPOWERPORTCLASS, ASN_INTEGER, RONLY, var_lldpV2Xdot3LocPowerTable, 5, {1, 2, 2, 1, 1}},
#define   LLDPV2XDOT3LOCPOWERMDISUPPORTED  7
	{(u_char) LLDPV2XDOT3LOCPOWERMDISUPPORTED, ASN_INTEGER, RONLY, var_lldpV2Xdot3LocPowerTable, 5, {1, 2, 2, 1, 2}},
#define   LLDPV2XDOT3LOCPOWERMDIENABLED  8
	{(u_char) LLDPV2XDOT3LOCPOWERMDIENABLED, ASN_INTEGER, RONLY, var_lldpV2Xdot3LocPowerTable, 5, {1, 2, 2, 1, 3}},
#define   LLDPV2XDOT3LOCPOWERPAIRCONTROLABLE  9
	{(u_char) LLDPV2XDOT3LOCPOWERPAIRCONTROLABLE, ASN_INTEGER, RONLY, var_lldpV2Xdot3LocPowerTable, 5, {1, 2, 2, 1, 4}},
#define   LLDPV2XDOT3LOCPOWERPAIRS  10
	{(u_char) LLDPV2XDOT3LOCPOWERPAIRS, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3LocPowerTable, 5, {1, 2, 2, 1, 5}},
#define   LLDPV2XDOT3LOCPOWERCLASS  11
	{(u_char) LLDPV2XDOT3LOCPOWERCLASS, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3LocPowerTable, 5, {1, 2, 2, 1, 6}},
#define   LLDPV2XDOT3LOCMAXFRAMESIZE  12
	{(u_char) LLDPV2XDOT3LOCMAXFRAMESIZE, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3LocMaxFrameSizeTable, 5, {1, 2, 3, 1, 1}},
#define   LLDPV2XDOT3REMPORTAUTONEGSUPPORTED  13
	{(u_char) LLDPV2XDOT3REMPORTAUTONEGSUPPORTED, ASN_INTEGER, RONLY, var_lldpV2Xdot3RemPortTable, 5, {1, 3, 1, 1, 1}},
#define   LLDPV2XDOT3REMPORTAUTONEGENABLED  14
	{(u_char) LLDPV2XDOT3REMPORTAUTONEGENABLED, ASN_INTEGER, RONLY, var_lldpV2Xdot3RemPortTable, 5, {1, 3, 1, 1, 2}},
#define   LLDPV2XDOT3REMPORTAUTONEGADVERTISEDCAP  15
	{(u_char) LLDPV2XDOT3REMPORTAUTONEGADVERTISEDCAP, ASN_OCTET_STR, RONLY, var_lldpV2Xdot3RemPortTable, 5, {1, 3, 1, 1, 3}},
#define   LLDPV2XDOT3REMPORTOPERMAUTYPE  16
	{(u_char) LLDPV2XDOT3REMPORTOPERMAUTYPE, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3RemPortTable, 5, {1, 3, 1, 1, 4}},
#define   LLDPV2XDOT3REMPOWERPORTCLASS  17
	{(u_char) LLDPV2XDOT3REMPOWERPORTCLASS, ASN_INTEGER, RONLY, var_lldpV2Xdot3RemPowerTable, 5, {1, 3, 2, 1, 1}},
#define   LLDPV2XDOT3REMPOWERMDISUPPORTED  18
	{(u_char) LLDPV2XDOT3REMPOWERMDISUPPORTED, ASN_INTEGER, RONLY, var_lldpV2Xdot3RemPowerTable, 5, {1, 3, 2, 1, 2}},
#define   LLDPV2XDOT3REMPOWERMDIENABLED  19
	{(u_char) LLDPV2XDOT3REMPOWERMDIENABLED, ASN_INTEGER, RONLY, var_lldpV2Xdot3RemPowerTable, 5, {1, 3, 2, 1, 3}},
#define   LLDPV2XDOT3REMPOWERPAIRCONTROLABLE  20
	{(u_char) LLDPV2XDOT3REMPOWERPAIRCONTROLABLE, ASN_INTEGER, RONLY, var_lldpV2Xdot3RemPowerTable, 5, {1, 3, 2, 1, 4}},
#define   LLDPV2XDOT3REMPOWERPAIRS  21
	{(u_char) LLDPV2XDOT3REMPOWERPAIRS, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3RemPowerTable, 5, {1, 3, 2, 1, 5}},
#define   LLDPV2XDOT3REMPOWERCLASS  22
	{(u_char) LLDPV2XDOT3REMPOWERCLASS, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3RemPowerTable, 5, {1, 3, 2, 1, 6}},
#define   LLDPV2XDOT3REMMAXFRAMESIZE  23
	{(u_char) LLDPV2XDOT3REMMAXFRAMESIZE, ASN_UNSIGNED, RONLY, var_lldpV2Xdot3RemMaxFrameSizeTable, 5, {1, 3, 3, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct lldpV2Xdot3MIB_data *lldpV2Xdot3MIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *lldpV2Xdot3PortConfigTableStorage = NULL;
struct header_complex_index *lldpV2Xdot3LocPortTableStorage = NULL;
struct header_complex_index *lldpV2Xdot3LocPowerTableStorage = NULL;
struct header_complex_index *lldpV2Xdot3LocMaxFrameSizeTableStorage = NULL;
struct header_complex_index *lldpV2Xdot3RemPortTableStorage = NULL;
struct header_complex_index *lldpV2Xdot3RemPowerTableStorage = NULL;
struct header_complex_index *lldpV2Xdot3RemMaxFrameSizeTableStorage = NULL;

void (*lldpV2Xdot3MIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void lldpV2Xdot3MIB_loop_handler(int);
void lldpV2Xdot3MIB_fd_handler(int, void *);

/**
 * @fn void init_lldpV2Xdot3MIB(void)
 * @brief lldpV2Xdot3MIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (lldpV2Xdot3MIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_lldpV2Xdot3MIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "init_lldpV2Xdot3MIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("lldpV2Xdot3MIB", lldpV2Xdot3MIB_variables, variable7, lldpV2Xdot3MIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_lldpV2Xdot3MIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("lldpV2Xdot3MIB", parse_lldpV2Xdot3MIB, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3PortConfigTable", parse_lldpV2Xdot3PortConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3LocPortTable", parse_lldpV2Xdot3LocPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3LocPowerTable", parse_lldpV2Xdot3LocPowerTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3LocMaxFrameSizeTable", parse_lldpV2Xdot3LocMaxFrameSizeTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3RemPortTable", parse_lldpV2Xdot3RemPortTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3RemPowerTable", parse_lldpV2Xdot3RemPowerTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot3RemMaxFrameSizeTable", parse_lldpV2Xdot3RemMaxFrameSizeTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3MIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3PortConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3LocPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3LocPowerTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3LocMaxFrameSizeTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3RemPortTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3RemPowerTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot3RemMaxFrameSizeTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, lldpV2Xdot3MIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, lldpV2Xdot3MIB_fd_handler, (void *) 1);
	}
	lldpV2Xdot3MIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &lldpV2Xdot3MIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/**
 * @fn void deinit_lldpV2Xdot3MIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (lldpV2Xdot3MIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_lldpV2Xdot3MIB(void)
{
	DEBUGMSGTL(("lldpV2Xdot3MIB", "deinit_lldpV2Xdot3MIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = lldpV2Xdot3MIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(lldpV2Xdot3MIB_variables_oid, sizeof(lldpV2Xdot3MIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("lldpV2Xdot3MIB");
	snmpd_unregister_config_handler("lldpV2Xdot3PortConfigTable");
	snmpd_unregister_config_handler("lldpV2Xdot3LocPortTable");
	snmpd_unregister_config_handler("lldpV2Xdot3LocPowerTable");
	snmpd_unregister_config_handler("lldpV2Xdot3LocMaxFrameSizeTable");
	snmpd_unregister_config_handler("lldpV2Xdot3RemPortTable");
	snmpd_unregister_config_handler("lldpV2Xdot3RemPowerTable");
	snmpd_unregister_config_handler("lldpV2Xdot3RemMaxFrameSizeTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

int
term_lldpV2Xdot3MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("lldpV2Xdot3MIB", "term_lldpV2Xdot3MIB: terminating...  "));
	deinit_lldpV2Xdot3MIB();
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return 0;
}

/**
 * @fn struct lldpV2Xdot3MIB_data *lldpV2Xdot3MIB_create(void)
 * @brief create a fresh data structure representing scalars in lldpV2Xdot3MIB.
 *
 * Creates a new lldpV2Xdot3MIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in lldpV2Xdot3MIB.
 */
struct lldpV2Xdot3MIB_data *
lldpV2Xdot3MIB_create(void)
{
	struct lldpV2Xdot3MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3MIB_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3MIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3MIB_data *lldpV2Xdot3MIB_duplicate(struct lldpV2Xdot3MIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct lldpV2Xdot3MIB_data *
lldpV2Xdot3MIB_duplicate(struct lldpV2Xdot3MIB_data *thedata)
{
	struct lldpV2Xdot3MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3MIB_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3MIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3MIB_destroy(struct lldpV2Xdot3MIB_data **thedata)
 * @param thedata pointer to the data structure in lldpV2Xdot3MIB.
 * @brief delete a scalars structure from lldpV2Xdot3MIB.
 *
 * Frees scalars that were previously removed from lldpV2Xdot3MIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3MIB_destroy(struct lldpV2Xdot3MIB_data **thedata)
{
	struct lldpV2Xdot3MIB_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3MIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3MIB_add(struct lldpV2Xdot3MIB_data *thedata)
 * @param thedata the structure representing lldpV2Xdot3MIB scalars.
 * @brief adds node to the lldpV2Xdot3MIB scalar data set.
 *
 * Adds a scalar structure to the lldpV2Xdot3MIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
lldpV2Xdot3MIB_add(struct lldpV2Xdot3MIB_data *thedata)
{
	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3MIB_add: adding data...  "));
	if (thedata)
		lldpV2Xdot3MIBStorage = thedata;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3MIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3MIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3MIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_lldpV2Xdot3MIB(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3MIB_data *StorageTmp = lldpV2Xdot3MIB_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3MIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	lldpV2Xdot3MIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3MIB(): stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3MIB_data *StorageTmp;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3MIB: storing data...  "));
	refresh_lldpV2Xdot3MIB(1);
	if ((StorageTmp = lldpV2Xdot3MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpV2Xdot3MIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "lldpV2Xdot3MIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpV2Xdot3MIB(struct lldpV2Xdot3MIB_data *StorageTmp, struct lldpV2Xdot3MIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_lldpV2Xdot3MIB(struct lldpV2Xdot3MIB_data *StorageTmp, struct lldpV2Xdot3MIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3MIB(struct lldpV2Xdot3MIB_data *StorageTmp, struct lldpV2Xdot3MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_lldpV2Xdot3MIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3MIB(struct lldpV2Xdot3MIB_data *StorageTmp, struct lldpV2Xdot3MIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3MIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_lldpV2Xdot3MIB(struct 
 * @fn void revert_lldpV2Xdot3MIB(struct lldpV2Xdot3MIB_data *StorageTmp, struct lldpV2Xdot3MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_lldpV2Xdot3MIB(struct lldpV2Xdot3MIB_data *StorageTmp, struct lldpV2Xdot3MIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3MIB(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3MIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of lldpV2Xdot3MIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_lldpV2Xdot3MIB(int force)
{
	if (lldpV2Xdot3MIBStorage == NULL) {
		struct lldpV2Xdot3MIB_data *StorageNew;

		if ((StorageNew = lldpV2Xdot3MIB_create()) == NULL)
			return;
		lldpV2Xdot3MIBStorage = StorageNew;
		lldpV2Xdot3MIB_refresh = 1;
	}
	if (!force && lldpV2Xdot3MIB_refresh == 0)
		return;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "refresh_lldpV2Xdot3MIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	lldpV2Xdot3MIB_refresh = 0;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/**
 * @fn u_char * var_lldpV2Xdot3MIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in lldpV2Xdot3MIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_lldpV2Xdot3MIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3MIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3MIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_lldpV2Xdot3MIB(0);
	if ((StorageTmp = lldpV2Xdot3MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpV2Xdot3MIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("lldpV2Xdot3MIB", "found.\n"));
	else
		DEBUGMSGTL(("lldpV2Xdot3MIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct lldpV2Xdot3PortConfigTable_data *lldpV2Xdot3PortConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3PortConfigTable table.
 *
 * Creates a new lldpV2Xdot3PortConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3PortConfigTable_data *
lldpV2Xdot3PortConfigTable_create(void)
{
	struct lldpV2Xdot3PortConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3PortConfigTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3PortConfigTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2PortConfigIfIndex = 0;
		StorageNew->lldpV2PortConfigDestAddressIndex = 0;
		if (memdup((u_char **) &StorageNew->lldpV2Xdot3PortConfigTLVsTxEnable, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2Xdot3PortConfigTLVsTxEnableLen = 1;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3PortConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3PortConfigTable_data *lldpV2Xdot3PortConfigTable_duplicate(struct lldpV2Xdot3PortConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3PortConfigTable_data *
lldpV2Xdot3PortConfigTable_duplicate(struct lldpV2Xdot3PortConfigTable_data *thedata)
{
	struct lldpV2Xdot3PortConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3PortConfigTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3PortConfigTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3PortConfigTable_id = thedata->lldpV2Xdot3PortConfigTable_id;
		StorageNew->lldpV2PortConfigIfIndex = thedata->lldpV2PortConfigIfIndex;
		StorageNew->lldpV2PortConfigDestAddressIndex = thedata->lldpV2PortConfigDestAddressIndex;
		if (!(StorageNew->lldpV2Xdot3PortConfigTLVsTxEnable = malloc(thedata->lldpV2Xdot3PortConfigTLVsTxEnableLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot3PortConfigTLVsTxEnable, thedata->lldpV2Xdot3PortConfigTLVsTxEnable, thedata->lldpV2Xdot3PortConfigTLVsTxEnableLen);
		StorageNew->lldpV2Xdot3PortConfigTLVsTxEnableLen = thedata->lldpV2Xdot3PortConfigTLVsTxEnableLen;
		StorageNew->lldpV2Xdot3PortConfigTLVsTxEnable[StorageNew->lldpV2Xdot3PortConfigTLVsTxEnableLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3PortConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3PortConfigTable_destroy(struct lldpV2Xdot3PortConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3PortConfigTable_destroy(struct lldpV2Xdot3PortConfigTable_data **thedata)
{
	struct lldpV2Xdot3PortConfigTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3PortConfigTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot3PortConfigTLVsTxEnable);
		StorageDel->lldpV2Xdot3PortConfigTLVsTxEnableLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3PortConfigTable_add(struct lldpV2Xdot3PortConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3PortConfigTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3PortConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3PortConfigTable_add(struct lldpV2Xdot3PortConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3PortConfigTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2PortConfigIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2PortConfigIfIndex, sizeof(thedata->lldpV2PortConfigIfIndex));
		/* lldpV2PortConfigDestAddressIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2PortConfigDestAddressIndex, sizeof(thedata->lldpV2PortConfigDestAddressIndex));
		header_complex_add_data(&lldpV2Xdot3PortConfigTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3PortConfigTable_del(struct lldpV2Xdot3PortConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3PortConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3PortConfigTable_del(struct lldpV2Xdot3PortConfigTable_data *thedata)
{
	struct lldpV2Xdot3PortConfigTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3PortConfigTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3PortConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3PortConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3PortConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3PortConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3PortConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3PortConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3PortConfigTable_data *StorageTmp = lldpV2Xdot3PortConfigTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3PortConfigTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable, &StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnableLen);
	if (StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable == NULL) {
		config_perror("invalid specification for lldpV2Xdot3PortConfigTLVsTxEnable");
		return;
	}
	lldpV2Xdot3PortConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3PortConfigTable(): store configuraiton file for lldpV2Xdot3PortConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3PortConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3PortConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3PortConfigTable: storing data...  "));
	refresh_lldpV2Xdot3PortConfigTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3PortConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3PortConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3PortConfigTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable, &StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnableLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot3LocPortTable_data *lldpV2Xdot3LocPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3LocPortTable table.
 *
 * Creates a new lldpV2Xdot3LocPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3LocPortTable_data *
lldpV2Xdot3LocPortTable_create(void)
{
	struct lldpV2Xdot3LocPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3LocPortTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot3LocPortAutoNegSupported = 0;
		StorageNew->lldpV2Xdot3LocPortAutoNegEnabled = 0;
		if ((StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCap = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen = 0;
		StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCap[StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen] = 0;
		StorageNew->lldpV2Xdot3LocPortOperMauType = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3LocPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3LocPortTable_data *lldpV2Xdot3LocPortTable_duplicate(struct lldpV2Xdot3LocPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3LocPortTable_data *
lldpV2Xdot3LocPortTable_duplicate(struct lldpV2Xdot3LocPortTable_data *thedata)
{
	struct lldpV2Xdot3LocPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3LocPortTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3LocPortTable_id = thedata->lldpV2Xdot3LocPortTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot3LocPortAutoNegSupported = thedata->lldpV2Xdot3LocPortAutoNegSupported;
		StorageNew->lldpV2Xdot3LocPortAutoNegEnabled = thedata->lldpV2Xdot3LocPortAutoNegEnabled;
		if (!(StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCap = malloc(thedata->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCap, thedata->lldpV2Xdot3LocPortAutoNegAdvertisedCap, thedata->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen);
		StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen = thedata->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen;
		StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCap[StorageNew->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen] = 0;
		StorageNew->lldpV2Xdot3LocPortOperMauType = thedata->lldpV2Xdot3LocPortOperMauType;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3LocPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3LocPortTable_destroy(struct lldpV2Xdot3LocPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3LocPortTable_destroy(struct lldpV2Xdot3LocPortTable_data **thedata)
{
	struct lldpV2Xdot3LocPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot3LocPortAutoNegAdvertisedCap);
		StorageDel->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3LocPortTable_add(struct lldpV2Xdot3LocPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3LocPortTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3LocPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3LocPortTable_add(struct lldpV2Xdot3LocPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot3LocPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3LocPortTable_del(struct lldpV2Xdot3LocPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3LocPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3LocPortTable_del(struct lldpV2Xdot3LocPortTable_data *thedata)
{
	struct lldpV2Xdot3LocPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3LocPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3LocPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3LocPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3LocPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3LocPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3LocPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3LocPortTable_data *StorageTmp = lldpV2Xdot3LocPortTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3LocPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3LocPortAutoNegSupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3LocPortAutoNegEnabled, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCap);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCap, &StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen);
	if (StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCap == NULL) {
		config_perror("invalid specification for lldpV2Xdot3LocPortAutoNegAdvertisedCap");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3LocPortOperMauType, &tmpsize);
	lldpV2Xdot3LocPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3LocPortTable(): store configuraiton file for lldpV2Xdot3LocPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3LocPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3LocPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3LocPortTable: storing data...  "));
	refresh_lldpV2Xdot3LocPortTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3LocPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3LocPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3LocPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3LocPortAutoNegSupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3LocPortAutoNegEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCap, &StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3LocPortOperMauType, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot3LocPowerTable_data *lldpV2Xdot3LocPowerTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3LocPowerTable table.
 *
 * Creates a new lldpV2Xdot3LocPowerTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3LocPowerTable_data *
lldpV2Xdot3LocPowerTable_create(void)
{
	struct lldpV2Xdot3LocPowerTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3LocPowerTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPowerTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot3LocPowerPortClass = 0;
		StorageNew->lldpV2Xdot3LocPowerMDISupported = 0;
		StorageNew->lldpV2Xdot3LocPowerMDIEnabled = 0;
		StorageNew->lldpV2Xdot3LocPowerPairControlable = 0;
		StorageNew->lldpV2Xdot3LocPowerPairs = 0;
		StorageNew->lldpV2Xdot3LocPowerClass = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3LocPowerTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3LocPowerTable_data *lldpV2Xdot3LocPowerTable_duplicate(struct lldpV2Xdot3LocPowerTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3LocPowerTable_data *
lldpV2Xdot3LocPowerTable_duplicate(struct lldpV2Xdot3LocPowerTable_data *thedata)
{
	struct lldpV2Xdot3LocPowerTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3LocPowerTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPowerTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3LocPowerTable_id = thedata->lldpV2Xdot3LocPowerTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot3LocPowerPortClass = thedata->lldpV2Xdot3LocPowerPortClass;
		StorageNew->lldpV2Xdot3LocPowerMDISupported = thedata->lldpV2Xdot3LocPowerMDISupported;
		StorageNew->lldpV2Xdot3LocPowerMDIEnabled = thedata->lldpV2Xdot3LocPowerMDIEnabled;
		StorageNew->lldpV2Xdot3LocPowerPairControlable = thedata->lldpV2Xdot3LocPowerPairControlable;
		StorageNew->lldpV2Xdot3LocPowerPairs = thedata->lldpV2Xdot3LocPowerPairs;
		StorageNew->lldpV2Xdot3LocPowerClass = thedata->lldpV2Xdot3LocPowerClass;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3LocPowerTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3LocPowerTable_destroy(struct lldpV2Xdot3LocPowerTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3LocPowerTable_destroy(struct lldpV2Xdot3LocPowerTable_data **thedata)
{
	struct lldpV2Xdot3LocPowerTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPowerTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3LocPowerTable_add(struct lldpV2Xdot3LocPowerTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3LocPowerTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3LocPowerTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3LocPowerTable_add(struct lldpV2Xdot3LocPowerTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPowerTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot3LocPowerTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3LocPowerTable_del(struct lldpV2Xdot3LocPowerTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3LocPowerTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3LocPowerTable_del(struct lldpV2Xdot3LocPowerTable_data *thedata)
{
	struct lldpV2Xdot3LocPowerTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocPowerTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3LocPowerTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3LocPowerTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3LocPowerTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3LocPowerTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3LocPowerTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3LocPowerTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3LocPowerTable_data *StorageTmp = lldpV2Xdot3LocPowerTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3LocPowerTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3LocPowerPortClass, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3LocPowerMDISupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3LocPowerMDIEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3LocPowerPairControlable, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3LocPowerPairs, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3LocPowerClass, &tmpsize);
	lldpV2Xdot3LocPowerTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3LocPowerTable(): store configuraiton file for lldpV2Xdot3LocPowerTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3LocPowerTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3LocPowerTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3LocPowerTable: storing data...  "));
	refresh_lldpV2Xdot3LocPowerTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3LocPowerTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3LocPowerTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3LocPowerTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3LocPowerPortClass, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3LocPowerMDISupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3LocPowerMDIEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3LocPowerPairControlable, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3LocPowerPairs, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3LocPowerClass, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot3LocMaxFrameSizeTable_data *lldpV2Xdot3LocMaxFrameSizeTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3LocMaxFrameSizeTable table.
 *
 * Creates a new lldpV2Xdot3LocMaxFrameSizeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3LocMaxFrameSizeTable_data *
lldpV2Xdot3LocMaxFrameSizeTable_create(void)
{
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3LocMaxFrameSizeTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocMaxFrameSizeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot3LocMaxFrameSize = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3LocMaxFrameSizeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3LocMaxFrameSizeTable_data *lldpV2Xdot3LocMaxFrameSizeTable_duplicate(struct lldpV2Xdot3LocMaxFrameSizeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3LocMaxFrameSizeTable_data *
lldpV2Xdot3LocMaxFrameSizeTable_duplicate(struct lldpV2Xdot3LocMaxFrameSizeTable_data *thedata)
{
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3LocMaxFrameSizeTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocMaxFrameSizeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3LocMaxFrameSizeTable_id = thedata->lldpV2Xdot3LocMaxFrameSizeTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot3LocMaxFrameSize = thedata->lldpV2Xdot3LocMaxFrameSize;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3LocMaxFrameSizeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3LocMaxFrameSizeTable_destroy(struct lldpV2Xdot3LocMaxFrameSizeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3LocMaxFrameSizeTable_destroy(struct lldpV2Xdot3LocMaxFrameSizeTable_data **thedata)
{
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocMaxFrameSizeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3LocMaxFrameSizeTable_add(struct lldpV2Xdot3LocMaxFrameSizeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3LocMaxFrameSizeTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3LocMaxFrameSizeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3LocMaxFrameSizeTable_add(struct lldpV2Xdot3LocMaxFrameSizeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocMaxFrameSizeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot3LocMaxFrameSizeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3LocMaxFrameSizeTable_del(struct lldpV2Xdot3LocMaxFrameSizeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3LocMaxFrameSizeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3LocMaxFrameSizeTable_del(struct lldpV2Xdot3LocMaxFrameSizeTable_data *thedata)
{
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3LocMaxFrameSizeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3LocMaxFrameSizeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3LocMaxFrameSizeTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3LocMaxFrameSizeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3LocMaxFrameSizeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3LocMaxFrameSizeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3LocMaxFrameSizeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp = lldpV2Xdot3LocMaxFrameSizeTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3LocMaxFrameSizeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3LocMaxFrameSize, &tmpsize);
	lldpV2Xdot3LocMaxFrameSizeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3LocMaxFrameSizeTable(): store configuraiton file for lldpV2Xdot3LocMaxFrameSizeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3LocMaxFrameSizeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3LocMaxFrameSizeTable: storing data...  "));
	refresh_lldpV2Xdot3LocMaxFrameSizeTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3LocMaxFrameSizeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3LocMaxFrameSizeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3LocMaxFrameSizeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3LocMaxFrameSize, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot3RemPortTable_data *lldpV2Xdot3RemPortTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3RemPortTable table.
 *
 * Creates a new lldpV2Xdot3RemPortTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3RemPortTable_data *
lldpV2Xdot3RemPortTable_create(void)
{
	struct lldpV2Xdot3RemPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3RemPortTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPortTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		StorageNew->lldpV2Xdot3RemPortAutoNegSupported = 0;
		StorageNew->lldpV2Xdot3RemPortAutoNegEnabled = 0;
		if ((StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCap = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen = 0;
		StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCap[StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen] = 0;
		StorageNew->lldpV2Xdot3RemPortOperMauType = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3RemPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3RemPortTable_data *lldpV2Xdot3RemPortTable_duplicate(struct lldpV2Xdot3RemPortTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3RemPortTable_data *
lldpV2Xdot3RemPortTable_duplicate(struct lldpV2Xdot3RemPortTable_data *thedata)
{
	struct lldpV2Xdot3RemPortTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3RemPortTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPortTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3RemPortTable_id = thedata->lldpV2Xdot3RemPortTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot3RemPortAutoNegSupported = thedata->lldpV2Xdot3RemPortAutoNegSupported;
		StorageNew->lldpV2Xdot3RemPortAutoNegEnabled = thedata->lldpV2Xdot3RemPortAutoNegEnabled;
		if (!(StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCap = malloc(thedata->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCap, thedata->lldpV2Xdot3RemPortAutoNegAdvertisedCap, thedata->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen);
		StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen = thedata->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen;
		StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCap[StorageNew->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen] = 0;
		StorageNew->lldpV2Xdot3RemPortOperMauType = thedata->lldpV2Xdot3RemPortOperMauType;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3RemPortTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3RemPortTable_destroy(struct lldpV2Xdot3RemPortTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3RemPortTable_destroy(struct lldpV2Xdot3RemPortTable_data **thedata)
{
	struct lldpV2Xdot3RemPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPortTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot3RemPortAutoNegAdvertisedCap);
		StorageDel->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3RemPortTable_add(struct lldpV2Xdot3RemPortTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3RemPortTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3RemPortTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3RemPortTable_add(struct lldpV2Xdot3RemPortTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPortTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot3RemPortTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3RemPortTable_del(struct lldpV2Xdot3RemPortTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3RemPortTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3RemPortTable_del(struct lldpV2Xdot3RemPortTable_data *thedata)
{
	struct lldpV2Xdot3RemPortTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPortTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3RemPortTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3RemPortTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3RemPortTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3RemPortTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3RemPortTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3RemPortTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3RemPortTable_data *StorageTmp = lldpV2Xdot3RemPortTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3RemPortTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3RemPortAutoNegSupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3RemPortAutoNegEnabled, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCap);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCap, &StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen);
	if (StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCap == NULL) {
		config_perror("invalid specification for lldpV2Xdot3RemPortAutoNegAdvertisedCap");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3RemPortOperMauType, &tmpsize);
	lldpV2Xdot3RemPortTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3RemPortTable(): store configuraiton file for lldpV2Xdot3RemPortTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3RemPortTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3RemPortTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3RemPortTable: storing data...  "));
	refresh_lldpV2Xdot3RemPortTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3RemPortTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3RemPortTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3RemPortTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3RemPortAutoNegSupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3RemPortAutoNegEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCap, &StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3RemPortOperMauType, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot3RemPowerTable_data *lldpV2Xdot3RemPowerTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3RemPowerTable table.
 *
 * Creates a new lldpV2Xdot3RemPowerTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3RemPowerTable_data *
lldpV2Xdot3RemPowerTable_create(void)
{
	struct lldpV2Xdot3RemPowerTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3RemPowerTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPowerTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		StorageNew->lldpV2Xdot3RemPowerPortClass = 0;
		StorageNew->lldpV2Xdot3RemPowerMDISupported = 0;
		StorageNew->lldpV2Xdot3RemPowerMDIEnabled = 0;
		StorageNew->lldpV2Xdot3RemPowerPairControlable = 0;
		StorageNew->lldpV2Xdot3RemPowerPairs = 0;
		StorageNew->lldpV2Xdot3RemPowerClass = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3RemPowerTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3RemPowerTable_data *lldpV2Xdot3RemPowerTable_duplicate(struct lldpV2Xdot3RemPowerTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3RemPowerTable_data *
lldpV2Xdot3RemPowerTable_duplicate(struct lldpV2Xdot3RemPowerTable_data *thedata)
{
	struct lldpV2Xdot3RemPowerTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3RemPowerTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPowerTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3RemPowerTable_id = thedata->lldpV2Xdot3RemPowerTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot3RemPowerPortClass = thedata->lldpV2Xdot3RemPowerPortClass;
		StorageNew->lldpV2Xdot3RemPowerMDISupported = thedata->lldpV2Xdot3RemPowerMDISupported;
		StorageNew->lldpV2Xdot3RemPowerMDIEnabled = thedata->lldpV2Xdot3RemPowerMDIEnabled;
		StorageNew->lldpV2Xdot3RemPowerPairControlable = thedata->lldpV2Xdot3RemPowerPairControlable;
		StorageNew->lldpV2Xdot3RemPowerPairs = thedata->lldpV2Xdot3RemPowerPairs;
		StorageNew->lldpV2Xdot3RemPowerClass = thedata->lldpV2Xdot3RemPowerClass;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3RemPowerTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3RemPowerTable_destroy(struct lldpV2Xdot3RemPowerTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3RemPowerTable_destroy(struct lldpV2Xdot3RemPowerTable_data **thedata)
{
	struct lldpV2Xdot3RemPowerTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPowerTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3RemPowerTable_add(struct lldpV2Xdot3RemPowerTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3RemPowerTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3RemPowerTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3RemPowerTable_add(struct lldpV2Xdot3RemPowerTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPowerTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot3RemPowerTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3RemPowerTable_del(struct lldpV2Xdot3RemPowerTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3RemPowerTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3RemPowerTable_del(struct lldpV2Xdot3RemPowerTable_data *thedata)
{
	struct lldpV2Xdot3RemPowerTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemPowerTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3RemPowerTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3RemPowerTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3RemPowerTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3RemPowerTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3RemPowerTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3RemPowerTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3RemPowerTable_data *StorageTmp = lldpV2Xdot3RemPowerTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3RemPowerTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3RemPowerPortClass, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3RemPowerMDISupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3RemPowerMDIEnabled, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot3RemPowerPairControlable, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3RemPowerPairs, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3RemPowerClass, &tmpsize);
	lldpV2Xdot3RemPowerTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3RemPowerTable(): store configuraiton file for lldpV2Xdot3RemPowerTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3RemPowerTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3RemPowerTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3RemPowerTable: storing data...  "));
	refresh_lldpV2Xdot3RemPowerTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3RemPowerTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3RemPowerTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3RemPowerTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3RemPowerPortClass, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3RemPowerMDISupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3RemPowerMDIEnabled, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot3RemPowerPairControlable, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3RemPowerPairs, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3RemPowerClass, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot3RemMaxFrameSizeTable_data *lldpV2Xdot3RemMaxFrameSizeTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot3RemMaxFrameSizeTable table.
 *
 * Creates a new lldpV2Xdot3RemMaxFrameSizeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot3RemMaxFrameSizeTable_data *
lldpV2Xdot3RemMaxFrameSizeTable_create(void)
{
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3RemMaxFrameSizeTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemMaxFrameSizeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		StorageNew->lldpV2Xdot3RemMaxFrameSize = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot3RemMaxFrameSizeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot3RemMaxFrameSizeTable_data *lldpV2Xdot3RemMaxFrameSizeTable_duplicate(struct lldpV2Xdot3RemMaxFrameSizeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot3RemMaxFrameSizeTable_data *
lldpV2Xdot3RemMaxFrameSizeTable_duplicate(struct lldpV2Xdot3RemMaxFrameSizeTable_data *thedata)
{
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot3RemMaxFrameSizeTable_data);

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemMaxFrameSizeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot3RemMaxFrameSizeTable_id = thedata->lldpV2Xdot3RemMaxFrameSizeTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot3RemMaxFrameSize = thedata->lldpV2Xdot3RemMaxFrameSize;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot3RemMaxFrameSizeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot3RemMaxFrameSizeTable_destroy(struct lldpV2Xdot3RemMaxFrameSizeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot3RemMaxFrameSizeTable_destroy(struct lldpV2Xdot3RemMaxFrameSizeTable_data **thedata)
{
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemMaxFrameSizeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3RemMaxFrameSizeTable_add(struct lldpV2Xdot3RemMaxFrameSizeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot3RemMaxFrameSizeTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot3RemMaxFrameSizeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot3RemMaxFrameSizeTable_add(struct lldpV2Xdot3RemMaxFrameSizeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemMaxFrameSizeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot3RemMaxFrameSizeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot3RemMaxFrameSizeTable_del(struct lldpV2Xdot3RemMaxFrameSizeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot3RemMaxFrameSizeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot3RemMaxFrameSizeTable_del(struct lldpV2Xdot3RemMaxFrameSizeTable_data *thedata)
{
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3RemMaxFrameSizeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot3RemMaxFrameSizeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot3RemMaxFrameSizeTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot3RemMaxFrameSizeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot3RemMaxFrameSizeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot3RemMaxFrameSizeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot3RemMaxFrameSizeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp = lldpV2Xdot3RemMaxFrameSizeTable_create();

	DEBUGMSGTL(("lldpV2Xdot3MIB", "parse_lldpV2Xdot3RemMaxFrameSizeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot3RemMaxFrameSize, &tmpsize);
	lldpV2Xdot3RemMaxFrameSizeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot3RemMaxFrameSizeTable(): store configuraiton file for lldpV2Xdot3RemMaxFrameSizeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot3RemMaxFrameSizeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "store_lldpV2Xdot3RemMaxFrameSizeTable: storing data...  "));
	refresh_lldpV2Xdot3RemMaxFrameSizeTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot3RemMaxFrameSizeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot3RemMaxFrameSizeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot3RemMaxFrameSizeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot3RemMaxFrameSize, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, struct lldpV2Xdot3PortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, struct lldpV2Xdot3PortConfigTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, struct lldpV2Xdot3PortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3PortConfigTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, struct lldpV2Xdot3PortConfigTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3PortConfigTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, struct lldpV2Xdot3PortConfigTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, struct lldpV2Xdot3PortConfigTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3PortConfigTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3PortConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3PortConfigTable_data *
refresh_lldpV2Xdot3PortConfigTable_row(struct lldpV2Xdot3PortConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3PortConfigTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3PortConfigTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3PortConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3PortConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3PortConfigTable(int force)
{
	if (!force && lldpV2Xdot3PortConfigTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3PortConfigTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3PortConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3PortConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3PortConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3PortConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3PortConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3PortConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3PortConfigTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3PortConfigTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3PORTCONFIGTLVSTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot3PortConfigTLVsTxEnable;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnableLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, struct lldpV2Xdot3LocPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, struct lldpV2Xdot3LocPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, struct lldpV2Xdot3LocPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3LocPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, struct lldpV2Xdot3LocPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3LocPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, struct lldpV2Xdot3LocPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, struct lldpV2Xdot3LocPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3LocPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3LocPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3LocPortTable_data *
refresh_lldpV2Xdot3LocPortTable_row(struct lldpV2Xdot3LocPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3LocPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3LocPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3LocPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3LocPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3LocPortTable(int force)
{
	if (!force && lldpV2Xdot3LocPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3LocPortTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3LocPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3LocPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3LocPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3LocPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3LocPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3LocPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3LocPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3LocPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3LOCPORTAUTONEGSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPortAutoNegSupported);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPortAutoNegSupported;
		break;
	case (u_char) LLDPV2XDOT3LOCPORTAUTONEGENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPortAutoNegEnabled);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPortAutoNegEnabled;
		break;
	case (u_char) LLDPV2XDOT3LOCPORTAUTONEGADVERTISEDCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCapLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot3LocPortAutoNegAdvertisedCap;
		break;
	case (u_char) LLDPV2XDOT3LOCPORTOPERMAUTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPortOperMauType);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPortOperMauType;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, struct lldpV2Xdot3LocPowerTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, struct lldpV2Xdot3LocPowerTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, struct lldpV2Xdot3LocPowerTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3LocPowerTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, struct lldpV2Xdot3LocPowerTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3LocPowerTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, struct lldpV2Xdot3LocPowerTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, struct lldpV2Xdot3LocPowerTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3LocPowerTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3LocPowerTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3LocPowerTable_data *
refresh_lldpV2Xdot3LocPowerTable_row(struct lldpV2Xdot3LocPowerTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3LocPowerTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3LocPowerTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3LocPowerTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3LocPowerTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3LocPowerTable(int force)
{
	if (!force && lldpV2Xdot3LocPowerTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3LocPowerTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3LocPowerTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3LocPowerTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3LocPowerTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3LocPowerTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3LocPowerTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3LocPowerTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3LocPowerTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3LocPowerTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3LOCPOWERPORTCLASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPowerPortClass);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPowerPortClass;
		break;
	case (u_char) LLDPV2XDOT3LOCPOWERMDISUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPowerMDISupported);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPowerMDISupported;
		break;
	case (u_char) LLDPV2XDOT3LOCPOWERMDIENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPowerMDIEnabled);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPowerMDIEnabled;
		break;
	case (u_char) LLDPV2XDOT3LOCPOWERPAIRCONTROLABLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPowerPairControlable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPowerPairControlable;
		break;
	case (u_char) LLDPV2XDOT3LOCPOWERPAIRS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPowerPairs);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPowerPairs;
		break;
	case (u_char) LLDPV2XDOT3LOCPOWERCLASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocPowerClass);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocPowerClass;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3LocMaxFrameSizeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3LocMaxFrameSizeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3LocMaxFrameSizeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3LocMaxFrameSizeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3LocMaxFrameSizeTable_data *
refresh_lldpV2Xdot3LocMaxFrameSizeTable_row(struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3LocMaxFrameSizeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3LocMaxFrameSizeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3LocMaxFrameSizeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3LocMaxFrameSizeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3LocMaxFrameSizeTable(int force)
{
	if (!force && lldpV2Xdot3LocMaxFrameSizeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3LocMaxFrameSizeTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3LocMaxFrameSizeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3LocMaxFrameSizeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3LocMaxFrameSizeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3LocMaxFrameSizeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3LocMaxFrameSizeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3LocMaxFrameSizeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3LocMaxFrameSizeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3LocMaxFrameSizeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3LOCMAXFRAMESIZE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3LocMaxFrameSize);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3LocMaxFrameSize;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, struct lldpV2Xdot3RemPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, struct lldpV2Xdot3RemPortTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, struct lldpV2Xdot3RemPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3RemPortTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, struct lldpV2Xdot3RemPortTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3RemPortTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, struct lldpV2Xdot3RemPortTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, struct lldpV2Xdot3RemPortTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3RemPortTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3RemPortTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3RemPortTable_data *
refresh_lldpV2Xdot3RemPortTable_row(struct lldpV2Xdot3RemPortTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3RemPortTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3RemPortTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3RemPortTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3RemPortTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3RemPortTable(int force)
{
	if (!force && lldpV2Xdot3RemPortTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3RemPortTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3RemPortTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3RemPortTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3RemPortTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3RemPortTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3RemPortTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3RemPortTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3RemPortTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3RemPortTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3REMPORTAUTONEGSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPortAutoNegSupported);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPortAutoNegSupported;
		break;
	case (u_char) LLDPV2XDOT3REMPORTAUTONEGENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPortAutoNegEnabled);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPortAutoNegEnabled;
		break;
	case (u_char) LLDPV2XDOT3REMPORTAUTONEGADVERTISEDCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCapLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot3RemPortAutoNegAdvertisedCap;
		break;
	case (u_char) LLDPV2XDOT3REMPORTOPERMAUTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPortOperMauType);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPortOperMauType;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, struct lldpV2Xdot3RemPowerTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, struct lldpV2Xdot3RemPowerTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, struct lldpV2Xdot3RemPowerTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3RemPowerTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, struct lldpV2Xdot3RemPowerTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3RemPowerTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, struct lldpV2Xdot3RemPowerTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, struct lldpV2Xdot3RemPowerTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3RemPowerTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3RemPowerTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3RemPowerTable_data *
refresh_lldpV2Xdot3RemPowerTable_row(struct lldpV2Xdot3RemPowerTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3RemPowerTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3RemPowerTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3RemPowerTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3RemPowerTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3RemPowerTable(int force)
{
	if (!force && lldpV2Xdot3RemPowerTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3RemPowerTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3RemPowerTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3RemPowerTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3RemPowerTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3RemPowerTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3RemPowerTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3RemPowerTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3RemPowerTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3RemPowerTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3REMPOWERPORTCLASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPowerPortClass);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPowerPortClass;
		break;
	case (u_char) LLDPV2XDOT3REMPOWERMDISUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPowerMDISupported);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPowerMDISupported;
		break;
	case (u_char) LLDPV2XDOT3REMPOWERMDIENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPowerMDIEnabled);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPowerMDIEnabled;
		break;
	case (u_char) LLDPV2XDOT3REMPOWERPAIRCONTROLABLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPowerPairControlable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPowerPairControlable;
		break;
	case (u_char) LLDPV2XDOT3REMPOWERPAIRS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPowerPairs);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPowerPairs;
		break;
	case (u_char) LLDPV2XDOT3REMPOWERCLASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemPowerClass);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemPowerClass;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot3RemMaxFrameSizeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot3RemMaxFrameSizeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot3RemMaxFrameSizeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot3RemMaxFrameSizeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot3RemMaxFrameSizeTable_data *
refresh_lldpV2Xdot3RemMaxFrameSizeTable_row(struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot3RemMaxFrameSizeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot3RemMaxFrameSizeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot3RemMaxFrameSizeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot3RemMaxFrameSizeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot3RemMaxFrameSizeTable(int force)
{
	if (!force && lldpV2Xdot3RemMaxFrameSizeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot3RemMaxFrameSizeTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot3RemMaxFrameSizeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot3RemMaxFrameSizeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot3MIB above.
 */
u_char *
var_lldpV2Xdot3RemMaxFrameSizeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot3RemMaxFrameSizeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "var_lldpV2Xdot3RemMaxFrameSizeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot3RemMaxFrameSizeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot3RemMaxFrameSizeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot3RemMaxFrameSizeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT3REMMAXFRAMESIZE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot3RemMaxFrameSize);
		rval = (u_char *) &StorageTmp->lldpV2Xdot3RemMaxFrameSize;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_lldpV2Xdot3PortConfigTLVsTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot3PortConfigTLVsTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot3PortConfigTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot3MIB", "write_lldpV2Xdot3PortConfigTLVsTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot3PortConfigTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot3PortConfigTLVsTxEnable not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot3PortConfigTLVsTxEnable: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot3PortConfigTLVsTxEnable: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot3PortConfigTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old = lldpV2Xdot3PortConfigTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot3PortConfigTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable);
		StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable = string;
		StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnableLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot3PortConfigTable_tsts == 0)
				if ((ret = check_lldpV2Xdot3PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot3PortConfigTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot3PortConfigTable_sets == 0)
				if ((ret = update_lldpV2Xdot3PortConfigTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot3PortConfigTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old) != NULL) {
			lldpV2Xdot3PortConfigTable_destroy(&StorageTmp->lldpV2Xdot3PortConfigTable_old);
			StorageTmp->lldpV2Xdot3PortConfigTable_rsvs = 0;
			StorageTmp->lldpV2Xdot3PortConfigTable_tsts = 0;
			StorageTmp->lldpV2Xdot3PortConfigTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot3PortConfigTable_sets == 0)
			revert_lldpV2Xdot3PortConfigTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot3PortConfigTable_old) == NULL)
			break;
		if (StorageOld->lldpV2Xdot3PortConfigTLVsTxEnable != NULL) {
			SNMP_FREE(StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable);
			StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnable = StorageOld->lldpV2Xdot3PortConfigTLVsTxEnable;
			StorageTmp->lldpV2Xdot3PortConfigTLVsTxEnableLen = StorageOld->lldpV2Xdot3PortConfigTLVsTxEnableLen;
			StorageOld->lldpV2Xdot3PortConfigTLVsTxEnable = NULL;
			StorageOld->lldpV2Xdot3PortConfigTLVsTxEnableLen = 0;
		}
		if (--StorageTmp->lldpV2Xdot3PortConfigTable_rsvs == 0)
			lldpV2Xdot3PortConfigTable_destroy(&StorageTmp->lldpV2Xdot3PortConfigTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void lldpV2Xdot3MIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
lldpV2Xdot3MIB_loop_handler(int sig)
{
	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3MIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (lldpV2Xdot3MIBold_signal_handler != NULL)
		(*lldpV2Xdot3MIBold_signal_handler) (sig);
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
}

/**
 * @fn void lldpV2Xdot3MIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
lldpV2Xdot3MIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("lldpV2Xdot3MIB", "lldpV2Xdot3MIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("lldpV2Xdot3MIB", "done.\n"));
	return;
}
