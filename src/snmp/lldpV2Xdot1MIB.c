/*****************************************************************************

 @(#) src/snmp/lldpv2xdot1mib.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/lldpv2xdot1mib.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "lldpV2Xdot1MIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "lldpV2Xdot1MIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int lldpV2Xdot1MIB_refresh = 1;
volatile int lldpV2Xdot1ConfigPortVlanTable_refresh = 1;
volatile int lldpV2Xdot1ConfigVlanNameTable_refresh = 1;
volatile int lldpV2Xdot1ConfigProtoVlanTable_refresh = 1;
volatile int lldpV2Xdot1ConfigProtocolTable_refresh = 1;
volatile int lldpV2Xdot1ConfigVidUsageDigestTable_refresh = 1;
volatile int lldpV2Xdot1ConfigManVidTable_refresh = 1;
volatile int lldpV2Xdot1LocTable_refresh = 1;
volatile int lldpV2Xdot1LocProtoVlanTable_refresh = 1;
volatile int lldpV2Xdot1LocVlanNameTable_refresh = 1;
volatile int lldpV2Xdot1LocProtocolTable_refresh = 1;
volatile int lldpV2Xdot1LocVidUsageDigestTable_refresh = 1;
volatile int lldpV2Xdot1LocManVidTable_refresh = 1;
volatile int lldpV2Xdot1LocLinkAggTable_refresh = 1;
volatile int lldpV2Xdot1RemTable_refresh = 1;
volatile int lldpV2Xdot1RemProtoVlanTable_refresh = 1;
volatile int lldpV2Xdot1RemVlanNameTable_refresh = 1;
volatile int lldpV2Xdot1RemProtocolTable_refresh = 1;
volatile int lldpV2Xdot1RemVidUsageDigestTable_refresh = 1;
volatile int lldpV2Xdot1RemManVidTable_refresh = 1;
volatile int lldpV2Xdot1RemLinkAggTable_refresh = 1;

/*
 * lldpV2Xdot1MIB_variables_oid: object identifier for lldpV2Xdot1MIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid lldpV2Xdot1MIB_variables_oid[11] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid lldpV2Xdot1TxRxCompliance_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 2, 1, 1 };
oid lldpV2Xdot1TxCompliance_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 2, 1, 2 };
oid lldpV2Xdot1RxCompliance_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 2, 1, 3 };
oid lldpV2Xdot1ConfigGroup_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 2, 2, 1 };
oid lldpV2Xdot1LocSysGroup_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 2, 2, 2 };
oid lldpV2Xdot1RemSysGroup_oid[14] = { 1, 3, 111, 2, 802, 1, 1, 13, 1, 5, 32962, 2, 2, 3 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 lldpV2Xdot1MIB_variables: tree for lldpV2Xdot1MIB
 * This variable defines function callbacks and type return information for the lldpV2Xdot1MIB mib section
 */
struct variable7 lldpV2Xdot1MIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   LLDPV2XDOT1CONFIGPORTVLANTXENABLE  1
	{(u_char) LLDPV2XDOT1CONFIGPORTVLANTXENABLE, ASN_INTEGER, RWRITE, var_lldpV2Xdot1ConfigPortVlanTable, 5, {1, 1, 1, 1, 1}},
#define   LLDPV2XDOT1CONFIGVLANNAMETXENABLE  2
	{(u_char) LLDPV2XDOT1CONFIGVLANNAMETXENABLE, ASN_INTEGER, RWRITE, var_lldpV2Xdot1ConfigVlanNameTable, 5, {1, 1, 2, 1, 1}},
#define   LLDPV2XDOT1CONFIGPROTOVLANTXENABLE  3
	{(u_char) LLDPV2XDOT1CONFIGPROTOVLANTXENABLE, ASN_INTEGER, RWRITE, var_lldpV2Xdot1ConfigProtoVlanTable, 5, {1, 1, 3, 1, 1}},
#define   LLDPV2XDOT1CONFIGPROTOCOLTXENABLE  4
	{(u_char) LLDPV2XDOT1CONFIGPROTOCOLTXENABLE, ASN_INTEGER, RWRITE, var_lldpV2Xdot1ConfigProtocolTable, 5, {1, 1, 4, 1, 1}},
#define   LLDPV2XDOT1CONFIGVIDUSAGEDIGESTTXENABLE  5
	{(u_char) LLDPV2XDOT1CONFIGVIDUSAGEDIGESTTXENABLE, ASN_INTEGER, RWRITE, var_lldpV2Xdot1ConfigVidUsageDigestTable, 5, {1, 1, 5, 1, 1}},
#define   LLDPV2XDOT1CONFIGMANVIDTXENABLE  6
	{(u_char) LLDPV2XDOT1CONFIGMANVIDTXENABLE, ASN_INTEGER, RWRITE, var_lldpV2Xdot1ConfigManVidTable, 5, {1, 1, 6, 1, 1}},
#define   LLDPV2XDOT1LOCPORTVLANID  7
	{(u_char) LLDPV2XDOT1LOCPORTVLANID, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1LocTable, 5, {1, 2, 1, 1, 1}},
#define   LLDPV2XDOT1LOCPROTOVLANSUPPORTED  8
	{(u_char) LLDPV2XDOT1LOCPROTOVLANSUPPORTED, ASN_INTEGER, RONLY, var_lldpV2Xdot1LocProtoVlanTable, 5, {1, 2, 2, 1, 2}},
#define   LLDPV2XDOT1LOCPROTOVLANENABLED  9
	{(u_char) LLDPV2XDOT1LOCPROTOVLANENABLED, ASN_INTEGER, RONLY, var_lldpV2Xdot1LocProtoVlanTable, 5, {1, 2, 2, 1, 3}},
#define   LLDPV2XDOT1LOCVLANNAME  10
	{(u_char) LLDPV2XDOT1LOCVLANNAME, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1LocVlanNameTable, 5, {1, 2, 3, 1, 2}},
#define   LLDPV2XDOT1LOCPROTOCOLID  11
	{(u_char) LLDPV2XDOT1LOCPROTOCOLID, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1LocProtocolTable, 5, {1, 2, 4, 1, 2}},
#define   LLDPV2XDOT1LOCVIDUSAGEDIGEST  12
	{(u_char) LLDPV2XDOT1LOCVIDUSAGEDIGEST, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1LocVidUsageDigestTable, 5, {1, 2, 5, 1, 1}},
#define   LLDPV2XDOT1LOCMANVID  13
	{(u_char) LLDPV2XDOT1LOCMANVID, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1LocManVidTable, 5, {1, 2, 6, 1, 1}},
#define   LLDPV2XDOT1LOCLINKAGGSTATUS  14
	{(u_char) LLDPV2XDOT1LOCLINKAGGSTATUS, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1LocLinkAggTable, 5, {1, 2, 7, 1, 1}},
#define   LLDPV2XDOT1LOCLINKAGGPORTID  15
	{(u_char) LLDPV2XDOT1LOCLINKAGGPORTID, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1LocLinkAggTable, 5, {1, 2, 7, 1, 2}},
#define   LLDPV2XDOT1REMPORTVLANID  16
	{(u_char) LLDPV2XDOT1REMPORTVLANID, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1RemTable, 5, {1, 3, 1, 1, 1}},
#define   LLDPV2XDOT1REMPROTOVLANSUPPORTED  17
	{(u_char) LLDPV2XDOT1REMPROTOVLANSUPPORTED, ASN_INTEGER, RONLY, var_lldpV2Xdot1RemProtoVlanTable, 5, {1, 3, 2, 1, 2}},
#define   LLDPV2XDOT1REMPROTOVLANENABLED  18
	{(u_char) LLDPV2XDOT1REMPROTOVLANENABLED, ASN_INTEGER, RONLY, var_lldpV2Xdot1RemProtoVlanTable, 5, {1, 3, 2, 1, 3}},
#define   LLDPV2XDOT1REMVLANNAME  19
	{(u_char) LLDPV2XDOT1REMVLANNAME, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1RemVlanNameTable, 5, {1, 3, 3, 1, 2}},
#define   LLDPV2XDOT1REMPROTOCOLID  20
	{(u_char) LLDPV2XDOT1REMPROTOCOLID, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1RemProtocolTable, 5, {1, 3, 4, 1, 2}},
#define   LLDPV2XDOT1REMVIDUSAGEDIGEST  21
	{(u_char) LLDPV2XDOT1REMVIDUSAGEDIGEST, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1RemVidUsageDigestTable, 5, {1, 3, 5, 1, 1}},
#define   LLDPV2XDOT1REMMANVID  22
	{(u_char) LLDPV2XDOT1REMMANVID, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1RemManVidTable, 5, {1, 3, 6, 1, 1}},
#define   LLDPV2XDOT1REMLINKAGGSTATUS  23
	{(u_char) LLDPV2XDOT1REMLINKAGGSTATUS, ASN_OCTET_STR, RONLY, var_lldpV2Xdot1RemLinkAggTable, 5, {1, 3, 7, 1, 1}},
#define   LLDPV2XDOT1REMLINKAGGPORTID  24
	{(u_char) LLDPV2XDOT1REMLINKAGGPORTID, ASN_UNSIGNED, RONLY, var_lldpV2Xdot1RemLinkAggTable, 5, {1, 3, 7, 1, 2}},
};

/* (L = length of the oidsuffix) */
struct lldpV2Xdot1MIB_data *lldpV2Xdot1MIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *lldpV2Xdot1ConfigPortVlanTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1ConfigVlanNameTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1ConfigProtoVlanTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1ConfigProtocolTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1ConfigVidUsageDigestTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1ConfigManVidTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocProtoVlanTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocVlanNameTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocProtocolTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocVidUsageDigestTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocManVidTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1LocLinkAggTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemProtoVlanTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemVlanNameTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemProtocolTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemVidUsageDigestTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemManVidTableStorage = NULL;
struct header_complex_index *lldpV2Xdot1RemLinkAggTableStorage = NULL;

void (*lldpV2Xdot1MIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void lldpV2Xdot1MIB_loop_handler(int);
void lldpV2Xdot1MIB_fd_handler(int, void *);

/**
 * @fn void init_lldpV2Xdot1MIB(void)
 * @brief lldpV2Xdot1MIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (lldpV2Xdot1MIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_lldpV2Xdot1MIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "init_lldpV2Xdot1MIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("lldpV2Xdot1MIB", lldpV2Xdot1MIB_variables, variable7, lldpV2Xdot1MIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_lldpV2Xdot1MIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("lldpV2Xdot1MIB", parse_lldpV2Xdot1MIB, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1ConfigPortVlanTable", parse_lldpV2Xdot1ConfigPortVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1ConfigVlanNameTable", parse_lldpV2Xdot1ConfigVlanNameTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1ConfigProtoVlanTable", parse_lldpV2Xdot1ConfigProtoVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1ConfigProtocolTable", parse_lldpV2Xdot1ConfigProtocolTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1ConfigVidUsageDigestTable", parse_lldpV2Xdot1ConfigVidUsageDigestTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1ConfigManVidTable", parse_lldpV2Xdot1ConfigManVidTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocTable", parse_lldpV2Xdot1LocTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocProtoVlanTable", parse_lldpV2Xdot1LocProtoVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocVlanNameTable", parse_lldpV2Xdot1LocVlanNameTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocProtocolTable", parse_lldpV2Xdot1LocProtocolTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocVidUsageDigestTable", parse_lldpV2Xdot1LocVidUsageDigestTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocManVidTable", parse_lldpV2Xdot1LocManVidTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1LocLinkAggTable", parse_lldpV2Xdot1LocLinkAggTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemTable", parse_lldpV2Xdot1RemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemProtoVlanTable", parse_lldpV2Xdot1RemProtoVlanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemVlanNameTable", parse_lldpV2Xdot1RemVlanNameTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemProtocolTable", parse_lldpV2Xdot1RemProtocolTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemVidUsageDigestTable", parse_lldpV2Xdot1RemVidUsageDigestTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemManVidTable", parse_lldpV2Xdot1RemManVidTable, NULL, "HELP STRING");
	snmpd_register_config_handler("lldpV2Xdot1RemLinkAggTable", parse_lldpV2Xdot1RemLinkAggTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1MIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1ConfigPortVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1ConfigVlanNameTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1ConfigProtoVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1ConfigProtocolTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1ConfigVidUsageDigestTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1ConfigManVidTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocProtoVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocVlanNameTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocProtocolTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocVidUsageDigestTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocManVidTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1LocLinkAggTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemProtoVlanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemVlanNameTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemProtocolTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemVidUsageDigestTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemManVidTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_lldpV2Xdot1RemLinkAggTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, lldpV2Xdot1MIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, lldpV2Xdot1MIB_fd_handler, (void *) 1);
	}
	lldpV2Xdot1MIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &lldpV2Xdot1MIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/**
 * @fn void deinit_lldpV2Xdot1MIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (lldpV2Xdot1MIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_lldpV2Xdot1MIB(void)
{
	DEBUGMSGTL(("lldpV2Xdot1MIB", "deinit_lldpV2Xdot1MIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = lldpV2Xdot1MIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(lldpV2Xdot1MIB_variables_oid, sizeof(lldpV2Xdot1MIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("lldpV2Xdot1MIB");
	snmpd_unregister_config_handler("lldpV2Xdot1ConfigPortVlanTable");
	snmpd_unregister_config_handler("lldpV2Xdot1ConfigVlanNameTable");
	snmpd_unregister_config_handler("lldpV2Xdot1ConfigProtoVlanTable");
	snmpd_unregister_config_handler("lldpV2Xdot1ConfigProtocolTable");
	snmpd_unregister_config_handler("lldpV2Xdot1ConfigVidUsageDigestTable");
	snmpd_unregister_config_handler("lldpV2Xdot1ConfigManVidTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocProtoVlanTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocVlanNameTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocProtocolTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocVidUsageDigestTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocManVidTable");
	snmpd_unregister_config_handler("lldpV2Xdot1LocLinkAggTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemProtoVlanTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemVlanNameTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemProtocolTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemVidUsageDigestTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemManVidTable");
	snmpd_unregister_config_handler("lldpV2Xdot1RemLinkAggTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

int
term_lldpV2Xdot1MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("lldpV2Xdot1MIB", "term_lldpV2Xdot1MIB: terminating...  "));
	deinit_lldpV2Xdot1MIB();
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return 0;
}

/**
 * @fn struct lldpV2Xdot1MIB_data *lldpV2Xdot1MIB_create(void)
 * @brief create a fresh data structure representing scalars in lldpV2Xdot1MIB.
 *
 * Creates a new lldpV2Xdot1MIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in lldpV2Xdot1MIB.
 */
struct lldpV2Xdot1MIB_data *
lldpV2Xdot1MIB_create(void)
{
	struct lldpV2Xdot1MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1MIB_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1MIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1MIB_data *lldpV2Xdot1MIB_duplicate(struct lldpV2Xdot1MIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct lldpV2Xdot1MIB_data *
lldpV2Xdot1MIB_duplicate(struct lldpV2Xdot1MIB_data *thedata)
{
	struct lldpV2Xdot1MIB_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1MIB_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1MIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1MIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1MIB_destroy(struct lldpV2Xdot1MIB_data **thedata)
 * @param thedata pointer to the data structure in lldpV2Xdot1MIB.
 * @brief delete a scalars structure from lldpV2Xdot1MIB.
 *
 * Frees scalars that were previously removed from lldpV2Xdot1MIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1MIB_destroy(struct lldpV2Xdot1MIB_data **thedata)
{
	struct lldpV2Xdot1MIB_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1MIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1MIB_add(struct lldpV2Xdot1MIB_data *thedata)
 * @param thedata the structure representing lldpV2Xdot1MIB scalars.
 * @brief adds node to the lldpV2Xdot1MIB scalar data set.
 *
 * Adds a scalar structure to the lldpV2Xdot1MIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
lldpV2Xdot1MIB_add(struct lldpV2Xdot1MIB_data *thedata)
{
	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1MIB_add: adding data...  "));
	if (thedata)
		lldpV2Xdot1MIBStorage = thedata;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1MIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1MIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1MIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_lldpV2Xdot1MIB(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1MIB_data *StorageTmp = lldpV2Xdot1MIB_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1MIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	lldpV2Xdot1MIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1MIB(): stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1MIB_data *StorageTmp;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1MIB: storing data...  "));
	refresh_lldpV2Xdot1MIB(1);
	if ((StorageTmp = lldpV2Xdot1MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpV2Xdot1MIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "lldpV2Xdot1MIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpV2Xdot1MIB(struct lldpV2Xdot1MIB_data *StorageTmp, struct lldpV2Xdot1MIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_lldpV2Xdot1MIB(struct lldpV2Xdot1MIB_data *StorageTmp, struct lldpV2Xdot1MIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1MIB(struct lldpV2Xdot1MIB_data *StorageTmp, struct lldpV2Xdot1MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_lldpV2Xdot1MIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1MIB(struct lldpV2Xdot1MIB_data *StorageTmp, struct lldpV2Xdot1MIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1MIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_lldpV2Xdot1MIB(struct 
 * @fn void revert_lldpV2Xdot1MIB(struct lldpV2Xdot1MIB_data *StorageTmp, struct lldpV2Xdot1MIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_lldpV2Xdot1MIB(struct lldpV2Xdot1MIB_data *StorageTmp, struct lldpV2Xdot1MIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1MIB(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1MIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of lldpV2Xdot1MIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_lldpV2Xdot1MIB(int force)
{
	if (lldpV2Xdot1MIBStorage == NULL) {
		struct lldpV2Xdot1MIB_data *StorageNew;

		if ((StorageNew = lldpV2Xdot1MIB_create()) == NULL)
			return;
		lldpV2Xdot1MIBStorage = StorageNew;
		lldpV2Xdot1MIB_refresh = 1;
	}
	if (!force && lldpV2Xdot1MIB_refresh == 0)
		return;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "refresh_lldpV2Xdot1MIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	lldpV2Xdot1MIB_refresh = 0;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/**
 * @fn u_char * var_lldpV2Xdot1MIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in lldpV2Xdot1MIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_lldpV2Xdot1MIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1MIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1MIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_lldpV2Xdot1MIB(0);
	if ((StorageTmp = lldpV2Xdot1MIBStorage) == NULL) {
		DEBUGMSGTL(("lldpV2Xdot1MIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("lldpV2Xdot1MIB", "found.\n"));
	else
		DEBUGMSGTL(("lldpV2Xdot1MIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct lldpV2Xdot1ConfigPortVlanTable_data *lldpV2Xdot1ConfigPortVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1ConfigPortVlanTable table.
 *
 * Creates a new lldpV2Xdot1ConfigPortVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1ConfigPortVlanTable_data *
lldpV2Xdot1ConfigPortVlanTable_create(void)
{
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigPortVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigPortVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2PortConfigIfIndex = 0;
		StorageNew->lldpV2PortConfigDestAddressIndex = 0;
		StorageNew->lldpV2Xdot1ConfigPortVlanTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1ConfigPortVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1ConfigPortVlanTable_data *lldpV2Xdot1ConfigPortVlanTable_duplicate(struct lldpV2Xdot1ConfigPortVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1ConfigPortVlanTable_data *
lldpV2Xdot1ConfigPortVlanTable_duplicate(struct lldpV2Xdot1ConfigPortVlanTable_data *thedata)
{
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigPortVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigPortVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1ConfigPortVlanTable_id = thedata->lldpV2Xdot1ConfigPortVlanTable_id;
		StorageNew->lldpV2PortConfigIfIndex = thedata->lldpV2PortConfigIfIndex;
		StorageNew->lldpV2PortConfigDestAddressIndex = thedata->lldpV2PortConfigDestAddressIndex;
		StorageNew->lldpV2Xdot1ConfigPortVlanTxEnable = thedata->lldpV2Xdot1ConfigPortVlanTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1ConfigPortVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1ConfigPortVlanTable_destroy(struct lldpV2Xdot1ConfigPortVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1ConfigPortVlanTable_destroy(struct lldpV2Xdot1ConfigPortVlanTable_data **thedata)
{
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigPortVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigPortVlanTable_add(struct lldpV2Xdot1ConfigPortVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1ConfigPortVlanTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1ConfigPortVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1ConfigPortVlanTable_add(struct lldpV2Xdot1ConfigPortVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigPortVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2PortConfigIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2PortConfigIfIndex, sizeof(thedata->lldpV2PortConfigIfIndex));
		/* lldpV2PortConfigDestAddressIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2PortConfigDestAddressIndex, sizeof(thedata->lldpV2PortConfigDestAddressIndex));
		header_complex_add_data(&lldpV2Xdot1ConfigPortVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigPortVlanTable_del(struct lldpV2Xdot1ConfigPortVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1ConfigPortVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1ConfigPortVlanTable_del(struct lldpV2Xdot1ConfigPortVlanTable_data *thedata)
{
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigPortVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1ConfigPortVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1ConfigPortVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1ConfigPortVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1ConfigPortVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1ConfigPortVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1ConfigPortVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp = lldpV2Xdot1ConfigPortVlanTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1ConfigPortVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable, &tmpsize);
	lldpV2Xdot1ConfigPortVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1ConfigPortVlanTable(): store configuraiton file for lldpV2Xdot1ConfigPortVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1ConfigPortVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1ConfigPortVlanTable: storing data...  "));
	refresh_lldpV2Xdot1ConfigPortVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1ConfigPortVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1ConfigPortVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1ConfigPortVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2PortConfigIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2PortConfigDestAddressIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1ConfigVlanNameTable_data *lldpV2Xdot1ConfigVlanNameTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1ConfigVlanNameTable table.
 *
 * Creates a new lldpV2Xdot1ConfigVlanNameTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1ConfigVlanNameTable_data *
lldpV2Xdot1ConfigVlanNameTable_create(void)
{
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigVlanNameTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVlanNameTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocVlanId = 0;
		StorageNew->lldpV2Xdot1ConfigVlanNameTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1ConfigVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1ConfigVlanNameTable_data *lldpV2Xdot1ConfigVlanNameTable_duplicate(struct lldpV2Xdot1ConfigVlanNameTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1ConfigVlanNameTable_data *
lldpV2Xdot1ConfigVlanNameTable_duplicate(struct lldpV2Xdot1ConfigVlanNameTable_data *thedata)
{
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigVlanNameTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVlanNameTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1ConfigVlanNameTable_id = thedata->lldpV2Xdot1ConfigVlanNameTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocVlanId = thedata->lldpV2Xdot1LocVlanId;
		StorageNew->lldpV2Xdot1ConfigVlanNameTxEnable = thedata->lldpV2Xdot1ConfigVlanNameTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1ConfigVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1ConfigVlanNameTable_destroy(struct lldpV2Xdot1ConfigVlanNameTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1ConfigVlanNameTable_destroy(struct lldpV2Xdot1ConfigVlanNameTable_data **thedata)
{
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVlanNameTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigVlanNameTable_add(struct lldpV2Xdot1ConfigVlanNameTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1ConfigVlanNameTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1ConfigVlanNameTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1ConfigVlanNameTable_add(struct lldpV2Xdot1ConfigVlanNameTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVlanNameTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		/* lldpV2Xdot1LocVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2Xdot1LocVlanId, sizeof(thedata->lldpV2Xdot1LocVlanId));
		header_complex_add_data(&lldpV2Xdot1ConfigVlanNameTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigVlanNameTable_del(struct lldpV2Xdot1ConfigVlanNameTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1ConfigVlanNameTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1ConfigVlanNameTable_del(struct lldpV2Xdot1ConfigVlanNameTable_data *thedata)
{
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVlanNameTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1ConfigVlanNameTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1ConfigVlanNameTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1ConfigVlanNameTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1ConfigVlanNameTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1ConfigVlanNameTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1ConfigVlanNameTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp = lldpV2Xdot1ConfigVlanNameTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1ConfigVlanNameTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1LocVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable, &tmpsize);
	lldpV2Xdot1ConfigVlanNameTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1ConfigVlanNameTable(): store configuraiton file for lldpV2Xdot1ConfigVlanNameTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1ConfigVlanNameTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1ConfigVlanNameTable: storing data...  "));
	refresh_lldpV2Xdot1ConfigVlanNameTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1ConfigVlanNameTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1ConfigVlanNameTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1ConfigVlanNameTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1LocVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1ConfigProtoVlanTable_data *lldpV2Xdot1ConfigProtoVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1ConfigProtoVlanTable table.
 *
 * Creates a new lldpV2Xdot1ConfigProtoVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1ConfigProtoVlanTable_data *
lldpV2Xdot1ConfigProtoVlanTable_create(void)
{
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigProtoVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtoVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocProtoVlanId = 0;
		StorageNew->lldpV2Xdot1ConfigProtoVlanTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1ConfigProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1ConfigProtoVlanTable_data *lldpV2Xdot1ConfigProtoVlanTable_duplicate(struct lldpV2Xdot1ConfigProtoVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1ConfigProtoVlanTable_data *
lldpV2Xdot1ConfigProtoVlanTable_duplicate(struct lldpV2Xdot1ConfigProtoVlanTable_data *thedata)
{
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigProtoVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtoVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1ConfigProtoVlanTable_id = thedata->lldpV2Xdot1ConfigProtoVlanTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocProtoVlanId = thedata->lldpV2Xdot1LocProtoVlanId;
		StorageNew->lldpV2Xdot1ConfigProtoVlanTxEnable = thedata->lldpV2Xdot1ConfigProtoVlanTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1ConfigProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1ConfigProtoVlanTable_destroy(struct lldpV2Xdot1ConfigProtoVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1ConfigProtoVlanTable_destroy(struct lldpV2Xdot1ConfigProtoVlanTable_data **thedata)
{
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtoVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigProtoVlanTable_add(struct lldpV2Xdot1ConfigProtoVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1ConfigProtoVlanTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1ConfigProtoVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1ConfigProtoVlanTable_add(struct lldpV2Xdot1ConfigProtoVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtoVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		/* lldpV2Xdot1LocProtoVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2Xdot1LocProtoVlanId, sizeof(thedata->lldpV2Xdot1LocProtoVlanId));
		header_complex_add_data(&lldpV2Xdot1ConfigProtoVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigProtoVlanTable_del(struct lldpV2Xdot1ConfigProtoVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1ConfigProtoVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1ConfigProtoVlanTable_del(struct lldpV2Xdot1ConfigProtoVlanTable_data *thedata)
{
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtoVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1ConfigProtoVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1ConfigProtoVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1ConfigProtoVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1ConfigProtoVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1ConfigProtoVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1ConfigProtoVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp = lldpV2Xdot1ConfigProtoVlanTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1ConfigProtoVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocProtoVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable, &tmpsize);
	lldpV2Xdot1ConfigProtoVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1ConfigProtoVlanTable(): store configuraiton file for lldpV2Xdot1ConfigProtoVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1ConfigProtoVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1ConfigProtoVlanTable: storing data...  "));
	refresh_lldpV2Xdot1ConfigProtoVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1ConfigProtoVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1ConfigProtoVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1ConfigProtoVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocProtoVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1ConfigProtocolTable_data *lldpV2Xdot1ConfigProtocolTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1ConfigProtocolTable table.
 *
 * Creates a new lldpV2Xdot1ConfigProtocolTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1ConfigProtocolTable_data *
lldpV2Xdot1ConfigProtocolTable_create(void)
{
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigProtocolTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtocolTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocProtocolIndex = 0;
		StorageNew->lldpV2Xdot1ConfigProtocolTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1ConfigProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1ConfigProtocolTable_data *lldpV2Xdot1ConfigProtocolTable_duplicate(struct lldpV2Xdot1ConfigProtocolTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1ConfigProtocolTable_data *
lldpV2Xdot1ConfigProtocolTable_duplicate(struct lldpV2Xdot1ConfigProtocolTable_data *thedata)
{
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigProtocolTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtocolTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1ConfigProtocolTable_id = thedata->lldpV2Xdot1ConfigProtocolTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocProtocolIndex = thedata->lldpV2Xdot1LocProtocolIndex;
		StorageNew->lldpV2Xdot1ConfigProtocolTxEnable = thedata->lldpV2Xdot1ConfigProtocolTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1ConfigProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1ConfigProtocolTable_destroy(struct lldpV2Xdot1ConfigProtocolTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1ConfigProtocolTable_destroy(struct lldpV2Xdot1ConfigProtocolTable_data **thedata)
{
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtocolTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigProtocolTable_add(struct lldpV2Xdot1ConfigProtocolTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1ConfigProtocolTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1ConfigProtocolTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1ConfigProtocolTable_add(struct lldpV2Xdot1ConfigProtocolTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtocolTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		/* lldpV2Xdot1LocProtocolIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2Xdot1LocProtocolIndex, sizeof(thedata->lldpV2Xdot1LocProtocolIndex));
		header_complex_add_data(&lldpV2Xdot1ConfigProtocolTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigProtocolTable_del(struct lldpV2Xdot1ConfigProtocolTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1ConfigProtocolTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1ConfigProtocolTable_del(struct lldpV2Xdot1ConfigProtocolTable_data *thedata)
{
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigProtocolTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1ConfigProtocolTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1ConfigProtocolTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1ConfigProtocolTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1ConfigProtocolTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1ConfigProtocolTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1ConfigProtocolTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp = lldpV2Xdot1ConfigProtocolTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1ConfigProtocolTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocProtocolIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable, &tmpsize);
	lldpV2Xdot1ConfigProtocolTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1ConfigProtocolTable(): store configuraiton file for lldpV2Xdot1ConfigProtocolTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1ConfigProtocolTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1ConfigProtocolTable: storing data...  "));
	refresh_lldpV2Xdot1ConfigProtocolTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1ConfigProtocolTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1ConfigProtocolTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1ConfigProtocolTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocProtocolIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1ConfigVidUsageDigestTable_data *lldpV2Xdot1ConfigVidUsageDigestTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1ConfigVidUsageDigestTable table.
 *
 * Creates a new lldpV2Xdot1ConfigVidUsageDigestTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1ConfigVidUsageDigestTable_data *
lldpV2Xdot1ConfigVidUsageDigestTable_create(void)
{
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigVidUsageDigestTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVidUsageDigestTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1ConfigVidUsageDigestTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1ConfigVidUsageDigestTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1ConfigVidUsageDigestTable_data *lldpV2Xdot1ConfigVidUsageDigestTable_duplicate(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1ConfigVidUsageDigestTable_data *
lldpV2Xdot1ConfigVidUsageDigestTable_duplicate(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *thedata)
{
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigVidUsageDigestTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVidUsageDigestTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1ConfigVidUsageDigestTable_id = thedata->lldpV2Xdot1ConfigVidUsageDigestTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1ConfigVidUsageDigestTxEnable = thedata->lldpV2Xdot1ConfigVidUsageDigestTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1ConfigVidUsageDigestTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1ConfigVidUsageDigestTable_destroy(struct lldpV2Xdot1ConfigVidUsageDigestTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1ConfigVidUsageDigestTable_destroy(struct lldpV2Xdot1ConfigVidUsageDigestTable_data **thedata)
{
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVidUsageDigestTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigVidUsageDigestTable_add(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1ConfigVidUsageDigestTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1ConfigVidUsageDigestTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1ConfigVidUsageDigestTable_add(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVidUsageDigestTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1ConfigVidUsageDigestTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigVidUsageDigestTable_del(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1ConfigVidUsageDigestTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1ConfigVidUsageDigestTable_del(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *thedata)
{
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigVidUsageDigestTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1ConfigVidUsageDigestTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1ConfigVidUsageDigestTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1ConfigVidUsageDigestTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1ConfigVidUsageDigestTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1ConfigVidUsageDigestTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1ConfigVidUsageDigestTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp = lldpV2Xdot1ConfigVidUsageDigestTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1ConfigVidUsageDigestTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable, &tmpsize);
	lldpV2Xdot1ConfigVidUsageDigestTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1ConfigVidUsageDigestTable(): store configuraiton file for lldpV2Xdot1ConfigVidUsageDigestTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1ConfigVidUsageDigestTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1ConfigVidUsageDigestTable: storing data...  "));
	refresh_lldpV2Xdot1ConfigVidUsageDigestTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1ConfigVidUsageDigestTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1ConfigVidUsageDigestTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1ConfigVidUsageDigestTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1ConfigManVidTable_data *lldpV2Xdot1ConfigManVidTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1ConfigManVidTable table.
 *
 * Creates a new lldpV2Xdot1ConfigManVidTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1ConfigManVidTable_data *
lldpV2Xdot1ConfigManVidTable_create(void)
{
	struct lldpV2Xdot1ConfigManVidTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigManVidTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigManVidTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1ConfigManVidTxEnable = TV_FALSE;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1ConfigManVidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1ConfigManVidTable_data *lldpV2Xdot1ConfigManVidTable_duplicate(struct lldpV2Xdot1ConfigManVidTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1ConfigManVidTable_data *
lldpV2Xdot1ConfigManVidTable_duplicate(struct lldpV2Xdot1ConfigManVidTable_data *thedata)
{
	struct lldpV2Xdot1ConfigManVidTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1ConfigManVidTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigManVidTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1ConfigManVidTable_id = thedata->lldpV2Xdot1ConfigManVidTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1ConfigManVidTxEnable = thedata->lldpV2Xdot1ConfigManVidTxEnable;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1ConfigManVidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1ConfigManVidTable_destroy(struct lldpV2Xdot1ConfigManVidTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1ConfigManVidTable_destroy(struct lldpV2Xdot1ConfigManVidTable_data **thedata)
{
	struct lldpV2Xdot1ConfigManVidTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigManVidTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigManVidTable_add(struct lldpV2Xdot1ConfigManVidTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1ConfigManVidTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1ConfigManVidTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1ConfigManVidTable_add(struct lldpV2Xdot1ConfigManVidTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigManVidTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1ConfigManVidTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1ConfigManVidTable_del(struct lldpV2Xdot1ConfigManVidTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1ConfigManVidTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1ConfigManVidTable_del(struct lldpV2Xdot1ConfigManVidTable_data *thedata)
{
	struct lldpV2Xdot1ConfigManVidTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1ConfigManVidTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1ConfigManVidTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1ConfigManVidTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1ConfigManVidTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1ConfigManVidTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1ConfigManVidTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1ConfigManVidTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp = lldpV2Xdot1ConfigManVidTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1ConfigManVidTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1ConfigManVidTxEnable, &tmpsize);
	lldpV2Xdot1ConfigManVidTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1ConfigManVidTable(): store configuraiton file for lldpV2Xdot1ConfigManVidTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1ConfigManVidTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1ConfigManVidTable: storing data...  "));
	refresh_lldpV2Xdot1ConfigManVidTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1ConfigManVidTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1ConfigManVidTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1ConfigManVidTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1ConfigManVidTxEnable, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocTable_data *lldpV2Xdot1LocTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocTable table.
 *
 * Creates a new lldpV2Xdot1LocTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocTable_data *
lldpV2Xdot1LocTable_create(void)
{
	struct lldpV2Xdot1LocTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocPortVlanId = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocTable_data *lldpV2Xdot1LocTable_duplicate(struct lldpV2Xdot1LocTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocTable_data *
lldpV2Xdot1LocTable_duplicate(struct lldpV2Xdot1LocTable_data *thedata)
{
	struct lldpV2Xdot1LocTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocTable_id = thedata->lldpV2Xdot1LocTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocPortVlanId = thedata->lldpV2Xdot1LocPortVlanId;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocTable_destroy(struct lldpV2Xdot1LocTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocTable_destroy(struct lldpV2Xdot1LocTable_data **thedata)
{
	struct lldpV2Xdot1LocTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocTable_add(struct lldpV2Xdot1LocTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocTable_add(struct lldpV2Xdot1LocTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1LocTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocTable_del(struct lldpV2Xdot1LocTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocTable_del(struct lldpV2Xdot1LocTable_data *thedata)
{
	struct lldpV2Xdot1LocTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocTable_data *StorageTmp = lldpV2Xdot1LocTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocPortVlanId, &tmpsize);
	lldpV2Xdot1LocTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocTable(): store configuraiton file for lldpV2Xdot1LocTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocTable: storing data...  "));
	refresh_lldpV2Xdot1LocTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocPortVlanId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocProtoVlanTable_data *lldpV2Xdot1LocProtoVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocProtoVlanTable table.
 *
 * Creates a new lldpV2Xdot1LocProtoVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocProtoVlanTable_data *
lldpV2Xdot1LocProtoVlanTable_create(void)
{
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocProtoVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtoVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocProtoVlanSupported = 0;
		StorageNew->lldpV2Xdot1LocProtoVlanEnabled = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocProtoVlanTable_data *lldpV2Xdot1LocProtoVlanTable_duplicate(struct lldpV2Xdot1LocProtoVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocProtoVlanTable_data *
lldpV2Xdot1LocProtoVlanTable_duplicate(struct lldpV2Xdot1LocProtoVlanTable_data *thedata)
{
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocProtoVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtoVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocProtoVlanTable_id = thedata->lldpV2Xdot1LocProtoVlanTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocProtoVlanId = thedata->lldpV2Xdot1LocProtoVlanId;
		StorageNew->lldpV2Xdot1LocProtoVlanSupported = thedata->lldpV2Xdot1LocProtoVlanSupported;
		StorageNew->lldpV2Xdot1LocProtoVlanEnabled = thedata->lldpV2Xdot1LocProtoVlanEnabled;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocProtoVlanTable_destroy(struct lldpV2Xdot1LocProtoVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocProtoVlanTable_destroy(struct lldpV2Xdot1LocProtoVlanTable_data **thedata)
{
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtoVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocProtoVlanTable_add(struct lldpV2Xdot1LocProtoVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocProtoVlanTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocProtoVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocProtoVlanTable_add(struct lldpV2Xdot1LocProtoVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtoVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		/* lldpV2Xdot1LocProtoVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2Xdot1LocProtoVlanId, sizeof(thedata->lldpV2Xdot1LocProtoVlanId));
		header_complex_add_data(&lldpV2Xdot1LocProtoVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocProtoVlanTable_del(struct lldpV2Xdot1LocProtoVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocProtoVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocProtoVlanTable_del(struct lldpV2Xdot1LocProtoVlanTable_data *thedata)
{
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtoVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocProtoVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocProtoVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocProtoVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocProtoVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocProtoVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocProtoVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp = lldpV2Xdot1LocProtoVlanTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocProtoVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocProtoVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1LocProtoVlanSupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1LocProtoVlanEnabled, &tmpsize);
	lldpV2Xdot1LocProtoVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocProtoVlanTable(): store configuraiton file for lldpV2Xdot1LocProtoVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocProtoVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocProtoVlanTable: storing data...  "));
	refresh_lldpV2Xdot1LocProtoVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocProtoVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocProtoVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocProtoVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocProtoVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1LocProtoVlanSupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1LocProtoVlanEnabled, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocVlanNameTable_data *lldpV2Xdot1LocVlanNameTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocVlanNameTable table.
 *
 * Creates a new lldpV2Xdot1LocVlanNameTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocVlanNameTable_data *
lldpV2Xdot1LocVlanNameTable_create(void)
{
	struct lldpV2Xdot1LocVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocVlanNameTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVlanNameTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		if ((StorageNew->lldpV2Xdot1LocVlanName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1LocVlanNameLen = 0;
		StorageNew->lldpV2Xdot1LocVlanName[StorageNew->lldpV2Xdot1LocVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocVlanNameTable_data *lldpV2Xdot1LocVlanNameTable_duplicate(struct lldpV2Xdot1LocVlanNameTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocVlanNameTable_data *
lldpV2Xdot1LocVlanNameTable_duplicate(struct lldpV2Xdot1LocVlanNameTable_data *thedata)
{
	struct lldpV2Xdot1LocVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocVlanNameTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVlanNameTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocVlanNameTable_id = thedata->lldpV2Xdot1LocVlanNameTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocVlanId = thedata->lldpV2Xdot1LocVlanId;
		if (!(StorageNew->lldpV2Xdot1LocVlanName = malloc(thedata->lldpV2Xdot1LocVlanNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1LocVlanName, thedata->lldpV2Xdot1LocVlanName, thedata->lldpV2Xdot1LocVlanNameLen);
		StorageNew->lldpV2Xdot1LocVlanNameLen = thedata->lldpV2Xdot1LocVlanNameLen;
		StorageNew->lldpV2Xdot1LocVlanName[StorageNew->lldpV2Xdot1LocVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocVlanNameTable_destroy(struct lldpV2Xdot1LocVlanNameTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocVlanNameTable_destroy(struct lldpV2Xdot1LocVlanNameTable_data **thedata)
{
	struct lldpV2Xdot1LocVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVlanNameTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1LocVlanName);
		StorageDel->lldpV2Xdot1LocVlanNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocVlanNameTable_add(struct lldpV2Xdot1LocVlanNameTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocVlanNameTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocVlanNameTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocVlanNameTable_add(struct lldpV2Xdot1LocVlanNameTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVlanNameTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		/* lldpV2Xdot1LocVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2Xdot1LocVlanId, sizeof(thedata->lldpV2Xdot1LocVlanId));
		header_complex_add_data(&lldpV2Xdot1LocVlanNameTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocVlanNameTable_del(struct lldpV2Xdot1LocVlanNameTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocVlanNameTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocVlanNameTable_del(struct lldpV2Xdot1LocVlanNameTable_data *thedata)
{
	struct lldpV2Xdot1LocVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVlanNameTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocVlanNameTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocVlanNameTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocVlanNameTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocVlanNameTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocVlanNameTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocVlanNameTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp = lldpV2Xdot1LocVlanNameTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocVlanNameTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1LocVlanId, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1LocVlanName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1LocVlanName, &StorageTmp->lldpV2Xdot1LocVlanNameLen);
	if (StorageTmp->lldpV2Xdot1LocVlanName == NULL) {
		config_perror("invalid specification for lldpV2Xdot1LocVlanName");
		return;
	}
	lldpV2Xdot1LocVlanNameTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocVlanNameTable(): store configuraiton file for lldpV2Xdot1LocVlanNameTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocVlanNameTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocVlanNameTable: storing data...  "));
	refresh_lldpV2Xdot1LocVlanNameTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocVlanNameTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocVlanNameTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocVlanNameTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1LocVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1LocVlanName, &StorageTmp->lldpV2Xdot1LocVlanNameLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocProtocolTable_data *lldpV2Xdot1LocProtocolTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocProtocolTable table.
 *
 * Creates a new lldpV2Xdot1LocProtocolTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocProtocolTable_data *
lldpV2Xdot1LocProtocolTable_create(void)
{
	struct lldpV2Xdot1LocProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocProtocolTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtocolTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		if ((StorageNew->lldpV2Xdot1LocProtocolId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1LocProtocolIdLen = 0;
		StorageNew->lldpV2Xdot1LocProtocolId[StorageNew->lldpV2Xdot1LocProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocProtocolTable_data *lldpV2Xdot1LocProtocolTable_duplicate(struct lldpV2Xdot1LocProtocolTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocProtocolTable_data *
lldpV2Xdot1LocProtocolTable_duplicate(struct lldpV2Xdot1LocProtocolTable_data *thedata)
{
	struct lldpV2Xdot1LocProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocProtocolTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtocolTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocProtocolTable_id = thedata->lldpV2Xdot1LocProtocolTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocProtocolIndex = thedata->lldpV2Xdot1LocProtocolIndex;
		if (!(StorageNew->lldpV2Xdot1LocProtocolId = malloc(thedata->lldpV2Xdot1LocProtocolIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1LocProtocolId, thedata->lldpV2Xdot1LocProtocolId, thedata->lldpV2Xdot1LocProtocolIdLen);
		StorageNew->lldpV2Xdot1LocProtocolIdLen = thedata->lldpV2Xdot1LocProtocolIdLen;
		StorageNew->lldpV2Xdot1LocProtocolId[StorageNew->lldpV2Xdot1LocProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocProtocolTable_destroy(struct lldpV2Xdot1LocProtocolTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocProtocolTable_destroy(struct lldpV2Xdot1LocProtocolTable_data **thedata)
{
	struct lldpV2Xdot1LocProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtocolTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1LocProtocolId);
		StorageDel->lldpV2Xdot1LocProtocolIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocProtocolTable_add(struct lldpV2Xdot1LocProtocolTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocProtocolTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocProtocolTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocProtocolTable_add(struct lldpV2Xdot1LocProtocolTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtocolTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		/* lldpV2Xdot1LocProtocolIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2Xdot1LocProtocolIndex, sizeof(thedata->lldpV2Xdot1LocProtocolIndex));
		header_complex_add_data(&lldpV2Xdot1LocProtocolTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocProtocolTable_del(struct lldpV2Xdot1LocProtocolTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocProtocolTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocProtocolTable_del(struct lldpV2Xdot1LocProtocolTable_data *thedata)
{
	struct lldpV2Xdot1LocProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocProtocolTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocProtocolTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocProtocolTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocProtocolTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocProtocolTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocProtocolTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocProtocolTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocProtocolTable_data *StorageTmp = lldpV2Xdot1LocProtocolTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocProtocolTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocProtocolIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1LocProtocolId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1LocProtocolId, &StorageTmp->lldpV2Xdot1LocProtocolIdLen);
	if (StorageTmp->lldpV2Xdot1LocProtocolId == NULL) {
		config_perror("invalid specification for lldpV2Xdot1LocProtocolId");
		return;
	}
	lldpV2Xdot1LocProtocolTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocProtocolTable(): store configuraiton file for lldpV2Xdot1LocProtocolTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocProtocolTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocProtocolTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocProtocolTable: storing data...  "));
	refresh_lldpV2Xdot1LocProtocolTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocProtocolTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocProtocolTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocProtocolTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocProtocolIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1LocProtocolId, &StorageTmp->lldpV2Xdot1LocProtocolIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocVidUsageDigestTable_data *lldpV2Xdot1LocVidUsageDigestTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocVidUsageDigestTable table.
 *
 * Creates a new lldpV2Xdot1LocVidUsageDigestTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocVidUsageDigestTable_data *
lldpV2Xdot1LocVidUsageDigestTable_create(void)
{
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocVidUsageDigestTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVidUsageDigestTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocVidUsageDigest = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocVidUsageDigestTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocVidUsageDigestTable_data *lldpV2Xdot1LocVidUsageDigestTable_duplicate(struct lldpV2Xdot1LocVidUsageDigestTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocVidUsageDigestTable_data *
lldpV2Xdot1LocVidUsageDigestTable_duplicate(struct lldpV2Xdot1LocVidUsageDigestTable_data *thedata)
{
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocVidUsageDigestTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVidUsageDigestTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocVidUsageDigestTable_id = thedata->lldpV2Xdot1LocVidUsageDigestTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocVidUsageDigest = thedata->lldpV2Xdot1LocVidUsageDigest;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocVidUsageDigestTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocVidUsageDigestTable_destroy(struct lldpV2Xdot1LocVidUsageDigestTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocVidUsageDigestTable_destroy(struct lldpV2Xdot1LocVidUsageDigestTable_data **thedata)
{
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVidUsageDigestTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocVidUsageDigestTable_add(struct lldpV2Xdot1LocVidUsageDigestTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocVidUsageDigestTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocVidUsageDigestTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocVidUsageDigestTable_add(struct lldpV2Xdot1LocVidUsageDigestTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVidUsageDigestTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1LocVidUsageDigestTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocVidUsageDigestTable_del(struct lldpV2Xdot1LocVidUsageDigestTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocVidUsageDigestTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocVidUsageDigestTable_del(struct lldpV2Xdot1LocVidUsageDigestTable_data *thedata)
{
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocVidUsageDigestTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocVidUsageDigestTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocVidUsageDigestTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocVidUsageDigestTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocVidUsageDigestTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocVidUsageDigestTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocVidUsageDigestTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp = lldpV2Xdot1LocVidUsageDigestTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocVidUsageDigestTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocVidUsageDigest, &tmpsize);
	lldpV2Xdot1LocVidUsageDigestTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocVidUsageDigestTable(): store configuraiton file for lldpV2Xdot1LocVidUsageDigestTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocVidUsageDigestTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocVidUsageDigestTable: storing data...  "));
	refresh_lldpV2Xdot1LocVidUsageDigestTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocVidUsageDigestTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocVidUsageDigestTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocVidUsageDigestTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocVidUsageDigest, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocManVidTable_data *lldpV2Xdot1LocManVidTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocManVidTable table.
 *
 * Creates a new lldpV2Xdot1LocManVidTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocManVidTable_data *
lldpV2Xdot1LocManVidTable_create(void)
{
	struct lldpV2Xdot1LocManVidTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocManVidTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocManVidTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		StorageNew->lldpV2Xdot1LocManVid = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocManVidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocManVidTable_data *lldpV2Xdot1LocManVidTable_duplicate(struct lldpV2Xdot1LocManVidTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocManVidTable_data *
lldpV2Xdot1LocManVidTable_duplicate(struct lldpV2Xdot1LocManVidTable_data *thedata)
{
	struct lldpV2Xdot1LocManVidTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocManVidTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocManVidTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocManVidTable_id = thedata->lldpV2Xdot1LocManVidTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		StorageNew->lldpV2Xdot1LocManVid = thedata->lldpV2Xdot1LocManVid;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocManVidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocManVidTable_destroy(struct lldpV2Xdot1LocManVidTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocManVidTable_destroy(struct lldpV2Xdot1LocManVidTable_data **thedata)
{
	struct lldpV2Xdot1LocManVidTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocManVidTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocManVidTable_add(struct lldpV2Xdot1LocManVidTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocManVidTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocManVidTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocManVidTable_add(struct lldpV2Xdot1LocManVidTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocManVidTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1LocManVidTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocManVidTable_del(struct lldpV2Xdot1LocManVidTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocManVidTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocManVidTable_del(struct lldpV2Xdot1LocManVidTable_data *thedata)
{
	struct lldpV2Xdot1LocManVidTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocManVidTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocManVidTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocManVidTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocManVidTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocManVidTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocManVidTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocManVidTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocManVidTable_data *StorageTmp = lldpV2Xdot1LocManVidTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocManVidTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocManVid, &tmpsize);
	lldpV2Xdot1LocManVidTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocManVidTable(): store configuraiton file for lldpV2Xdot1LocManVidTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocManVidTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocManVidTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocManVidTable: storing data...  "));
	refresh_lldpV2Xdot1LocManVidTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocManVidTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocManVidTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocManVidTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocManVid, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1LocLinkAggTable_data *lldpV2Xdot1LocLinkAggTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1LocLinkAggTable table.
 *
 * Creates a new lldpV2Xdot1LocLinkAggTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1LocLinkAggTable_data *
lldpV2Xdot1LocLinkAggTable_create(void)
{
	struct lldpV2Xdot1LocLinkAggTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocLinkAggTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocLinkAggTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2LocPortIfIndex = 0;
		if (memdup((u_char **) &StorageNew->lldpV2Xdot1LocLinkAggStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2Xdot1LocLinkAggStatusLen = 1;
		StorageNew->lldpV2Xdot1LocLinkAggPortId = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1LocLinkAggTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1LocLinkAggTable_data *lldpV2Xdot1LocLinkAggTable_duplicate(struct lldpV2Xdot1LocLinkAggTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1LocLinkAggTable_data *
lldpV2Xdot1LocLinkAggTable_duplicate(struct lldpV2Xdot1LocLinkAggTable_data *thedata)
{
	struct lldpV2Xdot1LocLinkAggTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1LocLinkAggTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocLinkAggTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1LocLinkAggTable_id = thedata->lldpV2Xdot1LocLinkAggTable_id;
		StorageNew->lldpV2LocPortIfIndex = thedata->lldpV2LocPortIfIndex;
		if (!(StorageNew->lldpV2Xdot1LocLinkAggStatus = malloc(thedata->lldpV2Xdot1LocLinkAggStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1LocLinkAggStatus, thedata->lldpV2Xdot1LocLinkAggStatus, thedata->lldpV2Xdot1LocLinkAggStatusLen);
		StorageNew->lldpV2Xdot1LocLinkAggStatusLen = thedata->lldpV2Xdot1LocLinkAggStatusLen;
		StorageNew->lldpV2Xdot1LocLinkAggStatus[StorageNew->lldpV2Xdot1LocLinkAggStatusLen] = 0;
		StorageNew->lldpV2Xdot1LocLinkAggPortId = thedata->lldpV2Xdot1LocLinkAggPortId;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1LocLinkAggTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1LocLinkAggTable_destroy(struct lldpV2Xdot1LocLinkAggTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1LocLinkAggTable_destroy(struct lldpV2Xdot1LocLinkAggTable_data **thedata)
{
	struct lldpV2Xdot1LocLinkAggTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocLinkAggTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1LocLinkAggStatus);
		StorageDel->lldpV2Xdot1LocLinkAggStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocLinkAggTable_add(struct lldpV2Xdot1LocLinkAggTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1LocLinkAggTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1LocLinkAggTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1LocLinkAggTable_add(struct lldpV2Xdot1LocLinkAggTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocLinkAggTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2LocPortIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2LocPortIfIndex, sizeof(thedata->lldpV2LocPortIfIndex));
		header_complex_add_data(&lldpV2Xdot1LocLinkAggTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1LocLinkAggTable_del(struct lldpV2Xdot1LocLinkAggTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1LocLinkAggTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1LocLinkAggTable_del(struct lldpV2Xdot1LocLinkAggTable_data *thedata)
{
	struct lldpV2Xdot1LocLinkAggTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1LocLinkAggTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1LocLinkAggTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1LocLinkAggTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1LocLinkAggTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1LocLinkAggTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1LocLinkAggTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1LocLinkAggTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp = lldpV2Xdot1LocLinkAggTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1LocLinkAggTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1LocLinkAggStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1LocLinkAggStatus, &StorageTmp->lldpV2Xdot1LocLinkAggStatusLen);
	if (StorageTmp->lldpV2Xdot1LocLinkAggStatus == NULL) {
		config_perror("invalid specification for lldpV2Xdot1LocLinkAggStatus");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1LocLinkAggPortId, &tmpsize);
	lldpV2Xdot1LocLinkAggTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1LocLinkAggTable(): store configuraiton file for lldpV2Xdot1LocLinkAggTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1LocLinkAggTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1LocLinkAggTable: storing data...  "));
	refresh_lldpV2Xdot1LocLinkAggTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1LocLinkAggTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1LocLinkAggTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1LocLinkAggTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2LocPortIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1LocLinkAggStatus, &StorageTmp->lldpV2Xdot1LocLinkAggStatusLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1LocLinkAggPortId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemTable_data *lldpV2Xdot1RemTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemTable table.
 *
 * Creates a new lldpV2Xdot1RemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemTable_data *
lldpV2Xdot1RemTable_create(void)
{
	struct lldpV2Xdot1RemTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		StorageNew->lldpV2Xdot1RemPortVlanId = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemTable_data *lldpV2Xdot1RemTable_duplicate(struct lldpV2Xdot1RemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemTable_data *
lldpV2Xdot1RemTable_duplicate(struct lldpV2Xdot1RemTable_data *thedata)
{
	struct lldpV2Xdot1RemTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemTable_id = thedata->lldpV2Xdot1RemTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot1RemPortVlanId = thedata->lldpV2Xdot1RemPortVlanId;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemTable_destroy(struct lldpV2Xdot1RemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemTable_destroy(struct lldpV2Xdot1RemTable_data **thedata)
{
	struct lldpV2Xdot1RemTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemTable_add(struct lldpV2Xdot1RemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemTable_add(struct lldpV2Xdot1RemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot1RemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemTable_del(struct lldpV2Xdot1RemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemTable_del(struct lldpV2Xdot1RemTable_data *thedata)
{
	struct lldpV2Xdot1RemTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemTable_data *StorageTmp = lldpV2Xdot1RemTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1RemPortVlanId, &tmpsize);
	lldpV2Xdot1RemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemTable(): store configuraiton file for lldpV2Xdot1RemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemTable: storing data...  "));
	refresh_lldpV2Xdot1RemTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1RemPortVlanId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemProtoVlanTable_data *lldpV2Xdot1RemProtoVlanTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemProtoVlanTable table.
 *
 * Creates a new lldpV2Xdot1RemProtoVlanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemProtoVlanTable_data *
lldpV2Xdot1RemProtoVlanTable_create(void)
{
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemProtoVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtoVlanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		StorageNew->lldpV2Xdot1RemProtoVlanSupported = 0;
		StorageNew->lldpV2Xdot1RemProtoVlanEnabled = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemProtoVlanTable_data *lldpV2Xdot1RemProtoVlanTable_duplicate(struct lldpV2Xdot1RemProtoVlanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemProtoVlanTable_data *
lldpV2Xdot1RemProtoVlanTable_duplicate(struct lldpV2Xdot1RemProtoVlanTable_data *thedata)
{
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemProtoVlanTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtoVlanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemProtoVlanTable_id = thedata->lldpV2Xdot1RemProtoVlanTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot1RemProtoVlanId = thedata->lldpV2Xdot1RemProtoVlanId;
		StorageNew->lldpV2Xdot1RemProtoVlanSupported = thedata->lldpV2Xdot1RemProtoVlanSupported;
		StorageNew->lldpV2Xdot1RemProtoVlanEnabled = thedata->lldpV2Xdot1RemProtoVlanEnabled;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemProtoVlanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemProtoVlanTable_destroy(struct lldpV2Xdot1RemProtoVlanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemProtoVlanTable_destroy(struct lldpV2Xdot1RemProtoVlanTable_data **thedata)
{
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtoVlanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemProtoVlanTable_add(struct lldpV2Xdot1RemProtoVlanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemProtoVlanTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemProtoVlanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemProtoVlanTable_add(struct lldpV2Xdot1RemProtoVlanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtoVlanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		/* lldpV2Xdot1RemProtoVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2Xdot1RemProtoVlanId, sizeof(thedata->lldpV2Xdot1RemProtoVlanId));
		header_complex_add_data(&lldpV2Xdot1RemProtoVlanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemProtoVlanTable_del(struct lldpV2Xdot1RemProtoVlanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemProtoVlanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemProtoVlanTable_del(struct lldpV2Xdot1RemProtoVlanTable_data *thedata)
{
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtoVlanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemProtoVlanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemProtoVlanTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemProtoVlanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemProtoVlanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemProtoVlanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemProtoVlanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp = lldpV2Xdot1RemProtoVlanTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemProtoVlanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1RemProtoVlanId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1RemProtoVlanSupported, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1RemProtoVlanEnabled, &tmpsize);
	lldpV2Xdot1RemProtoVlanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemProtoVlanTable(): store configuraiton file for lldpV2Xdot1RemProtoVlanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemProtoVlanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemProtoVlanTable: storing data...  "));
	refresh_lldpV2Xdot1RemProtoVlanTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemProtoVlanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemProtoVlanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemProtoVlanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1RemProtoVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1RemProtoVlanSupported, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1RemProtoVlanEnabled, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemVlanNameTable_data *lldpV2Xdot1RemVlanNameTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemVlanNameTable table.
 *
 * Creates a new lldpV2Xdot1RemVlanNameTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemVlanNameTable_data *
lldpV2Xdot1RemVlanNameTable_create(void)
{
	struct lldpV2Xdot1RemVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemVlanNameTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVlanNameTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if ((StorageNew->lldpV2Xdot1RemVlanName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1RemVlanNameLen = 0;
		StorageNew->lldpV2Xdot1RemVlanName[StorageNew->lldpV2Xdot1RemVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemVlanNameTable_data *lldpV2Xdot1RemVlanNameTable_duplicate(struct lldpV2Xdot1RemVlanNameTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemVlanNameTable_data *
lldpV2Xdot1RemVlanNameTable_duplicate(struct lldpV2Xdot1RemVlanNameTable_data *thedata)
{
	struct lldpV2Xdot1RemVlanNameTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemVlanNameTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVlanNameTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemVlanNameTable_id = thedata->lldpV2Xdot1RemVlanNameTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot1RemVlanId = thedata->lldpV2Xdot1RemVlanId;
		if (!(StorageNew->lldpV2Xdot1RemVlanName = malloc(thedata->lldpV2Xdot1RemVlanNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1RemVlanName, thedata->lldpV2Xdot1RemVlanName, thedata->lldpV2Xdot1RemVlanNameLen);
		StorageNew->lldpV2Xdot1RemVlanNameLen = thedata->lldpV2Xdot1RemVlanNameLen;
		StorageNew->lldpV2Xdot1RemVlanName[StorageNew->lldpV2Xdot1RemVlanNameLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemVlanNameTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemVlanNameTable_destroy(struct lldpV2Xdot1RemVlanNameTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemVlanNameTable_destroy(struct lldpV2Xdot1RemVlanNameTable_data **thedata)
{
	struct lldpV2Xdot1RemVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVlanNameTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1RemVlanName);
		StorageDel->lldpV2Xdot1RemVlanNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemVlanNameTable_add(struct lldpV2Xdot1RemVlanNameTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemVlanNameTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemVlanNameTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemVlanNameTable_add(struct lldpV2Xdot1RemVlanNameTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVlanNameTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		/* lldpV2Xdot1RemVlanId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2Xdot1RemVlanId, sizeof(thedata->lldpV2Xdot1RemVlanId));
		header_complex_add_data(&lldpV2Xdot1RemVlanNameTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemVlanNameTable_del(struct lldpV2Xdot1RemVlanNameTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemVlanNameTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemVlanNameTable_del(struct lldpV2Xdot1RemVlanNameTable_data *thedata)
{
	struct lldpV2Xdot1RemVlanNameTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVlanNameTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemVlanNameTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemVlanNameTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemVlanNameTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemVlanNameTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemVlanNameTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemVlanNameTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp = lldpV2Xdot1RemVlanNameTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemVlanNameTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2Xdot1RemVlanId, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1RemVlanName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1RemVlanName, &StorageTmp->lldpV2Xdot1RemVlanNameLen);
	if (StorageTmp->lldpV2Xdot1RemVlanName == NULL) {
		config_perror("invalid specification for lldpV2Xdot1RemVlanName");
		return;
	}
	lldpV2Xdot1RemVlanNameTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemVlanNameTable(): store configuraiton file for lldpV2Xdot1RemVlanNameTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemVlanNameTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemVlanNameTable: storing data...  "));
	refresh_lldpV2Xdot1RemVlanNameTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemVlanNameTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemVlanNameTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemVlanNameTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2Xdot1RemVlanId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1RemVlanName, &StorageTmp->lldpV2Xdot1RemVlanNameLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemProtocolTable_data *lldpV2Xdot1RemProtocolTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemProtocolTable table.
 *
 * Creates a new lldpV2Xdot1RemProtocolTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemProtocolTable_data *
lldpV2Xdot1RemProtocolTable_create(void)
{
	struct lldpV2Xdot1RemProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemProtocolTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtocolTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if ((StorageNew->lldpV2Xdot1RemProtocolId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->lldpV2Xdot1RemProtocolIdLen = 0;
		StorageNew->lldpV2Xdot1RemProtocolId[StorageNew->lldpV2Xdot1RemProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemProtocolTable_data *lldpV2Xdot1RemProtocolTable_duplicate(struct lldpV2Xdot1RemProtocolTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemProtocolTable_data *
lldpV2Xdot1RemProtocolTable_duplicate(struct lldpV2Xdot1RemProtocolTable_data *thedata)
{
	struct lldpV2Xdot1RemProtocolTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemProtocolTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtocolTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemProtocolTable_id = thedata->lldpV2Xdot1RemProtocolTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		StorageNew->lldpV2Xdot1RemProtocolIndex = thedata->lldpV2Xdot1RemProtocolIndex;
		if (!(StorageNew->lldpV2Xdot1RemProtocolId = malloc(thedata->lldpV2Xdot1RemProtocolIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1RemProtocolId, thedata->lldpV2Xdot1RemProtocolId, thedata->lldpV2Xdot1RemProtocolIdLen);
		StorageNew->lldpV2Xdot1RemProtocolIdLen = thedata->lldpV2Xdot1RemProtocolIdLen;
		StorageNew->lldpV2Xdot1RemProtocolId[StorageNew->lldpV2Xdot1RemProtocolIdLen] = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemProtocolTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemProtocolTable_destroy(struct lldpV2Xdot1RemProtocolTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemProtocolTable_destroy(struct lldpV2Xdot1RemProtocolTable_data **thedata)
{
	struct lldpV2Xdot1RemProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtocolTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1RemProtocolId);
		StorageDel->lldpV2Xdot1RemProtocolIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemProtocolTable_add(struct lldpV2Xdot1RemProtocolTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemProtocolTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemProtocolTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemProtocolTable_add(struct lldpV2Xdot1RemProtocolTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtocolTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		/* lldpV2Xdot1RemProtocolIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2Xdot1RemProtocolIndex, sizeof(thedata->lldpV2Xdot1RemProtocolIndex));
		header_complex_add_data(&lldpV2Xdot1RemProtocolTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemProtocolTable_del(struct lldpV2Xdot1RemProtocolTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemProtocolTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemProtocolTable_del(struct lldpV2Xdot1RemProtocolTable_data *thedata)
{
	struct lldpV2Xdot1RemProtocolTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemProtocolTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemProtocolTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemProtocolTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemProtocolTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemProtocolTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemProtocolTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemProtocolTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemProtocolTable_data *StorageTmp = lldpV2Xdot1RemProtocolTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemProtocolTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1RemProtocolIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1RemProtocolId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1RemProtocolId, &StorageTmp->lldpV2Xdot1RemProtocolIdLen);
	if (StorageTmp->lldpV2Xdot1RemProtocolId == NULL) {
		config_perror("invalid specification for lldpV2Xdot1RemProtocolId");
		return;
	}
	lldpV2Xdot1RemProtocolTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemProtocolTable(): store configuraiton file for lldpV2Xdot1RemProtocolTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemProtocolTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemProtocolTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemProtocolTable: storing data...  "));
	refresh_lldpV2Xdot1RemProtocolTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemProtocolTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemProtocolTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemProtocolTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1RemProtocolIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1RemProtocolId, &StorageTmp->lldpV2Xdot1RemProtocolIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemVidUsageDigestTable_data *lldpV2Xdot1RemVidUsageDigestTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemVidUsageDigestTable table.
 *
 * Creates a new lldpV2Xdot1RemVidUsageDigestTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemVidUsageDigestTable_data *
lldpV2Xdot1RemVidUsageDigestTable_create(void)
{
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemVidUsageDigestTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVidUsageDigestTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2Xdot1RemVidUsageDigest = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemVidUsageDigestTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemVidUsageDigestTable_data *lldpV2Xdot1RemVidUsageDigestTable_duplicate(struct lldpV2Xdot1RemVidUsageDigestTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemVidUsageDigestTable_data *
lldpV2Xdot1RemVidUsageDigestTable_duplicate(struct lldpV2Xdot1RemVidUsageDigestTable_data *thedata)
{
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemVidUsageDigestTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVidUsageDigestTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemVidUsageDigestTable_id = thedata->lldpV2Xdot1RemVidUsageDigestTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2Xdot1RemVidUsageDigest = thedata->lldpV2Xdot1RemVidUsageDigest;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemVidUsageDigestTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemVidUsageDigestTable_destroy(struct lldpV2Xdot1RemVidUsageDigestTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemVidUsageDigestTable_destroy(struct lldpV2Xdot1RemVidUsageDigestTable_data **thedata)
{
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVidUsageDigestTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemVidUsageDigestTable_add(struct lldpV2Xdot1RemVidUsageDigestTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemVidUsageDigestTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemVidUsageDigestTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemVidUsageDigestTable_add(struct lldpV2Xdot1RemVidUsageDigestTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVidUsageDigestTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		header_complex_add_data(&lldpV2Xdot1RemVidUsageDigestTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemVidUsageDigestTable_del(struct lldpV2Xdot1RemVidUsageDigestTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemVidUsageDigestTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemVidUsageDigestTable_del(struct lldpV2Xdot1RemVidUsageDigestTable_data *thedata)
{
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemVidUsageDigestTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemVidUsageDigestTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemVidUsageDigestTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemVidUsageDigestTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemVidUsageDigestTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemVidUsageDigestTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemVidUsageDigestTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp = lldpV2Xdot1RemVidUsageDigestTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemVidUsageDigestTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1RemVidUsageDigest, &tmpsize);
	lldpV2Xdot1RemVidUsageDigestTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemVidUsageDigestTable(): store configuraiton file for lldpV2Xdot1RemVidUsageDigestTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemVidUsageDigestTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemVidUsageDigestTable: storing data...  "));
	refresh_lldpV2Xdot1RemVidUsageDigestTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemVidUsageDigestTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemVidUsageDigestTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemVidUsageDigestTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1RemVidUsageDigest, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemManVidTable_data *lldpV2Xdot1RemManVidTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemManVidTable table.
 *
 * Creates a new lldpV2Xdot1RemManVidTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemManVidTable_data *
lldpV2Xdot1RemManVidTable_create(void)
{
	struct lldpV2Xdot1RemManVidTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemManVidTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemManVidTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2Xdot1RemManVid = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemManVidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemManVidTable_data *lldpV2Xdot1RemManVidTable_duplicate(struct lldpV2Xdot1RemManVidTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemManVidTable_data *
lldpV2Xdot1RemManVidTable_duplicate(struct lldpV2Xdot1RemManVidTable_data *thedata)
{
	struct lldpV2Xdot1RemManVidTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemManVidTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemManVidTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemManVidTable_id = thedata->lldpV2Xdot1RemManVidTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2Xdot1RemManVid = thedata->lldpV2Xdot1RemManVid;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemManVidTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemManVidTable_destroy(struct lldpV2Xdot1RemManVidTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemManVidTable_destroy(struct lldpV2Xdot1RemManVidTable_data **thedata)
{
	struct lldpV2Xdot1RemManVidTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemManVidTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemManVidTable_add(struct lldpV2Xdot1RemManVidTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemManVidTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemManVidTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemManVidTable_add(struct lldpV2Xdot1RemManVidTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemManVidTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		header_complex_add_data(&lldpV2Xdot1RemManVidTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemManVidTable_del(struct lldpV2Xdot1RemManVidTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemManVidTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemManVidTable_del(struct lldpV2Xdot1RemManVidTable_data *thedata)
{
	struct lldpV2Xdot1RemManVidTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemManVidTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemManVidTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemManVidTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemManVidTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemManVidTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemManVidTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemManVidTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemManVidTable_data *StorageTmp = lldpV2Xdot1RemManVidTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemManVidTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1RemManVid, &tmpsize);
	lldpV2Xdot1RemManVidTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemManVidTable(): store configuraiton file for lldpV2Xdot1RemManVidTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemManVidTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemManVidTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemManVidTable: storing data...  "));
	refresh_lldpV2Xdot1RemManVidTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemManVidTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemManVidTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemManVidTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1RemManVid, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct lldpV2Xdot1RemLinkAggTable_data *lldpV2Xdot1RemLinkAggTable_create(void)
 * @brief create a fresh data structure representing a new row in the lldpV2Xdot1RemLinkAggTable table.
 *
 * Creates a new lldpV2Xdot1RemLinkAggTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct lldpV2Xdot1RemLinkAggTable_data *
lldpV2Xdot1RemLinkAggTable_create(void)
{
	struct lldpV2Xdot1RemLinkAggTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemLinkAggTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemLinkAggTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->lldpV2RemTimeMark = 0;
		StorageNew->lldpV2RemLocalIfIndex = 0;
		StorageNew->lldpV2RemLocalDestMACAddress = 0;
		StorageNew->lldpV2RemIndex = 0;
		if (memdup((u_char **) &StorageNew->lldpV2Xdot1RemLinkAggStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->lldpV2Xdot1RemLinkAggStatusLen = 1;
		StorageNew->lldpV2Xdot1RemLinkAggPortId = 0;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	lldpV2Xdot1RemLinkAggTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct lldpV2Xdot1RemLinkAggTable_data *lldpV2Xdot1RemLinkAggTable_duplicate(struct lldpV2Xdot1RemLinkAggTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct lldpV2Xdot1RemLinkAggTable_data *
lldpV2Xdot1RemLinkAggTable_duplicate(struct lldpV2Xdot1RemLinkAggTable_data *thedata)
{
	struct lldpV2Xdot1RemLinkAggTable_data *StorageNew = SNMP_MALLOC_STRUCT(lldpV2Xdot1RemLinkAggTable_data);

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemLinkAggTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->lldpV2Xdot1RemLinkAggTable_id = thedata->lldpV2Xdot1RemLinkAggTable_id;
		StorageNew->lldpV2RemTimeMark = thedata->lldpV2RemTimeMark;
		StorageNew->lldpV2RemLocalIfIndex = thedata->lldpV2RemLocalIfIndex;
		StorageNew->lldpV2RemLocalDestMACAddress = thedata->lldpV2RemLocalDestMACAddress;
		StorageNew->lldpV2RemIndex = thedata->lldpV2RemIndex;
		if (!(StorageNew->lldpV2Xdot1RemLinkAggStatus = malloc(thedata->lldpV2Xdot1RemLinkAggStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->lldpV2Xdot1RemLinkAggStatus, thedata->lldpV2Xdot1RemLinkAggStatus, thedata->lldpV2Xdot1RemLinkAggStatusLen);
		StorageNew->lldpV2Xdot1RemLinkAggStatusLen = thedata->lldpV2Xdot1RemLinkAggStatusLen;
		StorageNew->lldpV2Xdot1RemLinkAggStatus[StorageNew->lldpV2Xdot1RemLinkAggStatusLen] = 0;
		StorageNew->lldpV2Xdot1RemLinkAggPortId = thedata->lldpV2Xdot1RemLinkAggPortId;
	}
      done:
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	lldpV2Xdot1RemLinkAggTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int lldpV2Xdot1RemLinkAggTable_destroy(struct lldpV2Xdot1RemLinkAggTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
lldpV2Xdot1RemLinkAggTable_destroy(struct lldpV2Xdot1RemLinkAggTable_data **thedata)
{
	struct lldpV2Xdot1RemLinkAggTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemLinkAggTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->lldpV2Xdot1RemLinkAggStatus);
		StorageDel->lldpV2Xdot1RemLinkAggStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemLinkAggTable_add(struct lldpV2Xdot1RemLinkAggTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the lldpV2Xdot1RemLinkAggTable table data set.
 *
 * Adds a table row structure to the lldpV2Xdot1RemLinkAggTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
lldpV2Xdot1RemLinkAggTable_add(struct lldpV2Xdot1RemLinkAggTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemLinkAggTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* lldpV2RemTimeMark */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_TIMETICKS, (u_char *) &thedata->lldpV2RemTimeMark, sizeof(thedata->lldpV2RemTimeMark));
		/* lldpV2RemLocalIfIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->lldpV2RemLocalIfIndex, sizeof(thedata->lldpV2RemLocalIfIndex));
		/* lldpV2RemLocalDestMACAddress */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemLocalDestMACAddress, sizeof(thedata->lldpV2RemLocalDestMACAddress));
		/* lldpV2RemIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->lldpV2RemIndex, sizeof(thedata->lldpV2RemIndex));
		header_complex_add_data(&lldpV2Xdot1RemLinkAggTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int lldpV2Xdot1RemLinkAggTable_del(struct lldpV2Xdot1RemLinkAggTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the lldpV2Xdot1RemLinkAggTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
lldpV2Xdot1RemLinkAggTable_del(struct lldpV2Xdot1RemLinkAggTable_data *thedata)
{
	struct lldpV2Xdot1RemLinkAggTable_data *StorageDel;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1RemLinkAggTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(lldpV2Xdot1RemLinkAggTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&lldpV2Xdot1RemLinkAggTableStorage, hciptr);
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_lldpV2Xdot1RemLinkAggTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for lldpV2Xdot1RemLinkAggTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case lldpV2Xdot1RemLinkAggTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_lldpV2Xdot1RemLinkAggTable(const char *token, char *line)
{
	size_t tmpsize;
	struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp = lldpV2Xdot1RemLinkAggTable_create();

	DEBUGMSGTL(("lldpV2Xdot1MIB", "parse_lldpV2Xdot1RemLinkAggTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2RemIndex, &tmpsize);
	SNMP_FREE(StorageTmp->lldpV2Xdot1RemLinkAggStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lldpV2Xdot1RemLinkAggStatus, &StorageTmp->lldpV2Xdot1RemLinkAggStatusLen);
	if (StorageTmp->lldpV2Xdot1RemLinkAggStatus == NULL) {
		config_perror("invalid specification for lldpV2Xdot1RemLinkAggStatus");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->lldpV2Xdot1RemLinkAggPortId, &tmpsize);
	lldpV2Xdot1RemLinkAggTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/*
 * store_lldpV2Xdot1RemLinkAggTable(): store configuraiton file for lldpV2Xdot1RemLinkAggTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_lldpV2Xdot1RemLinkAggTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "store_lldpV2Xdot1RemLinkAggTable: storing data...  "));
	refresh_lldpV2Xdot1RemLinkAggTable(1);
	(void) tmpsize;
	for (hcindex = lldpV2Xdot1RemLinkAggTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct lldpV2Xdot1RemLinkAggTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "lldpV2Xdot1RemLinkAggTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->lldpV2RemTimeMark, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->lldpV2RemLocalIfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemLocalDestMACAddress, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2RemIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lldpV2Xdot1RemLinkAggStatus, &StorageTmp->lldpV2Xdot1RemLinkAggStatusLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->lldpV2Xdot1RemLinkAggPortId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigPortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigPortVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigPortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1ConfigPortVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigPortVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1ConfigPortVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigPortVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigPortVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1ConfigPortVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1ConfigPortVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1ConfigPortVlanTable_data *
refresh_lldpV2Xdot1ConfigPortVlanTable_row(struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1ConfigPortVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1ConfigPortVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigPortVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1ConfigPortVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1ConfigPortVlanTable(int force)
{
	if (!force && lldpV2Xdot1ConfigPortVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1ConfigPortVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1ConfigPortVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1ConfigPortVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1ConfigPortVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1ConfigPortVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1ConfigPortVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1ConfigPortVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1ConfigPortVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1CONFIGPORTVLANTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot1ConfigPortVlanTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, struct lldpV2Xdot1ConfigVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, struct lldpV2Xdot1ConfigVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, struct lldpV2Xdot1ConfigVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1ConfigVlanNameTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, struct lldpV2Xdot1ConfigVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1ConfigVlanNameTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, struct lldpV2Xdot1ConfigVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, struct lldpV2Xdot1ConfigVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1ConfigVlanNameTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1ConfigVlanNameTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1ConfigVlanNameTable_data *
refresh_lldpV2Xdot1ConfigVlanNameTable_row(struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1ConfigVlanNameTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1ConfigVlanNameTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigVlanNameTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1ConfigVlanNameTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1ConfigVlanNameTable(int force)
{
	if (!force && lldpV2Xdot1ConfigVlanNameTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1ConfigVlanNameTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1ConfigVlanNameTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1ConfigVlanNameTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1ConfigVlanNameTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1ConfigVlanNameTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1ConfigVlanNameTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1ConfigVlanNameTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1ConfigVlanNameTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1CONFIGVLANNAMETXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot1ConfigVlanNameTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1ConfigProtoVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1ConfigProtoVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1ConfigProtoVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1ConfigProtoVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1ConfigProtoVlanTable_data *
refresh_lldpV2Xdot1ConfigProtoVlanTable_row(struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigProtoVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1ConfigProtoVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1ConfigProtoVlanTable(int force)
{
	if (!force && lldpV2Xdot1ConfigProtoVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1ConfigProtoVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1ConfigProtoVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1ConfigProtoVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1ConfigProtoVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1ConfigProtoVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1ConfigProtoVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1ConfigProtoVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1ConfigProtoVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1CONFIGPROTOVLANTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot1ConfigProtoVlanTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtocolTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1ConfigProtocolTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtocolTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1ConfigProtocolTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, struct lldpV2Xdot1ConfigProtocolTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1ConfigProtocolTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1ConfigProtocolTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1ConfigProtocolTable_data *
refresh_lldpV2Xdot1ConfigProtocolTable_row(struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1ConfigProtocolTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1ConfigProtocolTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigProtocolTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1ConfigProtocolTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1ConfigProtocolTable(int force)
{
	if (!force && lldpV2Xdot1ConfigProtocolTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1ConfigProtocolTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1ConfigProtocolTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1ConfigProtocolTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1ConfigProtocolTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1ConfigProtocolTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1ConfigProtocolTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1ConfigProtocolTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1ConfigProtocolTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1CONFIGPROTOCOLTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot1ConfigProtocolTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1ConfigVidUsageDigestTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1ConfigVidUsageDigestTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1ConfigVidUsageDigestTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1ConfigVidUsageDigestTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1ConfigVidUsageDigestTable_data *
refresh_lldpV2Xdot1ConfigVidUsageDigestTable_row(struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigVidUsageDigestTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1ConfigVidUsageDigestTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1ConfigVidUsageDigestTable(int force)
{
	if (!force && lldpV2Xdot1ConfigVidUsageDigestTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1ConfigVidUsageDigestTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1ConfigVidUsageDigestTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1ConfigVidUsageDigestTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1ConfigVidUsageDigestTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1ConfigVidUsageDigestTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1ConfigVidUsageDigestTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1ConfigVidUsageDigestTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1ConfigVidUsageDigestTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1CONFIGVIDUSAGEDIGESTTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot1ConfigVidUsageDigestTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, struct lldpV2Xdot1ConfigManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, struct lldpV2Xdot1ConfigManVidTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, struct lldpV2Xdot1ConfigManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1ConfigManVidTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, struct lldpV2Xdot1ConfigManVidTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1ConfigManVidTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, struct lldpV2Xdot1ConfigManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, struct lldpV2Xdot1ConfigManVidTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1ConfigManVidTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1ConfigManVidTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1ConfigManVidTable_data *
refresh_lldpV2Xdot1ConfigManVidTable_row(struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1ConfigManVidTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1ConfigManVidTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1ConfigManVidTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1ConfigManVidTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1ConfigManVidTable(int force)
{
	if (!force && lldpV2Xdot1ConfigManVidTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1ConfigManVidTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1ConfigManVidTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1ConfigManVidTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1ConfigManVidTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1ConfigManVidTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1ConfigManVidTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1ConfigManVidTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1ConfigManVidTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1CONFIGMANVIDTXENABLE:	/* ReadWrite */
		*write_method = write_lldpV2Xdot1ConfigManVidTxEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1ConfigManVidTxEnable);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1ConfigManVidTxEnable;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, struct lldpV2Xdot1LocTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, struct lldpV2Xdot1LocTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, struct lldpV2Xdot1LocTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, struct lldpV2Xdot1LocTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, struct lldpV2Xdot1LocTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, struct lldpV2Xdot1LocTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocTable_data *
refresh_lldpV2Xdot1LocTable_row(struct lldpV2Xdot1LocTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocTable(int force)
{
	if (!force && lldpV2Xdot1LocTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCPORTVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1LocPortVlanId);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1LocPortVlanId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1LocProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1LocProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1LocProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocProtoVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1LocProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocProtoVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1LocProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1LocProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocProtoVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocProtoVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocProtoVlanTable_data *
refresh_lldpV2Xdot1LocProtoVlanTable_row(struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocProtoVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocProtoVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocProtoVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocProtoVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocProtoVlanTable(int force)
{
	if (!force && lldpV2Xdot1LocProtoVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocProtoVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocProtoVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocProtoVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocProtoVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocProtoVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocProtoVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocProtoVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocProtoVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocProtoVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCPROTOVLANSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1LocProtoVlanSupported);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1LocProtoVlanSupported;
		break;
	case (u_char) LLDPV2XDOT1LOCPROTOVLANENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1LocProtoVlanEnabled);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1LocProtoVlanEnabled;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, struct lldpV2Xdot1LocVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, struct lldpV2Xdot1LocVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, struct lldpV2Xdot1LocVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocVlanNameTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, struct lldpV2Xdot1LocVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocVlanNameTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, struct lldpV2Xdot1LocVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, struct lldpV2Xdot1LocVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocVlanNameTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocVlanNameTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocVlanNameTable_data *
refresh_lldpV2Xdot1LocVlanNameTable_row(struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocVlanNameTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocVlanNameTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocVlanNameTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocVlanNameTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocVlanNameTable(int force)
{
	if (!force && lldpV2Xdot1LocVlanNameTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocVlanNameTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocVlanNameTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocVlanNameTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocVlanNameTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocVlanNameTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocVlanNameTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocVlanNameTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocVlanNameTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocVlanNameTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCVLANNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1LocVlanNameLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1LocVlanName;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, struct lldpV2Xdot1LocProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, struct lldpV2Xdot1LocProtocolTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, struct lldpV2Xdot1LocProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocProtocolTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, struct lldpV2Xdot1LocProtocolTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocProtocolTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, struct lldpV2Xdot1LocProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, struct lldpV2Xdot1LocProtocolTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocProtocolTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocProtocolTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocProtocolTable_data *
refresh_lldpV2Xdot1LocProtocolTable_row(struct lldpV2Xdot1LocProtocolTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocProtocolTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocProtocolTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocProtocolTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocProtocolTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocProtocolTable(int force)
{
	if (!force && lldpV2Xdot1LocProtocolTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocProtocolTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocProtocolTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocProtocolTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocProtocolTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocProtocolTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocProtocolTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocProtocolTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocProtocolTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocProtocolTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCPROTOCOLID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1LocProtocolIdLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1LocProtocolId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocVidUsageDigestTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocVidUsageDigestTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocVidUsageDigestTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocVidUsageDigestTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocVidUsageDigestTable_data *
refresh_lldpV2Xdot1LocVidUsageDigestTable_row(struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocVidUsageDigestTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocVidUsageDigestTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocVidUsageDigestTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocVidUsageDigestTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocVidUsageDigestTable(int force)
{
	if (!force && lldpV2Xdot1LocVidUsageDigestTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocVidUsageDigestTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocVidUsageDigestTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocVidUsageDigestTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocVidUsageDigestTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocVidUsageDigestTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocVidUsageDigestTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocVidUsageDigestTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocVidUsageDigestTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocVidUsageDigestTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCVIDUSAGEDIGEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1LocVidUsageDigest);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1LocVidUsageDigest;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, struct lldpV2Xdot1LocManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, struct lldpV2Xdot1LocManVidTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, struct lldpV2Xdot1LocManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocManVidTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, struct lldpV2Xdot1LocManVidTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocManVidTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, struct lldpV2Xdot1LocManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, struct lldpV2Xdot1LocManVidTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocManVidTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocManVidTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocManVidTable_data *
refresh_lldpV2Xdot1LocManVidTable_row(struct lldpV2Xdot1LocManVidTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocManVidTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocManVidTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocManVidTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocManVidTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocManVidTable(int force)
{
	if (!force && lldpV2Xdot1LocManVidTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocManVidTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocManVidTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocManVidTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocManVidTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocManVidTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocManVidTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocManVidTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocManVidTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocManVidTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCMANVID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1LocManVid);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1LocManVid;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, struct lldpV2Xdot1LocLinkAggTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, struct lldpV2Xdot1LocLinkAggTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, struct lldpV2Xdot1LocLinkAggTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1LocLinkAggTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, struct lldpV2Xdot1LocLinkAggTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1LocLinkAggTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, struct lldpV2Xdot1LocLinkAggTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, struct lldpV2Xdot1LocLinkAggTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1LocLinkAggTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1LocLinkAggTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1LocLinkAggTable_data *
refresh_lldpV2Xdot1LocLinkAggTable_row(struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1LocLinkAggTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1LocLinkAggTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1LocLinkAggTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1LocLinkAggTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1LocLinkAggTable(int force)
{
	if (!force && lldpV2Xdot1LocLinkAggTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1LocLinkAggTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1LocLinkAggTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1LocLinkAggTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1LocLinkAggTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1LocLinkAggTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1LocLinkAggTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1LocLinkAggTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1LocLinkAggTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1LocLinkAggTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1LOCLINKAGGSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1LocLinkAggStatusLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1LocLinkAggStatus;
		break;
	case (u_char) LLDPV2XDOT1LOCLINKAGGPORTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1LocLinkAggPortId);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1LocLinkAggPortId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, struct lldpV2Xdot1RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, struct lldpV2Xdot1RemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, struct lldpV2Xdot1RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, struct lldpV2Xdot1RemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, struct lldpV2Xdot1RemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, struct lldpV2Xdot1RemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemTable_data *
refresh_lldpV2Xdot1RemTable_row(struct lldpV2Xdot1RemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemTable(int force)
{
	if (!force && lldpV2Xdot1RemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMPORTVLANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1RemPortVlanId);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1RemPortVlanId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1RemProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1RemProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1RemProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemProtoVlanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1RemProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemProtoVlanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1RemProtoVlanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, struct lldpV2Xdot1RemProtoVlanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemProtoVlanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemProtoVlanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemProtoVlanTable_data *
refresh_lldpV2Xdot1RemProtoVlanTable_row(struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemProtoVlanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemProtoVlanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemProtoVlanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemProtoVlanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemProtoVlanTable(int force)
{
	if (!force && lldpV2Xdot1RemProtoVlanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemProtoVlanTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemProtoVlanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemProtoVlanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemProtoVlanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemProtoVlanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemProtoVlanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemProtoVlanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemProtoVlanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemProtoVlanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMPROTOVLANSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1RemProtoVlanSupported);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1RemProtoVlanSupported;
		break;
	case (u_char) LLDPV2XDOT1REMPROTOVLANENABLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1RemProtoVlanEnabled);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1RemProtoVlanEnabled;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, struct lldpV2Xdot1RemVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, struct lldpV2Xdot1RemVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, struct lldpV2Xdot1RemVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemVlanNameTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, struct lldpV2Xdot1RemVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemVlanNameTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, struct lldpV2Xdot1RemVlanNameTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, struct lldpV2Xdot1RemVlanNameTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemVlanNameTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemVlanNameTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemVlanNameTable_data *
refresh_lldpV2Xdot1RemVlanNameTable_row(struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemVlanNameTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemVlanNameTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemVlanNameTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemVlanNameTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemVlanNameTable(int force)
{
	if (!force && lldpV2Xdot1RemVlanNameTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemVlanNameTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemVlanNameTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemVlanNameTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemVlanNameTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemVlanNameTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemVlanNameTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemVlanNameTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemVlanNameTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemVlanNameTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMVLANNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1RemVlanNameLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1RemVlanName;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, struct lldpV2Xdot1RemProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, struct lldpV2Xdot1RemProtocolTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, struct lldpV2Xdot1RemProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemProtocolTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, struct lldpV2Xdot1RemProtocolTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemProtocolTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, struct lldpV2Xdot1RemProtocolTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, struct lldpV2Xdot1RemProtocolTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemProtocolTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemProtocolTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemProtocolTable_data *
refresh_lldpV2Xdot1RemProtocolTable_row(struct lldpV2Xdot1RemProtocolTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemProtocolTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemProtocolTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemProtocolTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemProtocolTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemProtocolTable(int force)
{
	if (!force && lldpV2Xdot1RemProtocolTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemProtocolTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemProtocolTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemProtocolTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemProtocolTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemProtocolTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemProtocolTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemProtocolTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemProtocolTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemProtocolTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMPROTOCOLID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1RemProtocolIdLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1RemProtocolId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemVidUsageDigestTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemVidUsageDigestTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemVidUsageDigestTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemVidUsageDigestTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemVidUsageDigestTable_data *
refresh_lldpV2Xdot1RemVidUsageDigestTable_row(struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemVidUsageDigestTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemVidUsageDigestTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemVidUsageDigestTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemVidUsageDigestTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemVidUsageDigestTable(int force)
{
	if (!force && lldpV2Xdot1RemVidUsageDigestTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemVidUsageDigestTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemVidUsageDigestTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemVidUsageDigestTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemVidUsageDigestTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemVidUsageDigestTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemVidUsageDigestTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemVidUsageDigestTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemVidUsageDigestTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemVidUsageDigestTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMVIDUSAGEDIGEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1RemVidUsageDigest);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1RemVidUsageDigest;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, struct lldpV2Xdot1RemManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, struct lldpV2Xdot1RemManVidTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, struct lldpV2Xdot1RemManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemManVidTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, struct lldpV2Xdot1RemManVidTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemManVidTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, struct lldpV2Xdot1RemManVidTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, struct lldpV2Xdot1RemManVidTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemManVidTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemManVidTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemManVidTable_data *
refresh_lldpV2Xdot1RemManVidTable_row(struct lldpV2Xdot1RemManVidTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemManVidTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemManVidTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemManVidTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemManVidTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemManVidTable(int force)
{
	if (!force && lldpV2Xdot1RemManVidTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemManVidTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemManVidTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemManVidTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemManVidTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemManVidTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemManVidTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemManVidTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemManVidTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemManVidTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMMANVID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1RemManVid);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1RemManVid;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, struct lldpV2Xdot1RemLinkAggTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, struct lldpV2Xdot1RemLinkAggTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, struct lldpV2Xdot1RemLinkAggTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_lldpV2Xdot1RemLinkAggTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, struct lldpV2Xdot1RemLinkAggTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	lldpV2Xdot1RemLinkAggTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, struct lldpV2Xdot1RemLinkAggTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, struct lldpV2Xdot1RemLinkAggTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_lldpV2Xdot1RemLinkAggTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the lldpV2Xdot1RemLinkAggTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct lldpV2Xdot1RemLinkAggTable_data *
refresh_lldpV2Xdot1RemLinkAggTable_row(struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->lldpV2Xdot1RemLinkAggTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->lldpV2Xdot1RemLinkAggTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_lldpV2Xdot1RemLinkAggTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the lldpV2Xdot1RemLinkAggTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_lldpV2Xdot1RemLinkAggTable(int force)
{
	if (!force && lldpV2Xdot1RemLinkAggTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	lldpV2Xdot1RemLinkAggTable_refresh = 0;
}

/**
 * @fn u_char *var_lldpV2Xdot1RemLinkAggTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in lldpV2Xdot1RemLinkAggTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_lldpV2Xdot1MIB above.
 */
u_char *
var_lldpV2Xdot1RemLinkAggTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct lldpV2Xdot1RemLinkAggTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "var_lldpV2Xdot1RemLinkAggTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_lldpV2Xdot1RemLinkAggTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(lldpV2Xdot1RemLinkAggTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_lldpV2Xdot1RemLinkAggTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LLDPV2XDOT1REMLINKAGGSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lldpV2Xdot1RemLinkAggStatusLen;
		rval = (u_char *) StorageTmp->lldpV2Xdot1RemLinkAggStatus;
		break;
	case (u_char) LLDPV2XDOT1REMLINKAGGPORTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->lldpV2Xdot1RemLinkAggPortId);
		rval = (u_char *) &StorageTmp->lldpV2Xdot1RemLinkAggPortId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_lldpV2Xdot1ConfigPortVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot1ConfigPortVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot1ConfigPortVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "write_lldpV2Xdot1ConfigPortVlanTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot1ConfigPortVlanTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigPortVlanTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigPortVlanTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigPortVlanTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot1ConfigPortVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old = lldpV2Xdot1ConfigPortVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot1ConfigPortVlanTable_rsvs++;
		StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot1ConfigPortVlanTable_tsts == 0)
				if ((ret = check_lldpV2Xdot1ConfigPortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigPortVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot1ConfigPortVlanTable_sets == 0)
				if ((ret = update_lldpV2Xdot1ConfigPortVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigPortVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old) != NULL) {
			lldpV2Xdot1ConfigPortVlanTable_destroy(&StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old);
			StorageTmp->lldpV2Xdot1ConfigPortVlanTable_rsvs = 0;
			StorageTmp->lldpV2Xdot1ConfigPortVlanTable_tsts = 0;
			StorageTmp->lldpV2Xdot1ConfigPortVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot1ConfigPortVlanTable_sets == 0)
			revert_lldpV2Xdot1ConfigPortVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old) == NULL)
			break;
		StorageTmp->lldpV2Xdot1ConfigPortVlanTxEnable = StorageOld->lldpV2Xdot1ConfigPortVlanTxEnable;
		if (--StorageTmp->lldpV2Xdot1ConfigPortVlanTable_rsvs == 0)
			lldpV2Xdot1ConfigPortVlanTable_destroy(&StorageTmp->lldpV2Xdot1ConfigPortVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2Xdot1ConfigVlanNameTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot1ConfigVlanNameTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot1ConfigVlanNameTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "write_lldpV2Xdot1ConfigVlanNameTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot1ConfigVlanNameTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigVlanNameTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigVlanNameTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigVlanNameTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot1ConfigVlanNameTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old = lldpV2Xdot1ConfigVlanNameTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot1ConfigVlanNameTable_rsvs++;
		StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot1ConfigVlanNameTable_tsts == 0)
				if ((ret = check_lldpV2Xdot1ConfigVlanNameTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigVlanNameTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot1ConfigVlanNameTable_sets == 0)
				if ((ret = update_lldpV2Xdot1ConfigVlanNameTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigVlanNameTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old) != NULL) {
			lldpV2Xdot1ConfigVlanNameTable_destroy(&StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old);
			StorageTmp->lldpV2Xdot1ConfigVlanNameTable_rsvs = 0;
			StorageTmp->lldpV2Xdot1ConfigVlanNameTable_tsts = 0;
			StorageTmp->lldpV2Xdot1ConfigVlanNameTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot1ConfigVlanNameTable_sets == 0)
			revert_lldpV2Xdot1ConfigVlanNameTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old) == NULL)
			break;
		StorageTmp->lldpV2Xdot1ConfigVlanNameTxEnable = StorageOld->lldpV2Xdot1ConfigVlanNameTxEnable;
		if (--StorageTmp->lldpV2Xdot1ConfigVlanNameTable_rsvs == 0)
			lldpV2Xdot1ConfigVlanNameTable_destroy(&StorageTmp->lldpV2Xdot1ConfigVlanNameTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2Xdot1ConfigProtoVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot1ConfigProtoVlanTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot1ConfigProtoVlanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "write_lldpV2Xdot1ConfigProtoVlanTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot1ConfigProtoVlanTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigProtoVlanTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigProtoVlanTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigProtoVlanTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old = lldpV2Xdot1ConfigProtoVlanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_rsvs++;
		StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_tsts == 0)
				if ((ret = check_lldpV2Xdot1ConfigProtoVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_sets == 0)
				if ((ret = update_lldpV2Xdot1ConfigProtoVlanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old) != NULL) {
			lldpV2Xdot1ConfigProtoVlanTable_destroy(&StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old);
			StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_rsvs = 0;
			StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_tsts = 0;
			StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_sets == 0)
			revert_lldpV2Xdot1ConfigProtoVlanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old) == NULL)
			break;
		StorageTmp->lldpV2Xdot1ConfigProtoVlanTxEnable = StorageOld->lldpV2Xdot1ConfigProtoVlanTxEnable;
		if (--StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_rsvs == 0)
			lldpV2Xdot1ConfigProtoVlanTable_destroy(&StorageTmp->lldpV2Xdot1ConfigProtoVlanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2Xdot1ConfigProtocolTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot1ConfigProtocolTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot1ConfigProtocolTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "write_lldpV2Xdot1ConfigProtocolTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot1ConfigProtocolTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigProtocolTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigProtocolTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigProtocolTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot1ConfigProtocolTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old = lldpV2Xdot1ConfigProtocolTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot1ConfigProtocolTable_rsvs++;
		StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot1ConfigProtocolTable_tsts == 0)
				if ((ret = check_lldpV2Xdot1ConfigProtocolTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigProtocolTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot1ConfigProtocolTable_sets == 0)
				if ((ret = update_lldpV2Xdot1ConfigProtocolTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigProtocolTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old) != NULL) {
			lldpV2Xdot1ConfigProtocolTable_destroy(&StorageTmp->lldpV2Xdot1ConfigProtocolTable_old);
			StorageTmp->lldpV2Xdot1ConfigProtocolTable_rsvs = 0;
			StorageTmp->lldpV2Xdot1ConfigProtocolTable_tsts = 0;
			StorageTmp->lldpV2Xdot1ConfigProtocolTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot1ConfigProtocolTable_sets == 0)
			revert_lldpV2Xdot1ConfigProtocolTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigProtocolTable_old) == NULL)
			break;
		StorageTmp->lldpV2Xdot1ConfigProtocolTxEnable = StorageOld->lldpV2Xdot1ConfigProtocolTxEnable;
		if (--StorageTmp->lldpV2Xdot1ConfigProtocolTable_rsvs == 0)
			lldpV2Xdot1ConfigProtocolTable_destroy(&StorageTmp->lldpV2Xdot1ConfigProtocolTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2Xdot1ConfigVidUsageDigestTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot1ConfigVidUsageDigestTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot1ConfigVidUsageDigestTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "write_lldpV2Xdot1ConfigVidUsageDigestTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot1ConfigVidUsageDigestTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigVidUsageDigestTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigVidUsageDigestTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigVidUsageDigestTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old = lldpV2Xdot1ConfigVidUsageDigestTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_rsvs++;
		StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_tsts == 0)
				if ((ret = check_lldpV2Xdot1ConfigVidUsageDigestTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_sets == 0)
				if ((ret = update_lldpV2Xdot1ConfigVidUsageDigestTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old) != NULL) {
			lldpV2Xdot1ConfigVidUsageDigestTable_destroy(&StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old);
			StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_rsvs = 0;
			StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_tsts = 0;
			StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_sets == 0)
			revert_lldpV2Xdot1ConfigVidUsageDigestTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old) == NULL)
			break;
		StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTxEnable = StorageOld->lldpV2Xdot1ConfigVidUsageDigestTxEnable;
		if (--StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_rsvs == 0)
			lldpV2Xdot1ConfigVidUsageDigestTable_destroy(&StorageTmp->lldpV2Xdot1ConfigVidUsageDigestTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_lldpV2Xdot1ConfigManVidTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_lldpV2Xdot1ConfigManVidTxEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct lldpV2Xdot1ConfigManVidTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("lldpV2Xdot1MIB", "write_lldpV2Xdot1ConfigManVidTxEnable entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(lldpV2Xdot1ConfigManVidTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigManVidTxEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigManVidTxEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to lldpV2Xdot1ConfigManVidTxEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old) == NULL)
			if (StorageTmp->lldpV2Xdot1ConfigManVidTable_rsvs == 0)
				if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old = lldpV2Xdot1ConfigManVidTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->lldpV2Xdot1ConfigManVidTable_rsvs++;
		StorageTmp->lldpV2Xdot1ConfigManVidTxEnable = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->lldpV2Xdot1ConfigManVidTable_tsts == 0)
				if ((ret = check_lldpV2Xdot1ConfigManVidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigManVidTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->lldpV2Xdot1ConfigManVidTxEnable for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->lldpV2Xdot1ConfigManVidTable_sets == 0)
				if ((ret = update_lldpV2Xdot1ConfigManVidTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->lldpV2Xdot1ConfigManVidTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old) != NULL) {
			lldpV2Xdot1ConfigManVidTable_destroy(&StorageTmp->lldpV2Xdot1ConfigManVidTable_old);
			StorageTmp->lldpV2Xdot1ConfigManVidTable_rsvs = 0;
			StorageTmp->lldpV2Xdot1ConfigManVidTable_tsts = 0;
			StorageTmp->lldpV2Xdot1ConfigManVidTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->lldpV2Xdot1ConfigManVidTable_sets == 0)
			revert_lldpV2Xdot1ConfigManVidTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->lldpV2Xdot1ConfigManVidTable_old) == NULL)
			break;
		StorageTmp->lldpV2Xdot1ConfigManVidTxEnable = StorageOld->lldpV2Xdot1ConfigManVidTxEnable;
		if (--StorageTmp->lldpV2Xdot1ConfigManVidTable_rsvs == 0)
			lldpV2Xdot1ConfigManVidTable_destroy(&StorageTmp->lldpV2Xdot1ConfigManVidTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void lldpV2Xdot1MIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
lldpV2Xdot1MIB_loop_handler(int sig)
{
	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1MIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (lldpV2Xdot1MIBold_signal_handler != NULL)
		(*lldpV2Xdot1MIBold_signal_handler) (sig);
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
}

/**
 * @fn void lldpV2Xdot1MIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
lldpV2Xdot1MIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("lldpV2Xdot1MIB", "lldpV2Xdot1MIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("lldpV2Xdot1MIB", "done.\n"));
	return;
}
